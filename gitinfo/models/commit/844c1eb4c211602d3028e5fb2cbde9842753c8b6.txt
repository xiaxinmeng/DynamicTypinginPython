commit 844c1eb4c211602d3028e5fb2cbde9842753c8b6
Author: Neal Wu <neal@nealwu.com>
Date:   Wed Feb 22 18:25:22 2017 -0800

    Set tf.logging verbosity to INFO

diff --git a/resnet/resnet_main.py b/resnet/resnet_main.py
index 09db6316..f57dc94a 100644
--- a/resnet/resnet_main.py
+++ b/resnet/resnet_main.py
@@ -162,10 +162,8 @@ def evaluate(hps):
         tag='Best Precision', simple_value=best_precision)
     summary_writer.add_summary(best_precision_summ, train_step)
     summary_writer.add_summary(summaries, train_step)
-    output_str = 'loss: %.3f, precision: %.3f, best precision: %.3f\n' % \
-                 (loss, precision, best_precision)
-    tf.logging.info(output_str)
-    print(output_str)
+    tf.logging.info('loss: %.3f, precision: %.3f, best precision: %.3f' %
+                    (loss, precision, best_precision))
     summary_writer.flush()
 
     if FLAGS.eval_once:
@@ -210,4 +208,5 @@ def main(_):
 
 
 if __name__ == '__main__':
+  tf.logging.set_verbosity(tf.logging.INFO)
   tf.app.run()
diff --git a/resnet/resnet_model.py b/resnet/resnet_model.py
index f020144c..f21db8ac 100644
--- a/resnet/resnet_model.py
+++ b/resnet/resnet_model.py
@@ -221,7 +221,7 @@ class ResNet(object):
                      [(out_filter-in_filter)//2, (out_filter-in_filter)//2]])
       x += orig_x
 
-    tf.logging.info('image after unit %s', x.get_shape())
+    tf.logging.debug('image after unit %s', x.get_shape())
     return x
 
   def _bottleneck_residual(self, x, in_filter, out_filter, stride,
