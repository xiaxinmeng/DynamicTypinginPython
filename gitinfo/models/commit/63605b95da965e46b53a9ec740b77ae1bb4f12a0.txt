commit 63605b95da965e46b53a9ec740b77ae1bb4f12a0
Author: Chris Mattmann <chris.mattmann@gmail.com>
Date:   Thu Jul 18 13:14:55 2019 -0700

    Fix for #7225: CIFAR-10 eval fails with error TypeError: Input 'predictions' of 'InTopKV2' Op has type float16 that contributed by mattmann. (#7227)

diff --git a/tutorials/image/cifar10/cifar10_eval.py b/tutorials/image/cifar10/cifar10_eval.py
index 1f25b5bc..fab39d1c 100644
--- a/tutorials/image/cifar10/cifar10_eval.py
+++ b/tutorials/image/cifar10/cifar10_eval.py
@@ -123,6 +123,9 @@ def evaluate():
     # inference model.
     logits = cifar10.inference(images)
 
+    logits = tf.cast(logits, "float32")
+    labels = tf.cast(labels, "int32")
+
     # Calculate predictions.
     top_k_op = tf.nn.in_top_k(logits, labels, 1)
 
