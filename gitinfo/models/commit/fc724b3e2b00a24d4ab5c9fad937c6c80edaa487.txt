commit fc724b3e2b00a24d4ab5c9fad937c6c80edaa487
Author: A. Unique TensorFlower <gardener@tensorflow.org>
Date:   Tue Jun 2 15:35:55 2020 -0700

    A minor change to model_training_utils of BERT.
    
    PiperOrigin-RevId: 314417394

diff --git a/official/nlp/bert/model_training_utils.py b/official/nlp/bert/model_training_utils.py
index 5ed498bd..a0c15e4d 100644
--- a/official/nlp/bert/model_training_utils.py
+++ b/official/nlp/bert/model_training_utils.py
@@ -235,10 +235,9 @@ def run_customized_training_loop(
           'TPUStrategy should not run eagerly as it heavily relies on graph'
           ' optimization for the distributed system.')
 
-  if eval_input_fn and (eval_steps is None or metric_fn is None):
+  if eval_input_fn and eval_steps is None:
     raise ValueError(
-        '`eval_step` and `metric_fn` are required when `eval_input_fn ` '
-        'is not none.')
+        '`eval_step` is required when `eval_input_fn ` is not none.')
   if metric_fn and not callable(metric_fn):
     raise ValueError(
         'if `metric_fn` is specified, metric_fn must be a callable.')
