commit 32fadf008318b669c33947f55d41aa4a6f0a6ed9
Author: Hongkun Yu <saberkun@users.noreply.github.com>
Date:   Thu Jul 18 19:47:48 2019 -0700

    Merged commit includes the following changes: (#7255)
    
    258881002  by hongkuny<hongkuny@google.com>:
    
        Fix lint.
    
    --
    258874998  by hongkuny<hongkuny@google.com>:
    
        Internal
    
    --
    258872662  by hongkuny<hongkuny@google.com>:
    
        Fix doc
    
    --
    258871624  by hongkuny<hongkuny@google.com>:
    
        Internal change
    
    PiperOrigin-RevId: 258881002

diff --git a/official/bert/optimization.py b/official/bert/optimization.py
index 6686f10b..22cc8ae6 100644
--- a/official/bert/optimization.py
+++ b/official/bert/optimization.py
@@ -135,6 +135,7 @@ class AdamWeightDecay(tf.keras.optimizers.Adam):
     return super(AdamWeightDecay, self).apply_gradients(zip(grads, tvars))
 
   def _get_lr(self, var_device, var_dtype, apply_state):
+    """Retrieves the learning rate with the given state."""
     if apply_state is None:
       return self._decayed_lr_t[var_dtype], {}
 
diff --git a/official/staging/shakespeare/README.md b/official/staging/shakespeare/README.md
index ee11f4b1..5395cc96 100644
--- a/official/staging/shakespeare/README.md
+++ b/official/staging/shakespeare/README.md
@@ -14,18 +14,18 @@ Note that files other than shakepeare.txt can also be used to train the model to
 
 Then train the model:
 
-```
+```python
 python3 shakespeare_main.py --training_data shakespeare.txt \
     --model_dir /tmp/shakespeare
-````
+```
 
 This will place model checkpoints in `/tmp/shakespeare`, so that we can use them to make predictions.
 
 Then generate predictions:
 
-```    
+```python
 python3 shakespeare_main.py --training_data shakespeare.txt \
     --model_dir /tmp/shakespeare --notrain --predict_context=ROMEO:
 ```
 
-Change `--predict_context` and `--predict_length` to suit your needs.
\ No newline at end of file
+Change `--predict_context` and `--predict_length` to suit your needs.
