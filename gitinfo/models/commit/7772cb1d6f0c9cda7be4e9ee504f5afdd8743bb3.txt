commit 7772cb1d6f0c9cda7be4e9ee504f5afdd8743bb3
Author: Toby Boyd <tobyboyd@google.com>
Date:   Mon Apr 22 13:41:37 2019 -0700

    Use tf.image.resize_with_crop_or_pad (#6632)

diff --git a/official/resnet/cifar10_main.py b/official/resnet/cifar10_main.py
index b32fa91c..2153f24a 100644
--- a/official/resnet/cifar10_main.py
+++ b/official/resnet/cifar10_main.py
@@ -93,7 +93,7 @@ def preprocess_image(image, is_training):
   """Preprocess a single image of layout [height, width, depth]."""
   if is_training:
     # Resize the image to add four extra pixels on each side.
-    image = tf.image.resize_image_with_crop_or_pad(
+    image = tf.image.resize_with_crop_or_pad(
         image, HEIGHT + 8, WIDTH + 8)
 
     # Randomly crop a [HEIGHT, WIDTH] section of the image.
