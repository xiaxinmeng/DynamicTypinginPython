commit 1c70448772d6f669d2cdaaad80b872bdbc68da1c
Author: James Pruegsanusak <korrawat@mit.edu>
Date:   Thu Jul 6 14:50:12 2017 +0800

    Change split name in slim's export_inference_graph

diff --git a/slim/export_inference_graph.py b/slim/export_inference_graph.py
index 13f10ce0..cc3210df 100644
--- a/slim/export_inference_graph.py
+++ b/slim/export_inference_graph.py
@@ -100,7 +100,7 @@ def main(_):
     raise ValueError('You must supply the path to save to with --output_file')
   tf.logging.set_verbosity(tf.logging.INFO)
   with tf.Graph().as_default() as graph:
-    dataset = dataset_factory.get_dataset(FLAGS.dataset_name, 'validation',
+    dataset = dataset_factory.get_dataset(FLAGS.dataset_name, 'train',
                                           FLAGS.dataset_dir)
     network_fn = nets_factory.get_network_fn(
         FLAGS.model_name,
