commit cfe945eff31dd25311b8b767ab6b6de8a5065893
Author: Asim Shankar <ashankar@google.com>
Date:   Tue Apr 10 18:14:56 2018 -0700

    official/mnist: Address comment in PR

diff --git a/official/mnist/mnist.py b/official/mnist/mnist.py
index e5beba9e..dc3aa375 100644
--- a/official/mnist/mnist.py
+++ b/official/mnist/mnist.py
@@ -58,6 +58,8 @@ def create_model(data_format):
   l = tf.keras.layers
   max_pool = l.MaxPooling2D(
       (2, 2), (2, 2), padding='same', data_format=data_format)
+  # The model consists of a sequential chain of layers, so tf.keras.Sequential
+  # (a subclass of tf.keras.Model) makes for a compact description.
   return tf.keras.Sequential(
       [
           l.Reshape(input_shape),
