commit 646c3f75abf152c55beedfae5693079af0c738b3
Author: Hanqing Sun <DefineFC@users.noreply.github.com>
Date:   Tue Mar 20 00:50:43 2018 +0800

    fix doc of func `learning_rate_with_decay` (#3634)

diff --git a/official/resnet/resnet_run_loop.py b/official/resnet/resnet_run_loop.py
index 2ed5061e..e2877c76 100644
--- a/official/resnet/resnet_run_loop.py
+++ b/official/resnet/resnet_run_loop.py
@@ -145,8 +145,8 @@ def learning_rate_with_decay(
     boundary_epochs: list of ints representing the epochs at which we
       decay the learning rate.
     decay_rates: list of floats representing the decay rates to be used
-      for scaling the learning rate. Should be the same length as
-      boundary_epochs.
+      for scaling the learning rate. It should have one more element
+      than `boundary_epochs`, and all elements should have the same type.
 
   Returns:
     Returns a function that takes a single argument - the number of batches
