commit dec7c89f5b6777b943ed24504b6f0d9ea0754c4c
Author: Joshua Howard <joshthoward@gmail.com>
Date:   Fri Mar 24 20:20:13 2017 -0400

    Variational Autoencoder generate() function fixed (z fed in rather than z_mean)

diff --git a/autoencoder/autoencoder_models/VariationalAutoencoder.py b/autoencoder/autoencoder_models/VariationalAutoencoder.py
index fad2cfb6..8767891a 100644
--- a/autoencoder/autoencoder_models/VariationalAutoencoder.py
+++ b/autoencoder/autoencoder_models/VariationalAutoencoder.py
@@ -1,5 +1,4 @@
 import tensorflow as tf
-import numpy as np
 
 class VariationalAutoencoder(object):
 
@@ -57,8 +56,8 @@ class VariationalAutoencoder(object):
 
     def generate(self, hidden = None):
         if hidden is None:
-            hidden = np.random.normal(size=self.weights["b1"])
-        return self.sess.run(self.reconstruction, feed_dict={self.z_mean: hidden})
+            hidden = self.sess.run(tf.random_normal([1, self.n_hidden]))
+        return self.sess.run(self.reconstruction, feed_dict={self.z: hidden})
 
     def reconstruct(self, X):
         return self.sess.run(self.reconstruction, feed_dict={self.x: X})
