commit d9ae2108122bf53bf7abc907d4a9bc29d2fac053
Author: Hongkun Yu <hongkuny@google.com>
Date:   Wed Mar 4 10:22:05 2020 -0800

    Delete docker files as they are old and not verified.
    
    PiperOrigin-RevId: 298881095

diff --git a/official/Dockerfile.cpu b/official/Dockerfile.cpu
deleted file mode 100644
index e40755c5..00000000
--- a/official/Dockerfile.cpu
+++ /dev/null
@@ -1,17 +0,0 @@
-# Docker image for running examples in Tensorflow models.
-# base_image depends on whether we are running on GPUs or non-GPUs
-FROM ubuntu:latest
-
-RUN apt-get update && apt-get install -y --no-install-recommends \
-    ca-certificates \
-    build-essential \
-    git \
-    python \
-    python-pip \
-    python-setuptools
-
-RUN pip install tf-nightly
-
-# Checkout tensorflow/models at HEAD
-RUN git clone https://github.com/tensorflow/models.git /tensorflow_models
-
diff --git a/official/Dockerfile.gpu b/official/Dockerfile.gpu
deleted file mode 100644
index 012b49cb..00000000
--- a/official/Dockerfile.gpu
+++ /dev/null
@@ -1,18 +0,0 @@
-# Docker image for running examples in Tensorflow models.
-# base_image depends on whether we are running on GPUs or non-GPUs
-FROM nvidia/cuda:9.0-cudnn7-runtime-ubuntu16.04
-
-RUN apt-get update && apt-get install -y --no-install-recommends \
-    ca-certificates \
-    build-essential \
-    git \
-    python \
-    python-pip \
-    python-setuptools
-
-RUN pip install tf-nightly-gpu
-
-# Checkout tensorflow/models at HEAD
-RUN git clone https://github.com/tensorflow/models.git /tensorflow_models
-
-
