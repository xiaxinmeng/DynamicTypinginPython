commit 6d0722dc021f38a6d680a9259972aeb02fd83848
Author: Yusuke HIDESHIMA <yusuke.hideshima@uei.co.jp>
Date:   Thu Jul 21 13:14:41 2016 +0900

    "threads" declared twice, so delete one

diff --git a/inception/inception/data/build_image_data.py b/inception/inception/data/build_image_data.py
index 6d943cfb..66941dfd 100644
--- a/inception/inception/data/build_image_data.py
+++ b/inception/inception/data/build_image_data.py
@@ -298,7 +298,6 @@ def _process_image_files(name, filenames, texts, labels, num_shards):
   # Break all images into batches with a [ranges[i][0], ranges[i][1]].
   spacing = np.linspace(0, len(filenames), FLAGS.num_threads + 1).astype(np.int)
   ranges = []
-  threads = []
   for i in xrange(len(spacing) - 1):
     ranges.append([spacing[i], spacing[i+1]])
 
