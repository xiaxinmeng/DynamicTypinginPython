commit 9e15020a82c2a2a5f4476a8410b82cec9780240f
Author: Mark Daoust <markdaoust@google.com>
Date:   Tue Jan 9 12:10:20 2018 -0500

    return dataset for r1.5

diff --git a/samples/core/get_started/iris_data.py b/samples/core/get_started/iris_data.py
index c9223b9f..71c0be24 100644
--- a/samples/core/get_started/iris_data.py
+++ b/samples/core/get_started/iris_data.py
@@ -35,8 +35,8 @@ def train_input_fn(features, labels, batch_size):
     # Shuffle, repeat, and batch the examples.
     dataset = dataset.shuffle(1000).repeat().batch(batch_size)
 
-    # Return the read end of the pipeline.
-    return dataset.make_one_shot_iterator().get_next()
+    # Return the dataset.
+    return dataset
 
 
 def eval_input_fn(features, labels, batch_size):
@@ -55,8 +55,8 @@ def eval_input_fn(features, labels, batch_size):
     assert batch_size is not None, "batch_size must not be None"
     dataset = dataset.batch(batch_size)
 
-    # Return the read end of the pipeline.
-    return dataset.make_one_shot_iterator().get_next()
+    # Return the dataset.
+    return dataset
 
 
 # The remainder of this file contains a simple example of a csv parser,
@@ -89,5 +89,5 @@ def csv_input_fn(csv_path, batch_size):
     # Shuffle, repeat, and batch the examples.
     dataset = dataset.shuffle(1000).repeat().batch(batch_size)
 
-    # Return the read end of the pipeline.
-    return dataset.make_one_shot_iterator().get_next()
\ No newline at end of file
+    # Return the dataset.
+    return dataset
