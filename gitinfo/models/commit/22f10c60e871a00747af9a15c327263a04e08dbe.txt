commit 22f10c60e871a00747af9a15c327263a04e08dbe
Author: Chen Chen <chendouble@google.com>
Date:   Wed Apr 22 15:30:01 2020 -0700

    Add model metrics to the training summary.
    
    PiperOrigin-RevId: 307914609

diff --git a/official/nlp/bert/model_training_utils.py b/official/nlp/bert/model_training_utils.py
index 56cd3703..c479bfe2 100644
--- a/official/nlp/bert/model_training_utils.py
+++ b/official/nlp/bert/model_training_utils.py
@@ -491,6 +491,8 @@ def run_customized_training_loop(
         'total_training_steps': total_training_steps,
         'train_loss': _float_metric_value(train_loss_metric),
     }
+    for metric in model.metrics:
+      training_summary[metric.name] = _float_metric_value(metric)
     if eval_metrics:
       # TODO(hongkuny): Cleans up summary reporting in text.
       training_summary['last_train_metrics'] = _float_metric_value(
