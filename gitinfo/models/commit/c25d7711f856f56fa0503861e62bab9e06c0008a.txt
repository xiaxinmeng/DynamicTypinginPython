commit c25d7711f856f56fa0503861e62bab9e06c0008a
Author: Hongkun Yu <hongkuny@google.com>
Date:   Sun Feb 23 11:20:55 2020 -0800

    tf.keras.backend.sparse_categorical_crossentropy -> tf.keras.losses.sparse_categorical_crossentropy
    
    PiperOrigin-RevId: 296755118

diff --git a/official/nlp/bert/run_squad.py b/official/nlp/bert/run_squad.py
index 1c85c5f6..2db740b5 100644
--- a/official/nlp/bert/run_squad.py
+++ b/official/nlp/bert/run_squad.py
@@ -109,9 +109,9 @@ def squad_loss_fn(start_positions,
                   end_logits,
                   loss_factor=1.0):
   """Returns sparse categorical crossentropy for start/end logits."""
-  start_loss = tf.keras.backend.sparse_categorical_crossentropy(
+  start_loss = tf.keras.losses.sparse_categorical_crossentropy(
       start_positions, start_logits, from_logits=True)
-  end_loss = tf.keras.backend.sparse_categorical_crossentropy(
+  end_loss = tf.keras.losses.sparse_categorical_crossentropy(
       end_positions, end_logits, from_logits=True)
 
   total_loss = (tf.reduce_mean(start_loss) + tf.reduce_mean(end_loss)) / 2
