commit 4215b4890ca08e5c813e8de7384e0aa0c63b2647
Author: Alan Yee <alyee@ucsd.edu>
Date:   Mon Aug 21 11:59:01 2017 -0700

    Update AutoencoderRunner.py
    
    Remove spacing according to PEP 8

diff --git a/autoencoder/AutoencoderRunner.py b/autoencoder/AutoencoderRunner.py
index f6933f2f..f06aa0cd 100644
--- a/autoencoder/AutoencoderRunner.py
+++ b/autoencoder/AutoencoderRunner.py
@@ -9,7 +9,7 @@ from tensorflow.examples.tutorials.mnist import input_data
 
 from autoencoder_models.Autoencoder import Autoencoder
 
-mnist = input_data.read_data_sets('MNIST_data', one_hot = True)
+mnist = input_data.read_data_sets('MNIST_data', one_hot=True)
 
 
 def standard_scale(X_train, X_test):
@@ -32,10 +32,10 @@ batch_size = 128
 display_step = 1
 
 autoencoder = Autoencoder(
-    n_input = 784,
-    n_hidden = 200,
-    transfer_function = tf.nn.softplus,
-    optimizer = tf.train.AdamOptimizer(learning_rate = 0.001))
+    n_input=784,
+    n_hidden=200,
+    transfer_function=tf.nn.softplus,
+    optimizer=tf.train.AdamOptimizer(learning_rate=0.001))
 
 for epoch in range(training_epochs):
     avg_cost = 0.
