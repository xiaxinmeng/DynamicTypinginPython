commit 8f16def11bd9ef4b874fbd86bb0f2792abbb07a0
Author: Byeongjoo Ahn <ByeongjooAhn@users.noreply.github.com>
Date:   Mon Jun 26 18:25:42 2017 +0900

    Fix compatibility for Python3
    
    iteritems() -> items()

diff --git a/object_detection/utils/variables_helper.py b/object_detection/utils/variables_helper.py
index 1e091a14..b27f814f 100644
--- a/object_detection/utils/variables_helper.py
+++ b/object_detection/utils/variables_helper.py
@@ -122,7 +122,7 @@ def get_variables_available_in_checkpoint(variables, checkpoint_path):
   ckpt_reader = tf.train.NewCheckpointReader(checkpoint_path)
   ckpt_vars = ckpt_reader.get_variable_to_shape_map().keys()
   vars_in_ckpt = {}
-  for variable_name, variable in sorted(variable_names_map.iteritems()):
+  for variable_name, variable in sorted(variable_names_map.items()):
     if variable_name in ckpt_vars:
       vars_in_ckpt[variable_name] = variable
     else:
