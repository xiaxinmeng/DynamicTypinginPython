commit 45261f52975f2da11e89dc172733fee16465cf86
Author: Hongkun Yu <hongkuny@google.com>
Date:   Thu Sep 5 23:45:49 2019 -0700

    Update NCF readme.
    
    PiperOrigin-RevId: 267535628

diff --git a/official/recommendation/README.md b/official/recommendation/README.md
index 28b699fa..cc83ed21 100644
--- a/official/recommendation/README.md
+++ b/official/recommendation/README.md
@@ -55,9 +55,13 @@ Note the ml-20m dataset is large (the rating file is ~500 MB), and it may take s
 Both the ml-1m and ml-20m datasets will be coerced into a common format when downloaded.
 
 ### Train and evaluate model
+
+[ncf_keras_main.py](ncf_keras_main.py) is the Keras trainer that supports
+features in TF 2.x. Users can train the model on both GPU and TPU.
+
 To train and evaluate the model, issue the following command:
 ```
-python ncf_main.py
+python ncf_keras_main.py
 ```
 Arguments:
   * `--model_dir`: Directory to save model training checkpoints. By default, it is `/tmp/ncf/`.
@@ -65,7 +69,3 @@ Arguments:
   * `--dataset`: The dataset name to be downloaded and preprocessed. By default, it is `ml-1m`.
 
 There are other arguments about models and training process. Use the `--help` or `-h` flag to get a full list of possible arguments with detailed descriptions.
-
-## Benchmarks (TODO)
-### Training times
-### Evaluation results
diff --git a/official/recommendation/run_keras.sh b/official/recommendation/run_keras.sh
deleted file mode 100755
index 3b185910..00000000
--- a/official/recommendation/run_keras.sh
+++ /dev/null
@@ -1,3 +0,0 @@
-#!/bin/bash
-set -e
-./run.sh keras
