commit d9f6b6f3707bbcb9b287759de58abe2172c88029
Author: Manoj Plakal <30733558+plakal@users.noreply.github.com>
Date:   Fri Feb 9 15:53:38 2018 -0500

    Explicitly specify checkpoint version.
    
    
    TF Saver now requires specifying the checkpoint version even when restoring to avoid errors when we specify
    a path to a checkpoint file instead of a directory.

diff --git a/research/audioset/vggish_slim.py b/research/audioset/vggish_slim.py
index 5f9e0b12..99885b45 100644
--- a/research/audioset/vggish_slim.py
+++ b/research/audioset/vggish_slim.py
@@ -124,5 +124,6 @@ def load_vggish_slim_checkpoint(session, checkpoint_path):
   vggish_vars = [v for v in tf.global_variables() if v.name in vggish_var_names]
 
   # Use a Saver to restore just the variables selected above.
-  saver = tf.train.Saver(vggish_vars, name='vggish_load_pretrained')
+  saver = tf.train.Saver(vggish_vars, name='vggish_load_pretrained',
+                         write_version=1)
   saver.restore(session, checkpoint_path)
