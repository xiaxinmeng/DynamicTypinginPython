commit 5691cc080b253af6ce3f635481cd60c63769e26c
Author: Alan Yee <alyee@ucsd.edu>
Date:   Fri Aug 18 14:52:59 2017 -0700

    Fix print statement formatting

diff --git a/autoencoder/AutoencoderRunner.py b/autoencoder/AutoencoderRunner.py
index 6d3808fa..f6933f2f 100644
--- a/autoencoder/AutoencoderRunner.py
+++ b/autoencoder/AutoencoderRunner.py
@@ -51,7 +51,7 @@ for epoch in range(training_epochs):
 
     # Display logs per epoch step
     if epoch % display_step == 0:
-        print("Epoch: ", '%d,' % (epoch + 1),
-              "Cost: ", "{:.9f}".format(avg_cost))
-        
+        print("Epoch:", '%d,' % (epoch + 1),
+              "Cost:", "{:.9f}".format(avg_cost))
+
 print("Total cost: " + str(autoencoder.calc_total_cost(X_test)))
