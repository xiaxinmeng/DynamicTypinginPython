commit 1b2c67af8a035fa90dc1dc507cdd101df3f5a589
Author: Duc Nguyen <justanhduc@users.noreply.github.com>
Date:   Sat Jun 17 10:14:50 2017 +0900

    Fix compatibility for model_builder_test.py (#1571)
    
    iteritems -> items

diff --git a/object_detection/builders/model_builder_test.py b/object_detection/builders/model_builder_test.py
index 513c5fab..28b15e65 100644
--- a/object_detection/builders/model_builder_test.py
+++ b/object_detection/builders/model_builder_test.py
@@ -255,7 +255,7 @@ class ModelBuilderTest(tf.test.TestCase):
       }"""
     model_proto = model_pb2.DetectionModel()
     text_format.Merge(model_text_proto, model_proto)
-    for extractor_type, extractor_class in FEATURE_EXTRACTOR_MAPS.iteritems():
+    for extractor_type, extractor_class in FEATURE_EXTRACTOR_MAPS.items():
       model_proto.faster_rcnn.feature_extractor.type = extractor_type
       model = model_builder.build(model_proto, is_training=True)
       self.assertIsInstance(model, faster_rcnn_meta_arch.FasterRCNNMetaArch)
@@ -445,7 +445,7 @@ class ModelBuilderTest(tf.test.TestCase):
       }"""
     model_proto = model_pb2.DetectionModel()
     text_format.Merge(model_text_proto, model_proto)
-    for extractor_type, extractor_class in FEATURE_EXTRACTOR_MAPS.iteritems():
+    for extractor_type, extractor_class in FEATURE_EXTRACTOR_MAPS.items():
       model_proto.faster_rcnn.feature_extractor.type = extractor_type
       model = model_builder.build(model_proto, is_training=True)
       self.assertIsInstance(model, rfcn_meta_arch.RFCNMetaArch)
