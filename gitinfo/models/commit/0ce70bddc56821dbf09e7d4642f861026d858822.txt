commit 0ce70bddc56821dbf09e7d4642f861026d858822
Author: None <toxic.xc@sjtu.edu.cn>
Date:   Fri Jun 2 15:58:37 2017 +0800

    Update resnet_model.py
    
    'elipson' should be epsilon

diff --git a/resnet/resnet_model.py b/resnet/resnet_model.py
index a8b7f10c..2be68a13 100644
--- a/resnet/resnet_model.py
+++ b/resnet/resnet_model.py
@@ -185,7 +185,7 @@ class ResNet(object):
             trainable=False)
         tf.summary.histogram(mean.op.name, mean)
         tf.summary.histogram(variance.op.name, variance)
-      # elipson used to be 1e-5. Maybe 0.001 solves NaN problem in deeper net.
+      # epsilon used to be 1e-5. Maybe 0.001 solves NaN problem in deeper net.
       y = tf.nn.batch_normalization(
           x, mean, variance, beta, gamma, 0.001)
       y.set_shape(x.get_shape())
