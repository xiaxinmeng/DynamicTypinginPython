commit 4c7c8fa794760658bdd95913928b6a7322b37b38
Author: Karmel Allison <karmel@arcaio.com>
Date:   Tue Mar 13 08:56:42 2018 -0700

    Update Dockerfile for GPU now that the branch has been merged (#3576)
    
    * Remove cd and checkout
    
    * Updating comment

diff --git a/official/Dockerfile.cpu b/official/Dockerfile.cpu
index aeced17a..e40755c5 100644
--- a/official/Dockerfile.cpu
+++ b/official/Dockerfile.cpu
@@ -12,6 +12,6 @@ RUN apt-get update && apt-get install -y --no-install-recommends \
 
 RUN pip install tf-nightly
 
-# Checkout tensorflow/models and pin to a particular version
+# Checkout tensorflow/models at HEAD
 RUN git clone https://github.com/tensorflow/models.git /tensorflow_models
 
diff --git a/official/Dockerfile.gpu b/official/Dockerfile.gpu
index e930db3c..012b49cb 100644
--- a/official/Dockerfile.gpu
+++ b/official/Dockerfile.gpu
@@ -12,7 +12,7 @@ RUN apt-get update && apt-get install -y --no-install-recommends \
 
 RUN pip install tf-nightly-gpu
 
-# Checkout tensorflow/models and pin to a particular version
-RUN git clone https://github.com/tensorflow/models.git /tensorflow_models && \
-    cd /tensorflow_models && git checkout feat/multi-gpu-resnet
+# Checkout tensorflow/models at HEAD
+RUN git clone https://github.com/tensorflow/models.git /tensorflow_models
+
 
