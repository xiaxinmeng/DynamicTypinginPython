commit b3158fb0183809400e9e7f8092dd541201b1c4d4
Author: Tayo Oguntebi <10927929+tayo@users.noreply.github.com>
Date:   Tue Jan 29 16:10:47 2019 -0800

    Explicitly allow for script execution from any directory.  Make env vars visible in python script. (#6105)

diff --git a/official/recommendation/run.sh b/official/recommendation/run.sh
index 035e6197..56acf0ff 100755
--- a/official/recommendation/run.sh
+++ b/official/recommendation/run.sh
@@ -6,6 +6,9 @@ if [ `id -u` != 0 ]; then
   sudo echo "Success"
 fi
 
+SCRIPT_DIR=`dirname "$BASH_SOURCE"`
+export PYTHONPATH="${SCRIPT_DIR}/../../"
+
 DATASET="ml-20m"
 
 BUCKET=${BUCKET:-""}
@@ -33,7 +36,7 @@ else
 fi
 
 DATA_DIR="${ROOT_DIR}/movielens_data"
-python ../datasets/movielens.py --data_dir ${DATA_DIR} --dataset ${DATASET}
+python "${SCRIPT_DIR}/../datasets/movielens.py" --data_dir ${DATA_DIR} --dataset ${DATASET}
 
 {
 
@@ -55,7 +58,7 @@ do
   # To reduce variation set the seed flag:
   #   --seed ${i}
 
-  python -u ncf_main.py \
+  python -u "${SCRIPT_DIR}/ncf_main.py" \
       --model_dir ${MODEL_DIR} \
       --data_dir ${DATA_DIR} \
       --dataset ${DATASET} --hooks "" \
diff --git a/official/recommendation/run_tpu.sh b/official/recommendation/run_tpu.sh
index 14849ccf..bcbb1929 100755
--- a/official/recommendation/run_tpu.sh
+++ b/official/recommendation/run_tpu.sh
@@ -2,8 +2,8 @@
 set -e
 
 # Example settings:
-# TPU="taylorrobie-tpu-0"
-# BUCKET="gs://taylorrobie-tpu-test-bucket-2"
+# export TPU="taylorrobie-tpu-0"
+# export BUCKET="gs://taylorrobie-tpu-test-bucket-2"
 
 # Remove IDE "not assigned" warning highlights.
 TPU=${TPU:-""}
