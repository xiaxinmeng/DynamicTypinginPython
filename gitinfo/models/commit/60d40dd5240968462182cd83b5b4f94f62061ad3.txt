commit 60d40dd5240968462182cd83b5b4f94f62061ad3
Author: Spencer Stirling <sstirling@uber.com>
Date:   Mon Apr 17 16:00:06 2017 -0600

    added OSX build instructions

diff --git a/street/README.md b/street/README.md
index 64ab6b65..199262ed 100644
--- a/street/README.md
+++ b/street/README.md
@@ -48,11 +48,18 @@ sudo pip install numpy
 
 Build the LSTM op:
 
+Linux
 ```
 cd cc
 TF_INC=$(python -c 'import tensorflow as tf; print(tf.sysconfig.get_include())')
 g++ -std=c++11 -shared rnn_ops.cc -o rnn_ops.so -fPIC -I $TF_INC -O3 -mavx
 ```
+Mac
+```
+cd cc
+TF_INC=$(python -c 'import tensorflow as tf; print(tf.sysconfig.get_include())')
+g++ -std=c++11 -shared rnn_ops.cc -o rnn_ops.so -fPIC -I $TF_INC -O3 -mavx -undefined dynamic_lookup
+```
 
 Run the unittests:
 
