commit 289b439308be829db258d5b1bb33a6de03769e57
Author: Neal Wu <neal@nealwu.com>
Date:   Mon Mar 13 19:10:59 2017 -0700

    Use the old clone loss name instead

diff --git a/slim/deployment/model_deploy.py b/slim/deployment/model_deploy.py
index 3b45a573..4888d08a 100644
--- a/slim/deployment/model_deploy.py
+++ b/slim/deployment/model_deploy.py
@@ -232,7 +232,7 @@ def _gather_clone_loss(clone, num_clones, regularization_losses):
       sum_loss = tf.add_n(all_losses)
   # Add the summaries out of the clone device block.
   if clone_loss is not None:
-    tf.summary.scalar('clone_loss', clone_loss)
+    tf.summary.scalar(clone.scope + '/clone_loss', clone_loss)
   if regularization_loss is not None:
     tf.summary.scalar('regularization_loss', regularization_loss)
   return sum_loss
