commit a2b595d25553101e5bc942be1b65f4228c66f2b1
Author: torzdf <36920800+torzdf@users.noreply.github.com>
Date:   Wed Feb 13 01:36:24 2019 +0000

    Basic icon for GUI

diff --git a/lib/gui/.cache/icons/logo.png b/lib/gui/.cache/icons/logo.png
new file mode 100755
index 0000000..c1b6581
Binary files /dev/null and b/lib/gui/.cache/icons/logo.png differ
diff --git a/lib/gui/display_analysis.py b/lib/gui/display_analysis.py
index 8d02c45..9e5584e 100644
--- a/lib/gui/display_analysis.py
+++ b/lib/gui/display_analysis.py
@@ -283,6 +283,7 @@ class StatsData(ttk.Frame):  # pylint: disable=too-many-ancestors
                                 self.session.modelname,
                                 self.selected_id.get())
         toplevel.title(self.data_popup_title())
+        toplevel.tk.call('wm', 'iconphoto', toplevel._w, get_images().icons["favicon"])
         position = self.data_popup_get_position()
         height = int(720 * scaling_factor)
         width = int(400 * scaling_factor)
diff --git a/lib/gui/popup_configure.py b/lib/gui/popup_configure.py
index c2331f8..9e25923 100644
--- a/lib/gui/popup_configure.py
+++ b/lib/gui/popup_configure.py
@@ -8,7 +8,7 @@ import tkinter as tk
 from tkinter import ttk
 
 from .tooltip import Tooltip
-from .utils import get_config, ContextMenu, set_slider_rounding
+from .utils import get_config, get_images, ContextMenu, set_slider_rounding
 
 logger = logging.getLogger(__name__)  # pylint: disable=invalid-name
 POPUP = dict()
@@ -32,6 +32,8 @@ class ConfigurePlugins(tk.Toplevel):
         super().__init__()
         name, self.config = config
         self.title("{} Plugins".format(name.title()))
+        self.tk.call('wm', 'iconphoto', self._w, get_images().icons["favicon"])
+
         self.set_geometry(root)
 
         self.page_frame = ttk.Frame(self)
diff --git a/lib/gui/utils.py b/lib/gui/utils.py
index 325ecb6..5632dee 100644
--- a/lib/gui/utils.py
+++ b/lib/gui/utils.py
@@ -217,6 +217,7 @@ class Images():
         self.icons["graph"] = ImageTk.PhotoImage(file=os.path.join(self.pathicons, "graph.png"))
         self.icons["zoom"] = ImageTk.PhotoImage(file=os.path.join(self.pathicons, "zoom.png"))
         self.icons["move"] = ImageTk.PhotoImage(file=os.path.join(self.pathicons, "move.png"))
+        self.icons["favicon"] = ImageTk.PhotoImage(file=os.path.join(self.pathicons, "logo.png"))
         logger.debug("Initialized %s: (icons: %s)", self.__class__.__name__, self.icons)
 
     def delete_preview(self):
diff --git a/scripts/gui.py b/scripts/gui.py
index c4110f1..da2b815 100644
--- a/scripts/gui.py
+++ b/scripts/gui.py
@@ -59,6 +59,7 @@ class FaceswapGui(tk.Tk):
         """ Build the GUI """
         logger.debug("Building GUI")
         self.title("Faceswap.py")
+        self.tk.call('wm', 'iconphoto', self._w, get_images().icons["favicon"])
         self.configure(menu=MainMenuBar(self))
 
         topcontainer, bottomcontainer = self.add_containers()
