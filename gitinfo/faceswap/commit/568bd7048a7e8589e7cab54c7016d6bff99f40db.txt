commit 568bd7048a7e8589e7cab54c7016d6bff99f40db
Author: Artem Ivanov <37909402+andenixa@users.noreply.github.com>
Date:   Sun Jul 7 14:19:08 2019 +0300

    RealFace Plugin: Encoder complexity fix (#782)
    
    - fixed a bug that prevented altering default value of `complexity_encoder`

diff --git a/plugins/train/model/realface.py b/plugins/train/model/realface.py
index d6f208a..64951ed 100644
--- a/plugins/train/model/realface.py
+++ b/plugins/train/model/realface.py
@@ -110,8 +110,9 @@ class Model(ModelBase):
 
     def decoder_b(self):
         """ RealFace Decoder Network """
+        input_filters = self.config['complexity_encoder'] * 2**(self.downscalers_no-1)
         input_width = self.config['input_size'] // self._downscale_ratio
-        input_ = Input(shape=(input_width, input_width, 1024))
+        input_ = Input(shape=(input_width, input_width, input_filters))
 
         var_xy = input_
 
@@ -149,8 +150,9 @@ class Model(ModelBase):
 
     def decoder_a(self):
         """ RealFace Decoder (A) Network """
+        input_filters = self.config['complexity_encoder'] * 2**(self.downscalers_no-1)
         input_width = self.config['input_size'] // self._downscale_ratio
-        input_ = Input(shape=(input_width, input_width, 1024))
+        input_ = Input(shape=(input_width, input_width, input_filters))
 
         var_xy = input_
 
