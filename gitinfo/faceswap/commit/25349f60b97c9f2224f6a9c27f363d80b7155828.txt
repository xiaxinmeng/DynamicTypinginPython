commit 25349f60b97c9f2224f6a9c27f363d80b7155828
Author: torzdf <36920800+torzdf@users.noreply.github.com>
Date:   Thu Dec 27 12:14:25 2018 +0000

    bugfix

diff --git a/lib/gpu_stats.py b/lib/gpu_stats.py
index 0ef5f1c..a0ecbc1 100644
--- a/lib/gpu_stats.py
+++ b/lib/gpu_stats.py
@@ -33,13 +33,12 @@ class GPUStats():
 
         self.driver = self.get_driver()
         self.devices = self.get_devices()
+        self.vram = self.get_vram()
         if self.device_count == 0:
             if self.logger:
                 self.logger.warning("No GPU detected. Switching to CPU mode")
             return
 
-        self.vram = self.get_vram()
-
         self.shutdown()
         if self.logger:
             self.logger.debug("Initialized %s", self.__class__.__name__)
