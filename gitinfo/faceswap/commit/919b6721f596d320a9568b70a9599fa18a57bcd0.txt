commit 919b6721f596d320a9568b70a9599fa18a57bcd0
Author: JPFrancoia <jeanpatrick.francoia@gmail.com>
Date:   Thu May 30 15:32:56 2019 +0100

    Bugfix: save interval on extract (#740)
    
    bug in extract.py: because of missing parentheses in a modulo operation, a condition on save-interval was never met. alignements.json
    file was never generated when starting extract script with -si option.

diff --git a/scripts/extract.py b/scripts/extract.py
index 4f81ef7..4ca7e32 100644
--- a/scripts/extract.py
+++ b/scripts/extract.py
@@ -191,7 +191,7 @@ class Extract():
                 if self.extractor.final_pass:
                     self.output_processing(faces, align_eyes, size, filename)
                     self.output_faces(filename, faces)
-                    if self.save_interval and idx + 1 % self.save_interval == 0:
+                    if self.save_interval and (idx + 1) % self.save_interval == 0:
                         self.alignments.save()
                 else:
                     del faces["image"]
