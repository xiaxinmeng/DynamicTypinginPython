commit a8eefc93cb816dfe41cb53dafbde5a4d4bef7d51
Author: Andy (Dong Young) Kim <andykdy@hotmail.com>
Date:   Thu Apr 5 16:51:45 2018 -0700

    change to flow with abrupt stop.
    
    Change to control flow when training is abruptly stopped.

diff --git a/scripts/train.py b/scripts/train.py
index 7b4e9c5..d2d5ce4 100644
--- a/scripts/train.py
+++ b/scripts/train.py
@@ -207,12 +207,12 @@ class TrainingProcessor(object):
                     model.save_weights()
 
                 if self.stop:
-                    model.save_weights()
-                    exit()
+                    break
 
                 if self.save_now:
                     model.save_weights()
                     self.save_now = False
+                    
             model.save_weights()
             print('Training complete\nModel has been saved')
             exit(0)
