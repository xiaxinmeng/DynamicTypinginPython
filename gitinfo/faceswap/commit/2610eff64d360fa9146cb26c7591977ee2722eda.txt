commit 2610eff64d360fa9146cb26c7591977ee2722eda
Author: torzdf <36920800+torzdf@users.noreply.github.com>
Date:   Fri Jul 26 16:39:24 2019 +0000

    Bugfix: GUI: Progress bar on times over 1 hour (extract/convert)

diff --git a/lib/gui/wrapper.py b/lib/gui/wrapper.py
index 4c6f87e..14417ea 100644
--- a/lib/gui/wrapper.py
+++ b/lib/gui/wrapper.py
@@ -150,7 +150,7 @@ class FaceswapControl():
         self.consoleregex = {
             "loss": re.compile(r"[\W]+(\d+)?[\W]+([a-zA-Z\s]*)[\W]+?(\d+\.\d+)"),
             "tqdm": re.compile(r"(?P<dsc>.*?)(?P<pct>\d+%).*?(?P<itm>\S+/\S+)\W\["
-                               r"(?P<tme>\d+:\d+<.*),\W(?P<rte>.*)[a-zA-Z/]*\]"),
+                               r"(?P<tme>[\d+:]+<.*),\W(?P<rte>.*)[a-zA-Z/]*\]"),
             "ffmpeg": re.compile(r"([a-zA-Z]+)=\s*(-?[\d|N/A]\S+)")}
         logger.debug("Initialized %s", self.__class__.__name__)
 
