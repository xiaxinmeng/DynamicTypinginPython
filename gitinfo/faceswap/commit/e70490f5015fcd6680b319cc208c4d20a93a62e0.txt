commit e70490f5015fcd6680b319cc208c4d20a93a62e0
Author: bryanlyon <3223233+bryanlyon@users.noreply.github.com>
Date:   Wed Jul 10 15:51:05 2019 -0700

    Manual Alignments Tool: Add a frame if it doesn't exist when adding face (#790)

diff --git a/lib/alignments.py b/lib/alignments.py
index 2780631..0250060 100644
--- a/lib/alignments.py
+++ b/lib/alignments.py
@@ -221,6 +221,8 @@ class Alignments():
     def add_face(self, frame, alignment):
         """ Add a new face for a frame and return it's index """
         logger.debug("Adding face to frame: '%s'", frame)
+        if frame not in self.data:
+            self.data[frame] = []
         self.data[frame].append(alignment)
         retval = self.count_faces_in_frame(frame) - 1
         logger.debug("Returning new face index: %s", retval)
