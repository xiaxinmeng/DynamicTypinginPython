commit 7ff077a05a74eb192df74a9f29c4cd6985e6fe01
Author: kilroythethird <44308116+kilroythethird@users.noreply.github.com>
Date:   Mon Dec 17 23:35:39 2018 +0100

    Catch pynvml.NVMLError_DriverNotLoaded Exception in gpu_stats (#552)
    
    * Catch pynvml.NVMLError_DriverNotLoaded Exception in gpu_stats
    
    * Log when no CUDA able GPU is found
    
    * Moved to logging to handle MacOs too

diff --git a/lib/gpu_stats.py b/lib/gpu_stats.py
index 11c5fa8..c70e980 100644
--- a/lib/gpu_stats.py
+++ b/lib/gpu_stats.py
@@ -32,6 +32,8 @@ class GPUStats():
         self.initialize()
 
         if self.device_count == 0:
+            if self.logger:
+                self.logger.warning("No GPU detected. Switching to CPU mode")
             return
 
         self.driver = self.get_driver()
@@ -58,7 +60,7 @@ class GPUStats():
                     if self.logger:
                         self.logger.debug("OS is not macOS. Using pynvml")
                     pynvml.nvmlInit()
-                except pynvml.NVMLError_LibraryNotFound:
+                except (pynvml.NVMLError_LibraryNotFound, pynvml.NVMLError_DriverNotLoaded):
                     self.initialized = True
                     return
             self.initialized = True
