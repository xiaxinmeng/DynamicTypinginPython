commit 3e08ba4387ee1f53d637f261033704a30750a09a
Author: torzdf <36920800+torzdf@users.noreply.github.com>
Date:   Sat Aug 24 20:03:58 2019 +0000

    Fix for Linux fullscreen

diff --git a/scripts/gui.py b/scripts/gui.py
index c32581a..10390e6 100644
--- a/scripts/gui.py
+++ b/scripts/gui.py
@@ -116,7 +116,10 @@ class Gui():  # pylint: disable=too-few-public-methods
         pathscript = os.path.realpath(os.path.dirname(cmd))
         self.args = arguments
         self.root = FaceswapGui(pathscript)
-        self.root.state("zoomed")
+        try:
+            self.root.state("zoomed")
+        except tk.TclError:
+            self.root.attributes('-zoomed', True)
 
     def process(self):
         """ Builds the GUI """
