commit b2d94655aad8c24ee3eaad06de8e5f918e72d20b
Author: torzdf <36920800+torzdf@users.noreply.github.com>
Date:   Wed Sep 19 11:54:40 2018 +0100

    Bugfix: Windows does not detect openCV2 window state properly

diff --git a/tools/lib_alignments/jobs_manual.py b/tools/lib_alignments/jobs_manual.py
index d10a9a0..b983a47 100644
--- a/tools/lib_alignments/jobs_manual.py
+++ b/tools/lib_alignments/jobs_manual.py
@@ -399,12 +399,13 @@ class Manual():
         press = self.get_keys()
 
         while True:
-            if cv2.getWindowProperty('Frame', cv2.WND_PROP_VISIBLE) < 1:
-                break
             self.help.render()
             cv2.imshow("Frame", frame)
             cv2.imshow("Faces", faces)
-            key = cv2.waitKey(1)
+            key = cv2.waitKey(0)
+
+            if key == -1:
+                break
 
             if key in press.keys():
                 action = press[key]["action"]
