commit bfc73f9d06f12df1f8ca69941627ae9886102ee9
Author: bryanlyon <bryanlyon@mail.weber.edu>
Date:   Thu Feb 21 11:35:07 2019 -0800

    Fan error message (#616)
    
    * More useful FAN failure error message
    
    Just added instructions on how to use the DLIB alignment when CPU-only is used for extract.
    
    * Typo fix.

diff --git a/plugins/extract/align/fan.py b/plugins/extract/align/fan.py
index e46ca3e..062cd7f 100644
--- a/plugins/extract/align/fan.py
+++ b/plugins/extract/align/fan.py
@@ -38,7 +38,7 @@ class Align(Aligner):
         if card_id == -1:
             self.init.set()
             raise ValueError("No Graphics Card Detected! FAN is not currently supported on CPU. "
-                             "Use another aligner.")
+                             "Use another aligner such as DLIB ( -A dlib ).")
 
         if vram_total <= self.vram:
             tf_ratio = 1.0
