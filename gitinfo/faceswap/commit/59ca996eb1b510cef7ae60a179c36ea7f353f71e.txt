commit 59ca996eb1b510cef7ae60a179c36ea7f353f71e
Author: CyberDain <lepersorium@gmail.com>
Date:   Sat Mar 10 13:46:22 2018 +0400

    PluginLoader.get_available_models()
    PluginLoader.get_default_model()
    provides easy integration of Model_* folders without changing convert.py and train.py

diff --git a/plugins/PluginLoader.py b/plugins/PluginLoader.py
index a2837ac..de527e0 100644
--- a/plugins/PluginLoader.py
+++ b/plugins/PluginLoader.py
@@ -1,3 +1,4 @@
+import os
 
 class PluginLoader():
     @staticmethod
@@ -21,3 +22,16 @@ class PluginLoader():
         print("Loading {} from {} plugin...".format(attr, name))
         module = __import__(name, globals(), locals(), [], 1)
         return getattr(module, attr)
+
+    @staticmethod
+    def get_available_models():
+        models = ()
+        for dir in next(os.walk( os.path.dirname(__file__) ))[1]:
+            if dir[0:6].lower() == 'model_':
+                models += (dir[6:],)
+        return models
+        
+    @staticmethod
+    def get_default_model():
+        models = PluginLoader.get_available_models()
+        return 'Original' if 'Original' in models else models[0]
\ No newline at end of file
diff --git a/scripts/convert.py b/scripts/convert.py
index 3ad51f0..1b1d633 100644
--- a/scripts/convert.py
+++ b/scripts/convert.py
@@ -40,8 +40,8 @@ class ConvertImage(DirectoryProcessor):
 
         parser.add_argument('-t', '--trainer',
                             type=str,
-                            choices=("Original", "LowMem", "GAN", "GAN128"), # case sensitive because this is used to load a plug-in.
-                            default="Original",
+                            choices=PluginLoader.get_available_models(), # case sensitive because this is used to load a plug-in.
+                            default=PluginLoader.get_default_model(),
                             help="Select the trainer that was used to create the model.")
 
         parser.add_argument('-s', '--swap-model',
diff --git a/scripts/train.py b/scripts/train.py
index 411408e..12f6503 100644
--- a/scripts/train.py
+++ b/scripts/train.py
@@ -72,8 +72,8 @@ class TrainingProcessor(object):
                             help="Writes the training result to a file even on preview mode.")
         parser.add_argument('-t', '--trainer',
                             type=str,
-                            choices=("Original", "LowMem", "GAN", "GAN128"),
-                            default="Original",
+                            choices=PluginLoader.get_available_models(),
+                            default=PluginLoader.get_default_model(),
                             help="Select which trainer to use, LowMem for cards < 2gb.")
         parser.add_argument('-pl', '--use-perceptual-loss',
                             action="store_true",
