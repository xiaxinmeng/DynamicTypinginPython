commit 2cdf8a903aec67af4a1b56a346f1ab0d55615b12
Author: Gareth Dunstone <freedom.2.the.leetle.people@gmail.com>
Date:   Mon Feb 12 10:28:05 2018 +1100

    fixes for some comments

diff --git a/lib/cli.py b/lib/cli.py
index 40de8d5..edd3342 100644
--- a/lib/cli.py
+++ b/lib/cli.py
@@ -98,11 +98,12 @@ class DirectoryProcessor(object):
     def have_face(self, filename):
         return filename in self.faces_detected
 
-    def get_faces_alignments(self, filename):
+    def get_faces_alignments(self, filename, image):
         faces_count = 0
         faces = self.faces_detected[filename]
         for rawface in faces:
             face = DetectedFace(**rawface)
+            face.image = image[face.y : face.y + face.h, face.x : face.x + face.w]
             if self.filter is not None and not self.filter.check(face):
                 print('Skipping not recognized face!')
                 continue
diff --git a/scripts/convert.py b/scripts/convert.py
index 0e18d35..0f19018 100644
--- a/scripts/convert.py
+++ b/scripts/convert.py
@@ -187,7 +187,7 @@ class ConvertImage(DirectoryProcessor):
             image = cv2.imread(filename)
 
             if self.have_face(filename):
-                faces = self.get_faces_alignments(filename)
+                faces = self.get_faces_alignments(filename, image)
             else:
                 faces = self.get_faces(image)
             yield filename, image, faces
