commit 616b8e7b164d93f2d11f88806e1395146e75b997
Author: Audrey Roy <audreyr@gmail.com>
Date:   Sat Sep 7 15:49:15 2013 +0200

    Rely on sensible defaults from DEFAULT_CONFIG if no conf file provided.

diff --git a/cookiecutter/config.py b/cookiecutter/config.py
index 686da16..70dded6 100755
--- a/cookiecutter/config.py
+++ b/cookiecutter/config.py
@@ -59,5 +59,5 @@ def get_user_config():
 
     if os.path.exists(USER_CONFIG_PATH):
         return get_config(USER_CONFIG_PATH)
-    return None
+    return copy.copy(DEFAULT_CONFIG)
 
diff --git a/cookiecutter/main.py b/cookiecutter/main.py
index 5a0bea7..4fdcc34 100755
--- a/cookiecutter/main.py
+++ b/cookiecutter/main.py
@@ -35,6 +35,7 @@ def cookiecutter(input_dir, checkout=None, no_input=False):
     """
 
     # Get user config from ~/.cookiecutterrc or equivalent
+    # If no config file, sensible defaults from config.DEFAULT_CONFIG are used
     config_dict = get_user_config()
 
     # If it's a git repo, clone
@@ -47,14 +48,9 @@ def cookiecutter(input_dir, checkout=None, no_input=False):
     context_file = os.path.join(repo_dir, 'cookiecutter.json')
     logging.debug('context_file is {0}'.format(context_file))
 
-    # Get default context key/value pairs, if they exist
-    default_context = None
-    if config_dict:
-        default_context = config_dict['default_context']
-
     context = generate_context(
         context_file=context_file,
-        default_context=default_context
+        default_context=config_dict['default_context']
     )
 
     # prompt the user to manually configure at the command line.
diff --git a/tests/test_config.py b/tests/test_config.py
index f0064a1..a618850 100755
--- a/tests/test_config.py
+++ b/tests/test_config.py
@@ -117,7 +117,7 @@ class TestGetUserConfig(unittest.TestCase):
 
     def test_get_user_config_nonexistent(self):
         """ Get config from a nonexistent ~/.cookiecutterrc file """
-        self.assertEqual(config.get_user_config(), None)
+        self.assertEqual(config.get_user_config(), config.DEFAULT_CONFIG)
         
 
 
