commit 63e532e451e2993b0eec5dac924c68aba25af0b5
Author: Benjamin Schwarze <benjamin.schwarze@mailboxd.de>
Date:   Tue Sep 10 22:29:25 2013 +0200

    first attempt to fix umask on travis to make permission tests pass there too

diff --git a/.travis.yml b/.travis.yml
index cb51c7a..741eca9 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -8,6 +8,8 @@ python:
   - "2.6"
   - "pypy"
 
+before_install: umask 0022
+
 # command to install dependencies, e.g. pip install -r requirements.txt --use-mirrors
 install:
  - if [[ $TRAVIS_PYTHON_VERSION == '2.6' ]]; then pip install -r requirements/test_26.txt; fi
