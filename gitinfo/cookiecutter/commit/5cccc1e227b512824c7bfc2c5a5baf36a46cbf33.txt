commit 5cccc1e227b512824c7bfc2c5a5baf36a46cbf33
Author: Audrey Roy <audreyr@gmail.com>
Date:   Thu Aug 8 10:43:15 2013 +0200

    Take existing value in repo's cookiecutter.json as default.

diff --git a/cookiecutter/prompt.py b/cookiecutter/prompt.py
index 9f2ff8f..fe5a7d6 100755
--- a/cookiecutter/prompt.py
+++ b/cookiecutter/prompt.py
@@ -24,14 +24,12 @@ def prompt_for_config(context):
     cookiecutter_dict = {}
     
     for key, val in context['cookiecutter'].iteritems():
-        prompt = "{0} (e.g. \"{1}\")? ".format(key, val)
+        prompt = "{0} (default is \"{1}\")? ".format(key, val)
         new_val = input(prompt)
         new_val = new_val.strip()
 
-        while new_val == '':
-            prompt = "Please enter a value for {0}: ".format(key)
-            new_val = input(prompt)
-            new_val = new_val.strip()
+        if new_val == '':
+            new_val = val
         
         cookiecutter_dict[key] = new_val
     
