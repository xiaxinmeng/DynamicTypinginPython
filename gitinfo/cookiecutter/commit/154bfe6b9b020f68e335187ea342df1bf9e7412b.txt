commit 154bfe6b9b020f68e335187ea342df1bf9e7412b
Author: Rachel Sanders <rsanders@linkedin.com>
Date:   Sat Oct 4 12:15:31 2014 -0700

    Adding a test around that

diff --git a/tests/test_vcs.py b/tests/test_vcs.py
index fcca3e0..03bdb9b 100755
--- a/tests/test_vcs.py
+++ b/tests/test_vcs.py
@@ -136,6 +136,14 @@ class TestVCSPrompt(unittest.TestCase):
         self.assertEqual(repo_dir, 'cookiecutter-pypackage')
         self.assertTrue(os.path.isfile('cookiecutter-pypackage/README.rst'))
 
+    def test_git_clone_overwrite_with_no_prompt(self):
+        repo_dir = vcs.clone(
+            'https://github.com/audreyr/cookiecutter-pypackage.git',
+            no_input=True
+        )
+        self.assertEqual(repo_dir, 'cookiecutter-pypackage')
+        self.assertTrue(os.path.isfile('cookiecutter-pypackage/README.rst'))
+
     @patch('cookiecutter.prompt.read_response', lambda x=u'': u'n')
     def test_git_clone_cancel(self):
         self.assertRaises(
