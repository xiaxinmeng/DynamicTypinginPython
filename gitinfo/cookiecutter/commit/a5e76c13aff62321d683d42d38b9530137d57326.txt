commit a5e76c13aff62321d683d42d38b9530137d57326
Author: Audrey Roy <audreyr@gmail.com>
Date:   Thu Aug 8 12:05:01 2013 +0200

    Read cookiecutter.json into OrderedDict to preserve order for prompt.

diff --git a/.travis.yml b/.travis.yml
index f0f7299..b9dc033 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -10,7 +10,7 @@ python:
 # command to install dependencies, e.g. pip install -r requirements.txt --use-mirrors
 install:
  - pip install -r requirements.txt
- - if [[ $TRAVIS_PYTHON_VERSION == '2.6' ]]; then pip install unittest2; fi
+ - if [[ $TRAVIS_PYTHON_VERSION == '2.6' ]]; then pip install unittest2 simplejson ordereddict; fi
 
 # command to run tests, e.g. python setup.py test
 script: python setup.py test
diff --git a/cookiecutter/generate.py b/cookiecutter/generate.py
index dbb24c5..6fe7925 100755
--- a/cookiecutter/generate.py
+++ b/cookiecutter/generate.py
@@ -8,9 +8,9 @@ cookiecutter.generate
 Functions for generating a project from a project template.
 """
 
-import json
 import logging
 import os
+import sys
 
 from jinja2 import FileSystemLoader, Template
 from jinja2.environment import Environment
@@ -19,6 +19,13 @@ from .exceptions import NonTemplatedInputDirException
 from .utils import make_sure_path_exists, unicode_open
 
 
+if sys.version_info[:2] < (2, 7):
+    import simplejson as json
+    from ordereddict import OrderedDict
+else:
+    import json
+    from collections import OrderedDict
+
 def generate_context(config_file='cookiecutter.json'):
     """
     Generates the context for a Cookiecutter project template.
@@ -31,7 +38,7 @@ def generate_context(config_file='cookiecutter.json'):
     context = {}
 
     file_handle = open(config_file)
-    obj = json.load(file_handle)
+    obj = json.load(file_handle, object_pairs_hook=OrderedDict)
 
     # Add the Python object to the context dictionary
     file_name = os.path.split(config_file)[1]
diff --git a/tox.ini b/tox.ini
index 74f4ca9..265d567 100644
--- a/tox.ini
+++ b/tox.ini
@@ -11,5 +11,7 @@ deps =
 [testenv:py26]
 deps =
     unittest2
+    ordereddict
+    simplejson
     -r{toxinidir}/requirements.txt
 commands = python setup.py test
