commit 94871c85af2b91d2bc2eb48769eac0932281d3ce
Author: Audrey Roy <audreyr@gmail.com>
Date:   Sun Jul 14 23:04:40 2013 +0200

    Add folder test and debug.

diff --git a/cookiecutter/cookiecutter.py b/cookiecutter/cookiecutter.py
index af9813c..67ab0a1 100755
--- a/cookiecutter/cookiecutter.py
+++ b/cookiecutter/cookiecutter.py
@@ -71,6 +71,7 @@ def generate_files(context=None, input_dir='input/', output_dir='output/'):
         full_path = '{0}/{1}'.format(input_dir, f)
         print full_path
         if os.path.isdir(full_path):
+            print "Found dir" + full_path
             make_sure_path_exists(full_path)
         elif os.path.isfile(full_path):   # If f is a file, render it
             tmpl = env.get_template(f)
diff --git a/tests/input2/folder/in_folder.txt b/tests/input2/folder/in_folder.txt
new file mode 100644
index 0000000..ac0709a
--- /dev/null
+++ b/tests/input2/folder/in_folder.txt
@@ -0,0 +1 @@
+The color is {{ stuff.color }} and the letter is {{ stuff.letter }}.
\ No newline at end of file
diff --git a/tests/json2/stuff.json b/tests/json2/stuff.json
index 4edc766..017a1b3 100644
--- a/tests/json2/stuff.json
+++ b/tests/json2/stuff.json
@@ -1,4 +1,6 @@
 {
 	"full_name": "Audrey Greenfeld",
-	"year": "2014"
+	"year": "2014",
+	"color": "green",
+	"letter": "D"
 }
\ No newline at end of file
diff --git a/tests/test_cookiecutter.py b/tests/test_cookiecutter.py
index 387903e..86821e8 100755
--- a/tests/test_cookiecutter.py
+++ b/tests/test_cookiecutter.py
@@ -52,6 +52,9 @@ My name is Audrey Greenfeld.
 It is 2014."""
         something2 = open('tests/output2/something.txt').read()
         self.assertEqual(something, something2)
+        in_folder = "The color is green and the letter is D."
+        in_folder2 = open('tests/output2/folder/in_folder.txt')
+        self.assertEqual(in_folder, in_folder2)
 
     def tearDown(self):
         if os.path.exists('tests/output'):
