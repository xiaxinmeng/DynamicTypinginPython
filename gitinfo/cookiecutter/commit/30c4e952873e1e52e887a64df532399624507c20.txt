commit 30c4e952873e1e52e887a64df532399624507c20
Author: Russell Keith-Magee <russell@keith-magee.com>
Date:   Sat Jun 17 13:42:13 2017 +0800

    Fixes for Python 2.7 compatibility.

diff --git a/cookiecutter/zipfile.py b/cookiecutter/zipfile.py
index a28534a..8b0d9a5 100644
--- a/cookiecutter/zipfile.py
+++ b/cookiecutter/zipfile.py
@@ -1,3 +1,5 @@
+from __future__ import absolute_import
+
 import os
 import requests
 import sys
diff --git a/tests/zipfile/test_unzip.py b/tests/zipfile/test_unzip.py
index 7e7c3d0..65ef98f 100644
--- a/tests/zipfile/test_unzip.py
+++ b/tests/zipfile/test_unzip.py
@@ -1,6 +1,5 @@
 # -*- coding: utf-8 -*-
 import os
-from unittest.mock import MagicMock
 
 import pytest
 
@@ -66,7 +65,7 @@ def test_unzip_url(mocker, tmpdir):
         autospec=True
     )
 
-    request = MagicMock()
+    request = mocker.MagicMock()
     request.iter_content.return_value = mock_download()
 
     mocker.patch(
@@ -97,7 +96,7 @@ def test_unzip_url_existing_cache(mocker, tmpdir):
         autospec=True
     )
 
-    request = MagicMock()
+    request = mocker.MagicMock()
     request.iter_content.return_value = mock_download()
 
     mocker.patch(
@@ -132,7 +131,7 @@ def test_unzip_url_existing_template(mocker, tmpdir):
         autospec=True
     )
 
-    request = MagicMock()
+    request = mocker.MagicMock()
     request.iter_content.return_value = mock_download()
 
     mocker.patch(
@@ -173,7 +172,7 @@ def test_unzip_url_existing_cache_and_template(mocker, tmpdir):
                 yield chunk
                 chunk = zipfile.read(1024)
 
-    request = MagicMock()
+    request = mocker.MagicMock()
     request.iter_content.return_value = mock_download()
 
     mocker.patch(
