commit dc64cae7142d6d2bed3ec07e8bc20dc97ffb988a
Author: Antony Lee <anntzer.lee@gmail.com>
Date:   Mon Sep 9 11:22:45 2019 +0200

    Don't install whichcraft on Py>=3.
    
    Uses the second (setuptools>=18) approach documented at
    https://hynek.me/articles/conditional-python-dependencies/
    to make the dependency Py2-only.

diff --git a/cookiecutter/vcs.py b/cookiecutter/vcs.py
index 6af7818..bbe63b1 100644
--- a/cookiecutter/vcs.py
+++ b/cookiecutter/vcs.py
@@ -7,7 +7,10 @@ import logging
 import os
 import subprocess
 
-from whichcraft import which
+try:
+    from shutil import which
+except ImportError:
+    from whichcraft import which
 
 from .exceptions import (
     RepositoryNotFound, RepositoryCloneFailed, UnknownRepoType, VCSNotInstalled
diff --git a/setup.py b/setup.py
index d3286b7..0ac8598 100644
--- a/setup.py
+++ b/setup.py
@@ -27,7 +27,6 @@ requirements = [
     'binaryornot>=0.2.0',
     'jinja2>=2.7',
     'click>=7.0',
-    'whichcraft>=0.4.0',
     'poyo>=0.1.0',
     'jinja2-time>=0.1.0',
     'requests>=2.18.0',
@@ -60,6 +59,9 @@ setup(
     include_package_data=True,
     python_requires='>=2.7, !=3.0.*, !=3.1.*, !=3.2.*, !=3.3.*, !=3.4.*',
     install_requires=requirements,
+    extras_require={
+        ':python_version<"3.3"': ['whichcraft>=0.4.0'],
+    },
     license='BSD',
     zip_safe=False,
     classifiers=[
