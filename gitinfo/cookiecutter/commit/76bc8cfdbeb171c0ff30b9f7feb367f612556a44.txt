commit 76bc8cfdbeb171c0ff30b9f7feb367f612556a44
Author: Audrey Roy <audreyr@gmail.com>
Date:   Sat Sep 7 12:44:25 2013 +0200

    If user has no ~/.cookiecutterrc, get_user_config() returns None.

diff --git a/cookiecutter/config.py b/cookiecutter/config.py
index b6a5fd8..b2e99b1 100755
--- a/cookiecutter/config.py
+++ b/cookiecutter/config.py
@@ -46,11 +46,13 @@ def get_config(config_path):
 
 def get_user_config():
     """
-    Retrieve settings from the user's ~/.cookiecutterrc or equivalent home dir
-    config file.
+    Retrieve config from the user's ~/.cookiecutterrc, if it exists.
+    Otherwise, return None.
     """
     
     # TODO: test on windows...
     USER_CONFIG_PATH = os.path.expanduser('~/.cookiecutter')
-    
-    return get_config(USER_CONFIG_PATH)
+
+    if os.path.exists(USER_CONFIG_PATH):
+        return get_config(USER_CONFIG_PATH)
+    return None
