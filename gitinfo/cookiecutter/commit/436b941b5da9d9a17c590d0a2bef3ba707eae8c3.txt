commit 436b941b5da9d9a17c590d0a2bef3ba707eae8c3
Author: Raphael Pierzina <raphael@hackebrot.de>
Date:   Mon Nov 23 23:49:08 2015 +0100

    Remove obsolete tests/skipif_markers.py as no test requires it anymore

diff --git a/tests/skipif_markers.py b/tests/skipif_markers.py
deleted file mode 100644
index 72b50d7..0000000
--- a/tests/skipif_markers.py
+++ /dev/null
@@ -1,35 +0,0 @@
-#!/usr/bin/env python
-# -*- coding: utf-8 -*-
-
-"""
-skipif_markers
---------------
-
-Contains pytest skipif markers to be used in the suite.
-"""
-
-import pytest
-import os
-
-
-try:
-    os.environ[u'TRAVIS']
-except KeyError:
-    travis = False
-else:
-    travis = True
-
-try:
-    os.environ[u'DISABLE_NETWORK_TESTS']
-except KeyError:
-    no_network = False
-else:
-    no_network = True
-
-skipif_travis = pytest.mark.skipif(
-    travis, reason='Works locally with tox but fails on Travis.'
-)
-
-skipif_no_network = pytest.mark.skipif(
-    no_network, reason='Needs a network connection to GitHub/Bitbucket.'
-)
