commit ed90a95203cd4899dfb4bd04389a6d808f4945db
Author: Trey Hunner <trey@treyhunner.com>
Date:   Sun Jun 8 14:19:43 2014 -0700

    Fix final newline stripping (don't strip them)

diff --git a/cookiecutter/generate.py b/cookiecutter/generate.py
index 95f4072..b3751c9 100755
--- a/cookiecutter/generate.py
+++ b/cookiecutter/generate.py
@@ -186,7 +186,7 @@ def generate_files(repo_dir, context=None, output_dir="."):
         run_hook('pre_gen_project', project_dir)
 
     with work_in(template_dir):
-        env = Environment()
+        env = Environment(keep_trailing_newline=True)
         env.loader = FileSystemLoader(".")
 
         for root, dirs, files in os.walk("."):
