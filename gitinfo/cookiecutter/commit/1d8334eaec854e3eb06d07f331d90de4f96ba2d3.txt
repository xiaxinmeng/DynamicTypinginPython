commit 1d8334eaec854e3eb06d07f331d90de4f96ba2d3
Author: Wieland Hoffmann <themineo@gmail.com>
Date:   Wed Feb 5 17:02:45 2014 +0100

    Let sphinx parse :param: properly by inserting newlines

diff --git a/cookiecutter/utils.py b/cookiecutter/utils.py
index c40d4e4..a7405f8 100755
--- a/cookiecutter/utils.py
+++ b/cookiecutter/utils.py
@@ -26,9 +26,10 @@ else:
 def make_sure_path_exists(path):
     """
     Ensures that a directory exists.
+
     :param path: A directory path.
     """
-    
+
     logging.debug("Making sure path exists: {0}".format(path))
     try:
         os.makedirs(path)
@@ -41,6 +42,7 @@ def make_sure_path_exists(path):
 def unicode_open(filename, *args, **kwargs):
     """
     Opens a file as usual on Python 3, and with UTF-8 encoding on Python 2.
+
     :param filename: Name of file to open.
     """
     kwargs['encoding'] = "utf-8"
diff --git a/cookiecutter/vcs.py b/cookiecutter/vcs.py
index 510e535..e70ea30 100755
--- a/cookiecutter/vcs.py
+++ b/cookiecutter/vcs.py
@@ -24,6 +24,7 @@ def prompt_and_delete_repo(repo_dir):
     """
     Asks the user whether it's okay to delete the previously-cloned repo.
     If yes, deletes it. Otherwise, Cookiecutter exits.
+
     :param repo_dir: Directory of previously-cloned repo.
     """
 
@@ -40,10 +41,11 @@ def prompt_and_delete_repo(repo_dir):
 def identify_repo(repo_url):
     """
     Determines if `repo_url` should be treated as a URL to a git or hg repo.
+
     :param repo_url: Repo URL of unknown type.
     :returns: "git", "hg", or None.
     """
-    
+
     if "git" in repo_url:
         return "git"
     elif "bitbucket" in repo_url:
@@ -63,9 +65,9 @@ def clone(repo_url, checkout=None, clone_to_dir="."):
     # Ensure that clone_to_dir exists
     clone_to_dir = os.path.expanduser(clone_to_dir)
     make_sure_path_exists(clone_to_dir)
-    
+
     repo_type = identify_repo(repo_url)
-    
+
     tail = os.path.split(repo_url)[1]
     if repo_type == "git":
         repo_dir = os.path.normpath(os.path.join(clone_to_dir, tail.rsplit('.git')[0]))
