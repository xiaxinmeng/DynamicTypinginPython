commit aa08469372cffe868079ef6ad772f95325ecd6df
Author: Raphael Pierzina <raphael@hackebrot.de>
Date:   Tue Mar 1 18:36:55 2016 +0000

    Implement a test for expanduser on userconfig dirs

diff --git a/tests/test-config/config-expand-user.yaml b/tests/test-config/config-expand-user.yaml
new file mode 100644
index 0000000..2b6b14e
--- /dev/null
+++ b/tests/test-config/config-expand-user.yaml
@@ -0,0 +1,2 @@
+cookiecutters_dir: "~/templates"
+replay_dir: "~/replay-files"
diff --git a/tests/test_get_user_config.py b/tests/test_get_user_config.py
index b923954..1e45137 100644
--- a/tests/test_get_user_config.py
+++ b/tests/test_get_user_config.py
@@ -141,3 +141,15 @@ def test_force_default_config(mocker):
 
     assert user_config == config.DEFAULT_CONFIG
     assert not spy_get_config.called
+
+
+def test_expand_user_for_directories_in_config(monkeypatch):
+    def _expanduser(path):
+        return path.replace('~', 'Users/bob')
+    monkeypatch.setattr('os.path.expanduser', _expanduser)
+
+    config_file = 'tests/test-config/config-expand-user.yaml'
+
+    user_config = config.get_user_config(config_file)
+    assert user_config['replay_dir'] == 'Users/bob/replay-files'
+    assert user_config['cookiecutters_dir'] == 'Users/bob/templates'
