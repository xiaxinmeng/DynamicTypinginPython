commit a35b3a2692a6596840a9ec958277308e02ab3758
Author: Audrey Roy <audreyr@gmail.com>
Date:   Mon Aug 19 21:50:18 2013 +0200

    Decode input strings on Python 2.x to fix UnicodeDecodeError from values entered at prompt.

diff --git a/cookiecutter/prompt.py b/cookiecutter/prompt.py
index 5b39e70..99702d2 100755
--- a/cookiecutter/prompt.py
+++ b/cookiecutter/prompt.py
@@ -32,8 +32,10 @@ def prompt_for_config(context):
         if new_val == '':
             new_val = val
         
-        cookiecutter_dict[key] = new_val
-    
+        if PY3:
+            cookiecutter_dict[key] = new_val
+        else:
+            cookiecutter_dict[key] = new_val.decode('utf-8')
     return cookiecutter_dict
 
 
diff --git a/tests/test_prompt.py b/tests/test_prompt.py
index 38937f4..e35e991 100755
--- a/tests/test_prompt.py
+++ b/tests/test_prompt.py
@@ -35,6 +35,15 @@ class TestPrompt(unittest.TestCase):
         cookiecutter_dict = prompt.prompt_for_config(context)
         self.assertEqual(cookiecutter_dict, {"full_name": "Audrey Roy"})
 
+    @patch(input_str, lambda x: 'Pizzä ïs Gööd')
+    def test_prompt_for_config_unicode(self):
+        context = {"cookiecutter": {"full_name": "Your Name"}}
+        cookiecutter_dict = prompt.prompt_for_config(context)
+        if PY3:
+            self.assertEqual(cookiecutter_dict, {"full_name": "Pizzä ïs Gööd"})
+        else:
+            self.assertEqual(cookiecutter_dict, {"full_name": "Pizzä ïs Gööd"})
+
 @unittest.skipUnless(condition=PY3, reason='Only works on PY3 as of now.')
 class TestQueryAnswers(unittest.TestCase):
 
