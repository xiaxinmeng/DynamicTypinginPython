commit 3b3674dd4999de8e3fa8441bdb32291969cca8bc
Author: Audrey Roy <audreyr@gmail.com>
Date:   Thu Nov 7 20:11:42 2013 -0800

    More upgrade notes in docs.

diff --git a/HISTORY.rst b/HISTORY.rst
index 5e91b75..fa177b7 100644
--- a/HISTORY.rst
+++ b/HISTORY.rst
@@ -51,6 +51,7 @@ Other changes:
   of `subprocess.Popen` for Python 2 compatibility.
 * Removal of cookiecutter's `cleanup` module.
 * A bit of `setup.py` cleanup, thanks to `@oubiga`_.
+* Now depends on binaryornot 0.2.0.
 
 .. _`@foobacca`: https://github.com/foobacca/
 .. _`@raphigaziano`: https://github.com/raphigaziano/
diff --git a/docs/installation.rst b/docs/installation.rst
index 81e5552..87b32c7 100644
--- a/docs/installation.rst
+++ b/docs/installation.rst
@@ -6,7 +6,7 @@ At the command line:
 
 .. code-block:: bash
 
-    $ easy_install cookiecutter
+    $ [sudo] easy_install cookiecutter
 
 Or, if you have pip (the Python package installer tool):
 
@@ -24,9 +24,16 @@ changes. The big ones are:
 * Cloned repos are saved into `~/.cookiecutters/`. 
 * You can optionally create a `~/.cookiecutterrc` config file.
 
-Upgrade Cookiecutter with one of the following:
+Upgrade Cookiecutter either with easy_install:
+
+.. code-block:: bash
+
+    $ [sudo] easy_install --upgrade cookiecutter
+
+Or with easy_install:
+
+.. code-block:: bash
 
-    $ easy_install --upgrade cookiecutter
     $ [sudo] pip install -U cookiecutter
 
 Then you should be good to go.
