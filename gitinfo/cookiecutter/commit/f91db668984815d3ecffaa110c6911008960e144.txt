commit f91db668984815d3ecffaa110c6911008960e144
Author: Ionel Cristian Mărieș <contact@ionelmc.ro>
Date:   Thu Nov 5 21:48:30 2015 +0200

    Encode the result of the hook template to utf8.
    
    Fixes #577,

diff --git a/cookiecutter/hooks.py b/cookiecutter/hooks.py
index 81045d1..254bc2e 100755
--- a/cookiecutter/hooks.py
+++ b/cookiecutter/hooks.py
@@ -90,10 +90,11 @@ def run_script_with_context(script_path, cwd, context):
 
     with tempfile.NamedTemporaryFile(
         delete=False,
-        mode='w',
+        mode='wb',
         suffix=extension
     ) as temp:
-        temp.write(Template(contents).render(**context))
+        output = Template(contents).render(**context)
+        temp.write(output.encode('utf-8'))
 
     run_script(temp.name, cwd)
 
