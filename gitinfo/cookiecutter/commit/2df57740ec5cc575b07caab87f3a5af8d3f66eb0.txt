commit 2df57740ec5cc575b07caab87f3a5af8d3f66eb0
Author: michaeljoseph <michaeljoseph+github@gmail.com>
Date:   Wed Jul 13 18:27:49 2016 +0200

    Import update and additional assertion

diff --git a/tests/repository/test_determine_repo_dir_clones_repo.py b/tests/repository/test_determine_repo_dir_clones_repo.py
index 58b782a..5526122 100644
--- a/tests/repository/test_determine_repo_dir_clones_repo.py
+++ b/tests/repository/test_determine_repo_dir_clones_repo.py
@@ -3,7 +3,7 @@ import os
 
 import pytest
 
-from cookiecutter.repository import determine_repo_dir
+from cookiecutter import repository
 
 
 @pytest.fixture
@@ -15,7 +15,7 @@ def template_url():
     return 'https://github.com/pytest-dev/cookiecutter-pytest-plugin.git'
 
 
-def test_determine_repository_url_should_clone(
+def test_repository_url_should_clone(
         mocker, template_url, user_config_data):
     """`clone()` should be called with correct args when `cookiecutter()` is
     called.
@@ -27,7 +27,7 @@ def test_determine_repository_url_should_clone(
         autospec=True
     )
 
-    project_dir = determine_repo_dir(
+    project_dir = repository.determine_repo_dir(
         template_url,
         abbreviations={},
         clone_to_dir=user_config_data['cookiecutters_dir'],
@@ -43,3 +43,4 @@ def test_determine_repository_url_should_clone(
     )
 
     assert os.path.isdir(project_dir)
+    assert 'tests/fake-repo-tmpl' == project_dir
