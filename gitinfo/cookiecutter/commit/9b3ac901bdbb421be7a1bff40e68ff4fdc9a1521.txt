commit 9b3ac901bdbb421be7a1bff40e68ff4fdc9a1521
Author: Raphael Pierzina <raphael@hackebrot.de>
Date:   Sun Apr 10 16:42:34 2016 +0100

    Add clean-tox make target to remove testing artifacts

diff --git a/Makefile b/Makefile
index d214493..bc96b93 100644
--- a/Makefile
+++ b/Makefile
@@ -1,4 +1,4 @@
-.PHONY: clean-pyc clean-build docs
+.PHONY: clean-tox clean-pyc clean-build docs
 define BROWSER_PYSCRIPT
 import os, webbrowser, sys
 try:
@@ -14,6 +14,7 @@ BROWSER := python -c "$$BROWSER_PYSCRIPT"
 help:
 	@echo "clean-build - remove build artifacts"
 	@echo "clean-pyc - remove Python file artifacts"
+	@echo "clean-tox - remove tox testing artifacts"
 	@echo "lint - check style with flake8"
 	@echo "test - run tests quickly with the default Python"
 	@echo "test-all - run tests on every Python version with tox"
@@ -22,7 +23,7 @@ help:
 	@echo "release - package and upload a release"
 	@echo "sdist - package"
 
-clean: clean-build clean-pyc
+clean: clean-tox clean-build clean-pyc
 
 clean-build:
 	rm -fr build/
@@ -34,6 +35,9 @@ clean-pyc:
 	find . -name '*.pyo' -exec rm -f {} +
 	find . -name '*~' -exec rm -f {} +
 
+clean-tox:
+	rm -rf .tox/
+
 lint:
 	flake8 cookiecutter tests
 
