commit b7004e4eaab3fd811207902cf62a336cd82a90a7
Author: Raphael Pierzina <raphael@hackebrot.de>
Date:   Sat Aug 20 13:32:23 2016 +0100

    Update doc str of is_copy_only_path()

diff --git a/cookiecutter/generate.py b/cookiecutter/generate.py
index bb83ff8..578eafe 100644
--- a/cookiecutter/generate.py
+++ b/cookiecutter/generate.py
@@ -28,7 +28,11 @@ from .hooks import run_hook
 
 
 def is_copy_only_path(path, context):
-    """Return True if `path` matches context dict pattern.
+    """Check whether the given `path` should only be copied as opposed to being
+    rendered.
+
+    Returns True if `path` matches a pattern in the given `context` dict,
+    otherwise False.
 
     :param path: A file-system path referring to a file or dir that
         should be rendered or just copied.
