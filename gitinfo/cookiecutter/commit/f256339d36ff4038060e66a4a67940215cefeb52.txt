commit f256339d36ff4038060e66a4a67940215cefeb52
Author: Ramiro Batista da Luz <ramiroluz@gmail.com>
Date:   Fri Oct 30 22:53:50 2015 -0200

    Remove the trailing slash with rstrip. issue #546.

diff --git a/cookiecutter/vcs.py b/cookiecutter/vcs.py
index 7c9f3c6..4f7dbd9 100755
--- a/cookiecutter/vcs.py
+++ b/cookiecutter/vcs.py
@@ -103,8 +103,7 @@ def clone(repo_url, checkout=None, clone_to_dir=".", no_input=False):
         msg = "'{0}' is not installed.".format(repo_type)
         raise VCSNotInstalled(msg)
 
-    if repo_url.endswith('/'):
-        repo_url = repo_url[:-1]
+    repo_url = repo_url.rstrip('/')
     tail = os.path.split(repo_url)[1]
     if repo_type == 'git':
         repo_dir = os.path.normpath(os.path.join(clone_to_dir,
