commit 508bc4b83c31da3913a4a0d6c29940c39cc0195a
Author: Russell Keith-Magee <russell@keith-magee.com>
Date:   Sat Sep 16 19:18:20 2017 +0800

    Another Windows compatibility fix.

diff --git a/cookiecutter/zipfile.py b/cookiecutter/zipfile.py
index b148e83..4b030c7 100644
--- a/cookiecutter/zipfile.py
+++ b/cookiecutter/zipfile.py
@@ -34,7 +34,7 @@ def unzip(zip_uri, is_url, clone_to_dir='.', no_input=False, password=None):
     if is_url:
         # Build the name of the cached zipfile,
         # and prompt to delete if it already exists.
-        identifier = zip_uri.rsplit(os.path.sep, 1)[1]
+        identifier = zip_uri.rsplit('/', 1)[1]
         zip_path = os.path.join(clone_to_dir, identifier)
 
         if os.path.exists(zip_path):
