commit f4cb832d61437ad6e871a1596393adc06ceafab9
Author: Audrey Roy <audreyr@gmail.com>
Date:   Sat Aug 31 13:51:15 2013 +0200

    Add docstring. 4 spaces for consistency.

diff --git a/cookiecutter/utils.py b/cookiecutter/utils.py
index 6eb2950..61fee63 100755
--- a/cookiecutter/utils.py
+++ b/cookiecutter/utils.py
@@ -47,10 +47,14 @@ def unicode_open(filename, *args, **kwargs):
 
 @contextlib.contextmanager
 def work_in(dirname=None):
-  curdir = os.getcwd()
-  try:
-    if dirname is not None:
-      os.chdir(dirname)
-    yield
-  finally:
-    os.chdir(curdir)
+    """
+    Context manager version of os.chdir. When exited, returns to the working
+    directory prior to entering.
+    """
+    curdir = os.getcwd()
+    try:
+        if dirname is not None:
+            os.chdir(dirname)
+        yield
+    finally:
+        os.chdir(curdir)
