commit 5e9f4c74a97e1b52acf11f6897d8b8ed470b0478
Author: Raphael Pierzina <raphael@hackebrot.de>
Date:   Sun Jun 25 14:20:11 2017 +0200

    Use merge_config for defaults and user config

diff --git a/cookiecutter/config.py b/cookiecutter/config.py
index 2503939..2be7772 100644
--- a/cookiecutter/config.py
+++ b/cookiecutter/config.py
@@ -69,8 +69,7 @@ def get_config(config_path):
                 ''.format(config_path, e)
             )
 
-    config_dict = copy.copy(DEFAULT_CONFIG)
-    config_dict.update(yaml_dict)
+    config_dict = merge_config(copy.copy(DEFAULT_CONFIG), yaml_dict)
 
     raw_replay_dir = config_dict['replay_dir']
     config_dict['replay_dir'] = _expand_path(raw_replay_dir)
