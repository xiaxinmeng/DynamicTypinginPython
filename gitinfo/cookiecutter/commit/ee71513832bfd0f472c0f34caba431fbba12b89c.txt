commit ee71513832bfd0f472c0f34caba431fbba12b89c
Author: Audrey Roy <audreyr@gmail.com>
Date:   Tue Aug 20 21:05:07 2013 +0200

    Fixes to version-specific dependencies (#42).

diff --git a/setup.py b/setup.py
index a6f78a3..427e3ac 100755
--- a/setup.py
+++ b/setup.py
@@ -20,13 +20,19 @@ history = open('HISTORY.rst').read().replace('.. :changelog:', '')
 requirements = ['binaryornot>=0.1.1', 'jinja2>=2.4']
 test_requirements = []
 
+# Add Python 2.6-specific dependencies
 if sys.version_info[:2] < (2, 7):
     requirements.append('argparse')
     requirements.append('ordereddict')
+    requirements.append('simplejson')
+    test_requirements.append('unittest2')
 
-if sys.version > '3':
+# Add Python 2.6 and 2.7-specific dependencies
+if sys.version < '3':
     test_requirements.append('mock')
 
+# There are no Python 3-specific dependencies to add
+    
 setup(
     name='cookiecutter',
     version=cookiecutter.__version__,
