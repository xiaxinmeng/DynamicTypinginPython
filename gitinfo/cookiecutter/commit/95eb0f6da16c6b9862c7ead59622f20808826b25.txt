commit 95eb0f6da16c6b9862c7ead59622f20808826b25
Author: Raphael Pierzina <raphael@hackebrot.de>
Date:   Sat Aug 8 19:40:02 2015 +0200

    Implement a test for the cli to accept --replay

diff --git a/tests/test_cli.py b/tests/test_cli.py
index d45e6ec..f1e5fbd 100644
--- a/tests/test_cli.py
+++ b/tests/test_cli.py
@@ -55,3 +55,24 @@ def test_cli_verbose():
     result = runner.invoke(main, ['tests/fake-repo-pre/', '--no-input', '-v'])
     assert result.exit_code == 0
     assert os.path.isdir('fake-project')
+
+
+def test_cli_replay(mocker):
+    mock_cookiecutter = mocker.patch(
+        'cookiecutter.cli.cookiecutter'
+    )
+
+    template_path = 'tests/fake-repo-pre/'
+    result = runner.invoke(main, [
+        template_path,
+        '--replay',
+        '-v'
+    ])
+
+    assert result.exit_code == 0
+    mock_cookiecutter.assert_once_called_with(
+        template_path,
+        None,
+        False,
+        True
+    )
