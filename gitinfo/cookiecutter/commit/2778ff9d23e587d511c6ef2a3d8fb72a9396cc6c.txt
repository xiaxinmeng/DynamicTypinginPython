commit 2778ff9d23e587d511c6ef2a3d8fb72a9396cc6c
Author: Audrey Roy <audreyr@gmail.com>
Date:   Sat Aug 10 11:41:30 2013 +0200

    Add Coveralls to Travis config.

diff --git a/.travis.yml b/.travis.yml
index b9dc033..708bcb2 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -10,7 +10,11 @@ python:
 # command to install dependencies, e.g. pip install -r requirements.txt --use-mirrors
 install:
  - pip install -r requirements.txt
+ - pip install coveralls
  - if [[ $TRAVIS_PYTHON_VERSION == '2.6' ]]; then pip install unittest2 simplejson ordereddict; fi
 
 # command to run tests, e.g. python setup.py test
-script: python setup.py test
+script: coverage run --source cookiecutter setup.py test
+
+after_success:
+    coveralls
