commit 05c189205722c27d7a51bb7178d21b133ec3c2c0
Author: Sorin Sbarnea <ssbarnea@users.noreply.github.com>
Date:   Sun Jan 19 12:54:38 2020 +0000

    D401 rule activation (#1285)
    
    Fixes D401 rule and activates it.

diff --git a/setup.cfg b/setup.cfg
index 492220c..ef548a7 100644
--- a/setup.cfg
+++ b/setup.cfg
@@ -11,7 +11,8 @@ tag_name = {new_version}
 [flake8]
 # BLK100 excluded temporary until we enable python black
 # D* added temporary to ease enablement remove them while addressing them
-ignore = E731,BLK100,D100,D101,D102,D103,D200,D202,D205,D400,D401
+ignore = E731,BLK100,D100,D101,D102,D103,D200,D202,D205,D400
+# D400 First line should end with a period
 
 [bdist_wheel]
 universal = 1
diff --git a/tests/conftest.py b/tests/conftest.py
index 8c86602..3e17e2c 100644
--- a/tests/conftest.py
+++ b/tests/conftest.py
@@ -23,7 +23,7 @@ replay_dir: "{replay_dir}"
 
 
 def backup_dir(original_dir, backup_dir):
-    """Method to generate backup directory based on original directory."""
+    """Generate backup directory based on original directory."""
     # If the default original_dir is pre-existing, move it to a temp location
     if not os.path.isdir(original_dir):
         return False
@@ -37,7 +37,7 @@ def backup_dir(original_dir, backup_dir):
 
 
 def restore_backup_dir(original_dir, backup_dir, original_dir_found):
-    """Method restores default contents."""
+    """Restore default contents."""
     # Carefully delete the created original_dir only in certain
     # conditions.
     original_dir_is_dir = os.path.isdir(original_dir)
diff --git a/tests/test_hooks.py b/tests/test_hooks.py
index 4402599..fcc95ad 100644
--- a/tests/test_hooks.py
+++ b/tests/test_hooks.py
@@ -17,7 +17,7 @@ from cookiecutter import hooks, utils, exceptions
 
 
 def make_test_repo(name):
-    """Helper function which is called in the test setup methods."""
+    """Create test repository for test setup methods."""
     hook_dir = os.path.join(name, 'hooks')
     template = os.path.join(name, 'input{{hooks}}')
     os.mkdir(name)
diff --git a/tests/test_utils.py b/tests/test_utils.py
index 8504e6b..5d61207 100644
--- a/tests/test_utils.py
+++ b/tests/test_utils.py
@@ -16,9 +16,7 @@ from cookiecutter import utils
 
 
 def make_readonly(path):
-    """Helper function that is called in the tests to change the access
-    permissions of the given file.
-    """
+    """Change the access permissions to readonly for a given file."""
     mode = os.stat(path).st_mode
     os.chmod(path, mode & ~stat.S_IWRITE)
 
diff --git a/tests/vcs/test_clone.py b/tests/vcs/test_clone.py
index 5f77eb9..da9ab17 100644
--- a/tests/vcs/test_clone.py
+++ b/tests/vcs/test_clone.py
@@ -9,7 +9,7 @@ from cookiecutter import exceptions, vcs
 
 @pytest.fixture
 def clone_dir(tmpdir):
-    """Simulates creation of a directory called `clone_dir` inside of `tmpdir`.
+    """Simulate creation of a directory called `clone_dir` inside of `tmpdir`.
     Returns a str to said directory.
     """
     return str(tmpdir.mkdir('clone_dir'))
