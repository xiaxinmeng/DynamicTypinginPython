commit b889a920fd632ed378f07cc785e4c9cf78fb459e
Author: michaeljoseph <michaeljoseph+github@gmail.com>
Date:   Sun Jul 31 22:52:10 2016 +0200

    Rename valid_repository => repository_has_cookiecutter_json

diff --git a/cookiecutter/repository.py b/cookiecutter/repository.py
index 996ce75..e7ec4fc 100644
--- a/cookiecutter/repository.py
+++ b/cookiecutter/repository.py
@@ -41,8 +41,8 @@ def expand_abbreviations(template, abbreviations):
     return template
 
 
-def valid_repository(repo_directory):
-    """Determines if `repo_directory` is a valid cookiecutter.
+def repository_has_cookiecutter_json(repo_directory):
+    """Determines if `repo_directory` contains a `cookiecutter.json` file.
 
     :param repo_directory: The candidate repository directory.
     :return: True if the `repo_directory` is valid, else False.
@@ -88,9 +88,10 @@ def determine_repo_dir(template, abbreviations, clone_to_dir, checkout,
         # cookiecutters_dir
         repo_dir = template
 
-    if not valid_repository(repo_dir):
-        raise RepositoryNotFound(
-            'The repository {} could not be located or does not contain '
-            'a "cookiecutter.json" file.'.format(repo_dir)
-        )
-    return repo_dir
+    if repository_has_cookiecutter_json(repo_dir):
+        return repo_dir
+
+    raise RepositoryNotFound(
+        'The repository {} could not be located or does not contain '
+        'a "cookiecutter.json" file.'.format(repo_dir)
+    )
diff --git a/tests/vcs/test_valid_repository.py b/tests/repository/test_repository_has_cookiecutter_json.py
similarity index 56%
rename from tests/vcs/test_valid_repository.py
rename to tests/repository/test_repository_has_cookiecutter_json.py
index 669bc1f..5d53889 100644
--- a/tests/vcs/test_valid_repository.py
+++ b/tests/repository/test_repository_has_cookiecutter_json.py
@@ -1,11 +1,11 @@
 # -*- coding: utf-8 -*-
-from cookiecutter.repository import valid_repository
+from cookiecutter.repository import repository_has_cookiecutter_json
 
 import pytest
 
 
 def test_valid_repository():
-    assert valid_repository('tests/fake-repo')
+    assert repository_has_cookiecutter_json('tests/fake-repo')
 
 
 @pytest.fixture(params=[
@@ -17,4 +17,4 @@ def invalid_repository(request):
 
 
 def test_invalid_repository(invalid_repository):
-    assert not valid_repository(invalid_repository)
+    assert not repository_has_cookiecutter_json(invalid_repository)
