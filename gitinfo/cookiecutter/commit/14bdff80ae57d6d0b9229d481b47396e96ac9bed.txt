commit 14bdff80ae57d6d0b9229d481b47396e96ac9bed
Author: Raphael Pierzina <raphael@hackebrot.de>
Date:   Thu Apr 7 19:49:11 2016 +0100

    Implement parametrized tests for vcs.is_vcs_installed

diff --git a/tests/test_is_vcs_installed.py b/tests/test_is_vcs_installed.py
deleted file mode 100644
index 844d904..0000000
--- a/tests/test_is_vcs_installed.py
+++ /dev/null
@@ -1,17 +0,0 @@
-#!/usr/bin/env python
-# -*- coding: utf-8 -*-
-
-"""
-test_is_vcs_installed
----------------------
-"""
-
-from cookiecutter import vcs
-
-
-def test_existing_repo_type():
-    assert vcs.is_vcs_installed("git")
-
-
-def test_non_existing_repo_type():
-    assert not vcs.is_vcs_installed("stringthatisntashellcommand")
diff --git a/tests/test_vcs.py b/tests/test_vcs.py
index b34c0f1..231a6e5 100644
--- a/tests/test_vcs.py
+++ b/tests/test_vcs.py
@@ -128,6 +128,21 @@ def test_clone_should_raise_if_vcs_not_installed(mocker, clone_dir):
         vcs.clone(repo_url, clone_to_dir=clone_dir)
 
 
+@pytest.mark.parametrize('which_return, result', [
+    ('', False),
+    (None, False),
+    (False, False),
+    ('/usr/local/bin/git', True),
+])
+def test_is_vcs_installed(mocker, which_return, result):
+    mocker.patch(
+        'cookiecutter.vcs.which',
+        autospec=True,
+        return_value=which_return
+    )
+    assert vcs.is_vcs_installed('git') == result
+
+
 @pytest.mark.parametrize('repo_type, repo_url, repo_name', [
     ('git', 'https://github.com/hello/world.git', 'world'),
     ('hg', 'https://bitbucket.org/foo/bar', 'bar'),
