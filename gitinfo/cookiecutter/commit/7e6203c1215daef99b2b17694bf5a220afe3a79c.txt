commit 7e6203c1215daef99b2b17694bf5a220afe3a79c
Author: maiksensi <maiksensi@gmail.com>
Date:   Mon Nov 23 12:45:32 2015 +0100

    add correct ca-bundle path to git
    
    When GIT is invoked within a new virtualenv
    it fails as it cant find the correct ca-bundle.
    To ensure it finds the right file we set the
    configuration beforehand.

diff --git a/appveyor.yml b/appveyor.yml
index eaad642..a2b5a93 100644
--- a/appveyor.yml
+++ b/appveyor.yml
@@ -29,6 +29,7 @@ environment:
 
 
 init:
+  - "git config --system http.sslcainfo \"C:\\Program Files\\Git\\mingw64\\ssl\\certs\\ca-bundle.crt\""
   - "%PYTHON%/python -V"
   - "%PYTHON%/python -c \"import struct;print(8 * struct.calcsize(\'P\'))\""
 
