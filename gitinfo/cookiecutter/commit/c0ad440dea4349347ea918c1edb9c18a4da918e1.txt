commit c0ad440dea4349347ea918c1edb9c18a4da918e1
Author: Audrey Roy <audreyr@gmail.com>
Date:   Sun Jul 14 22:44:06 2013 +0200

    Add tests.

diff --git a/setup.py b/setup.py
index 744f10c..5605fcb 100755
--- a/setup.py
+++ b/setup.py
@@ -53,4 +53,5 @@ setup(
         'Programming Language :: Python :: 3.3',
     ),
     keywords='cookiecutter, Python, projects, project templates, Jinja2, project directory, setup.py, package, packaging',
+    test_suite='tests',
 )
diff --git a/tests/__init__.py b/tests/__init__.py
new file mode 100644
index 0000000..e69de29
diff --git a/tests/input/simple.txt b/tests/input/simple.txt
new file mode 100644
index 0000000..a459ce5
--- /dev/null
+++ b/tests/input/simple.txt
@@ -0,0 +1 @@
+I eat {{ food }}
\ No newline at end of file
diff --git a/tests/input2/something.txt b/tests/input2/something.txt
new file mode 100644
index 0000000..3892c5b
--- /dev/null
+++ b/tests/input2/something.txt
@@ -0,0 +1,3 @@
+Hi!
+My name is {{ stuff.full_name }}.
+It is {{ stuff.year }}.
\ No newline at end of file
diff --git a/tests/json/test.json b/tests/json/test.json
new file mode 100644
index 0000000..3c31e3f
--- /dev/null
+++ b/tests/json/test.json
@@ -0,0 +1 @@
+{"1": 2}
\ No newline at end of file
diff --git a/tests/json2/stuff.json b/tests/json2/stuff.json
new file mode 100644
index 0000000..4edc766
--- /dev/null
+++ b/tests/json2/stuff.json
@@ -0,0 +1,4 @@
+{
+	"full_name": "Audrey Greenfeld",
+	"year": "2014"
+}
\ No newline at end of file
diff --git a/tests/test_cookiecutter.py b/tests/test_cookiecutter.py
new file mode 100755
index 0000000..387903e
--- /dev/null
+++ b/tests/test_cookiecutter.py
@@ -0,0 +1,63 @@
+#!/usr/bin/env python
+# -*- coding: utf-8 -*-
+
+"""
+test_cookiecutter
+-----------------
+
+Tests for `cookiecutter` module.
+"""
+
+import os
+import shutil
+import unittest
+
+from cookiecutter import cookiecutter
+
+
+class TestCookiecutter(unittest.TestCase):
+
+    def test_make_sure_path_exists(self):
+        self.assertTrue(cookiecutter.make_sure_path_exists('/usr/'))
+        self.assertTrue(cookiecutter.make_sure_path_exists('tests/output/'))
+        self.assertFalse(
+            cookiecutter.make_sure_path_exists(
+                '/this-dir-does-not-exist-and-cant-be-created/'
+            )
+        )
+
+    def test_generate_files(self):
+        cookiecutter.generate_files(
+            context={'food': 'pizza'},
+            input_dir='tests/input',
+            output_dir='tests/output'
+        )
+        self.assertTrue(os.path.isfile('tests/output/simple.txt'))
+        simple_text = open('tests/output/simple.txt', 'rt').read()
+        self.assertEqual(simple_text, 'I eat pizza')
+
+    def test_generate_context(self):
+        context = cookiecutter.generate_context(json_dir='tests/json')
+        self.assertEqual(context, {"test": {"1": 2}})
+
+    def test_output_folder(self):
+        context = cookiecutter.generate_context(json_dir='tests/json2')
+        cookiecutter.generate_files(
+            context=context,
+            input_dir='tests/input2',
+            output_dir='tests/output2'
+        )
+        something = """Hi!
+My name is Audrey Greenfeld.
+It is 2014."""
+        something2 = open('tests/output2/something.txt').read()
+        self.assertEqual(something, something2)
+
+    def tearDown(self):
+        if os.path.exists('tests/output'):
+            shutil.rmtree('tests/output')
+        if os.path.exists('tests/output2'):
+            shutil.rmtree('tests/output2')
+
+if __name__ == '__main__':
+    unittest.main()
