commit 43c0ccfbdc0d7560aacf4ee135415497e48bdfe9
Author: Andrey Shpak <insspb@users.noreply.github.com>
Date:   Sun Dec 22 11:45:43 2019 +0300

    Fix tox dependencies versions for tests (#1257)
    
    Fix tox dependencies versions for tests

diff --git a/tox.ini b/tox.ini
index 2fa3a52..6611298 100644
--- a/tox.ini
+++ b/tox.ini
@@ -7,24 +7,27 @@ envlist =
     py37
     py38
     pypy
-minversion = 3.13.2
+minversion = 3.14.2
 requires =
     # https://github.com/tox-dev/tox/issues/765
-    virtualenv >= 16.7.4
-    pip >= 19.2.3
+    virtualenv >= 16.7.9
+    pip >= 19.3.1
 
 [testenv]
 passenv =
     LC_ALL
     LANG
     HOME
-commands = pytest --cov=cookiecutter {posargs:tests}
+commands =
+    pip install -e .
+    pytest --cov=cookiecutter {posargs:tests}
 deps = -rtest_requirements.txt
+skip_install = true
 
 [testenv:lint]
 commands =
     python -m pre_commit run {posargs:--all}
-deps = pre-commit>=1.17.0
+deps = pre-commit>=1.20.0
 skip_install = true
 usedevelop = false
 
