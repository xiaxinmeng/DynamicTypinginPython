commit 2eb393e9509b8c311f99e4c163434a864b327025
Author: Tzu-ping Chung <uranusjr@gmail.com>
Date:   Sun Jul 6 21:24:08 2014 +0800

    Do not convert input prompt to bytes
    
    Python 3 input() takes str objects. Encoding the prompt makes it
    a bytestring, resulting the b'' format.

diff --git a/cookiecutter/prompt.py b/cookiecutter/prompt.py
index cc29d58..1e27215 100755
--- a/cookiecutter/prompt.py
+++ b/cookiecutter/prompt.py
@@ -30,7 +30,7 @@ def prompt_for_config(context):
         prompt = "{0} (default is \"{1}\")? ".format(key, val)
 
         if PY3:
-            new_val = input(prompt.encode('utf-8'))
+            new_val = input(prompt)
         else:
             new_val = input(prompt.encode('utf-8')).decode('utf-8')
 
