commit 9d57f85837d577e733cf1a45e560fc763a62be1f
Author: Raphael Pierzina <raphael@hackebrot.de>
Date:   Sun Nov 22 02:28:44 2015 +0100

    Implement a custom exception type for undefined variable errors

diff --git a/cookiecutter/exceptions.py b/cookiecutter/exceptions.py
index 415f99e..0179625 100755
--- a/cookiecutter/exceptions.py
+++ b/cookiecutter/exceptions.py
@@ -87,3 +87,20 @@ class FailedHookException(CookiecutterException):
     """
     Raised when a hook script fails
     """
+
+
+class UndefinedVariableInTemplate(CookiecutterException):
+    """Raised when a template uses a variable which is not defined in the
+    context.
+    """
+    def __init__(self, message, error, context):
+        self.message = message
+        self.error = error
+        self.context = context
+
+    def __str__(self):
+        return (
+            "{self.message}. "
+            "Error message: {self.error.message}. "
+            "Context: {self.context}"
+        ).format(**locals())
