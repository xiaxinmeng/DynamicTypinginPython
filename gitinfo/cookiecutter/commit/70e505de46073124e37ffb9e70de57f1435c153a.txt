commit 70e505de46073124e37ffb9e70de57f1435c153a
Author: Raphael Pierzina <raphael@hackebrot.de>
Date:   Sat Aug 8 00:49:31 2015 +0200

    Implement a test to make sure replay.load raises on invalid name

diff --git a/tests/test_replay.py b/tests/test_replay.py
index 3c09713..ffdc775 100644
--- a/tests/test_replay.py
+++ b/tests/test_replay.py
@@ -109,3 +109,9 @@ def test_run_json_dump(
     with open(replay_file, 'r') as f:
         dumped_context = json.load(f)
         assert dumped_context == context
+
+
+def test_load_value_error_if_no_template_name(context):
+    """Test that replay.load raises if the tempate_name is not a valid str."""
+    with pytest.raises(ValueError):
+        replay.load(None)
