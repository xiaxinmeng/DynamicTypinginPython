commit d5e36c3ab88083a0ff371a05ab49413f8257c109
Author: Raphael Pierzina <raphael@hackebrot.de>
Date:   Sun May 24 01:35:58 2015 +0200

    Implement read_user_yes_no in prompt.py

diff --git a/cookiecutter/prompt.py b/cookiecutter/prompt.py
index 0f07342..e2bb331 100755
--- a/cookiecutter/prompt.py
+++ b/cookiecutter/prompt.py
@@ -27,6 +27,22 @@ def read_user_variable(var_name, default_value):
     return click.prompt(var_name, default=default_value)
 
 
+def read_user_yes_no(question, default_value):
+    """Prompt the user to reply with 'yes' or 'no' (or equivalent values).
+
+    Note:
+      Possible choices are 'true', '1', 'yes', 'y' or 'false', '0', 'no', 'n'
+
+    :param str question: Question to the user
+    :param default_value: Value that will be returned if no input happens
+    """
+    return click.prompt(
+        question,
+        default=default_value,
+        type=click.BOOL
+    )
+
+
 def read_response(prompt=''):
     """Prompt the user and return the entered value or an empty string.
 
