commit b18c079df7130ac200b35645a5bf7c821722dc08
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Wed Mar 16 13:06:53 2016 -0700

    Guard our wsgi wrapper where another middleware tampers with the HttpResponse object
    
    This specifically happens for New Relic

diff --git a/src/sentry/wsgi.py b/src/sentry/wsgi.py
index b32ffa77f0..8f66a212c1 100644
--- a/src/sentry/wsgi.py
+++ b/src/sentry/wsgi.py
@@ -38,7 +38,7 @@ class FileWrapperWSGIHandler(WSGIHandler):
     we upgraded, this wouldn't be relevant anymore."""
     def __call__(self, environ, start_response):
         response = super(FileWrapperWSGIHandler, self).__call__(environ, start_response)
-        if response.streaming:
+        if hasattr(response, 'streaming') and response.streaming:
             try:
                 response = environ['wsgi.file_wrapper'](response.streaming_content)
             except KeyError:
