commit 733452d58df4435b3928b72f6e5cff373120cc30
Author: David Cramer <dcramer@gmail.com>
Date:   Sun Sep 15 15:43:39 2013 +0900

    Dont hard error outside of test env for missing data

diff --git a/src/sentry/db/models/fields/node.py b/src/sentry/db/models/fields/node.py
index 9079b9e433..25bdb3c041 100644
--- a/src/sentry/db/models/fields/node.py
+++ b/src/sentry/db/models/fields/node.py
@@ -10,6 +10,7 @@ from __future__ import absolute_import
 
 import collections
 import logging
+import warnings
 
 from django.db import models
 
@@ -53,9 +54,16 @@ class NodeData(collections.MutableMapping):
 
     @memoize
     def data(self):
-        if self._node_data is None:
-            raise Exception('Must populate node data first')
-        return self._node_data
+        from sentry import app
+
+        if self._node_data is not None:
+            return self._node_data
+
+        elif self.id:
+            warnings.warn('You should populate node data before accessing it.')
+            return app.nodestore.get(self.id) or {}
+
+        return {}
 
     def bind_node_data(self, data):
         self._node_data = data
