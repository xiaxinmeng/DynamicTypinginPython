commit 0e4a0bb68b1bafeccdc42345ca323783c722ef82
Author: Billy Vong <billyvg@gmail.com>
Date:   Wed Aug 23 18:14:44 2017 -0700

    ui: add permalink to events in Issue Details
    
    Closes #5777

diff --git a/src/sentry/static/sentry/app/views/groupDetails/eventToolbar.jsx b/src/sentry/static/sentry/app/views/groupDetails/eventToolbar.jsx
index a9166eaabf..cb9483e345 100644
--- a/src/sentry/static/sentry/app/views/groupDetails/eventToolbar.jsx
+++ b/src/sentry/static/sentry/app/views/groupDetails/eventToolbar.jsx
@@ -146,7 +146,15 @@ let GroupEventToolbar = React.createClass({
             {eventNavNodes}
           </div>
         </div>
-        <h4>{t('Event')} <span className="event-id">{evt.eventID}</span></h4>
+        <h4>
+          {t('Event')}
+          {' '}
+          <Link
+            to={`/${orgId}/${projectId}/issues/${groupId}/events/${evt.id}/`}
+            className="event-id">
+            {evt.eventID}
+          </Link>
+        </h4>
         <span>
           {/* use a key here to force removal of tooltip parent - fixes #3341 */}
           <span className="tip" data-title={this.getDateTooltip()} key={evt.id}>
