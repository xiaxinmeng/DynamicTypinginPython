commit 4bf73ccb4f8bdca80b67b6702442cbed7046196e
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Sep 15 21:50:32 2015 -0700

    Tags are a list of a strings for Datadog

diff --git a/src/sentry/metrics/datadog.py b/src/sentry/metrics/datadog.py
index 1578cd49b3..e4b2890c25 100644
--- a/src/sentry/metrics/datadog.py
+++ b/src/sentry/metrics/datadog.py
@@ -37,6 +37,7 @@ class DatadogMetricsBackend(MetricsBackend):
             tags.update(self.tags)
         if instance:
             tags['instance'] = instance
+        tags = ['{}:{}'.format(*i) for i in tags.items()]
         self.stats.increment(self._get_key(key), amount,
                              sample_rate=sample_rate,
                              tags=tags,
@@ -49,6 +50,7 @@ class DatadogMetricsBackend(MetricsBackend):
             tags.update(self.tags)
         if instance:
             tags['instance'] = instance
+        tags = ['{}:{}'.format(*i) for i in tags.items()]
         self.stats.timing(self._get_key(key), value, sample_rate=sample_rate,
                           tags=tags,
                           host=self.host)
