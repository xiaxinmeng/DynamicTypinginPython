commit 257fe2dc8886fdb8d28d6184b7a83c665dd3a025
Author: ckj <chriskjennings@gmail.com>
Date:   Tue Oct 13 13:52:09 2015 -0700

    Clean up tag list

diff --git a/src/sentry/templates/sentry/emails/base.html b/src/sentry/templates/sentry/emails/base.html
index 0a9cdbe6e0..3437fbbcf1 100644
--- a/src/sentry/templates/sentry/emails/base.html
+++ b/src/sentry/templates/sentry/emails/base.html
@@ -33,7 +33,7 @@
     white-space: pre;
     white-space: pre-wrap;
     margin-bottom: 15px;
-    background: #F6F7F8;
+    background: #F4F5F6;
     color: #3D4649;
     padding: 15px;
     border-radius: 4px;
@@ -166,31 +166,31 @@
   .interface .title {
     margin-bottom: 15px;
   }
-  pre.message {
-    margin-bottom: 30px;
+  .inner pre.message {
+    margin: 0 0 30px !important;
   }
   .tag-list {
     list-style: none;
-    margin: 0 0 30px;
+    margin: 0 0 20px;
     padding: 0;
   }
   .tag-list li {
-    display: inline;
-    background: #eee;
-    padding: 2px 5px;
-    margin-right: 10px;
-    margin-bottom: 5px;
-    border-radius: 4px;
+    display: inline-block;
+    padding: 5px 10px 6px;
+    margin-right: 5px;
+    margin-bottom: 10px;
+    border-radius: 3px;
+    background: #F4F5F6;
   }
   .tag-list li strong {
     font-weight: 200;
   }
 
   .via {
-    border: 1px solid #ddd;
-    background: #eee;
+    border: 1px solid #CFD9DE;
     text-align: center;
     padding: 15px;
+    border-radius: 3px;
   }
 
   strong {
diff --git a/src/sentry/templates/sentry/emails/error.html b/src/sentry/templates/sentry/emails/error.html
index 3d2979550a..b66e4beab8 100644
--- a/src/sentry/templates/sentry/emails/error.html
+++ b/src/sentry/templates/sentry/emails/error.html
@@ -20,17 +20,19 @@
 
     <pre class="message">{{ event.message }}</pre>
 
-    <h3>Tags</h3>
+    {% if tags %}
+      <h3>Tags</h3>
 
-    <ul class="tag-list">
-    {% for tag_key, tag_value in tags %}
+      <ul class="tag-list">
+      {% for tag_key, tag_value in tags %}
         <li>
             <strong>{{ tag_key }}</strong>
             <em>=</em>
             <span><a href="{% absolute_uri project_link %}?{{ tag_key }}={{ tag_value }}">{{ tag_value }}</a> {% if tag_value|is_url %}<a href="{{ tag_value }}" class="icon-share"></a>{% endif %}</span>
         </li>
-    {% endfor %}
-    </ul>
+      {% endfor %}
+      </ul>
+    {% endif %}
 
     {% for label, html, _ in interfaces %}
     <div class="interface">
