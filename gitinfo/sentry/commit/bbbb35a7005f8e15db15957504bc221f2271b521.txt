commit bbbb35a7005f8e15db15957504bc221f2271b521
Author: Ben Vinegar <ben@benv.ca>
Date:   Thu Apr 28 15:38:40 2016 -0700

    Some minor query breadcrumb style changes ...
    
    * Drop "Query:" title
    * Make query keywords dark grey (same color as other crumb titles)
    * Make loupe icon green (same as http_request, "external service request")
    
    /cc @getsentry/ui

diff --git a/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbComponents/query.jsx b/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbComponents/query.jsx
index ce95466ba8..05e10ea827 100644
--- a/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbComponents/query.jsx
+++ b/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbComponents/query.jsx
@@ -7,7 +7,7 @@ import Duration from '../../../duration';
 function summarizeSqlQuery(sql) {
   // select
   let match;
- 
+
   match = sql.match(/^\s*(select\s+(?:\s+all\b|distinct\b)?)(.*?)\bfrom\s+["`]?([^\s,."`]+)/im);
   if (match) {
     let selectors = match[2].split(/,/g);
@@ -34,7 +34,7 @@ function summarizeSqlQuery(sql) {
       </span>
     );
   }
- 
+
   match = sql.match(/^\s*(\S+)/);
   if (match) {
     return (
@@ -125,7 +125,6 @@ const QueryCrumbComponent = React.createClass({
   render() {
     return (
       <p>
-        <strong className="preamble">Query:</strong>
         {this.renderQuery()}
         {this.renderTiming()}
         <Classifier value={this.props.data.classifier} title="%s query" />
diff --git a/src/sentry/static/sentry/less/group-detail.less b/src/sentry/static/sentry/less/group-detail.less
index 7e3236db8e..af4e2606bb 100644
--- a/src/sentry/static/sentry/less/group-detail.less
+++ b/src/sentry/static/sentry/less/group-detail.less
@@ -1646,12 +1646,12 @@ ul.crumbs {
 
     &.crumb-query {
       .icon-container {
-        border-color: lighten(@purple, 10);
+        border-color: @green-dark;
 
         .icon {
           top: 5px;
           left: 5.5px;
-          color: darken(@purple, 5);
+          color: @green-dark;
           &:before {content: "\e601";}
         }
       }
@@ -1667,6 +1667,7 @@ ul.crumbs {
       }
 
       code {
+        padding-left: 0;
         white-space: pre-wrap;
         word-wrap: break-word;
         background: transparent;
@@ -1695,15 +1696,15 @@ ul.crumbs {
 
         span.keyword {
           font-weight: bold;
-          color: darken(@blue-dark, 10);
+          color: @gray-darker;
         }
 
         span.param {
-          color: darken(@blue-dark, 10);
+          color: @gray-darker;
           font-weight: bold;
           padding: 2px 5px;
           margin: -2px -3px;
-          background: lighten(@blue-light, 25);
+          background: @gray-lightest;
           border-radius: 2px;
         }
       }
