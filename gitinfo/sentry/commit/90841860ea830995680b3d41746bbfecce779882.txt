commit 90841860ea830995680b3d41746bbfecce779882
Author: Lyn Nagara <lyn.nagara@gmail.com>
Date:   Thu May 23 11:45:29 2019 -0700

    test(events-v2): Fix events not showing up in Percy (#13361)

diff --git a/tests/acceptance/test_organization_events_v2.py b/tests/acceptance/test_organization_events_v2.py
index 613beca667..c313dce4ba 100644
--- a/tests/acceptance/test_organization_events_v2.py
+++ b/tests/acceptance/test_organization_events_v2.py
@@ -1,9 +1,12 @@
 from __future__ import absolute_import
 
-from datetime import timedelta
+from datetime import datetime, timedelta
 from django.utils import timezone
+import pytz
+from mock import patch
 from sentry.testutils import AcceptanceTestCase, SnubaTestCase
 
+
 FEATURE_NAME = 'organizations:events-v2'
 
 
@@ -34,7 +37,9 @@ class OrganizationEventsTest(AcceptanceTestCase, SnubaTestCase):
             self.browser.wait_until_not('.loading-indicator')
             self.browser.snapshot('events-v2 - all events empty state')
 
-    def test_all_events(self):
+    @patch('django.utils.timezone.now')
+    def test_all_events(self, mock_now):
+        mock_now.return_value = datetime.utcnow().replace(tzinfo=pytz.utc)
         min_ago = (timezone.now() - timedelta(minutes=1)).isoformat()[:19]
         self.store_event(
             data={
