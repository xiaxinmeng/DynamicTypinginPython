commit a511b1af3af80006a934c9e395bd64db09bd0ee1
Author: Armin Ronacher <armin.ronacher@active-4.com>
Date:   Wed Sep 9 15:53:59 2015 -0500

    Added basic bar charts into the groupTagValues page

diff --git a/src/sentry/static/sentry/app/views/groupTagValues.jsx b/src/sentry/static/sentry/app/views/groupTagValues.jsx
index ee221d923f..c7a2e68c77 100644
--- a/src/sentry/static/sentry/app/views/groupTagValues.jsx
+++ b/src/sentry/static/sentry/app/views/groupTagValues.jsx
@@ -93,11 +93,14 @@ var GroupTagValues = React.createClass({
     var router = this.context.router;
     var tagKey = this.state.tagKey;
     var children = this.state.tagValueList.map((tagValue, tagValueIdx) => {
-      var pct = percent(tagValue.count, tagKey.totalValues);
+      var pct = Math.round(percent(tagValue.count, tagKey.totalValues) * 100) / 100;
       var params = router.getCurrentParams();
       return (
         <tr key={tagValueIdx}>
-        <td>{pct}%</td>
+          <td className="bar-cell">
+            <span className="bar" style={{width: pct + '%'}}></span>
+            <span className="label">{pct}%</span>
+          </td>
           <td>
             <Router.Link
                 to="stream"
diff --git a/src/sentry/static/sentry/less/misc.less b/src/sentry/static/sentry/less/misc.less
index 5dc3e8f324..1e26ec5cd5 100644
--- a/src/sentry/static/sentry/less/misc.less
+++ b/src/sentry/static/sentry/less/misc.less
@@ -31,3 +31,16 @@ img {
 .no-border {
 	border: 0 !important;
 }
+
+table .bar-cell {
+    position: relative;
+}
+
+table .bar-cell span.bar {
+    position: absolute;
+    left: 0;
+    top: 0;
+    display: block;
+    height: 3px;
+    background: @blue;
+}
