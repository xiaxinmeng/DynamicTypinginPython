commit 8ec022ec40ec800b84c0d40fef182d0829664aa8
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Feb 7 17:59:39 2013 -0800

    Dont pull in context without pre_context or post_context being set

diff --git a/src/sentry/interfaces.py b/src/sentry/interfaces.py
index dcbb0d420e..bb0bdf0f54 100644
--- a/src/sentry/interfaces.py
+++ b/src/sentry/interfaces.py
@@ -390,7 +390,9 @@ class Stacktrace(Interface):
         system_frames = 0
         frames = []
         for frame in self.frames:
-            if frame.get('context_line') and frame.get('lineno') is not None:
+            if (frame.get('context_line') and frame.get('lineno') is not None
+                and (frame.get('pre_context') or frame.get('post_context'))):
+
                 context = get_context(
                     lineno=frame['lineno'],
                     context_line=frame['context_line'],
