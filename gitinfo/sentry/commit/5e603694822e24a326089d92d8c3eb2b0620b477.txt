commit 5e603694822e24a326089d92d8c3eb2b0620b477
Author: James Cunningham <cunninghamjt09@gmail.com>
Date:   Wed Jun 15 14:31:03 2016 -0700

    Use the right variable for emails being queued. (#3494)
    
    * Don't log empty emails.
    
    * Use the right variable.

diff --git a/src/sentry/utils/email.py b/src/sentry/utils/email.py
index 463fb77a2d..2618416ad7 100644
--- a/src/sentry/utils/email.py
+++ b/src/sentry/utils/email.py
@@ -379,9 +379,9 @@ class MessageBuilder(object):
             )
             logger.bind(message_id=message.extra_headers['Message-Id'])
             if fmt == LoggingFormat.HUMAN:
-                log_mail_queued(to=self.format_to(to))
+                log_mail_queued(to=self.format_to(message.to))
             elif fmt == LoggingFormat.MACHINE:
-                for recipient in to:
+                for recipient in message.to:
                     log_mail_queued(to=recipient)
 
 
