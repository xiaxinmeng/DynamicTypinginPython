commit 1a9267c3dd4488d1930ea7965462e830c23f034f
Author: David Cramer <dcramer@gmail.com>
Date:   Fri Oct 7 13:52:48 2016 -0700

    [notifications] improve visuals (#4301)
    
    * [notifications] improve visuals
    
    - clean up select2 defaults
    - compact styles for fine tuning
    - improve help text
    
    * Purple highlighted results

diff --git a/src/sentry/static/sentry/less/forms.less b/src/sentry/static/sentry/less/forms.less
index f2a75fbec0..7d5ef4598b 100644
--- a/src/sentry/static/sentry/less/forms.less
+++ b/src/sentry/static/sentry/less/forms.less
@@ -57,6 +57,7 @@
   font-weight: normal;
   color: @gray;
   font-size: 90%;
+  margin-bottom: 0;
 }
 
 textarea.form-control {
diff --git a/src/sentry/static/sentry/less/includes/select2.less b/src/sentry/static/sentry/less/includes/select2.less
index aa1c9e67b8..2539db4da1 100644
--- a/src/sentry/static/sentry/less/includes/select2.less
+++ b/src/sentry/static/sentry/less/includes/select2.less
@@ -9,8 +9,6 @@
   .form-control;
   display: inline-block;
   padding: 4px 10px 3px;
-  top: -1px;
-  box-shadow: 0 1px 1px rgba(0, 0, 0, .05);
 }
 
 .select2-container,
@@ -143,13 +141,13 @@ html[dir="rtl"] .select2-container .select2-choice > .select2-chosen {
 
 .select2-drop.select2-drop-above {
   margin-top: 1px;
-  border-top: 1px solid @trim;
+  border-top: 1px solid lighten(@gray-lighter, 10);
   border-bottom: 0;
   border-radius: 3px 3px 0 0;
 }
 
 .select2-drop-active {
-  border: 1px solid @trim;
+  border: 1px solid lighten(@gray-lighter, 10);
   border-top: none;
 }
 
@@ -232,7 +230,7 @@ html[dir="rtl"] .select2-container .select2-choice .select2-arrow b {
   font-family: sans-serif;
   font-size: 1em;
 
-  border: 1px solid @trim;
+  border: 1px solid lighten(@gray-lighter, 10);
   border-radius: 2px;
 
   -webkit-box-shadow: none;
@@ -355,7 +353,7 @@ html[dir="rtl"] .select2-results {
 .select2-results-dept-7 .select2-result-label { padding-left: 120px }
 
 .select2-results .select2-highlighted {
-  background: @blue;
+  background: @purple;
   color: #fff;
 }
 
diff --git a/src/sentry/static/sentry/less/type.less b/src/sentry/static/sentry/less/type.less
index 43b67d1bd7..bb390607b6 100644
--- a/src/sentry/static/sentry/less/type.less
+++ b/src/sentry/static/sentry/less/type.less
@@ -71,6 +71,10 @@ ul, ol, table {
   margin: 0 0 20px;
 }
 
+ul, ol {
+  padding-left: 20px;
+}
+
 small {
   font-size: 0.9em;
 }
diff --git a/src/sentry/templates/sentry/account/notifications.html b/src/sentry/templates/sentry/account/notifications.html
index 828c8eccf1..ac26ab80da 100644
--- a/src/sentry/templates/sentry/account/notifications.html
+++ b/src/sentry/templates/sentry/account/notifications.html
@@ -21,14 +21,39 @@
     td label small {
         font-weight: normal;
     }
+    table.notifications-fine-tuning {
+      margin-bottom: 20px;
+    }
+    table.notifications-fine-tuning > tbody > tr > td,
+    table.notifications-fine-tuning > thead > tr > th {
+      padding: 8px 10px;
+      font-size: 14px;
+    }
+    table.notifications-fine-tuning > tbody > tr > td.route {
+      padding: 0;
+    }
+    table.notifications-fine-tuning > tbody > tr > td.route .form-control {
+      padding: 8px 10px;
+      border: 1px solid #fff;
+      border-width: 0 1px;
+      box-shadow: none;
+      top: 0;
+      border-radius: 0;
+    }
+    table.notifications-fine-tuning > tbody > tr > td.route .form-control .select2-choice .select2-arrow {
+      right: -3px;
+    }
+    table.notifications-fine-tuning > tbody > tr > td.route .form-control .select2-choice > .select2-chosen {
+      margin-right: 20px;
+    }
+    table.notifications-fine-tuning > tbody > tr > td.route .form-control.select2-dropdown-open {
+      border-color: #c9c0d1;
+    }
+    td .form-group { margin-bottom: 0; }
     </style>
     <form action="" method="post" class="">
         {% csrf_token %}
 
-        <h4>{% trans "General" %}</h4>
-
-        <hr />
-
         <h4>{% trans "Alerts" %}</h4>
 
         <p>Alerts are generated based on a project's rules, defined in <strong>[Project] &raquo; Settings &raquo; Rules</strong>.</p>
@@ -39,14 +64,21 @@
 
         <h4>{% trans "Workflow" %}</h4>
 
-        <p>{% blocktrans %}Workflow notifications are separate from alerts, and are generated for things like comments and issue updates. You may subscribe (or unsubscribe) from individual issues on their respective pages.{% endblocktrans %}</p>
+        <p>{% blocktrans %}Workflow notifications are separate from alerts and are generated for issue updates, such as:{% endblocktrans %}</p>
 
-        {{ settings_form.workflow_notifications|as_crispy_field }}
+        <ul>
+          <li>{% trans "Assignment" %}</li>
+          <li>{% trans "Comments" %}</li>
+          <li>{% trans "Regressions" %}</li>
+          <li>{% trans "Resolution" %}</li>
+        </ul>
 
-        <p class="help-text help-block">Note: You'll always receive notifications if you're explicitly participating on an issue.</p>
+        {{ settings_form.workflow_notifications|as_crispy_field }}
 
         {{ settings_form.self_notifications|as_crispy_field }}
 
+        <p>{% blocktrans %}You may subscribe (or unsubscribe) from individual issues on their respective pages.{% endblocktrans %}</p>
+
         <hr />
 
         <h4>{% trans "Weekly Reports" %}</h4>
@@ -59,7 +91,7 @@
 
         <h4>{% trans "Fine Tuning" %}</h4>
 
-        <p>Use the settings below to fine tune notification settings per-project.<br>Add or verify emails in the <a href="{% url "sentry-account-settings-emails" %}">Emails</a> Tab.</p>
+        <p>Use the settings below to fine tune notification settings per-project. You may add or verify additional email addresses via the <a href="{% url "sentry-account-settings-emails" %}">Emails</a> settings.</p>
 
         {% for project, form in project_forms %}
             {{ form|as_crispy_errors }}
@@ -71,21 +103,19 @@
                     </table>
                 {% endif %}
 
-                <h5 style="margin-top: 20px; font-weight: normal">{{ project.organization.name }}</h5>
-
-                <table class="table table-bordered table-striped" style="margin-bottom: 20px">
+                <table class="table notifications-fine-tuning">
                     <thead>
                         <tr>
                             <th>{% trans "Project" %}</th>
+                            <th style="width:300px;overflow:hidden;text-align:right">
+                              {% trans "Email Address" %}
+                            </th>
                             <th style="width:50px;text-align:center">
                               <a data-toggle="alert">Alerts</a>
                             </th>
                             <th style="width:50px;text-align:center">
                               <a data-toggle="workflow">Workflow</a>
                             </th>
-                            <th style="width:300px;overflow:hidden;text-align:center">
-                              {% trans "Email Address" %}
-                            </th>
                         </tr>
                     </thead>
                     <tbody>
@@ -94,13 +124,13 @@
               <td>
                 <a href="{% absolute_uri '/{}/{}/settings/alerts/' project.organization.slug project.slug %}">{{ project.organization.slug}} / {{ project.slug }}</a>
               </td>
-              <td style="text-align:center">{{ form.alert }}</td>
-              <td style="text-align:center">{{ form.workflow }}</td>
-              <td style="text-align:center">
+              <td style="text-align:right" class="route">
               {% with form.email as field %}
                 {{ field|as_crispy_field }}
               {% endwith %}
               </td>
+              <td style="text-align:center">{{ form.alert }}</td>
+              <td style="text-align:center">{{ form.workflow }}</td>
             </tr>
         {% endfor %}
         </tbody></table>
diff --git a/src/sentry/web/forms/accounts.py b/src/sentry/web/forms/accounts.py
index 36f1fa0d2c..32a072c60b 100644
--- a/src/sentry/web/forms/accounts.py
+++ b/src/sentry/web/forms/accounts.py
@@ -451,10 +451,12 @@ class NotificationSettingsForm(forms.Form):
     )
     workflow_notifications = forms.BooleanField(
         label=_('Receive updates for all issues by default'),
+        help_text=_('You\'ll always receive notifications if you\'re explicitly participating on an issue.'),
         required=False,
     )
     self_notifications = forms.BooleanField(
         label=_('Receive notifications about my own activity'),
+        help_text=_('Enable this if you wish to receive emails for your own actions, as well as others.'),
         required=False,
     )
 
