commit 95a1b33667600b3298b0d73df531e31ed7581cb0
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Jun 10 22:46:13 2014 -0700

    Remove unused logic from team settings

diff --git a/src/sentry/templates/sentry/teams/manage.html b/src/sentry/templates/sentry/teams/manage.html
index 5f8b5bd14a..9c4f5c554c 100644
--- a/src/sentry/templates/sentry/teams/manage.html
+++ b/src/sentry/templates/sentry/teams/manage.html
@@ -10,7 +10,6 @@
 {% block bodyclass %}{% endblock %}
 
 {% block main %}
-
   {% block inner %}
   <div class="box">
       <div class="box-header">
@@ -38,12 +37,11 @@
           </form>
       </div>
   </div>
-  {% endblock %}
 
   <script type="text/javascript">
       require(['config', 'app/init'], function(_, app){
           new app.TeamDetailsPage();
       });
   </script>
-
+  {% endblock %}
 {% endblock %}
diff --git a/src/sentry/web/frontend/teams.py b/src/sentry/web/frontend/teams.py
index 40693dc946..3e2a72d798 100644
--- a/src/sentry/web/frontend/teams.py
+++ b/src/sentry/web/frontend/teams.py
@@ -81,9 +81,7 @@ def manage_team(request, team):
     if result is False and not request.user.is_superuser:
         return HttpResponseRedirect(reverse('sentry'))
 
-    form = EditTeamForm(request.POST or None, initial={
-        'owner': team.owner,
-    }, instance=team)
+    form = EditTeamForm(request.POST or None, instance=team)
     if form.is_valid():
         team = form.save()
         messages.add_message(request, messages.SUCCESS,
@@ -93,7 +91,6 @@ def manage_team(request, team):
 
     context = csrf(request)
     context.update({
-        'can_remove_team': can_remove_team(request.user, team),
         'page': 'details',
         'form': form,
         'SUBSECTION': 'settings',
