commit 7e7af4e163846a9bde713c01daf65c900f153cb6
Author: Armin Ronacher <armin.ronacher@active-4.com>
Date:   Mon Feb 8 21:27:55 2016 -0800

    Properly enter and exit the driver

diff --git a/src/sentry/lang/native/plugin.py b/src/sentry/lang/native/plugin.py
index a30cccfa7e..2fc977de16 100644
--- a/src/sentry/lang/native/plugin.py
+++ b/src/sentry/lang/native/plugin.py
@@ -66,7 +66,8 @@ def preprocess_apple_crash_event(data):
     bt = None
     for thread in crash['threads']:
         if thread['crashed']:
-            bt = sym.symbolize_backtrace(thread['backtrace']['contents'])
+            with sym.driver:
+                bt = sym.symbolize_backtrace(thread['backtrace']['contents'])
 
     if bt is not None:
         inject_apple_backtrace(data, bt, crash.get('diagnosis'),
diff --git a/src/sentry/lang/native/symbolizer.py b/src/sentry/lang/native/symbolizer.py
index b025c580e1..2955481cc4 100644
--- a/src/sentry/lang/native/symbolizer.py
+++ b/src/sentry/lang/native/symbolizer.py
@@ -4,10 +4,8 @@ from symsynd.report import ReportSymbolizer
 from sentry.lang.native.dsymcache import dsymcache
 
 
-driver = Driver()
-
-
 def make_symbolizer(project, binary_images):
+    driver = Driver()
     dsym_path, loaded = dsymcache.fetch_dsyms(
         [x['uuid'] for x in binary_images])
     return ReportSymbolizer(driver, dsym_path, binary_images)
