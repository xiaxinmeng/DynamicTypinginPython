commit 1fa6a15c50aff8070ada45546ad0ac46bd28c086
Author: Jess MacQueen <jessmacqueen@gmail.com>
Date:   Fri Aug 3 14:12:19 2018 -0700

    ref(integrations): Remove link advertising old issue integrations

diff --git a/src/sentry/static/sentry/app/views/groupDetails/actions.jsx b/src/sentry/static/sentry/app/views/groupDetails/actions.jsx
index cc9b8c32ab..0a54a92b0d 100644
--- a/src/sentry/static/sentry/app/views/groupDetails/actions.jsx
+++ b/src/sentry/static/sentry/app/views/groupDetails/actions.jsx
@@ -1,4 +1,4 @@
-import {browserHistory, Link} from 'react-router';
+import {browserHistory} from 'react-router';
 import PropTypes from 'prop-types';
 import React from 'react';
 
@@ -222,9 +222,6 @@ const GroupDetailsActions = createReactClass({
 
     let hasRelease = this.getProjectFeatures().has('releases');
 
-    // account for both old and new style plugins
-    let hasIssueTracking = group.pluginActions.length || group.pluginIssues.length;
-
     let isResolved = group.status === 'resolved';
     let isIgnored = group.status === 'ignored';
 
@@ -300,18 +297,6 @@ const GroupDetailsActions = createReactClass({
           group.pluginIssues.map(plugin => {
             return <IssuePluginActions key={plugin.slug} plugin={plugin} />;
           })}
-        {!hasIssueTracking && (
-          <GuideAnchor type="text" target="issue_tracking">
-            <Link
-              to={`/${this.getOrganization().slug}/${this.getProject()
-                .slug}/settings/issue-tracking/`}
-              className={'btn btn-default btn-sm btn-config-issue-tracking'}
-              style={{marginRight: '5px'}}
-            >
-              {t('Link Issue Tracker')}
-            </Link>
-          </GuideAnchor>
-        )}
       </div>
     );
   },
diff --git a/tests/js/spec/views/groupDetails/__snapshots__/actions.spec.jsx.snap b/tests/js/spec/views/groupDetails/__snapshots__/actions.spec.jsx.snap
index 9d7c046700..5db5a55bec 100644
--- a/tests/js/spec/views/groupDetails/__snapshots__/actions.spec.jsx.snap
+++ b/tests/js/spec/views/groupDetails/__snapshots__/actions.spec.jsx.snap
@@ -98,22 +98,5 @@ exports[`GroupActions render() renders correctly 1`] = `
       }
     }
   />
-  <GuideAnchor
-    target="issue_tracking"
-    type="text"
-  >
-    <Link
-      className="btn btn-default btn-sm btn-config-issue-tracking"
-      onlyActiveOnIndex={false}
-      style={
-        Object {
-          "marginRight": "5px",
-        }
-      }
-      to="/org/project/settings/issue-tracking/"
-    >
-      Link Issue Tracker
-    </Link>
-  </GuideAnchor>
 </div>
 `;
