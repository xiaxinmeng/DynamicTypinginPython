commit 0603bf5efe8ad046248514201cba4f909357742f
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Nov 6 15:56:55 2014 -0800

    sentry-new-team => sentry-create-team

diff --git a/src/sentry/templates/sentry/organization-home.html b/src/sentry/templates/sentry/organization-home.html
index 30e1207d7a..145b344ac9 100644
--- a/src/sentry/templates/sentry/organization-home.html
+++ b/src/sentry/templates/sentry/organization-home.html
@@ -5,7 +5,7 @@
 {% block org_teams_nav %}active{% endblock %}
 
 {% block actions %}
-  <a href="{% url 'sentry-new-team' organization.id %}"
+  <a href="{% url 'sentry-create-team' organization.id %}"
      class="btn btn-small btn-primary">{% trans "New Team" %}</a>
 {% endblock %}
 
diff --git a/src/sentry/web/urls.py b/src/sentry/web/urls.py
index d85a03e123..6677910ad3 100644
--- a/src/sentry/web/urls.py
+++ b/src/sentry/web/urls.py
@@ -133,7 +133,7 @@ urlpatterns += patterns('',
     url(r'^organizations/(?P<organization_id>\d+)/teams/$', OrganizationTeamsView.as_view(),
         name='sentry-organization-teams'),
     url(r'^organizations/(?P<organization_id>\d+)/teams/new/$', CreateTeamView.as_view(),
-        name='sentry-new-team'),
+        name='sentry-create-team'),
     url(r'^accept/(?P<member_id>\d+)/(?P<token>\w+)/$', AcceptOrganizationInviteView.as_view(),
         name='sentry-accept-invite'),
 
diff --git a/tests/sentry/web/frontend/generic/tests.py b/tests/sentry/web/frontend/generic/tests.py
index c717e870c3..d8b08106b2 100644
--- a/tests/sentry/web/frontend/generic/tests.py
+++ b/tests/sentry/web/frontend/generic/tests.py
@@ -25,7 +25,7 @@ class DashboardTest(TestCase):
 
         can_create_teams.assert_called_once_with(self.user)
         assert resp.status_code == 302
-        assert resp['Location'] == 'http://testserver' + reverse('sentry-new-team')
+        assert resp['Location'] == 'http://testserver' + reverse('sentry-create-team')
 
     @mock.patch('sentry.web.frontend.generic.can_create_teams', mock.Mock(return_value=True))
     def test_shows_team_selector_with_single(self):
diff --git a/tests/sentry/web/frontend/test_create_team.py b/tests/sentry/web/frontend/test_create_team.py
index 9151260978..8c49f4bb45 100644
--- a/tests/sentry/web/frontend/test_create_team.py
+++ b/tests/sentry/web/frontend/test_create_team.py
@@ -9,7 +9,7 @@ from sentry.testutils import TestCase
 class CreateTeamTest(TestCase):
     def test_renders_with_context(self):
         organization = self.create_organization()
-        path = reverse('sentry-new-team', args=[organization.id])
+        path = reverse('sentry-create-team', args=[organization.id])
         self.login_as(self.user)
         resp = self.client.get(path)
         assert resp.status_code == 200
@@ -19,7 +19,7 @@ class CreateTeamTest(TestCase):
 
     def test_valid_params(self):
         organization = self.create_organization()
-        path = reverse('sentry-new-team', args=[organization.id])
+        path = reverse('sentry-create-team', args=[organization.id])
         self.login_as(self.user)
         resp = self.client.post(path, {
             'name': 'bar',
