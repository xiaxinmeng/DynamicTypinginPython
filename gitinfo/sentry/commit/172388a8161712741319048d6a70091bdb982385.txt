commit 172388a8161712741319048d6a70091bdb982385
Author: ckj <chriskjennings@gmail.com>
Date:   Thu Jul 14 15:14:21 2016 -0700

    Add support for more distribution graph colors

diff --git a/src/sentry/static/sentry/app/components/group/tagDistributionMeter.jsx b/src/sentry/static/sentry/app/components/group/tagDistributionMeter.jsx
index e1c38733d8..a49e8ca492 100644
--- a/src/sentry/static/sentry/app/components/group/tagDistributionMeter.jsx
+++ b/src/sentry/static/sentry/app/components/group/tagDistributionMeter.jsx
@@ -84,14 +84,15 @@ const TagDistributionMeter = React.createClass({
     let {orgId, projectId} = this.props;
     return (
       <div className="segments">
-        {data.topValues.map((value) => {
+        {data.topValues.map((value, index) => {
           let pct = percent(value.count, totalValues);
           let pctLabel = Math.floor(pct);
+          let className = "segment segment-" + index;
 
           return (
             <Link
                 key={value.id}
-                className="segment" style={{width: pct + '%'}}
+                className={className} style={{width: pct + '%'}}
                 to={`/${orgId}/${projectId}/issues/${this.props.group.id}/tags/${this.props.tag}/`}
                 title={'<div class="truncate">' + escape(value.name) + '</div>' + pctLabel + '%'}>
               <span className="tag-description">
diff --git a/src/sentry/static/sentry/less/shared-components.less b/src/sentry/static/sentry/less/shared-components.less
index 896e461e6c..434aace2de 100644
--- a/src/sentry/static/sentry/less/shared-components.less
+++ b/src/sentry/static/sentry/less/shared-components.less
@@ -1459,21 +1459,45 @@ table.integrations {
       border-bottom-right-radius: 3px;
     }
 
-    &:nth-child(4) {
+    &.segment-9 {
+      background-color: rgba(135, 146, 163, 0.1);
+    }
+
+    &.segment-8 {
       background-color: rgba(135, 146, 163, 0.2);
     }
 
-    &:nth-child(3) {
+    &.segment-7 {
+      background-color: rgba(135, 146, 163, 0.3);
+    }
+
+    &.segment-6 {
       background-color: rgba(135, 146, 163, 0.4);
     }
 
-    &:nth-child(2) {
+    &.segment-5 {
+      background-color: rgba(135, 146, 163, 0.5);
+    }
+
+    &.segment-4 {
       background-color: rgba(135, 146, 163, 0.6);
     }
 
-    &:nth-child(1) {
+    &.segment-3 {
+      background-color: rgba(135, 146, 163, 0.7);
+    }
+
+    &.segment-2 {
       background-color: rgba(135, 146, 163, 0.8);
     }
+
+    &.segment-1 {
+      background-color: rgba(135, 146, 163, 0.9);
+    }
+
+    &.segment-0 {
+      background-color: rgba(135, 146, 163, 1);
+    }
   }
 }
 
