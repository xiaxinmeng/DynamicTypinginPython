commit 95777d4d7702d253e03fb25dea772a02264fe145
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Jan 5 19:04:49 2012 -0800

    Change get_all_values to return a dict

diff --git a/sentry/manager.py b/sentry/manager.py
index 231245e5c6..9bb274062e 100644
--- a/sentry/manager.py
+++ b/sentry/manager.py
@@ -527,8 +527,8 @@ class MetaManager(models.Manager):
         if not hasattr(self, '_metadata'):
             self._metadata = {}
         if instance.pk not in self._metadata:
-            result = self.filter(**{
+            result = dict(self.filter(**{
                 self.field_name: instance,
-            }).values('key', 'value')
+            }).values_list('key', 'value'))
             self._metadata[instance.pk] = result
         return self._metadata[instance.pk]
