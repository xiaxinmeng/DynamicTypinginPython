commit f507d1efb15e1d03e4d8a320ca3d8370b3783490
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Aug 20 21:46:25 2014 -0700

    Remove remaining use of backbone/underscore

diff --git a/bower.json b/bower.json
index 76bed5e164..9d14dc50ba 100644
--- a/bower.json
+++ b/bower.json
@@ -14,13 +14,11 @@
     "angular-linkify": "~0.3.0",
     "angular-mocks": "1.2.9",
     "angular-raven": "~0.5.4",
-    "backbone": "~1.1.2",
     "bootstrap": "~3.1.1",
     "bootstrap-datepicker": "~1.3.0",
     "moment": "~2.1.0",
     "jquery": "~2.0.3",
     "jquery-flot": "~0.8.3",
-    "json2": "*",
     "selectize": "~0.10.1",
     "simple-slider": "https://github.com/loopj/jquery-simple-slider.git"
   },
diff --git a/src/sentry/conf/server.py b/src/sentry/conf/server.py
index c179539449..8b95c9199c 100644
--- a/src/sentry/conf/server.py
+++ b/src/sentry/conf/server.py
@@ -445,10 +445,6 @@ SENTRY_STATIC_BUNDLES = {
         # TODO(dcramer): this needs to go away ASAP
         "sentry/dist/legacy-app.min.js": {
             "src": [
-                "sentry/vendor/json2/json2.js",
-                "sentry/vendor/underscore/underscore.js",
-                "sentry/vendor/backbone/backbone.js",
-
                 "sentry/app/init.js",
 
                 "sentry/app/charts.js",
diff --git a/src/sentry/static/sentry/app/utils.js b/src/sentry/static/sentry/app/utils.js
index 23cf013445..d0ef3a7b73 100644
--- a/src/sentry/static/sentry/app/utils.js
+++ b/src/sentry/static/sentry/app/utils.js
@@ -143,33 +143,35 @@
         },
 
         makeSearchableUsersInput: function(el) {
-            this.makeSearchableInput(el, this.getSearchUsersUrl(), _.bind(function(data){
+            var self = this;
+
+            this.makeSearchableInput(el, this.getSearchUsersUrl(), function(data){
                 var results = [];
-                $(data.results).each(_.bind(function(_, val){
+                $(data.results).each(function(_, val){
                     var label;
                     if (val.first_name) {
-                        label = this.escape(val.first_name) + ' &mdash; ' + this.escape(val.username);
+                        label = self.escape(val.first_name) + ' &mdash; ' + self.escape(val.username);
                     } else {
-                        label = this.escape(val.username);
+                        label = self.escape(val.username);
                     }
-                    label += '<br>' + this.escape(val.email);
+                    label += '<br>' + self.escape(val.email);
                     results.push({
                         id: val.username,
                         text: label
                     });
-                }, this));
+                });
 
                 if (data.query && $(results).filter(function(){
                     return this.id.localeCompare(data.query) === 0;
                 }).length === 0) {
                     results.push({
-                        id: this.escape(data.query),
-                        text: this.escape(data.query)
+                        id: self.escape(data.query),
+                        text: self.escape(data.query)
                     });
                 }
 
                 return results;
-            }));
+            });
         }
     };
 }());
