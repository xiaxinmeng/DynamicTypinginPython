commit b6521325892a00715c7e293471c3954a062b5b7f
Author: Jess MacQueen <macqueen@users.noreply.github.com>
Date:   Mon Nov 28 17:18:45 2016 -0800

    fix repo form submission (#4616)

diff --git a/src/sentry/static/sentry/app/views/organizationRepositories.jsx b/src/sentry/static/sentry/app/views/organizationRepositories.jsx
index ee62358e32..1c3c39185c 100644
--- a/src/sentry/static/sentry/app/views/organizationRepositories.jsx
+++ b/src/sentry/static/sentry/app/views/organizationRepositories.jsx
@@ -31,7 +31,7 @@ class AddRepositoryLink extends PluginComponentBase {
 
     ['onOpen',
      'onCancel',
-     'onSubmit',
+     'formSubmit',
      'changeField'].map(method => this[method] = this[method].bind(this));
   }
 
@@ -43,6 +43,13 @@ class AddRepositoryLink extends PluginComponentBase {
     this.setState({isModalOpen: false});
   }
 
+  formSubmit(ev) {
+    // since this doesn't use the Form component, wrap onSubmit
+    // in a function that calls preventDefault
+    ev.preventDefault();
+    this.onSubmit();
+  }
+
   onSubmit() {
     // TODO(dcramer): set form saving state
     this.setState({
@@ -79,7 +86,7 @@ class AddRepositoryLink extends PluginComponentBase {
     let errors = this.state.error.errors || {};
     let provider = this.props.provider;
     return (
-      <form onSubmit={this.onSubmit}>
+      <form onSubmit={this.formSubmit}>
         {errors.__all__ &&
           <div className="alert alert-error alert-block" key="_errors">
             <p>{errors.__all__}</p>
