commit d62b558ed011beaee6c12ab9efdc74bac870e0c6
Author: Manu <manu@sentry.io>
Date:   Mon Aug 5 10:17:22 2019 -0700

    test: Do not test issue.id for erroring (#14282)
    
    The set up of this test creates two groups. backend.py includes all
    valid groups when querying snuba. So, there's no point in querying
    for all valid groups AND is issues NULL or is issues NOT NULL.
    
    So, just skip the erroring test for issue.id.

diff --git a/tests/snuba/search/test_backend.py b/tests/snuba/search/test_backend.py
index afb571a73f..1f270ceed0 100644
--- a/tests/snuba/search/test_backend.py
+++ b/tests/snuba/search/test_backend.py
@@ -1569,7 +1569,7 @@ class SnubaSearchTest(TestCase, SnubaTestCase):
                 self.fail('Query %s errored. Error info: %s' % (query, e))
 
         for key in SENTRY_SNUBA_MAP:
-            if key == 'project.id':
+            if key in ['project.id', 'issue.id']:
                 continue
             test_query('has:%s' % key)
             test_query('!has:%s' % key)
