commit 07399393d4d65dda17ba3a28a03ca22e8b51a0b1
Author: Lyn Nagara <lyn.nagara@gmail.com>
Date:   Thu Nov 30 14:15:10 2017 -0800

    fix(ui): Set a max height on the assign to user dropdown (#6604)
    
    Set max height of 400px to prevent this from getting too long in large teams.
    Fixes #2198

diff --git a/src/sentry/static/sentry/app/components/assigneeSelector.jsx b/src/sentry/static/sentry/app/components/assigneeSelector.jsx
index 99ab32dac2..88f0e087ee 100644
--- a/src/sentry/static/sentry/app/components/assigneeSelector.jsx
+++ b/src/sentry/static/sentry/app/components/assigneeSelector.jsx
@@ -274,7 +274,11 @@ const AssigneeSelector = React.createClass({
                     <span className="icon-circle-cross" /> {t('Clear Assignee')}
                   </MenuItem>
                 )}
-              {!memberListLoading && memberNodes}
+              {!memberListLoading && (
+                <li>
+                  <ul>{memberNodes}</ul>
+                </li>
+              )}
 
               {memberListLoading && (
                 <li>
diff --git a/src/sentry/static/sentry/less/shared-components.less b/src/sentry/static/sentry/less/shared-components.less
index 309506ba7c..2d61444910 100644
--- a/src/sentry/static/sentry/less/shared-components.less
+++ b/src/sentry/static/sentry/less/shared-components.less
@@ -1952,9 +1952,8 @@ ul.faces {
 
     ul {
       .list-unstyled();
-      max-height: 180px;
-      overflow: auto;
-      margin: 0;
+      max-height: 500px;
+      overflow-y: auto;
     }
 
     li {
