commit 9bd33965862c3a68476a9bbfcf7207002f95d836
Author: Mark Story <mark@sentry.io>
Date:   Mon Jun 22 09:53:25 2020 -0400

    ref(ui) Convert icons in smart searchbar to new icons (#19486)
    
    Convert all the icons used in smartsearchbar to the new icon components.

diff --git a/src/sentry/static/sentry/app/components/smartSearchBar/searchDropdown.tsx b/src/sentry/static/sentry/app/components/smartSearchBar/searchDropdown.tsx
index e9f9529110..a2410d1cb7 100644
--- a/src/sentry/static/sentry/app/components/smartSearchBar/searchDropdown.tsx
+++ b/src/sentry/static/sentry/app/components/smartSearchBar/searchDropdown.tsx
@@ -1,5 +1,4 @@
 import React from 'react';
-import classNames from 'classnames';
 import styled from '@emotion/styled';
 
 import {t} from 'app/locale';
@@ -53,7 +52,7 @@ class SearchDropdown extends React.PureComponent<Props> {
   renderHeaderItem = (item: SearchGroup) => (
     <SearchDropdownGroup key={item.title}>
       <SearchDropdownGroupTitle>
-        <GroupTitleIcon className={classNames('icon', item.icon)} />
+        {item.icon}
         {item.title && item.title}
         {item.desc && <span>{item.desc}</span>}
       </SearchDropdownGroupTitle>
@@ -147,10 +146,6 @@ const ListItem = styled('li')`
 
 const SearchDropdownGroup = styled(ListItem)``;
 
-const GroupTitleIcon = styled('span')`
-  margin-right: ${space(1)};
-`;
-
 const SearchDropdownGroupTitle = styled('header')`
   display: flex;
   align-items: center;
@@ -162,6 +157,10 @@ const SearchDropdownGroupTitle = styled('header')`
 
   margin: 0;
   padding: ${space(1)} ${space(2)};
+
+  & > svg {
+    margin-right: ${space(1)};
+  }
 `;
 
 const SearchItemsList = styled('ul')`
diff --git a/src/sentry/static/sentry/app/components/smartSearchBar/types.ts b/src/sentry/static/sentry/app/components/smartSearchBar/types.ts
index 381a3abaac..5322d5ceb0 100644
--- a/src/sentry/static/sentry/app/components/smartSearchBar/types.ts
+++ b/src/sentry/static/sentry/app/components/smartSearchBar/types.ts
@@ -9,7 +9,7 @@ export type ItemType =
 export type SearchGroup = {
   type: ItemType | 'header';
   title: string;
-  icon: string;
+  icon: React.ReactNode;
   value?: string;
   desc?: string;
   children: SearchItem[];
diff --git a/src/sentry/static/sentry/app/components/smartSearchBar/utils.ts b/src/sentry/static/sentry/app/components/smartSearchBar/utils.tsx
similarity index 88%
rename from src/sentry/static/sentry/app/components/smartSearchBar/utils.ts
rename to src/sentry/static/sentry/app/components/smartSearchBar/utils.tsx
index b130834252..8726eba2de 100644
--- a/src/sentry/static/sentry/app/components/smartSearchBar/utils.ts
+++ b/src/sentry/static/sentry/app/components/smartSearchBar/utils.tsx
@@ -1,4 +1,7 @@
+import React from 'react';
+
 import {t} from 'app/locale';
+import {IconClock, IconStar, IconTag, IconToggle, IconUser} from 'app/icons';
 
 import {ItemType, SearchGroup, SearchItem} from './types';
 
@@ -36,26 +39,26 @@ function getTitleForType(type: ItemType) {
 
 function getIconForTypeAndTag(type: ItemType, tagName: string) {
   if (type === 'recent-search') {
-    return 'icon-clock';
+    return <IconClock size="xs" />;
   }
 
   if (type === 'default') {
-    return 'icon-star-outline';
+    return <IconStar size="xs" />;
   }
 
   // Change based on tagName and default to "icon-tag"
   switch (tagName) {
     case 'is':
-      return 'icon-toggle';
+      return <IconToggle size="xs" />;
     case 'assigned':
     case 'bookmarks':
-      return 'icon-user';
+      return <IconUser size="xs" />;
     case 'firstSeen':
     case 'lastSeen':
     case 'event.timestamp':
-      return 'icon-av_timer';
+      return <IconClock size="xs" />;
     default:
-      return 'icon-tag';
+      return <IconTag size="xs" />;
   }
 }
 
@@ -82,7 +85,7 @@ export function createSearchGroups(
   const recentSearchGroup: SearchGroup | undefined = recentSearchItems && {
     title: t('Recent Searches'),
     type: 'header',
-    icon: 'icon-clock',
+    icon: <IconClock size="xs" />,
     children: [...recentSearchItems],
   };
 
