commit cba05e9b55e52b7718cb1f6e550a261fc0df22d5
Author: David Cramer <dcramer@gmail.com>
Date:   Fri Jan 2 12:57:20 2015 -0800

    Base interface slots

diff --git a/src/sentry/interfaces/base.py b/src/sentry/interfaces/base.py
index a85bc4b685..232306a86d 100644
--- a/src/sentry/interfaces/base.py
+++ b/src/sentry/interfaces/base.py
@@ -27,9 +27,12 @@ class Interface(object):
     render differently than the default ``extra`` metadata in an event.
     """
 
+    _data = None
     score = 0
     display_score = None
 
+    __slots__ = ['_data']
+
     def __init__(self, **data):
         self._data = data or {}
 
@@ -38,6 +41,12 @@ class Interface(object):
             return False
         return self._data == other._data
 
+    def __getstate__(self):
+        return dict(
+            (slot, self.__dict__.get(slot))
+            for slot in self.__slots__
+        )
+
     def __setstate__(self, state):
         self.__dict__.update(state)
         if not hasattr(self, '_data'):
diff --git a/src/sentry/interfaces/stacktrace.py b/src/sentry/interfaces/stacktrace.py
index cbb808557d..997b4e685f 100644
--- a/src/sentry/interfaces/stacktrace.py
+++ b/src/sentry/interfaces/stacktrace.py
@@ -134,12 +134,6 @@ def validate_bool(value, required=True):
 
 
 class Frame(Interface):
-    __slots__ = [
-        'abs_path', 'filename', 'module', 'function', 'in_app',
-        'context_line', 'pre_context', 'post_context', 'vars', 'data',
-        'lineno', 'colno',
-    ]
-
     @classmethod
     def to_python(cls, data):
         abs_path = data.get('abs_path')
