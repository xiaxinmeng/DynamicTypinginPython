commit 3915c5cd1e962002543bded29b026acde49d71fd
Author: David Cramer <dcramer@gmail.com>
Date:   Sat Nov 24 00:20:45 2012 -0800

    Add coffee-script to develop command and general cleanup

diff --git a/Makefile b/Makefile
index f699ba147f..e3817213c7 100644
--- a/Makefile
+++ b/Makefile
@@ -8,7 +8,8 @@ UGLIFY_JS ?= `which uglifyjs`
 COFFEE ?= `which coffee`
 WATCHR ?= `which watchr`
 
-develop: update-submodules
+develop: update-submodules bootstrap-tests
+	npm install coffee-script
 	pip install -e . --use-mirrors
 
 build: static coffee locale
@@ -39,7 +40,7 @@ cwatch:
 
 bootstrap-tests:
 	npm install phantomjs
-	pip install flake8>=1.6 --use-mirrors
+	pip install "flake8>=1.6" --use-mirrors
 
 test: lint test-js test-python
 
