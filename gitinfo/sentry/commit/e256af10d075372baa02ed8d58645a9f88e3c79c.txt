commit e256af10d075372baa02ed8d58645a9f88e3c79c
Author: Dora <dora.l.chan@gmail.com>
Date:   Wed Jan 22 12:59:16 2020 -0800

    feat(discv2 graph) fix menu dropdown (#16581)
    
    * fix menu dropdown
    
    * auto width
    
    * fix dropdown button treatment

diff --git a/src/sentry/static/sentry/app/views/events/yAxisSelector.jsx b/src/sentry/static/sentry/app/views/events/yAxisSelector.jsx
index e0efb3effd..8ab8c42225 100644
--- a/src/sentry/static/sentry/app/views/events/yAxisSelector.jsx
+++ b/src/sentry/static/sentry/app/views/events/yAxisSelector.jsx
@@ -15,9 +15,10 @@ const YAxisSelector = props => {
     <ChartControls>
       <StyledLabel>{t('Y-Axis')}</StyledLabel>
       <DropdownControl
-        menuOffset="29px"
-        button={({isOpen, getActorProps}) => (
-          <StyledDropdownButton {...getActorProps()} size="zero" isOpen={isOpen}>
+        menuWidth="auto"
+        alignRight
+        button={({getActorProps}) => (
+          <StyledDropdownButton {...getActorProps()} size="zero" isOpen={false}>
             {selectedOption.label}
           </StyledDropdownButton>
         )}
@@ -37,27 +38,25 @@ const YAxisSelector = props => {
   );
 };
 
+const ChartControls = styled('div')`
+  display: flex;
+  justify-content: flex-end;
+  padding: ${space(1)};
+  border-top: 1px solid ${p => p.theme.borderLight};
+`;
+
 const StyledDropdownButton = styled(DropdownButton)`
-  border-radius: ${p =>
-    p.isOpen && `${p.theme.borderRadius} ${p.theme.borderRadius} 0 0`};
   padding: ${space(1)} ${space(2)};
   font-weight: normal;
-  min-width: 180px;
-  color: ${p => p.theme.gray4};
+  color: ${p => p.theme.gray3};
 
   &:hover,
-  &:focus {
-    color: inherit;
+  &:focus,
+  &:active {
+    color: ${p => p.theme.gray4};
   }
 `;
 
-const ChartControls = styled('div')`
-  display: flex;
-  justify-content: flex-end;
-  padding: ${space(1)};
-  border-top: 1px solid ${p => p.theme.borderLight};
-`;
-
 const StyledLabel = styled(SectionHeading)`
   padding-right: ${space(1)};
   line-height: 1.2;
