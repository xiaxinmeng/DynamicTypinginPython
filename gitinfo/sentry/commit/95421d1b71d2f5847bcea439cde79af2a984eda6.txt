commit 95421d1b71d2f5847bcea439cde79af2a984eda6
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Feb 18 13:55:53 2015 -0800

    Maintain project release sort order

diff --git a/src/sentry/api/endpoints/project_releases.py b/src/sentry/api/endpoints/project_releases.py
index 65606e4224..a83b9812f9 100644
--- a/src/sentry/api/endpoints/project_releases.py
+++ b/src/sentry/api/endpoints/project_releases.py
@@ -20,12 +20,11 @@ class ProjectReleasesEndpoint(ProjectEndpoint):
         """
         queryset = Release.objects.filter(
             project=project,
-        ).order_by('-date_added')
+        )
 
         return self.paginate(
             request=request,
             queryset=queryset,
-            # TODO(dcramer): we want to sort by date_added
             order_by='-id',
             on_results=lambda x: serialize(x, request.user),
         )
