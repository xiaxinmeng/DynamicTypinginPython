commit b84bcbfebb244a635aacbd4a37d0a4b08a95e783
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Mon Feb 22 15:04:08 2016 -0800

    Make sure we print out warnings

diff --git a/src/sentry/runner/settings.py b/src/sentry/runner/settings.py
index 18139aa4e3..6c57235f0d 100644
--- a/src/sentry/runner/settings.py
+++ b/src/sentry/runner/settings.py
@@ -266,6 +266,10 @@ def configure(ctx, py, yaml, skip_backend_validation=False):
     if __installed:
         return
 
+    # Make sure that our warnings are always displayed
+    import warnings
+    warnings.filterwarnings('default', '', Warning, r'^sentry')
+
     from .importer import install
 
     if yaml is None:
