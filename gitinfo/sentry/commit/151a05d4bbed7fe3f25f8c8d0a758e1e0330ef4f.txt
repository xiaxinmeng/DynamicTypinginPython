commit 151a05d4bbed7fe3f25f8c8d0a758e1e0330ef4f
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Feb 2 00:34:12 2012 -0800

    Correct rendering of builtins on their detail panes

diff --git a/CHANGES b/CHANGES
index fb45b82a34..819b769cb4 100644
--- a/CHANGES
+++ b/CHANGES
@@ -1,3 +1,8 @@
+Version 2.3.1
+-------------
+
+* Correct rendering of sites, urls, and servers on details panes.
+
 Version 2.3.0
 -------------
 
diff --git a/sentry/plugins/sentry_servers/__init__.py b/sentry/plugins/sentry_servers/__init__.py
index 45d504f5f0..d0bed472cb 100644
--- a/sentry/plugins/sentry_servers/__init__.py
+++ b/sentry/plugins/sentry_servers/__init__.py
@@ -32,7 +32,10 @@ class ServersPlugin(Plugin):
         return panel_list
 
     def view(self, request, group, **kwargs):
-        return self.render('sentry/plugins/sentry_servers/index.html')
+        return self.render('sentry/plugins/sentry_servers/index.html', {
+            'unique_servers': self.get_unique_servers(group),
+            'group': group,
+        })
 
     def widget(self, request, group, **kwargs):
         return self.render('sentry/plugins/sentry_servers/widget.html', {
diff --git a/sentry/plugins/sentry_servers/templates/sentry/plugins/sentry_servers/index.html b/sentry/plugins/sentry_servers/templates/sentry/plugins/sentry_servers/index.html
index a610d46b76..dd7689469f 100644
--- a/sentry/plugins/sentry_servers/templates/sentry/plugins/sentry_servers/index.html
+++ b/sentry/plugins/sentry_servers/templates/sentry/plugins/sentry_servers/index.html
@@ -4,7 +4,7 @@
 {% load sentry_helpers %}
 
 {% block inner %}
-    {% paginate group.unique_servers from request as server_list per_page 25 %}
+    {% paginate unique_servers from request as server_list per_page 25 %}
 
     {% if server_list.objects %}
         {{ server_list.paging }}
diff --git a/sentry/plugins/sentry_sites/__init__.py b/sentry/plugins/sentry_sites/__init__.py
index 47649bae0f..99706f92bf 100644
--- a/sentry/plugins/sentry_sites/__init__.py
+++ b/sentry/plugins/sentry_sites/__init__.py
@@ -31,7 +31,10 @@ class SitesPlugin(Plugin):
         return panel_list
 
     def view(self, request, group, **kwargs):
-        return self.render('sentry/plugins/sentry_sites/index.html')
+        return self.render('sentry/plugins/sentry_sites/index.html', {
+            'group': group,
+            'unique_sites': self.get_unique_sites(group),
+        })
 
     def widget(self, request, group, **kwargs):
         return self.render('sentry/plugins/sentry_sites/widget.html', {
diff --git a/sentry/plugins/sentry_sites/templates/sentry/plugins/sentry_sites/index.html b/sentry/plugins/sentry_sites/templates/sentry/plugins/sentry_sites/index.html
index 1c8520d727..c2d089e166 100644
--- a/sentry/plugins/sentry_sites/templates/sentry/plugins/sentry_sites/index.html
+++ b/sentry/plugins/sentry_sites/templates/sentry/plugins/sentry_sites/index.html
@@ -4,7 +4,7 @@
 {% load sentry_helpers %}
 
 {% block inner %}
-    {% paginate group.unique_sites from request as site_list per_page 25 %}
+    {% paginate unique_sites from request as site_list per_page 25 %}
 
     {% if site_list.objects %}
         {{ site_list.paging }}
diff --git a/sentry/plugins/sentry_urls/__init__.py b/sentry/plugins/sentry_urls/__init__.py
index 943c745c9b..e9c4206f63 100644
--- a/sentry/plugins/sentry_urls/__init__.py
+++ b/sentry/plugins/sentry_urls/__init__.py
@@ -31,7 +31,10 @@ class UrlsPlugin(Plugin):
         return panel_list
 
     def view(self, request, group, **kwargs):
-        return self.render('sentry/plugins/sentry_urls/index.html')
+        return self.render('sentry/plugins/sentry_urls/index.html', {
+            'group': group,
+            'unique_urls': self.get_unique_urls(group),
+        })
 
     def widget(self, request, group, **kwargs):
         return self.render('sentry/plugins/sentry_urls/widget.html', {
diff --git a/sentry/plugins/sentry_urls/templates/sentry/plugins/sentry_urls/index.html b/sentry/plugins/sentry_urls/templates/sentry/plugins/sentry_urls/index.html
index 5751dad000..7efa78eb1b 100644
--- a/sentry/plugins/sentry_urls/templates/sentry/plugins/sentry_urls/index.html
+++ b/sentry/plugins/sentry_urls/templates/sentry/plugins/sentry_urls/index.html
@@ -4,7 +4,7 @@
 {% load sentry_helpers %}
 
 {% block inner %}
-    {% paginate group.unique_urls from request as url_list per_page 25 %}
+    {% paginate unique_urls from request as url_list per_page 25 %}
 
     {% if url_list.objects %}
         {{ url_list.paging }}
diff --git a/setup.py b/setup.py
index 4c65f4cecd..1edf263f74 100755
--- a/setup.py
+++ b/setup.py
@@ -45,7 +45,7 @@ install_requires = [
 
 setup(
     name='sentry',
-    version='2.3.0',
+    version='2.3.1',
     author='David Cramer',
     author_email='dcramer@gmail.com',
     url='http://github.com/dcramer/sentry',
