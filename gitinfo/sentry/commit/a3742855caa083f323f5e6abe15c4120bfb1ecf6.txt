commit a3742855caa083f323f5e6abe15c4120bfb1ecf6
Author: Mark Story <mark@sentry.io>
Date:   Mon Aug 26 10:39:19 2019 -0400

    fix(api) Align ProjectKey.label max length to database (#14499)
    
    Make the serializer for ProjectKey have the same length as in the
    database field.
    
    Fixes SENTRY-CC6

diff --git a/src/sentry/api/endpoints/project_keys.py b/src/sentry/api/endpoints/project_keys.py
index 2144925a55..1534d9db1b 100644
--- a/src/sentry/api/endpoints/project_keys.py
+++ b/src/sentry/api/endpoints/project_keys.py
@@ -28,7 +28,7 @@ def create_key_scenario(runner):
 
 
 class KeySerializer(serializers.Serializer):
-    name = serializers.CharField(max_length=200, required=False, allow_blank=True, allow_null=True)
+    name = serializers.CharField(max_length=64, required=False, allow_blank=True, allow_null=True)
     public = serializers.RegexField(r"^[a-f0-9]{32}$", required=False, allow_null=True)
     secret = serializers.RegexField(r"^[a-f0-9]{32}$", required=False, allow_null=True)
 
diff --git a/src/sentry/static/sentry/app/views/settings/project/projectKeys/projectKeyDetails.jsx b/src/sentry/static/sentry/app/views/settings/project/projectKeys/projectKeyDetails.jsx
index 5bec43d8d2..694ea69ccf 100644
--- a/src/sentry/static/sentry/app/views/settings/project/projectKeys/projectKeyDetails.jsx
+++ b/src/sentry/static/sentry/app/views/settings/project/projectKeys/projectKeyDetails.jsx
@@ -366,6 +366,7 @@ class KeySettings extends React.Component {
                     label={t('Name')}
                     disabled={!hasAccess}
                     required={false}
+                    maxLength={64}
                   />
                   <BooleanField
                     name="isActive"
