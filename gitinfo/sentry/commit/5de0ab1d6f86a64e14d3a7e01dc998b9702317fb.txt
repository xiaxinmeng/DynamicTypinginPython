commit 5de0ab1d6f86a64e14d3a7e01dc998b9702317fb
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Sun Sep 17 08:08:13 2017 -0700

    fix(cleanup): Add ORDER BY to Event cleanup (#6139)

diff --git a/src/sentry/runner/commands/cleanup.py b/src/sentry/runner/commands/cleanup.py
index 980a1cb19e..4d29746243 100644
--- a/src/sentry/runner/commands/cleanup.py
+++ b/src/sentry/runner/commands/cleanup.py
@@ -105,7 +105,7 @@ def cleanup(days, project, concurrency, silent, model, router, timed):
     # Deletions that use the `deletions` code path (which handles their child relations)
     # (model, datetime_field, order_by)
     DELETES = (
-        (models.Event, 'datetime', None),
+        (models.Event, 'datetime', 'datetime'),
         (models.Group, 'last_seen', 'last_seen'),
     )
 
