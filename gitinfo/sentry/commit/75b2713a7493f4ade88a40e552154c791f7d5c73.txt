commit 75b2713a7493f4ade88a40e552154c791f7d5c73
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Dec 26 17:18:24 2011 -0800

    log levels no longer get translated

diff --git a/sentry/conf/settings.py b/sentry/conf/settings.py
index 2a6fd3a6d3..67f6a32518 100644
--- a/sentry/conf/settings.py
+++ b/sentry/conf/settings.py
@@ -10,7 +10,6 @@ from sentry.conf.defaults import *
 
 from django.conf import settings
 from django.utils.hashcompat import md5_constructor
-from django.utils.translation import ugettext_lazy as _
 
 import warnings
 
@@ -25,8 +24,6 @@ for k in dir(settings):
     if k.startswith('SENTRY_'):
         locals()[k.split('SENTRY_', 1)[1]] = getattr(settings, k)
 
-LOG_LEVELS = [(k, _(v)) for k, v in LOG_LEVELS]
-
 if locals().get('REMOTE_URL'):
     if isinstance(REMOTE_URL, basestring):
         SERVERS = [REMOTE_URL]
