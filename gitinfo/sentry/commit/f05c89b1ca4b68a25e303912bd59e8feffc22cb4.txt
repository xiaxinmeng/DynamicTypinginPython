commit f05c89b1ca4b68a25e303912bd59e8feffc22cb4
Author: John MacKenzie <john@nineteeneightd.com>
Date:   Mon Dec 17 14:12:11 2012 -0600

    Configurable Bind Address
    
    Update the http service to allow configuring the bind address for
    gunicorn via the SENTRY_WEB_OPTIONS setting. This makes it possible to
    bind to a unix socket rather than to a particular port on some local
    interface.

diff --git a/src/sentry/services/http.py b/src/sentry/services/http.py
index d46c34be4d..72e31add26 100644
--- a/src/sentry/services/http.py
+++ b/src/sentry/services/http.py
@@ -33,8 +33,8 @@ class SentryHTTPServer(Service):
         #     worker.init_process = profiling_init_process.__get__(worker)
 
         options = (settings.WEB_OPTIONS or {}).copy()
-        options['bind'] = '%s:%s' % (self.host, self.port)
         options['debug'] = debug
+        options.setdefault('bind', '%s:%s' % (self.host, self.port))
         options.setdefault('daemon', False)
         options.setdefault('timeout', 30)
         if workers:
