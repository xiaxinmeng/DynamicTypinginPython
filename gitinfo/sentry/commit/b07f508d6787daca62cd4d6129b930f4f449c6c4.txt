commit b07f508d6787daca62cd4d6129b930f4f449c6c4
Author: Armin Ronacher <armin.ronacher@active-4.com>
Date:   Mon Dec 21 22:01:56 2015 +0100

    Only convert to string if not string for headers.

diff --git a/src/sentry/interfaces/http.py b/src/sentry/interfaces/http.py
index d237cf50bc..833a45fc85 100644
--- a/src/sentry/interfaces/http.py
+++ b/src/sentry/interfaces/http.py
@@ -42,6 +42,8 @@ def format_headers(value):
         if k.lower() == 'cookie':
             cookie_header = v
         else:
+            if not isinstance(v, basestring):
+                v = unicode(v)
             result.append((k.title(), v))
     return result, cookie_header
 
