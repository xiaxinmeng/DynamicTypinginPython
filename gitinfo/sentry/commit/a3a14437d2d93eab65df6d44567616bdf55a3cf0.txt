commit a3a14437d2d93eab65df6d44567616bdf55a3cf0
Author: José Moreira <zemanel@zemanel.eu>
Date:   Thu Oct 4 18:01:09 2012 +0100

    fix default daemon setting value

diff --git a/src/sentry/services/http.py b/src/sentry/services/http.py
index 4b886b7382..d46c34be4d 100644
--- a/src/sentry/services/http.py
+++ b/src/sentry/services/http.py
@@ -35,7 +35,7 @@ class SentryHTTPServer(Service):
         options = (settings.WEB_OPTIONS or {}).copy()
         options['bind'] = '%s:%s' % (self.host, self.port)
         options['debug'] = debug
-        options.setdefault('daemon', 30)
+        options.setdefault('daemon', False)
         options.setdefault('timeout', 30)
         if workers:
             options['workers'] = workers
