commit c71bf0aaa04fcd693a2bd1323973814dd1b87407
Author: Jess MacQueen <jess@getsentry.com>
Date:   Thu Jul 13 13:20:12 2017 -0700

    a few more ted comments

diff --git a/src/sentry/event_manager.py b/src/sentry/event_manager.py
index cf19d31d3a..2bfb434a1b 100644
--- a/src/sentry/event_manager.py
+++ b/src/sentry/event_manager.py
@@ -829,7 +829,7 @@ class EventManager(object):
                 existing_group_id = h.group_id
                 break
             if h.group_tombstone_id is not None:
-                raise HashDiscarded('Matches discarded group %s' % h.group_tombstone_id)
+                raise HashDiscarded('Matches group tombstone %s' % h.group_tombstone_id)
 
         # XXX(dcramer): this has the opportunity to create duplicate groups
         # it should be resolved by the hash merging function later but this
diff --git a/src/sentry/tasks/store.py b/src/sentry/tasks/store.py
index 95d9ab9fb7..3fdc768cd8 100644
--- a/src/sentry/tasks/store.py
+++ b/src/sentry/tasks/store.py
@@ -25,7 +25,7 @@ from sentry.utils.dates import to_datetime
 from sentry.models import ProjectOption, Activity, Project
 
 error_logger = logging.getLogger('sentry.errors.events')
-
+info_logger = logging.getLogger('sentry.store')
 
 # Is reprocessing on or off by default?
 REPROCESSING_DEFAULT = False
@@ -294,7 +294,7 @@ def save_event(cache_key=None, data=None, start_time=None, event_id=None, **kwar
         manager = EventManager(data)
         manager.save(project)
     except HashDiscarded as exc:
-        error_logger.info('discarded.hash', extra={
+        info_logger.info('discarded.hash', extra={
             'project_id': project.id,
             'message': exc.message,
         })
