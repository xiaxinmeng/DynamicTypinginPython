commit a6f8fa04a676cdfa2e478b7e2d9f2afcd85f6080
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Fri Mar 18 11:53:36 2016 -0700

    Ignore celery status check when CELERY_ALWAYS_EAGER is on

diff --git a/src/sentry/status_checks/celery_alive.py b/src/sentry/status_checks/celery_alive.py
index e3d9346822..f5498c7ae1 100644
--- a/src/sentry/status_checks/celery_alive.py
+++ b/src/sentry/status_checks/celery_alive.py
@@ -2,6 +2,7 @@ from __future__ import absolute_import
 
 from time import time
 
+from django.conf import settings
 from django.core.urlresolvers import reverse
 
 from sentry import options
@@ -12,6 +13,9 @@ from .base import Problem, StatusCheck
 
 class CeleryAliveCheck(StatusCheck):
     def check(self):
+        # There is no queue, and celery is not running, so never show error
+        if settings.CELERY_ALWAYS_EAGER:
+            return []
         last_ping = options.get('sentry:last_worker_ping') or 0
         if last_ping >= time() - 300:
             return []
