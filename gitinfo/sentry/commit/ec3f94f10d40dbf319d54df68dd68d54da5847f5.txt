commit ec3f94f10d40dbf319d54df68dd68d54da5847f5
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Apr 6 22:20:07 2015 -0700

    Tweak charts to handle 24h vs 48h

diff --git a/src/sentry/api/serializers/models/group.py b/src/sentry/api/serializers/models/group.py
index d299d30e5f..161cf196f6 100644
--- a/src/sentry/api/serializers/models/group.py
+++ b/src/sentry/api/serializers/models/group.py
@@ -47,7 +47,7 @@ class GroupSerializer(Serializer):
             model=tsdb.models.group,
             keys=group_ids,
             end=now,
-            start=now - timedelta(days=2),
+            start=now - timedelta(days=1),
             rollup=3600,
         )
         daily_stats = tsdb.get_range(
@@ -122,7 +122,7 @@ class GroupSerializer(Serializer):
                 'slug': obj.project.slug,
             },
             'stats': {
-                '48h': attrs['hourly_stats'],
+                '24h': attrs['hourly_stats'],
                 '30d': attrs['daily_stats'],
             },
             'assignedTo': attrs['assigned_to'],
diff --git a/src/sentry/static/sentry/app/views/groupDetails.jsx b/src/sentry/static/sentry/app/views/groupDetails.jsx
index a6f03927d3..0ae17196d5 100644
--- a/src/sentry/static/sentry/app/views/groupDetails.jsx
+++ b/src/sentry/static/sentry/app/views/groupDetails.jsx
@@ -38,7 +38,7 @@ var GroupDetails = React.createClass({
   getInitialState() {
     return {
       group: null,
-      statsPeriod: '48h'
+      statsPeriod: '30d'
     };
   },
 
diff --git a/src/sentry/static/sentry/app/views/groupDetails/chart.jsx b/src/sentry/static/sentry/app/views/groupDetails/chart.jsx
index 3c580b15a2..1b1ae12c4b 100644
--- a/src/sentry/static/sentry/app/views/groupDetails/chart.jsx
+++ b/src/sentry/static/sentry/app/views/groupDetails/chart.jsx
@@ -8,21 +8,26 @@ var PropTypes = require("../../proptypes");
 var GroupChart = React.createClass({
   mixins: [GroupState],
 
-  propTypes: {
-    statsPeriod: React.PropTypes.string.isRequired
-  },
-
   render: function() {
     var group = this.getGroup();
-    var stats = group.stats[this.props.statsPeriod];
-    var points = stats.map((point) => {
+    var stats_24h = group.stats['24h'];
+    var points_24h = stats_24h.map((point) => {
+      return {x: point[0], y: point[1]};
+    });
+
+    var stats_30d = group.stats['30d'];
+    var points_30d = stats_30d.map((point) => {
       return {x: point[0], y: point[1]};
     });
 
+
     return (
       <div className="bar-chart group-chart">
-        <h6>Last 48 Hours</h6>
-        <BarChart points={points} className="sparkline" />
+        <h6>Last 24 hours</h6>
+        <BarChart points={points_24h} className="sparkline" />
+
+        <h6>Last 30 Days</h6>
+        <BarChart points={points_30d} className="sparkline small" />
       </div>
     );
   }
diff --git a/src/sentry/static/sentry/app/views/groupOverview.jsx b/src/sentry/static/sentry/app/views/groupOverview.jsx
index 32c1b16942..cf41917433 100644
--- a/src/sentry/static/sentry/app/views/groupOverview.jsx
+++ b/src/sentry/static/sentry/app/views/groupOverview.jsx
@@ -127,7 +127,10 @@ var GroupOverview = React.createClass({
     return (
       <div>
         <div className="row group-overview">
-          <div className="col-md-6 bordered-column">
+          <div className="col-md-8 bordered-column">
+            <GroupActivity />
+          </div>
+          <div className="col-md-4">
             <GroupChart statsPeriod={this.props.statsPeriod} />
             <div className="row group-stats">
               <div className="col-md-6">
@@ -144,9 +147,6 @@ var GroupOverview = React.createClass({
               </div>
             </div>
           </div>
-          <div className="col-md-6">
-            <GroupActivity />
-          </div>
         </div>
 
         <MutedBox status={group.status} />
diff --git a/src/sentry/static/sentry/app/views/stream/group.jsx b/src/sentry/static/sentry/app/views/stream/group.jsx
index 0c4ac61535..cdfed30861 100644
--- a/src/sentry/static/sentry/app/views/stream/group.jsx
+++ b/src/sentry/static/sentry/app/views/stream/group.jsx
@@ -49,7 +49,7 @@ var StreamGroup = React.createClass({
 
     switch(this.props.statsPeriod) {
       case '24h':
-        points = data.stats['48h'].slice(-24);
+        points = data.stats['24h'].slice(-24);
         break;
       default:
         points = data.stats[this.props.statsPeriod];
diff --git a/src/sentry/static/sentry/less/shared-components.less b/src/sentry/static/sentry/less/shared-components.less
index 3614ca14f4..0caa560d41 100644
--- a/src/sentry/static/sentry/less/shared-components.less
+++ b/src/sentry/static/sentry/less/shared-components.less
@@ -778,6 +778,10 @@ table.integrations {
     margin-bottom: 20px;
     .clearfix();
 
+    &.small {
+      a { height: 20px; }
+    }
+
     a {
       height: 90px;
       position: relative;
@@ -791,13 +795,13 @@ table.integrations {
         top: 0;
         left: 0;
         bottom: 0;
-        min-width: 4px;
+        min-width: 5px;
       }
 
       span {
         border-radius: 1px;
         min-height: 4px;
-        min-width: 4px;
+        min-width: 5px;
         background: @grayDark;
       }
     }
