commit 2a77b6b2ea384612ee1844505d766af2ddc98eee
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Dec 15 14:10:49 2014 -0800

    Actually re-set owner

diff --git a/src/sentry/admin.py b/src/sentry/admin.py
index 81cbb82ccf..e1a96ac0b1 100644
--- a/src/sentry/admin.py
+++ b/src/sentry/admin.py
@@ -90,10 +90,11 @@ class TeamAdmin(admin.ModelAdmin):
     inlines = (TeamProjectInline,)
 
     def save_model(self, request, obj, form, change):
-        super(TeamAdmin, self).save_model(request, obj, form, change)
+        # TODO(dcramer): remove when ownership is irrelevant
         if change:
-            # TODO(dcramer): remove when ownership is irrelevant
             obj.owner = obj.organization.owner
+        super(TeamAdmin, self).save_model(request, obj, form, change)
+        if change:
             Project.objects.filter(
                 team=obj,
             ).update(
