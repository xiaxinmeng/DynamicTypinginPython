commit df1384ffc5805ff055deffc654eb48574565ac3e
Author: Megan Heskett <meg.heskett@gmail.com>
Date:   Tue Nov 12 14:35:14 2019 -0800

    fix(ui): Fix invite request team selector (#15558)

diff --git a/src/sentry/static/sentry/app/views/settings/organizationMembers/inviteRequestRow.tsx b/src/sentry/static/sentry/app/views/settings/organizationMembers/inviteRequestRow.tsx
index 531963abd3..db574538db 100644
--- a/src/sentry/static/sentry/app/views/settings/organizationMembers/inviteRequestRow.tsx
+++ b/src/sentry/static/sentry/app/views/settings/organizationMembers/inviteRequestRow.tsx
@@ -80,7 +80,7 @@ const InviteRequestRow = ({
         value={inviteRequest.role}
         roles={allRoles}
       />
-      <SelectControl
+      <TeamSelectControl
         name="teams"
         placeholder={t('Add to teams...')}
         onChange={teams => onUpdate({teams: teams.map(team => team.value)})}
@@ -168,15 +168,14 @@ const JoinRequestIndicator = styled(Tag)`
 
 const StyledPanelItem = styled(PanelItem)`
   display: grid;
-  grid-template-columns: minmax(200px, auto) repeat(2, minmax(100px, 140px)) max-content;
+  grid-template-columns: minmax(200px, auto) minmax(100px, 140px) 220px max-content;
   grid-gap: ${space(2)};
   align-items: center;
 `;
 
 const UserName = styled('div')`
   font-size: ${p => p.theme.fontSizeLarge};
-  overflow: hidden;
-  text-overflow: ellipsis;
+  word-break: break-all;
 `;
 
 const Description = styled('div')`
@@ -184,6 +183,13 @@ const Description = styled('div')`
   font-size: 14px;
 `;
 
+const TeamSelectControl = styled(SelectControl)`
+  .Select-value-label {
+    max-width: 150px;
+    word-break: break-all;
+  }
+`;
+
 const ButtonGroup = styled('div')`
   display: inline-grid;
   grid-template-columns: auto auto;
