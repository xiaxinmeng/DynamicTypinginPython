commit 9812b3bffd8565564fa20ccf8f3d36b4a716b6d8
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Sep 10 15:53:18 2015 -0500

    Remove bad references to owner

diff --git a/src/sentry/web/frontend/admin.py b/src/sentry/web/frontend/admin.py
index d1f57795f4..2ba28c71f0 100644
--- a/src/sentry/web/frontend/admin.py
+++ b/src/sentry/web/frontend/admin.py
@@ -58,7 +58,7 @@ def manage_projects(request):
     project_list = Project.objects.filter(
         status=0,
         team__isnull=False,
-    ).select_related('owner', 'team')
+    ).select_related('team')
 
     project_query = request.GET.get('pquery')
     if project_query:
@@ -255,7 +255,7 @@ def list_user_projects(request, user_id):
 
 @requires_admin
 def manage_teams(request):
-    team_list = Team.objects.order_by('-date_added').select_related('owner')
+    team_list = Team.objects.order_by('-date_added')
 
     team_query = request.GET.get('tquery')
     if team_query:
