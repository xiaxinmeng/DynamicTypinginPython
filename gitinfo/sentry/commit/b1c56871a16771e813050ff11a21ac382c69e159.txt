commit b1c56871a16771e813050ff11a21ac382c69e159
Author: David Cramer <dcramer@gmail.com>
Date:   Sat Feb 4 02:20:40 2012 -0800

    Correct behavior with getPosition being used incorrectly in id fetch case

diff --git a/sentry/static/scripts/global.js b/sentry/static/scripts/global.js
index 3cbee7d17e..cce9225ba3 100644
--- a/sentry/static/scripts/global.js
+++ b/sentry/static/scripts/global.js
@@ -292,7 +292,7 @@ if (Sentry === undefined) {
         });
     };
 
-    function getPosition(list, value, idx) {
+    function getRankedPosition(list, value, idx) {
         for (var i=0, item; (item = list[i]); i++) {
             if (value > item[idx]) {
                 return i;
@@ -300,7 +300,14 @@ if (Sentry === undefined) {
         }
         return -1;
     }
-
+    function getPosition(list, value, idx) {
+        for (var i=0, item; (item = list[i]); i++) {
+            if (value == item[idx]) {
+                return i;
+            }
+        }
+        return -1;
+    }
     Sentry.realtime = {};
     Sentry.realtime.options = {
         viewId: null,
@@ -379,7 +386,7 @@ if (Sentry === undefined) {
         } else {
             row = $(data.html);
         }
-        pos = getPosition(Sentry.realtime.events, data.score, 0);
+        pos = getRankedPosition(Sentry.realtime.events, data.score, 0);
         old_pos = getPosition(Sentry.realtime.events, id, 1);
         if (old_pos !== -1) {
             Sentry.realtime.events[old_pos][0] = data.score;
diff --git a/setup.py b/setup.py
index e1b28f5856..b67819c492 100755
--- a/setup.py
+++ b/setup.py
@@ -46,7 +46,7 @@ install_requires = [
 
 setup(
     name='sentry',
-    version='2.6.1',
+    version='2.6.2',
     author='David Cramer',
     author_email='dcramer@gmail.com',
     url='http://github.com/dcramer/sentry',
