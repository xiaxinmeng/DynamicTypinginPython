commit f85e9eb73a90f2e5cb63f334a2fe22dc5883500b
Author: David Cramer <dcramer@gmail.com>
Date:   Sat Oct 19 18:43:45 2013 -0700

    Ensure datetime reflects accurate ordering for tests

diff --git a/tests/sentry/web/frontend/groups/tests.py b/tests/sentry/web/frontend/groups/tests.py
index 9fec163bfb..3c9e65829d 100644
--- a/tests/sentry/web/frontend/groups/tests.py
+++ b/tests/sentry/web/frontend/groups/tests.py
@@ -113,8 +113,10 @@ class GroupEventListTest(TestCase):
         })
 
     def test_does_render(self):
-        event = self.create_event(event_id='a' * 32)
-        event2 = self.create_event(event_id='b' * 32)
+        event = self.create_event(
+            event_id='a' * 32, datetime=timezone.now() - timedelta(minutes=1))
+        event2 = self.create_event(
+            event_id='b' * 32, datetime=timezone.now())
 
         self.login()
         resp = self.client.get(self.path)
@@ -193,8 +195,10 @@ class GroupEventListJsonTest(TestCase):
     def test_does_render(self):
         self.login()
 
-        event = self.create_event(event_id='a' * 32)
-        event2 = self.create_event(event_id='b' * 32)
+        event = self.create_event(
+            event_id='a' * 32, datetime=timezone.now() - timedelta(minutes=1))
+        event2 = self.create_event(
+            event_id='b' * 32, datetime=timezone.now())
 
         resp = self.client.get(self.path)
         assert resp.status_code == 200
