commit b4fbfba4dd9838e5a7b74c9168fdf50749e1075f
Author: Joe Borg <cyborg101010@gmail.com>
Date:   Fri Feb 24 11:47:14 2017 -0500

    Adding 'discover' command to 'config' (#4974)
    
    * Adding 'discover' command to 'config' to see where config files have been
    placed.  Added .idea/ directory to .gitignore to allow development under
    IDEA based IDEs.
    
    * Adding modifications suggested by @mattrobenolt

diff --git a/.gitignore b/.gitignore
index 81afdd56de..8d923720f2 100644
--- a/.gitignore
+++ b/.gitignore
@@ -32,3 +32,4 @@ example/db.sqlite
 /wheelhouse
 /test_cli/
 Gemfile.lock
+.idea/
diff --git a/src/sentry/runner/commands/config.py b/src/sentry/runner/commands/config.py
index 49281eda87..72a10cc4ee 100644
--- a/src/sentry/runner/commands/config.py
+++ b/src/sentry/runner/commands/config.py
@@ -92,3 +92,12 @@ def generate_secret_key():
     "Generate a new cryptographically secure secret key value."
     from sentry.runner.settings import generate_secret_key
     click.echo(generate_secret_key())
+
+
+@config.command()
+def discover():
+    "Print paths to config files."
+    from sentry.runner.settings import discover_configs
+    _, py, yaml = discover_configs()
+    click.echo(py)
+    click.echo(yaml)
