commit a2f40080866f7dac27706e1f60be1bbccab86567
Author: Evan Purkhiser <evanpurkhiser@gmail.com>
Date:   Fri Jul 12 12:34:52 2019 -0700

    ref(ui): Add margin to auth config loading error (#13987)

diff --git a/src/sentry/static/sentry/app/views/auth/login.jsx b/src/sentry/static/sentry/app/views/auth/login.jsx
index ec2cc4b77e..5525f5ce4d 100644
--- a/src/sentry/static/sentry/app/views/auth/login.jsx
+++ b/src/sentry/static/sentry/app/views/auth/login.jsx
@@ -94,7 +94,7 @@ class Login extends React.Component {
         </Header>
         {loading && <LoadingIndicator />}
         {error && (
-          <LoadingError
+          <StyledLoadingError
             message={t('Unable to load authentication configuration')}
             onRetry={this.fetchData}
           />
@@ -109,6 +109,10 @@ class Login extends React.Component {
   }
 }
 
+const StyledLoadingError = styled(LoadingError)`
+  margin: ${space(2)};
+`;
+
 const Header = styled('div')`
   border-bottom: 1px solid ${p => p.theme.borderLight};
   padding: 20px 40px 0;
