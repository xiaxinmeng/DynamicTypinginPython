commit 5cbd4b7b450138a83d00ede3f1fc7c5d131a01f3
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Oct 15 11:19:09 2014 +0100

    Remove unused template_info util

diff --git a/src/sentry/utils/template_info.py b/src/sentry/utils/template_info.py
deleted file mode 100644
index 90946a5c46..0000000000
--- a/src/sentry/utils/template_info.py
+++ /dev/null
@@ -1,44 +0,0 @@
-"""
-sentry.utils.template_info
-~~~~~~~~~~~~~~~~~~~~~~~~~~
-
-:copyright: (c) 2010-2014 by the Sentry Team, see AUTHORS for more details.
-:license: BSD, see LICENSE for more details.
-"""
-
-
-def linebreak_iter(template_source):
-    yield 0
-    p = template_source.find('\n')
-    while p >= 0:
-        yield p + 1
-        p = template_source.find('\n', p + 1)
-    yield len(template_source) + 1
-
-
-def get_template_info(template_info):
-    source, start, end, name = template_info
-
-    lineno = None
-    upto = 0
-    source_lines = []
-    for num, next in enumerate(linebreak_iter(source)):
-        if start >= upto and end <= next:
-            lineno = num
-        source_lines.append((num, source[upto:next]))
-        upto = next
-
-    if not source_lines or lineno is None:
-        return {}
-
-    pre_context = source_lines[max(lineno - 3, 0):lineno]
-    post_context = source_lines[(lineno + 1):(lineno + 4)]
-    context_line = source_lines[lineno][1]
-
-    return {
-        'filename': name,
-        'pre_context': pre_context,
-        'context_line': context_line,
-        'lineno': lineno,
-        'post_context': post_context,
-    }
