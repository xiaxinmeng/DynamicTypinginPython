commit 608c4c8ab70d2da2607b3bb925d37979a593b185
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Tue Apr 22 02:53:40 2014 -0700

    Fix newfound lint errors

diff --git a/src/sentry/plugins/bases/notify.py b/src/sentry/plugins/bases/notify.py
index 79337ce793..b938a8041b 100644
--- a/src/sentry/plugins/bases/notify.py
+++ b/src/sentry/plugins/bases/notify.py
@@ -103,7 +103,7 @@ class NotificationPlugin(Plugin):
         event = create_sample_event(project, default='python')
         return self.post_process(event.group, event, is_new=True, is_sample=False)
 
-    ## plugin hooks
+    # plugin hooks
 
     def post_process(self, group, event, is_new, is_sample, **kwargs):
         if not is_new:
diff --git a/src/sentry/plugins/sentry_useragents/models.py b/src/sentry/plugins/sentry_useragents/models.py
index 9d280d62a1..52ecbaff06 100644
--- a/src/sentry/plugins/sentry_useragents/models.py
+++ b/src/sentry/plugins/sentry_useragents/models.py
@@ -89,4 +89,5 @@ class OsPlugin(UserAgentPlugin):
 
         return tag
 
+
 register(OsPlugin)
diff --git a/src/sentry/utils/dates.py b/src/sentry/utils/dates.py
index 49ee6a2c63..b2407967cf 100644
--- a/src/sentry/utils/dates.py
+++ b/src/sentry/utils/dates.py
@@ -30,7 +30,7 @@ def get_sql_date_trunc(col, db='default', grouper='hour'):
     # TODO: does extract work for sqlite?
     if engine.startswith('oracle'):
         method = DATE_TRUNC_GROUPERS['oracle'].get(grouper, DATE_TRUNC_GROUPERS['default'][grouper])
-        if not '"' in col:
+        if '"' not in col:
             col = '"%s"' % col.upper()
     else:
         method = DATE_TRUNC_GROUPERS['default'][grouper]
diff --git a/src/sentry/utils/debug.py b/src/sentry/utils/debug.py
index 82bdbd00f7..3dd2fbbfb2 100644
--- a/src/sentry/utils/debug.py
+++ b/src/sentry/utils/debug.py
@@ -117,12 +117,12 @@ class ProfileMiddleware(object):
                 total += time
                 filename = fields[6].split(":")[0]
 
-                if not filename in mystats:
+                if filename not in mystats:
                     mystats[filename] = 0
                 mystats[filename] += time
 
                 group = self.get_group(filename)
-                if not group in mygroups:
+                if group not in mygroups:
                     mygroups[group] = 0
                 mygroups[group] += time
 
diff --git a/src/sentry/utils/imports.py b/src/sentry/utils/imports.py
index 370cf73ace..3b9bb057d4 100644
--- a/src/sentry/utils/imports.py
+++ b/src/sentry/utils/imports.py
@@ -11,7 +11,7 @@ import sys
 
 class ModuleProxyCache(dict):
     def __missing__(self, key):
-        if not '.' in key:
+        if '.' not in key:
             return __import__(key)
 
         module_name, class_name = key.rsplit('.', 1)
