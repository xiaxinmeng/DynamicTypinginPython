commit daf8361ac702b21a54fc867df78abb0910f18bbc
Author: Bryan Chow <bryan@fullfactor.com>
Date:   Tue Sep 7 22:22:36 2010 -0700

    Remove duplicate testBadData from RemoteSentryTest

diff --git a/sentry/tests/tests.py b/sentry/tests/tests.py
index e422925b16..2f0a43a522 100644
--- a/sentry/tests/tests.py
+++ b/sentry/tests/tests.py
@@ -567,14 +567,6 @@ class RemoteSentryTest(TestCase):
         self.assertEquals(resp.status_code, 403)
         self.assertEquals(resp.content, 'Bad data')
 
-    def testBadData(self):
-        resp = self.client.post(reverse('sentry-store'), {
-            'key': settings.KEY,
-            'data': 'hello world',
-        })
-        self.assertEquals(resp.status_code, 403)
-        self.assertEquals(resp.content, 'Bad data')
-
     def testCorrectData(self):
         kwargs = {'message': 'hello', 'server_name': 'not_dcramer.local', 'level': 40}
         data = {
