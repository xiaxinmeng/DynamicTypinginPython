commit a42c12d463f649e4dc5e3cc60fa0f1edba84cda8
Author: Evan Purkhiser <evanpurkhiser@gmail.com>
Date:   Thu Oct 3 14:45:36 2019 -0700

    fix(frontend): Correct proptype for statuspage (#14938)

diff --git a/src/sentry/static/sentry/app/sentryTypes.jsx b/src/sentry/static/sentry/app/sentryTypes.jsx
index 7f3c018136..70e47cf147 100644
--- a/src/sentry/static/sentry/app/sentryTypes.jsx
+++ b/src/sentry/static/sentry/app/sentryTypes.jsx
@@ -68,7 +68,10 @@ export const Config = PropTypes.shape({
   termsUrl: PropTypes.string,
   urlPrefix: PropTypes.string,
   user: User,
-  statuspage: PropTypes.string,
+  statuspage: PropTypes.shape({
+    id: PropTypes.string,
+    api_host: PropTypes.string,
+  }),
   version: PropTypes.shape({
     current: PropTypes.string,
     build: PropTypes.string,
