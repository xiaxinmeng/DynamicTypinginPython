commit 3b2858088043406b29973244eb86850d2383ceea
Author: Armin Ronacher <armin.ronacher@active-4.com>
Date:   Tue Mar 14 23:58:06 2017 +0100

    Use symbolserver.options to match other patterns

diff --git a/src/sentry/lang/native/systemsymbols.py b/src/sentry/lang/native/systemsymbols.py
index 4e943b5f9a..79bf73edcd 100644
--- a/src/sentry/lang/native/systemsymbols.py
+++ b/src/sentry/lang/native/systemsymbols.py
@@ -23,7 +23,7 @@ def lookup_system_symbols(symbols, sdk_info=None, cpu_name=None):
     if not options.get('symbolserver.enabled'):
         return
 
-    url = '%s/lookup' % options.get('symbolserver.url').rstrip('/')
+    url = '%s/lookup' % options.get('symbolserver.options')['url'].rstrip('/')
     sess = Session()
     symbol_query = {
         'sdk_id': sdk_info_to_sdk_id(sdk_info),
diff --git a/src/sentry/options/defaults.py b/src/sentry/options/defaults.py
index 599e2db99a..d048c4ef2b 100644
--- a/src/sentry/options/defaults.py
+++ b/src/sentry/options/defaults.py
@@ -87,4 +87,5 @@ register('filestore.options', default={'location': '/tmp/sentry-files'}, flags=F
 
 # Symbol server
 register('symbolserver.enabled', default=False, flags=FLAG_ALLOW_EMPTY | FLAG_PRIORITIZE_DISK)
-register('symbolserver.url', default='http://127.0.0.1:3000', flags=FLAG_ALLOW_EMPTY | FLAG_PRIORITIZE_DISK)
+register('symbolserver.options', default={'url': 'http://127.0.0.1:3000'},
+         flags=FLAG_ALLOW_EMPTY | FLAG_PRIORITIZE_DISK)
