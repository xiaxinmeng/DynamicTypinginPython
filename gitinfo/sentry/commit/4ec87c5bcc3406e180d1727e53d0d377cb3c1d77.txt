commit 4ec87c5bcc3406e180d1727e53d0d377cb3c1d77
Author: David Cramer <dcramer@gmail.com>
Date:   Fri May 20 16:54:11 2011 -0700

    Ensure LOG_LEVELS is a list, not a generator (fixes #159)

diff --git a/CHANGES b/CHANGES
index cd564b3314..4e52506573 100644
--- a/CHANGES
+++ b/CHANGES
@@ -1,3 +1,7 @@
+1.8.4.1
+
+* Fixed an error that was causing LOG_LEVELS to not display correctly.
+
 1.8.4
 
 * The Sentry base client will now pass along the timestamp from when the
diff --git a/sentry/conf/settings.py b/sentry/conf/settings.py
index c178518ed4..8c326e5106 100644
--- a/sentry/conf/settings.py
+++ b/sentry/conf/settings.py
@@ -17,7 +17,7 @@ for k in dir(settings):
     if k.startswith('SENTRY_'):
         locals()[k.split('SENTRY_', 1)[1]] = getattr(settings, k)
 
-LOG_LEVELS = ((k, _(v)) for k, v in LOG_LEVELS)
+LOG_LEVELS = [(k, _(v)) for k, v in LOG_LEVELS]
 
 if REMOTE_URL:
     if isinstance(REMOTE_URL, basestring):
diff --git a/setup.py b/setup.py
index bec7f286fa..0ba1525b85 100644
--- a/setup.py
+++ b/setup.py
@@ -39,7 +39,7 @@ except ImportError:
 
 setup(
     name='django-sentry',
-    version='1.8.4',
+    version='1.8.4.1',
     author='David Cramer',
     author_email='dcramer@gmail.com',
     url='http://github.com/dcramer/django-sentry',
