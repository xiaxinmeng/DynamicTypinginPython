commit 2d3728034ff8d0b1c16e136bc053b264f73c6c09
Author: Lyn Nagara <lyn.nagara@gmail.com>
Date:   Wed Feb 20 15:07:13 2019 -0800

    chore(percy): Mark file size text as dynamic (#12141)
    
    Prevent file size differences causing unnecessary Percy diffs

diff --git a/src/sentry/static/sentry/app/components/fileSize.jsx b/src/sentry/static/sentry/app/components/fileSize.jsx
index ddf450366f..08d8e61e40 100644
--- a/src/sentry/static/sentry/app/components/fileSize.jsx
+++ b/src/sentry/static/sentry/app/components/fileSize.jsx
@@ -1,6 +1,7 @@
 import PropTypes from 'prop-types';
 import React from 'react';
 import {formatBytes} from 'app/utils';
+import getDynamicText from 'app/utils/getDynamicText';
 
 class FileSize extends React.Component {
   static propTypes = {
@@ -8,7 +9,11 @@ class FileSize extends React.Component {
   };
 
   render() {
-    return <span>{formatBytes(this.props.bytes)}</span>;
+    return (
+      <span>
+        {getDynamicText({value: formatBytes(this.props.bytes), fixed: 'xx KB'})}
+      </span>
+    );
   }
 }
 
