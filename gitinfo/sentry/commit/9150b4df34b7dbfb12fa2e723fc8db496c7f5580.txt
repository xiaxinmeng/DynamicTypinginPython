commit 9150b4df34b7dbfb12fa2e723fc8db496c7f5580
Author: David Cramer <dcramer@gmail.com>
Date:   Sun Dec 15 20:14:51 2013 -0800

    Tweaks for new note email

diff --git a/src/sentry/templates/sentry/emails/base.html b/src/sentry/templates/sentry/emails/base.html
index fbcc2c6c9d..76c45cc59e 100644
--- a/src/sentry/templates/sentry/emails/base.html
+++ b/src/sentry/templates/sentry/emails/base.html
@@ -46,6 +46,16 @@
         td {
             text-align: center;
         }
+        blockquote {
+            color: #000;
+            border: 0;
+            padding: 0;
+            margin-left: 0;
+        }
+        blockquote cite {
+            font-weight: 500;
+            font-style: normal;
+        }
         .body {
             max-width: 600px;
             margin: 0 auto;
@@ -158,7 +168,7 @@
 
         .event h3 {
             padding: 0;
-            margin: 0;
+            margin: 0 0 4px;
             font-size: 16px;
             line-height: 20px;
             font-weight: bold;
@@ -203,16 +213,14 @@
         }
         .event .message {
             color: #898f98;
-            margin: 3px 0;
+            margin: 0 0 4px;
+            padding: 0;
             font-size: 14px;
             padding-right: 24px;
             white-space: nowrap;
             overflow: hidden;
             text-overflow: ellipsis;
         }
-        .event .message .tag {
-            float: right;
-        }
         .event .meta {
             color: #b6bcc2;
             font-size: 12px;
diff --git a/src/sentry/templates/sentry/emails/new_note.html b/src/sentry/templates/sentry/emails/new_note.html
index b801c30e81..64cee6304c 100644
--- a/src/sentry/templates/sentry/emails/new_note.html
+++ b/src/sentry/templates/sentry/emails/new_note.html
@@ -19,14 +19,14 @@
         <div class="details">
             <h3>
                 <a href="{% absolute_uri group_link %}">
-                    {{ group.message_top }}
+                    {{ group.title }}
                 </a>
             </h3>
             <p class="message">
-                {{ group.error }}
+                {{ group.message_short }}
             </p>
             <div class="meta">
-                <span class="last-seen pretty-date">{{ group.last_seen|timesince }}</span>
+                <span class="last-seen pretty-date">{{ group.last_seen }} UTC</span>
                 {% if group.avg_time_spent %}
                     <span class="time-spent">{{ group.avg_time_spent|duration }}</span>
                 {% endif %}
@@ -44,5 +44,7 @@
 
     <h2>New Note by <span class="highlight">{{ author }}</span></h2>
 
-    <blockquote class="note">{{ text|urlize|linebreaks }}</blockquote>
+    <blockquote>
+        {{ text|urlize|linebreaks }}
+    </blockquote>
 {% endblock %}
diff --git a/src/sentry/web/frontend/debug/mail.py b/src/sentry/web/frontend/debug/mail.py
index 27e25b7e8e..7c8cbaf79f 100644
--- a/src/sentry/web/frontend/debug/mail.py
+++ b/src/sentry/web/frontend/debug/mail.py
@@ -112,6 +112,7 @@ def new_note(request):
         context={
             'text': note.data['text'],
             'author': note.user,
+            'date': note.datetime,
             'group': group,
             'link': group.get_absolute_url(),
         },
