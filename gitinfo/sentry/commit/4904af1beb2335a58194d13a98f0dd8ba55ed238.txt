commit 4904af1beb2335a58194d13a98f0dd8ba55ed238
Author: David Cramer <dcramer@gmail.com>
Date:   Sun Sep 11 11:09:56 2011 -0700

    Version bump to 1.11.0

diff --git a/CHANGES b/CHANGES
index 8edc09bbfa..f41b02cf09 100644
--- a/CHANGES
+++ b/CHANGES
@@ -1,3 +1,8 @@
+1.11.0
+
+* Added license headers.
+* Removed ``sentry.helpers`` and ``sentry.routers``.
+
 1.10.1
 
 * Fixed an issue that was causing servers to show logger names.
diff --git a/sentry/client/processors.py b/sentry/client/processors.py
new file mode 100644
index 0000000000..999b6296ec
--- /dev/null
+++ b/sentry/client/processors.py
@@ -0,0 +1,30 @@
+"""
+sentry.client.processors
+~~~~~~~~~~~~~~~~~~~~~~
+
+:copyright: (c) 2010 by the Sentry Team, see AUTHORS for more details.
+:license: BSD, see LICENSE for more details.
+"""
+
+class Processor(object):
+    def process(self, data):
+        resp = self.get_data(data)
+        if resp:
+            data['extra'].update(resp)
+        return data
+    
+    def get_data(self, data):
+        return {}
+
+def sanitize_passwords_processor(data):
+    """ Asterisk out passwords from password fields in frames.
+    """
+    if 'sentry.interfaces.Stacktrace' in data:
+        if 'frames' in data['sentry.interfaces.Stacktrace']:
+            for frame in data['sentry.interfaces.Stacktrace']['frames']:
+                if 'vars' in frame:
+                    for k,v in frame['vars'].iteritems():
+                        if k.startswith('password'):
+                            # store mask as a fixed length for security
+                            frame['vars'][k] = '*'*16
+    return data
\ No newline at end of file
diff --git a/setup.py b/setup.py
index b38d59a118..dbe0f7d59d 100755
--- a/setup.py
+++ b/setup.py
@@ -38,7 +38,7 @@ if sys.version_info[:2] < (2, 5):
 
 setup(
     name='django-sentry',
-    version='1.10.1',
+    version='1.11.0',
     author='David Cramer',
     author_email='dcramer@gmail.com',
     url='http://github.com/dcramer/django-sentry',
