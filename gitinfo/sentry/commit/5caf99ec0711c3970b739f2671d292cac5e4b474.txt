commit 5caf99ec0711c3970b739f2671d292cac5e4b474
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Oct 6 22:29:31 2016 -0700

    [mocks] add signal for post-loading of mocks

diff --git a/bin/load-mocks b/bin/load-mocks
index 2cb534f659..e959e647b0 100755
--- a/bin/load-mocks
+++ b/bin/load-mocks
@@ -21,6 +21,7 @@ from sentry.models import (
     OrganizationMember, Project, Release, ReleaseFile, Team, User, UserReport,
     GroupRelease, Environment, ReleaseEnvironment
 )
+from sentry.signals import mocks_loaded
 from sentry.utils.hashlib import md5_text
 from sentry.utils.samples import create_sample_event
 
@@ -383,6 +384,8 @@ def main(num_events=1):
                 print('    > Processing pending buffers')
                 buffer.process_pending()
 
+            mocks_loaded.send(project=project, sender=__name__)
+
         OrganizationAccessRequest.objects.create_or_update(
             member=dummy_member,
             team=team,
diff --git a/src/sentry/signals.py b/src/sentry/signals.py
index c5227e7201..1a3d1d2017 100644
--- a/src/sentry/signals.py
+++ b/src/sentry/signals.py
@@ -47,3 +47,5 @@ issue_tracker_used = BetterSignal(providing_args=["plugin", "project", "user"])
 plugin_enabled = BetterSignal(providing_args=["plugin", "project", "user"])
 
 email_verified = BetterSignal(providing_args=["email"])
+
+mocks_loaded = BetterSignal(providing_args=["project"])
