commit 9a713edd3bcc232926ef66ac631c1872dc03ce23
Author: Billy Vong <billyvg@users.noreply.github.com>
Date:   Fri Sep 1 14:06:38 2017 -0700

    fix(workflow): broadcasts w/ failed api req (#6016)
    
    Simple fix for when the `/broadcasts/` api fails.
    
    Fixes JAVASCRIPT-2AD

diff --git a/src/sentry/static/sentry/app/components/sidebar/broadcasts.jsx b/src/sentry/static/sentry/app/components/sidebar/broadcasts.jsx
index 1285c385cc..548b673dea 100644
--- a/src/sentry/static/sentry/app/components/sidebar/broadcasts.jsx
+++ b/src/sentry/static/sentry/app/components/sidebar/broadcasts.jsx
@@ -56,7 +56,7 @@ const Broadcasts = React.createClass({
       method: 'GET',
       success: data => {
         this.setState({
-          broadcasts: data,
+          broadcasts: data || [],
           loading: false
         });
         this.poller = window.setTimeout(this.fetchData, POLLER_DELAY);
