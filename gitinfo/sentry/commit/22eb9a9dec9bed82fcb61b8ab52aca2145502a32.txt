commit 22eb9a9dec9bed82fcb61b8ab52aca2145502a32
Author: ckj <chriskjennings@gmail.com>
Date:   Wed Jul 29 17:13:51 2015 -0400

    Revert animation css

diff --git a/src/sentry/static/sentry/less/stream.less b/src/sentry/static/sentry/less/stream.less
index 287cea969a..d79b46b499 100644
--- a/src/sentry/static/sentry/less/stream.less
+++ b/src/sentry/static/sentry/less/stream.less
@@ -130,16 +130,18 @@
   border: 1px solid darken(@trim, 2);
   box-shadow: 0 1px 0 rgba(52, 60, 69, 0.02);
   border-radius: 2px;
-  position: relative;
+  // position: relative;
 }
 
 .group-list, .events {
   .list-unstyled;
 
   .group {
-    position: absolute;
-    left: 15px;
-    width: 100%;
+    position: relative;
+    margin: 0;
+    // position: absolute;
+    // left: 15px;
+    // width: 100%;
     border-bottom: 1px solid lighten(@trim, 8);
     padding-right: 10px;
     background: #fff;
@@ -433,6 +435,7 @@
 
 // Generate .group positions for animating
 
+/*
 .generate-groups(50);
 
 .generate-groups(@n, @i: 1, ) when (@i =< @n) {
@@ -444,7 +447,7 @@
   }
   .generate-groups(@n, (@i + 1));
 }
-
+*/
 
 /**
 * Stream Filters
