commit f72844dc1634c451c444cc3ec4ddd303c45fd446
Author: David Cramer <dcramer@gmail.com>
Date:   Fri Oct 2 10:39:17 2015 -0700

    Dont call out to is_unresolved (requires relational query)

diff --git a/src/sentry/api/endpoints/project_group_index.py b/src/sentry/api/endpoints/project_group_index.py
index f9daa22938..04df2644f9 100644
--- a/src/sentry/api/endpoints/project_group_index.py
+++ b/src/sentry/api/endpoints/project_group_index.py
@@ -208,18 +208,20 @@ class ProjectGroupIndexEndpoint(ProjectEndpoint):
 
         results = list(cursor_result)
 
+        context = serialize(
+            results, request.user, StreamGroupSerializer(
+                stats_period=stats_period
+            )
+        )
+
         # HACK: remove auto resolved entries
         if query_kwargs.get('status') == STATUS_UNRESOLVED:
-            results = [
-                r for r in results
-                if not r.is_resolved()
+            context = [
+                r for r in context
+                if r['status'] == 'unresolved'
             ]
 
-        response = Response(serialize(
-            results, request.user, StreamGroupSerializer(
-                stats_period=stats_period
-            )
-        ))
+        response = Response(context)
         response['Link'] = ', '.join([
             self.build_cursor_link(request, 'previous', cursor_result.prev),
             self.build_cursor_link(request, 'next', cursor_result.next),
