commit 98e6d879edfe8ac6590bcfe7730130165ed30ef4
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Mon Jan 9 10:47:20 2017 -0800

    webpack: work around broken webpack compression plugin
    
    See: https://github.com/webpack/compression-webpack-plugin/issues/24

diff --git a/webpack.config.js b/webpack.config.js
index 378b230d46..64a6bee136 100644
--- a/webpack.config.js
+++ b/webpack.config.js
@@ -190,7 +190,9 @@ var config = {
 // server as well as nginx when paired with the gzip_static module.
 if (IS_PRODUCTION) {
   config.plugins.push(new (require('compression-webpack-plugin'))({
-    algorithm: 'gzip',
+    algorithm: function(buffer, options, callback) {
+      require('zlib').gzip(buffer, callback);
+    },
     regExp: /\.(js|map|css|svg|html|txt|ico|eot|ttf)$/,
   }));
 }
