commit 984176b76a15217c7662e2f4498a289ea0482ba7
Author: Jess MacQueen <jessmacqueen@gmail.com>
Date:   Mon Jan 22 14:50:22 2018 -0800

    ref(project): Add remove team method to project model

diff --git a/src/sentry/models/project.py b/src/sentry/models/project.py
index ea152e2a42..30af4a7d4d 100644
--- a/src/sentry/models/project.py
+++ b/src/sentry/models/project.py
@@ -323,6 +323,12 @@ class Project(Model):
         else:
             return True
 
+    def remove_team(self, team):
+        ProjectTeam.objects.filter(
+            project=self,
+            team=team,
+        ).delete()
+
     def get_security_token(self):
         lock = locks.get(self.get_lock_key(), duration=5)
         with TimedRetryPolicy(10)(lock.acquire):
