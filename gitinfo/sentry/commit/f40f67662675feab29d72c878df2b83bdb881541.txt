commit f40f67662675feab29d72c878df2b83bdb881541
Author: Matej Minar <matej.minar@sentry.io>
Date:   Wed May 13 20:01:18 2020 +0200

    test(ui): Add allowAbsoluteDatetime test coverage to getParams (#18797)

diff --git a/tests/js/spec/components/organizations/getParams.spec.jsx b/tests/js/spec/components/organizations/getParams.spec.jsx
index 52c5b799cf..430104538a 100644
--- a/tests/js/spec/components/organizations/getParams.spec.jsx
+++ b/tests/js/spec/components/organizations/getParams.spec.jsx
@@ -158,4 +158,13 @@ describe('parseStatsPeriod', function() {
     expect(parseStatsPeriod('')).toEqual(undefined);
     expect(parseStatsPeriod('24')).toEqual({period: '24', periodLength: 's'});
   });
+
+  it('does not return start and end if `allowAbsoluteDatetime` option is passed', function() {
+    expect(
+      getParams(
+        {start: '2019-10-01T00:00:00', end: '2019-10-02T00:00:00'},
+        {allowAbsoluteDatetime: false}
+      )
+    ).toEqual({statsPeriod: '14d'});
+  });
 });
