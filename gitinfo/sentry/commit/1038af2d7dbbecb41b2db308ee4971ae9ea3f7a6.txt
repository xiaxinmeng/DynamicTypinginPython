commit 1038af2d7dbbecb41b2db308ee4971ae9ea3f7a6
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Fri Oct 13 16:22:37 2017 -0700

    ref(analytics): guid -> uuid (#6336)

diff --git a/src/sentry/analytics/event.py b/src/sentry/analytics/event.py
index ec3bb7b810..a5ab43eaf9 100644
--- a/src/sentry/analytics/event.py
+++ b/src/sentry/analytics/event.py
@@ -72,14 +72,14 @@ class Map(Attribute):
 
 
 class Event(object):
-    __slots__ = ['guid', 'attributes', 'data', 'datetime', 'type']
+    __slots__ = ['uuid', 'attributes', 'data', 'datetime', 'type']
 
     type = None
 
     attributes = ()
 
     def __init__(self, type=None, datetime=None, **items):
-        self.guid = uuid1()
+        self.uuid = uuid1()
 
         self.datetime = datetime or timezone.now()
         if type is not None:
@@ -106,7 +106,7 @@ class Event(object):
 
     def serialize(self):
         return {
-            'guid': b64encode(self.guid.bytes),
+            'uuid': b64encode(self.uuid.bytes),
             'timestamp': to_timestamp(self.datetime),
             'type': self.type,
             'data': self.data,
diff --git a/tests/sentry/analytics/test_event.py b/tests/sentry/analytics/test_event.py
index 3df9de8c12..8aa899742d 100644
--- a/tests/sentry/analytics/test_event.py
+++ b/tests/sentry/analytics/test_event.py
@@ -53,7 +53,7 @@ class EventTest(TestCase):
             },
             'type': 'example',
             'timestamp': 987552000,
-            'guid': 'AAEC',
+            'uuid': 'AAEC',
         }
 
     def test_optional_is_optional(self):
