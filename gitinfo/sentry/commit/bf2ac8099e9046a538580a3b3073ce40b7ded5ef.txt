commit bf2ac8099e9046a538580a3b3073ce40b7ded5ef
Author: Jonas Haag <jonas@lophus.org>
Date:   Mon Nov 23 10:08:47 2015 +0100

    Clarify comment in autogenerated sentry.conf.py

diff --git a/src/sentry/utils/runner.py b/src/sentry/utils/runner.py
index 3adef12447..dcab1b5dac 100644
--- a/src/sentry/utils/runner.py
+++ b/src/sentry/utils/runner.py
@@ -179,7 +179,7 @@ SENTRY_FILESTORE_OPTIONS = {
 # You MUST configure the absolute URI root for Sentry:
 SENTRY_URL_PREFIX = 'http://sentry.example.com'  # No trailing slash!
 
-# If you're using a reverse proxy, you should enable the X-Forwarded-Proto
+# If you're using a reverse SSL proxy, you should enable the X-Forwarded-Proto
 # header and uncomment the following settings
 # SECURE_PROXY_SSL_HEADER = ('HTTP_X_FORWARDED_PROTO', 'https')
 # SESSION_COOKIE_SECURE = True
