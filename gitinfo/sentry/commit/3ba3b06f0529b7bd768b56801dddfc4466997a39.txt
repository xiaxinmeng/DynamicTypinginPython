commit 3ba3b06f0529b7bd768b56801dddfc4466997a39
Author: ted kaemming <ted@getsentry.com>
Date:   Thu Aug 22 10:57:03 2019 -0700

    fix(snuba): Ensure limits are provided as integers, not floats (#14483)

diff --git a/src/sentry/tsdb/snuba.py b/src/sentry/tsdb/snuba.py
index 72f01a530c..a7a184f1b6 100644
--- a/src/sentry/tsdb/snuba.py
+++ b/src/sentry/tsdb/snuba.py
@@ -86,7 +86,7 @@ class SnubaTSDB(BaseTSDB):
         rollup, series = self.get_optimal_rollup_series(start, end, rollup)
         start = to_datetime(series[0])
         end = to_datetime(series[-1] + rollup)
-        limit = min(10000, len(keys) * ((end - start).total_seconds() / rollup))
+        limit = min(10000, int(len(keys) * ((end - start).total_seconds() / rollup)))
 
         if keys:
             result = snuba.query(
