commit 10ffa05359e38a732c0d62256c092fdef32fd1ff
Author: Ben Vinegar <ben@benv.ca>
Date:   Wed Jan 13 15:13:02 2016 -0800

    Fix sidebar filters not matching initial query (refs #2526)

diff --git a/src/sentry/static/sentry/app/views/stream/tagFilter.jsx b/src/sentry/static/sentry/app/views/stream/tagFilter.jsx
index 7613efdda4..4debf9e9fd 100644
--- a/src/sentry/static/sentry/app/views/stream/tagFilter.jsx
+++ b/src/sentry/static/sentry/app/views/stream/tagFilter.jsx
@@ -34,7 +34,7 @@ const StreamTagFilter = React.createClass({
   },
 
   componentDidMount() {
-    let select = ReactDOM.findDOMNode(this.refs.select);
+    let select = this.refs.select;
 
     let selectOpts = {
       placeholder: '--',
@@ -68,6 +68,7 @@ const StreamTagFilter = React.createClass({
 
     $(select)
       .select2(selectOpts)
+      .select2('val', this.state.value)
       .on('change', this.onSelectValue);
   },
 
@@ -111,7 +112,7 @@ const StreamTagFilter = React.createClass({
 
         {this.props.tag.predefined ?
 
-          <select ref="select" value={this.props.value} onChange={function(){}}>
+          <select ref="select" onChange={function(){}}>
             <option key="empty"></option>
             {this.props.tag.values.map((val) => {
               return (
