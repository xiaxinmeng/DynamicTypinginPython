commit cdc37ea05d2453b6180fff057e186b825ba4433c
Author: CKJ <chriskjennings@gmail.com>
Date:   Sat Jan 19 22:29:11 2013 -0800

    form input improvement pass

diff --git a/src/sentry/static/sentry/less/sentry.less b/src/sentry/static/sentry/less/sentry.less
index 39a8176496..9c2ec61d73 100644
--- a/src/sentry/static/sentry/less/sentry.less
+++ b/src/sentry/static/sentry/less/sentry.less
@@ -239,7 +239,7 @@ h3 {
     }
 
     .select2-container .select2-choice { 
-      width: 150px;
+      width: 160px;
       height: 28px;
       line-height: 28px;
       top: -3px;
@@ -247,6 +247,10 @@ h3 {
       .box-shadow(0 2px 0 rgba(185, 200, 215, .26));
     }
 
+    .select2-dropdown-open .select2-choice {
+      .box-shadow(0 0 0 2px #a7aeb7);
+    }
+
     .project-settings {
       margin: 0 4px;
       a {
@@ -324,6 +328,15 @@ h3 {
   }
   .body {
     padding: 25px 30px 10px;
+
+    .select2-drop {
+      .box-shadow(0 0 0 2px #c3c8ce);
+    }
+
+    .select2-container .select2-choice {
+      @shadow:0 0 0 2px #c3c8ce, 0 4px 0 rgba(0,0,0, .05);
+      .box-shadow(@shadow);
+    }
   }
   .btn-toolbar {
     .clearfix();
@@ -1242,6 +1255,7 @@ small {
 
 legend {
   margin-bottom: 18px;
+  border-bottom-width: 2px;
 }
 
 
@@ -1263,7 +1277,6 @@ legend {
   padding-bottom: 0;
   border-bottom: 0;
   margin: 20px 0;
-  border-bottom: 2px solid #eaedf0;
 
   .pull-right {
     margin-top: 4px;
@@ -1469,6 +1482,7 @@ legend {
   text-shadow: none;
   font-size: 13px;
   .box-shadow(0 2px 0 rgba(205,216,227, .54));
+  -webkit-font-smoothing: antialiased;
 
   i {
     color: lighten(#a4adb7, 5);
@@ -1503,6 +1517,18 @@ legend {
   }
 }
 
+.body {
+  textarea, input[type="text"], input[type="password"], input[type="datetime"], input[type="datetime-local"], input[type="date"], input[type="month"], input[type="time"], input[type="week"], input[type="number"], input[type="email"], input[type="url"], input[type="search"], input[type="tel"], input[type="color"], .uneditable-input {
+    height: auto;
+    line-height: auto;
+    border: 2px solid #c3c8ce;
+    padding: 6px 8px;
+    font-size: 15px;
+    .box-shadow(inset 0 2px 0 rgba(0,0,0, .04));
+    .border-radius(5px); 
+  }
+}
+
 .btn.disabled, .btn[disabled] {
   background: #ecf0f4;
   .opacity(55);
