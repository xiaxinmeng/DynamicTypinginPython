commit b7c30fd919a05f5af2fd433bc1e047a5d6b46e95
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Feb 27 18:42:50 2012 -0800

    Show gevent worker class as an example in the config

diff --git a/docs/quickstart/index.rst b/docs/quickstart/index.rst
index 0f95977687..69b3df3336 100644
--- a/docs/quickstart/index.rst
+++ b/docs/quickstart/index.rst
@@ -82,9 +82,6 @@ configuration, as well as the default Sentry configuration values. It will use S
         }
     }
 
-    SENTRY_WEB_HOST = '0.0.0.0'
-    SENTRY_WEB_PORT = 9000
-
     # No trailing slash!
     SENTRY_URL_PREFIX = 'http://sentry.example.com'
 
@@ -92,6 +89,17 @@ configuration, as well as the default Sentry configuration values. It will use S
     # acts as a signing token
     SENTRY_KEY = '0123456789abcde'
 
+    SENTRY_WEB_HOST = '0.0.0.0'
+    SENTRY_WEB_PORT = 9000
+    SENTRY_WEB_OPTIONS = {
+        'workers': 3,  # the number of gunicorn workers
+        # 'worker_class': 'gevent',
+    }
+
+
+.. note:: We highly recommend using the gevent worker class. To do this, simply ``pip install gevent`` and
+          adjust the worker_class setting in ``SENTRY_WEB_OPTIONS``.
+
 Running Migrations
 ------------------
 
diff --git a/sentry/utils/runner.py b/sentry/utils/runner.py
index 3da216cdb4..4b845f65ff 100644
--- a/sentry/utils/runner.py
+++ b/sentry/utils/runner.py
@@ -47,6 +47,7 @@ SENTRY_WEB_HOST = '0.0.0.0'
 SENTRY_WEB_PORT = 9000
 SENTRY_WEB_OPTIONS = {
     'workers': 3,  # the number of gunicorn workers
+    # 'worker_class': 'gevent',
 }
 
 """
