commit f82190a7b5f7baea44297c293c76e236855aaecd
Author: Evan Purkhiser <evanpurkhiser@gmail.com>
Date:   Mon Jul 6 16:02:28 2020 -0700

    feat(analytics): Log usage of the NumberDragControl component (#19727)
    
    Co-authored-by: Billy Vong <billyvg@users.noreply.github.com>

diff --git a/src/sentry/static/sentry/app/components/numberDragControl.tsx b/src/sentry/static/sentry/app/components/numberDragControl.tsx
index c864d4275d..0eb5627291 100644
--- a/src/sentry/static/sentry/app/components/numberDragControl.tsx
+++ b/src/sentry/static/sentry/app/components/numberDragControl.tsx
@@ -3,6 +3,7 @@ import styled from '@emotion/styled';
 
 import {IconArrow} from 'app/icons';
 import space from 'app/styles/space';
+import {trackAnalyticsEvent} from 'app/utils/analytics';
 
 type NumberDragControlProps = {
   onChange: (delta: number, event: React.MouseEvent<HTMLDivElement>) => void;
@@ -40,6 +41,14 @@ class NumberDragControl extends React.Component<Props, State> {
           if (event.button !== 0) {
             return;
           }
+
+          // XXX(epurkhiser): We can remove this later, just curious if people
+          // are actually using the drag control
+          trackAnalyticsEvent({
+            eventName: 'Number Drag Control: Clicked',
+            eventKey: 'number_drag_control.clicked',
+          });
+
           event.currentTarget.requestPointerLock();
           this.setState({isClicked: true});
         }}
