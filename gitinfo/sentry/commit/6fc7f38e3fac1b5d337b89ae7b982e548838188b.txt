commit 6fc7f38e3fac1b5d337b89ae7b982e548838188b
Author: ckj <chriskjennings@gmail.com>
Date:   Fri Dec 4 15:39:57 2015 -0800

    Vertically align graph

diff --git a/src/sentry/static/sentry/app/components/organizationIssueList.jsx b/src/sentry/static/sentry/app/components/organizationIssueList.jsx
index 0aebd55a0d..4765132169 100644
--- a/src/sentry/static/sentry/app/components/organizationIssueList.jsx
+++ b/src/sentry/static/sentry/app/components/organizationIssueList.jsx
@@ -43,12 +43,12 @@ const OrganizationIssueList = React.createClass({
         <div className="pull-right">
           <div className="btn-group">
             <Link to={path}
-                  className={'btn btn-default' + (status === 'unresolved' ? ' active' : '')}>
+                  className={'btn btn-sm btn-default' + (status === 'unresolved' ? ' active' : '')}>
               {t('Unresolved')}
             </Link>
             <Link to={path}
                   query={{status: ''}}
-                  className={'btn btn-default' + (status === '' ? ' active' : '')}>
+                  className={'btn btn-sm btn-default' + (status === '' ? ' active' : '')}>
               {t('All Issues')}
             </Link>
           </div>
diff --git a/src/sentry/static/sentry/app/views/myIssues/assignedToMe.jsx b/src/sentry/static/sentry/app/views/myIssues/assignedToMe.jsx
index 8d81f9d4dd..0ffad3ec19 100644
--- a/src/sentry/static/sentry/app/views/myIssues/assignedToMe.jsx
+++ b/src/sentry/static/sentry/app/views/myIssues/assignedToMe.jsx
@@ -9,7 +9,7 @@ const AssignedToMe = React.createClass({
   },
 
   getTitle() {
-    return t('Assigned to Me');
+    return t('Assigned to me');
   },
 
   render() {
diff --git a/src/sentry/static/sentry/less/stream.less b/src/sentry/static/sentry/less/stream.less
index f5b633f95f..5cd3a70afe 100644
--- a/src/sentry/static/sentry/less/stream.less
+++ b/src/sentry/static/sentry/less/stream.less
@@ -681,6 +681,10 @@
       }
     }
 
+    .event-graph {
+      padding-top: 7px;
+    }
+
     &:last-child {
       border-bottom: 0;
     }
