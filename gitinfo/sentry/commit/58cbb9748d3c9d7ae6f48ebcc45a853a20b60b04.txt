commit 58cbb9748d3c9d7ae6f48ebcc45a853a20b60b04
Author: Armin Ronacher <armin.ronacher@active-4.com>
Date:   Wed Feb 1 23:29:27 2017 +0100

    Fix failure on close when no symbolication took place
    
    Fixes SENTRY-2PN

diff --git a/src/sentry/lang/native/plugin.py b/src/sentry/lang/native/plugin.py
index da351d1cb4..d2f5112048 100644
--- a/src/sentry/lang/native/plugin.py
+++ b/src/sentry/lang/native/plugin.py
@@ -348,6 +348,7 @@ class NativeStacktraceProcessor(StacktraceProcessor):
     def __init__(self, *args, **kwargs):
         StacktraceProcessor.__init__(self, *args, **kwargs)
         debug_meta = self.data.get('debug_meta')
+        self.sym = None
         if debug_meta:
             self.available = True
             self.debug_meta = debug_meta
@@ -357,7 +358,9 @@ class NativeStacktraceProcessor(StacktraceProcessor):
 
     def close(self):
         StacktraceProcessor.close(self)
-        self.sym.close()
+        if self.sym is not None:
+            self.sym.close()
+            self.sym = None
 
     def preprocess_related_data(self):
         if not self.available:
