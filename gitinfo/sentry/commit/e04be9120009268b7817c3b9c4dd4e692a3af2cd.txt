commit e04be9120009268b7817c3b9c4dd4e692a3af2cd
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Nov 25 18:35:26 2014 -0800

    Throw a proper error when we discard an event

diff --git a/src/sentry/web/api.py b/src/sentry/web/api.py
index a54376f044..f871b68e28 100644
--- a/src/sentry/web/api.py
+++ b/src/sentry/web/api.py
@@ -356,7 +356,7 @@ class StoreView(APIView):
 
         if cache.get(cache_key) is not None:
             logger.warning('Discarded recent duplicate event from project %s/%s (id=%s)', project.team.slug, project.slug, event_id)
-            return
+            raise InvalidRequest('An event with the same ID already exists.')
 
         # We filter data immediately before it ever gets into the queue
         inst = SensitiveDataFilter()
