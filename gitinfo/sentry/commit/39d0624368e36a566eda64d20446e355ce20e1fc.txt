commit 39d0624368e36a566eda64d20446e355ce20e1fc
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Mar 29 11:50:30 2012 -0700

    Correctly inject the cookie header if it was present (fixes GH-402)

diff --git a/sentry/interfaces.py b/sentry/interfaces.py
index a31188ec3b..3da4f12e98 100644
--- a/sentry/interfaces.py
+++ b/sentry/interfaces.py
@@ -334,8 +334,8 @@ class Http(Interface):
         # strip them out
         if headers and 'Cookie' in headers:
             cookies = headers.pop('Cookie')
-            if not self.cookies:
-                cookies = self.cookies
+            if cookies:
+                self.cookies = cookies
         self.headers = headers or {}
         self.env = env or {}
 
