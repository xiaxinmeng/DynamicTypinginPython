commit 535fff3dcf15513412f3d6e43c7f57a088ff41c0
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Mar 29 12:13:54 2012 -0700

    Lint/pep8

diff --git a/sentry/interfaces.py b/sentry/interfaces.py
index 3da4f12e98..bad72bd3ea 100644
--- a/sentry/interfaces.py
+++ b/sentry/interfaces.py
@@ -304,7 +304,6 @@ class Http(Interface):
     # methods as defined by http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html + PATCH
     METHODS = ('GET', 'POST', 'PUT', 'OPTIONS', 'HEAD', 'DELETE', 'TRACE', 'CONNECT', 'PATCH')
 
-
     def __init__(self, url, method=None, data=None, query_string=None, cookies=None, headers=None, env=None, **kwargs):
         if data is None:
             data = {}
diff --git a/sentry/middleware.py b/sentry/middleware.py
index 5aff446dfc..5667e9e258 100644
--- a/sentry/middleware.py
+++ b/sentry/middleware.py
@@ -7,5 +7,3 @@ class SentryMiddleware(object):
         # HACK: bootstrap some env crud if we havent yet
         if not settings.URL_PREFIX:
             settings.URL_PREFIX = request.build_absolute_uri(reverse('sentry')).strip('/')
-
-
diff --git a/sentry/models.py b/sentry/models.py
index 1f309d8489..3576dbbf96 100644
--- a/sentry/models.py
+++ b/sentry/models.py
@@ -224,6 +224,7 @@ class PendingProjectMember(Model):
             logger = logging.getLogger('sentry.mail.errors')
             logger.exception(e)
 
+
 class ProjectMember(Model):
     """
     Identifies relationships between projects and users, including
@@ -457,7 +458,6 @@ class Event(MessageBase):
 
     objects = BaseManager()
 
-
     class Meta:
         verbose_name = _('message')
         verbose_name_plural = _('messages')
@@ -509,7 +509,7 @@ class Event(MessageBase):
             m, c = k.rsplit('.', 1)
             cls = getattr(__import__(m, {}, {}, [c]), c)
             v = cls(**v)
-            result.append((v.score, k,  v))
+            result.append((v.score, k, v))
         return SortedDict((k, v) for _, k, v in sorted(result, key=lambda x: x[0], reverse=True))
 
     def get_version(self):
@@ -740,4 +740,4 @@ post_save.connect(
     sender=Group,
     dispatch_uid="update_document",
     weak=False,
-)
\ No newline at end of file
+)
diff --git a/sentry/plugins/base.py b/sentry/plugins/base.py
index 0db1ab3d98..f8ca35deaa 100644
--- a/sentry/plugins/base.py
+++ b/sentry/plugins/base.py
@@ -383,4 +383,3 @@ class Plugin(IPlugin):
     it will happen, or happen more than once.
     """
     __metaclass__ = PluginMount
-
diff --git a/sentry/plugins/sentry_sites/__init__.py b/sentry/plugins/sentry_sites/__init__.py
index 32399bc117..dd320a1006 100644
--- a/sentry/plugins/sentry_sites/__init__.py
+++ b/sentry/plugins/sentry_sites/__init__.py
@@ -41,4 +41,4 @@ class SitesPlugin(Plugin):
             'group': group,
             'unique_sites': list(self.get_unique_sites(group)[:10]),
         })
-register(SitesPlugin)
\ No newline at end of file
+register(SitesPlugin)
diff --git a/sentry/processors/base.py b/sentry/processors/base.py
index f7a11c4bc3..57c058e145 100644
--- a/sentry/processors/base.py
+++ b/sentry/processors/base.py
@@ -22,4 +22,3 @@ def send_group_processors(group, **kwargs):
             transaction.rollback_unless_managed(using=group._state.db)
             logger = logging.getLogger('sentry.plugins')
             logger.exception('Error processing post_process() on %r: %s', inst.__class__, e)
-
diff --git a/sentry/web/__init__.py b/sentry/web/__init__.py
index d0624def33..aac3dd08b4 100644
--- a/sentry/web/__init__.py
+++ b/sentry/web/__init__.py
@@ -5,4 +5,3 @@ sentry.web
 :copyright: (c) 2010-2012 by the Sentry Team, see AUTHORS for more details.
 :license: BSD, see LICENSE for more details.
 """
-
diff --git a/sentry/wsgi.py b/sentry/wsgi.py
index f3fd18544a..c726e7dc4c 100644
--- a/sentry/wsgi.py
+++ b/sentry/wsgi.py
@@ -34,4 +34,3 @@ if settings.SESSION_FILE_PATH:
         os.makedirs(settings.SESSION_FILE_PATH)
     except OSError:
         pass
-
