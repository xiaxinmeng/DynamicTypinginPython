commit 845a2ece9ef63a68075a243d2ce2649919a8e3d2
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Oct 22 19:06:41 2014 -0700

    Correct load-mocks

diff --git a/bin/load-mocks b/bin/load-mocks
index aacde8feeb..42f14b48b7 100755
--- a/bin/load-mocks
+++ b/bin/load-mocks
@@ -8,7 +8,7 @@ from pytz import utc
 from random import randint
 
 from sentry.app import tsdb
-from sentry.models import Activity, User, Project, Team
+from sentry.models import Activity, User, Organization, Project, Team
 from sentry.utils.samples import create_sample_event
 
 def create_sample_time_series(event):
@@ -31,10 +31,17 @@ def create_sample_time_series(event):
 
 user = User.objects.filter(is_superuser=True)[0]
 
+org, _ = Organization.objects.get_or_create(
+    name='Default',
+    defaults={
+        'owner': user,
+    },
+)
+
 team, _ = Team.objects.get_or_create(
     name='Earth', slug='earth',
     defaults={
-        'owner': user,
+        'owner': org.owner,
     },
 )
 
