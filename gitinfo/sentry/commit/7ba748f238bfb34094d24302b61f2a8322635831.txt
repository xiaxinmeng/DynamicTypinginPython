commit 7ba748f238bfb34094d24302b61f2a8322635831
Author: David Cramer <dcramer@gmail.com>
Date:   Fri May 20 10:50:22 2016 -0500

    Check auth + authenticated on various endpoints
    
    Refs GH-3282
    
    @getsentry/api

diff --git a/src/sentry/api/endpoints/organization_index.py b/src/sentry/api/endpoints/organization_index.py
index 40527380b5..fed27b2113 100644
--- a/src/sentry/api/endpoints/organization_index.py
+++ b/src/sentry/api/endpoints/organization_index.py
@@ -54,7 +54,7 @@ class OrganizationIndexEndpoint(Endpoint):
             status=OrganizationStatus.VISIBLE,
         )
 
-        if request.auth:
+        if request.auth and not request.user.is_authenticated():
             if hasattr(request.auth, 'project'):
                 queryset = queryset.filter(
                     id=request.auth.project.organization_id
diff --git a/src/sentry/api/endpoints/organization_projects.py b/src/sentry/api/endpoints/organization_projects.py
index ee7a9d6236..7ae8d32d2a 100644
--- a/src/sentry/api/endpoints/organization_projects.py
+++ b/src/sentry/api/endpoints/organization_projects.py
@@ -32,7 +32,7 @@ class OrganizationProjectsEndpoint(OrganizationEndpoint):
                                           which the projects should be listed.
         :auth: required
         """
-        if request.auth:
+        if request.auth and not request.user.is_authenticated():
             # TODO: remove this, no longer supported probably
             if hasattr(request.auth, 'project'):
                 team_list = [request.auth.project.team]
diff --git a/src/sentry/api/endpoints/project_index.py b/src/sentry/api/endpoints/project_index.py
index fa50a1a57c..fa239169ce 100644
--- a/src/sentry/api/endpoints/project_index.py
+++ b/src/sentry/api/endpoints/project_index.py
@@ -49,7 +49,7 @@ class ProjectIndexEndpoint(Endpoint):
         elif status:
             queryset = queryset.none()
 
-        if request.auth:
+        if request.auth and not request.user.is_authenticated():
             if hasattr(request.auth, 'project'):
                 queryset = queryset.filter(
                     id=request.auth.project_id,
