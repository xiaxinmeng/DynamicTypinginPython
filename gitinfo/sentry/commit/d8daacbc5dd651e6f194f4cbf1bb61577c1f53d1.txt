commit d8daacbc5dd651e6f194f4cbf1bb61577c1f53d1
Author: David Cramer <dcramer@gmail.com>
Date:   Fri Aug 17 01:42:32 2012 -0700

    Add csrf to default Response context

diff --git a/sentry/plugins/base.py b/sentry/plugins/base.py
index a5f22ddcf1..939fb3a38e 100644
--- a/sentry/plugins/base.py
+++ b/sentry/plugins/base.py
@@ -10,6 +10,7 @@ __all__ = ('Plugin', 'plugins', 'register', 'unregister')
 
 import logging
 
+from django.core.context_processors import csrf
 from django.core.urlresolvers import reverse
 from django.http import HttpResponseRedirect, HttpResponse
 
@@ -30,9 +31,12 @@ class Response(object):
 
         if not context:
             context = {}
+
         if self.context:
             context.update(self.context)
 
+        context.update(csrf(request))
+
         return render_to_string(self.template, context, request)
 
 
