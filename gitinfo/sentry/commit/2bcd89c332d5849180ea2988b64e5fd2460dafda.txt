commit 2bcd89c332d5849180ea2988b64e5fd2460dafda
Author: David Cramer <dcramer@gmail.com>
Date:   Fri Sep 23 12:51:04 2011 -0700

    Move message to bottom since it tends to be multi-part

diff --git a/CHANGES b/CHANGES
index 914325e876..d18059e026 100644
--- a/CHANGES
+++ b/CHANGES
@@ -1,3 +1,8 @@
+1.11.2
+
+* Cleaned up message and group details pages (adding back some missing information).
+* Cleaned up some design around odd margins/padding.
+
 1.11.1
 
 * Fixed a bug that would cause the admin (and potentially other modules) to get loaded multiple times.
diff --git a/sentry/templates/sentry/group/details.html b/sentry/templates/sentry/group/details.html
index 5d4e079597..455a66cf30 100644
--- a/sentry/templates/sentry/group/details.html
+++ b/sentry/templates/sentry/group/details.html
@@ -78,12 +78,6 @@
             <div id="summary">
                 <h3>{% if exception_type %}{{ exception_type }}: {{ exception_value|escape }}{% else %}Summary{% endif %}</h3>
                 <table class="meta">
-                    {% if group.has_two_part_message %}
-                        <tr>
-                          <th>Full Message</th>
-                          <td><pre id="full-message">{{ group.message }}</pre></td>
-                        </tr>
-                    {% endif %}
                     {% if exception_type %}
                         <tr>
                           <th>Exception Value:</th>
@@ -97,10 +91,16 @@
                         </tr>
                     {% endif %}
                     {% if lastframe %}
-                    <tr>
-                      <th>Location:</th>
-                      <td>{{ lastframe.filename|escape }} in {{ lastframe.function|escape }}, line {{ lastframe.lineno }}</td>
-                    </tr>
+                        <tr>
+                          <th>Location:</th>
+                          <td>{{ lastframe.filename|escape }} in {{ lastframe.function|escape }}, line {{ lastframe.lineno }}</td>
+                        </tr>
+                    {% endif %}
+                    {% if group.has_two_part_message %}
+                        <tr>
+                          <th>Full Message</th>
+                          <td><pre id="full-message">{{ group.message }}</pre></td>
+                        </tr>
                     {% endif %}
                 </table>
             </div>
diff --git a/sentry/templates/sentry/group/message.html b/sentry/templates/sentry/group/message.html
index 14c539a697..44302ecc11 100644
--- a/sentry/templates/sentry/group/message.html
+++ b/sentry/templates/sentry/group/message.html
@@ -34,12 +34,6 @@
     <div id="summary">
         <h3>{% if exception_type %}{{ exception_type }}: {{ exception_value|escape }}{% else %}Summary{% endif %}</h3>
         <table class="meta">
-            {% if group.has_two_part_message %}
-                <tr>
-                  <th>Full Message</th>
-                  <td><pre id="full-message">{{ group.message }}</pre></td>
-                </tr>
-            {% endif %}
             {% if exception_type %}
                 <tr>
                   <th>Exception Value:</th>
@@ -53,10 +47,16 @@
                 </tr>
             {% endif %}
             {% if lastframe %}
-            <tr>
-              <th>Location:</th>
-              <td>{{ lastframe.filename|escape }} in {{ lastframe.function|escape }}, line {{ lastframe.lineno }}</td>
-            </tr>
+              <tr>
+                <th>Location:</th>
+                <td>{{ lastframe.filename|escape }} in {{ lastframe.function|escape }}, line {{ lastframe.lineno }}</td>
+              </tr>
+            {% endif %}
+            {% if group.has_two_part_message %}
+                <tr>
+                  <th>Full Message</th>
+                  <td><pre id="full-message">{{ group.message }}</pre></td>
+                </tr>
             {% endif %}
         </table>
     </div>
diff --git a/setup.py b/setup.py
index d2188f2141..db52eb32c0 100755
--- a/setup.py
+++ b/setup.py
@@ -38,7 +38,7 @@ if sys.version_info[:2] < (2, 5):
 
 setup(
     name='django-sentry',
-    version='1.11.1',
+    version='1.11.2',
     author='David Cramer',
     author_email='dcramer@gmail.com',
     url='http://github.com/dcramer/django-sentry',
