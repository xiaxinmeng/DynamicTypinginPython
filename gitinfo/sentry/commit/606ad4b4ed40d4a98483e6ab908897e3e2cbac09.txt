commit 606ad4b4ed40d4a98483e6ab908897e3e2cbac09
Author: Jess MacQueen <jessmacqueen@gmail.com>
Date:   Mon Sep 10 13:47:21 2018 -0700

    get repo provider from integration provider in task

diff --git a/src/sentry/tasks/integrations.py b/src/sentry/tasks/integrations.py
index 6829a1e945..7da5cdcf11 100644
--- a/src/sentry/tasks/integrations.py
+++ b/src/sentry/tasks/integrations.py
@@ -172,7 +172,7 @@ def migrate_repo(repo_id, integration_id, organization_id):
     repo = Repository.objects.get(id=repo_id)
     if installation.has_repo_access(repo):
         repo.integration_id = integration_id
-        repo.provider = 'integrations:github'
+        repo.provider = 'integrations:%s' % (integration.provider,)
         repo.save()
 
         PluginMigrator(
