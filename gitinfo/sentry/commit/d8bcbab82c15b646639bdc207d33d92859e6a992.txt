commit d8bcbab82c15b646639bdc207d33d92859e6a992
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Aug 10 10:47:51 2015 -0700

    Move debug urls to root conf

diff --git a/src/sentry/conf/urls.py b/src/sentry/conf/urls.py
index bc5deb32f4..e64d73fe2b 100644
--- a/src/sentry/conf/urls.py
+++ b/src/sentry/conf/urls.py
@@ -9,6 +9,7 @@ These are additional urls used by the Sentry-provided web server
 """
 from __future__ import absolute_import
 
+import debug_toolbar
 import os
 
 try:
@@ -53,4 +54,5 @@ urlpatterns = patterns('',
     url(r'^404/', handler404, name='error-400'),
     url(r'^_health/$', handler_healthcheck, name='healthcheck'),
     url(r'^403-csrf-failure/', CsrfFailureView.as_view(), name='error-403-csrf-failure'),
+    url(r'^__debug__/', include(debug_toolbar.urls)),
 ) + web_urlpatterns
diff --git a/src/sentry/web/urls.py b/src/sentry/web/urls.py
index ad57eb56eb..5558c80d01 100644
--- a/src/sentry/web/urls.py
+++ b/src/sentry/web/urls.py
@@ -9,8 +9,6 @@ from __future__ import absolute_import
 
 __all__ = ('urlpatterns',)
 
-import debug_toolbar
-
 from django.conf.urls import include, patterns, url
 from django.conf import settings
 
@@ -243,9 +241,6 @@ urlpatterns += patterns('',
     url(r'^accept/(?P<member_id>\d+)/(?P<token>\w+)/$', AcceptOrganizationInviteView.as_view(),
         name='sentry-accept-invite'),
 
-    # Debugger
-    url(r'^__debug__/', include(debug_toolbar.urls)),
-
     # Settings - Projects
     url(r'^(?P<organization_slug>[\w_-]+)/(?P<project_id>[\w_-]+)/get-started/$',
         sentry.web.frontend.projects.general.get_started,
