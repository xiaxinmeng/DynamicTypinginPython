commit 400322dec5aaddd1acf1d0e0ff37fedb92f37d3a
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Thu Mar 31 12:28:43 2016 -0700

    Set env var from http service to signal that it's running from uwsgi

diff --git a/src/sentry/services/http.py b/src/sentry/services/http.py
index 1b07914853..a48bdb34a1 100644
--- a/src/sentry/services/http.py
+++ b/src/sentry/services/http.py
@@ -127,6 +127,9 @@ class SentryHTTPServer(Service):
         for k, v in convert_options_to_env(self.options):
             env.setdefault(k, v)
 
+        # Signal that we're running within uwsgi
+        env['SENTRY_RUNNING_UWSGI'] = '1'
+
         # This has already been validated inside __init__
         env['SENTRY_SKIP_BACKEND_VALIDATION'] = '1'
 
