commit e3728cd6f79eb9317360f9a352b43f7163f138aa
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Feb 13 11:00:27 2012 -0800

    Gracefully handle socket errors

diff --git a/sentry/replays.py b/sentry/replays.py
index b7f91f43eb..5825b63780 100644
--- a/sentry/replays.py
+++ b/sentry/replays.py
@@ -1,6 +1,7 @@
 from httplib import HTTPConnection, HTTPSConnection
 from urllib import urlencode
 from urlparse import urlparse
+import socket
 
 
 class Replayer(object):
@@ -24,9 +25,15 @@ class Replayer(object):
             data = urlencode(data)
 
         conn = conn_cls(urlparts.netloc, timeout=5)
-        conn.request(self.method, urlparts.path, data, self.headers or {})
+        try:
+            conn.request(self.method, urlparts.path, data, self.headers or {})
 
-        response = conn.getresponse()
+            response = conn.getresponse()
+        except socket.error, e:
+            return {
+                'status': 'error',
+                'reason': str(e),
+            }
 
         return {
             'status': response.status,
