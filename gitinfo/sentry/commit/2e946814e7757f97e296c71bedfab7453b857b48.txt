commit 2e946814e7757f97e296c71bedfab7453b857b48
Author: Armin Ronacher <armin.ronacher@active-4.com>
Date:   Thu Feb 23 20:01:10 2017 +0100

    Disallow print statements in linter

diff --git a/src/sentry/lint/sentry_check.py b/src/sentry/lint/sentry_check.py
index 0afdcad7e5..6765bf8320 100644
--- a/src/sentry/lint/sentry_check.py
+++ b/src/sentry/lint/sentry_check.py
@@ -153,6 +153,17 @@ class SentryVisitor(ast.NodeVisitor):
                         col=node.col_offset,
                     ),
                 )
+        if node.id == 'print':
+            self.errors.append(B314(
+                lineno=node.lineno,
+                col=node.col_offset
+            ))
+
+    def visit_Print(self, node):
+        self.errors.append(B314(
+            lineno=node.lineno,
+            col=node.col_offset
+        ))
 
     def compose_call_path(self, node):
         if isinstance(node, ast.Attribute):
@@ -354,3 +365,9 @@ B313 = partial(
             "sentry.utils.compat.implements_to_string class decorator.",
     type=SentryCheck,
 )
+
+B314 = partial(
+    error,
+    message="B314: print functions or statements are not allowed.",
+    type=SentryCheck,
+)
