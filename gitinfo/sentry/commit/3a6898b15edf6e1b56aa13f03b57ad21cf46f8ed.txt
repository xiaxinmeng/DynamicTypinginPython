commit 3a6898b15edf6e1b56aa13f03b57ad21cf46f8ed
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Jun 14 11:44:42 2011 -0700

    Add support for defaulting config to ~/.sentry/sentry.conf.py. Ensure Sentry always pulls in default server values.

diff --git a/CHANGES b/CHANGES
index 43d4c4fdaf..918044e921 100644
--- a/CHANGES
+++ b/CHANGES
@@ -1,3 +1,10 @@
+1.8.6
+
+* Corrected an issue which was causing certain settings (like WEB_HOST) to
+  not take affect in custom configuration files.
+* The Sentry server will now pull in default server settings, as well as
+  ~/.sentry/sentry.conf.py if --config is not passed to it.
+
 1.8.5.1
 
 * Fixed a bug which caused thrashing prevention to not function correctly.
diff --git a/docs/install/index.rst b/docs/install/index.rst
index 59513bf404..f8c72c87af 100644
--- a/docs/install/index.rst
+++ b/docs/install/index.rst
@@ -126,6 +126,8 @@ configuration via --config, you will likely want to preface the file with import
 	# filename: /etc/sentry.conf.py
 	from sentry.conf.defaults import *
 
+By default, Sentry will also look for ``~/.sentry/sentry.conf.py`` and load it if it exists, and ``--config`` is not passed.
+
 .. note::
 
    The default database is SQLite, which generally does not perform very well.
diff --git a/sentry/scripts/runner.py b/sentry/scripts/runner.py
index 122187aa61..f75987862f 100644
--- a/sentry/scripts/runner.py
+++ b/sentry/scripts/runner.py
@@ -165,13 +165,18 @@ def main():
 
     (options, args) = parser.parse_args()
 
-    if options.config:
-        settings_from_file(options.config)
-
-    # TODO: we should attempt to discover settings modules
+    # Install default server values
     if not django_settings.configured:
         os.environ['DJANGO_SETTINGS_MODULE'] = 'sentry.conf.server'
 
+    if options.config:
+        # assumed to be a file
+        settings_from_file(options.config)
+    else:
+        config_path = os.path.expanduser(os.path.join('~', '.sentry', 'sentry.conf.py'))
+        if os.path.exists(config_path):
+            settings_from_file(config_path)
+
     if getattr(options, 'debug', False):
         django_settings.DEBUG = True
 
diff --git a/setup.py b/setup.py
index d68b691406..88165538fa 100644
--- a/setup.py
+++ b/setup.py
@@ -37,7 +37,7 @@ except ImportError:
 
 setup(
     name='django-sentry',
-    version='1.8.5.1',
+    version='1.8.6',
     author='David Cramer',
     author_email='dcramer@gmail.com',
     url='http://github.com/dcramer/django-sentry',
