commit 814125f7b94a8d9794044594bf80440b61dc2d60
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Fri Jan 29 13:52:14 2016 -0800

    Comment up the "empty" value for assignTo
    
    This also restores existing value of sending an empty string, rather
    than 0. Both are same result, but I feel that empty string is a bit less
    magic.

diff --git a/src/sentry/static/sentry/app/api.jsx b/src/sentry/static/sentry/app/api.jsx
index 7688f6f9ea..a76173dd96 100644
--- a/src/sentry/static/sentry/app/api.jsx
+++ b/src/sentry/static/sentry/app/api.jsx
@@ -196,7 +196,10 @@ export class Client {
 
     return this._wrapRequest(path, {
       method: 'PUT',
-      data: {assignedTo: params.member && params.member.id || 0},
+      // Sending an empty value to assignedTo is the same as "clear",
+      // so if no member exists, that implies that we want to clear the
+      // current assignee.
+      data: {assignedTo: params.member && params.member.id || ''},
       success: (response) => {
         GroupActions.assignToSuccess(id, params.id, response);
       },
