commit 7d93fb332fc0be257677445c642924b59d92c243
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Mar 6 12:51:19 2013 -0800

    Set time limits on post_process_group and plugin_post_process_group

diff --git a/src/sentry/tasks/post_process.py b/src/sentry/tasks/post_process.py
index c214509c23..594357ea09 100644
--- a/src/sentry/tasks/post_process.py
+++ b/src/sentry/tasks/post_process.py
@@ -12,18 +12,18 @@ from sentry.utils.safe import safe_execute
 from sentry.utils.queue import maybe_delay
 
 
-@task(ignore_result=True)
+@task(ignore_result=True, time_limit=15)
 def post_process_group(group, **kwargs):
     """
     Fires post processing hooks for a group.
     """
     for plugin in plugins.all():
         if safe_execute(plugin.is_enabled, group.project):
-            maybe_delay(plugin_post_process_group,
-                plugin.slug, group=group, **kwargs)
+            maybe_delay(
+                plugin_post_process_group, plugin.slug, group=group, **kwargs)
 
 
-@task(ignore_result=True)
+@task(ignore_result=True, time_limit=5)
 def plugin_post_process_group(plugin_slug, group, **kwargs):
     """
     Fires post processing hooks for a group.
