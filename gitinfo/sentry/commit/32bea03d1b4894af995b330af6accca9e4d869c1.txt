commit 32bea03d1b4894af995b330af6accca9e4d869c1
Author: Ben Vinegar <ben@benv.ca>
Date:   Fri Apr 29 14:47:19 2016 -0700

    First pass at implementing Default/Generic crumb + styles

diff --git a/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbComponents/default.jsx b/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbComponents/default.jsx
new file mode 100644
index 0000000000..b7eeb37528
--- /dev/null
+++ b/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbComponents/default.jsx
@@ -0,0 +1,51 @@
+import React from 'react';
+import _ from 'underscore';
+
+import Classifier from './classifier';
+import KeyValueList from '../keyValueList';
+
+import ContextData from '../../../contextData';
+
+const DefaultCrumbComponent = React.createClass({
+  propTypes: {
+    crumb: React.PropTypes.object.isRequired,
+  },
+
+  render() {
+    let crumb = this.props.crumb;
+
+    let message = crumb.message || crumb.data && crumb.data.message;
+
+    let body = _.chain(crumb.data)
+      .map((val, key) => [val, key])
+      .filter(([val, key]) => key !== 'message' && key !== 'level' && key !== 'classifier')
+      .map(([val, key]) => {
+        return (
+          <tr>
+            <td className="key">{key}</td>
+            <td><pre>{val + ''}</pre></td>
+          </tr>
+        );
+      })
+      .value();
+
+    return (
+      <table className="table key-value">
+        <thead>
+          <tr>
+            <td className="key">{crumb.type}</td>
+            <td>{message && <pre><code>{message}</code></pre>}</td>
+          </tr>
+        </thead>
+        {crumb.data &&
+          <tbody>
+            {body}
+          </tbody>
+        }
+      </table>
+
+    );
+  }
+});
+
+export default DefaultCrumbComponent;
diff --git a/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbs.jsx b/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbs.jsx
index a98a0b0c8b..b908c15864 100644
--- a/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbs.jsx
+++ b/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbs.jsx
@@ -4,6 +4,7 @@ import moment from 'moment';
 import GroupEventDataSection from '../eventDataSection';
 import PropTypes from '../../../proptypes';
 
+import DefaultCrumb from './breadcrumbComponents/default';
 import MessageCrumbComponent from './breadcrumbComponents/message';
 import RpcCrumbComponent from './breadcrumbComponents/rpc';
 import QueryCrumbComponent from './breadcrumbComponents/query';
@@ -52,7 +53,7 @@ const BreadcrumbsInterface = React.createClass({
   },
 
   statics: {
-    MAX_CRUMBS_WHEN_COLLAPSED: 5
+    MAX_CRUMBS_WHEN_COLLAPSED: 10
   },
 
   getInitialState() {
@@ -74,7 +75,8 @@ const BreadcrumbsInterface = React.createClass({
       let Component = CRUMB_COMPONENTS[item.type];
       let el;
       if (Component) {
-        el = <Component data={item.data} />;
+        // el = <Component data={item.data} />;
+        el = <DefaultCrumb crumb={item} />;
       } else {
         el = <div className="errors">Missing crumb "{item.type}"</div>;
       }
diff --git a/src/sentry/static/sentry/less/group-detail.less b/src/sentry/static/sentry/less/group-detail.less
index 82dfdae3e1..906d4cf3d2 100644
--- a/src/sentry/static/sentry/less/group-detail.less
+++ b/src/sentry/static/sentry/less/group-detail.less
@@ -1575,9 +1575,10 @@ ul.crumbs {
     }
 
     time, span.dt {
-      float: right;
       font-size: 12px;
       color: @gray;
+      position: absolute;
+      right: 20px
     }
 
     span.crumb-classifier {
@@ -1592,6 +1593,10 @@ ul.crumbs {
       margin-bottom: 0;
       background: none;
 
+      thead td:first-child {
+        font-weight: bold;
+      }
+
       td {
         padding-top: 0 !important;
         padding-bottom: 5px !important;
@@ -1745,19 +1750,19 @@ ul.crumbs {
       padding-top: 7px;
       clear: both;
 
-      & + .crumb-message {
-        margin-top: -9px;
-        background: #fff;
-        min-height: 0;
-        padding-top: 0;
-        padding-bottom: 6px;
-        border-left: 1px solid @trim;
-        border-right: 1px solid @trim;
-
-        .icon-container {
-          display: none;
-        }
-      }
+      //& + .crumb-message {
+      //  margin-top: -9px;
+      //  background: #fff;
+      //  min-height: 0;
+      //  padding-top: 0;
+      //  padding-bottom: 6px;
+      //  border-left: 1px solid @trim;
+      //  border-right: 1px solid @trim;
+      //
+      //  .icon-container {
+      //    display: none;
+      //  }
+      //}
 
       .icon-container {
         border-color: @blue-dark;
