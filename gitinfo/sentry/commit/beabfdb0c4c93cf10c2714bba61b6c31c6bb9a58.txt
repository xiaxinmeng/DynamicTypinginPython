commit beabfdb0c4c93cf10c2714bba61b6c31c6bb9a58
Author: Ben Vinegar <ben@benv.ca>
Date:   Wed Oct 14 13:57:17 2015 -0700

    React.findDOMNode -> ReactDOM.findDOMNode

diff --git a/package.json b/package.json
index f47628ff41..ab98f7293e 100644
--- a/package.json
+++ b/package.json
@@ -30,7 +30,7 @@
     "react-document-title": "^1.0.2",
     "react-dom": "^0.14.0",
     "react-lazy-load": "git@github.com:benvinegar/react-lazy-load.git#3abc798f58afdf34e4bf691e6324bcc636c579ae",
-    "react-router": "git@github.com:benvinegar/react-router.git#f1ff6c68facff8cd5948e56e3bf9742ffe30625c",
+    "react-router": "git@github.com:benvinegar/react-router.git#46998d030de3802ace768a673b07272586f7bf99",
     "react-sticky": "^2.5.2",
     "reflux": "^0.3.0",
     "select2": "^3.5.1",
diff --git a/src/sentry/static/sentry/app/components/assigneeSelector.jsx b/src/sentry/static/sentry/app/components/assigneeSelector.jsx
index 071df29e57..2be3c67d47 100644
--- a/src/sentry/static/sentry/app/components/assigneeSelector.jsx
+++ b/src/sentry/static/sentry/app/components/assigneeSelector.jsx
@@ -1,4 +1,5 @@
 import React from "react";
+import ReactDOM from "react-dom";
 import Reflux from "reflux";
 import classNames from "classnames";
 import api from "../api";
@@ -87,7 +88,7 @@ var AssigneeSelector = React.createClass({
   },
 
   onDropdownOpen() {
-    React.findDOMNode(this.refs.filter).focus();
+    ReactDOM.findDOMNode(this.refs.filter).focus();
   },
 
   onDropdownClose() {
@@ -118,7 +119,7 @@ var AssigneeSelector = React.createClass({
 
   componentDidUpdate(prevProps, prevState) {
     // XXX(dcramer): fix odd dedraw issue as of Chrome 45.0.2454.15 dev (64-bit)
-    var node = jQuery(React.findDOMNode(this.refs.container));
+    var node = jQuery(ReactDOM.findDOMNode(this.refs.container));
     node.hide().show(0);
   },
 
diff --git a/src/sentry/static/sentry/app/views/stream/searchBar.jsx b/src/sentry/static/sentry/app/views/stream/searchBar.jsx
index 59f1a6ca03..d63b125f3a 100644
--- a/src/sentry/static/sentry/app/views/stream/searchBar.jsx
+++ b/src/sentry/static/sentry/app/views/stream/searchBar.jsx
@@ -156,7 +156,7 @@ var SearchBar = React.createClass({
   },
 
   getCursorPosition() {
-    return React.findDOMNode(this.refs.searchInput).selectionStart;
+    return ReactDOM.findDOMNode(this.refs.searchInput).selectionStart;
   },
 
   /**
@@ -218,7 +218,7 @@ var SearchBar = React.createClass({
       this.setState(
         { query: this.state.query + ' ' },
         () => {
-          React.findDOMNode(this.refs.searchInput).setSelectionRange(cursor + 1, cursor + 1);
+          ReactDOM.findDOMNode(this.refs.searchInput).setSelectionRange(cursor + 1, cursor + 1);
           this.updateAutoCompleteItems();
         }
       );
@@ -377,7 +377,7 @@ var SearchBar = React.createClass({
       query: newQuery
     }, () => {
       // setting a new input value will lose focus; restore it
-      var node = React.findDOMNode(this.refs.searchInput);
+      var node = ReactDOM.findDOMNode(this.refs.searchInput);
       node.focus();
 
       // then update the autocomplete box with new contextTypes
diff --git a/src/sentry/static/sentry/app/views/stream/tagFilter.jsx b/src/sentry/static/sentry/app/views/stream/tagFilter.jsx
index 3644ee67ca..564b30204b 100644
--- a/src/sentry/static/sentry/app/views/stream/tagFilter.jsx
+++ b/src/sentry/static/sentry/app/views/stream/tagFilter.jsx
@@ -1,4 +1,5 @@
 import React from "react";
+import ReactDOM from "react-dom";
 import _ from "underscore";
 
 var StreamTagFilter = React.createClass({
@@ -46,7 +47,7 @@ var StreamTagFilter = React.createClass({
   },
 
   componentDidMount() {
-    let select = React.findDOMNode(this.refs.select);
+    let select = ReactDOM.findDOMNode(this.refs.select);
 
     let selectOpts = {
       placeholder: '--',
@@ -84,7 +85,7 @@ var StreamTagFilter = React.createClass({
   },
 
   componentWillUnmount() {
-    let select = React.findDOMNode(this.refs.select);
+    let select = ReactDOM.findDOMNode(this.refs.select);
     $(select).select2('destroy');
   },
 
diff --git a/tests/js/spec/views/stream/searchBar.spec.jsx b/tests/js/spec/views/stream/searchBar.spec.jsx
index a5a64d63ff..6be2d688cb 100644
--- a/tests/js/spec/views/stream/searchBar.spec.jsx
+++ b/tests/js/spec/views/stream/searchBar.spec.jsx
@@ -1,4 +1,5 @@
 import React from "react/addons";
+import ReactDOM from "react-dom";
 import api from "app/api";
 import SearchBar from "app/views/stream/searchBar";
 import SearchDropdown from "app/views/stream/searchDropdown";
@@ -133,7 +134,7 @@ describe("SearchBar", function() {
         var wrapper = React.render(<this.ContextStubbedSearchBar />, document.body).refs.wrapped;
         wrapper.state.dropdownVisible = true;
 
-        var input = React.findDOMNode(wrapper.refs.searchInput);
+        var input = ReactDOM.findDOMNode(wrapper.refs.searchInput);
 
         input.focus();
 
