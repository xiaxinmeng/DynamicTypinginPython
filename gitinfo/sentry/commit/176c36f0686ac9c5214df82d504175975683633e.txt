commit 176c36f0686ac9c5214df82d504175975683633e
Author: Ted Kaemming <ted@kaemming.com>
Date:   Tue Oct 13 17:57:10 2015 -0700

    Add a dummy backend.
    
    - Make the default backend the `DummyBackend`.
    - Make the default backend in the configuration file `RedisBackend`.

diff --git a/src/sentry/conf/server.py b/src/sentry/conf/server.py
index 38c43fbe5a..14eb4c383e 100644
--- a/src/sentry/conf/server.py
+++ b/src/sentry/conf/server.py
@@ -713,7 +713,7 @@ CACHES = {
 CACHE_VERSION = 1
 
 # Digests backend
-SENTRY_DIGESTS = 'sentry.digests.backends.redis.RedisBackend'
+SENTRY_DIGESTS = 'sentry.digests.backends.dummy.DummyBackend'
 SENTRY_DIGESTS_OPTIONS = {}
 
 # Quota backend
diff --git a/src/sentry/digests/backends/dummy.py b/src/sentry/digests/backends/dummy.py
new file mode 100644
index 0000000000..c6d39f613f
--- /dev/null
+++ b/src/sentry/digests/backends/dummy.py
@@ -0,0 +1,21 @@
+from __future__ import absolute_import
+
+from contextlib import contextmanager
+
+from sentry.digests.backends.base import Backend
+
+
+class DummyBackend(Backend):
+    def add(self, key, record):
+        pass
+
+    @contextmanager
+    def digest(self, key):
+        yield []
+
+    def schedule(self, deadline):
+        return
+        yield  # make this a generator
+
+    def maintenance(self, deadline):
+        pass
diff --git a/src/sentry/utils/runner.py b/src/sentry/utils/runner.py
index 2325014e9e..d2e1dd2a74 100644
--- a/src/sentry/utils/runner.py
+++ b/src/sentry/utils/runner.py
@@ -152,6 +152,14 @@ SENTRY_QUOTAS = 'sentry.quotas.redis.RedisQuota'
 
 SENTRY_TSDB = 'sentry.tsdb.redis.RedisTSDB'
 
+###########
+# Digests #
+###########
+
+# The digest backend powers notification summaries.
+
+SENTRY_DIGESTS = 'sentry.digests.backends.redis.RedisBackend'
+
 ################
 # File storage #
 ################
