commit fbb7427f2bb181c454dc03401b9249d19b99bc18
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Tue Oct 6 17:40:30 2015 -0700

    Use force_bytes when coercing api error responses
    
    Fixes GH-2113

diff --git a/src/sentry/web/api.py b/src/sentry/web/api.py
index 6a5e25aa5e..52e7699118 100644
--- a/src/sentry/web/api.py
+++ b/src/sentry/web/api.py
@@ -17,6 +17,7 @@ from django.core.urlresolvers import reverse
 from django.db.models import Sum, Q
 from django.http import HttpResponse, HttpResponseRedirect
 from django.utils import timezone
+from django.utils.encoding import force_bytes
 from django.views.decorators.cache import never_cache, cache_control
 from django.views.decorators.csrf import csrf_exempt
 from django.views.generic.base import View as BaseView
@@ -109,7 +110,7 @@ class APIView(BaseView):
                                       *args, **kwargs)
         except APIError as e:
             context = {
-                'error': unicode(e.msg).encode('utf-8'),
+                'error': force_bytes(e.msg, errors='replace'),
             }
             if e.name:
                 context['error_name'] = e.name
