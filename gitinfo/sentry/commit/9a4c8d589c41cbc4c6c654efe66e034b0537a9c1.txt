commit 9a4c8d589c41cbc4c6c654efe66e034b0537a9c1
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Feb 12 18:31:59 2013 -0800

    Dont error when theres no password set

diff --git a/src/sentry/utils/auth.py b/src/sentry/utils/auth.py
index a859eb9175..d07e26fecf 100644
--- a/src/sentry/utils/auth.py
+++ b/src/sentry/utils/auth.py
@@ -51,12 +51,14 @@ class EmailAuthBackend(ModelBackend):
             if '@' in username:
                 # email isn't guaranteed unique
                 for user in qs.filter(email__iexact=username):
+                    if not user.password:
+                        continue
                     if user.check_password(password):
                         return user
             return None
 
         try:
-            if user.check_password(password):
+            if user.password and user.check_password(password):
                 return user
         except ValueError:
             return None
