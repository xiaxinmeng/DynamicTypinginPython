commit d210c4f3dc32eae9df31fd3374b6ef596997f084
Author: Mark Story <mark@mark-story.com>
Date:   Mon Nov 5 16:46:00 2018 -0500

    fix(integrations) Don't call replace on null (#10401)
    
    We have quite a few repository records in production that do not have
    a URL as they do not come from an integration.
    
    Fixes JAVASCRIPT-4HJ

diff --git a/src/sentry/static/sentry/app/views/organizationIntegrations/integrationRepos.jsx b/src/sentry/static/sentry/app/views/organizationIntegrations/integrationRepos.jsx
index c7ca2b1dd7..abbaa97d34 100644
--- a/src/sentry/static/sentry/app/views/organizationIntegrations/integrationRepos.jsx
+++ b/src/sentry/static/sentry/app/views/organizationIntegrations/integrationRepos.jsx
@@ -286,7 +286,9 @@ export default class IntegrationRepos extends AsyncComponent {
                       </Box>
                       <Box>
                         <small>
-                          <a href={repo.url}>{repo.url.replace('https://', '')}</a>
+                          {repo.url && (
+                            <a href={repo.url}>{repo.url.replace('https://', '')}</a>
+                          )}
                         </small>
                       </Box>
                     </Flex>
