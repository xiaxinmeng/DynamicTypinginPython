commit f093187f97b9256e24a139396d51c2cac2d70696
Author: Chris Streeter <chris@chrisstreeter.com>
Date:   Thu Mar 15 13:56:29 2012 -0700

    Add a User interface to display user data.

diff --git a/sentry/interfaces.py b/sentry/interfaces.py
index 473ad0ab7c..2125996542 100644
--- a/sentry/interfaces.py
+++ b/sentry/interfaces.py
@@ -466,4 +466,41 @@ class Template(Interface):
 
 
 class User(Interface):
-    pass
+    def __init__(self, is_authenticated, **kwargs):
+        self.is_authenticated = is_authenticated
+        self.id = kwargs.get('id')
+        self.username = kwargs.get('username')
+        self.email = kwargs.get('email')
+
+    def serialize(self):
+        if self.is_authenticated:
+            return {
+                'is_authenticated': self.is_authenticated,
+                'id': self.id,
+                'username': self.username,
+                'email': self.email,
+            }
+        else:
+            return {
+                'is_authenticated': self.is_authenticated
+            }
+
+    def get_hash(self):
+        if not self.is_authenticated:
+            return []
+        else:
+            return [self.id, self.username, self.email]
+
+    def to_html(self, event):
+        return render_to_string('sentry/partial/interfaces/user.html', {
+            'event': event,
+            'user_authenticated': self.is_authenticated,
+            'user_id': self.id,
+            'user_username': self.username,
+            'user_email': self.email
+        })
+
+    def get_search_context(self, event):
+        return {
+            'text': [self.id, self.username, self.email]
+        }
diff --git a/sentry/templates/sentry/partial/interfaces/user.html b/sentry/templates/sentry/partial/interfaces/user.html
new file mode 100644
index 0000000000..89e02de7e8
--- /dev/null
+++ b/sentry/templates/sentry/partial/interfaces/user.html
@@ -0,0 +1,35 @@
+{% load i18n %}
+{% load sentry_helpers %}
+
+<div id="request" class="module">
+    <div class="page-header">
+        <h2>{% trans "User" %}</h2>
+    </div>
+    <div class="module-content">
+        <table class="table table-bordered table-striped">
+            <colgroup>
+                <col style="width:60px;">
+            </colgroup>
+            <tbody>
+                <tr>
+                    <th>{% trans "Authenticated:" %}</th>
+                    <td>{{ user_authenticated }}</td>
+                </tr>
+                {% if user_authenticated %}
+                    <tr>
+                        <th>{% trans "ID:" %}</th>
+                        <td>{{ user_id }}</td>
+                    </tr>
+                    <tr>
+                        <th>{% trans "Username:" %}</th>
+                        <td>{{ user_username }}</td>
+                    </tr>
+                    <tr>
+                        <th>{% trans "Email:" %}</th>
+                        <td>{{ user_email }}</td>
+                    </tr>
+                {% endif %}
+            </tbody>
+        </table>
+    </div>
+</div>
