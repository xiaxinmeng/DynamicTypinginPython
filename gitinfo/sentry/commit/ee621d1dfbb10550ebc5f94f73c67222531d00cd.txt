commit ee621d1dfbb10550ebc5f94f73c67222531d00cd
Author: Ben Vinegar <ben@benv.ca>
Date:   Wed Dec 9 15:46:32 2015 -0800

    Searching always refetches, even if query didn't change

diff --git a/src/sentry/static/sentry/app/views/stream.jsx b/src/sentry/static/sentry/app/views/stream.jsx
index 78a338615b..b689a0911f 100644
--- a/src/sentry/static/sentry/app/views/stream.jsx
+++ b/src/sentry/static/sentry/app/views/stream.jsx
@@ -295,9 +295,14 @@ const Stream = React.createClass({
   },
 
   onSearch(query) {
-    this.setState({
-      query: query
-    }, this.transitionTo);
+    if (query === this.state.query) {
+      // if query is the same, just re-fetch data
+      this.fetchData();
+    } else {
+      this.setState({
+        query: query
+      }, this.transitionTo);
+    }
   },
 
   onSortChange(sort) {
