commit cdebe08a5224cc60477dee7ceffd4590e720f77e
Author: Armin Ronacher <armin.ronacher@active-4.com>
Date:   Thu Feb 11 09:27:41 2016 -0800

    Fixed interface iteration code

diff --git a/src/sentry/event_manager.py b/src/sentry/event_manager.py
index 52a3613cc5..1a589545a4 100644
--- a/src/sentry/event_manager.py
+++ b/src/sentry/event_manager.py
@@ -436,7 +436,8 @@ class EventManager(object):
         data['fingerprint'] = fingerprint or ['{{ default }}']
 
         # Get rid of ephemeral interface data
-        for interface, _ in iter_interfaces():
+        for interface_class, _ in iter_interfaces():
+            interface = interface_class()
             if interface.ephemeral:
                 data.pop(interface.get_path(), None)
 
diff --git a/src/sentry/interfaces/base.py b/src/sentry/interfaces/base.py
index c0996ae54b..98b3c5374c 100644
--- a/src/sentry/interfaces/base.py
+++ b/src/sentry/interfaces/base.py
@@ -15,7 +15,7 @@ def iter_interfaces():
 
     for import_path, keys in rv.iteritems():
         iface = import_string(import_path)
-        yield iface(import_path), keys
+        yield iface, keys
 
 
 def get_interface(name):
