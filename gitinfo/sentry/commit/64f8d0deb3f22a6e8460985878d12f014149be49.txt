commit 64f8d0deb3f22a6e8460985878d12f014149be49
Author: Billy Vong <billyvg@users.noreply.github.com>
Date:   Wed Oct 2 13:32:24 2019 -0700

    ref(ts): Convert `getRouteStringFromRoutes()` to typescript (#14883)
    
    typescript me

diff --git a/src/sentry/static/sentry/app/utils/getRouteStringFromRoutes.jsx b/src/sentry/static/sentry/app/utils/getRouteStringFromRoutes.tsx
similarity index 68%
rename from src/sentry/static/sentry/app/utils/getRouteStringFromRoutes.jsx
rename to src/sentry/static/sentry/app/utils/getRouteStringFromRoutes.tsx
index e43fd86cf3..12335857bd 100644
--- a/src/sentry/static/sentry/app/utils/getRouteStringFromRoutes.jsx
+++ b/src/sentry/static/sentry/app/utils/getRouteStringFromRoutes.tsx
@@ -1,5 +1,8 @@
+import {PlainRoute} from 'react-router/lib/Route';
 import {findLastIndex} from 'lodash';
 
+type RouteWithPath = Omit<PlainRoute, 'path'> & Required<Pick<PlainRoute, 'path'>>;
+
 /**
  * Creates a route string from an array of `routes` from react-router
  *
@@ -9,12 +12,12 @@ import {findLastIndex} from 'lodash';
  * @param {Array<{}>} routes An array of route objects from react-router
  * @return String Returns a route path
  */
-export default function getRouteStringFromRoutes(routes) {
+export default function getRouteStringFromRoutes(routes: PlainRoute[]): string {
   if (!Array.isArray(routes)) {
     return '';
   }
 
-  const routesWithPaths = routes.filter(({path}) => !!path);
+  const routesWithPaths = routes.filter((route): route is RouteWithPath => !!route.path);
 
   const lastAbsolutePathIndex = findLastIndex(routesWithPaths, ({path}) =>
     path.startsWith('/')
