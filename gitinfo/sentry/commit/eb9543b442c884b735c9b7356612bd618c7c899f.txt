commit eb9543b442c884b735c9b7356612bd618c7c899f
Author: Billy Vong <billyvg@users.noreply.github.com>
Date:   Mon Feb 24 11:49:14 2020 -0800

    test(pytest): Fix "reruns" for sentry-pytest (#17224)
    
    The "reruns" option was not being properly used in `setup.cfg`, move it an env var only in CI environment.

diff --git a/.travis.yml b/.travis.yml
index 3958edba7f..e2d636911a 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -36,6 +36,7 @@ env:
     - NODE_DIR="${HOME}/.nvm/versions/node/v$(< .nvmrc)"
     - NODE_OPTIONS=--max-old-space-size=4096
     - PYTEST_SENTRY_DSN=https://6fd5cfea2d4d46b182ad214ac7810508@sentry.io/2423079
+    - PYTEST_ADDOPTS="--reruns 5"
 
 before_install:
   - &pip_install pip install --no-cache-dir "pip>=20.0.2"
diff --git a/setup.cfg b/setup.cfg
index e655307e3e..07aefa0aab 100644
--- a/setup.cfg
+++ b/setup.cfg
@@ -5,7 +5,6 @@ norecursedirs = bin dist docs htmlcov script hooks node_modules .* {args}
 looponfailroots = src tests
 selenium_driver = chrome
 self-contained-html = true
-reruns = 1
 markers =
     snuba: mark a test as requiring snuba
 
