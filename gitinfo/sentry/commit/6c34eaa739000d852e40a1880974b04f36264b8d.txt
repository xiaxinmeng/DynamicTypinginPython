commit 6c34eaa739000d852e40a1880974b04f36264b8d
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Jun 8 11:04:48 2017 -0700

    releases: populate author ahead of use

diff --git a/src/sentry/api/serializers/models/release.py b/src/sentry/api/serializers/models/release.py
index 81983af7b4..00efd83b7a 100644
--- a/src/sentry/api/serializers/models/release.py
+++ b/src/sentry/api/serializers/models/release.py
@@ -108,7 +108,7 @@ class ReleaseSerializer(Serializer):
         if commit_ids:
             commit_list = list(Commit.objects.filter(
                 id__in=commit_ids,
-            ))
+            ).select_related('author'))
             commits = {
                 c.id: d for c, d in izip(commit_list, serialize(commit_list, user))
             }
