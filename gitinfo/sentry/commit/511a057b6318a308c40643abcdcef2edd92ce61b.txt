commit 511a057b6318a308c40643abcdcef2edd92ce61b
Author: Lyn Nagara <lyn.nagara@gmail.com>
Date:   Tue Jul 30 10:26:38 2019 -0700

    ref(ts): Convert getDynamicText util to TypeScript (#14198)

diff --git a/src/sentry/static/sentry/app/utils/getDynamicText.jsx b/src/sentry/static/sentry/app/utils/getDynamicText.tsx
similarity index 59%
rename from src/sentry/static/sentry/app/utils/getDynamicText.jsx
rename to src/sentry/static/sentry/app/utils/getDynamicText.tsx
index 8da120cbda..e37250f233 100644
--- a/src/sentry/static/sentry/app/utils/getDynamicText.jsx
+++ b/src/sentry/static/sentry/app/utils/getDynamicText.tsx
@@ -2,6 +2,12 @@
 
 // Return a specified "fixed" string when we are in a testing environment
 // (more specifically in a PERCY env (e.g. CI))
-export default function getDynamicText({value, fixed}) {
+export default function getDynamicText({
+  value,
+  fixed,
+}: {
+  value: React.ReactNode;
+  fixed: string;
+}): React.ReactNode {
   return process.env.IS_PERCY ? fixed : value;
 }
