commit 1f8449c1e7dde3fed7ae041a2dc5c0df3d171ba8
Author: Ben Vinegar <ben@benv.ca>
Date:   Thu Aug 20 15:08:37 2015 -0700

    Add user tooltips to "assigned to" avatars

diff --git a/src/sentry/static/sentry/app/components/assigneeSelector.jsx b/src/sentry/static/sentry/app/components/assigneeSelector.jsx
index 470192b470..12b4b7b300 100644
--- a/src/sentry/static/sentry/app/components/assigneeSelector.jsx
+++ b/src/sentry/static/sentry/app/components/assigneeSelector.jsx
@@ -9,6 +9,8 @@ import MenuItem from "./menuItem";
 import PropTypes from "../proptypes";
 import LoadingIndicator from "../components/loadingIndicator";
 import {compareArrays, valueIsEqual} from "../utils";
+import classNames from "classnames";
+import {userDisplayName} from "../utils/formatters";
 
 var AssigneeSelector = React.createClass({
   mixins: [
@@ -30,6 +32,26 @@ var AssigneeSelector = React.createClass({
     };
   },
 
+  componentDidMount() {
+    this.attachTooltips();
+  },
+
+  componentWillUnmount() {
+    this.removeTooltips();
+    $(this.getDOMNode()).unbind();
+  },
+
+  attachTooltips() {
+    $(this.getDOMNode()).tooltip({
+      html: true,
+      selector: ".tip"
+    });
+  },
+
+  removeTooltips() {
+    $(this.getDOMNode()).tooltip("destroy");
+  },
+
   componentWillReceiveProps(nextProps) {
     var loading = GroupStore.hasStatus(nextProps.id, 'assignTo');
     if (nextProps.id != this.props.id || loading != this.state.loading) {
@@ -143,6 +165,11 @@ var AssigneeSelector = React.createClass({
       );
     }.bind(this));
 
+    var tooltipTitle = null;
+    if (assignedTo) {
+      tooltipTitle = userDisplayName(assignedTo);
+    }
+
     return (
       <div ref="container">
         <div className={className}>
@@ -154,7 +181,7 @@ var AssigneeSelector = React.createClass({
               onOpen={this.onDropdownOpen}
               onClose={this.onDropdownClose}
               title={assignedTo ?
-                <Gravatar email={assignedTo.email} className="avatar"
+                <Gravatar email={assignedTo.email} title={tooltipTitle} className="avatar tip"
                           size={48} />
                 :
                 <span className="icon-user" />
diff --git a/src/sentry/static/sentry/app/components/gravatar.jsx b/src/sentry/static/sentry/app/components/gravatar.jsx
index 28353e8b23..b6f369ce63 100644
--- a/src/sentry/static/sentry/app/components/gravatar.jsx
+++ b/src/sentry/static/sentry/app/components/gravatar.jsx
@@ -11,6 +11,7 @@ var Gravatar = React.createClass({
 
   getDefaultProps() {
     return {
+      className: "avatar",
       size: 64
     };
   },
@@ -37,7 +38,7 @@ var Gravatar = React.createClass({
     }
 
     return (
-      <img src={this.buildGravatarUrl()} className="avatar"/>
+      <img src={this.buildGravatarUrl()} className={this.props.className} title={this.props.title}/>
     );
   }
 });
