commit 8f55ef5f5bbf71eaa31311206dd7b39886a002ef
Author: James Cunningham <cunninghamjt09@gmail.com>
Date:   Tue May 9 11:15:24 2017 -0700

    Route index_tag_events into TRIGGER_TASKS so it can live with its friends. (#5365)
    
    * Route index_tag_events into TRIGGER_TASKS so it can live with its friends.
    
    * apparently changing a task name is a bad idea.

diff --git a/src/sentry/queue/routers.py b/src/sentry/queue/routers.py
index 700c438f84..3f559f101e 100644
--- a/src/sentry/queue/routers.py
+++ b/src/sentry/queue/routers.py
@@ -16,6 +16,7 @@ TRIGGER_TASKS = set([
     'sentry.tasks.post_process.plugin_post_process_group',
     'sentry.tasks.post_process.record_affected_user',
     'sentry.tasks.post_process.record_affected_code',
+    'sentry.tasks.index_event_tags',
 ])
 
 
