commit 3f88342fa6ffc44fb53ea16876b82ca25feac8c2
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Dec 30 22:00:30 2014 -0800

    Wire up browserSync for css changes

diff --git a/gulpfile.js b/gulpfile.js
index 83d829c3f7..b687fda1b5 100644
--- a/gulpfile.js
+++ b/gulpfile.js
@@ -1,6 +1,7 @@
 "use strict";
 
-var gulp = require("gulp"),
+var browserSync = require("browser-sync"),
+    gulp = require("gulp"),
     gp_cached = require("gulp-cached"),
     gp_clean = require("gulp-clean"),
     gp_concat = require("gulp-concat"),
@@ -61,6 +62,7 @@ function buildCssCompileTask(name, fileList) {
     }))
     .pipe(gp_concat(name))
     .pipe(gulp.dest(distPath))
+    .pipe(browserSync.reload({stream: true}))
     .on("error", gp_util.log);
   };
 }
@@ -107,12 +109,18 @@ gulp.task("watch:webpack", function(callback){
   var config = require('./webpack.config.js');
   config.watch = true;
   webpack(config, function(err, stats) {
-      if(err) throw new gutil.PluginError("webpack", err);
-      gp_util.log("[webpack]", stats.toString(webpackStatsOptions));
+    if(err) throw new gutil.PluginError("webpack", err);
+    gp_util.log("[webpack]", stats.toString(webpackStatsOptions));
   });
   callback();
 });
 
-gulp.task("watch", ["watch:css", "watch:webpack"]);
+gulp.task("watch", ["watch:css", "watch:webpack", "livereload"]);
+
+gulp.task("livereload", function() {
+  browserSync({
+    proxy: 'localhost:8000'
+  });
+});
 
 gulp.task("default", ["dist"]);
diff --git a/package.json b/package.json
index 8a9677d62d..e11bed0bb3 100644
--- a/package.json
+++ b/package.json
@@ -8,6 +8,7 @@
   "dependencies": {
     "bootstrap": "^3.3.1",
     "bower": "^1.2.8",
+    "browser-sync": "^1.8.2",
     "chai": "1.7.x",
     "gulp": "^3.8.10",
     "gulp-cached": "^1.0.1",
