commit 1f252018b730de89fe33f71aa246fa3f72f3bc26
Author: ckj <chriskjennings@gmail.com>
Date:   Tue Feb 2 17:55:21 2016 -0800

    Style pending stage

diff --git a/src/sentry/static/sentry/app/components/todos.jsx b/src/sentry/static/sentry/app/components/todos.jsx
index ff3a93996c..4723890e35 100644
--- a/src/sentry/static/sentry/app/components/todos.jsx
+++ b/src/sentry/static/sentry/app/components/todos.jsx
@@ -67,10 +67,12 @@ const TodoItem = React.createClass({
 
     return (
       <li className={classNames}>
+        { this.props.task['status'] == 'Pending' ? <span className="pending-bar" /> : null }
         <div className="todo-content">
           <div className="ob-checkbox">
             { this.props.task['status'] == 'Complete' ? <span className="icon-checkmark" /> : null }
             { this.props.task['status'] == 'Skipped' ? <span className="icon-x" /> : null }
+            { this.props.task['status'] == 'Pending' ? <span className="icon-ellipsis" /> : null }
           </div>
           <h4>{ this.props.task['title'] }</h4>
           <p>
@@ -286,4 +288,4 @@ const Todos = React.createClass({
   }
 });
 
-export default Todos;
\ No newline at end of file
+export default Todos;
diff --git a/src/sentry/static/sentry/less/organization.less b/src/sentry/static/sentry/less/organization.less
index bc0c7185f4..e901fc2f9f 100644
--- a/src/sentry/static/sentry/less/organization.less
+++ b/src/sentry/static/sentry/less/organization.less
@@ -379,9 +379,18 @@ table.project-list {
        }
 
        &.pending {
-        .ob-checkbox {
-          border-color: yellow;
-        }
+         position: relative;
+         .pending-bar {
+           position: absolute;
+           top: -12px;
+           right: -15px;
+           left:  -15px;
+           bottom: -10px;
+           background-size: 30px 30px;
+           background-image: linear-gradient(135deg, rgba(0, 0, 0, .03) 25%, transparent 25%,transparent 50%, rgba(0, 0, 0, .03) 50%, rgba(0, 0, 0, .03) 75%,transparent 75%, transparent);
+           animation: animate-stripes 3s linear infinite;
+           box-shadow: inset 0 2px 0 rgba(0,0,0, .05), inset 0 -2px 0 rgba(0,0,0, .05);
+          }
        }
 
        &.blur .todo-content {
@@ -403,6 +412,7 @@ table.project-list {
 
   .ob-checkbox {
     .square(46px);
+      background: #fff;
       border: 3px solid @white-darkest;
       border-radius: 46px;
       position: absolute;
@@ -417,13 +427,19 @@ table.project-list {
       top: 7px;
     }
 
-    .icon-x {
+    .icon-x, .icon-ellipsis {
       color: @gray-lightest;
       font-size: 26px;
       left: 8px;
       position: relative;
       top: 8px;
     }
+
+    .icon-ellipsis {
+      color: @gray-light;
+      top: 7px;
+      left: 7px;
+    }
   }
 
   .skip-btn {
@@ -445,8 +461,8 @@ table.project-list {
     position: absolute;
     z-index: 100;
     background: rgba(255,255,255, .75);
-    left: -20px;
-    right: -20px;
+    left: -15px;
+    right: -15px;
     bottom: -10px;
     top: -20px;
     padding: 15px;
@@ -461,6 +477,10 @@ table.project-list {
   }
 }
 
+@keyframes animate-stripes {
+  0% {background-position: 0 0;} 100% {background-position: 60px 0;}
+}
+
 .onboarding-progress-bar {
   background: rgba(255,255,255, .15);
   width: 150px;
@@ -499,7 +519,7 @@ table.project-list {
       border: 0;
       margin: 0;
       box-shadow: none;
-      zoom: .9;
+      padding: 20px 15px 0;
 
       a:not(.btn) {
         color: @link-color;
