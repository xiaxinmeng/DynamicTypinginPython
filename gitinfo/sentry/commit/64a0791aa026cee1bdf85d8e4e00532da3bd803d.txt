commit 64a0791aa026cee1bdf85d8e4e00532da3bd803d
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Feb 27 10:29:54 2020 -0800

    feat(sentry): Fill in additional organization context in frontend
    
    - Tag organization.slug
    - Add organization context

diff --git a/src/sentry/static/sentry/app/views/organizationContext.jsx b/src/sentry/static/sentry/app/views/organizationContext.jsx
index 8146b45bac..abdc567d36 100644
--- a/src/sentry/static/sentry/app/views/organizationContext.jsx
+++ b/src/sentry/static/sentry/app/views/organizationContext.jsx
@@ -197,7 +197,10 @@ const OrganizationContext = createReactClass({
 
       // Configure scope to have organization tag
       Sentry.configureScope(scope => {
+        // XXX(dcramer): this is duplicated in sdk.py on the backend
         scope.setTag('organization', organization.id);
+        scope.setTag('organization.slug', organization.slug);
+        scope.setContext('organization', {id: organization.id, slug: organization.slug});
       });
       // Make an exception for issue details in the case where it is accessed directly (e.g. from email)
       // We do not want to load the user's last used env/project in this case, otherwise will
diff --git a/src/sentry/utils/sdk.py b/src/sentry/utils/sdk.py
index 363d6a4563..67bec5b1a6 100644
--- a/src/sentry/utils/sdk.py
+++ b/src/sentry/utils/sdk.py
@@ -244,6 +244,7 @@ class RavenShim(object):
 def bind_organization_context(organization):
     helper = settings.SENTRY_ORGANIZATION_CONTEXT_HELPER
 
+    # XXX(dcramer): this is duplicated in organizationContext.jsx on the frontend
     with sentry_sdk.configure_scope() as scope:
         scope.set_tag("organization", organization.id)
         scope.set_tag("organization.slug", organization.slug)
