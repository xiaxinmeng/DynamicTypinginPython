commit ba54da00c2602ab1299d73a6627530b15e6b2c8b
Author: James Cunningham <cunninghamjt09@gmail.com>
Date:   Fri Jul 22 05:42:29 2016 -1000

    Instantiate the sentry logger so celery will log at the default level. (#3771)
    
    * Instantiate the sentry logger so celery will log at the default level.
    
    * Apparently this can happen.

diff --git a/src/sentry/conf/server.py b/src/sentry/conf/server.py
index d4a58556e3..bdb181f269 100644
--- a/src/sentry/conf/server.py
+++ b/src/sentry/conf/server.py
@@ -546,11 +546,14 @@ LOGGING = {
     },
     # LOGGING.overridable is a list of loggers including root that will change
     # based on the overridden level defined above.
-    'overridable': ['celery'],
+    'overridable': ['celery', 'sentry'],
     'loggers': {
         'celery': {
             'level': 'WARN',
         },
+        'sentry': {
+            'level': 'INFO',
+        },
         'sentry.errors': {
             'handlers': ['console'],
             'propagate': False,
diff --git a/src/sentry/logging/handlers.py b/src/sentry/logging/handlers.py
index 620d8706a3..7142ae20b7 100644
--- a/src/sentry/logging/handlers.py
+++ b/src/sentry/logging/handlers.py
@@ -50,7 +50,7 @@ class HumanRenderer(object):
             now().strftime('%H:%M:%S'),
             real_level,
             event_dict.pop('name', 'root'),
-            event_dict.pop('event'),
+            event_dict.pop('event', ''),
         )
         join = ' '.join(k + '=' + repr(v)
                for k, v in event_dict.iteritems())
