commit b8141d786b54675dd375a25186cd5a3c7a695fdc
Author: Jess MacQueen <jessmacqueen@gmail.com>
Date:   Mon Jun 11 15:57:29 2018 -0700

    fix(integrations): Use join correctly in jwt generation

diff --git a/src/sentry/integrations/atlassian_connect.py b/src/sentry/integrations/atlassian_connect.py
index 52119095b3..8d7508c4a2 100644
--- a/src/sentry/integrations/atlassian_connect.py
+++ b/src/sentry/integrations/atlassian_connect.py
@@ -30,7 +30,7 @@ def get_query_hash(uri, method, query_params=None):
         # don't include jwt query param
         if k != 'jwt':
             if isinstance(v, list):
-                param_val = [percent_encode(val) for val in v].join(',')
+                param_val = ','.join([percent_encode(val) for val in v])
             else:
                 param_val = percent_encode(v)
             sorted_query.append('%s=%s' % (percent_encode(k), param_val))
