commit c0eb696c108a27ac472510fb0e5cdc4841b432b2
Author: Billy Vong <billyvg@users.noreply.github.com>
Date:   Mon Jun 11 12:18:26 2018 -0700

    feat(ui): Add prop to `Version` to display full version string (#8702)
    
    ...instead of always showing short version (default to show short version).

diff --git a/src/sentry/static/sentry/app/components/version.jsx b/src/sentry/static/sentry/app/components/version.jsx
index 67bf72c256..0ad3e8dd6f 100644
--- a/src/sentry/static/sentry/app/components/version.jsx
+++ b/src/sentry/static/sentry/app/components/version.jsx
@@ -10,15 +10,17 @@ class Version extends React.Component {
     version: PropTypes.string.isRequired,
     orgId: PropTypes.string,
     projectId: PropTypes.string,
+    showShortVersion: PropTypes.bool,
   };
 
   static defaultProps = {
     anchor: true,
+    showShortVersion: true,
   };
 
   render() {
-    let {orgId, projectId, version} = this.props;
-    let shortVersion = getShortVersion(version);
+    let {orgId, projectId, showShortVersion, version} = this.props;
+    let versionTitle = showShortVersion ? getShortVersion(version) : version;
 
     if (this.props.anchor) {
       return (
@@ -27,11 +29,11 @@ class Version extends React.Component {
         <ProjectLink
           to={`/${orgId}/${projectId}/releases/${encodeURIComponent(version)}/`}
         >
-          <span title={version}>{shortVersion}</span>
+          <span title={version}>{versionTitle}</span>
         </ProjectLink>
       );
     }
-    return <span title={version}>{shortVersion}</span>;
+    return <span title={version}>{versionTitle}</span>;
   }
 }
 
diff --git a/tests/js/spec/components/__snapshots__/resolutionBox.spec.jsx.snap b/tests/js/spec/components/__snapshots__/resolutionBox.spec.jsx.snap
index 5293712b74..1b59b2849a 100644
--- a/tests/js/spec/components/__snapshots__/resolutionBox.spec.jsx.snap
+++ b/tests/js/spec/components/__snapshots__/resolutionBox.spec.jsx.snap
@@ -93,6 +93,7 @@ exports[`ResolutionBox render() handles inRelease 1`] = `
         key="1"
         orgId="org"
         projectId="project"
+        showShortVersion={true}
         version="1.0"
       />
       <span
@@ -151,6 +152,7 @@ exports[`ResolutionBox render() handles inRelease with actor 1`] = `
         key="2"
         orgId="org"
         projectId="project"
+        showShortVersion={true}
         version="1.0"
       />
       <span
