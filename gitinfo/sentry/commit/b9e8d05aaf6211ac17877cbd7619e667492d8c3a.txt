commit b9e8d05aaf6211ac17877cbd7619e667492d8c3a
Author: Mark Story <mark@sentry.io>
Date:   Wed Sep 4 10:06:54 2019 -0400

    fix(github) Don't KeyError on None (#14567)
    
    Not all github events have `repository` payloads. For example
    installation `added` events don't.
    
    Fixes SENTRY-CEV

diff --git a/src/sentry/integrations/github/webhook.py b/src/sentry/integrations/github/webhook.py
index fe576479cf..1c910b5509 100644
--- a/src/sentry/integrations/github/webhook.py
+++ b/src/sentry/integrations/github/webhook.py
@@ -56,7 +56,7 @@ class Webhook(object):
                 "github.missing-integration",
                 extra={
                     "action": event.get("action"),
-                    "repository": event.get("repository")["full_name"],
+                    "repository": event.get("repository", {}).get("full_name", None),
                     "external_id": six.text_type(external_id),
                 },
             )
