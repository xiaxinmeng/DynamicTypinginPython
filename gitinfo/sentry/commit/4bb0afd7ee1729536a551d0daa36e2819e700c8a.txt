commit 4bb0afd7ee1729536a551d0daa36e2819e700c8a
Author: Mark Story <mark@sentry.io>
Date:   Wed Jul 8 16:58:43 2020 -0400

    feat(performance) Add tracking event for performance overview (#19764)
    
    We want to be able to see more holistic user journeys and need
    a starting point event.
    
    Refs getsentry/reload#173

diff --git a/src/sentry/static/sentry/app/views/performance/landing.tsx b/src/sentry/static/sentry/app/views/performance/landing.tsx
index d685f419f4..9c85a02142 100644
--- a/src/sentry/static/sentry/app/views/performance/landing.tsx
+++ b/src/sentry/static/sentry/app/views/performance/landing.tsx
@@ -69,6 +69,11 @@ class PerformanceLanding extends React.Component<Props, State> {
   componentDidMount() {
     const {api, organization, selection} = this.props;
     loadOrganizationTags(api, organization.slug, selection);
+    trackAnalyticsEvent({
+      eventKey: 'performance_views.overview.view',
+      eventName: 'Performance Views: Transaction overview view',
+      organization_id: parseInt(organization.id, 10),
+    });
   }
 
   componentDidUpdate(prevProps: Props) {
