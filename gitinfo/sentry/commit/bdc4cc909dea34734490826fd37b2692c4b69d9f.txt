commit bdc4cc909dea34734490826fd37b2692c4b69d9f
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Oct 11 19:20:25 2011 -0700

    Haystack should not be required

diff --git a/sentry/conf/server.py b/sentry/conf/server.py
index df98dfb87e..22b324e950 100644
--- a/sentry/conf/server.py
+++ b/sentry/conf/server.py
@@ -104,7 +104,7 @@ INSTALLED_APPS = (
     'sentry.plugins.sentry_servers',
     'sentry.plugins.sentry_sites',
     'sentry.plugins.sentry_urls',
-    'haystack',
+    # 'haystack',
     'south',
 )
 
diff --git a/setup.py b/setup.py
index dec48352d4..ad47b5c9ae 100755
--- a/setup.py
+++ b/setup.py
@@ -27,8 +27,8 @@ install_requires = [
     'eventlet>=0.9.15',
     'south',
     # haystack support
-    'django-haystack',
-    'whoosh',
+    # 'django-haystack',
+    # 'whoosh',
 ]
 
 if sys.version_info[:2] < (2, 5):
@@ -44,9 +44,9 @@ setup(
     packages=find_packages(exclude=("example_project", "tests")),
     zip_safe=False,
     install_requires=install_requires,
-    dependency_links=[
-        'https://github.com/disqus/django-haystack/tarball/master#egg=django-haystack',
-    ],
+    # dependency_links=[
+    #     'https://github.com/disqus/django-haystack/tarball/master#egg=django-haystack',
+    # ],
     tests_require=tests_require,
     extras_require={'test': tests_require},
     test_suite='runtests.runtests',
