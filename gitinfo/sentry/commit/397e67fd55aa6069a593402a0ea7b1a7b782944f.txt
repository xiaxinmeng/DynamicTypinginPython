commit 397e67fd55aa6069a593402a0ea7b1a7b782944f
Author: Armin Ronacher <armin.ronacher@active-4.com>
Date:   Fri Dec 4 01:45:34 2015 +0100

    Only list supported locales

diff --git a/src/sentry/conf/locale.py b/src/sentry/conf/locale.py
new file mode 100644
index 0000000000..9906210fdd
--- /dev/null
+++ b/src/sentry/conf/locale.py
@@ -0,0 +1,8 @@
+import os
+import json
+import sentry
+
+
+with open(os.path.join(os.path.dirname(sentry.__file__),
+                       'locale', 'catalogs.json'), 'r') as f:
+    CATALOGS = json.load(f)['supported_locales']
diff --git a/src/sentry/conf/server.py b/src/sentry/conf/server.py
index 37be4344a3..3dd4c68f3a 100644
--- a/src/sentry/conf/server.py
+++ b/src/sentry/conf/server.py
@@ -169,6 +169,10 @@ LANGUAGES = (
     ('zh-tw', gettext_noop('Traditional Chinese')),
 )
 
+from .locale import CATALOGS
+LANGUAGES = tuple((code, name) for code, name in LANGUAGES
+                  if code in CATALOGS)
+
 SITE_ID = 1
 
 # If you set this to False, Django will make some optimizations so as not
