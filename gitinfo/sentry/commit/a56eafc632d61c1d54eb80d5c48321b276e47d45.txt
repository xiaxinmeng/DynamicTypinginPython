commit a56eafc632d61c1d54eb80d5c48321b276e47d45
Author: Mark Story <mark@mark-story.com>
Date:   Tue Jan 15 11:40:42 2019 -0500

    feat(integrations) Enable Jira-Server for all, and hide the jira-plugin (#11341)
    
    Hide Jira and Jira-AC as they are both superseded by integrations.
    Jira-AC  is the old atlassian connect based plugin for SaaS Jira
    
    Refs APP-895

diff --git a/src/sentry/api/endpoints/organization_config_integrations.py b/src/sentry/api/endpoints/organization_config_integrations.py
index eedb826fc9..9fbdb3f6bd 100644
--- a/src/sentry/api/endpoints/organization_config_integrations.py
+++ b/src/sentry/api/endpoints/organization_config_integrations.py
@@ -6,20 +6,10 @@ from sentry import integrations
 from sentry.api.bases.organization import OrganizationEndpoint
 from sentry.api.serializers import serialize, IntegrationProviderSerializer
 
-from sentry import features
-
 
 class OrganizationConfigIntegrationsEndpoint(OrganizationEndpoint):
     def get(self, request, organization):
-        has_jira_server = features.has('organizations:jira-server-integration',
-                                       organization,
-                                       actor=request.user)
-        providers = []
-        for provider in integrations.all():
-            if not has_jira_server and provider.key == 'jira_server':
-                continue
-            providers.append(provider)
-
+        providers = list(integrations.all())
         providers.sort(key=lambda i: i.key)
 
         serialized = serialize(
diff --git a/src/sentry/plugins/__init__.py b/src/sentry/plugins/__init__.py
index cea512faaa..937da300c4 100644
--- a/src/sentry/plugins/__init__.py
+++ b/src/sentry/plugins/__init__.py
@@ -12,7 +12,9 @@ HIDDEN_PLUGINS = (
     'gitlab',
     'github',
     'slack',
-    'vsts'
+    'vsts',
+    'jira',
+    'jira_ac',
 )
 
 from sentry.plugins.base import *  # NOQA
