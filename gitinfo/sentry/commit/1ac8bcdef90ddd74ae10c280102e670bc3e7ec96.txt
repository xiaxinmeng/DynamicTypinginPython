commit 1ac8bcdef90ddd74ae10c280102e670bc3e7ec96
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Wed Jan 27 19:38:18 2016 -0800

    Spit out sha with --version if known

diff --git a/src/sentry/runner/__init__.py b/src/sentry/runner/__init__.py
index b0006ca755..31c64aef93 100755
--- a/src/sentry/runner/__init__.py
+++ b/src/sentry/runner/__init__.py
@@ -10,8 +10,15 @@ from __future__ import absolute_import, print_function
 import os
 import click
 import sys
+import sentry
 from sentry.utils.imports import import_string
 
+# Parse out a pretty version for use with --version
+if sentry.__build__ is None:
+    version_string = sentry.VERSION
+else:
+    version_string = '%s (%s)' % (sentry.VERSION, sentry.__build__[:12])
+
 
 @click.group()
 @click.option(
@@ -20,7 +27,7 @@ from sentry.utils.imports import import_string
     envvar='SENTRY_CONF',
     help='Path to configuration files.',
     metavar='PATH')
-@click.version_option()
+@click.version_option(version=version_string)
 @click.pass_context
 def cli(ctx, config):
     """Sentry is cross-platform crash reporting built with love.
