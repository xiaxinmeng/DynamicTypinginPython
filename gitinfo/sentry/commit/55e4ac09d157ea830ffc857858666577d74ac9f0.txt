commit 55e4ac09d157ea830ffc857858666577d74ac9f0
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Tue Mar 18 01:24:28 2014 -0700

    Don't even use NoIfModifiedSinceMiddleware when not in DEBUG

diff --git a/src/sentry/middleware/debug.py b/src/sentry/middleware/debug.py
index d4d7b7f27e..7bca49d836 100644
--- a/src/sentry/middleware/debug.py
+++ b/src/sentry/middleware/debug.py
@@ -9,6 +9,10 @@ from django.conf import settings
 
 
 class NoIfModifiedSinceMiddleware(object):
+    def __init__(self):
+        if not settings.DEBUG:
+            from django.core.exceptions import MiddlewareNotUsed
+            raise MiddlewareNotUsed
+
     def process_request(self, request):
-        if settings.DEBUG:
-            request.META.pop('HTTP_IF_MODIFIED_SINCE', None)
+        request.META.pop('HTTP_IF_MODIFIED_SINCE', None)
