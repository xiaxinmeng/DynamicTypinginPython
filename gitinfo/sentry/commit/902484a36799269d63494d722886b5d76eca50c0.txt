commit 902484a36799269d63494d722886b5d76eca50c0
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Feb 4 15:19:22 2015 -0800

    Add breadcrumbs

diff --git a/src/sentry/static/sentry/app/components/breadcrumbs.jsx b/src/sentry/static/sentry/app/components/breadcrumbs.jsx
new file mode 100644
index 0000000000..a8cc775cac
--- /dev/null
+++ b/src/sentry/static/sentry/app/components/breadcrumbs.jsx
@@ -0,0 +1,41 @@
+/*** @jsx React.DOM */
+
+var React = require("react");
+var Reflux = require("reflux");
+
+var BreadcrumbStore = require('../stores/breadcrumbStore');
+
+var Breadcrumbs = React.createClass({
+  mixins: [Reflux.listenTo(BreadcrumbStore, "onBreadcrumbChange", "onBreadcrumbChange")],
+
+  getInitialState() {
+      return {
+          nodes: []
+      };
+  },
+
+  onBreadcrumbChange() {
+    this.setState({
+      nodes: BreadcrumbStore.getNodes()
+    });
+  },
+
+  render() {
+    var children = this.state.nodes.map((node, _) => {
+      return (
+        <li key={'bc-' + _}>
+          {node}
+        </li>
+      );
+    });
+
+    return (
+      <ul className="breadcrumb">
+        {children}
+      </ul>
+    );
+  }
+});
+
+
+module.exports = Breadcrumbs;
diff --git a/src/sentry/static/sentry/app/main.jsx b/src/sentry/static/sentry/app/main.jsx
index e10a823adc..87c658fca2 100644
--- a/src/sentry/static/sentry/app/main.jsx
+++ b/src/sentry/static/sentry/app/main.jsx
@@ -53,5 +53,6 @@ module.exports = {
 
   routes: require("./routes"),
 
-  Alerts: require("./components/alerts")
+  Alerts: require("./components/alerts"),
+  Breadcrumbs: require("./components/breadcrumbs")
 };
diff --git a/src/sentry/static/sentry/app/stores/breadcrumbStore.jsx b/src/sentry/static/sentry/app/stores/breadcrumbStore.jsx
new file mode 100644
index 0000000000..9c879db25d
--- /dev/null
+++ b/src/sentry/static/sentry/app/stores/breadcrumbStore.jsx
@@ -0,0 +1,25 @@
+/** @jsx React.DOM */
+
+var Reflux = require("reflux");
+
+var BreadcrumbStore = Reflux.createStore({
+  init() {
+    this.nodes = [];
+  },
+
+  push(node) {
+    this.nodes.push(node);
+    this.trigger();
+  },
+
+  pop() {
+    this.nodes.pop();
+    this.trigger();
+  },
+
+  getNodes() {
+    return this.nodes;
+  }
+});
+
+module.exports = BreadcrumbStore;
diff --git a/src/sentry/static/sentry/app/views/projectDetails.jsx b/src/sentry/static/sentry/app/views/projectDetails.jsx
index b205de118b..a60a35b681 100644
--- a/src/sentry/static/sentry/app/views/projectDetails.jsx
+++ b/src/sentry/static/sentry/app/views/projectDetails.jsx
@@ -5,6 +5,7 @@ var Reflux = require("reflux");
 var Router = require("react-router");
 
 var api = require("../api");
+var BreadcrumbStore = require("../stores/breadcrumbStore");
 var MemberListStore = require("../stores/memberListStore");
 
 var ProjectDetails = React.createClass({
@@ -22,6 +23,14 @@ var ProjectDetails = React.createClass({
         MemberListStore.loadInitialData(data);
       }.bind(this)
     });
+
+    BreadcrumbStore.push(
+      <a href="#">Foobar</a>
+    );
+  },
+
+  componentWillUnmount: function() {
+    BreadcrumbStore.pop();
   },
 
   getMemberListEndpoint: function() {
diff --git a/src/sentry/templates/sentry/bases/react.html b/src/sentry/templates/sentry/bases/react.html
index c79cc72c2d..e7b4017ae9 100644
--- a/src/sentry/templates/sentry/bases/react.html
+++ b/src/sentry/templates/sentry/bases/react.html
@@ -1,5 +1,15 @@
 {% extends "sentry/layout.html" %}
 
+{% block breadcrumb_outer %}
+  <div id="blk_breadcrumb"></div>
+  <script>
+  $(function(){
+    React.render(React.createFactory(Breadcrumbs)({
+    }), document.getElementById('blk_breadcrumb'));
+  });
+  </script>
+{% endblock %}
+
 {% block content %}
   <div id="blk_content"></div>
   <script>
