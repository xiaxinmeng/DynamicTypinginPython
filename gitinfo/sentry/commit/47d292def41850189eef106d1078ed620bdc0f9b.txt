commit 47d292def41850189eef106d1078ed620bdc0f9b
Author: Jess MacQueen <macqueen@users.noreply.github.com>
Date:   Mon Apr 9 15:21:51 2018 -0700

    fix(ui): Sort teams in assignee dropdown (#7978)

diff --git a/src/sentry/static/sentry/app/components/assigneeSelector.jsx b/src/sentry/static/sentry/app/components/assigneeSelector.jsx
index 75cd5a07bd..382752d15d 100644
--- a/src/sentry/static/sentry/app/components/assigneeSelector.jsx
+++ b/src/sentry/static/sentry/app/components/assigneeSelector.jsx
@@ -122,7 +122,7 @@ const AssigneeSelector = createReactClass({
     return AssigneeSelector.filterAssignees(
       (ProjectsStore.getAll().find(p => p.slug == group.project.slug) || {
         teams: [],
-      }).teams,
+      }).teams.sort((a, b) => a.slug > b.slug),
       this.state.filter
     ).map(team => ({
       id: buildTeamId(team.id),
