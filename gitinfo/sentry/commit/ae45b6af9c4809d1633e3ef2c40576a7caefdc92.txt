commit ae45b6af9c4809d1633e3ef2c40576a7caefdc92
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Jan 29 09:59:13 2014 -0800

    Correct validation error for duplicate username

diff --git a/src/sentry/web/forms/accounts.py b/src/sentry/web/forms/accounts.py
index ba0f43d85d..f867774c38 100644
--- a/src/sentry/web/forms/accounts.py
+++ b/src/sentry/web/forms/accounts.py
@@ -151,7 +151,7 @@ class AccountSettingsForm(forms.Form):
     def clean_username(self):
         value = self.cleaned_data['username']
         if User.objects.filter(username__iexact=value).exists():
-            raise forms.ValidationError_("That username is already in use.")
+            raise forms.ValidationError(_("That username is already in use."))
         return value
 
     def clean_old_password(self):
