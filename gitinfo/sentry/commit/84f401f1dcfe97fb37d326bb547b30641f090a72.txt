commit 84f401f1dcfe97fb37d326bb547b30641f090a72
Author: Christoph Burgmer <christoph@nwebs.de>
Date:   Fri Oct 8 10:03:24 2010 +0200

    Fixe usage of SENTRY_KEY in regex.
    This assumes that Django passes any bits that have been urlencoded on the way as clear strings again, which I haven't tested.

diff --git a/sentry/urls.py b/sentry/urls.py
index 180f00a79a..a330b6e206 100644
--- a/sentry/urls.py
+++ b/sentry/urls.py
@@ -1,4 +1,5 @@
 import os
+import re
 
 from django.conf.urls.defaults import *
 
@@ -14,8 +15,8 @@ urlpatterns = patterns('',
 
     # Feeds
 
-    url(r'^feeds/%s/messages.xml$' % KEY, MessageFeed(), name='sentry-feed-messages'),
-    url(r'^feeds/%s/summaries.xml$' % KEY, SummaryFeed(), name='sentry-feed-summaries'),
+    url(r'^feeds/%s/messages.xml$' % re.escape(KEY), MessageFeed(), name='sentry-feed-messages'),
+    url(r'^feeds/%s/summaries.xml$' % re.escape(KEY), SummaryFeed(), name='sentry-feed-summaries'),
 
     # JS and API
 
