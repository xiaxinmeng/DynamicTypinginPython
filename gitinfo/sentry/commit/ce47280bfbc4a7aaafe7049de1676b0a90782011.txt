commit ce47280bfbc4a7aaafe7049de1676b0a90782011
Author: Jan Michael Auer <jan.auer@sentry.io>
Date:   Mon Sep 16 13:56:24 2019 +0200

    ref(native): Enable symbol servers for all accounts (#14712)
    
    This enables symbol server project options for all accounts.

diff --git a/src/sentry/conf/server.py b/src/sentry/conf/server.py
index 64c5b17a22..f805be4f8d 100644
--- a/src/sentry/conf/server.py
+++ b/src/sentry/conf/server.py
@@ -821,7 +821,7 @@ SENTRY_FEATURES = {
     # Enable attaching arbitrary files to events.
     "organizations:event-attachments": False,
     # Allow organizations to configure custom external symbol sources.
-    "organizations:symbol-sources": False,
+    "organizations:symbol-sources": True,
     # Enable the events stream interface.
     "organizations:events": False,
     # Enable events v2 instead of the events stream
diff --git a/tests/sentry/api/serializers/test_organization.py b/tests/sentry/api/serializers/test_organization.py
index 9587603ec7..60c97e114f 100644
--- a/tests/sentry/api/serializers/test_organization.py
+++ b/tests/sentry/api/serializers/test_organization.py
@@ -27,5 +27,6 @@ class OrganizationSerializerTest(TestCase):
                 "sso-saml2",
                 "sso-basic",
                 "sentry10",
+                "symbol-sources",
             ]
         )
