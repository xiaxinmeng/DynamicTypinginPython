commit d0f2e0edcd4863a578e63ad9f75471fd96b204dc
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Aug 26 17:35:37 2015 -0700

    Auto select release tracking token

diff --git a/src/sentry/templates/sentry/layout.html b/src/sentry/templates/sentry/layout.html
index 02c44a0a7b..c567715494 100644
--- a/src/sentry/templates/sentry/layout.html
+++ b/src/sentry/templates/sentry/layout.html
@@ -307,6 +307,19 @@
   });
   </script>
   {% endblock %}
+  <script>
+  $('.auto-select').click(function() {
+    if (document.selection) {
+      var range = document.body.createTextRange();
+      range.moveToElementText(this);
+      range.select();
+    } else if (window.getSelection) {
+      var range = document.createRange();
+      range.selectNode(this);
+      window.getSelection().addRange(range);
+    }
+  });
+  </script>
   <script type="text/javascript">
   $(function() {
     // This is duplicated from <ProjectSelector>
diff --git a/src/sentry/templates/sentry/project-release-tracking.html b/src/sentry/templates/sentry/project-release-tracking.html
index 1878ef3dd3..bc96fe5b7b 100644
--- a/src/sentry/templates/sentry/project-release-tracking.html
+++ b/src/sentry/templates/sentry/project-release-tracking.html
@@ -37,7 +37,7 @@ Raven.config({
           {% csrf_token %}
             <p>Your token is a unique secret which is used to generate deploy hook URLs. If a service becomes compromised, you should regenerate the token and re-configure any deploy hooks with the newly generated URL.</p>
             <p>
-              <pre style="display:inline-block;margin-bottom:0" class="clippy">{{ token }}</pre>
+              <code style="display:inline-block" class="auto-select">{{ token }}</code>
               <button type="submit" class="btn btn-sm btn-danger" name="op" value="regenerate-token">Regenerate Token</button>
             </p>
         </form>
diff --git a/src/sentry/templates/sentry/projects/edit_key.html b/src/sentry/templates/sentry/projects/edit_key.html
index d812f03589..b6b1482103 100644
--- a/src/sentry/templates/sentry/projects/edit_key.html
+++ b/src/sentry/templates/sentry/projects/edit_key.html
@@ -39,32 +39,32 @@
               <div class="form-group">
                   <label>{% trans "DSN" %}</label>
                   <div class="controls">
-                      <div class="form-control disabled">{{ key.dsn_private }}</div>
+                      <div class="form-control disabled auto-select">{{ key.dsn_private }}</div>
                   </div>
               </div>
               <div class="form-group">
                   <label>{% trans "DSN (Public)" %}</label>
                   <div class="controls">
-                      <div class="form-control disabled">{{ key.dsn_public }}</div>
+                      <div class="form-control disabled auto-select">{{ key.dsn_public }}</div>
                   </div>
                   <div class="help-block">{% blocktrans with 'https://github.com/getsentry/raven-js' as link %}Use your public DSN with browser-based clients such as <a href="{{ link }}">raven-js</a>.{% endblocktrans %}</div>
               </div>
               <div class="form-group">
                   <label>{% trans "Public Key" %}</label>
                   <div class="controls">
-                      <div class="form-control disabled">{{ key.public_key }}</div>
+                      <div class="form-control disabled auto-select">{{ key.public_key }}</div>
                   </div>
               </div>
               <div class="form-group">
                   <label>{% trans "Secret Key" %}</label>
                   <div class="controls">
-                      <div class="form-control disabled">{{ key.secret_key }}</div>
+                      <div class="form-control disabled auto-select">{{ key.secret_key }}</div>
                   </div>
               </div>
               <div class="form-group">
                   <label>{% trans "Project ID" %}</label>
                   <div class="controls">
-                      <div class="form-control disabled">{{ key.project_id }}</div>
+                      <div class="form-control disabled auto-select">{{ key.project_id }}</div>
                   </div>
               </div>
             </div>
@@ -74,17 +74,4 @@
             <button type="submit" class="btn btn-primary">{% trans "Save Changes" %}</button>
         </div>
     </form>
-    <script>
-    $('.dsn-credentials .form-control').click(function() {
-      if (document.selection) {
-        var range = document.body.createTextRange();
-        range.moveToElementText(this);
-        range.select();
-      } else if (window.getSelection) {
-        var range = document.createRange();
-        range.selectNode(this);
-        window.getSelection().addRange(range);
-      }
-    });
-    </script>
 {% endblock %}
