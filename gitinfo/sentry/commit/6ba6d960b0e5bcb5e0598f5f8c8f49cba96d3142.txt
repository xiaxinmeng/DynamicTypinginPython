commit 6ba6d960b0e5bcb5e0598f5f8c8f49cba96d3142
Author: ted kaemming <ted@kaemming.com>
Date:   Mon Mar 13 13:10:57 2017 -0700

    Explicitly use event timestamp when recording data in similarity index. (#5071)

diff --git a/src/sentry/similarity.py b/src/sentry/similarity.py
index f59889b517..65ad43ee14 100644
--- a/src/sentry/similarity.py
+++ b/src/sentry/similarity.py
@@ -12,6 +12,7 @@ from django.conf import settings
 
 from sentry.utils import redis
 from sentry.utils.datastructures import BidirectionalMapping
+from sentry.utils.dates import to_timestamp
 from sentry.utils.iterators import shingle
 from sentry.utils.redis import load_script
 
@@ -299,6 +300,7 @@ class FeatureSet(object):
             '{}'.format(event.project_id),
             '{}'.format(event.group_id),
             items,
+            timestamp=to_timestamp(event.datetime),
         )
 
     def query(self, group):
