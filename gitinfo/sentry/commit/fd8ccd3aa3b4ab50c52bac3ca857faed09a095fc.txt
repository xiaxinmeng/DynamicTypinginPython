commit fd8ccd3aa3b4ab50c52bac3ca857faed09a095fc
Author: Damian Zaremba <damian@damianzaremba.co.uk>
Date:   Wed Sep 12 20:36:59 2012 +0100

    Pep8 fixes

diff --git a/src/sentry/conf/urls.py b/src/sentry/conf/urls.py
index 0042b86387..e99e323174 100644
--- a/src/sentry/conf/urls.py
+++ b/src/sentry/conf/urls.py
@@ -10,9 +10,9 @@ These are additional urls used by the Sentry-provided web server
 
 import os
 
-from sentry.web.urls import *
+from sentry.web.urls import urlpatterns as web_urlpatterns
 from sentry.web.frontend import generic
-from django.conf.urls.defaults import *
+from django.conf.urls.defaults import patterns, url, include
 from django.contrib import admin
 from django.views.defaults import page_not_found
 
@@ -44,4 +44,4 @@ urlpatterns = patterns('',
     url(r'^_admin_media/(?P<path>.*)$', generic.static_media,
         kwargs={'root': admin_media_dir},
         name='admin-media'),
-) + urlpatterns
+) + web_urlpatterns
diff --git a/src/sentry/manager.py b/src/sentry/manager.py
index 5895985286..03e93d9d36 100644
--- a/src/sentry/manager.py
+++ b/src/sentry/manager.py
@@ -43,6 +43,7 @@ logger = logging.getLogger('sentry.errors')
 UNSAVED = dict()
 MAX_TAG_LENGTH = 200
 
+
 def get_checksum_from_event(event):
     for interface in event.interfaces.itervalues():
         result = interface.get_hash()
