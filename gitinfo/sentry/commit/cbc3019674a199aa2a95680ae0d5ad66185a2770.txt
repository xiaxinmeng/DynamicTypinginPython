commit cbc3019674a199aa2a95680ae0d5ad66185a2770
Author: ckj <chriskjennings@gmail.com>
Date:   Thu Mar 24 13:38:37 2016 -0700

    Style breadcrumb level badges

diff --git a/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbComponents/message.jsx b/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbComponents/message.jsx
index fc3c47fafd..beacaf9893 100644
--- a/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbComponents/message.jsx
+++ b/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbComponents/message.jsx
@@ -9,9 +9,11 @@ const MessageCrumbComponent = React.createClass({
 
   render() {
     let data = this.props.data;
+    let levelClasses = 'level level-' + data.level;
+
     return (
       <p>
-        {data.level ? <span className="level">{data.level}</span> : null}
+        {data.level ? <span className={levelClasses}>{data.level}</span> : null}
         {' ' + data.message + ' '}
         {data.logger ? <span className="logger">[{data.logger}]</span> : null}
         <Classifier value={data.classifier} title="%s" hideIfEmpty={true}/>
diff --git a/src/sentry/static/sentry/less/group-detail.less b/src/sentry/static/sentry/less/group-detail.less
index 8a218bdac6..d314532193 100644
--- a/src/sentry/static/sentry/less/group-detail.less
+++ b/src/sentry/static/sentry/less/group-detail.less
@@ -1611,7 +1611,7 @@ ul.crumbs {
     &.crumbs-collapsed {
       background: @white-dark;
       margin: 0 1px;
-      box-shadow: inset 0 -1px 2px rgba(0,0,0, .04);
+      box-shadow: inset 0 -1px 2px rgba(0,0,0, .03);
 
       &:before {
         left: 34px;
@@ -1680,6 +1680,8 @@ ul.crumbs {
     }
 
     &.crumb-message {
+      padding-top: 11px;
+
       &:before {
         background: #D7E1EA;
       }
@@ -1690,23 +1692,51 @@ ul.crumbs {
 
       .icon {
         color: @blue;
-        top: 8px;
+        top: 7px;
+
         &:before {
           content: "\e909";
         }
       }
 
       span.level {
+        font-size: 11px;
         font-weight: bold;
         text-transform: capitalize;
+        background: @white-darkest;
+        color: @gray-dark;
+        border-radius: 2px;
+        display: inline-block;
+        padding: 4px 8px;
+        margin-right: 6px;
+        line-height: 1;
+        position: relative;
+        top: -2px;
+      }
+
+      span.level-info {
+        background: @blue;
+        color: @white;
+      }
+
+      span.level-error {
+        background: @orange;
+        color: @white;
       }
-      span.level:after {
-        content: ": ";
+
+      span.level-warning {
+        background: @yellow-orange;
+        color: @white;
+      }
+
+      span.level-fatal {
+        background: @red;
+        color: @white;
       }
+
       span.logger {
-        font-size: 11px;
-        padding-left: 10px;
-        color: @blue;
+        padding-left: 8px;
+        color: @gray;
       }
     }
 
diff --git a/src/sentry/utils/samples.py b/src/sentry/utils/samples.py
index 18ace7f272..d6757cfa7d 100644
--- a/src/sentry/utils/samples.py
+++ b/src/sentry/utils/samples.py
@@ -88,6 +88,7 @@ def load_data(platform, default=None):
                 "timestamp": "2016-01-17T12:30:03",
                 "data": {
                     "message": "This is a message breadcrumb",
+                    "level": "info"
                 }
             },
             {
