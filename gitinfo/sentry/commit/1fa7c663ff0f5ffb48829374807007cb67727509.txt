commit 1fa7c663ff0f5ffb48829374807007cb67727509
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Feb 29 22:20:39 2016 -0800

    Store received as float
    
    This reverts commit 1a00555f86e82f80817e7369b91fc179fc986891.

diff --git a/src/sentry/constants.py b/src/sentry/constants.py
index 985997a19c..1205cd9e3e 100644
--- a/src/sentry/constants.py
+++ b/src/sentry/constants.py
@@ -147,6 +147,7 @@ CLIENT_RESERVED_ATTRS = (
     'logger',
     'server_name',
     'site',
+    'received',
     'timestamp',
     'extra',
     'modules',
diff --git a/src/sentry/event_manager.py b/src/sentry/event_manager.py
index 1a589545a4..5b2ffa35b5 100644
--- a/src/sentry/event_manager.py
+++ b/src/sentry/event_manager.py
@@ -263,7 +263,7 @@ class EventManager(object):
             timestamp = float(timestamp.strftime('%s'))
 
         data['timestamp'] = timestamp
-        data['received'] = current_timestamp
+        data['received'] = float(timezone.now().strftime('%s'))
 
         if not data.get('event_id'):
             data['event_id'] = uuid4().hex
diff --git a/src/sentry/nodestore/django/models.py b/src/sentry/nodestore/django/models.py
index 7c44956e6c..3087610cd2 100644
--- a/src/sentry/nodestore/django/models.py
+++ b/src/sentry/nodestore/django/models.py
@@ -19,6 +19,8 @@ class Node(BaseModel):
     __core__ = False
 
     id = models.CharField(max_length=40, primary_key=True)
+    # TODO(dcramer): this being pickle and not JSON has the ability to cause
+    # hard errors as it accepts other serialization than native JSON
     data = GzippedDictField()
     timestamp = models.DateTimeField(default=timezone.now, db_index=True)
 
