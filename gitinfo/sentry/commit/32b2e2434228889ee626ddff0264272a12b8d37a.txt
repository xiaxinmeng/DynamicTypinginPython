commit 32b2e2434228889ee626ddff0264272a12b8d37a
Author: Chris Jennings <chriskjennings@gmail.com>
Date:   Thu Apr 19 15:03:07 2018 -0700

    bug(ui): fix padding for 404 and project not found components (#8086)
    
    * fix padding for 404 and project not found components
    
    * Add todo for future scale pass

diff --git a/src/sentry/static/sentry/app/components/errors/notFound.jsx b/src/sentry/static/sentry/app/components/errors/notFound.jsx
index dc4766810e..0b6e74cbfa 100644
--- a/src/sentry/static/sentry/app/components/errors/notFound.jsx
+++ b/src/sentry/static/sentry/app/components/errors/notFound.jsx
@@ -3,8 +3,9 @@ import {t} from '../../locale';
 
 class NotFound extends React.Component {
   render() {
+    // TODO(chrissy): use scale for margin values
     return (
-      <div className="alert alert-block alert-error">
+      <div className="alert alert-block alert-error" style={{margin: '30px 0 10px'}}>
         <div style={{fontSize: 24, marginBottom: 10}}>
           <span className="icon-exclamation" style={{fontSize: 20, marginRight: 10}} />
           <span>{t('Page Not Found')}</span>
diff --git a/src/sentry/static/sentry/app/views/projects/projectContext.jsx b/src/sentry/static/sentry/app/views/projects/projectContext.jsx
index 7d0d7327ee..a9c86dc46c 100644
--- a/src/sentry/static/sentry/app/views/projects/projectContext.jsx
+++ b/src/sentry/static/sentry/app/views/projects/projectContext.jsx
@@ -251,9 +251,10 @@ const ProjectContext = createReactClass({
     } else if (this.state.error) {
       switch (this.state.errorType) {
         case ERROR_TYPES.PROJECT_NOT_FOUND:
+          // TODO(chrissy): use scale for margin values
           return (
             <div className="container">
-              <div className="alert alert-block">
+              <div className="alert alert-block" style={{margin: '30px 0 10px'}}>
                 {t('The project you were looking for was not found.')}
               </div>
             </div>
