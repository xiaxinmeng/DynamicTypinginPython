commit 28daebaccb4636652aa15c195aba06905a4b9ccd
Author: David Cramer <dcramer@gmail.com>
Date:   Sun Jan 19 10:25:23 2014 -0800

    Handle case of no pending updates

diff --git a/src/sentry/buffer/redis.py b/src/sentry/buffer/redis.py
index 3ddb185758..22a4ae9ed0 100644
--- a/src/sentry/buffer/redis.py
+++ b/src/sentry/buffer/redis.py
@@ -89,6 +89,8 @@ class RedisBuffer(Buffer):
     def process_pending(self):
         for conn in self.conn.hosts.itervalues():
             keys = conn.zrange(self.pending_key, 0, -1)
+            if not keys:
+                continue
             for key in keys:
                 process_incr.apply_async(kwargs={
                     'key': key,
