commit f90c0b27a628fe7405dd3aef13466970a6850986
Author: Jan Michael Auer <account@jauer.org>
Date:   Tue Dec 11 11:00:25 2018 -0800

    fix(minidump): Filter code modules without IDs (#11004)

diff --git a/src/sentry/lang/native/minidump.py b/src/sentry/lang/native/minidump.py
index ecfe65a485..9021ec1895 100644
--- a/src/sentry/lang/native/minidump.py
+++ b/src/sentry/lang/native/minidump.py
@@ -91,10 +91,14 @@ def merge_process_state_event(data, state, cfi=None):
         'image_addr': '0x%x' % module.addr,
         'image_size': module.size,
         'name': module.name,
-    } for module in state.modules()]
+    } for module in state.modules() if is_valid_module_id(module.id)]
     data.setdefault('debug_meta', {})['images'] = images
 
 
+def is_valid_module_id(id):
+    return id is not None and id != '000000000000000000000000000000000'
+
+
 def frames_from_minidump_thread(thread):
     return [{
         'instruction_addr': '0x%x' % frame.return_address,
