commit 02fe96c22b4ab445da06bec2f8d97698faa017f0
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Oct 12 12:09:55 2011 -0700

    Change break after for pprint to be every 10 chars

diff --git a/sentry/templatetags/sentry_helpers.py b/sentry/templatetags/sentry_helpers.py
index 384228b3d5..6448876103 100644
--- a/sentry/templatetags/sentry_helpers.py
+++ b/sentry/templatetags/sentry_helpers.py
@@ -17,13 +17,13 @@ import datetime
 register = template.Library()
 
 @register.filter
-def pprint(value, length=80):
+def pprint(value, break_after=10):
     """A wrapper around pprint.pprint -- for debugging, really."""
     from pprint import pformat
 
     value = pformat(value).decode('utf-8', 'replace')
 
-    return u'\u200B'.join([value[i:i+length] for i in xrange(0, len(value), length)])
+    return u'\u200B'.join([value[i:i+break_after] for i in xrange(0, len(value), break_after)])
 
 # seriously Django?
 @register.filter
