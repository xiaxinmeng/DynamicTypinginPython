commit 21ffccbb792b8a867298abd8bb8fb225fdd44e64
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Fri Apr 29 17:35:42 2016 -0700

    Prevent file seeking negatives like builtin api

diff --git a/src/sentry/models/file.py b/src/sentry/models/file.py
index 938416da3b..a1a38562c2 100644
--- a/src/sentry/models/file.py
+++ b/src/sentry/models/file.py
@@ -244,6 +244,8 @@ class ChunkedFileBlobIndexWrapper(object):
     def seek(self, pos):
         if self.closed:
             raise ValueError('I/O operation on closed file')
+        if pos < 0:
+            raise IOError('Invalid argument')
         for n, idx in enumerate(self._indexes[::-1]):
             if idx.offset <= pos:
                 if idx != self._curidx:
diff --git a/tests/sentry/models/test_file.py b/tests/sentry/models/test_file.py
index 34c94deab2..adde02383b 100644
--- a/tests/sentry/models/test_file.py
+++ b/tests/sentry/models/test_file.py
@@ -36,7 +36,6 @@ class FileTest(TestCase):
         assert results[2].offset == 6
 
         fp = None
-
         with file1.getfile() as fp:
             assert fp.read() == 'foo bar'
             fp.seek(2)
@@ -51,6 +50,9 @@ class FileTest(TestCase):
             fp.seek(1000)
             fp.tell() == 1000
 
+            with self.assertRaises(IOError):
+                fp.seek(-1)
+
         with self.assertRaises(ValueError):
             fp.seek(0)
 
