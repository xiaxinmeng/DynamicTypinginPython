commit e24ae7ecd7c77179699de8c3ca379d257078f068
Author: ckj <chriskjennings@gmail.com>
Date:   Mon Aug 11 13:04:29 2014 -0400

    Polish plugin list

diff --git a/src/sentry/static/sentry/less/sentry.less b/src/sentry/static/sentry/less/sentry.less
index 18cf6459d1..8250ec9fbd 100644
--- a/src/sentry/static/sentry/less/sentry.less
+++ b/src/sentry/static/sentry/less/sentry.less
@@ -2054,6 +2054,15 @@ table.vars .toggle-expand {
   }
 }
 
+table.integrations {
+  td, th {
+    padding: 12px !important;
+  }
+  h4 {
+    margin-bottom: 6px;
+  }
+}
+
 .activity {
   position: relative;
 
diff --git a/src/sentry/templates/sentry/projects/plugins/list.html b/src/sentry/templates/sentry/projects/plugins/list.html
index 70108b8530..1c7fc2be21 100644
--- a/src/sentry/templates/sentry/projects/plugins/list.html
+++ b/src/sentry/templates/sentry/projects/plugins/list.html
@@ -12,7 +12,7 @@
     </div>
     <form method="POST">
         {% csrf_token %}
-        <table class="table table-striped">
+        <table class="table integrations">
             <thead>
                 <tr>
                     <th>{% trans "Integration" %}</th>
@@ -24,15 +24,12 @@
                 {% for plugin, is_enabled in project|get_plugins_with_status %}
                     <tr>
                         <td>
-                            {% if plugin.has_project_conf %}
-                                <a href="{% url 'sentry-configure-project-plugin' project.team.slug project.slug plugin.slug %}">{{ plugin.get_title }}</a>
-                            {% else %}
-                                {{ plugin.get_title }}
-                            {% endif %}
-                            <br />
+                            <h4>{{ plugin.get_title }}</h4>
                             <small>{% if plugin.author %}
                                 By {% if plugin.author_url %}<a href="{{ plugin.author_url }}">{{ plugin.author }}</a>{% else %}{{ plugin.author }}{% endif %}
-                            {% endif %}</small>
+                            {% endif %}
+                            {% if plugin.has_project_conf %} &middot; <a href="{% url 'sentry-configure-project-plugin' project.team.slug project.slug plugin.slug %}">Configure plugin</a>{% endif %}
+                            </small>
                         </td>
                         <td style="text-align:center;">
                             {% if plugin.version %}{{ plugin.version }}{% else %}<em>{% trans "n/a" %}</em>{% endif %}
@@ -48,8 +45,8 @@
                 {% endfor %}
             </tbody>
         </table>
-        <fieldset class="form-actions">
+        <div class="form-actions" style="border: 0">
             <input type="submit" class="btn btn-primary pull-right" value="Save Changes">
-        </fieldset>
+        </div>
     </form>
 {% endblock %}
