commit db9e8cfd84716fee30dfc93674b2431f1355b0d7
Author: Stephen Cefali <scefali@sentry.io>
Date:   Tue Feb 25 10:26:20 2020 -0800

    stops using token as query param for github enterprise (#17284)

diff --git a/src/sentry/identity/github_enterprise/provider.py b/src/sentry/identity/github_enterprise/provider.py
index 5bca1316b6..2c71bc3d4a 100644
--- a/src/sentry/identity/github_enterprise/provider.py
+++ b/src/sentry/identity/github_enterprise/provider.py
@@ -8,8 +8,10 @@ def get_user_info(url, access_token):
     session = http.build_session()
     resp = session.get(
         u"https://{}/api/v3/user".format(url),
-        params={"access_token": access_token},
-        headers={"Accept": "application/vnd.github.machine-man-preview+json"},
+        headers={
+            "Accept": "application/vnd.github.machine-man-preview+json",
+            "Authorization": "token %s" % access_token,
+        },
         verify=False,
     )
     resp.raise_for_status()
