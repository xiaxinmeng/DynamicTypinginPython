commit f3884e2b2603ffdbcccf6d1809e2d01b1fa72ea6
Author: Armin Ronacher <armin.ronacher@active-4.com>
Date:   Mon May 2 22:05:28 2016 +0200

    Add event_id to crumbs

diff --git a/src/sentry/interfaces/breadcrumbs.py b/src/sentry/interfaces/breadcrumbs.py
index e7992cc201..ca9235a9aa 100644
--- a/src/sentry/interfaces/breadcrumbs.py
+++ b/src/sentry/interfaces/breadcrumbs.py
@@ -106,6 +106,10 @@ class Breadcrumbs(Interface):
         if duration is not None:
             rv['data'] = float(duration)
 
+        event_id = crumb.get('event_id')
+        if event_id is not None:
+            rv['event_id'] = event_id
+
         if 'data' in crumb:
             rv['data'] = trim(crumb['data'], 4096)
 
@@ -127,6 +131,7 @@ class Breadcrumbs(Interface):
                 'message': x.get('message'),
                 'category': x.get('category'),
                 'data': x.get('data') or None,
+                'event_id': x.get('event_id'),
             }
         return {
             'values': map(_convert, self.values),
