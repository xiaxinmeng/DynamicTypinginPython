commit 149c7f7994961c18221d4748a99ca0bdc11dc1bb
Author: Lyn Nagara <lyn.nagara@gmail.com>
Date:   Mon Oct 29 15:26:43 2018 -0700

    fix(discover): Fix bug in combined time selector (#10317)
    
    Relative value can be undefined when absolute time is used

diff --git a/src/sentry/static/sentry/app/components/organizations/timeRangeSelector/combinedSelector.jsx b/src/sentry/static/sentry/app/components/organizations/timeRangeSelector/combinedSelector.jsx
index 59ab3d57ae..5090ee79c0 100644
--- a/src/sentry/static/sentry/app/components/organizations/timeRangeSelector/combinedSelector.jsx
+++ b/src/sentry/static/sentry/app/components/organizations/timeRangeSelector/combinedSelector.jsx
@@ -81,7 +81,7 @@ export default class CombinedSelector extends React.Component {
             onChange={val => this.handleChange('relative', val.value)}
           />
         </Box>
-        {relative === null && (
+        {!relative && (
           <React.Fragment>
             <Box mb={1}>
               <DateTimeField
