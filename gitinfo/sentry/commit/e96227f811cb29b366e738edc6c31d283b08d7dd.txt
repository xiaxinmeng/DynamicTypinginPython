commit e96227f811cb29b366e738edc6c31d283b08d7dd
Author: Jess MacQueen <jess@getsentry.com>
Date:   Mon Jul 11 18:39:25 2016 -0700

    fix avatar size and pluralization in participants component
    /cc @getsentry/ui

diff --git a/src/sentry/models/useravatar.py b/src/sentry/models/useravatar.py
index e94990b47b..36e8585722 100644
--- a/src/sentry/models/useravatar.py
+++ b/src/sentry/models/useravatar.py
@@ -24,7 +24,7 @@ class UserAvatar(Model):
         (2, 'gravatar'),
     )
 
-    ALLOWED_SIZES = (20, 48, 52, 64, 80, 96, 120)
+    ALLOWED_SIZES = (20, 32, 48, 52, 64, 80, 96, 120)
 
     user = FlexibleForeignKey('sentry.User', unique=True, related_name='avatar')
     file = FlexibleForeignKey('sentry.File', unique=True, null=True, on_delete=models.SET_NULL)
diff --git a/src/sentry/static/sentry/app/components/group/sidebar.jsx b/src/sentry/static/sentry/app/components/group/sidebar.jsx
index 27c9c86cb6..6656580eaf 100644
--- a/src/sentry/static/sentry/app/components/group/sidebar.jsx
+++ b/src/sentry/static/sentry/app/components/group/sidebar.jsx
@@ -19,7 +19,8 @@ const GroupParticipants = React.createClass({
 
     return (
       <div>
-        <h6><span>{participants.length} Participants</span></h6>
+        <h6><span>{participants.length} {'Participant' +
+                                         (participants.length === 1 ? '' : 's')}</span></h6>
         <ul className="faces">
           {participants.map((user) => {
             return (
