commit 152ebf4ee599584dfbe6dd82ed32a33c2a469ac8
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Jan 30 11:37:54 2013 -0800

    Support calling sentry-group in a compatible way with legacy applications

diff --git a/src/sentry/web/frontend/groups.py b/src/sentry/web/frontend/groups.py
index b2b09214d2..02a5c93aae 100644
--- a/src/sentry/web/frontend/groups.py
+++ b/src/sentry/web/frontend/groups.py
@@ -154,7 +154,7 @@ def _get_group_list(request, project):
 
 
 @login_required
-def redirect_to_group(request, group_id):
+def redirect_to_group(request, project_id, group_id):
     group = get_object_or_404(Group, id=group_id)
 
     return HttpResponseRedirect(reverse('sentry-group', kwargs={
diff --git a/src/sentry/web/urls.py b/src/sentry/web/urls.py
index b6c04939a0..5ad5adec0d 100644
--- a/src/sentry/web/urls.py
+++ b/src/sentry/web/urls.py
@@ -192,5 +192,5 @@ urlpatterns = patterns('',
     url(r'^(?P<team_slug>[\w_-]+)/$', groups.dashboard, name='sentry'),
 
     # Legacy
-    url(r'^[\w_-]+/group/(?P<group_id>\d+)/$', groups.redirect_to_group),
+    url(r'^(?P<project_id>[\w_-]+)/group/(?P<group_id>\d+)/$', groups.redirect_to_group, name='sentry-group'),
 )
