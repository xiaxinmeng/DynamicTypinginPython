commit 8672f68e657cdc0e9a30706ac1f1a770afdd6595
Author: Mark Story <mark@sentry.io>
Date:   Tue Dec 3 15:28:41 2019 -0500

    feat(discover) Add transaction.status to the list of fields (#15913)
    
    Expose transaction_status as a field on transaction events. This doesn't
    handle the str/int mappings but where that happens hasn't been decided
    yet.

diff --git a/src/sentry/snuba/events.py b/src/sentry/snuba/events.py
index 1ff42b29aa..dbaef1f154 100644
--- a/src/sentry/snuba/events.py
+++ b/src/sentry/snuba/events.py
@@ -206,6 +206,9 @@ class Columns(Enum):
     # Transactions specific columns
     TRANSACTION_OP = Column(None, None, "transaction_op", "transaction_op", "transaction.op")
     TRANSACTION_DURATION = Column(None, None, "duration", "duration", "transaction.duration")
+    TRANSACTION_STATUS = Column(
+        None, None, "transaction_status", "transaction_status", "transaction.status"
+    )
 
 
 def get_columns_from_aliases(aliases):
diff --git a/src/sentry/static/sentry/app/views/eventsV2/eventQueryParams.tsx b/src/sentry/static/sentry/app/views/eventsV2/eventQueryParams.tsx
index 32435cd442..73562399f7 100644
--- a/src/sentry/static/sentry/app/views/eventsV2/eventQueryParams.tsx
+++ b/src/sentry/static/sentry/app/views/eventsV2/eventQueryParams.tsx
@@ -147,6 +147,7 @@ export const FIELDS = {
 
   'transaction.duration': 'duration',
   'transaction.op': 'string',
+  'transaction.status': 'string',
   apdex: 'number',
   impact: 'number',
   // duration aliases
@@ -164,6 +165,7 @@ export const TRACING_FIELDS = [
   'sum',
   'transaction.duration',
   'transaction.op',
+  'transaction.status',
   'apdex',
   'impact',
   'p99',
