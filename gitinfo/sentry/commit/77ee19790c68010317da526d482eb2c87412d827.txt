commit 77ee19790c68010317da526d482eb2c87412d827
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Sun Mar 6 15:31:33 2016 -0800

    Comment compression-webpack-plugin usage

diff --git a/webpack.config.js b/webpack.config.js
index e1de9a1000..71949b5574 100644
--- a/webpack.config.js
+++ b/webpack.config.js
@@ -170,6 +170,9 @@ var config = {
   devtool: 'source-map'
 };
 
+// This compression-webpack-plugin generates pre-compressed files
+// ending in .gz, to be picked up and served by our internal static media
+// server as well as nginx when paired with the gzip_static module.
 if (process.env.NODE_ENV === 'production') {
   config.plugins.push(new (require('compression-webpack-plugin'))({
     // zopfli gives us a better gzip compression
