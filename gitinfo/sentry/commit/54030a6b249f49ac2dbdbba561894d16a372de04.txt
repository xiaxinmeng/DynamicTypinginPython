commit 54030a6b249f49ac2dbdbba561894d16a372de04
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Fri Jul 18 06:20:44 2014 -0700

    Let's just always format_cookies when creating the interface

diff --git a/src/sentry/interfaces/http.py b/src/sentry/interfaces/http.py
index 3d45d41d38..f13a957fae 100644
--- a/src/sentry/interfaces/http.py
+++ b/src/sentry/interfaces/http.py
@@ -137,7 +137,7 @@ class Http(Interface):
         elif not cookies:
             cookies = {}
 
-        kwargs['cookies'] = trim_dict(cookies)
+        kwargs['cookies'] = format_cookies(trim_dict(cookies))
         kwargs['env'] = trim_dict(data.get('env') or {})
         kwargs['headers'] = headers
         kwargs['data'] = body
@@ -193,7 +193,7 @@ class Http(Interface):
         method = self.method.upper()
         if self.cookies:
             try:
-                cookies = SmartCookie(format_cookies(self.cookies))
+                cookies = SmartCookie(self.cookies)
             except Exception:
                 pass
             else:
