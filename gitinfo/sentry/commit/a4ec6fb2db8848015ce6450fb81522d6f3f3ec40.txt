commit a4ec6fb2db8848015ce6450fb81522d6f3f3ec40
Author: ckj <chriskjennings@gmail.com>
Date:   Thu May 5 17:34:31 2016 -0700

    Add avatar upload blankslate

diff --git a/src/sentry/static/sentry/app/components/avatarSettings.jsx b/src/sentry/static/sentry/app/components/avatarSettings.jsx
index b00d7f30e4..e6f7999eab 100644
--- a/src/sentry/static/sentry/app/components/avatarSettings.jsx
+++ b/src/sentry/static/sentry/app/components/avatarSettings.jsx
@@ -349,13 +349,17 @@ const AvatarCropper = React.createClass({
   render() {
     return (
       <div>
-        <legend>{t('Upload a Photo')}</legend>
-          <div className="form-group">
-            <span className="btn btn-primary" onClick={this.uploadClick}>{t('Upload Photo')}</span>
-            <input ref="file" type="file" accept="image/*" onChange={this.onChange}/>
-          </div>
-          {this.getImgSrc() && this.renderImageCrop()}
-          {this.renderCanvas()}
+        {!this.getImgSrc() &&
+        <div className="image-well well blankslate">
+          <p><a onClick={this.uploadClick}><strong>Upload a photo</strong></a> to get started.</p>
+        </div>}
+
+        <div className="form-group">
+          <span className="btn btn-primary" onClick={this.uploadClick}>{t('Upload Photo')}</span>
+          <input ref="file" type="file" accept="image/*" onChange={this.onChange}/>
+        </div>
+        {this.getImgSrc() && this.renderImageCrop()}
+        {this.renderCanvas()}
       </div>
     );
   }
diff --git a/src/sentry/static/sentry/less/shared-components.less b/src/sentry/static/sentry/less/shared-components.less
index 0c0257ba44..f72136a70c 100644
--- a/src/sentry/static/sentry/less/shared-components.less
+++ b/src/sentry/static/sentry/less/shared-components.less
@@ -2926,6 +2926,12 @@ ul.radio-inputs {
   margin-bottom: 20px;
   border-radius: 3px;
 
+  p {
+    &:last-child {
+      margin-bottom: 0;
+    }
+  }
+
   &.important {
     background: @yellow;
     border: 0;
@@ -2933,6 +2939,10 @@ ul.radio-inputs {
       color: @gray-dark;
     }
   }
+
+  &.image-well {
+    padding: 80px 30px;
+  }
 }
 
 .blankslate {
