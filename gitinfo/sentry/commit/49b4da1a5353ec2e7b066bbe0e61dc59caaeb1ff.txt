commit 49b4da1a5353ec2e7b066bbe0e61dc59caaeb1ff
Author: ted kaemming <t.kaemming+github@gmail.com>
Date:   Fri Jul 22 14:13:40 2016 -0700

    Fade and strikethrough links to resolved issues in generic email display. (#3778)
    
    * Fade and strikethrough links to resolved issues in generic email display.
    * Use random status in Group generator.

diff --git a/src/sentry/templates/sentry/emails/_group.html b/src/sentry/templates/sentry/emails/_group.html
index 7cf7a633a3..4fc213520a 100644
--- a/src/sentry/templates/sentry/emails/_group.html
+++ b/src/sentry/templates/sentry/emails/_group.html
@@ -2,19 +2,23 @@
 
 {% url 'sentry-group' group.organization.slug group.project.slug group.id as group_link %}
 
-{% if group.data.type == "error" %}
+<div class="issue {% if group.is_resolved %}resolved{% endif %}">
+
+  {% if group.data.type == "error" %}
   <div class="event-type error">
-    <h3>
-      <a href="{% absolute_uri group_link %}">{{ group.data.metadata.type }}</a>
-      <small>{{ group.title|soft_break:50 }}</small>
-    </h3>
-    <p>{{ group.data.metadata.value }}</p>
-  </div>
-{% else %}
-  <div class="event-type default">
-    <h3>
-      <a href="{% absolute_uri group_link %}">{{ group.message_short|soft_break:40 }}</a>
-    </h3>
-    <p>{{ group.title|soft_break:50 }}</p>
-  </div>
-{% endif %}
+      <h3>
+        <a href="{% absolute_uri group_link %}">{{ group.data.metadata.type }}</a>
+        <small>{{ group.title|soft_break:50 }}</small>
+      </h3>
+      <p>{{ group.data.metadata.value }}</p>
+    </div>
+  {% else %}
+    <div class="event-type default">
+      <h3>
+        <a href="{% absolute_uri group_link %}">{{ group.message_short|soft_break:40 }}</a>
+      </h3>
+      <p>{{ group.title|soft_break:50 }}</p>
+    </div>
+  {% endif %}
+
+</div>
diff --git a/src/sentry/templates/sentry/emails/email-styles.html b/src/sentry/templates/sentry/emails/email-styles.html
index 98b2e4c9bd..f2ea6b4e0e 100644
--- a/src/sentry/templates/sentry/emails/email-styles.html
+++ b/src/sentry/templates/sentry/emails/email-styles.html
@@ -352,23 +352,28 @@
     display: block;
   }
 
-  /* Event type */
+  /* Issue */
 
-  .event-type h3 {
+  .issue h3 {
     line-height: 24px;
     margin: 0;
   }
 
-  .event-type h3 a {
+  .issue h3 a {
     font-size: 16px;
     font-weight: 600;
   }
 
-  .event-type h3 small {
+  .issue.resolved h3 * {
+    color: #98A4B6;
+    text-decoration: line-through;
+  }
+
+  .issue h3 small {
     font-size: 14px;
   }
 
-  .event-type p {
+  .issue p {
     margin: 0;
     font-size: 14px;
   }
diff --git a/src/sentry/web/frontend/debug/mail.py b/src/sentry/web/frontend/debug/mail.py
index 1ed1fc82f0..fec1b07150 100644
--- a/src/sentry/web/frontend/debug/mail.py
+++ b/src/sentry/web/frontend/debug/mail.py
@@ -29,6 +29,7 @@ from sentry.models import (
     Activity,
     Event,
     Group,
+    GroupStatus,
     Organization,
     OrganizationMember,
     Project,
@@ -95,6 +96,10 @@ def make_group_generator(random, project):
             message=make_message(random),
             first_seen=to_datetime(first_seen),
             last_seen=to_datetime(last_seen),
+            status=random.choice((
+                GroupStatus.UNRESOLVED,
+                GroupStatus.RESOLVED,
+            )),
         )
 
         if random.random() < 0.8:
