commit 41f710234cd02a10f3721e744ac6354a64bd1c48
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Aug 6 15:49:02 2018 -0700

    Revert BitField breakage again?

diff --git a/src/bitfield/query.py b/src/bitfield/query.py
index 939232ddbf..f3d36faf19 100644
--- a/src/bitfield/query.py
+++ b/src/bitfield/query.py
@@ -29,7 +29,10 @@ try:
         def process_lhs(self, qn, connection, lhs=None):
             lhs_sql, params = super(BitQueryLookupWrapper, self).process_lhs(
                 qn, connection, lhs)
-            lhs_sql = lhs_sql + ' | %s'
+            if self.rhs:
+                lhs_sql = lhs_sql + ' & %s'
+            else:
+                lhs_sql = lhs_sql + ' | %s'
             params.extend(self.process_rhs(qn, connection)[1])
             return lhs_sql, params
 
