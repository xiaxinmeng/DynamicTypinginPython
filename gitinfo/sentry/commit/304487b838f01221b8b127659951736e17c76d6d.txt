commit 304487b838f01221b8b127659951736e17c76d6d
Author: ckj <chriskjennings@gmail.com>
Date:   Mon Aug 1 12:51:06 2016 -0700

    Add empty state in sidebar-panel

diff --git a/src/sentry/static/sentry/app/components/header/broadcasts.jsx b/src/sentry/static/sentry/app/components/header/broadcasts.jsx
index c7c0418dbd..8f19a17ac9 100644
--- a/src/sentry/static/sentry/app/components/header/broadcasts.jsx
+++ b/src/sentry/static/sentry/app/components/header/broadcasts.jsx
@@ -116,7 +116,7 @@ const Broadcasts = React.createClass({
               {loading ?
                 <LoadingIndicator />
               : (broadcasts.length === 0 ?
-                <div className="empty">{t('No recent updates from the Sentry team.')}</div>
+                <div className="sidebar-panel-empty">{t('No recent updates from the Sentry team.')}</div>
               :
                 broadcasts.map((item) => {
                   return (
diff --git a/src/sentry/static/sentry/less/layout.less b/src/sentry/static/sentry/less/layout.less
index 26b00eab59..c2d0fc07d7 100644
--- a/src/sentry/static/sentry/less/layout.less
+++ b/src/sentry/static/sentry/less/layout.less
@@ -325,6 +325,8 @@ body.auth {
   }
 }
 
+@sidebar-panel-header-height: 62px;
+
 .sidebar-panel {
   position: fixed;
   width: @sidebar-panel-width;
@@ -345,6 +347,7 @@ body.auth {
     padding: 23px 20px 20px;
     background: #fff;
     box-shadow: 0 1px 2px rgba(0,0,0, .06);
+    height: 62px;
 
     .close {
       font-size: 11px;
@@ -364,6 +367,10 @@ body.auth {
     }
   }
 
+  .sidebar-panel-body {
+
+  }
+
   .sidebar-panel-item {
     padding: 15px 20px;
     line-height: 1.2;
@@ -404,6 +411,15 @@ body.auth {
       margin-top: -1px;
     }
   }
+
+  .sidebar-panel-empty {
+    color: @50;
+    text-align: center;
+    position: absolute;
+    top: 50%;
+    padding: 0 50px;
+    margin-top: (-62px + -16px) / 2; // offset for sidebar-panel-header + font-size
+  }
 }
 
 .range-picker {
