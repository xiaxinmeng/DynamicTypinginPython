commit 42f6f0ea2171e26131ed899372cb848074c021d6
Author: David Cramer <dcramer@gmail.com>
Date:   Sat Jan 19 16:07:12 2013 -0800

    Correct ident check in upgrade

diff --git a/src/sentry/migrations/0081_fill_trackeduser.py b/src/sentry/migrations/0081_fill_trackeduser.py
index a9403fe3b2..14328b9d04 100644
--- a/src/sentry/migrations/0081_fill_trackeduser.py
+++ b/src/sentry/migrations/0081_fill_trackeduser.py
@@ -12,8 +12,8 @@ class Migration(DataMigration):
         "Write your forwards methods here."
         # Note: Remember to use orm['appname.ModelName'] rather than "from appname.models..."
         for affecteduser in orm['sentry.AffectedUserByGroup'].objects.filter(ident__isnull=False):
-            if ident.startswith('email:'):
-                email = ident.split('email:', 1)[-1]
+            if affecteduser.ident.startswith('email:'):
+                email = affecteduser.ident.split('email:', 1)[-1]
             else:
                 email = None
             tuser, created = orm['sentry.TrackedUser'].objects.get_or_create(
