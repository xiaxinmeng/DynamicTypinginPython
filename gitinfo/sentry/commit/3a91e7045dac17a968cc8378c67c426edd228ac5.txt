commit 3a91e7045dac17a968cc8378c67c426edd228ac5
Author: Billy Vong <billyvg@users.noreply.github.com>
Date:   Wed Jan 2 14:40:04 2019 -0800

    feat(charts): Change LineChart to allow single series properties (#11321)
    
    This allows line series objects to specify properties per series.

diff --git a/src/sentry/static/sentry/app/components/charts/lineChart.jsx b/src/sentry/static/sentry/app/components/charts/lineChart.jsx
index 4e378233a2..ad4de0c6e0 100644
--- a/src/sentry/static/sentry/app/components/charts/lineChart.jsx
+++ b/src/sentry/static/sentry/app/components/charts/lineChart.jsx
@@ -16,11 +16,12 @@ export default class LineChart extends React.Component {
     return (
       <BaseChart
         {...props}
-        series={series.map(s => {
+        series={series.map(({seriesName, data, ...options}) => {
           return LineSeries({
             ...seriesOptions,
-            name: s.seriesName,
-            data: s.data.map(({value, name}) => [name, value]),
+            ...options,
+            name: seriesName,
+            data: data.map(({value, name}) => [name, value]),
           });
         })}
       />
