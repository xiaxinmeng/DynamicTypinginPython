commit 1b094b965947ea2792f1953df3e1c0dd8c1c7a3c
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Oct 19 18:02:35 2011 -0700

    Ensure we only float numbers that we need to

diff --git a/sentry/templatetags/sentry_helpers.py b/sentry/templatetags/sentry_helpers.py
index b6067b343f..f03de9082a 100644
--- a/sentry/templatetags/sentry_helpers.py
+++ b/sentry/templatetags/sentry_helpers.py
@@ -57,9 +57,9 @@ def small_count(v):
     v = int(v)
     for x, y in z:
         o, p = divmod(v, x)
-        if o > 1:
-            if len(str(o)) > 2:
-                return '%d%s' % (v, y)
+        if o:
+            if len(str(o)) > 2 or not p:
+                return '%d%s' % (o, y)
             return '%.1f%s' % (v / float(x), y)
     return v
 
