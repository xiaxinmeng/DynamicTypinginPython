commit ca64ad467db84cd2a4d68f2fc75fbfd0ceff0e0e
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Sep 29 20:24:56 2015 -0700

    Sort platforms

diff --git a/src/sentry/tasks/sync_docs.py b/src/sentry/tasks/sync_docs.py
index 7dba8d99c2..9839ba8645 100644
--- a/src/sentry/tasks/sync_docs.py
+++ b/src/sentry/tasks/sync_docs.py
@@ -29,10 +29,15 @@ def sync_docs():
                     'name': i_data['name'],
                     'type': i_data['type'],
                     'link': i_data['doc_link'],
-                } for i_id, i_data in integrations.iteritems()
+                } for i_id, i_data in sorted(
+                    integrations.iteritems(),
+                    key=lambda x: x[1]['name']
+                )
             ],
         })
 
+    platform_list.sort(key=lambda x: x['name'])
+
     options.set('sentry:docs', {'platforms': platform_list})
 
     for platform_id, platform_data in data['platforms'].iteritems():
