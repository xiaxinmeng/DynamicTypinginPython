commit e2e49925101f1c542c02de36d4b563bf5556bc1e
Author: David Cramer <dcramer@gmail.com>
Date:   Sat Feb 23 03:30:27 2013 -0800

    Ensure we coerce resolve age to an int

diff --git a/src/sentry/models.py b/src/sentry/models.py
index 8411ec7932..710cff2784 100644
--- a/src/sentry/models.py
+++ b/src/sentry/models.py
@@ -557,7 +557,7 @@ class Group(EventBase):
         resolve_age = self.project.get_option('sentry:resolve_age', None)
         if not resolve_age:
             return False
-        return self.active_at < timezone.now() - timedelta(hours=resolve_age)
+        return self.active_at < timezone.now() - timedelta(hours=int(resolve_age))
 
     def get_status(self):
         if self.status == STATUS_UNRESOLVED and self.is_over_resolve_age():
