commit d1393c597e4768bad73f6e01c33381a8b720190d
Author: Ted Kaemming <ted@kaemming.com>
Date:   Thu Feb 25 11:48:20 2016 -0800

    Don't request trailing hour statistics when fetching organization stats.
    
    This prevents reading into data that has been TTLed out.
    
    This is kind of a hack fix and should probably be handled more
    appropriately elsewhere. (I'm not sure where that is yet, though.)

diff --git a/src/sentry/static/sentry/app/views/organizationStats/index.jsx b/src/sentry/static/sentry/app/views/organizationStats/index.jsx
index cab5a5f99e..fb7a675fef 100644
--- a/src/sentry/static/sentry/app/views/organizationStats/index.jsx
+++ b/src/sentry/static/sentry/app/views/organizationStats/index.jsx
@@ -18,7 +18,7 @@ const OrganizationStats = React.createClass({
 
   getInitialState() {
     let until = Math.floor(new Date().getTime() / 1000);
-    let since = until - 3600 * 24 * 7;
+    let since = until - 3600 * ((24 * 7) - 1);
 
     return {
       projectsError: false,
