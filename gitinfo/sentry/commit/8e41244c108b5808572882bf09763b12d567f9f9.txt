commit 8e41244c108b5808572882bf09763b12d567f9f9
Author: Mark Story <mark@sentry.io>
Date:   Wed Jun 19 21:48:31 2019 -0400

    fix(tests) Stabilize event id in snapshots (#13744)
    
    Fixate the eventid so that it doesn't change on each percy run.

diff --git a/tests/acceptance/test_organization_events_v2.py b/tests/acceptance/test_organization_events_v2.py
index b790c95d80..80bbf8d655 100644
--- a/tests/acceptance/test_organization_events_v2.py
+++ b/tests/acceptance/test_organization_events_v2.py
@@ -109,9 +109,12 @@ class OrganizationEventsTest(AcceptanceTestCase, SnubaTestCase):
         min_ago = (timezone.now() - timedelta(minutes=1)).isoformat()[:19]
 
         event_data = load_data('python')
-        event_data['timestamp'] = min_ago
-        event_data['received'] = min_ago
-        event_data['fingerprint'] = ['group-1']
+        event_data.update({
+            'event_id': 'a' * 32,
+            'timestamp': min_ago,
+            'received': min_ago,
+            'fingerprint': ['group-1']
+        })
         self.store_event(
             data=event_data,
             project_id=self.project.id,
