commit 9a8191a49fa6ecc74f44cff82b5a2336a087d3d7
Author: David Cramer <dcramer@gmail.com>
Date:   Fri Oct 9 00:04:08 2015 -0700

    lessen/prioritize tag count

diff --git a/src/sentry/models/tagkey.py b/src/sentry/models/tagkey.py
index acd5b85bdd..7d408abc77 100644
--- a/src/sentry/models/tagkey.py
+++ b/src/sentry/models/tagkey.py
@@ -37,7 +37,9 @@ class TagKeyManager(BaseManager):
             result = list(self.filter(
                 project=project,
                 status=TagKeyStatus.VISIBLE,
-            ).values_list('key', flat=True)[:100])
+            ).order_by(
+                '-values_seen'
+            ).values_list('key', flat=True)[:20])
             cache.set(key, result, 60)
         return result
 
