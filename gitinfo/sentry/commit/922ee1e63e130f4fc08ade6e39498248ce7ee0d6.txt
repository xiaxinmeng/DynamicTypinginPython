commit 922ee1e63e130f4fc08ade6e39498248ce7ee0d6
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Mar 19 15:43:57 2015 -0700

    Prioritize User ip address

diff --git a/src/sentry/models/event.py b/src/sentry/models/event.py
index 7fc3f0d302..b683d5bbb8 100644
--- a/src/sentry/models/event.py
+++ b/src/sentry/models/event.py
@@ -87,18 +87,18 @@ class Event(Model):
 
     @memoize
     def ip_address(self):
-        http_data = self.data.get('sentry.interfaces.Http')
-        if http_data and 'env' in http_data:
-            value = http_data['env'].get('REMOTE_ADDR')
-            if value:
-                return value
-
         user_data = self.data.get('sentry.interfaces.User')
         if user_data:
             value = user_data.get('ip_address')
             if value:
                 return value
 
+        http_data = self.data.get('sentry.interfaces.Http')
+        if http_data and 'env' in http_data:
+            value = http_data['env'].get('REMOTE_ADDR')
+            if value:
+                return value
+
         return None
 
     @memoize
