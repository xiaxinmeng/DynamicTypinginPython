commit 19e74c34795a9130afdfb893da74a606d74ed2fb
Author: David Cramer <dcramer@gmail.com>
Date:   Thu May 3 17:38:19 2012 -0700

    initial documentation for raven-ruby and raven-js

diff --git a/bootstrap/sentry.less b/bootstrap/sentry.less
index 57500cd4c8..237cb94a23 100644
--- a/bootstrap/sentry.less
+++ b/bootstrap/sentry.less
@@ -651,4 +651,22 @@ legend {
 }
 .page-header h2 {
   font-weight: normal;
+}
+
+.modal {
+  width: 760px;
+  margin-left: -480px;
+}
+
+.image {
+  border: 1px solid #ddd;
+  background: #ccc;
+  padding: 10px;
+  margin-bottom: 10px;
+  display: inline-block;
+  .border-radius(3px);
+}
+.image img {
+  .border-radius(3px);
+  display: block;
 }
\ No newline at end of file
diff --git a/sentry/static/images/client_security.png b/sentry/static/images/client_security.png
new file mode 100644
index 0000000000..b4c92bb553
Binary files /dev/null and b/sentry/static/images/client_security.png differ
diff --git a/sentry/static/styles/global.css b/sentry/static/styles/global.css
index 1a2cb9deb7..9136810533 100644
--- a/sentry/static/styles/global.css
+++ b/sentry/static/styles/global.css
@@ -4099,3 +4099,23 @@ legend {
 .page-header h2 {
   font-weight: normal;
 }
+.modal {
+  width: 760px;
+  margin-left: -480px;
+}
+.image {
+  border: 1px solid #ddd;
+  background: #ccc;
+  padding: 10px;
+  margin-bottom: 10px;
+  display: inline-block;
+  -webkit-border-radius: 3px;
+  -moz-border-radius: 3px;
+  border-radius: 3px;
+}
+.image img {
+  -webkit-border-radius: 3px;
+  -moz-border-radius: 3px;
+  border-radius: 3px;
+  display: block;
+}
diff --git a/sentry/static/styles/global.min.css b/sentry/static/styles/global.min.css
index 8c55e36470..65173252c5 100644
--- a/sentry/static/styles/global.min.css
+++ b/sentry/static/styles/global.min.css
@@ -722,3 +722,6 @@ legend{margin-bottom:18px;}
 .page-header{padding-bottom:0;border-bottom:0;}
 .page-header .pull-right{margin-top:4px;}
 .page-header h2{font-weight:normal;}
+.modal{width:760px;margin-left:-480px;}
+.image{border:1px solid #ddd;background:#ccc;padding:10px;margin-bottom:10px;display:inline-block;-webkit-border-radius:3px;-moz-border-radius:3px;border-radius:3px;}
+.image img{-webkit-border-radius:3px;-moz-border-radius:3px;border-radius:3px;display:block;}
diff --git a/sentry/templates/sentry/partial/_client_config.html b/sentry/templates/sentry/partial/_client_config.html
index a832f3c3ae..f1b0b2533f 100644
--- a/sentry/templates/sentry/partial/_client_config.html
+++ b/sentry/templates/sentry/partial/_client_config.html
@@ -8,6 +8,13 @@
     </li>
     <li><a href="{% url sentry-docs-client project.slug 'php' %}" data-toggle="modal">PHP</a></li>
     <li><a href="{% url sentry-docs-client project.slug 'java' %}" data-toggle="modal">Java</a></li>
+    <li>
+        <a href="{% url sentry-docs-client project.slug 'ruby' %}" data-toggle="modal">Ruby</a>
+        <ul>
+            <li><a href="{% url sentry-docs-client project.slug 'rails3' %}" data-toggle="modal">Rails 3</a></li>
+        </ul>
+    </li>
+    <li><a href="{% url sentry-docs-client project.slug 'javascript' %}" data-toggle="modal">JavaScript</a></li>
     <li>
         <a href="{% url sentry-docs-client project.slug 'node.js' %}" data-toggle="modal">Node.js</a>
         <ul>
diff --git a/sentry/templates/sentry/partial/client_config/javascript.html b/sentry/templates/sentry/partial/client_config/javascript.html
new file mode 100644
index 0000000000..06342d87de
--- /dev/null
+++ b/sentry/templates/sentry/partial/client_config/javascript.html
@@ -0,0 +1,36 @@
+{% load i18n %}
+
+{% url manage-project project.pk as project_link %}
+
+<p>{% blocktrans with 'https://github.com/lincolnloop/raven-js' as link and 'http://jquery.com/' as jquery and 'http://zeptojs.com/' as zepto %}Start by installing <a href="{{ link }}">raven-js</a> and either <a href="{{ jquery }}">jQuery</a> or <a href="{{ zepto }}">Zepto</a>.{% endblocktrans %}</p>
+
+<p>{% blocktrans %}Add the any URLs which will be logging errors in your project's settings under <a href="{{ project_link }}#client-security">Client Security</a>:{% endblocktrans %}</p>
+
+<div class="image"><img src="{% url sentry-media "images/client_security.png" %}"></div>
+
+<p>{% trans "Configure the client:" %}</p>
+
+<pre>&lt;script type="text/javascript"&gt;
+Raven.config('{% if dsn %}{{ dsn }}{% else %}<strong>SENTRY_DSN</strong>{% endif %}');
+&lt;/script&gt;</pre>
+
+<p>{% trans "Now call out to the raven client to capture events:" %}</p>
+
+<pre>
+// {% trans "record a simple message" %}
+Raven.captureMessage('hello world!')
+
+// {% trans "capture an exception" %}
+try {
+    errorThrowingCode();
+} catch(err) {
+    Raven.captureException(err);
+}</pre>
+
+<p>{% trans "You can also optionally install a default error handler to catch all exceptions:" %}</p>
+
+<pre>window.onerror = Raven.process;</pre>
+
+<p>{% trans "Note: This will only work on browsers that properly support <code>window.onerror</code>." %}</p>
+
+<p>{% blocktrans with 'https://github.com/lincolnloop/raven-js' as link %}For more information on other uses of Raven with Ruby, please see the <a href="{{ link }}">official documentation for raven-js</a>.{% endblocktrans %}</p>
diff --git a/sentry/templates/sentry/partial/client_config/rails3.html b/sentry/templates/sentry/partial/client_config/rails3.html
new file mode 100644
index 0000000000..b61d7b6384
--- /dev/null
+++ b/sentry/templates/sentry/partial/client_config/rails3.html
@@ -0,0 +1,21 @@
+{% load i18n %}
+
+<p>{% blocktrans with 'https://github.com/coderanger/raven-ruby' as link %}Start by installing <a href="{{ link }}">raven-ruby</a>:{% endblocktrans %}</p>
+
+<pre>gem install sentry-raven</pre>
+
+<p>{% trans "Or add it to your <code>Gemfile</code>:" %}</p>
+
+<pre>gem "sentry-raven", :git => "https://github.com/coderanger/raven-ruby.git"</pre>
+
+<p>{% trans "Add a <code>config/initializers/raven.rb</code> containing:" %}</p>
+
+<pre>require 'raven'
+
+Raven.configure do |config|
+  config.dsn = '{% if dsn %}{{ dsn }}{% else %}<strong>SENTRY_DSN</strong>{% endif %}'
+end</pre>
+
+<p>{% trans "That's it! Raven automatically installs an error handling hook to pipe all uncaught exceptions to Sentry." %}</p>
+
+<p>{% blocktrans with 'https://github.com/coderanger/raven-ruby' as link %}For more information on other uses of Raven with Ruby, please see the <a href="{{ link }}">official documentation for raven-ruby</a>.{% endblocktrans %}</p>
diff --git a/sentry/templates/sentry/partial/client_config/ruby.html b/sentry/templates/sentry/partial/client_config/ruby.html
new file mode 100644
index 0000000000..f8c6268730
--- /dev/null
+++ b/sentry/templates/sentry/partial/client_config/ruby.html
@@ -0,0 +1,25 @@
+{% load i18n %}
+
+<p>{% blocktrans with 'https://github.com/coderanger/raven-ruby' as link %}Start by installing <a href="{{ link }}">raven-ruby</a>:{% endblocktrans %}</p>
+
+<pre>gem install sentry-raven</pre>
+
+<p>{% trans "Or add it to your <code>Gemfile</code>:" %}</p>
+
+<pre>gem "sentry-raven", :git => "https://github.com/coderanger/raven-ruby.git"</pre>
+
+<p>{% trans "Create an instance of the client:" %}</p>
+
+<pre>require 'raven'
+
+Raven.configure do |config|
+  config.dsn = '{% if dsn %}{{ dsn }}{% else %}<strong>SENTRY_DSN</strong>{% endif %}'
+end</pre>
+
+<p>{% trans "Now wrap your application to capture events:" %}</p>
+
+<pre>Raven.capture do
+    MyApp.run
+end</pre>
+
+<p>{% blocktrans with 'https://github.com/coderanger/raven-ruby' as link %}For more information on other uses of Raven with Ruby, please see the <a href="{{ link }}">official documentation for raven-ruby</a>.{% endblocktrans %}</p>
diff --git a/sentry/templates/sentry/projects/manage.html b/sentry/templates/sentry/projects/manage.html
index 0e29b98873..e41019f4fc 100644
--- a/sentry/templates/sentry/projects/manage.html
+++ b/sentry/templates/sentry/projects/manage.html
@@ -49,7 +49,7 @@
                         {% endif %}
                     {% endfor %}
                 </fieldset>
-                <fieldset>
+                <fieldset id="client-security">
                     <div><legend>Client Security</legend></div>
                     {% with form.origins as field %}
                         <p>{% blocktrans with 'https://github.com/lincolnloop/raven-js' as link %}Configure origin URLs which Sentry should accept events from. This is used for communication with client's like <a href="{{ link }}">raven-js</a>.{% endblocktrans %}
@@ -76,7 +76,7 @@
                     {% endif %}
                 </div>
             </form>
-            <div class="page-header">
+            <div class="page-header" id="members">
                 {% if can_add_member %}
                     <a href="{% url sentry-new-team-member project.slug %}" class="btn pull-right btn-primary">{% trans "New Member" %}</a>
                 {% endif %}
diff --git a/sentry/web/frontend/docs.py b/sentry/web/frontend/docs.py
index 7a095e4abc..a2072ca34a 100644
--- a/sentry/web/frontend/docs.py
+++ b/sentry/web/frontend/docs.py
@@ -22,6 +22,9 @@ PLATFORM_LIST = (
     'express',
     'connect',
     'r',
+    'ruby',
+    'rails3',
+    'javascript',
 )
 
 
