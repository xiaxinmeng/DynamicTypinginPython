commit a90e663ee495fb227880dc09e1da3aac774fabbe
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Jun 3 16:17:37 2010 -0700

    Catch logging failures and throw a warning

diff --git a/djangodblog/manager.py b/djangodblog/manager.py
index 339a097691..9fbfe71284 100644
--- a/djangodblog/manager.py
+++ b/djangodblog/manager.py
@@ -74,7 +74,10 @@ class DBLogManager(models.Manager):
                     last_seen=datetime.datetime.now(),
                 )
         except Exception, exc:
-            logger.exception(exc)
+            try:
+                logger.exception(exc)
+            except Exception, exc:
+                warnings.warn(u'Unable to process log entry: %s' % (exc,))
         else:
             return instance
     
