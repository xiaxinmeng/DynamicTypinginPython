commit ec9605016904d4c68ef6253b0a3861fd97e3cad2
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Sep 23 14:56:03 2015 -0700

    Correct populating existing values on rule editor

diff --git a/src/sentry/rules/base.py b/src/sentry/rules/base.py
index 88d4486434..93a0552337 100644
--- a/src/sentry/rules/base.py
+++ b/src/sentry/rules/base.py
@@ -73,6 +73,7 @@ class RuleBase(object):
         return self.data.get(key)
 
     def get_form_instance(self):
+        print(self.data)
         if self.had_data:
             data = self.data
         else:
diff --git a/src/sentry/static/sentry/app/views/ruleEditor/ruleNode.jsx b/src/sentry/static/sentry/app/views/ruleEditor/ruleNode.jsx
index 9a175049d7..85740320f1 100644
--- a/src/sentry/static/sentry/app/views/ruleEditor/ruleNode.jsx
+++ b/src/sentry/static/sentry/app/views/ruleEditor/ruleNode.jsx
@@ -3,11 +3,16 @@ import $ from "jquery";
 
 var RuleNode = React.createClass({
   componentDidMount() {
-    $(this.refs.html.getDOMNode()).find('select').selectize();
+    let $html = $(this.refs.html.getDOMNode());
+    $html.find('select').selectize();
+    $html.find('select, input, textarea').each((_, el) => {
+      let $el = $(el);
+      $el.val(this.props[el.name]);
+    });
   },
 
   render() {
-    var {id, node} = this.props;
+    let {id, node} = this.props;
     return (
       <tr>
         <td className="rule-form">
diff --git a/src/sentry/static/sentry/app/views/ruleEditor/ruleNodeList.jsx b/src/sentry/static/sentry/app/views/ruleEditor/ruleNodeList.jsx
index 69fc94e571..624381a694 100644
--- a/src/sentry/static/sentry/app/views/ruleEditor/ruleNodeList.jsx
+++ b/src/sentry/static/sentry/app/views/ruleEditor/ruleNodeList.jsx
@@ -51,9 +51,9 @@ var RuleNodeList = React.createClass({
             {this.state.items.map((item, idx) => {
               return (
                 <RuleNode key={idx}
-                  id={item.id}
                   node={this.getNode(item.id)}
-                  onDelete={this.onDeleteRow.bind(this, idx)} />
+                  onDelete={this.onDeleteRow.bind(this, idx)}
+                  {...item} />
               );
             })}
           </tbody>
