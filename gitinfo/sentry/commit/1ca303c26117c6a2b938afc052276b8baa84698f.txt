commit 1ca303c26117c6a2b938afc052276b8baa84698f
Author: David Cramer <dcramer@gmail.com>
Date:   Sun May 31 14:35:21 2015 +0200

    Improve configStore api

diff --git a/src/sentry/static/sentry/app/stores/configStore.jsx b/src/sentry/static/sentry/app/stores/configStore.jsx
index a8f0c47745..a8c9e302c6 100644
--- a/src/sentry/static/sentry/app/stores/configStore.jsx
+++ b/src/sentry/static/sentry/app/stores/configStore.jsx
@@ -11,14 +11,21 @@ var ConfigStore = Reflux.createStore({
     return this.config[key];
   },
 
+  set(key, value) {
+    this.config[key] = value;
+    var out = {};
+    out[key] = value;
+    this.trigger(out);
+  },
+
   getConfig() {
     return this.config;
   },
 
   loadInitialData(config) {
     config.features = new Set(config.features || []);
-
     this.config = config;
+    this.trigger(config);
   }
 });
 
