commit 6156b305a8ae7bcb60e33b20e748ab478efee5f9
Author: Billy Vong <billyvg@users.noreply.github.com>
Date:   Mon Nov 5 12:44:57 2018 -0800

    feat(ui): Switch order of Discover and Events sidebar icons (#10419)
    
    Projects -> Events -> Discover

diff --git a/src/sentry/static/sentry/app/components/sidebar/index.jsx b/src/sentry/static/sentry/app/components/sidebar/index.jsx
index 23b7ec0365..b4747dbf94 100644
--- a/src/sentry/static/sentry/app/components/sidebar/index.jsx
+++ b/src/sentry/static/sentry/app/components/sidebar/index.jsx
@@ -200,23 +200,23 @@ class Sidebar extends React.Component {
                   to={`/${organization.slug}/`}
                 />
 
-                <Feature features={['discover']}>
+                <Feature features={['events-stream']}>
                   <SidebarItem
                     {...sidebarItemProps}
                     onClick={this.hidePanel}
-                    icon={<InlineSvg src="icon-discover" />}
-                    label={t('Discover')}
-                    to={`/organizations/${organization.slug}/discover/`}
+                    icon={<InlineSvg src="icon-stack" />}
+                    label={t('Events')}
+                    to={`/organizations/${organization.slug}/events/`}
                   />
                 </Feature>
 
-                <Feature features={['events-stream']}>
+                <Feature features={['discover']}>
                   <SidebarItem
                     {...sidebarItemProps}
                     onClick={this.hidePanel}
-                    icon={<InlineSvg src="icon-stack" />}
-                    label={t('Events')}
-                    to={`/organizations/${organization.slug}/events/`}
+                    icon={<InlineSvg src="icon-discover" />}
+                    label={t('Discover')}
+                    to={`/organizations/${organization.slug}/discover/`}
                   />
                 </Feature>
               </SidebarSection>
