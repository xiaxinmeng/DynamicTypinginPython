commit 582174431bc013ea3af3cb98578efefcf834dc0a
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Aug 24 11:47:26 2015 -0700

    Expand group data in mocks

diff --git a/bin/load-mocks b/bin/load-mocks
index e62fb6e681..df3f6811f8 100755
--- a/bin/load-mocks
+++ b/bin/load-mocks
@@ -3,9 +3,12 @@
 from sentry.utils.runner import configure
 configure()
 
+import itertools
+
 from datetime import datetime, timedelta
 from django.conf import settings
 from django.db import IntegrityError, transaction
+from hashlib import md5
 from pytz import utc
 from random import randint
 
@@ -16,6 +19,14 @@ from sentry.models import (
 )
 from sentry.utils.samples import create_sample_event
 
+PLATFORMS = itertools.cycle([
+    'ruby',
+    'php',
+    'python',
+    'java',
+    'javascript',
+])
+
 
 def create_system_time_series():
     now = datetime.utcnow().replace(tzinfo=utc)
@@ -189,6 +200,18 @@ def main(num_events=1):
                     message='This is a an example PHP event with an extremely long and annoying title\nIt also happens to contain some newlines in it,\nthus making it even more annoying.',
                 )
 
+
+            # Add a bunch of additional dummy events to support pagination
+            for _ in range(45):
+                platform = PLATFORMS.next()
+                create_sample_event(
+                    project=project,
+                    platform=platform,
+                    release=release.version,
+                    message='This is a mostly useless example %s exception' % platform,
+                    checksum=md5(platform + str(_)).hexdigest(),
+                )
+
             Activity.objects.create(
                 type=Activity.RELEASE,
                 project=project,
