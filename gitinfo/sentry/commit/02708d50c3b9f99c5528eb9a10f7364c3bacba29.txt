commit 02708d50c3b9f99c5528eb9a10f7364c3bacba29
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Jul 13 11:30:09 2015 -0700

    Fill UserReport.group_id on save

diff --git a/src/sentry/event_manager.py b/src/sentry/event_manager.py
index 6edbbfc20d..25cc8c4e1c 100644
--- a/src/sentry/event_manager.py
+++ b/src/sentry/event_manager.py
@@ -27,7 +27,7 @@ from sentry.constants import (
 from sentry.interfaces.base import get_interface
 from sentry.models import (
     Activity, Event, EventMapping, Group, GroupHash, GroupStatus, Project,
-    Release
+    Release, UserReport
 )
 from sentry.plugins import plugins
 from sentry.signals import regression_signal
@@ -369,6 +369,10 @@ class EventManager(object):
             self.logger.info('Duplicate EventMapping found for event_id=%s', event_id)
             return event
 
+        UserReport.objects.filter(
+            project=project, event_id=event_id,
+        ).update(group=group)
+
         # save the event unless its been sampled
         if not is_sample:
             try:
