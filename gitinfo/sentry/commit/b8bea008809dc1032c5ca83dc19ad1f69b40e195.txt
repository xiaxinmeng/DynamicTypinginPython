commit b8bea008809dc1032c5ca83dc19ad1f69b40e195
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Oct 5 14:01:32 2010 -0700

    Change GET/POST to be lowercase

diff --git a/sentry/client/base.py b/sentry/client/base.py
index 0227910ff8..f56c4c17ef 100644
--- a/sentry/client/base.py
+++ b/sentry/client/base.py
@@ -50,7 +50,7 @@ class SentryClient(object):
                     'key': conf.KEY,
                 }
                 try:
-                    urlread(url, GET=data, timeout=conf.REMOTE_TIMEOUT)
+                    urlread(url, get=data, timeout=conf.REMOTE_TIMEOUT)
                 except urllib2.URLError, e:
                     logger.error('Unable to reach Sentry log server: %s' % (e,), exc_info=sys.exc_info(), extra={'remote_url': url})
                     logger.log(kwargs.pop('level', None) or logging.ERROR, kwargs.pop('message', None))
diff --git a/sentry/helpers.py b/sentry/helpers.py
index 912b7cdf7f..a95223c40a 100644
--- a/sentry/helpers.py
+++ b/sentry/helpers.py
@@ -140,7 +140,7 @@ class cached_property(object):
             obj.__dict__[self.__name__] = value
         return value
 
-def urlread(url, GET={}, POST={}, timeout=None):
-    req = urllib2.Request(url, urllib.urlencode(GET))
-    response = urllib2.urlopen(req, urllib.urlencode(POST), timeout).read()
+def urlread(url, get={}, post={}, headers={}, timeout=None):
+    req = urllib2.Request(url, urllib.urlencode(get), headers=headers)
+    response = urllib2.urlopen(req, urllib.urlencode(post), timeout).read()
     return response
\ No newline at end of file
