commit cf4744dd6eb18d12a33d8bacf279e49442b15c74
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Jul 2 14:25:05 2015 -0700

    Handle decoding of response body

diff --git a/src/sentry/api/client.py b/src/sentry/api/client.py
index ea09b0afab..095a3825ce 100644
--- a/src/sentry/api/client.py
+++ b/src/sentry/api/client.py
@@ -41,7 +41,13 @@ class ApiClient(object):
 
         if 200 <= response.status_code < 400:
             return response
-        raise self.ApiError(response.status_code, getattr(response, 'data', ''))
+
+        if response.content_type == 'application/json' and response.body:
+            data = json.loads(response.body)
+        else:
+            data = response.body
+
+        raise self.ApiError(response.status_code, data)
 
     def get(self, *args, **kwargs):
         return self.request('GET', *args, **kwargs)
