commit f3f52bd4ace445f6338afae0418a550149eeeeb1
Author: Alberto Leal <mail4alberto@gmail.com>
Date:   Wed Apr 15 12:25:13 2020 -0400

    fix(discover2): Don't link issues that are unknown (#18262)

diff --git a/src/sentry/static/sentry/app/utils/discover/fieldRenderers.tsx b/src/sentry/static/sentry/app/utils/discover/fieldRenderers.tsx
index 9cc5a4ebc4..a314d791e9 100644
--- a/src/sentry/static/sentry/app/utils/discover/fieldRenderers.tsx
+++ b/src/sentry/static/sentry/app/utils/discover/fieldRenderers.tsx
@@ -175,10 +175,20 @@ const SPECIAL_FIELDS: SpecialFields = {
   issue: {
     sortField: null,
     renderFunc: (data, {organization}) => {
-      const target = `/organizations/${organization.slug}/issues/${data['issue.id']}/`;
+      const issueID = data['issue.id'];
+
+      if (!issueID) {
+        return (
+          <Container>
+            <ShortId shortId={`${data.issue}`} />
+          </Container>
+        );
+      }
+
+      const target = `/organizations/${organization.slug}/issues/${issueID}/`;
       return (
         <Container>
-          <OverflowLink to={target} aria-label={data['issue.id']}>
+          <OverflowLink to={target} aria-label={issueID}>
             <ShortId shortId={`${data.issue}`} />
           </OverflowLink>
         </Container>
