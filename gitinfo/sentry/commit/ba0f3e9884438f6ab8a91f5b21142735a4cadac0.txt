commit ba0f3e9884438f6ab8a91f5b21142735a4cadac0
Author: David Cramer <dcramer@gmail.com>
Date:   Fri Nov 13 17:14:23 2015 -0800

    Actually fix input cursor being empty

diff --git a/src/sentry/api/base.py b/src/sentry/api/base.py
index abc9f4d066..5751b8d4fe 100644
--- a/src/sentry/api/base.py
+++ b/src/sentry/api/base.py
@@ -172,6 +172,8 @@ class Endpoint(APIView):
         input_cursor = request.GET.get('cursor')
         if input_cursor:
             input_cursor = Cursor.from_string(input_cursor)
+        else:
+            input_cursor = None
 
         assert per_page <= max(100, default_per_page)
 
diff --git a/src/sentry/api/paginator.py b/src/sentry/api/paginator.py
index 5efa69a1ac..524fb9d273 100644
--- a/src/sentry/api/paginator.py
+++ b/src/sentry/api/paginator.py
@@ -84,7 +84,7 @@ class BasePaginator(object):
     def get_result(self, limit=100, cursor=None):
         # cursors are:
         #   (identifier(integer), row offset, is_prev)
-        if not cursor:
+        if cursor is None:
             cursor = Cursor(0, 0, 0)
 
         if cursor.value:
