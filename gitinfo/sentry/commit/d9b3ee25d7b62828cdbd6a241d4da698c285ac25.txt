commit d9b3ee25d7b62828cdbd6a241d4da698c285ac25
Author: Markus Unterwaditzer <markus@unterwaditzer.net>
Date:   Wed Nov 13 22:52:34 2019 +0100

    ref: Update semaphore to 0.4.61 (#15574)
    
    * ref: Update semaphore
    
    * fix: Fix sign error
    
    * ref: Bump semaphore again
    
    * fix: Revert reorder of requirements.txt
    
    * fix: Fix snuba test

diff --git a/requirements-base.txt b/requirements-base.txt
index 23862d05aa..d986a31bef 100644
--- a/requirements-base.txt
+++ b/requirements-base.txt
@@ -1,6 +1,9 @@
 BeautifulSoup>=3.2.1
 boto3>=1.4.1,<1.4.6
 botocore<1.5.71
+
+# sentry-plugins specific dependencies
+cached-property
 celery>=3.1.8,<3.1.19
 cffi>=1.11.5,<2.0
 click>=5.0,<7.0
@@ -65,7 +68,7 @@ redis>=2.10.3,<2.10.6
 requests-oauthlib==0.3.3
 requests[security]>=2.20.0,<2.21.0
 selenium==3.141.0
-semaphore>=0.4.59,<0.5.0
+semaphore>=0.4.61,<0.5.0
 sentry-sdk>=0.13.1
 setproctitle>=1.1.7,<1.2.0
 simplejson>=3.2.0,<3.9.0
diff --git a/src/sentry/utils/samples.py b/src/sentry/utils/samples.py
index 0f1845565a..61f124cbf3 100644
--- a/src/sentry/utils/samples.py
+++ b/src/sentry/utils/samples.py
@@ -145,7 +145,7 @@ def load_data(platform, default=None, sample_name=None):
     if platform == "transaction":
         now = timezone.now()
         now_time = to_timestamp(now)
-        start_time = to_timestamp(now - timedelta(seconds=-2))
+        start_time = to_timestamp(now - timedelta(seconds=2))
         data.setdefault("timestamp", now_time)
         data.setdefault("start_timestamp", start_time)
         for span in data["spans"]:
diff --git a/tests/snuba/api/endpoints/test_organization_events_v2.py b/tests/snuba/api/endpoints/test_organization_events_v2.py
index 30c6e19a71..8c83e149d5 100644
--- a/tests/snuba/api/endpoints/test_organization_events_v2.py
+++ b/tests/snuba/api/endpoints/test_organization_events_v2.py
@@ -556,8 +556,8 @@ class OrganizationEventsV2EndpointTest(APITestCase, SnubaTestCase):
 
         project = self.create_project()
         data = load_data("transaction")
-        data["timestamp"] = iso_format(before_now(minutes=1, seconds=5))
-        data["start_timestamp"] = iso_format(before_now(minutes=1))
+        data["timestamp"] = iso_format(before_now(minutes=1))
+        data["start_timestamp"] = iso_format(before_now(minutes=1, seconds=5))
         self.store_event(data=data, project_id=project.id)
 
         with self.feature("organizations:events-v2"):
