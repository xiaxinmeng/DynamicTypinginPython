commit 4ccca37018fd4f64de1a5c30a88eac46acba9efb
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Sep 1 11:12:11 2010 -0700

    Realtime only happens on page 1 now. Remove server name from tags.

diff --git a/example_project/urls.py b/example_project/urls.py
index df985cd9f4..a6bb3f1002 100644
--- a/example_project/urls.py
+++ b/example_project/urls.py
@@ -1,5 +1,6 @@
 from django.conf.urls.defaults import *
 
 urlpatterns = patterns('',
-    (r'^', include('sentry.urls')),
+    url(r'^trigger-500$', 'sentry.tests.views.raise_exc', name='sentry-raise-exc'),
+    url(r'^', include('sentry.urls')),
 )
diff --git a/sentry/media/scripts/global.js b/sentry/media/scripts/global.js
index 48f5aa6e5d..fe766cd1ff 100644
--- a/sentry/media/scripts/global.js
+++ b/sentry/media/scripts/global.js
@@ -54,11 +54,6 @@ $.fn.setAllToMaxHeight = function(){
 	return this.height( Math.max.apply(this, $.map( this , function(e){ return $(e).height() }) ) );
 }
 
-$(document).ready(function(){
-	//$("div.column").setAllToMaxHeight();
-	setTimeout('sentryRefresh()', 3000);
-});
-
 function sentryRefresh(){
     $.ajax({
       url: './',
diff --git a/sentry/media/styles/global.css b/sentry/media/styles/global.css
index 721ca2dc35..a3c8d376cd 100644
--- a/sentry/media/styles/global.css
+++ b/sentry/media/styles/global.css
@@ -255,7 +255,6 @@ dl.flat dd {
 .messages li:hover, .messages .active {
 	background: #e0ebff;
 	background: -moz-linear-gradient(top,  #e5e5e5,  #ddd); /* for firefox 3.6+ */
-	cursor: pointer;
 	-webkit-border-radius: 6px;
 	-moz-border-radius: 6px;
 	border-radius: 6px;
diff --git a/sentry/templates/sentry/index.html b/sentry/templates/sentry/index.html
index 7e164d5839..c3931e494d 100644
--- a/sentry/templates/sentry/index.html
+++ b/sentry/templates/sentry/index.html
@@ -79,4 +79,12 @@
 		</div> 
 		{% endblock %}
 	</body>
+	{% if realtime %}
+	<script type="text/javascript">
+	$(document).ready(function(){
+		//$("div.column").setAllToMaxHeight();
+		setTimeout('sentryRefresh()', 3000);
+	});
+	</script>
+	{% endif %}
 </html>
\ No newline at end of file
diff --git a/sentry/templates/sentry/partial/_group.html b/sentry/templates/sentry/partial/_group.html
index 6056d7d609..fa02ef79e7 100644
--- a/sentry/templates/sentry/partial/_group.html
+++ b/sentry/templates/sentry/partial/_group.html
@@ -1,8 +1,8 @@
-<li class="{% cycle 'row1' 'row2' %} level-{{ group.level }}{% if priority %} priority-{{ priority }}{% endif %}" onclick="window.location.href='{% url sentry-group group.pk %}'" id="group_{{ group.pk }}" data-sentry-count="{{ group.times_seen }}">
+<li class="{% cycle 'row1' 'row2' %} level-{{ group.level }}{% if priority %} priority-{{ priority }}{% endif %}" id="group_{{ group.pk }}" data-sentry-count="{{ group.times_seen }}">
 	<span class="count">{{ group.times_seen }}</span>
 	<h3><a href="{% url sentry-group group.pk %}">{% if group.view %}{{ group.view }}{% else %}{{ group.message }}{% endif %}</a></h3>
 	<span class="last_seen">{{ group.last_seen|timesince }} ago</span>
-	<p class="message"><span class="tag tag-level">{{ group.get_level_display }}</span> <span class="tag tag-server">{{ group.server_name }}</span> {{ group.error }}</p>
+	<p class="message"><span class="tag tag-level">{{ group.get_level_display }}</span> {{ group.error }}</p>
 	<pre class="traceback">{{ group.shortened_traceback }}</pre>
 	<span class="hidden">&raquo;</span>
 </li>
\ No newline at end of file
diff --git a/sentry/views.py b/sentry/views.py
index b7328e9078..836fddccd7 100644
--- a/sentry/views.py
+++ b/sentry/views.py
@@ -25,6 +25,8 @@ def index(request):
     server_name = request.GET.get('server_name') or ''
     level = request.GET.get('level') or ''
 
+    realtime = not (request.GET.get('p') > 1)
+
     if logger not in logger_names:
         logger = ''
 
diff --git a/setup.py b/setup.py
index f604848231..b7e89105bf 100644
--- a/setup.py
+++ b/setup.py
@@ -12,6 +12,7 @@ setup(
     packages=find_packages(),
     requirements=[
         'django-paging',
+        'django-indexer',
     ],
     include_package_data=True,
     classifiers=[
