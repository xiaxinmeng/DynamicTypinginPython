commit 6e6b70cc83062e17f795f0ad95a543162e122866
Author: David Cramer <dcramer@gmail.com>
Date:   Tue May 12 01:53:24 2015 -0700

    Use separate form for submit buttons
    
    This ensures the default submit action in the followup form works correctly, as well as removes the duplicate form.

diff --git a/src/sentry/templates/sentry/project-issue-tracking.html b/src/sentry/templates/sentry/project-issue-tracking.html
index 9fddf0df47..13cd502da3 100644
--- a/src/sentry/templates/sentry/project-issue-tracking.html
+++ b/src/sentry/templates/sentry/project-issue-tracking.html
@@ -21,7 +21,11 @@
           <legend>
             {{ plugin.get_title }}
             {% if plugin.can_disable %}
-              <button type="submit" class="btn btn-small btn-default" name="op" value="disable">Disable</button>
+              <form method="POST" style="display: inline-block">
+                {% csrf_token %}
+                <input type="hidden" name="plugin" value="{{ plugin.slug }}" />
+                <button type="submit" class="btn btn-small btn-default" name="op" value="disable">Disable</button>
+              </form>
             {% endif %}
           </legend>
           {{ content }}
diff --git a/src/sentry/templates/sentry/project-release-tracking.html b/src/sentry/templates/sentry/project-release-tracking.html
index 0522569f8a..0bd204ab7b 100644
--- a/src/sentry/templates/sentry/project-release-tracking.html
+++ b/src/sentry/templates/sentry/project-release-tracking.html
@@ -24,19 +24,19 @@
     </form>
 
     {% for plugin, content in enabled_plugins %}
-      <form method="POST">
-        {% csrf_token %}
-        <input type="hidden" name="plugin" value="{{ plugin.slug }}" />
-        <fieldset>
-          <legend>
-            {{ plugin.get_title }}
-            {% if plugin.can_disable %}
+      <fieldset>
+        <legend>
+          {{ plugin.get_title }}
+          {% if plugin.can_disable %}
+            <form method="POST" style="display: inline-block">
+              {% csrf_token %}
+              <input type="hidden" name="plugin" value="{{ plugin.slug }}" />
               <button type="submit" class="btn btn-small btn-default" name="op" value="disable">Disable</button>
-            {% endif %}
-          </legend>
-          {{ content }}
-        </fieldset>
-      </form>
+            </form>
+          {% endif %}
+        </legend>
+        {{ content }}
+      </fieldset>
     {% endfor %}
 
     {% if other_plugins %}
