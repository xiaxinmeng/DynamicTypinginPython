commit f67afaffd720dce332e115c4a4051dfa90578c08
Author: Armin Ronacher <armin.ronacher@active-4.com>
Date:   Mon Aug 8 14:59:51 2016 +0200

    warning -> error for frame symbolication

diff --git a/src/sentry/lang/native/plugin.py b/src/sentry/lang/native/plugin.py
index c5c598e865..aa4cc06969 100644
--- a/src/sentry/lang/native/plugin.py
+++ b/src/sentry/lang/native/plugin.py
@@ -409,8 +409,7 @@ def resolve_frame_symbols(data):
                         'symbol_addr': frame['symbol_addr'],
                     }, sdk_info, report_error=report_error)
                     if not sfrm:
-                        logger.warning('Frame did not symbolize (%s)',
-                                       frame)
+                        logger.error('Frame did not symbolize (%s)', frame)
                         continue
                     # XXX: log here if symbol could not be found?
                     frame['function'] = sfrm.get('symbol_name') or \
