commit 171dea0284838cc13a78c8572ab8fb975e758cba
Author: David Wang <davidw1339@gmail.com>
Date:   Wed Sep 4 15:04:35 2019 -0700

    ref(ui): Change utils/selectText.jsx to typescript (#14581)
    
    Converted the `selectText` function to typescript with relevant type annotations.

diff --git a/src/sentry/static/sentry/app/utils/selectText.jsx b/src/sentry/static/sentry/app/utils/selectText.jsx
deleted file mode 100644
index 0c72c938bc..0000000000
--- a/src/sentry/static/sentry/app/utils/selectText.jsx
+++ /dev/null
@@ -1,15 +0,0 @@
-export function selectText(node) {
-  if (node.type && node.type === 'text') {
-    node.select();
-  } else if (document.selection) {
-    const range = document.body.createTextRange();
-    range.moveToElementText(node);
-    range.select();
-  } else if (window.getSelection) {
-    const range = document.createRange();
-    range.selectNode(node);
-    const selection = window.getSelection();
-    selection.removeAllRanges();
-    selection.addRange(range);
-  }
-}
diff --git a/src/sentry/static/sentry/app/utils/selectText.tsx b/src/sentry/static/sentry/app/utils/selectText.tsx
new file mode 100644
index 0000000000..19e5108e0b
--- /dev/null
+++ b/src/sentry/static/sentry/app/utils/selectText.tsx
@@ -0,0 +1,13 @@
+export function selectText(node: EventTarget): void {
+  if (node instanceof HTMLInputElement && node.type === 'text') {
+    node.select();
+  } else if (node instanceof Node && window.getSelection) {
+    const range = document.createRange();
+    range.selectNode(node);
+    const selection = window.getSelection();
+    if (selection) {
+      selection.removeAllRanges();
+      selection.addRange(range);
+    }
+  }
+}
