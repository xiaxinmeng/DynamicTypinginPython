commit ec33289d80b2f2af19537cc29f0965fa5b8e5044
Author: Jan Michael Auer <jan.auer@sentry.io>
Date:   Wed May 8 14:14:12 2019 +0200

    fix(symbolicator): Merge frame trust into event payload (#13125)
    
    Now that symbolicator supplies a frame trust, it needs to be merged into the symbolicated frame.

diff --git a/src/sentry/lang/native/utils.py b/src/sentry/lang/native/utils.py
index d813aefc2d..3c2abf62da 100644
--- a/src/sentry/lang/native/utils.py
+++ b/src/sentry/lang/native/utils.py
@@ -123,6 +123,8 @@ def merge_symbolicated_frame(new_frame, sfrm, platform=None):
         new_frame['colno'] = sfrm['colno']
     if sfrm.get('package'):
         new_frame['package'] = sfrm['package']
+    if sfrm.get('trust'):
+        new_frame['trust'] = sfrm['trust']
     if sfrm.get('status'):
         frame_meta = new_frame.setdefault('data', {})
         frame_meta['symbolicator_status'] = sfrm['status']
