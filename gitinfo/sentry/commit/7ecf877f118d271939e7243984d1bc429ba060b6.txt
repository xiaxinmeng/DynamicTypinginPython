commit 7ecf877f118d271939e7243984d1bc429ba060b6
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Tue Feb 16 17:16:51 2016 -0800

    Provide a more sensible error in devserver when a watcher isn't found
    
    Popen raises an ugly OSError that just says "[Errno 2] No such file or
    directory" with no indication of what or where this came from.

diff --git a/src/sentry/management/commands/runserver.py b/src/sentry/management/commands/runserver.py
index 7a13c04056..0558d59ec4 100644
--- a/src/sentry/management/commands/runserver.py
+++ b/src/sentry/management/commands/runserver.py
@@ -5,6 +5,7 @@ import os.path
 import sys
 
 from django.conf import settings
+from django.core.management.base import CommandError
 from django.core.management.color import color_style
 from django.core.management.commands.runserver import Command as RunserverCommand
 from optparse import make_option
@@ -53,7 +54,10 @@ class Command(RunserverCommand):
         for watcher in self.get_watchers():
             if self.verbosity:
                 self.stdout.write(self.style.HTTP_INFO('>> Running {0}'.format(watcher)))
-            result.append(Popen(watcher, cwd=self.cwd, stdout=stdout, env=env))
+            try:
+                result.append(Popen(watcher, cwd=self.cwd, stdout=stdout, env=env))
+            except OSError:
+                raise CommandError('{0} not found.'.format(watcher[0]))
         return result
 
     def run_server(self, verbosity, **options):
