commit 551e349c4210a6a7c7b27e4a21f2d2283f812ea7
Author: Markus Unterwaditzer <markus@unterwaditzer.net>
Date:   Mon Jul 6 14:48:36 2020 +0200

    fix(pii): Remove wrong information about the regex syntax (#19696)
    
    Our regex engine *is* PCRE, however it seems that this leads people to
    try and use the PHP/PCRE option in regex101.com. Just link to the docs
    where we explain all things in a bit more detail

diff --git a/src/sentry/static/sentry/app/views/settings/components/dataScrubbing/form/form.tsx b/src/sentry/static/sentry/app/views/settings/components/dataScrubbing/form/form.tsx
index 24ac948db2..23935b023b 100644
--- a/src/sentry/static/sentry/app/views/settings/components/dataScrubbing/form/form.tsx
+++ b/src/sentry/static/sentry/app/views/settings/components/dataScrubbing/form/form.tsx
@@ -129,7 +129,7 @@ class Form extends React.Component<Props<Rule, KeysOfUnion<Rule>>, State> {
           {rule.type === RuleType.PATTERN && (
             <Field
               label={t('Regex matches')}
-              help={t('Custom Perl-style regex (PCRE)')}
+              help={t('Custom regular expression (see documentation)')}
               inline={false}
               error={errors?.pattern}
               flexibleControlStateSize
