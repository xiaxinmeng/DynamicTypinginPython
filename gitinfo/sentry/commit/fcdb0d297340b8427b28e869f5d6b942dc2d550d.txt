commit fcdb0d297340b8427b28e869f5d6b942dc2d550d
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Mar 7 13:42:46 2017 -0800

    use real deletion (for signals) on organization options

diff --git a/src/sentry/models/organizationoption.py b/src/sentry/models/organizationoption.py
index 476ffd73d5..56a3db6253 100644
--- a/src/sentry/models/organizationoption.py
+++ b/src/sentry/models/organizationoption.py
@@ -52,7 +52,11 @@ class OrganizationOptionManager(BaseManager):
         return result.get(key, default)
 
     def unset_value(self, organization, key):
-        self.filter(organization=organization, key=key).delete()
+        try:
+            inst = self.get(organization=organization, key=key)
+        except self.model.DoesNotExist:
+            return
+        inst.delete()
         self.reload_cache(organization.id)
 
     def set_value(self, organization, key, value):
