commit 4852227ed56fc3daec869652e78e36058d3c6e5f
Author: Billy Vong <billyvg@users.noreply.github.com>
Date:   Wed Oct 9 10:09:44 2019 -0700

    fix(ui): Fix `<CursorPoller>` accessing undefined response (#15000)
    
    Firefox seems to sometimes return undefined data which we attempt to
    access its `length` property without checking for existance.
    
    Fixes JAVASCRIPT-1JXS

diff --git a/src/sentry/static/sentry/app/utils/cursorPoller.jsx b/src/sentry/static/sentry/app/utils/cursorPoller.jsx
index 5a8b6e2471..deb57d134a 100644
--- a/src/sentry/static/sentry/app/utils/cursorPoller.jsx
+++ b/src/sentry/static/sentry/app/utils/cursorPoller.jsx
@@ -50,7 +50,7 @@ class CursorPoller {
         }
 
         // if theres no data, nothing changes
-        if (!data.length) {
+        if (!data || !data.length) {
           this._reqsWithoutData += 1;
           return;
         }
