commit ad59198ed9fcedbede50e2b5dcb54e99c96747d2
Author: David Cramer <dcramer@gmail.com>
Date:   Sat Mar 23 14:11:45 2013 -0700

    Optimize query in get_tags

diff --git a/src/sentry/models.py b/src/sentry/models.py
index 5dd03eb113..d5035f4d05 100644
--- a/src/sentry/models.py
+++ b/src/sentry/models.py
@@ -604,7 +604,9 @@ class Group(EventBase):
 
     def get_tags(self):
         if not hasattr(self, '_tag_cache'):
-            tags = sorted(self.grouptagkey_set.values_list('key', flat=True))
+            tags = sorted(self.grouptagkey_set.filter(
+                project=self.project,
+            ).values_list('key', flat=True))
             self._tag_cache = tags
         return self._tag_cache
 
