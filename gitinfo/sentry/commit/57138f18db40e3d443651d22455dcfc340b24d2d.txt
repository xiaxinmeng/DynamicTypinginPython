commit 57138f18db40e3d443651d22455dcfc340b24d2d
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Dec 10 23:34:17 2015 -0800

    Improve tag docs awareness

diff --git a/src/sentry/static/sentry/app/views/groupTags.jsx b/src/sentry/static/sentry/app/views/groupTags.jsx
index 085e846508..5c40de13e5 100644
--- a/src/sentry/static/sentry/app/views/groupTags.jsx
+++ b/src/sentry/static/sentry/app/views/groupTags.jsx
@@ -56,6 +56,10 @@ const GroupTags = React.createClass({
     });
   },
 
+  getTagsDocsUrl() {
+    return 'https://docs.getsentry.com/hosted/learn/context/';
+  },
+
   render() {
     if (this.state.loading) {
       return <LoadingIndicator />;
@@ -110,6 +114,13 @@ const GroupTags = React.createClass({
     return (
       <div className="row">
         {children}
+
+        <div className="col-md-12">
+          <div className="alert alert-block alert-info">
+            Tags are automatically indexed for searching and breakdown charts.
+            Learn how to <a href={this.getTagsDocsUrl()}>add custom tags to issues</a>.
+          </div>
+        </div>
       </div>
     );
   }
diff --git a/src/sentry/templates/sentry/projects/manage_tags.html b/src/sentry/templates/sentry/projects/manage_tags.html
index d62c4cc36b..40310209f0 100644
--- a/src/sentry/templates/sentry/projects/manage_tags.html
+++ b/src/sentry/templates/sentry/projects/manage_tags.html
@@ -10,7 +10,8 @@
   <h2>{% trans "Tags" %}</h2>
 
   {% if tag_list %}
-    <p>{% blocktrans %}Each event in Sentry may be annotated with various tags (key and value pairs). For more information on how send additional tags, see your client's documentation.{% endblocktrans %}</p>
+    <p>{% blocktrans with link='https://docs.getsentry.com/hosted/learn/context/' %}Each event in Sentry may be annotated with various tags (key and value pairs). Learn how to <a href="{{ link }}">add custom tags</a>.{% endblocktrans %}</p>
+
     <form action="" method="post">
       {% csrf_token %}
       {{ form|as_crispy_errors }}
