commit c9b06badd6786d014e0a3d24473fb038ace15d2e
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Mon Oct 19 15:26:21 2015 -0700

    Make sure isUrl is only called with strings.
    
    Anything else, is obviously not a url. Fixes GH-2211

diff --git a/src/sentry/static/sentry/app/utils.jsx b/src/sentry/static/sentry/app/utils.jsx
index ad468252da..a568e7f463 100644
--- a/src/sentry/static/sentry/app/utils.jsx
+++ b/src/sentry/static/sentry/app/utils.jsx
@@ -1,3 +1,5 @@
+import _ from "underscore";
+
 /*eslint no-use-before-define:0*/
 var modelsEqual = function(obj1, obj2) {
   if (!obj1 && !obj2)
@@ -171,7 +173,7 @@ export default {
   },
 
   isUrl(str) {
-    return (str.indexOf('http://') === 0) || (str.indexOf('https://') === 0);
+    return _.isString(str) && (str.indexOf('http://') === 0 || str.indexOf('https://') === 0);
   },
 
   escape(str) {
