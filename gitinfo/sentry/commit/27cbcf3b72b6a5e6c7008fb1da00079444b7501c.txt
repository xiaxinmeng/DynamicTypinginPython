commit 27cbcf3b72b6a5e6c7008fb1da00079444b7501c
Author: David Cramer <dcramer@gmail.com>
Date:   Fri Apr 7 11:38:15 2017 -0700

    [releases] improve usability of short IDs

diff --git a/src/sentry/static/sentry/app/views/groupDetails/header.jsx b/src/sentry/static/sentry/app/views/groupDetails/header.jsx
index cd3e7870a0..c06abfafd4 100644
--- a/src/sentry/static/sentry/app/views/groupDetails/header.jsx
+++ b/src/sentry/static/sentry/app/views/groupDetails/header.jsx
@@ -10,6 +10,7 @@ import ListLink from '../../components/listLink';
 import ShortId from '../../components/shortId';
 import GroupTitle from '../../components/group/title';
 import ProjectState from '../../mixins/projectState';
+import TooltipMixin from '../../mixins/tooltip';
 import {t} from '../../locale';
 
 const GroupHeader = React.createClass({
@@ -22,7 +23,13 @@ const GroupHeader = React.createClass({
     location: React.PropTypes.object
   },
 
-  mixins: [ApiMixin, ProjectState],
+  mixins: [
+    ApiMixin,
+    ProjectState,
+    TooltipMixin({
+      selector: '.tip',
+    }),
+  ],
 
   onToggleMute() {
     let group = this.props.group;
@@ -150,7 +157,11 @@ const GroupHeader = React.createClass({
               {group.shortId &&
                 this.getFeatures().has('callsigns') &&
                 <div className="short-id-box count align-right">
-                  <h6 className="nav-header">{t('Issue #')}</h6>
+                  <h6 className="nav-header">
+                    <a className="help-link tip"
+                       title={t('This identifier is unique across your organization, and can be used to reference an issue in various places, like commit messages.')}
+                       href="https://docs.sentry.io/learn/releases/#resolving-issues-via-commits">{t('Issue #')}</a>
+                  </h6>
                   <ShortId shortId={group.shortId} />
                 </div>}
               <div className="assigned-to">
diff --git a/src/sentry/static/sentry/less/group-detail.less b/src/sentry/static/sentry/less/group-detail.less
index ca61949b9e..472af13925 100644
--- a/src/sentry/static/sentry/less/group-detail.less
+++ b/src/sentry/static/sentry/less/group-detail.less
@@ -26,7 +26,8 @@
   }
 
   .short-id {
-    font-size: 20px !important;
+    font-size: 17px !important;
+    font-family: @font-family-code;
     margin-top: 11px;
   }
 
diff --git a/src/sentry/static/sentry/less/shared-components.less b/src/sentry/static/sentry/less/shared-components.less
index f454016e79..13c9264598 100644
--- a/src/sentry/static/sentry/less/shared-components.less
+++ b/src/sentry/static/sentry/less/shared-components.less
@@ -2535,6 +2535,11 @@ ul.faces {
   letter-spacing: 0.1px;
   .clearfix;
 
+  a.help-link {
+    color: inherit;
+    border-bottom: 1px dotted @gray-light;
+  }
+
   .view-more {
     color: @gray-dark;
     float: right;
diff --git a/src/sentry/static/sentry/less/stream.less b/src/sentry/static/sentry/less/stream.less
index b9d4718a59..a4e27ef8a4 100644
--- a/src/sentry/static/sentry/less/stream.less
+++ b/src/sentry/static/sentry/less/stream.less
@@ -256,6 +256,8 @@
 
     .short-id {
       font-weight: bold;
+      font-family: @font-family-code;
+      font-size: 11px;
     }
 
     .icon-star-solid, .icon-soundoff {
