commit fa875e568cc75facfa2f3865426227cc3cf3c70b
Author: Mark Story <mark@sentry.io>
Date:   Fri May 8 13:51:05 2020 -0400

    fix(perf-views) Start performance overview charts at data min (#18684)
    
    Starting y-axis at dataMin allows the charts to start their yaxis in the
    middle allowing smaller differences to be seen as the charts don't start
    at 0.

diff --git a/src/sentry/static/sentry/app/views/performance/charts/chart.tsx b/src/sentry/static/sentry/app/views/performance/charts/chart.tsx
index 7abfc63874..d7cb23b54d 100644
--- a/src/sentry/static/sentry/app/views/performance/charts/chart.tsx
+++ b/src/sentry/static/sentry/app/views/performance/charts/chart.tsx
@@ -57,7 +57,16 @@ class Chart extends React.Component<Props> {
           type: 'time',
         },
       ],
-      yAxes: [{gridIndex: 0}, {gridIndex: 1}],
+      yAxes: [
+        {
+          gridIndex: 0,
+          scale: true,
+        },
+        {
+          gridIndex: 1,
+          scale: true,
+        },
+      ],
       utc,
       isGroupedByDate: true,
       showTimeInTooltip: true,
