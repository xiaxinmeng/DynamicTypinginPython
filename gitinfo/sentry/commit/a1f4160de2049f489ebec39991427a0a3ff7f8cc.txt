commit a1f4160de2049f489ebec39991427a0a3ff7f8cc
Author: James Cunningham <jtcunning@getsentry.com>
Date:   Mon Jul 11 13:17:09 2016 -0700

    Move lower() to reflect actual format when calling `env`

diff --git a/src/sentry/runner/decorators.py b/src/sentry/runner/decorators.py
index 763d9440ae..8096626809 100644
--- a/src/sentry/runner/decorators.py
+++ b/src/sentry/runner/decorators.py
@@ -60,7 +60,7 @@ def log_options(default=None):
             if loglevel:
                 os.environ['SENTRY_LOG_LEVEL'] = loglevel
             if logformat:
-                os.environ['SENTRY_LOG_FORMAT'] = logformat
+                os.environ['SENTRY_LOG_FORMAT'] = logformat.lower()
             return ctx.invoke(f, *args, **kwargs)
         return update_wrapper(inner, f)
     return decorator
