commit f94e0dc9071bf57b51eb323e612a96af4ebf4d9d
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Sep 15 11:50:54 2010 -0700

    Revert change to adjust checksum

diff --git a/sentry/helpers.py b/sentry/helpers.py
index 8a2e742e23..95eab02f0b 100644
--- a/sentry/helpers.py
+++ b/sentry/helpers.py
@@ -40,6 +40,7 @@ def construct_checksum(level=logging.ERROR, class_name='', traceback='', message
     message = traceback or message
     if isinstance(message, unicode):
         message = message.encode('utf-8', 'replace')
+    checksum.update(message)
     return checksum.hexdigest()
 
 def varmap(func, var):
