commit e02fde8ab5a321d240af28e59ad29d711a3e820f
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Oct 22 20:11:40 2014 -0700

    Correct implicit org checks

diff --git a/src/sentry/web/frontend/base.py b/src/sentry/web/frontend/base.py
index 42efd56140..a3add4a59a 100644
--- a/src/sentry/web/frontend/base.py
+++ b/src/sentry/web/frontend/base.py
@@ -21,16 +21,19 @@ class OrganizationMixin(object):
         if no organization.
         """
         active_organization = None
-        try:
-            organization_id = int(organization_id)
-        except (TypeError, ValueError):
-            return None
 
         is_implicit = organization_id is None
 
         if is_implicit:
             organization_id = request.session.get('activeorg')
 
+        if organization_id:
+            try:
+                organization_id = int(organization_id)
+            except (TypeError, ValueError):
+                if not is_implicit:
+                    return None
+
         if organization_id is not None:
             if request.user.is_superuser:
                 try:
@@ -65,6 +68,7 @@ class OrganizationMixin(object):
             if not is_implicit:
                 return None
 
+            print(organizations)
             try:
                 active_organization = organizations[0]
             except IndexError:
