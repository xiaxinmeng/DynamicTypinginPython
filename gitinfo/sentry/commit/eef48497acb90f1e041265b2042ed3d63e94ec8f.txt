commit eef48497acb90f1e041265b2042ed3d63e94ec8f
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Thu May 26 16:57:03 2016 -0700

    Remove hardcoded celery options through `sentry run {celery,cron}`

diff --git a/src/sentry/runner/commands/run.py b/src/sentry/runner/commands/run.py
index cc0af9e5ab..a599c2b0a7 100644
--- a/src/sentry/runner/commands/run.py
+++ b/src/sentry/runner/commands/run.py
@@ -133,9 +133,9 @@ def worker(**options):
 
     from sentry.celery import app
     worker = app.Worker(
-        without_gossip=True,
-        without_mingle=True,
-        without_heartbeat=True,
+        # without_gossip=True,
+        # without_mingle=True,
+        # without_heartbeat=True,
         pool_cls='processes',
         **options
     )
@@ -170,8 +170,8 @@ def cron(**options):
 
     from sentry.celery import app
     app.Beat(
-        without_gossip=True,
-        without_mingle=True,
-        without_heartbeat=True,
+        # without_gossip=True,
+        # without_mingle=True,
+        # without_heartbeat=True,
         **options
     ).run()
