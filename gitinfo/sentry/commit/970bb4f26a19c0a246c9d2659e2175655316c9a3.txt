commit 970bb4f26a19c0a246c9d2659e2175655316c9a3
Author: David Cramer <dcramer@gmail.com>
Date:   Fri Jan 23 16:18:13 2015 -0800

    Draft interface handling code

diff --git a/src/sentry/static/sentry/app/views/aggregate/event.jsx b/src/sentry/static/sentry/app/views/aggregate/event.jsx
index 40b723b598..26a5f03071 100644
--- a/src/sentry/static/sentry/app/views/aggregate/event.jsx
+++ b/src/sentry/static/sentry/app/views/aggregate/event.jsx
@@ -15,10 +15,36 @@ var AggregateEvent = React.createClass({
     event: PropTypes.Event.isRequired
   },
 
+  // TODO(dcramer): figure out how we make this extensible
+  interfaces: {
+    exception: require("./interfaces/exception")
+  },
+
   render: function(){
     var agg = this.props.aggregate;
     var evt = this.props.event;
 
+    var entries = [];
+    evt.entries.forEach(function(entry, _){
+      try {
+        var Component = this.interfaces[entry.type];
+        if (!Component) {
+          throw new Error('Unregistered interface: ' + entry.type);
+
+        }
+        entries.push(
+          <Component
+              key={"entry-" + _}
+              aggregate={agg}
+              event={evt}
+              data={entry.data} />
+        );
+      } catch (ex) {
+        // TODO(dcramer): this should log to Sentry
+        console.error(ex);
+      }
+    }.bind(this));
+
     return (
       <div>
         <AggregateEventHeader
@@ -27,6 +53,7 @@ var AggregateEvent = React.createClass({
         <AggregateEventTags
             aggregate={agg}
             event={evt} />
+        {entries}
         <AggregateEventDataSection
             aggregate={agg}
             event={evt}
diff --git a/src/sentry/static/sentry/app/views/aggregate/interfaces/exception.jsx b/src/sentry/static/sentry/app/views/aggregate/interfaces/exception.jsx
new file mode 100644
index 0000000000..693bb73c84
--- /dev/null
+++ b/src/sentry/static/sentry/app/views/aggregate/interfaces/exception.jsx
@@ -0,0 +1,31 @@
+/*** @jsx React.DOM */
+
+var React = require("react");
+var Reflux = require("reflux");
+var Router = require("react-router");
+
+var AggregateEventDataSection = require("../eventDataSection");
+var PropTypes = require("../../../proptypes");
+
+var ExceptionInterface = React.createClass({
+  propTypes: {
+    aggregate: PropTypes.Aggregate.isRequired,
+    event: PropTypes.Event.isRequired,
+    data: React.PropTypes.object.isRequired
+  },
+
+  render: function(){
+    var agg = this.props.aggregate;
+    var evt = this.props.event;
+
+    return (
+      <AggregateEventDataSection
+          aggregate={agg}
+          event={evt}
+          title="Exception">
+      </AggregateEventDataSection>
+    );
+  }
+});
+
+module.exports = ExceptionInterface;
