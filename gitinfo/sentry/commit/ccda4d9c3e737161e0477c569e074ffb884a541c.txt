commit ccda4d9c3e737161e0477c569e074ffb884a541c
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Nov 11 13:00:20 2015 -0800

    Raise hard error when API key is invalid

diff --git a/src/sentry/api/authentication.py b/src/sentry/api/authentication.py
index 9a40ba4289..4021811e00 100644
--- a/src/sentry/api/authentication.py
+++ b/src/sentry/api/authentication.py
@@ -22,7 +22,7 @@ class ApiKeyAuthentication(QuietBasicAuthentication):
         try:
             key = ApiKey.objects.get_from_cache(key=userid)
         except ApiKey.DoesNotExist:
-            return None
+            raise AuthenticationFailed('API key is not valid')
 
         if not key.is_active:
             raise AuthenticationFailed('Key is disabled')
