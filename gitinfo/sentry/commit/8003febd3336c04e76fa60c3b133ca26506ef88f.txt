commit 8003febd3336c04e76fa60c3b133ca26506ef88f
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Tue Dec 13 13:29:09 2016 -0800

    mocks: add ReleaseCommit (#4665)

diff --git a/bin/load-mocks b/bin/load-mocks
index c04cc1aa4d..1c36a9d580 100755
--- a/bin/load-mocks
+++ b/bin/load-mocks
@@ -19,7 +19,8 @@ from sentry.app import tsdb, buffer
 from sentry.models import (
     Activity, Broadcast, File, GroupMeta, Organization, OrganizationAccessRequest,
     OrganizationMember, Project, Release, ReleaseFile, Team, User, UserReport,
-    GroupRelease, Environment, ReleaseEnvironment
+    GroupRelease, Environment, ReleaseEnvironment, ReleaseCommit, Commit, Repository,
+    CommitAuthor, CommitFileChange,
 )
 from sentry.signals import mocks_loaded
 from sentry.utils.hashlib import md5_text
@@ -285,6 +286,38 @@ def main(num_events=1):
                 )[0],
             )
 
+            commit = Commit.objects.get_or_create(
+                organization_id=org.id,
+                repository_id=Repository.objects.get_or_create(
+                    organization_id=org.id,
+                    name='Example Repo',
+                    provider='github',
+                    external_id='example/example',
+                )[0].id,
+                key='6fb7672bee9c469a8aa074f52ed72eddc939ae7d',
+                author=CommitAuthor.objects.get_or_create(
+                    organization_id=org.id,
+                    name=user.name,
+                    email=user.email,
+                )[0],
+                message='real good stuff here\n\nWe did lots of good stuff over and over.'
+            )[0]
+
+            CommitFileChange.objects.get_or_create(
+                organization_id=org.id,
+                commit=commit,
+                filename='an-example.js',
+                type='M',
+            )
+
+            ReleaseCommit.objects.get_or_create(
+                organization_id=org.id,
+                project_id=project.id,
+                release=release,
+                commit=commit,
+                order=0,
+            )
+
             Activity.objects.create(
                 type=Activity.RELEASE,
                 project=project,
