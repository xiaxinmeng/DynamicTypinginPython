commit 98c938ad76409f9ec312bd29beda85effa4f7886
Author: Bruno Garcia <github@brunogarcia.com>
Date:   Wed Dec 5 13:41:28 2018 +0100

    feat(unreal): query string data to event (#10710)
    
    * feat(unreal): query string data to event
    * ref: don't rely on version here
    * engine version is part of the context xml parsing

diff --git a/src/sentry/web/api.py b/src/sentry/web/api.py
index f1047fd611..0d4c09e57a 100644
--- a/src/sentry/web/api.py
+++ b/src/sentry/web/api.py
@@ -744,9 +744,16 @@ class UnrealView(StoreView):
         attachments_enabled = features.has('organizations:event-attachments',
                                            project.organization, actor=request.user)
 
-        data = {}
         event_id = uuid.uuid4().hex
-        data['event_id'] = event_id
+        data = {
+            'event_id': event_id,
+            'environment': request.GET.get('AppEnvironment'),
+        }
+        user_id = request.GET.get('UserID')
+        if user_id:
+            data['user'] = {
+                'id': user_id
+            }
 
         attachments = []
         try:
