commit e5d5a444776e91d946bcccf2f19ce466c8a9b281
Author: Katie Lundsgaard <katie@getsentry.com>
Date:   Tue Mar 14 14:05:02 2017 -0700

    Encode deploy environment and version for url string

diff --git a/src/sentry/static/sentry/app/views/releases/releaseOverview.jsx b/src/sentry/static/sentry/app/views/releases/releaseOverview.jsx
index c5ded35afb..2f0131fe3c 100644
--- a/src/sentry/static/sentry/app/views/releases/releaseOverview.jsx
+++ b/src/sentry/static/sentry/app/views/releases/releaseOverview.jsx
@@ -263,10 +263,10 @@ const ReleaseOverview = React.createClass({
             <h6 className="nav-header m-b-1">{t('Deploys')}</h6>
             <ul className="nav nav-stacked">
               { !deploys.length ? this.renderEmpty() :
-                deploys.map((deploy) => {
+                deploys.map(deploy => {
                   return (
                     <li key={deploy.id}>
-                      <a href={`/${orgId}/${projectId}/?query=environment:${deploy.environment}+release:${version}`}>
+                      <a href={`/${orgId}/${projectId}/?query=environment${encodeURIComponent(':' + deploy.environment)}+release${encodeURIComponent(':' + version)}`}>
                         <div className="row row-flex row-center-vertically">
                           <div className="col-xs-6">
                             <span className="repo-label" style={{verticalAlign: 'bottom'}}>{deploy.environment}</span>
