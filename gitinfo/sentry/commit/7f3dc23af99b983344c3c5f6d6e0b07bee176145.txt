commit 7f3dc23af99b983344c3c5f6d6e0b07bee176145
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Jan 5 19:02:41 2012 -0800

    missing argument in get_all_values

diff --git a/sentry/manager.py b/sentry/manager.py
index f65fba455e..231245e5c6 100644
--- a/sentry/manager.py
+++ b/sentry/manager.py
@@ -501,7 +501,7 @@ class MetaManager(models.Manager):
         self.field_name = field_name
 
     def get_value(self, instance, key, default=NOTSET):
-        result = self.get_all_values()
+        result = self.get_all_values(instance)
         if default is self.NOTSET:
             return result[key]
         return result.get(key, default)
