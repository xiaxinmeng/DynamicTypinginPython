commit e522c9efe84bfbca77ae89131b2c837710320553
Author: David Cramer <dcramer@gmail.com>
Date:   Mon May 28 17:35:05 2012 -0700

    Better reporting on client api errors

diff --git a/sentry/web/api.py b/sentry/web/api.py
index 1f207258f0..f932305583 100644
--- a/sentry/web/api.py
+++ b/sentry/web/api.py
@@ -150,7 +150,9 @@ def store(request, project=None):
 
             insert_data_to_database(data)
         except APIError, error:
-            logger.error('Client %r raised API error: %s' % (client, error))
+            logger.exception('Client %r raised API error: %s' % (client, error), extra={
+                'request': request,
+            })
             response = HttpResponse(unicode(error.msg), status=error.http_status)
         else:
             logger.info('New event from client %r (id=%%s)' % client, data['event_id'])
