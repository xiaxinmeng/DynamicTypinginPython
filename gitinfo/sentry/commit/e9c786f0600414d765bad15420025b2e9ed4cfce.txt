commit e9c786f0600414d765bad15420025b2e9ed4cfce
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Dec 15 14:53:40 2016 -0800

    [issues] remove sdk from shared issue details (#4682)

diff --git a/CHANGES b/CHANGES
index dd4348b515..17a4081cea 100644
--- a/CHANGES
+++ b/CHANGES
@@ -7,6 +7,8 @@ Version 8.12 (Unreleased)
 - Added data migration to backfill legacy release data
 - Added data migration to backfill legacy commit data
 - Added data migration to backfill legacy release file and environment data
+- Allow gziped/deflated JavaScript artifacts to be uploaded through the API.
+- Shared issue view no longer shows SDK.
 
 SDKs
 ~~~~
diff --git a/src/sentry/api/serializers/models/event.py b/src/sentry/api/serializers/models/event.py
index f05b97519f..93ac3488cc 100644
--- a/src/sentry/api/serializers/models/event.py
+++ b/src/sentry/api/serializers/models/event.py
@@ -149,6 +149,7 @@ class SharedEventSerializer(EventSerializer):
         del result['contexts']
         del result['user']
         del result['tags']
+        del result['sdk']
         result['entries'] = [
             e for e in result['entries']
             if e['type'] != 'breadcrumbs'
diff --git a/tests/acceptance/test_shared_issue.py b/tests/acceptance/test_shared_issue.py
new file mode 100644
index 0000000000..0ffde726b8
--- /dev/null
+++ b/tests/acceptance/test_shared_issue.py
@@ -0,0 +1,51 @@
+from __future__ import absolute_import
+
+from datetime import datetime
+from django.utils import timezone
+
+from sentry.testutils import AcceptanceTestCase
+from sentry.utils.samples import create_sample_event
+
+
+class SharedIssueTest(AcceptanceTestCase):
+    def setUp(self):
+        super(SharedIssueTest, self).setUp()
+        self.user = self.create_user('foo@example.com')
+        self.org = self.create_organization(
+            owner=self.user,
+            name='Rowdy Tiger'
+        )
+        self.team = self.create_team(
+            organization=self.org,
+            name='Mariachi Band'
+        )
+        self.project = self.create_project(
+            organization=self.org,
+            team=self.team,
+            name='Bengal',
+        )
+        self.login_as(self.user)
+
+    def create_sample_event(self, platform):
+        event = create_sample_event(
+            project=self.project,
+            platform=platform,
+            event_id='d964fdbd649a4cf8bfc35d18082b6b0e',
+            timestamp=1452683305,
+        )
+        event.group.update(
+            first_seen=datetime(2015, 8, 13, 3, 8, 25, tzinfo=timezone.utc),
+            last_seen=datetime(2016, 1, 13, 3, 8, 25, tzinfo=timezone.utc),
+        )
+        return event
+
+    def test_cocoa_event(self):
+        event = self.create_sample_event(
+            platform='cocoa',
+        )
+
+        self.browser.get('/share/issue/{}/'.format(
+            event.group.get_share_id()
+        ))
+        self.browser.wait_until('.entries')
+        self.browser.snapshot('shared issue cocoa')
diff --git a/tests/sentry/api/serializers/test_event.py b/tests/sentry/api/serializers/test_event.py
index 9721519185..e215a2e8fe 100644
--- a/tests/sentry/api/serializers/test_event.py
+++ b/tests/sentry/api/serializers/test_event.py
@@ -45,5 +45,6 @@ class SharedEventSerializerTest(TestCase):
         assert result.get('contexts') is None
         assert result.get('user') is None
         assert result.get('tags') is None
+        assert 'sdk' not in result
         for entry in result['entries']:
             assert entry['type'] != 'breadcrumbs'
