commit b2b1c0f623623242f9a40407c19196fa0bcb5731
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Sep 17 16:32:50 2015 -0700

    Add cell highlight + click to project list

diff --git a/src/sentry/static/sentry/app/views/organizationTeams/expandedTeamList.jsx b/src/sentry/static/sentry/app/views/organizationTeams/expandedTeamList.jsx
index c64563a199..44eff3de7b 100644
--- a/src/sentry/static/sentry/app/views/organizationTeams/expandedTeamList.jsx
+++ b/src/sentry/static/sentry/app/views/organizationTeams/expandedTeamList.jsx
@@ -1,5 +1,4 @@
 import React from "react";
-import Router from "react-router";
 
 import api from "../../api";
 import BarChart from "../../components/barChart";
@@ -56,12 +55,19 @@ var ExpandedTeamList = React.createClass({
     );
   },
 
-  renderProject(project) {
-    var projectStats = this.props.projectStats;
+  toProjectDetails(project, event) {
     var projectRouteParams = {
       orgId: this.props.organization.slug,
       projectId: project.slug
     };
+
+    event.preventDefault();
+
+    this.context.router.transitionTo('projectDetails', projectRouteParams);
+  },
+
+  renderProject(project) {
+    var projectStats = this.props.projectStats;
     var chartData = null;
     if (projectStats[project.id]) {
       chartData = projectStats[project.id].map((point) => {
@@ -70,11 +76,12 @@ var ExpandedTeamList = React.createClass({
     }
 
     return (
-      <tr key={project.id}>
+      <tr key={project.id} className="clickable"
+          onClick={this.toProjectDetails.bind(this, project)}>
         <td>
-          <Router.Link to="projectDetails" params={projectRouteParams}>
+          <a onClick={this.toProjectDetails.bind(this, project)}>
             {project.name}
-          </Router.Link>
+          </a>
         </td>
         <td className="align-right project-chart">
           {chartData && <BarChart points={chartData} className="sparkline" /> }
diff --git a/src/sentry/static/sentry/less/organization.less b/src/sentry/static/sentry/less/organization.less
index 4ac6505cbd..ffbb850b50 100644
--- a/src/sentry/static/sentry/less/organization.less
+++ b/src/sentry/static/sentry/less/organization.less
@@ -191,21 +191,35 @@ table.project-list {
   margin-bottom: 0;
   font-size: 16px;
 
-  tbody tr td {
-    border: 0;
-    padding: 15px 20px !important;
-    height: 55px;
-    width: 150px;
-
-    &:first-child {
-      width: auto;
-    }
+  tbody tr {
+    td {
+      border: 0;
+      padding: 15px 20px !important;
+      height: 55px;
+      width: 150px;
+
+      &:first-child {
+        width: auto;
 
-    &.project-chart {
-      width: 250px;
-      .sparkline a {
-        height: 20px;
+        a {
+          color: @gray-dark;
+        }
       }
+
+      &.project-chart {
+        width: 250px;
+        .sparkline a {
+          height: 20px;
+        }
+      }
+    }
+    &:hover.clickable td {
+      background-color: @bgColor;
+      cursor: pointer;
+    }
+
+    &:hover td a {
+      color: @black;
     }
   }
 }
