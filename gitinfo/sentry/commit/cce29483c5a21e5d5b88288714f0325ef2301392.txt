commit cce29483c5a21e5d5b88288714f0325ef2301392
Author: Lyn Nagara <lyn.nagara@gmail.com>
Date:   Wed Mar 28 09:11:07 2018 -0700

    fix(alert-rules): Fix alerts with levels (#7826)
    
    Fixes JAVASCRIPT-3HJ

diff --git a/src/sentry/rules/conditions/level.py b/src/sentry/rules/conditions/level.py
index a93140c8d1..1a6dde2af0 100644
--- a/src/sentry/rules/conditions/level.py
+++ b/src/sentry/rules/conditions/level.py
@@ -46,6 +46,10 @@ class LevelEventForm(forms.Form):
 class LevelCondition(EventCondition):
     form_cls = LevelEventForm
     label = 'An event\'s level is {match} {level}'
+    form_fields = {
+        'level': {'type': 'choice', 'choices': LEVEL_CHOICES.items()},
+        'match': {'type': 'choice', 'choices': MATCH_CHOICES.items()}
+    }
 
     def passes(self, event, state, **kwargs):
         desired_level = self.get_option('level')
