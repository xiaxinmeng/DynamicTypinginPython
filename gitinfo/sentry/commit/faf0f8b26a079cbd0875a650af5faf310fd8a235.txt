commit faf0f8b26a079cbd0875a650af5faf310fd8a235
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Apr 14 16:25:44 2015 -0700

    Correct logging arg order

diff --git a/src/sentry/tasks/check_auth.py b/src/sentry/tasks/check_auth.py
index e57a04328a..d6a46b792f 100644
--- a/src/sentry/tasks/check_auth.py
+++ b/src/sentry/tasks/check_auth.py
@@ -63,8 +63,8 @@ def check_auth_identity(auth_identity_id, **kwargs):
     except IdentityNotValid as exc:
         logger.warning(
             u'AuthIdentity(id=%s) notified as not valid: %s',
-            unicode(exc),
             auth_identity_id,
+            unicode(exc),
             exc_info=True,
         )
         metrics.incr('auth.identities.invalidated', 1)
@@ -76,8 +76,8 @@ def check_auth_identity(auth_identity_id, **kwargs):
         metrics.incr('auth.identities.refresh_error', 1)
         logger.exception(
             u'AuthIdentity(id=%s) returned an error during validation: %s',
-            unicode(exc),
             auth_identity_id,
+            unicode(exc),
         )
         is_linked = True
         is_valid = False
