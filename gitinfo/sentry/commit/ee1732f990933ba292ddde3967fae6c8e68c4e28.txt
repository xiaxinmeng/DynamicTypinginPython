commit ee1732f990933ba292ddde3967fae6c8e68c4e28
Author: Alberto Leal <mail4alberto@gmail.com>
Date:   Mon Aug 19 11:55:34 2019 -0400

    chore(ts): Better generics for getDynamicText function (#14415)

diff --git a/src/sentry/static/sentry/app/utils/getDynamicText.tsx b/src/sentry/static/sentry/app/utils/getDynamicText.tsx
index e37250f233..9fc9209010 100644
--- a/src/sentry/static/sentry/app/utils/getDynamicText.tsx
+++ b/src/sentry/static/sentry/app/utils/getDynamicText.tsx
@@ -2,12 +2,12 @@
 
 // Return a specified "fixed" string when we are in a testing environment
 // (more specifically in a PERCY env (e.g. CI))
-export default function getDynamicText({
+export default function getDynamicText<Value, Fixed = Value>({
   value,
   fixed,
 }: {
-  value: React.ReactNode;
-  fixed: string;
-}): React.ReactNode {
+  value: Value;
+  fixed: Fixed;
+}): Value | Fixed {
   return process.env.IS_PERCY ? fixed : value;
 }
