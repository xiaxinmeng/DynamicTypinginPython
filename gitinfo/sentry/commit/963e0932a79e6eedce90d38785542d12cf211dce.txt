commit 963e0932a79e6eedce90d38785542d12cf211dce
Author: Mark Story <mark@sentry.io>
Date:   Wed Oct 9 12:59:56 2019 -0400

    fix(tests) Improve stability of discover store tests (#14991)
    
    Add more ticks to try and flush out the remaining race in this test.

diff --git a/tests/js/spec/stores/discoverSavedQueriesStore.spec.jsx b/tests/js/spec/stores/discoverSavedQueriesStore.spec.jsx
index 142b240894..264db6f544 100644
--- a/tests/js/spec/stores/discoverSavedQueriesStore.spec.jsx
+++ b/tests/js/spec/stores/discoverSavedQueriesStore.spec.jsx
@@ -130,6 +130,7 @@ describe('DiscoverSavedQueriesStore', function() {
     });
     fetchSavedQueries(api, 'org-1');
     await tick();
+    await tick();
 
     const query = {
       id: '9',
@@ -138,6 +139,7 @@ describe('DiscoverSavedQueriesStore', function() {
     };
     updateSavedQuery(api, 'org-1', query);
     await tick();
+    await tick();
 
     const state = DiscoverSavedQueriesStore.get();
     expect(state.isLoading).toEqual(false);
