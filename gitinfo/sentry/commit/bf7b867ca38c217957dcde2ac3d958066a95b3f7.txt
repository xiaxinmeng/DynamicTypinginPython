commit bf7b867ca38c217957dcde2ac3d958066a95b3f7
Author: Ben Vinegar <ben@benv.ca>
Date:   Fri Mar 25 12:05:24 2016 -0700

    For now, just reload stats on mount; ignore team/project changes

diff --git a/src/sentry/static/sentry/app/views/organizationTeams/index.jsx b/src/sentry/static/sentry/app/views/organizationTeams/index.jsx
index aad8d972b2..f0f61c7d41 100644
--- a/src/sentry/static/sentry/app/views/organizationTeams/index.jsx
+++ b/src/sentry/static/sentry/app/views/organizationTeams/index.jsx
@@ -8,7 +8,7 @@ import OrganizationHomeContainer from '../../components/organizations/homeContai
 import OrganizationState from '../../mixins/organizationState';
 import TeamStore from '../../stores/teamStore';
 import TooltipMixin from '../../mixins/tooltip';
-import {sortArray, arrayIsEqual} from '../../utils';
+import {sortArray} from '../../utils';
 
 import ExpandedTeamList from './expandedTeamList';
 import OrganizationStatOverview from './organizationStatOverview';
@@ -37,11 +37,6 @@ const OrganizationTeams = React.createClass({
     this.fetchStats();
   },
 
-  componentWillUpdate(nextProps, nextState) {
-    if (!arrayIsEqual(nextState.teamList.map(team => team.id), this.state.teamList.map(team => team.id)))
-      this.fetchStats();
-  },
-
   fetchStats() {
     loadStats(this.api, {
       orgId: this.props.params.orgId,
