commit e807b027c679eabe50229ec52f95ae8fcc0cb172
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Jul 15 14:50:10 2013 -0700

    Support tuple and remove dupe check

diff --git a/src/sentry/templatetags/sentry_helpers.py b/src/sentry/templatetags/sentry_helpers.py
index 9929f6f3b0..16ee7792c5 100644
--- a/src/sentry/templatetags/sentry_helpers.py
+++ b/src/sentry/templatetags/sentry_helpers.py
@@ -389,10 +389,12 @@ def github_button(user, repo):
 
 @register.inclusion_tag('sentry/partial/data_values.html')
 def render_values(value, threshold=5, collapse_to=3):
-    if isinstance(value, list):
+    if isinstance(value, (list, tuple)):
         value = dict(("[%04d]" % (idx + 1), val) for idx, val in enumerate(value))
+        is_dict = True
+    else:
+        is_dict = isinstance(value, dict)
 
-    is_dict = isinstance(value, dict)
     context = {
         'is_dict': is_dict,
         'threshold': threshold,
