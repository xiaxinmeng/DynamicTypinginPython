commit ee01d1db8d656449a8c6bc50b8f0fd63ca5c186b
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Wed Sep 2 11:25:56 2015 -0700

    Clean up GroupEmailThread table

diff --git a/src/sentry/management/commands/cleanup.py b/src/sentry/management/commands/cleanup.py
index 1dfc790175..04f6db0c80 100644
--- a/src/sentry/management/commands/cleanup.py
+++ b/src/sentry/management/commands/cleanup.py
@@ -16,7 +16,7 @@ from optparse import make_option
 from sentry.app import nodestore
 from sentry.models import (
     Activity, Event, EventMapping, Group, GroupRuleStatus, GroupTagValue,
-    LostPasswordHash, TagValue
+    LostPasswordHash, TagValue, GroupEmailThread,
 )
 from sentry.utils import db
 from sentry.utils.threadpool import ThreadPool
@@ -41,6 +41,7 @@ class Command(BaseCommand):
         (GroupTagValue, 'last_seen'),
         (Activity, 'datetime'),
         (TagValue, 'last_seen'),
+        (GroupEmailThread, 'date'),
     )
 
     GENERIC_DELETES = (
diff --git a/src/sentry/tasks/deletion.py b/src/sentry/tasks/deletion.py
index f96742dfcd..66ded76f8a 100644
--- a/src/sentry/tasks/deletion.py
+++ b/src/sentry/tasks/deletion.py
@@ -136,7 +136,7 @@ def delete_project(object_id, continuous=True, **kwargs):
 def delete_group(object_id, continuous=True, **kwargs):
     from sentry.models import (
         EventMapping, Group, GroupHash, GroupRuleStatus, GroupStatus,
-        GroupTagKey, GroupTagValue
+        GroupTagKey, GroupTagValue, GroupEmailThread,
     )
 
     try:
@@ -148,7 +148,8 @@ def delete_group(object_id, continuous=True, **kwargs):
         group.update(status=GroupStatus.DELETION_IN_PROGRESS)
 
     bulk_model_list = (
-        GroupHash, GroupRuleStatus, GroupTagValue, GroupTagKey, EventMapping
+        GroupHash, GroupRuleStatus, GroupTagValue, GroupTagKey,
+        EventMapping, GroupEmailThread,
     )
     for model in bulk_model_list:
         has_more = bulk_delete_objects(model, group_id=object_id, logger=logger)
