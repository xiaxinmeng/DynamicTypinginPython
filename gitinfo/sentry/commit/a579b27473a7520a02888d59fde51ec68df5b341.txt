commit a579b27473a7520a02888d59fde51ec68df5b341
Author: David Cramer <dcramer@gmail.com>
Date:   Tue May 31 16:30:57 2011 -0700

    Ensure we only call configure() if the settings have not yet been configured

diff --git a/sentry/scripts/runner.py b/sentry/scripts/runner.py
index 7a3871018d..8ed592af8a 100644
--- a/sentry/scripts/runner.py
+++ b/sentry/scripts/runner.py
@@ -30,8 +30,10 @@ def settings_from_file(filename, silent=False):
         raise
     
     tuple_settings = ("INSTALLED_APPS", "TEMPLATE_DIRS")
+    
+    if not django_settings.configured:
+        django_settings.configure()
 
-    django_settings.configure()
     for setting in dir(mod):
         if setting == setting.upper():
             setting_value = getattr(mod, setting)
