commit ae4e1034d05935c2ae48920dfb3792bb914ff3ef
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Tue Nov 29 16:39:20 2011 -0500

    Correctly use self.group_id instead of self.pk when reversing the message url

diff --git a/sentry/models.py b/sentry/models.py
index ab5897a418..b1669960d1 100644
--- a/sentry/models.py
+++ b/sentry/models.py
@@ -257,8 +257,8 @@ class Event(MessageBase):
     @models.permalink
     def get_absolute_url(self):
         if self.project_id:
-            return ('sentry-group-event', [], {'group_id': self.pk, 'event_id': self.pk, 'project_id': self.project_id})
-        return ('sentry-group-event', [], {'group_id': self.pk, 'event_id': self.pk})
+            return ('sentry-group-event', [], {'group_id': self.group_id, 'event_id': self.pk, 'project_id': self.project_id})
+        return ('sentry-group-event', [], {'group_id': self.group_id, 'event_id': self.pk})
 
     @cached_property
     def request(self):
