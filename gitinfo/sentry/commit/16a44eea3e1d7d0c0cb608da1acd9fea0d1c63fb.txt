commit 16a44eea3e1d7d0c0cb608da1acd9fea0d1c63fb
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Oct 9 01:31:29 2013 -0700

    Remove unused timezone helper

diff --git a/src/sentry/templates/sentry/layout.html b/src/sentry/templates/sentry/layout.html
index a8f58bb3df..28793774f1 100644
--- a/src/sentry/templates/sentry/layout.html
+++ b/src/sentry/templates/sentry/layout.html
@@ -28,7 +28,6 @@
             {% if request.LANGUAGE_CODE %}
                 moment.lang('{{ request.LANGUAGE_CODE }}');
             {% endif %}
-            app.config.timezoneOffset = '{% get_timezone %}';
             app.config.popupCss = '{% url 'sentry-media' "sentry" 'styles/popup.css' %}';
             app.config.mediaUrl = '{% url 'sentry-media' "sentry" '' %}';
             app.config.urlPrefix = {{ URL_PREFIX|to_json|safe }};
diff --git a/src/sentry/templatetags/sentry_helpers.py b/src/sentry/templatetags/sentry_helpers.py
index 82efb8367c..90ef6353a9 100644
--- a/src/sentry/templatetags/sentry_helpers.py
+++ b/src/sentry/templatetags/sentry_helpers.py
@@ -494,13 +494,3 @@ def localized_datetime(context, dt, format='DATETIME_FORMAT'):
     dt = dt.astimezone(timezone)
 
     return date(dt, format)
-
-
-@register.simple_tag(takes_context=True)
-def get_timezone(context):
-    request = context['request']
-    timezone = getattr(request, 'timezone', None)
-    if not timezone:
-        timezone = pytz.timezone(settings.TIME_ZONE)
-    now = datetime.datetime.now(timezone)
-    return now.strftime('%z')
