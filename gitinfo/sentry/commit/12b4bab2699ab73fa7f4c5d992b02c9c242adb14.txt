commit 12b4bab2699ab73fa7f4c5d992b02c9c242adb14
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Dec 18 15:16:49 2014 -0800

    Correct project example

diff --git a/src/sentry/templates/sentry/help/api_index.html b/src/sentry/templates/sentry/help/api_index.html
index f93a211500..bc261e344e 100644
--- a/src/sentry/templates/sentry/help/api_index.html
+++ b/src/sentry/templates/sentry/help/api_index.html
@@ -22,7 +22,9 @@
 
     <p>As an example, to get information about the project which your key is bound to, you might make a request like so:</p>
 
-    <pre>curl -u PUBLIC_KEY:SECRET_KEY /api/0/projects/</pre>
+    <pre>curl -u PUBLIC_KEY:SECRET_KEY /api/0/projects/{project_id}/</pre>
+
+    <p>Note: Currently requests are limited to project-scoped endpoints only.</p>
 
     <p>All API requests and responses are based on JSON, so you'll receive something similar to the following back:</p>
 
