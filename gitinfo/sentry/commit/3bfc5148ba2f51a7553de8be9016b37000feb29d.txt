commit 3bfc5148ba2f51a7553de8be9016b37000feb29d
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Sep 6 11:22:57 2011 -0700

    Correct server names in use of unique_servers

diff --git a/CHANGES b/CHANGES
index dc6c68c959..8edc09bbfa 100644
--- a/CHANGES
+++ b/CHANGES
@@ -1,3 +1,7 @@
+1.10.1
+
+* Fixed an issue that was causing servers to show logger names.
+
 1.10.0
 
 * You can now pass ``extra={'stack': True}`` to logging methods to capture
diff --git a/sentry/models.py b/sentry/models.py
index 8a53627b46..b82cdeeeb4 100644
--- a/sentry/models.py
+++ b/sentry/models.py
@@ -198,7 +198,7 @@ class GroupedMessage(MessageBase):
 
     @property
     def unique_servers(self):
-        return self.messagefiltervalue_set.filter(key='logger')\
+        return self.messagefiltervalue_set.filter(key='server_name')\
                    .values_list('value')\
                    .annotate(times_seen=Sum('times_seen'))\
                    .values_list('value', 'times_seen')\
diff --git a/sentry/utils/manager.py b/sentry/utils/manager.py
index 7dfffaba61..afb8dbfe78 100644
--- a/sentry/utils/manager.py
+++ b/sentry/utils/manager.py
@@ -178,7 +178,7 @@ class SentryManager(models.Manager):
             for key, value in (
                     ('server_name', server_name),
                     ('site', site),
-                    ('logger', logger_name)
+                    ('logger', logger_name),
                 ):
                 if not value:
                     continue
diff --git a/setup.py b/setup.py
index eee7681ea2..b38d59a118 100755
--- a/setup.py
+++ b/setup.py
@@ -38,7 +38,7 @@ if sys.version_info[:2] < (2, 5):
 
 setup(
     name='django-sentry',
-    version='1.10.0',
+    version='1.10.1',
     author='David Cramer',
     author_email='dcramer@gmail.com',
     url='http://github.com/dcramer/django-sentry',
