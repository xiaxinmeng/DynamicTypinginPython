commit 3f896c98e4c30f95be58804231d048679e0a7b8e
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Aug 12 16:49:11 2015 -0700

    Correct self assignment check

diff --git a/src/sentry/static/sentry/app/views/groupActivity.jsx b/src/sentry/static/sentry/app/views/groupActivity.jsx
index db2331b31a..3f2ade4edf 100644
--- a/src/sentry/static/sentry/app/views/groupActivity.jsx
+++ b/src/sentry/static/sentry/app/views/groupActivity.jsx
@@ -36,11 +36,11 @@ var formatActivity = function(item) {
       return "first saw this event";
     case "assigned":
       var assignee;
-      if (data.assignee == item.user) {
+      if (data.assignee === item.user.id) {
+        assignee = 'themselves';
+      } else {
         assignee = MemberListStore.getById(data.assignee);
         assignee = (assignee ? assignee.email : 'an unknown user');
-      } else {
-        assignee = 'themselves';
       }
       return `assigned this event to ${assignee}`;
     case "unassigned":
