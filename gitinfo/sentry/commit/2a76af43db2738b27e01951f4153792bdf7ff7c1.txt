commit 2a76af43db2738b27e01951f4153792bdf7ff7c1
Author: Evan Purkhiser <evanpurkhiser@gmail.com>
Date:   Wed May 23 10:03:11 2018 -0700

    fix(integrations): Remove unused get_config / is_configured (#8502)

diff --git a/src/sentry/api/endpoints/organization_config_integrations.py b/src/sentry/api/endpoints/organization_config_integrations.py
index 4339cdc01e..b060e6396f 100644
--- a/src/sentry/api/endpoints/organization_config_integrations.py
+++ b/src/sentry/api/endpoints/organization_config_integrations.py
@@ -26,7 +26,6 @@ class OrganizationConfigIntegrationsEndpoint(OrganizationEndpoint):
                     'key': provider.key,
                     'name': provider.name,
                     'metadata': metadata,
-                    'config': provider.get_config(),
                     'canAdd': provider.can_add,
                     'setupDialog': dict(
                         url='/organizations/{}/integrations/{}/setup/'.format(
diff --git a/src/sentry/integrations/base.py b/src/sentry/integrations/base.py
index 9525e0a667..608454af7f 100644
--- a/src/sentry/integrations/base.py
+++ b/src/sentry/integrations/base.py
@@ -100,31 +100,6 @@ class IntegrationProvider(PipelineProvider):
         """
         raise NotImplementedError
 
-    # XXX(dcramer): this is not yet exposed anywhere in the UI
-    def get_config(self):
-        """
-        Return a list of configuration attributes for this integration.
-
-        The results of this are stored per-organization per-integration.
-
-        >>> def get_config(self):
-        >>>     return [{
-        >>>         'name': 'instance',
-        >>>         'label': 'Instance',
-        >>>         'type': 'text',
-        >>>         'placeholder': 'e.g. https://example.atlassian.net',
-        >>>         'required': True,
-        >>>     }]
-        """
-        return []
-
-    def is_configured(self):
-        """
-        Return a boolean describing whether this integration should be made
-        available (e.g. per system-configuration).
-        """
-        return True
-
     def build_integration(self, state):
         """
         Given state captured during the setup pipeline, return a dictionary
diff --git a/src/sentry/integrations/manager.py b/src/sentry/integrations/manager.py
index 4ab4150053..481d3f0714 100644
--- a/src/sentry/integrations/manager.py
+++ b/src/sentry/integrations/manager.py
@@ -18,9 +18,7 @@ class IntegrationManager(object):
 
     def all(self):
         for key in six.iterkeys(self.__values):
-            provider = self.get(key)
-            if provider.is_configured():
-                yield provider
+            yield self.get(key)
 
     def get(self, key, **kwargs):
         try:
diff --git a/tests/sentry/api/endpoints/test_organization_config_integrations.py b/tests/sentry/api/endpoints/test_organization_config_integrations.py
index 191aabe455..785257d4bd 100644
--- a/tests/sentry/api/endpoints/test_organization_config_integrations.py
+++ b/tests/sentry/api/endpoints/test_organization_config_integrations.py
@@ -20,5 +20,4 @@ class OrganizationConfigIntegrationsTest(APITestCase):
         assert len(provider) == 1
         provider = provider[0]
         assert provider['name'] == 'Example'
-        assert provider['config']
         assert provider['setupDialog']['url']
