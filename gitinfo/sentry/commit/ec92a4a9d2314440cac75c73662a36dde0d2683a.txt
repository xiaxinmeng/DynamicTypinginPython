commit ec92a4a9d2314440cac75c73662a36dde0d2683a
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Sep 3 10:50:22 2015 -0700

    Add simple permalink helpers (fixes GH-1889)

diff --git a/src/sentry/static/sentry/app/components/events/eventDataSection.jsx b/src/sentry/static/sentry/app/components/events/eventDataSection.jsx
index 38b7216310..7622e5ccb5 100644
--- a/src/sentry/static/sentry/app/components/events/eventDataSection.jsx
+++ b/src/sentry/static/sentry/app/components/events/eventDataSection.jsx
@@ -19,9 +19,11 @@ var GroupEventDataSection = React.createClass({
   render: function() {
     return (
       <div className={(this.props.className || '') + ' box'}>
-        <a name={this.props.type} />
         {this.props.title &&
           <div className="box-header">
+            <a name={this.props.type} href={'#' + this.props.type} className="permalink">
+              <em className="icon-browser" />
+            </a>
             {this.props.wrapTitle ?
               <h3>{this.props.title}</h3>
             :
diff --git a/src/sentry/static/sentry/app/components/events/eventEntries.jsx b/src/sentry/static/sentry/app/components/events/eventEntries.jsx
index 11b1674c6c..f77035152f 100644
--- a/src/sentry/static/sentry/app/components/events/eventEntries.jsx
+++ b/src/sentry/static/sentry/app/components/events/eventEntries.jsx
@@ -81,7 +81,9 @@ var EventEntries = React.createClass({
     return (
       <div>
         {hasMultipartMessage(evt) &&
-          <EventMessage group={group} event={evt} />
+          <EventMessage
+            group={group}
+            event={evt} />
         }
         {!utils.objectIsEmpty(evt.errors) &&
           <EventErrors
@@ -89,18 +91,18 @@ var EventEntries = React.createClass({
             event={evt} />
         }
         <EventTags
-            group={group}
-            event={evt} />
+          group={group}
+          event={evt} />
         {entries}
         {!utils.objectIsEmpty(evt.context) &&
           <EventExtraData
-              group={group}
-              event={evt} />
+            group={group}
+            event={evt} />
         }
         {!utils.objectIsEmpty(evt.packages) &&
           <EventPackageData
-              group={group}
-              event={evt} />
+            group={group}
+            event={evt} />
         }
       </div>
     );
diff --git a/src/sentry/static/sentry/app/components/events/eventTags.jsx b/src/sentry/static/sentry/app/components/events/eventTags.jsx
index 4cb6bada7d..10e631c5de 100644
--- a/src/sentry/static/sentry/app/components/events/eventTags.jsx
+++ b/src/sentry/static/sentry/app/components/events/eventTags.jsx
@@ -1,6 +1,7 @@
 import React from "react";
 import PropTypes from "../../proptypes";
 import Router from "react-router";
+import EventDataSection from "./eventDataSection";
 import {isUrl} from "../../utils";
 
 var EventTags = React.createClass({
@@ -18,6 +19,8 @@ var EventTags = React.createClass({
       children = [],
       value;
 
+    var {group, event} = this.props;
+
     for (var key in this.props.event.tags) {
       value = this.props.event.tags[key];
 
@@ -43,16 +46,15 @@ var EventTags = React.createClass({
     }
 
     return (
-      <div id="tags" className="box">
-        <div className="box-header">
-          <h3>Tags</h3>
-        </div>
-        <div className="box-content with-padding">
-          <ul className="mini-tag-list">
-            {children}
-          </ul>
-        </div>
-      </div>
+      <EventDataSection
+          group={group}
+          event={event}
+          title="Tags"
+          type="tags">
+        <ul className="mini-tag-list">
+          {children}
+        </ul>
+      </EventDataSection>
     );
   }
 });
diff --git a/src/sentry/static/sentry/less/shared-components.less b/src/sentry/static/sentry/less/shared-components.less
index 5a73310562..27a7404ba0 100644
--- a/src/sentry/static/sentry/less/shared-components.less
+++ b/src/sentry/static/sentry/less/shared-components.less
@@ -1582,8 +1582,23 @@ pre.clippy {
     border-bottom: 1px solid @trim;
     padding-top: 9px;
     padding-bottom: 8px;
+    position: relative;
     .clearfix;
 
+    &:hover .permalink {
+      display: inline-block;
+    }
+
+    .permalink {
+      font-size: 10px;
+      line-height: 27px;
+      display: none;
+      position: absolute;
+      left: 7px;
+      top: 9px;
+      bottom: 8px;
+    }
+
     &.box-header-borderless {
       border: 0;
       font-size: 15px;
