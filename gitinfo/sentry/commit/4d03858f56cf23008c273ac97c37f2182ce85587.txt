commit 4d03858f56cf23008c273ac97c37f2182ce85587
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Apr 21 21:10:51 2014 -0700

    Handle unset resolved_at

diff --git a/src/sentry/utils/javascript.py b/src/sentry/utils/javascript.py
index 15a13af7b4..141ff95b36 100644
--- a/src/sentry/utils/javascript.py
+++ b/src/sentry/utils/javascript.py
@@ -147,6 +147,11 @@ class GroupTransformer(Transformer):
         else:
             status_label = 'unresolved'
 
+        version = obj.last_seen
+        if obj.resolved_at:
+            version = max(obj.resolved_at, obj.last_seen)
+        version = int(version.strftime('%s'))
+
         d = {
             'id': str(obj.id),
             'count': str(obj.times_seen),
@@ -169,7 +174,7 @@ class GroupTransformer(Transformer):
                 'name': obj.project.name,
                 'slug': obj.project.slug,
             },
-            'version': int(max([obj.resolved_at, obj.last_seen]).strftime('%s')),
+            'version': version,
         }
         if hasattr(obj, 'is_bookmarked'):
             d['isBookmarked'] = obj.is_bookmarked
