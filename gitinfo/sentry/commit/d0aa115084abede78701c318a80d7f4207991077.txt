commit d0aa115084abede78701c318a80d7f4207991077
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Jun 29 14:13:44 2015 -0700

    Add suffix prop to TimeSince

diff --git a/src/sentry/static/sentry/app/components/timeSince.jsx b/src/sentry/static/sentry/app/components/timeSince.jsx
index 853221eaab..d6df477b52 100644
--- a/src/sentry/static/sentry/app/components/timeSince.jsx
+++ b/src/sentry/static/sentry/app/components/timeSince.jsx
@@ -3,7 +3,14 @@ var moment = require("moment");
 
 var TimeSince = React.createClass({
   propTypes: {
-    date: React.PropTypes.any.isRequired
+    date: React.PropTypes.any.isRequired,
+    suffix: React.PropTypes.string
+  },
+
+  getDefaultProps() {
+    return {
+      suffix: 'ago'
+    };
   },
 
   componentDidMount() {
@@ -36,7 +43,7 @@ var TimeSince = React.createClass({
     }
 
     return (
-      <time>{moment.utc(date).fromNow()}</time>
+      <time>{moment.utc(date).fromNow(true)} {this.props.suffix || ''}</time>
     );
   }
 });
