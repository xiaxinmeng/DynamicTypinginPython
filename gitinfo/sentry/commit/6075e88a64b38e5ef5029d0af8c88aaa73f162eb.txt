commit 6075e88a64b38e5ef5029d0af8c88aaa73f162eb
Author: Ben Vinegar <ben@benv.ca>
Date:   Wed Sep 9 16:57:07 2015 -0700

    Package, extra data interfaces now use DefinitionList, are sorted (fixes #1899)

diff --git a/src/sentry/static/sentry/app/components/events/extraData.jsx b/src/sentry/static/sentry/app/components/events/extraData.jsx
index 427de71fce..29dfc96c7f 100644
--- a/src/sentry/static/sentry/app/components/events/extraData.jsx
+++ b/src/sentry/static/sentry/app/components/events/extraData.jsx
@@ -1,8 +1,11 @@
 import React from "react";
-import EventDataSection from "./eventDataSection";
-import ContextData from "../contextData";
+import _ from "underscore";
+
 import PropTypes from "../../proptypes";
 
+import EventDataSection from "./eventDataSection";
+import DefinitionList from "./interfaces/definitionList";
+
 var EventExtraData = React.createClass({
   propTypes: {
     group: PropTypes.Group.isRequired,
@@ -14,16 +17,9 @@ var EventExtraData = React.createClass({
   },
 
   render() {
-    var children = [];
-    var context = this.props.event.context;
-    for (var key in context) {
-      children.push(<dt key={'dt-' + key}>{key}</dt>);
-      children.push((
-        <dd key={'dd-' + key}>
-          <ContextData data={context[key]} />
-        </dd>
-      ));
-    }
+    let extraDataArray = _.chain(this.props.event.context)
+      .map((val, key) => [key, val])
+      .value();
 
     return (
       <EventDataSection
@@ -31,9 +27,7 @@ var EventExtraData = React.createClass({
           event={this.props.event}
           type="extra"
           title="Additional Data">
-        <dl className="vars">
-          {children}
-        </dl>
+          <DefinitionList data={extraDataArray} isContextData={true}/>
       </EventDataSection>
     );
   }
diff --git a/src/sentry/static/sentry/app/components/events/interfaces/definitionList.jsx b/src/sentry/static/sentry/app/components/events/interfaces/definitionList.jsx
index 404a8dc85b..f6787c2817 100644
--- a/src/sentry/static/sentry/app/components/events/interfaces/definitionList.jsx
+++ b/src/sentry/static/sentry/app/components/events/interfaces/definitionList.jsx
@@ -1,9 +1,18 @@
 import React from "react";
 import _ from "underscore";
 
+import ContextData from "../../contextData";
+
 var DefinitionList = React.createClass({
   propTypes: {
-    data: React.PropTypes.array.isRequired
+    data: React.PropTypes.array.isRequired,
+    isContextData: React.PropTypes.bool
+  },
+
+  getDefaultProps() {
+    return {
+      isContextData: false
+    }
   },
 
   render() {
@@ -12,8 +21,11 @@ var DefinitionList = React.createClass({
       <dl className="vars">
         {data.map(([key, value]) => {
           return [
-            <dt key={'dt-' + key }>{key}</dt>,
-            <dd key={'dd-' + key }><pre>{value || '&nbsp;'}</pre></dd>
+            <dt key={"dt-" + key}>{key}</dt>,
+            this.props.isContextData
+              ? <dd key={"dd-" + key}><ContextData data={value}/></dd>
+              : <dd key={"dd-" + key}><pre>{value || '&nbsp;'}</pre></dd>
+
           ];
         })}
       </dl>
