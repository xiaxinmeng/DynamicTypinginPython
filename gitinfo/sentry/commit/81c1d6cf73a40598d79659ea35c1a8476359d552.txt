commit 81c1d6cf73a40598d79659ea35c1a8476359d552
Author: ckj <chriskjennings@gmail.com>
Date:   Mon Jul 27 14:40:25 2015 -0400

    Try out dropdown for grouped / ungrouped events

diff --git a/src/sentry/static/sentry/app/views/stream/filterSelectLink.jsx b/src/sentry/static/sentry/app/views/stream/filterSelectLink.jsx
index b33b238108..03d844402a 100644
--- a/src/sentry/static/sentry/app/views/stream/filterSelectLink.jsx
+++ b/src/sentry/static/sentry/app/views/stream/filterSelectLink.jsx
@@ -19,16 +19,18 @@ var FilterSelectLink = React.createClass({
 
   render() {
     var className = this.props.extraClass;
-    className += ' btn btn-default';
+    var isActive;
 
     if (this.props.isActive) {
-      className += ' active';
+      isActive = 'active';
     }
 
     return (
-      <a className={className} onClick={this.onSelect}>
-        {this.props.label}
-      </a>
+      <li className={isActive}>
+        <a className={className} onClick={this.onSelect}>
+          {this.props.label}
+        </a>
+      </li>
     );
   }
 });
diff --git a/src/sentry/static/sentry/app/views/stream/filters.jsx b/src/sentry/static/sentry/app/views/stream/filters.jsx
index ed3c4e6cef..382f2084c7 100644
--- a/src/sentry/static/sentry/app/views/stream/filters.jsx
+++ b/src/sentry/static/sentry/app/views/stream/filters.jsx
@@ -66,11 +66,15 @@ var StreamFilters = React.createClass({
   render() {
     var activeButton = this.state.activeButton;
     return (
-      <div className="filter-nav">
+      <div className="filter-nav stream-header">
         <div className="row">
-          <div className="col-sm-4 primary-filters">
-            <div className="btn-group btn-group-justified">
-              <FilterSelectLink label="All Events"
+          <div className="col-sm-8 primary-filters">
+            <ul className="nav nav-tabs">
+              <li className="dropdown highlight">
+                <a href="#" className="dropdown-toggle">Events <span className="icon-arrow-down"></span></a>
+              </li>
+              <li className="divider" />
+              <FilterSelectLink label="All"
                 isActive={activeButton === 'all'}
                 onSelect={this.onFilterChange.bind(this, {})}
                 extraClass="btn-all-events" />
@@ -82,9 +86,16 @@ var StreamFilters = React.createClass({
                 isActive={activeButton === 'assigned'}
                 onSelect={this.onFilterChange.bind(this, {assigned: "1"})}
                 extraClass="btn-assigned" />
-            </div>
+              <li className="divider" />
+              <li className="dropdown highlight">
+                <a href="#" className="dropdown-toggle">Sort by: Last Seen  <span className="icon-arrow-down"></span></a>
+              </li>
+              <li className="dropdown highlight">
+                <a href="#" className="dropdown-toggle">All Time <span className="icon-arrow-down"></span></a>
+              </li>
+            </ul>
           </div>
-          <div className="col-sm-8">
+          <div className="col-sm-4">
             <SearchBar defaultQuery={this.props.defaultQuery}
               query={this.props.query}
               onQueryChange={this.props.onQueryChange}
diff --git a/src/sentry/static/sentry/less/shared-components.less b/src/sentry/static/sentry/less/shared-components.less
index 102559ab99..a2555ee93b 100644
--- a/src/sentry/static/sentry/less/shared-components.less
+++ b/src/sentry/static/sentry/less/shared-components.less
@@ -696,8 +696,9 @@ table.integrations {
   .icon-search {
     color: #8E8E97;
     position: absolute;
-    top: 15px;
-    left: 15px;
+    top: 12px;
+    left: 14px;
+    font-size: 14px;
   }
 
   .search-save-search {
@@ -727,16 +728,19 @@ table.integrations {
   }
 
   .search-input {
-    padding: 8px 76px 7px 40px;
-    font-size: 15px;
+    padding: 8px 76px 8px 37px;
+    margin-top: -8px;
+    font-size: 14px;
     background: #fff;
-    height: 44px;
+    // height: 40px;
     border-radius: 2px;
-    box-shadow: inset 0 1px 0 rgba(52, 60, 69, 0.04);
+    box-shadow: inset 0 1px 0 rgba(52, 60, 69, 0.03);
     .transition(none);
+    border-color: @trim;
 
     &:focus {
       border-radius: 2px 2px 0 0;
+      border-color: darken(@trim, 5);
     }
   }
 
@@ -754,7 +758,7 @@ table.integrations {
   border: 1px solid darken(@trim, 6) !important;
   border-radius: 0 0 4px 4px;
   position: absolute;
-  top: 43px;
+  top: 37px;
   right: 0;
   left: 0;
   background: #fff;
@@ -1898,6 +1902,8 @@ header ~ #messages {
       border: 0;
       background: none;
       color: lighten(@gray, 20);
+      min-width: 30px;
+      text-align: center;
 
       &:hover, &:active, &:focus {
         background: none;
@@ -1928,6 +1934,42 @@ header ~ #messages {
   }
 }
 
+.text-tabs {
+  a {
+    font-size: 20px !important;
+    padding: 0 !important;
+    line-height: 1 !important;
+  }
+
+  li {
+    padding-left: 17px;
+    margin-right: 15px;
+    position: relative;
+
+    &:before {
+      display: block;
+      content: "";
+      position: absolute;
+      top: 1px;
+      left: 0;
+      height: 18px;
+      width: 1px;
+      background: @trim;
+    }
+
+    &:first-child {
+      padding: 0 !important;
+      &:before {
+        display: none;
+      }
+    }
+  }
+
+  li.active a, li.active a:hover, li.active a:focus, li.active a:active {
+    border: 0;
+  }
+}
+
 /**
 * Project nav
 * ============================================================================
diff --git a/src/sentry/static/sentry/less/stream.less b/src/sentry/static/sentry/less/stream.less
index 5f8ff6a5dc..c11bf8b69a 100644
--- a/src/sentry/static/sentry/less/stream.less
+++ b/src/sentry/static/sentry/less/stream.less
@@ -439,8 +439,52 @@
 }
 
 .filter-nav {
-  margin-top: -10px;
-  margin-bottom: 18px;
+  margin-top: -3px;
+  margin-bottom: 20px;
+  border-bottom: 1px solid lighten(@trim, 5);
+
+  .nav-tabs {
+    margin: 0;
+
+    li {
+      > a {
+        padding-bottom: 26px;
+        margin: 0;
+
+        .icon-arrow-down {
+          margin-left: 2px;
+          position: relative;
+          top: 2px;
+          color: lighten(@gray, 25);
+        }
+      }
+
+      &.highlight {
+        a {
+          font-weight: 600;
+          color: @grayDark;
+          font-size: 95%;
+        }
+      }
+
+      &:first-child {
+        a {
+          font-size: 108% !important;
+        }
+      }
+
+      &.divider {
+        height: 22px;
+        border-left: 1px solid lighten(@trim, 4);
+        margin-right: 20px;
+        margin-left: 1px;
+      }
+    }
+  }
+
+  .text-tabs {
+    margin-bottom: 25px;
+  }
 }
 
 /**
