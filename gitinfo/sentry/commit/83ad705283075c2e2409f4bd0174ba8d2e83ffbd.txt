commit 83ad705283075c2e2409f4bd0174ba8d2e83ffbd
Author: Billy Vong <billyvg@users.noreply.github.com>
Date:   Tue Jul 7 08:45:01 2020 -0700

    test(acceptance): Inject listener to prevent browser "required" bubbles on form fields (#19729)
    
    This injects an event listener to disable the browser "required" popups for form fields to not cause flakes in snapshots

diff --git a/tests/acceptance/test_auth.py b/tests/acceptance/test_auth.py
index eb0a4ccf22..dfb84a992e 100644
--- a/tests/acceptance/test_auth.py
+++ b/tests/acceptance/test_auth.py
@@ -15,6 +15,9 @@ class AuthTest(AcceptanceTestCase):
         self.browser.snapshot(name="login")
 
     def test_no_credentials(self):
+        self.browser.driver.execute_script(
+            "document.addEventListener('invalid', function(e) { e.preventDefault(); }, true);"
+        )
         self.enter_auth("", "")
         self.browser.snapshot(name="login fields required")
 
