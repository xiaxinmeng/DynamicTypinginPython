commit 4e8f4f1a38a766b46517126bb869f03e060e5e5f
Author: DZ <david@nodnod.net>
Date:   Wed Sep 1 16:43:46 2010 -0700

    merged settings

diff --git a/example_project/settings.py b/example_project/settings.py
index 98956e4766..fa6c4c7926 100644
--- a/example_project/settings.py
+++ b/example_project/settings.py
@@ -18,8 +18,8 @@ sys.path.insert(0, os.path.abspath(os.path.join(PROJECT_ROOT, '..')))
 DATABASES = {
     'default': {
         'ENGINE': 'django.db.backends.postgresql_psycopg2', # Add 'postgresql_psycopg2', 'postgresql', 'mysql', 'sqlite3' or 'oracle'.
-        'NAME': 'sentry',                      # Or path to database file if using sqlite3.
-        'USER': 'postgres',                      # Not used with sqlite3.
+        'NAME': 'disqus',                      # Or path to database file if using sqlite3.
+        'USER': 'dzhou',                      # Not used with sqlite3.
         'PASSWORD': '',                  # Not used with sqlite3.
         'HOST': '',                      # Set to empty string for localhost. Not used with sqlite3.
         'PORT': '',                      # Set to empty string for default. Not used with sqlite3.
@@ -114,4 +114,4 @@ SENTRY_FILTERS = (
     'sentry.filters.LevelFilter',
     'sentry.filters.ServerNameFilter',
     'example_project.filters.IPFilter',
-)
\ No newline at end of file
+)
