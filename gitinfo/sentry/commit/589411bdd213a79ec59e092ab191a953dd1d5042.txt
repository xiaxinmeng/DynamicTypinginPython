commit 589411bdd213a79ec59e092ab191a953dd1d5042
Author: David Cramer <dcramer@gmail.com>
Date:   Thu May 10 00:44:03 2012 -0500

    Remove url as a FilterValue

diff --git a/sentry/manager.py b/sentry/manager.py
index f33f7afac6..dfd339fcf4 100644
--- a/sentry/manager.py
+++ b/sentry/manager.py
@@ -548,16 +548,9 @@ class GroupManager(BaseManager, ChartMixin):
             defaults=update_kwargs
         )
 
-        http = event.interfaces.get('sentry.interfaces.Http')
-        if http:
-            url = http.url
-        else:
-            url = None
-
         for key, value in (
                 ('server_name', event.server_name),
                 ('site', event.site),
-                ('url', url),
                 ('logger', event.logger),
             ):
             if not value:
