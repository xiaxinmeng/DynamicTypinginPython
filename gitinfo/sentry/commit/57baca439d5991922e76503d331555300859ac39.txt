commit 57baca439d5991922e76503d331555300859ac39
Author: Markus Unterwaditzer <markus@unterwaditzer.net>
Date:   Tue Mar 31 21:43:17 2020 +0200

    fix: Remove broken get_from_cache (#17903)
    
    See #16512
    
    Fix SENTRY-FPR

diff --git a/src/sentry/runner/commands/cleanup.py b/src/sentry/runner/commands/cleanup.py
index 421ab64777..990a48be55 100644
--- a/src/sentry/runner/commands/cleanup.py
+++ b/src/sentry/runner/commands/cleanup.py
@@ -25,7 +25,7 @@ def get_project(value):
         if "/" not in value:
             return None
         org, proj = value.split("/", 1)
-        return Project.objects.get_from_cache(organization__slug=org, slug=proj).id
+        return Project.objects.get(organization__slug=org, slug=proj).id
     except Project.DoesNotExist:
         return None
 
