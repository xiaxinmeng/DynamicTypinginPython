commit 36753c88a61233dffb2508722200630c0d47f0f1
Author: Matej Minar <matej.minar@sentry.io>
Date:   Tue Jan 28 14:31:31 2020 +0100

    ref(ts): Convert <ErrorLevel> to typescript (#16640)

diff --git a/src/sentry/static/sentry/app/components/events/errorLevel.jsx b/src/sentry/static/sentry/app/components/events/errorLevel.tsx
similarity index 75%
rename from src/sentry/static/sentry/app/components/events/errorLevel.jsx
rename to src/sentry/static/sentry/app/components/events/errorLevel.tsx
index 11b39c6660..22edf45de7 100644
--- a/src/sentry/static/sentry/app/components/events/errorLevel.jsx
+++ b/src/sentry/static/sentry/app/components/events/errorLevel.tsx
@@ -1,8 +1,9 @@
 import styled from '@emotion/styled';
+import {Level} from 'app/types';
 
 const DEFAULT_SIZE = '13px';
 
-function getLevelColor({level, theme}) {
+function getLevelColor({level = '', theme}) {
   const COLORS = {
     error: theme.orange,
     info: theme.blue,
@@ -14,7 +15,12 @@ function getLevelColor({level, theme}) {
   return `background-color: ${COLORS[level] || theme.orange};`;
 }
 
-const ErrorLevel = styled('span')`
+type Props = {
+  size?: string;
+  level?: Level;
+};
+
+const ErrorLevel = styled('span')<Props>`
   padding: 0;
   position: relative;
   width: ${p => p.size || DEFAULT_SIZE};
diff --git a/src/sentry/static/sentry/app/components/events/eventMessage.tsx b/src/sentry/static/sentry/app/components/events/eventMessage.tsx
index bf05ad1547..2cca9cf13d 100644
--- a/src/sentry/static/sentry/app/components/events/eventMessage.tsx
+++ b/src/sentry/static/sentry/app/components/events/eventMessage.tsx
@@ -2,12 +2,13 @@ import PropTypes from 'prop-types';
 import React from 'react';
 import styled from '@emotion/styled';
 
+import {Level} from 'app/types';
 import ErrorLevel from 'app/components/events/errorLevel';
 import overflowEllipsis from 'app/styles/overflowEllipsis';
 import space from 'app/styles/space';
 
 type Props = {
-  level?: 'error' | 'fatal' | 'info' | 'warning' | 'sample';
+  level?: Level;
   levelIndicatorSize?: string;
   message?: React.ReactNode;
   annotations?: React.ReactNode;
diff --git a/src/sentry/static/sentry/app/types/index.tsx b/src/sentry/static/sentry/app/types/index.tsx
index c71f2921d2..bbe05999eb 100644
--- a/src/sentry/static/sentry/app/types/index.tsx
+++ b/src/sentry/static/sentry/app/types/index.tsx
@@ -757,3 +757,5 @@ export type Tag = {
   totalValues?: number;
   predefined?: boolean;
 };
+
+export type Level = 'error' | 'fatal' | 'info' | 'warning' | 'sample';
