commit bfd786dad5b19d46c8ce956b904fe23c378c9202
Author: James Cunningham <cunninghamjt09@gmail.com>
Date:   Fri Aug 26 10:12:32 2016 -1000

    Patch usage of period in key. (#4020)
    
    * Patch usage of period in key.
    
    * Switch back to old keys.

diff --git a/src/sentry/tasks/merge.py b/src/sentry/tasks/merge.py
index 0588d6c51a..04c473dd20 100644
--- a/src/sentry/tasks/merge.py
+++ b/src/sentry/tasks/merge.py
@@ -168,8 +168,8 @@ def merge_objects(models, group, new_group, limit=1000,
     for model in models:
         if logger is not None:
             logger.info('%s.merge' % model.__name__.lower(), extra={
-                'group_id.old': group.id,
-                'group_id.new': new_group.id
+                'new_group_id': new_group.id,
+                'old_group_id': group.id,
             })
         all_fields = model._meta.get_all_field_names()
         has_group = 'group' in all_fields
