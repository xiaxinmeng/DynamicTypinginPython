commit 8e7e56deb51351b37b683e4ec12f7b06b499bb73
Author: Max Bittker <maxbittker@gmail.com>
Date:   Mon Nov 7 15:34:30 2016 -0800

    ensure loading indicator gets removed (#4491)

diff --git a/src/sentry/static/sentry/app/components/activity/noteInput.jsx b/src/sentry/static/sentry/app/components/activity/noteInput.jsx
index b158892428..cacb6a3cbb 100644
--- a/src/sentry/static/sentry/app/components/activity/noteInput.jsx
+++ b/src/sentry/static/sentry/app/components/activity/noteInput.jsx
@@ -151,6 +151,7 @@ const NoteInput = React.createClass({
           error: true,
           errorJSON: error.responseJSON || makeDefaultErrorJson()
         });
+        IndicatorStore.remove(loadingIndicator);
       },
       success: (data) => {
         this.setState({
@@ -159,10 +160,8 @@ const NoteInput = React.createClass({
           loading: false
         });
         GroupStore.updateActivity(group.id, item.id, {text: this.state.value});
-        this.finish();
-      },
-      complete: () => {
         IndicatorStore.remove(loadingIndicator);
+        this.finish();
       }
     });
   },
