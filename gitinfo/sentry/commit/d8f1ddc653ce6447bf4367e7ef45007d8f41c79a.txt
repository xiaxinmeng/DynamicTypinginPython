commit d8f1ddc653ce6447bf4367e7ef45007d8f41c79a
Author: Armin Ronacher <armin.ronacher@active-4.com>
Date:   Thu Nov 26 20:04:56 2015 +0100

    Only eslint files that changed

diff --git a/src/sentry/lint/engine.py b/src/sentry/lint/engine.py
index 38f574085f..22a265b1f7 100644
--- a/src/sentry/lint/engine.py
+++ b/src/sentry/lint/engine.py
@@ -3,6 +3,7 @@ from __future__ import absolute_import
 import os
 import pep8
 import sys
+from subprocess import Popen
 
 os.environ['PYFLAKES_NODOCTEST'] = '1'
 
@@ -42,8 +43,9 @@ def js_lint(file_list):
         return False
     has_errors = False
     file_list = filter(lambda x: x.endswith(('.js', '.jsx')), file_list)
-    if file_list and os.system('npm run-script lint'):
-        has_errors = True
+    if file_list:
+        status = Popen(['node_modules/.bin/eslint'] + list(file_list)).wait()
+        has_errors = status != 0
 
     return has_errors
 
