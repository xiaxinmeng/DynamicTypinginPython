commit 3fde9cc5c1166715b2ac50b477a18ca780a9343f
Author: James Cunningham <cunninghamjt09@gmail.com>
Date:   Thu Nov 15 12:58:40 2018 -0800

    fix(cleanup); iterate of 100k instead of 1mil. (#10609)

diff --git a/src/sentry/db/deletion.py b/src/sentry/db/deletion.py
index e6c47abc4d..e281d98402 100644
--- a/src/sentry/db/deletion.py
+++ b/src/sentry/db/deletion.py
@@ -121,7 +121,7 @@ class BulkDeleteQuery(object):
         for chunk in g:
             yield chunk
 
-    def iterator_postgres(self, chunk_size, batch_size=1000000):
+    def iterator_postgres(self, chunk_size, batch_size=100000):
         assert self.days is not None
         assert self.dtfield is not None and self.dtfield == self.order_by
 
