commit f2954ca6c14bf94accf875d0f9e0b4c941782d1f
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Feb 13 12:42:34 2012 -0800

    Ensure we return the cls on register/unregister hooks

diff --git a/sentry/plugins/base.py b/sentry/plugins/base.py
index 4becb812e8..a58a1a4c8a 100644
--- a/sentry/plugins/base.py
+++ b/sentry/plugins/base.py
@@ -54,9 +54,11 @@ class PluginManager(InstanceManager):
 
     def register(self, cls):
         self.add('%s.%s' % (cls.__module__, cls.__name__))
+        return cls
 
     def unregister(self, cls):
         self.remove('%s.%s' % (cls.__module__, cls.__name__))
+        return cls
 
 plugins = PluginManager()
 register = plugins.register
