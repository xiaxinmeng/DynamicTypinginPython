commit 60a5278af1465e0fd039fd685def816fef337efc
Author: Evan Purkhiser <evanpurkhiser@gmail.com>
Date:   Tue Nov 12 16:20:11 2019 -0800

    chore(ts): Convert SidebarPanelItem (#15510)

diff --git a/src/sentry/static/sentry/app/components/sidebar/sidebarPanelItem.jsx b/src/sentry/static/sentry/app/components/sidebar/sidebarPanelItem.jsx
deleted file mode 100644
index 1bae589120..0000000000
--- a/src/sentry/static/sentry/app/components/sidebar/sidebarPanelItem.jsx
+++ /dev/null
@@ -1,74 +0,0 @@
-import PropTypes from 'prop-types';
-import React from 'react';
-import styled from 'react-emotion';
-
-import {t} from '../../locale';
-import ExternalLink from '../links/externalLink';
-
-class SidebarPanelItem extends React.Component {
-  static propTypes = {
-    title: PropTypes.string,
-    image: PropTypes.string,
-    message: PropTypes.any,
-    link: PropTypes.string,
-    hasSeen: PropTypes.bool,
-    cta: PropTypes.string,
-  };
-
-  render() {
-    const {hasSeen, title, image, message, link} = this.props;
-    const cta = this.props.cta || t('Read More');
-
-    return (
-      <SidebarPanelItemRoot>
-        {title && <Title hasSeen={hasSeen}>{title}</Title>}
-        {image && (
-          <div className="image">
-            <img src={image} />
-          </div>
-        )}
-        {message && <Message>{message}</Message>}
-
-        {link && (
-          <Text>
-            <ExternalLink href={link}>{cta}</ExternalLink>
-          </Text>
-        )}
-      </SidebarPanelItemRoot>
-    );
-  }
-}
-
-export default SidebarPanelItem;
-
-const SidebarPanelItemRoot = styled('div')`
-  padding: 15px 20px;
-  line-height: 1.2;
-  border-bottom: 1px solid ${p => p.theme.borderLight};
-  background: ${p => p.theme.white};
-  box-shadow: 0 1px 2px rgba(0, 0, 0, 0.06);
-  font-size: 14px;
-`;
-
-const Title = styled(({hasSeen, ...props}) => <div {...props} />)`
-  font-size: 15px;
-  margin-bottom: 5px;
-  color: ${p => p.theme.gray5};
-  ${p => !p.hasSeen && 'font-weight: 600;'};
-
-  .culprit {
-    font-weight: normal;
-  }
-`;
-
-const Text = styled('div')`
-  margin-bottom: 5px;
-
-  &:last-child {
-    margin-bottom: 0;
-  }
-`;
-
-const Message = styled(Text)`
-  color: ${p => p.theme.gray3};
-`;
diff --git a/src/sentry/static/sentry/app/components/sidebar/sidebarPanelItem.tsx b/src/sentry/static/sentry/app/components/sidebar/sidebarPanelItem.tsx
new file mode 100644
index 0000000000..e1f152eff0
--- /dev/null
+++ b/src/sentry/static/sentry/app/components/sidebar/sidebarPanelItem.tsx
@@ -0,0 +1,89 @@
+import PropTypes from 'prop-types';
+import React from 'react';
+import styled from 'react-emotion';
+
+import {t} from '../../locale';
+import ExternalLink from '../links/externalLink';
+
+type Props = {
+  hasSeen?: boolean;
+  title?: string;
+  image?: string;
+  message?: React.ReactNode;
+  link?: string;
+  cta?: string;
+};
+
+const SidebarPanelItem: React.FC<Props> = ({
+  hasSeen,
+  title,
+  image,
+  message,
+  link,
+  cta,
+}) => (
+  <SidebarPanelItemRoot>
+    {title && <Title hasSeen={hasSeen}>{title}</Title>}
+    {image && (
+      <ImageBox>
+        <img src={image} />
+      </ImageBox>
+    )}
+    {message && <Message>{message}</Message>}
+
+    {link && (
+      <Text>
+        <ExternalLink href={link}>{cta || t('Read More')}</ExternalLink>
+      </Text>
+    )}
+  </SidebarPanelItemRoot>
+);
+
+SidebarPanelItem.propTypes = {
+  title: PropTypes.string,
+  image: PropTypes.string,
+  message: PropTypes.node,
+  link: PropTypes.string,
+  hasSeen: PropTypes.bool,
+  cta: PropTypes.string,
+};
+
+export default SidebarPanelItem;
+
+const SidebarPanelItemRoot = styled('div')`
+  padding: 15px 20px;
+  line-height: 1.2;
+  border-bottom: 1px solid ${p => p.theme.borderLight};
+  background: ${p => p.theme.white};
+  box-shadow: 0 1px 2px rgba(0, 0, 0, 0.06);
+  font-size: 14px;
+`;
+
+const ImageBox = styled('div')`
+  border: 1px solid #e1e4e5;
+  padding: 15px;
+  border-radius: 2px;
+`;
+
+const Title = styled('div')<Pick<Props, 'hasSeen'>>`
+  font-size: 15px;
+  margin-bottom: 5px;
+  color: ${p => p.theme.gray5};
+  ${p => !p.hasSeen && 'font-weight: 600;'};
+
+  .culprit {
+    font-weight: normal;
+  }
+`;
+
+const Text = styled('div')`
+  margin-bottom: 5px;
+
+  &:last-child {
+    margin-bottom: 0;
+  }
+`;
+
+const Message = styled(Text)`
+  color: ${p => p.theme.gray3};
+`;
diff --git a/src/sentry/static/sentry/less/misc.less b/src/sentry/static/sentry/less/misc.less
index aed74f7d9b..f9856e8d10 100644
--- a/src/sentry/static/sentry/less/misc.less
+++ b/src/sentry/static/sentry/less/misc.less
@@ -2,12 +2,6 @@ img {
   max-width: 100%;
 }
 
-.image {
-  border: 1px solid #e1e4e5;
-  padding: 15px;
-  border-radius: 2px;
-}
-
 .truncate {
   max-width: 100%;
   display: block;
diff --git a/tests/js/spec/components/sidebar/__snapshots__/index.spec.jsx.snap b/tests/js/spec/components/sidebar/__snapshots__/index.spec.jsx.snap
index 1aab912142..d619d860bf 100644
--- a/tests/js/spec/components/sidebar/__snapshots__/index.spec.jsx.snap
+++ b/tests/js/spec/components/sidebar/__snapshots__/index.spec.jsx.snap
@@ -1149,32 +1149,27 @@ exports[`Sidebar SidebarPanel can display Broadcasts panel and mark as seen 1`]
 >
   <SidebarPanelItemRoot>
     <div
-      className="css-11zw7jo-SidebarPanelItemRoot er110870"
+      className="css-11zw7jo-SidebarPanelItemRoot ekutvbl0"
     >
       <Title
         hasSeen={false}
       >
-        <Component
-          className="css-hs42c5-Title er110871"
-          hasSeen={false}
+        <div
+          className="css-hs42c5-Title ekutvbl2"
         >
-          <div
-            className="css-hs42c5-Title er110871"
-          >
-            Learn about Source Maps
-          </div>
-        </Component>
+          Learn about Source Maps
+        </div>
       </Title>
       <Message>
         <div
-          className="css-1rszf37-Text-Message er110873"
+          className="css-1rszf37-Text-Message ekutvbl4"
         >
           Source maps are JSON files that contain information on how to map your transpiled source code back to their original source.
         </div>
       </Message>
       <Text>
         <div
-          className="css-7bhqyd-Text er110872"
+          className="css-7bhqyd-Text ekutvbl3"
         >
           <ForwardRef
             href="https://docs.sentry.io/hosted/clients/javascript/sourcemaps/#uploading-source-maps-to-sentry"
