commit 81472a306cfe453ef04f55c0cb35623bc879d952
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Jun 4 20:03:31 2014 -0700

    Default back to single queue per

diff --git a/src/sentry/conf/server.py b/src/sentry/conf/server.py
index ce00c27eff..6eba675d29 100644
--- a/src/sentry/conf/server.py
+++ b/src/sentry/conf/server.py
@@ -295,10 +295,11 @@ CELERY_ROUTES = ('sentry.queue.routers.SplitQueueRouter',)
 
 
 def create_partitioned_queues(name):
-    for num in range(4):
+    exchange = Exchange(name, type='direct')
+    for num in range(1):
         CELERY_QUEUES.append(Queue(
             '{0}-{1}'.format(name, num),
-            exchange=Exchange(name, type='direct')
+            exchange=exchange,
         ))
 
 create_partitioned_queues('counters')
