commit 28d9719896bdae7f1f85fc0b3a0c64d1383e1498
Author: Armin Ronacher <armin.ronacher@active-4.com>
Date:   Mon Sep 26 17:51:47 2016 +0200

    Temporarily undo the address normalization because of limitations

diff --git a/src/sentry/interfaces/stacktrace.py b/src/sentry/interfaces/stacktrace.py
index e3af52f1bd..3ac519510f 100644
--- a/src/sentry/interfaces/stacktrace.py
+++ b/src/sentry/interfaces/stacktrace.py
@@ -56,7 +56,10 @@ def to_hex_addr(addr):
         return '0x%x' % addr
     elif isinstance(addr, six.string_types):
         if addr[:2] == '0x':
-            addr = int(addr[2:], 16)
+            # XXX: More correct would be this but we currently can't do
+            # that yet.
+            # addr = int(addr[2:], 16)
+            return addr
         return '0x%x' % int(addr)
     raise ValueError('Unsupported address format %r' % (addr,))
 
