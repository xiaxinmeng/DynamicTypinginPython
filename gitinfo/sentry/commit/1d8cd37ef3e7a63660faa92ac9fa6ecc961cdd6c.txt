commit 1d8cd37ef3e7a63660faa92ac9fa6ecc961cdd6c
Author: Chris Jennings <chriskjennings@gmail.com>
Date:   Tue Aug 31 16:32:25 2010 -0700

    Added new layout structure to group template

diff --git a/sentry/templates/sentry/group.html b/sentry/templates/sentry/group.html
index 641469a44b..51a4565da0 100644
--- a/sentry/templates/sentry/group.html
+++ b/sentry/templates/sentry/group.html
@@ -5,62 +5,70 @@
 {% block title %}{{ group.error }} | Django Sentry{% endblock %}
 
 {% block body %}
-	<div id="header"> 
-		<h1><a href="{% url sentry %}">Django Sentry</a></h1> 
-		<h2>Message Details</h2>
+	<div id="header">
+		<div class="container">
+			<h1><a href="{% url sentry %}">Django Sentry</a></h1> 
+			<h2>Message Details</h2>
+		</div>
 	</div> 
-
-	<div id="sidebar">
-		<h2>Last 24 Hours</h2>
+	<div id="content">
+		<div class="container">
+			<div id="sidebar">
+				<h2>Last 24 Hours</h2>
 		
-		<img src="{{ chart_url }}" class="chart" alt="chart"/>
+				<img src="{{ chart_url }}" class="chart" alt="chart"/>
 		
-		<h2>Meta</h2>
+				<h2>Meta</h2>
 		
-		<dl class="flat">
-			<dt>First Seen:</dt>
-			<dd>{{ group.first_seen }}</dd>
-			<dt>Last Seen:</dt>
-			<dd>{{ group.last_seen }}</dd>
-			<dt>Status:</dt>
-			<dd>{{ group.get_status_display|title }}</dd>
-		</dl>
+				<dl class="flat">
+					<dt>First Seen:</dt>
+					<dd>{{ group.first_seen }}</dd>
+					<dt>Last Seen:</dt>
+					<dd>{{ group.last_seen }}</dd>
+					<dt>Status:</dt>
+					<dd>{{ group.get_status_display|title }}</dd>
+				</dl>
 		
-		<h2>URLs</h2>
+				<h2>URLs</h2>
 		
-		<ul class="url-list">
-			{% for link, priority in unique_urls|with_priority:"times_seen" %}
-				<li class="priority-{{ priority }}"><span class="count">{{ link.times_seen }}</span> <a href="{{ link.url }}">{{ link.url }}</a></li>
-			{% endfor %}
-		</ul>
+				<ul class="url-list">
+					{% for link, priority in unique_urls|with_priority:"times_seen" %}
+						<li class="priority-{{ priority }}"><span class="count">{{ link.times_seen }}</span> <a href="{{ link.url }}">{{ link.url }}</a></li>
+					{% endfor %}
+				</ul>
 
-		<h2>Servers</h2>
+				<h2>Servers</h2>
 		
-		<ul class="server-list">
-			{% for server, priority in unique_servers|with_priority:"times_seen" %}
-				<li class="priority-{{ priority }}"><span class="count">{{ server.times_seen }}</span> {{ server.server_name }}</li>
-			{% endfor %}
-		</ul>
-	</div>
+				<ul class="server-list">
+					{% for server, priority in unique_servers|with_priority:"times_seen" %}
+						<li class="priority-{{ priority }}"><span class="count">{{ server.times_seen }}</span> {{ server.server_name }}</li>
+					{% endfor %}
+				</ul>
+			</div>
 
-	<div id="body" class="with-sidebar">
-		<ul class="messages">
-			<li class="{% cycle 'row1' 'row2' %} level-{{ group.level }} active">
-				<span class="count">{{ group.times_seen }}</span>
-				<h3>{% if group.view %}{{ group.view }}{% else %}{{ group.message }}{% endif %}</h3>
-				<p class="message">{{ group.error }}</p>
-				<pre class="traceback">{{ group.shortened_traceback }}</pre>
-			</li>
-		</ul>
+			<div id="body" class="with-sidebar">
+				<ul class="messages">
+					<li class="{% cycle 'row1' 'row2' %} level-{{ group.level }} active">
+						<span class="count">{{ group.times_seen }}</span>
+						<h3>{% if group.view %}{{ group.view }}{% else %}{{ group.message }}{% endif %}</h3>
+						<p class="message">{{ group.error }}</p>
+						<pre class="traceback">{{ group.shortened_traceback }}</pre>
+					</li>
+				</ul>
 		
-		{% if traceback %}
+				{% if traceback %}
 
-			<h2>Sample Traceback</h2>
+					<h2>Sample Traceback</h2>
 
-			{{ traceback }}
+					{{ traceback }}
 
-		{% endif %}
+				{% endif %}
+			</div>
+		</div>
 	</div>
-
-	<div id="footer">Powered by sexy Django magic.</div> 
+	<div id="footer">
+		<div class="container">
+			Powered by sexy Django magic.
+		</div>	
+	</div> 
 {% endblock %}
\ No newline at end of file
