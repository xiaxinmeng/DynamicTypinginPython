commit ed691a627f1504f147029bba7c3743fe2dfe019c
Author: John-Scott Atlakson <john.scott.atlakson@gmail.com>
Date:   Sun Jun 13 15:08:22 2010 -0400

    Added missing colon which broke rst formatting in README

diff --git a/README.rst b/README.rst
index e226913b38..832d0e849c 100644
--- a/README.rst
+++ b/README.rst
@@ -123,7 +123,7 @@ A good example use case for this, is if you want to write to something like a sy
 Integration with ``logging``
 ############################
 
-django-db-log supports the ability to directly tie into the ``logging`` module. To use it simply add ``DBLogHandler`` to your logger:
+django-db-log supports the ability to directly tie into the ``logging`` module. To use it simply add ``DBLogHandler`` to your logger::
 
 	import logging
 	from djangodblog.handlers import DBLogHandler
