commit bf4ac6467449a146b72ee669f81d23304b0181a4
Author: Markus Unterwaditzer <markus@unterwaditzer.net>
Date:   Tue Mar 10 08:34:22 2020 +0100

    ref: Make Advanced Data Scrubber Settings monospaced and full width (#17530)
    
    * ref: Make Advanced Data Scrubber Settings monospaced and full width
    
    * fix: Fix indentation
    
    * fix typedef

diff --git a/src/sentry/static/sentry/app/data/forms/organizationSecurityAndPrivacy.tsx b/src/sentry/static/sentry/app/data/forms/organizationSecurityAndPrivacy.tsx
index 074b3d436c..cd7b9fdc18 100644
--- a/src/sentry/static/sentry/app/data/forms/organizationSecurityAndPrivacy.tsx
+++ b/src/sentry/static/sentry/app/data/forms/organizationSecurityAndPrivacy.tsx
@@ -129,7 +129,9 @@ const organizationSecurityAndPrivacy: Array<JsonFormObject> = [
         label: t('Advanced datascrubber configuration'),
         placeholder: t('Paste a JSON configuration here.'),
         multiline: true,
+        monospace: true,
         autosize: true,
+        inline: false,
         maxRows: 20,
         help: tct(
           'Advanced JSON-based configuration for datascrubbing. Applied in addition to the settings above. This list of rules can be extended on a per-project level, but never overridden. [learn_more:Learn more]',
diff --git a/src/sentry/static/sentry/app/data/forms/projectGeneralSettings.jsx b/src/sentry/static/sentry/app/data/forms/projectGeneralSettings.jsx
index 03a6702418..d8638c1d80 100644
--- a/src/sentry/static/sentry/app/data/forms/projectGeneralSettings.jsx
+++ b/src/sentry/static/sentry/app/data/forms/projectGeneralSettings.jsx
@@ -342,7 +342,9 @@ export const fields = {
     label: t('Advanced datascrubber configuration'),
     placeholder: t('Paste a JSON configuration here.'),
     multiline: true,
+    monospace: true,
     autosize: true,
+    inline: false,
     maxRows: 20,
     help: tct(
       'Advanced JSON-based configuration for datascrubbing. Applied in addition to the settings above. [learn_more:Learn more]',
diff --git a/src/sentry/static/sentry/app/views/settings/components/forms/type.tsx b/src/sentry/static/sentry/app/views/settings/components/forms/type.tsx
index d00235e826..660eeb7bcd 100644
--- a/src/sentry/static/sentry/app/views/settings/components/forms/type.tsx
+++ b/src/sentry/static/sentry/app/views/settings/components/forms/type.tsx
@@ -32,6 +32,7 @@ type BaseField = {
   required?: boolean;
   placeholder?: string | (() => string);
   multiline?: boolean;
+  monospace?: boolean;
   visible?: boolean | ((props: any) => boolean);
   disabled?: boolean | ((props: any) => boolean);
   disabledReason?: string;
