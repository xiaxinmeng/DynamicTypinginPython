commit cfd3760a7f23b439edfed1eb2bcaa6c88aa13404
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Mon Apr 25 20:21:21 2016 -0700

    devserver should use `sentry run web` instead of `sentry start`

diff --git a/src/sentry/runner/commands/devserver.py b/src/sentry/runner/commands/devserver.py
index 1be51c9036..4ae18d867d 100644
--- a/src/sentry/runner/commands/devserver.py
+++ b/src/sentry/runner/commands/devserver.py
@@ -79,7 +79,7 @@ def devserver(reload, watchers, workers, bind):
     # This sets all the appropriate uwsgi env vars, etc
     server.prepare_environment()
     daemons += [
-        ('server', ['sentry', 'start']),
+        ('server', ['sentry', 'run', 'web']),
     ]
 
     cwd = os.path.realpath(os.path.join(settings.PROJECT_ROOT, os.pardir, os.pardir))
