commit d4b89b1c704d01f210ca3561eef303941a223028
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Sep 30 19:19:55 2014 -0700

    Cache GroupMeta
    
    Conflicts:
            src/sentry/api/endpoints/project_group_index.py
            src/sentry/templates/sentry/groups/group_list.html
            src/sentry/web/api.py
            src/sentry/web/frontend/groups.py
            src/sentry/web/urls.py
            tests/sentry/web/frontend/groups/tests.py

diff --git a/src/sentry/api/endpoints/project_group_index.py b/src/sentry/api/endpoints/project_group_index.py
index ab893a4683..0a9ece01a6 100644
--- a/src/sentry/api/endpoints/project_group_index.py
+++ b/src/sentry/api/endpoints/project_group_index.py
@@ -13,7 +13,7 @@ from sentry.api.serializers import serialize
 from sentry.constants import DEFAULT_SORT_OPTION, STATUS_CHOICES
 from sentry.db.models.query import create_or_update
 from sentry.models import (
-    Activity, Group, GroupBookmark, GroupStatus, Project, TagKey
+    Activity, Group, GroupBookmark, GroupMeta, GroupStatus, Project, TagKey
 )
 from sentry.search.utils import parse_query
 from sentry.utils.dates import parse_date
@@ -105,9 +105,11 @@ class ProjectGroupIndexEndpoint(Endpoint):
         if query is not None:
             query_kwargs.update(parse_query(query, request.user))
 
-        results = search.query(**query_kwargs)
+        results = list(search.query(**query_kwargs))
 
-        return Response(serialize(list(results), request.user))
+        GroupMeta.objects.populate_cache(results)
+
+        return Response(serialize(results, request.user))
 
     def put(self, request, project_id):
         """
