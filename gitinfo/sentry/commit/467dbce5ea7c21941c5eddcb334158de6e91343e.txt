commit 467dbce5ea7c21941c5eddcb334158de6e91343e
Author: Chris Jennings <chriskjennings@gmail.com>
Date:   Wed Apr 3 18:08:01 2019 -0700

    fix(ui): truncate long release strings (#12626)
    
    * fix(ui): truncate long release strings
    
    * Try a classname-based approach instead

diff --git a/src/sentry/static/sentry/app/components/group/seenInfo.jsx b/src/sentry/static/sentry/app/components/group/seenInfo.jsx
index 061e4d494b..63501e7485 100644
--- a/src/sentry/static/sentry/app/components/group/seenInfo.jsx
+++ b/src/sentry/static/sentry/app/components/group/seenInfo.jsx
@@ -103,7 +103,12 @@ const SeenInfo = createReactClass({
               projectId={projectId}
               version={release.version}
             >
-              <Version orgId={orgId} projectId={projectId} version={release.version} />
+              <Version
+                orgId={orgId}
+                projectId={projectId}
+                version={release.version}
+                className="truncate"
+              />
             </VersionHoverCard>
           </dd>
         ) : !this.props.hasRelease ? (
diff --git a/src/sentry/static/sentry/app/components/version.jsx b/src/sentry/static/sentry/app/components/version.jsx
index 9beded6e06..1fae14ee64 100644
--- a/src/sentry/static/sentry/app/components/version.jsx
+++ b/src/sentry/static/sentry/app/components/version.jsx
@@ -30,6 +30,7 @@ class Version extends React.Component {
       showShortVersion,
       version,
       anchor,
+      className,
     } = this.props;
     const versionTitle = showShortVersion ? getShortVersion(version) : version;
 
@@ -42,6 +43,7 @@ class Version extends React.Component {
           //       which can interfere with URL construction
           <ProjectLink
             to={`/${orgId}/${projectId}/releases/${encodeURIComponent(version)}/`}
+            className={className}
           >
             <span title={version}>{versionTitle}</span>
           </ProjectLink>
@@ -50,7 +52,10 @@ class Version extends React.Component {
 
       if (orgId) {
         return (
-          <Link to={`/organizations/${orgId}/releases/${encodeURIComponent(version)}`}>
+          <Link
+            to={`/organizations/${orgId}/releases/${encodeURIComponent(version)}`}
+            className={className}
+          >
             <span title={version}>{versionTitle}</span>
           </Link>
         );
