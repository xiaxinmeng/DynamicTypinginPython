commit 834a177f7314b509ae62deb6be8354e5b32acf4f
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Sep 15 18:11:56 2014 -0700

    Add assigned param to group list

diff --git a/src/sentry/api/endpoints/project_group_index.py b/src/sentry/api/endpoints/project_group_index.py
index b3bf06ffbf..ed5389f010 100644
--- a/src/sentry/api/endpoints/project_group_index.py
+++ b/src/sentry/api/endpoints/project_group_index.py
@@ -34,6 +34,9 @@ class ProjectGroupIndexEndpoint(Endpoint):
         if request.user.is_authenticated() and request.GET.get('bookmarks'):
             query_kwargs['bookmarked_by'] = request.user
 
+        if request.user.is_authenticated() and request.GET.get('assigned'):
+            query_kwargs['assigned_to'] = request.user
+
         sort_by = request.GET.get('sort') or request.session.get('streamsort')
         if sort_by is None:
             sort_by = DEFAULT_SORT_OPTION
diff --git a/src/sentry/web/frontend/groups.py b/src/sentry/web/frontend/groups.py
index c993bd6dea..023f6f923d 100644
--- a/src/sentry/web/frontend/groups.py
+++ b/src/sentry/web/frontend/groups.py
@@ -57,6 +57,9 @@ def _get_group_list(request, project):
     if request.user.is_authenticated() and request.GET.get('bookmarks'):
         query_kwargs['bookmarked_by'] = request.user
 
+    if request.user.is_authenticated() and request.GET.get('assigned'):
+        query_kwargs['assigned_to'] = request.user
+
     sort_by = request.GET.get('sort') or request.session.get('streamsort')
     if sort_by is None:
         sort_by = DEFAULT_SORT_OPTION
