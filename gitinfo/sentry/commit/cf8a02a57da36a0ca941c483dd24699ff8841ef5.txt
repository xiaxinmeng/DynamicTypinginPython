commit cf8a02a57da36a0ca941c483dd24699ff8841ef5
Author: ckj <chriskjennings@gmail.com>
Date:   Wed Jul 30 14:33:10 2014 -0400

    Remove select box from access groups. Add breadcrumb.

diff --git a/src/sentry/static/sentry/app/views.js b/src/sentry/static/sentry/app/views.js
index 236a08d7d8..2f6888d3a8 100644
--- a/src/sentry/static/sentry/app/views.js
+++ b/src/sentry/static/sentry/app/views.js
@@ -308,6 +308,7 @@
 
         setEmpty: function(){
             this.$parent.html(this.$empty);
+            $('.stream-actions').hide();
         },
 
         extend: function(data){
diff --git a/src/sentry/static/sentry/less/sentry.less b/src/sentry/static/sentry/less/sentry.less
index f9b158ef32..0b3d53afbe 100644
--- a/src/sentry/static/sentry/less/sentry.less
+++ b/src/sentry/static/sentry/less/sentry.less
@@ -2334,3 +2334,17 @@ table.vars {
         color: @gray;
     }
 }
+
+
+.crumbs {
+  border-bottom: 3px solid #f1f2f3;
+  padding-bottom: 18px;
+  font-size: 20px;
+  margin-top: -8px;
+  a {
+    color: #9CA3AC;
+  }
+  span {
+    opacity: .4;
+  }
+}
diff --git a/src/sentry/templates/sentry/teams/groups/details.html b/src/sentry/templates/sentry/teams/groups/details.html
index a166d69bf2..926dab8b4b 100644
--- a/src/sentry/templates/sentry/teams/groups/details.html
+++ b/src/sentry/templates/sentry/teams/groups/details.html
@@ -10,14 +10,15 @@
 
 {% block bodyclass %} with-left-sidebar{% endblock %}
 
+{% block page_header %}
+  <h2 class="crumbs">
+    <a href="{% url 'sentry-manage-access-groups' team.slug %}">Access Groups</a> <span>/</span> {{ group.name }}
+  </h2>
+{% endblock %}
+
 {% block sidebar %}
-    <select style="margin-left:-14px" onchange="window.location.href=$(this.options[this.selectedIndex]).data('url');">
-        {% for g in group_list %}
-            <option {% if g == group %} selected="selected"{% endif %} data-url="{% url 'sentry-edit-access-group' g.team.slug g.id %}">{{ g.name }}</option>
-        {% endfor %}
-    </select>
-    <ul class="nav nav-list">
-        <li class="nav-header">{% trans "Access Group" %}</li>
+    <ul class="nav nav-stacked">
+
         <li{% if page == 'details' %} class="active"{% endif %}>
             <a href="{% url 'sentry-edit-access-group' group.team.slug group.id %}">{% trans "Settings" %}</a>
         </li>
