commit 04f0fcd4ebc4f94d14bd536f6904d531d2e8f46c
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Nov 23 17:17:37 2015 -0800

    Add seconds to default datetimes

diff --git a/src/sentry/static/sentry/app/components/dateTime.jsx b/src/sentry/static/sentry/app/components/dateTime.jsx
index df9c746e72..3c0e0ee404 100644
--- a/src/sentry/static/sentry/app/components/dateTime.jsx
+++ b/src/sentry/static/sentry/app/components/dateTime.jsx
@@ -4,14 +4,26 @@ import ConfigStore from '../stores/configStore.jsx';
 
 const DateTime = React.createClass({
   propTypes: {
-    date: React.PropTypes.any.isRequired
+    date: React.PropTypes.any.isRequired,
+  },
+
+  getDefaultProps() {
+    return {
+      seconds: true,
+    };
+  },
+
+  getDefaultFormat() {
+    return this.props.seconds ? 'll LTS z' : 'lll';
   },
 
   render() {
     let date = this.props.date;
     let user = ConfigStore.get('user');
     let options = user ? user.options : {};
-    let format = options.clock24Hours ? 'MMMM D YYYY HH:mm:ss z' : 'LLL z';
+    let format = (
+      options.clock24Hours ? 'MMMM D YYYY HH:mm:ss z' : this.getdefaultFormat()
+    );
 
     if (typeof date === 'string' || typeof date === 'number') {
       date = new Date(date);
diff --git a/src/sentry/static/sentry/app/components/group/seenInfo.jsx b/src/sentry/static/sentry/app/components/group/seenInfo.jsx
index e0b6130680..e01a1a83a3 100644
--- a/src/sentry/static/sentry/app/components/group/seenInfo.jsx
+++ b/src/sentry/static/sentry/app/components/group/seenInfo.jsx
@@ -21,7 +21,7 @@ const SeenInfo = React.createClass({
         <dt key={0}>When:</dt>
         <dd key={1}><TimeSince date={date} /></dd>
         <dt key={2}>Date:</dt>
-        <dd key={3}><DateTime date={date} /></dd>
+        <dd key={3}><DateTime date={date} seconds={true} /></dd>
         {utils.defined(release) && [
           <dt key={4}>Release:</dt>,
           <dd key={5}><Version orgId={this.props.orgId} projectId={this.props.projectId} version={release.version} /></dd>
