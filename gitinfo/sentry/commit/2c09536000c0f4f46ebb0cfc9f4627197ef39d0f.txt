commit 2c09536000c0f4f46ebb0cfc9f4627197ef39d0f
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Feb 13 12:12:34 2013 -0800

    Correct get_accelerated when no queryset is passed

diff --git a/src/sentry/manager.py b/src/sentry/manager.py
index df95fa88f1..259706b805 100644
--- a/src/sentry/manager.py
+++ b/src/sentry/manager.py
@@ -692,9 +692,9 @@ class GroupManager(BaseManager, ChartMixin):
         mcbm_tbl = GroupCountByMinute._meta.db_table
         if queryset is None:
             queryset = self
-
-        queryset = queryset._clone()
-        queryset.query.select_related = False
+        else:
+            queryset = queryset._clone()
+            queryset.query.select_related = False
 
         normalization = float(MINUTE_NORMALIZATION)
         assert minutes >= normalization
