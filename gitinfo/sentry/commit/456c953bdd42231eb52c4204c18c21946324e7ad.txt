commit 456c953bdd42231eb52c4204c18c21946324e7ad
Author: Lyn Nagara <lyn.nagara@gmail.com>
Date:   Tue Jan 21 14:32:17 2020 -0800

    fix(deletion): Fix group deletion (#16564)
    
    Fixes a bug in group deletion introduced in https://github.com/getsentry/sentry/pull/15676
    
    Fixes SENTRY-E81

diff --git a/src/sentry/runner/commands/cleanup.py b/src/sentry/runner/commands/cleanup.py
index d0b24e11b5..2c2ceb0f10 100644
--- a/src/sentry/runner/commands/cleanup.py
+++ b/src/sentry/runner/commands/cleanup.py
@@ -182,7 +182,7 @@ def cleanup(days, project, concurrency, silent, model, router, timed):
 
     # Deletions that use the `deletions` code path (which handles their child relations)
     # (model, datetime_field, order_by)
-    DELETES = (models.Group, "last_seen", "last_seen")
+    DELETES = ((models.Group, "last_seen", "last_seen"),)
 
     if not silent:
         click.echo("Removing expired values for LostPasswordHash")
