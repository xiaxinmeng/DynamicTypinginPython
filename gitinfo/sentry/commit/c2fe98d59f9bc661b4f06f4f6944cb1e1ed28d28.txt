commit c2fe98d59f9bc661b4f06f4f6944cb1e1ed28d28
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Jan 27 23:33:14 2014 -0800

    Filter out empty email addresses

diff --git a/src/sentry/utils/email.py b/src/sentry/utils/email.py
index f84bf865a7..c80107a3c3 100644
--- a/src/sentry/utils/email.py
+++ b/src/sentry/utils/email.py
@@ -133,9 +133,9 @@ class MessageBuilder(object):
                     pass
 
         if user_ids:
-            email_list |= set(User.objects.filter(
-                pk__in=user_ids, is_active=True
-            ).values_list('email', flat=True))
+            email_list |= set(filter(bool, User.objects.filter(
+                pk__in=user_ids, is_active=True,
+            ).values_list('email', flat=True)))
 
         self._send_to.update(email_list)
 
