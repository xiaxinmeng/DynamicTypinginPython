commit 1d0edea4525937a101216ed7646f83d4a0859098
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Oct 15 00:39:33 2012 -0700

    Clarify owner vs admin in the UI

diff --git a/src/sentry/constants.py b/src/sentry/constants.py
index 5dc31cc96b..c3d339aef9 100644
--- a/src/sentry/constants.py
+++ b/src/sentry/constants.py
@@ -56,7 +56,7 @@ MEMBER_OWNER = 0
 MEMBER_USER = 50
 MEMBER_SYSTEM = 100
 MEMBER_TYPES = (
-    (MEMBER_OWNER, _('owner')),
+    (MEMBER_OWNER, _('admin')),
     (MEMBER_USER, _('user')),
     (MEMBER_SYSTEM, _('system agent')),
 )
diff --git a/src/sentry/templates/sentry/projects/manage.html b/src/sentry/templates/sentry/projects/manage.html
index d4fa4115e4..ecf1ef0d83 100644
--- a/src/sentry/templates/sentry/projects/manage.html
+++ b/src/sentry/templates/sentry/projects/manage.html
@@ -94,7 +94,11 @@
                                     {{ user.email }}
                                 </td>
                                 <td style="text-align:center;">
-                                    {{ member.get_type_display }}
+                                    {% if user == project.team.owner %}
+                                        {% trans "Owner" %}
+                                    {% else %}
+                                        {{ member.get_type_display }}
+                                    {% endif %}
                                 </td>
                             </tr>
                         {% endfor %}
diff --git a/src/sentry/templates/sentry/teams/manage.html b/src/sentry/templates/sentry/teams/manage.html
index 0134e1c387..2e6f58680d 100644
--- a/src/sentry/templates/sentry/teams/manage.html
+++ b/src/sentry/templates/sentry/teams/manage.html
@@ -107,7 +107,11 @@
                                         {{ user.email }}
                                     </td>
                                     <td style="text-align:center;">
-                                        {{ member.get_type_display }}
+                                        {% if user == team.owner %}
+                                            {% trans "Owner" %}
+                                        {% else %}
+                                            {{ member.get_type_display }}
+                                        {% endif %}
                                     </td>
                                     {% if user == team.owner %}
                                         <td style="text-align:center;" colspan="2">
diff --git a/src/sentry/web/frontend/projects.py b/src/sentry/web/frontend/projects.py
index d687ff5074..2f0dd29bed 100644
--- a/src/sentry/web/frontend/projects.py
+++ b/src/sentry/web/frontend/projects.py
@@ -149,7 +149,8 @@ def manage_project(request, project):
     if result is False and not request.user.has_perm('sentry.can_change_project'):
         return HttpResponseRedirect(reverse('sentry'))
 
-    team_list = Team.objects.get_for_user(request.user, MEMBER_OWNER)
+    # XXX: We probably shouldnt allow changing the team unless they're the project owner
+    team_list = Team.objects.get_for_user(project.owner or request.user, MEMBER_OWNER)
 
     if request.user.has_perm('sentry.can_change_project'):
         form_cls = EditProjectAdminForm
