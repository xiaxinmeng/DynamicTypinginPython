commit 7b040fb06f10876e066cacfb60652da1b2e33617
Author: ckj <chriskjennings@gmail.com>
Date:   Mon May 9 16:38:36 2016 -0700

    Remove hidden file upload input from document flow

diff --git a/src/sentry/static/sentry/app/components/avatarSettings.jsx b/src/sentry/static/sentry/app/components/avatarSettings.jsx
index bec87c75dd..c77a9c0f32 100644
--- a/src/sentry/static/sentry/app/components/avatarSettings.jsx
+++ b/src/sentry/static/sentry/app/components/avatarSettings.jsx
@@ -353,6 +353,10 @@ const AvatarCropper = React.createClass({
 
   render() {
     let src = this.getImgSrc();
+    let style = {
+        position : "absolute"
+    };
+
     return (
       <div>
         {!src &&
@@ -363,7 +367,7 @@ const AvatarCropper = React.createClass({
         {this.renderCanvas()}
         <div className="form-group">
           {src && <a onClick={this.uploadClick}>{t('Change Photo')}</a>}
-          <input ref="file" type="file" accept="image/*" onChange={this.onChange}/>
+          <input ref="file" type="file" accept="image/*" onChange={this.onChange} style={style}/>
         </div>
       </div>
     );
