commit c6249556ec5fe58f74ed8a5a372ba0f1d8e1f084
Author: Armin Ronacher <armin.ronacher@active-4.com>
Date:   Mon Mar 14 15:07:22 2016 +0100

    Ensure that group ID fits within the bounds of the field. This fixes #2838

diff --git a/src/sentry/models/group.py b/src/sentry/models/group.py
index ece50fe55e..2529ac6abd 100644
--- a/src/sentry/models/group.py
+++ b/src/sentry/models/group.py
@@ -63,6 +63,11 @@ class GroupManager(BaseManager):
         callsign = callsign.upper()
         try:
             short_id = base32_decode(id)
+            # We need to make sure the short id is not overflowing the
+            # field's max or the lookup will fail with an assertion error.
+            max_id = Group._meta.get_field_by_name('short_id')[0].MAX_VALUE
+            if short_id > max_id:
+                raise ValueError()
         except ValueError:
             raise Group.DoesNotExist()
         return Group.objects.get(
