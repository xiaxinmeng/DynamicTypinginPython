commit 9f4db70ea1524256665354fce7d7f7650ec2ccff
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Sat Feb 13 18:02:53 2016 -0800

    Guard ourselves around premature request.LANGUAGE_CODE access

diff --git a/src/sentry/templatetags/sentry_assets.py b/src/sentry/templatetags/sentry_assets.py
index 45aed84036..cd851362b1 100644
--- a/src/sentry/templatetags/sentry_assets.py
+++ b/src/sentry/templatetags/sentry_assets.py
@@ -44,7 +44,15 @@ def locale_js_include(context):
     to the relevant locale JavaScript file
     """
     request = context['request']
-    lang_code = request.LANGUAGE_CODE
+
+    try:
+        lang_code = request.LANGUAGE_CODE
+    except AttributeError:
+        # it's possible that request at this point, LANGUAGE_CODE hasn't be bound
+        # to the Request object yet. This specifically happens when rendering our own
+        # 500 error page, resulting in yet another error trying to render our error.
+        return ''
+
     if lang_code == 'en' or lang_code not in settings.SUPPORTED_LANGUAGES:
         return ''
 
