commit b0c524ae3b99f07572a8be54436b6c141a2d59b1
Author: Katie Byers <lobsterkatie@gmail.com>
Date:   Tue Jul 10 13:02:42 2018 -0700

    fix(tests): Fix duplicate window.tick definition (#8995)
    
    Two is one too many - this code also exists 7 lines above.

diff --git a/tests/js/setup.js b/tests/js/setup.js
index 83db47f57a..516fb829ca 100644
--- a/tests/js/setup.js
+++ b/tests/js/setup.js
@@ -63,9 +63,6 @@ window.$ = window.jQuery = jQuery;
 window.sinon = sinon;
 window.scrollTo = jest.fn();
 
-// Instead of wrapping codeblocks in `setTimeout`
-window.tick = () => new Promise(res => setTimeout(res));
-
 // emotion context broadcast
 const broadcast = createBroadcast(theme);
 
