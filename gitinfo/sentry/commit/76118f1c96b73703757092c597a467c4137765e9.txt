commit 76118f1c96b73703757092c597a467c4137765e9
Author: David Cramer <dcramer@gmail.com>
Date:   Sun Feb 26 21:15:19 2012 -0800

    Improve docs and default conf

diff --git a/docs/quickstart/index.rst b/docs/quickstart/index.rst
index 96c7303758..0f95977687 100644
--- a/docs/quickstart/index.rst
+++ b/docs/quickstart/index.rst
@@ -85,6 +85,9 @@ configuration, as well as the default Sentry configuration values. It will use S
     SENTRY_WEB_HOST = '0.0.0.0'
     SENTRY_WEB_PORT = 9000
 
+    # No trailing slash!
+    SENTRY_URL_PREFIX = 'http://sentry.example.com'
+
     # SENTRY_KEY is a unique randomly generated secret key for your server, and it
     # acts as a signing token
     SENTRY_KEY = '0123456789abcde'
diff --git a/sentry/utils/runner.py b/sentry/utils/runner.py
index 6d429eef97..3da216cdb4 100644
--- a/sentry/utils/runner.py
+++ b/sentry/utils/runner.py
@@ -39,8 +39,16 @@ SENTRY_KEY = %(default_key)r
 # Set this to false to require authentication
 SENTRY_PUBLIC = True
 
+# You should configure the absolute URI to Sentry. It will attempt to guess it if you don't
+# but proxies may interfere with this.
+# SENTRY_URL_PREFIX = 'http://sentry.example.com'  # No trailing slash!
+
 SENTRY_WEB_HOST = '0.0.0.0'
 SENTRY_WEB_PORT = 9000
+SENTRY_WEB_OPTIONS = {
+    'workers': 3,  # the number of gunicorn workers
+}
+
 """
 
 
