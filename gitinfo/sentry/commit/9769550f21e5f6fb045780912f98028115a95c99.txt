commit 9769550f21e5f6fb045780912f98028115a95c99
Author: Markus Unterwaditzer <markus@unterwaditzer.net>
Date:   Tue May 12 00:26:10 2020 +0200

    fix: Do not spew out all docker logs in devserver (#18737)

diff --git a/src/sentry/runner/commands/devservices.py b/src/sentry/runner/commands/devservices.py
index 7db6b9b092..c54eac9c80 100644
--- a/src/sentry/runner/commands/devservices.py
+++ b/src/sentry/runner/commands/devservices.py
@@ -1,5 +1,6 @@
 from __future__ import absolute_import, print_function
 
+import time
 import atexit
 import signal
 import os
@@ -91,7 +92,7 @@ def attach(project, fast, service):
     signal.signal(signal.SIGINT, exit_handler)
     signal.signal(signal.SIGTERM, exit_handler)
 
-    for line in container.logs(stream=True):
+    for line in container.logs(stream=True, since=int(time.time() - 20)):
         click.echo(line, nl=False)
 
 
