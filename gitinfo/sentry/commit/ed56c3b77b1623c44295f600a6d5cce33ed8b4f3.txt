commit ed56c3b77b1623c44295f600a6d5cce33ed8b4f3
Author: Billy Vong <billyvg@users.noreply.github.com>
Date:   Mon Mar 18 10:13:43 2019 -0700

    build(webpack): Only show errors in webpack output (#12425)
    
    Also adds an overlay to show webpack errors in browser.

diff --git a/webpack.config.js b/webpack.config.js
index 354b3e5b39..f312a7e2f8 100644
--- a/webpack.config.js
+++ b/webpack.config.js
@@ -296,9 +296,6 @@ const appConfig = {
     },
   },
   devtool: IS_PRODUCTION ? 'source-map' : 'cheap-module-eval-source-map',
-  watchOptions: {
-    ignored: ['node_modules'],
-  },
 };
 
 /**
@@ -361,6 +358,11 @@ if (USE_HOT_MODULE_RELOAD) {
     // If below is false, will reload on errors
     hotOnly: true,
     port: WEBPACK_DEV_PORT,
+    stats: 'errors-only',
+    overlay: true,
+    watchOptions: {
+      ignored: ['node_modules'],
+    },
   };
 
   // Required, without this we get this on updates:
