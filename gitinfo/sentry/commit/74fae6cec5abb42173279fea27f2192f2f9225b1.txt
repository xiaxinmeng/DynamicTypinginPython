commit 74fae6cec5abb42173279fea27f2192f2f9225b1
Author: Burak Yigit Kaya <byk@sentry.io>
Date:   Fri Jun 21 18:53:14 2019 +0300

    ci(travis): Trigger docker-sentry git builds on successful master (#13769)
    
    This will build getsentry/docker-sentry master on each successful
    commit to getsentry/sentry master. This both will ensure nothing is
    broken in our docker images and will open the door for nightly docker
    image builds.

diff --git a/.travis.yml b/.travis.yml
index 4e4bef026a..c469b4348c 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -62,6 +62,27 @@ after_script:
   - zeus upload -t "text/plain+pycodestyle" .artifacts/*pycodestyle.log
   - zeus upload -t "text/xml+checkstyle" .artifacts/*checkstyle.xml
   - zeus upload -t "application/webpack-stats+json" .artifacts/*webpack-stats.json
+  # Trigger a build for the `git` image on docker-sentry
+  - >
+      curl -s -X POST
+      -H "Content-Type: application/json"
+      -H "Accept: application/json"
+      -H "Travis-API-Version: 3"
+      -H "Authorization: token $TRAVIS_TOKEN"
+      -d '{
+        "request": {
+          "branch": "master",
+          "config": {
+            "env": {
+              "matrix": [
+                "VERSION=git"
+              ]
+            }
+          },
+          "message": "Build for getsentry/sentry@'"$TRAVIS_COMMIT"'"
+        }
+      }'
+      https://api.travis-ci.org/repo/getsentry%2Fdocker-sentry/requests
 
 base_postgres: &postgres_default
   python: 2.7
