commit fc7acac23e04ad1b3609bee1c4d9f1c5776e576a
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Oct 6 08:38:34 2011 -0700

    Some compatibility and cleanup in TransactionTestCase

diff --git a/tests/testcases.py b/tests/testcases.py
index 0ba8f7c0a5..ece8e1d7f7 100644
--- a/tests/testcases.py
+++ b/tests/testcases.py
@@ -41,10 +41,7 @@ class TransactionTestCase(TransactionTestCase):
     fixtures and doesn't `flush` on setup.  This enables tests to be run in
     any order.
     """
-    tags = ['transaction']
-
-    multi_db = True
-    databases = None
+    urls = 'tests.urls'
 
     def __call__(self, result=None):
         """
@@ -52,7 +49,7 @@ class TransactionTestCase(TransactionTestCase):
         set up. This means that user-defined Test Cases aren't required to
         include a call to super().setUp().
         """
-        self.client = Client()
+        self.client = getattr(self, 'client_class', Client)()
         try:
             self._pre_setup()
         except (KeyboardInterrupt, SystemExit):
@@ -71,7 +68,6 @@ class TransactionTestCase(TransactionTestCase):
             except Exception:
                 import sys
                 result.addError(self, sys.exc_info())
-                return
 
     def _get_databases(self):
         if getattr(self, 'multi_db', False):
