commit 9b059c5d38c9294692c8420f590fa3fad7e6ce48
Author: Xavier Ordoquy <xordoquy@linovia.com>
Date:   Wed Jan 4 03:47:32 2012 +0100

    Bugfixed the 'all' view polling.

diff --git a/sentry/web/frontend/groups.py b/sentry/web/frontend/groups.py
index 5f8df63db8..39ef8b511f 100644
--- a/sentry/web/frontend/groups.py
+++ b/sentry/web/frontend/groups.py
@@ -53,6 +53,8 @@ def ajax_handler(request, project):
         event_list = Group.objects.filter(project=project)
 
         view_id = request.GET.get('view_id')
+        if view_id.lower() == u'null':
+            view_id = None
         if view_id:
             try:
                 view = View.objects.get(pk=view_id)
