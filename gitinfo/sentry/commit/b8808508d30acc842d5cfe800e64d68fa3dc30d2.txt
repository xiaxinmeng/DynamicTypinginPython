commit b8808508d30acc842d5cfe800e64d68fa3dc30d2
Author: Jess MacQueen <macqueen@users.noreply.github.com>
Date:   Fri Jan 20 14:09:04 2017 -0800

    add organization member list test, make dates in email tests consistent (#4801)

diff --git a/src/sentry/web/frontend/debug/debug_mfa_added_email.py b/src/sentry/web/frontend/debug/debug_mfa_added_email.py
index 5ac0a38e87..03f46b8d10 100644
--- a/src/sentry/web/frontend/debug/debug_mfa_added_email.py
+++ b/src/sentry/web/frontend/debug/debug_mfa_added_email.py
@@ -1,5 +1,7 @@
 from __future__ import absolute_import, print_function
 
+import datetime
+
 from django.views.generic import View
 
 from sentry.models import Authenticator
@@ -24,6 +26,8 @@ class DebugMfaAddedEmailView(View):
             context={
                 'authenticator': authenticator,
             },
+            # make this consistent for acceptance tests
+            current_datetime=datetime.datetime(2017, 1, 20, 21, 39, 23, 30723)
         )
         return MailPreview(
             html_template=email.html_template,
diff --git a/src/sentry/web/frontend/debug/debug_mfa_removed_email.py b/src/sentry/web/frontend/debug/debug_mfa_removed_email.py
index 311b6c4a71..e3bc2eb1c5 100644
--- a/src/sentry/web/frontend/debug/debug_mfa_removed_email.py
+++ b/src/sentry/web/frontend/debug/debug_mfa_removed_email.py
@@ -1,5 +1,7 @@
 from __future__ import absolute_import, print_function
 
+import datetime
+
 from django.views.generic import View
 
 from sentry.models import Authenticator
@@ -24,6 +26,8 @@ class DebugMfaRemovedEmailView(View):
             context={
                 'authenticator': authenticator,
             },
+            # make this consistent for acceptance tests
+            current_datetime=datetime.datetime(2017, 1, 20, 21, 39, 23, 30723)
         )
         return MailPreview(
             html_template=email.html_template,
diff --git a/src/sentry/web/frontend/debug/debug_password_changed_email.py b/src/sentry/web/frontend/debug/debug_password_changed_email.py
index 34b4e8b60d..e3b65ecd24 100644
--- a/src/sentry/web/frontend/debug/debug_password_changed_email.py
+++ b/src/sentry/web/frontend/debug/debug_password_changed_email.py
@@ -1,5 +1,7 @@
 from __future__ import absolute_import, print_function
 
+import datetime
+
 from django.views.generic import View
 
 from sentry.security.emails import generate_security_email
@@ -14,6 +16,8 @@ class DebugPasswordChangedEmailView(View):
             actor=request.user,
             type='password-changed',
             ip_address=request.META['REMOTE_ADDR'],
+            # make this consistent for acceptance tests
+            current_datetime=datetime.datetime(2017, 1, 20, 21, 39, 23, 30723)
         )
         return MailPreview(
             html_template=email.html_template,
diff --git a/tests/acceptance/test_member_list.py b/tests/acceptance/test_member_list.py
new file mode 100644
index 0000000000..5cb07d813c
--- /dev/null
+++ b/tests/acceptance/test_member_list.py
@@ -0,0 +1,28 @@
+from __future__ import absolute_import
+
+from sentry.testutils import AcceptanceTestCase
+
+
+class ListOrganizationMembersTest(AcceptanceTestCase):
+    def setUp(self):
+        super(ListOrganizationMembersTest, self).setUp()
+        self.user = self.create_user('foo@example.com')
+        self.org = self.create_organization(
+            name='Rowdy Tiger',
+            owner=None,
+        )
+        self.team = self.create_team(
+            organization=self.org,
+            name='Mariachi Band'
+        )
+        self.create_member(
+            user=self.user,
+            organization=self.org,
+            role='owner',
+            teams=[self.team],
+        )
+        self.login_as(self.user)
+
+    def test_list(self):
+        self.browser.get('/organizations/{}/members/'.format(self.org.slug))
+        self.browser.snapshot(name='list organization members')
