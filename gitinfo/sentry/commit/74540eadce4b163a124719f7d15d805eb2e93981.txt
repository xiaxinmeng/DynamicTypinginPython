commit 74540eadce4b163a124719f7d15d805eb2e93981
Author: Brett Hoerner <brett@bretthoerner.com>
Date:   Tue Mar 20 06:10:32 2018 -0500

    fix(tagstore): Add partition_key to child relations in v2 (#7696)
    
    Fixes SENTRY-61M

diff --git a/src/sentry/tagstore/v2/backend.py b/src/sentry/tagstore/v2/backend.py
index ad85ab0757..a6b622282e 100644
--- a/src/sentry/tagstore/v2/backend.py
+++ b/src/sentry/tagstore/v2/backend.py
@@ -89,10 +89,10 @@ class V2TagStorage(TagStorage):
                 key_id_field_name = 'key_id' if (db.is_postgres() or db.is_mysql()) else '_key_id'
 
                 relations = [
-                    ModelRelation(m, {
+                    ModelRelation(m, query={
                         'project_id': instance.project_id,
                         key_id_field_name: instance.id,
-                    }) for m in model_list
+                    }, partition_key={'project_id': instance.project_id}) for m in model_list
                 ]
                 return relations
 
