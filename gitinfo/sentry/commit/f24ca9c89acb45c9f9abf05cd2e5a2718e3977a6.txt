commit f24ca9c89acb45c9f9abf05cd2e5a2718e3977a6
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Aug 20 21:53:08 2014 -0700

    Default header levels to h3

diff --git a/src/sentry/templatetags/sentry_markup.py b/src/sentry/templatetags/sentry_markup.py
index 89ef87d2e4..24b8a5c3f2 100644
--- a/src/sentry/templatetags/sentry_markup.py
+++ b/src/sentry/templatetags/sentry_markup.py
@@ -10,7 +10,7 @@ register = template.Library()
 
 
 @register.filter(is_safe=True)
-def markdown(value):
+def markdown(value, header_level=3):
     """
     Runs Markdown over a given value, optionally using various
     extensions python-markdown supports.
@@ -19,4 +19,6 @@ def markdown(value):
 
         {{ value|markdown }}
     """
-    return mark_safe(markdown_func(force_text(value)))
+    return mark_safe(markdown_func(force_text(value), extensions=[
+        'headerid(level=%d)' % (int(header_level),),
+    ]))
