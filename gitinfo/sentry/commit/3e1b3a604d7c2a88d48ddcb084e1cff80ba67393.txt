commit 3e1b3a604d7c2a88d48ddcb084e1cff80ba67393
Author: Ted Kaemming <ted@kaemming.com>
Date:   Wed Oct 14 11:21:35 2015 -0700

    Don't add muted groups to digests.

diff --git a/src/sentry/plugins/bases/notify.py b/src/sentry/plugins/bases/notify.py
index 95483fe240..da40c06844 100644
--- a/src/sentry/plugins/bases/notify.py
+++ b/src/sentry/plugins/bases/notify.py
@@ -108,14 +108,12 @@ class NotificationPlugin(Plugin):
         return member_set
 
     def should_notify(self, group, event):
-        # If digests are enabled for this project, we always want to add the
-        # notification to the digest (even if it may be filtered out later.)
-        if self.__can_be_digested(event):
-            return True
-
         if group.is_muted():
             return False
 
+        if self.__can_be_digested(event):
+            return True
+
         project = group.project
 
         rate_limited = ratelimiter.is_limited(
