commit 0903edec9e12c685a08a58d218ae3e5862e8a20d
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Sun Jan 12 23:34:47 2014 -0800

    Fix tests

diff --git a/tests/sentry/plugins/mail/tests.py b/tests/sentry/plugins/mail/tests.py
index 3a41803a86..cd2d8a0c1e 100644
--- a/tests/sentry/plugins/mail/tests.py
+++ b/tests/sentry/plugins/mail/tests.py
@@ -36,6 +36,7 @@ class MailPluginTest(TestCase):
 
         event = Event()
         event.group = group
+        event.project = self.project
         event.message = 'hello world'
         event.logger = 'root'
         event.site = None
@@ -62,6 +63,7 @@ class MailPluginTest(TestCase):
 
         event = Event()
         event.group = group
+        event.project = self.project
         event.message = 'hello world'
         event.logger = 'root'
         event.site = None
@@ -88,6 +90,7 @@ class MailPluginTest(TestCase):
 
         event = Event()
         event.group = group
+        event.project = self.project
         event.message = 'Soubor ji\xc5\xbe existuje'
         event.logger = 'root'
         event.site = None
