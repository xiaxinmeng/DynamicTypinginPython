commit a2a422d7c75d05bb751eab5f6ead0d0d8c936329
Author: Evan Purkhiser <evanpurkhiser@gmail.com>
Date:   Wed Mar 25 11:18:34 2020 -0700

    chore(css): Remove some trivial usages of Box (#17894)

diff --git a/src/sentry/static/sentry/app/views/settings/account/accountNotifications.jsx b/src/sentry/static/sentry/app/views/settings/account/accountNotifications.jsx
index b0651a9839..1240730a62 100644
--- a/src/sentry/static/sentry/app/views/settings/account/accountNotifications.jsx
+++ b/src/sentry/static/sentry/app/views/settings/account/accountNotifications.jsx
@@ -1,4 +1,3 @@
-import {Box} from 'reflexbox';
 import PropTypes from 'prop-types';
 import React from 'react';
 import styled from '@emotion/styled';
@@ -56,20 +55,18 @@ export default class AccountNotifications extends AsyncView {
           apiMethod="PUT"
           apiEndpoint="/users/me/notifications/"
         >
-          <Box>
-            <JsonForm
-              forms={accountNotificationFields}
-              renderFooter={({title}) => {
-                if (FINE_TUNE_FOOTERS[title]) {
-                  return <FineTuningFooter {...FINE_TUNE_FOOTERS[title]} />;
-                }
-                return null;
-              }}
-            />
-            <AlertLink to="/settings/account/emails" icon="icon-mail">
-              {t('Looking to add or remove an email address? Use the emails panel.')}
-            </AlertLink>
-          </Box>
+          <JsonForm
+            forms={accountNotificationFields}
+            renderFooter={({title}) => {
+              if (FINE_TUNE_FOOTERS[title]) {
+                return <FineTuningFooter {...FINE_TUNE_FOOTERS[title]} />;
+              }
+              return null;
+            }}
+          />
+          <AlertLink to="/settings/account/emails" icon="icon-mail">
+            {t('Looking to add or remove an email address? Use the emails panel.')}
+          </AlertLink>
         </Form>
       </div>
     );
diff --git a/src/sentry/static/sentry/app/views/settings/organizationTeams/teamSettings/index.jsx b/src/sentry/static/sentry/app/views/settings/organizationTeams/teamSettings/index.jsx
index d203510ef2..9e74d542e4 100644
--- a/src/sentry/static/sentry/app/views/settings/organizationTeams/teamSettings/index.jsx
+++ b/src/sentry/static/sentry/app/views/settings/organizationTeams/teamSettings/index.jsx
@@ -1,4 +1,3 @@
-import {Box} from 'reflexbox';
 import PropTypes from 'prop-types';
 import React from 'react';
 
@@ -81,9 +80,7 @@ export default class TeamSettings extends AsyncView {
             slug: team.slug,
           }}
         >
-          <Box>
-            <JsonForm access={access} location={location} forms={teamSettingsFields} />
-          </Box>
+          <JsonForm access={access} location={location} forms={teamSettingsFields} />
         </Form>
 
         <Panel>
