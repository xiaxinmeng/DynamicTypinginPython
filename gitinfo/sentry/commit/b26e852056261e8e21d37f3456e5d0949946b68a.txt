commit b26e852056261e8e21d37f3456e5d0949946b68a
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Jul 15 10:50:42 2013 -0700

    lint

diff --git a/src/sentry/middleware.py b/src/sentry/middleware.py
index 9804065d86..82f23b58f0 100644
--- a/src/sentry/middleware.py
+++ b/src/sentry/middleware.py
@@ -3,6 +3,7 @@ from sentry.conf import settings
 from sentry.models import UserOption
 from sentry.utils.http import absolute_uri
 from django.core.urlresolvers import reverse
+from social_auth.middleware import SocialAuthExceptionMiddleware
 
 
 class SentryMiddleware(object):
@@ -25,9 +26,6 @@ class SentryMiddleware(object):
             request.session['django_language'] = language
 
 
-
-from social_auth.middleware import SocialAuthExceptionMiddleware
-
 class SentrySocialAuthExceptionMiddleware(SocialAuthExceptionMiddleware):
     def get_redirect_uri(self, request, exception):
         return absolute_uri(reverse('sentry-login'))
diff --git a/src/sentry/utils/runner.py b/src/sentry/utils/runner.py
index e413b2e667..a2f496d1e0 100644
--- a/src/sentry/utils/runner.py
+++ b/src/sentry/utils/runner.py
@@ -234,6 +234,7 @@ def apply_legacy_settings(config):
         if urlbits.hostname:
             settings.ALLOWED_HOSTS = (urlbits.hostname,)
 
+
 def table_exists(name):
     from django.db import connections
     return name in connections['default'].introspection.table_names()
