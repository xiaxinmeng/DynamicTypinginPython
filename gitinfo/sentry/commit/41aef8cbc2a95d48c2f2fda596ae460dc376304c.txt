commit 41aef8cbc2a95d48c2f2fda596ae460dc376304c
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Mar 29 11:13:14 2016 -0700

    Correctly pass organization for identifying mergeable users

diff --git a/src/sentry/management/commands/merge_users.py b/src/sentry/management/commands/merge_users.py
index 152ae86242..3406653f01 100644
--- a/src/sentry/management/commands/merge_users.py
+++ b/src/sentry/management/commands/merge_users.py
@@ -24,7 +24,9 @@ class Command(BaseCommand):
     )
 
     def _get_organization_user_sets(self, organization):
-        queryset = OrganizationMember.objects.filter(organization__slug='hubspot-dev').select_related('user')
+        queryset = OrganizationMember.objects.filter(
+            organization=organization,
+        ).select_related('user')
 
         members_by_email = defaultdict(list)
         for member in queryset:
