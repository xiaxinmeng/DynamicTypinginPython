commit 12fbe50abcffdaa8bd0def1e4a1304a5ea356da8
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Dec 23 19:00:00 2014 -0800

    Ensure tag keys are case insensitive

diff --git a/src/sentry/rules/conditions/tagged_event.py b/src/sentry/rules/conditions/tagged_event.py
index 5c69e89871..63763cf172 100644
--- a/src/sentry/rules/conditions/tagged_event.py
+++ b/src/sentry/rules/conditions/tagged_event.py
@@ -48,8 +48,9 @@ class TaggedEventCondition(EventCondition):
             return False
 
         value = value.lower()
+        key = key.lower()
 
-        tags = (v.lower() for k, v in event.get_tags() if k == key)
+        tags = (v.lower() for k, v in event.get_tags() if k.lower() == key)
 
         if match == MatchType.EQUAL:
             for t_value in tags:
