commit 930be801c422b7e972c384e5ce28cae0e58f09ff
Author: Megan Heskett <meg.heskett@gmail.com>
Date:   Fri Nov 1 17:49:16 2019 -0700

    ref(join-requests): Increase rate limit (#15406)

diff --git a/src/sentry/api/endpoints/organization_join_request.py b/src/sentry/api/endpoints/organization_join_request.py
index da50c7155a..1105d40322 100644
--- a/src/sentry/api/endpoints/organization_join_request.py
+++ b/src/sentry/api/endpoints/organization_join_request.py
@@ -72,7 +72,9 @@ class OrganizationJoinRequestEndpoint(OrganizationEndpoint):
         ip_address = request.META["REMOTE_ADDR"]
 
         if ratelimiter.is_limited(
-            u"org-join-request:ip:{}".format(ip_address), limit=5, window=60  # 5 per minute
+            u"org-join-request:ip:{}".format(ip_address),
+            limit=5,
+            window=86400,  # 5 per day, 60 x 60 x 24
         ):
             return Response({"detail": "Rate limit exceeded."}, status=429)
 
