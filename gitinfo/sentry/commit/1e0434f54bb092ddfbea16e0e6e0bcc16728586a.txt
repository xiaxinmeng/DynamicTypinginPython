commit 1e0434f54bb092ddfbea16e0e6e0bcc16728586a
Author: ckj <chriskjennings@gmail.com>
Date:   Sat Aug 8 13:28:09 2015 -0400

    Implement muted icon in stream

diff --git a/src/sentry/static/sentry/app/components/streamGroup.jsx b/src/sentry/static/sentry/app/components/streamGroup.jsx
index d565c18ab0..a634c761e8 100644
--- a/src/sentry/static/sentry/app/components/streamGroup.jsx
+++ b/src/sentry/static/sentry/app/components/streamGroup.jsx
@@ -149,6 +149,9 @@ var StreamGroup = React.createClass({
     if (data.status === "resolved") {
       className += " isResolved";
     }
+    if (data.status === "muted") {
+      className += " isMuted";
+    }
 
     var routeParams = {
       orgId: params.orgId,
@@ -166,6 +169,7 @@ var StreamGroup = React.createClass({
           }
           <h3 className="truncate">
             <Router.Link to="groupDetails" params={routeParams}>
+              <span className="icon icon-soundoff"></span>
               <span className="icon icon-bookmark"></span>
               {data.title}
             </Router.Link>
diff --git a/src/sentry/static/sentry/less/stream.less b/src/sentry/static/sentry/less/stream.less
index 38f7af0ebe..05503a6ec0 100644
--- a/src/sentry/static/sentry/less/stream.less
+++ b/src/sentry/static/sentry/less/stream.less
@@ -146,16 +146,29 @@
     background: #fff;
     .transition(all .4s ease-in-out);
 
-    .icon-bookmark {
-      display: none;
+    .icon-bookmark, .icon-soundoff {
+      padding-right: 5px;
       font-size: 95%;
       position: relative;
+      .transition(all .2s ease-in-out);
+      width: auto;
+    }
+
+    .icon-bookmark {
       top: 2px;
-      color: #D4DBDF;
+      display: none;
+      color: lighten(@gray, 15%);
+      overflow: hidden;
+    }
+
+    .icon-soundoff {
+      display: none;
+      font-size: 90%;
+      top: 1px;
+      color: @red;
       width: auto;
-      padding-right: 3px;
       overflow: hidden;
-      .transition(all .2s ease-in-out);
+      padding-left: 1px;
     }
 
     &.isBookmarked {
@@ -164,6 +177,12 @@
       }
     }
 
+    &.isMuted {
+      .icon-soundoff {
+        display: inline-block;
+      }
+    }
+
     .event-details:after {
       position: absolute;
       content: "";
