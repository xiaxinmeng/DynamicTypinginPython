commit 9c61e3dab7e4688d1c16ff81acb86c253a6932b9
Author: Evan Purkhiser <evanpurkhiser@gmail.com>
Date:   Thu Mar 21 18:28:14 2019 -0700

    ref(ui): Move Monitors under a SidebarSection (#12498)

diff --git a/src/sentry/static/sentry/app/components/sidebar/index.jsx b/src/sentry/static/sentry/app/components/sidebar/index.jsx
index 9026520b52..572679f451 100644
--- a/src/sentry/static/sentry/app/components/sidebar/index.jsx
+++ b/src/sentry/static/sentry/app/components/sidebar/index.jsx
@@ -356,23 +356,22 @@ class Sidebar extends React.Component {
                     id="discover"
                   />
                 </Feature>
+                <Feature features={['monitors']} organization={organization}>
+                  <SidebarItem
+                    {...sidebarItemProps}
+                    onClick={(_id, evt) =>
+                      this.navigateWithGlobalSelection(
+                        `/organizations/${organization.slug}/monitors/`,
+                        evt
+                      )}
+                    icon={<InlineSvg src="icon-labs" />}
+                    label={t('Monitors')}
+                    to={`/organizations/${organization.slug}/monitors/`}
+                    id="monitors"
+                  />
+                </Feature>
               </SidebarSection>
 
-              <Feature features={['monitors']} organization={organization}>
-                <SidebarItem
-                  {...sidebarItemProps}
-                  onClick={(_id, evt) =>
-                    this.navigateWithGlobalSelection(
-                      `/organizations/${organization.slug}/monitors/`,
-                      evt
-                    )}
-                  icon={<InlineSvg src="icon-labs" />}
-                  label={t('Monitors')}
-                  to={`/organizations/${organization.slug}/monitors/`}
-                  id="monitors"
-                />
-              </Feature>
-
               {!hasSentry10 && (
                 <SidebarSection>
                   <SidebarItem
