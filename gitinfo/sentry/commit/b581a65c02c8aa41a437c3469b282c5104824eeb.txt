commit b581a65c02c8aa41a437c3469b282c5104824eeb
Author: Jess MacQueen <jessmacqueen@gmail.com>
Date:   Wed Mar 7 17:31:23 2018 -0800

    ref(teams): Change team name to #team-slug in various places

diff --git a/src/sentry/static/sentry/app/components/missingProjectMembership.jsx b/src/sentry/static/sentry/app/components/missingProjectMembership.jsx
index 5ca0d1ca55..98ba4a21b8 100644
--- a/src/sentry/static/sentry/app/components/missingProjectMembership.jsx
+++ b/src/sentry/static/sentry/app/components/missingProjectMembership.jsx
@@ -121,7 +121,7 @@ const MissingProjectMembership = createReactClass({
     return teams.map(team => {
       return (
         <p key={team.slug}>
-          {team.name}: {this.renderJoinTeam(team, features)}
+          #{team.slug}: {this.renderJoinTeam(team, features)}
         </p>
       );
     });
diff --git a/src/sentry/static/sentry/app/components/teamAvatar.jsx b/src/sentry/static/sentry/app/components/teamAvatar.jsx
index 266bab8de5..3c0a7631f9 100644
--- a/src/sentry/static/sentry/app/components/teamAvatar.jsx
+++ b/src/sentry/static/sentry/app/components/teamAvatar.jsx
@@ -17,7 +17,7 @@ class TeamAvatar extends React.Component {
 
   getDisplayName = () => {
     let team = this.props.team;
-    return team.name || team.slug;
+    return team.slug;
   };
 
   render() {
diff --git a/src/sentry/static/sentry/app/views/onboarding/project/index.jsx b/src/sentry/static/sentry/app/views/onboarding/project/index.jsx
index 28a339e49e..86b3aaab75 100644
--- a/src/sentry/static/sentry/app/views/onboarding/project/index.jsx
+++ b/src/sentry/static/sentry/app/views/onboarding/project/index.jsx
@@ -58,7 +58,7 @@ class OnboardingProject extends React.Component {
           >
             {teams.map(({slug, name, id}, i) => (
               <option key={id} value={slug}>
-                {name}
+                #{slug}
               </option>
             ))}
           </SelectInput>
diff --git a/src/sentry/static/sentry/app/views/organizationTeams/allTeamsRow.jsx b/src/sentry/static/sentry/app/views/organizationTeams/allTeamsRow.jsx
index b1ae019341..f89f57f6e9 100644
--- a/src/sentry/static/sentry/app/views/organizationTeams/allTeamsRow.jsx
+++ b/src/sentry/static/sentry/app/views/organizationTeams/allTeamsRow.jsx
@@ -99,7 +99,7 @@ const AllTeamsRow = createReactClass({
     return (
       <tr>
         <td>
-          <h5>{team.name}</h5>
+          <h5>#{team.slug}</h5>
         </td>
         <td className="actions align-right">
           {this.state.loading ? (
diff --git a/src/sentry/static/sentry/app/views/organizationTeams/expandedTeamList.jsx b/src/sentry/static/sentry/app/views/organizationTeams/expandedTeamList.jsx
index f5167c15f8..c61862d784 100644
--- a/src/sentry/static/sentry/app/views/organizationTeams/expandedTeamList.jsx
+++ b/src/sentry/static/sentry/app/views/organizationTeams/expandedTeamList.jsx
@@ -115,7 +115,7 @@ const ExpandedTeamList = createReactClass({
               </Link>
             )}
           </div>
-          <h3>{team.name}</h3>
+          <h3>#{team.slug}</h3>
         </div>
         <div className="box-content">
           <table className="table table-no-top-border m-b-0">
diff --git a/src/sentry/static/sentry/app/views/projectChooser.jsx b/src/sentry/static/sentry/app/views/projectChooser.jsx
index e33e8cba22..c976c331ba 100644
--- a/src/sentry/static/sentry/app/views/projectChooser.jsx
+++ b/src/sentry/static/sentry/app/views/projectChooser.jsx
@@ -69,7 +69,7 @@ const ProjectChooser = createReactClass({
         <div className="box" key={i}>
           <div key={team.id}>
             <div className="box-header" key={team.id}>
-              <h2>{features.has('internal-catchall') ? team.slug : team.name}</h2>
+              <h2>{features.has('internal-catchall') ? `#${team.slug}` : team.name}</h2>
             </div>
             <div className="box-content">
               <table className="table">
diff --git a/src/sentry/static/sentry/app/views/settings/organization/members/organizationAccessRequests.jsx b/src/sentry/static/sentry/app/views/settings/organization/members/organizationAccessRequests.jsx
index be0900c063..85d9fd7c6d 100644
--- a/src/sentry/static/sentry/app/views/settings/organization/members/organizationAccessRequests.jsx
+++ b/src/sentry/static/sentry/app/views/settings/organization/members/organizationAccessRequests.jsx
@@ -63,7 +63,7 @@ class OrganizationAccessRequests extends React.Component {
                 <Box p={2} flex="1">
                   {tct('[name] requests access to the [team] team.', {
                     name: <strong>{displayName}</strong>,
-                    team: <strong>{team.name}</strong>,
+                    team: <strong>#{team.slug}</strong>,
                   })}
                 </Box>
                 <Box p={2}>
diff --git a/src/sentry/static/sentry/app/views/settings/project/projectTeams.jsx b/src/sentry/static/sentry/app/views/settings/project/projectTeams.jsx
index a8fad778da..a45a29a9f9 100644
--- a/src/sentry/static/sentry/app/views/settings/project/projectTeams.jsx
+++ b/src/sentry/static/sentry/app/views/settings/project/projectTeams.jsx
@@ -69,10 +69,10 @@ const TeamRow = createReactClass({
           <h5 style={{margin: '10px 0px'}}>
             {access.has('team:write') ? (
               <Link to={`/settings/organization/${orgId}/teams/${team.slug}`}>
-                {team.name}
+                #{team.slug}
               </Link>
             ) : (
-              team.name
+              `#${team.slug}`
             )}
           </h5>
         </Box>
diff --git a/src/sentry/static/sentry/app/views/settings/team/allTeamsRow.jsx b/src/sentry/static/sentry/app/views/settings/team/allTeamsRow.jsx
index 4bdd4d76f1..6fa20991ce 100644
--- a/src/sentry/static/sentry/app/views/settings/team/allTeamsRow.jsx
+++ b/src/sentry/static/sentry/app/views/settings/team/allTeamsRow.jsx
@@ -99,7 +99,7 @@ const AllTeamsRow = createReactClass({
   render() {
     let {access, team, urlPrefix, openMembership, organization} = this.props;
     let features = new Set(organization.features);
-    let display = features.has('internal-catchall') ? team.slug : team.name;
+    let display = features.has('internal-catchall') ? `#${team.slug}` : team.name;
     return (
       <PanelItem p={0} align="center">
         <Box flex="1" p={2}>
diff --git a/src/sentry/static/sentry/app/views/settings/team/expandedTeamList.jsx b/src/sentry/static/sentry/app/views/settings/team/expandedTeamList.jsx
index 06787eea45..b87354fbb0 100644
--- a/src/sentry/static/sentry/app/views/settings/team/expandedTeamList.jsx
+++ b/src/sentry/static/sentry/app/views/settings/team/expandedTeamList.jsx
@@ -121,7 +121,7 @@ const ExpandedTeamList = createReactClass({
               </TeamHeaderLink>
             )}
           </div>
-          {team.name}
+          #{team.slug}
         </PanelHeader>
         <div className="box-content">
           <table className="table table-no-top-border m-b-0">
diff --git a/src/sentry/static/sentry/app/views/teamDetails.jsx b/src/sentry/static/sentry/app/views/teamDetails.jsx
index d24f026fd8..a739367ecd 100644
--- a/src/sentry/static/sentry/app/views/teamDetails.jsx
+++ b/src/sentry/static/sentry/app/views/teamDetails.jsx
@@ -97,7 +97,7 @@ const TeamDetails = createReactClass({
     const features = new Set(this.context.organization.features);
     return (
       <div>
-        <h3>{team.name}</h3>
+        <h3>{features.has('internal-catchall') ? `#${team.slug}` : team.name}</h3>
 
         {!features.has('new-settings') &&
           access.has('team:admin') && (
diff --git a/src/sentry/templates/sentry/emails/access-approved.html b/src/sentry/templates/sentry/emails/access-approved.html
index b4ee7edd12..13701dbf9d 100644
--- a/src/sentry/templates/sentry/emails/access-approved.html
+++ b/src/sentry/templates/sentry/emails/access-approved.html
@@ -4,5 +4,5 @@
 
 {% block main %}
     <h3>Access Approved</h3>
-    <p>You have been granted access to the <strong>{{ organization.name }} / {{ team.name }}</strong> team on Sentry.</p>
+    <p>You have been granted access to the <strong>{{ organization.slug }} / #{{ team.slug }}</strong> team on Sentry.</p>
 {% endblock %}
diff --git a/src/sentry/templates/sentry/emails/access-approved.txt b/src/sentry/templates/sentry/emails/access-approved.txt
index bff189c8e1..ecb100cfe5 100644
--- a/src/sentry/templates/sentry/emails/access-approved.txt
+++ b/src/sentry/templates/sentry/emails/access-approved.txt
@@ -1 +1 @@
-You have been granted access to the {{ organization.name }} / {{ team.name }} team on Sentry.
+You have been granted access to the {{ organization.slug }} / #{{ team.slug }} team on Sentry.
diff --git a/src/sentry/templates/sentry/emails/request-team-access.html b/src/sentry/templates/sentry/emails/request-team-access.html
index c58a4bac41..dbaf3f7c8a 100644
--- a/src/sentry/templates/sentry/emails/request-team-access.html
+++ b/src/sentry/templates/sentry/emails/request-team-access.html
@@ -4,7 +4,7 @@
 
 {% block main %}
     <h3>Request for Access</h3>
-    <p><strong>{{ name }}</strong> is requesting access to the {{ team.name }} team on Sentry.</p>
+    <p><strong>{{ name }}</strong> is requesting access to the #{{ team.slug }} team on Sentry.</p>
 
     <p>Accept or decline this request in the <a href="{{ url }}">pending requests panel</a>.</p>
 {% endblock %}
diff --git a/src/sentry/templates/sentry/emails/request-team-access.txt b/src/sentry/templates/sentry/emails/request-team-access.txt
index 82a3b85d98..a250137eaa 100644
--- a/src/sentry/templates/sentry/emails/request-team-access.txt
+++ b/src/sentry/templates/sentry/emails/request-team-access.txt
@@ -1,4 +1,4 @@
-{{ name }} is requesting access to {{ team.name }}.
+{{ name }} is requesting access to #{{ team.slug }}.
 
 You can accept or decline this request in via the pending requests panel:
 
diff --git a/src/sentry/templates/sentry/teams/remove.html b/src/sentry/templates/sentry/teams/remove.html
index 8888baea54..06bd6807b4 100644
--- a/src/sentry/templates/sentry/teams/remove.html
+++ b/src/sentry/templates/sentry/teams/remove.html
@@ -10,7 +10,7 @@
   <div class="page-header">
     <h2>
       {% trans "Remove Team" %}
-      <small>{{ team.name }}</small>
+      <small>#{{ team.slug }}</small>
     </h2>
   </div>
   <form action="" method="post">
diff --git a/src/sentry/web/frontend/remove_team.py b/src/sentry/web/frontend/remove_team.py
index 157251cd4e..10910f1d63 100644
--- a/src/sentry/web/frontend/remove_team.py
+++ b/src/sentry/web/frontend/remove_team.py
@@ -33,7 +33,7 @@ class RemoveTeamView(TeamView):
 
             messages.add_message(
                 request, messages.SUCCESS,
-                _(u'The team %r was scheduled for deletion.') % (team.name.encode('utf-8'), )
+                _(u'The team #%r was scheduled for deletion.') % (team.slug.encode('utf-8'), )
             )
 
             return HttpResponseRedirect(reverse('sentry'))
diff --git a/tests/js/spec/components/__snapshots__/teamAvatar.spec.jsx.snap b/tests/js/spec/components/__snapshots__/teamAvatar.spec.jsx.snap
index f8e8176ef9..c7d64796db 100644
--- a/tests/js/spec/components/__snapshots__/teamAvatar.spec.jsx.snap
+++ b/tests/js/spec/components/__snapshots__/teamAvatar.spec.jsx.snap
@@ -6,10 +6,10 @@ exports[`TeamAvatar render() renders 1`] = `
 >
   <Tooltip
     disabled={true}
-    title="Team Name"
+    title="team-slug"
   >
     <LetterAvatar
-      displayName="Team Name"
+      displayName="team-slug"
       identifier="team-slug"
     />
   </Tooltip>
diff --git a/tests/js/spec/views/__snapshots__/organizationAccessRequests.spec.jsx.snap b/tests/js/spec/views/__snapshots__/organizationAccessRequests.spec.jsx.snap
index 5db5a21476..e02bdc4183 100644
--- a/tests/js/spec/views/__snapshots__/organizationAccessRequests.spec.jsx.snap
+++ b/tests/js/spec/views/__snapshots__/organizationAccessRequests.spec.jsx.snap
@@ -46,7 +46,8 @@ exports[`OrganizationAccessRequests renders list 1`] = `
           <strong
             key="2"
           >
-            Team Name
+            #
+            team-slug
           </strong>
           <span
             key="3"
