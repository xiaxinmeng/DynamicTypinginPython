commit cf8089270d141500e40253c9c4813a348409b38f
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Aug 24 11:52:53 2015 -0700

    Add varying levels to mocks

diff --git a/bin/load-mocks b/bin/load-mocks
index df3f6811f8..40f628c2a6 100755
--- a/bin/load-mocks
+++ b/bin/load-mocks
@@ -27,6 +27,14 @@ PLATFORMS = itertools.cycle([
     'javascript',
 ])
 
+LEVELS = itertools.cycle([
+    'error',
+    'error',
+    'error',
+    'fatal',
+    'warning',
+])
+
 
 def create_system_time_series():
     now = datetime.utcnow().replace(tzinfo=utc)
@@ -208,6 +216,7 @@ def main(num_events=1):
                     project=project,
                     platform=platform,
                     release=release.version,
+                    level=LEVELS.next(),
                     message='This is a mostly useless example %s exception' % platform,
                     checksum=md5(platform + str(_)).hexdigest(),
                 )
