commit 1b74c9762e0f8cc7f91d1a22a4b6910246a81dfd
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Mon Nov 30 10:36:29 2015 -0800

    Fill out the available base64 bits when generating secret-key
    
    At 40 bytes of data, base64 encoding has to fill with '=='
    
    >>> len(b64encode(os.urandom(42)))
    56
    >>> len(b64encode(os.urandom(40)))
    56

diff --git a/src/sentry/runner/settings.py b/src/sentry/runner/settings.py
index 42ee373410..2a23eeb078 100644
--- a/src/sentry/runner/settings.py
+++ b/src/sentry/runner/settings.py
@@ -56,7 +56,7 @@ def generate_settings():
     """
     from base64 import b64encode
     output = CONFIG_TEMPLATE % dict(
-        default_key=b64encode(os.urandom(40)),
+        default_key=b64encode(os.urandom(42)),
     )
     return output
 
