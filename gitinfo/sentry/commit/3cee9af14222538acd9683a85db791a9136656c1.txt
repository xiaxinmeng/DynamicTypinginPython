commit 3cee9af14222538acd9683a85db791a9136656c1
Author: Burak Yigit Kaya <byk@sentry.io>
Date:   Tue Jan 21 23:02:08 2020 +0300

    built(config): Depracate GITHUB_API settings (#16550)
    
    We already use and need the `github-app.client-*` settings from the `config.yml` file. This patch maps these to the legacy DJANGO configs and deprecates the DJANGO config values.
    
    Supercedes getsentry/onpremise#327.

diff --git a/src/sentry/conf/server.py b/src/sentry/conf/server.py
index f2a6c5f351..50a14d9bde 100644
--- a/src/sentry/conf/server.py
+++ b/src/sentry/conf/server.py
@@ -1471,6 +1471,9 @@ EMAIL_USE_TLS = DEAD
 SERVER_EMAIL = DEAD
 EMAIL_SUBJECT_PREFIX = DEAD
 
+GITHUB_APP_ID = DEAD
+GITHUB_API_SECRET = DEAD
+
 SUDO_URL = "sentry-sudo"
 
 
diff --git a/src/sentry/runner/initializer.py b/src/sentry/runner/initializer.py
index c703262f5f..f24c3df625 100644
--- a/src/sentry/runner/initializer.py
+++ b/src/sentry/runner/initializer.py
@@ -128,6 +128,8 @@ options_mapper = {
     "mail.use-tls": "EMAIL_USE_TLS",
     "mail.from": "SERVER_EMAIL",
     "mail.subject-prefix": "EMAIL_SUBJECT_PREFIX",
+    "github-app.client-id": "GITHUB_APP_ID",
+    "github-app.client-secret": "GITHUB_API_SECRET",
 }
 
 
