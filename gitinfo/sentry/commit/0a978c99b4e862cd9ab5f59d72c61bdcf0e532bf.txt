commit 0a978c99b4e862cd9ab5f59d72c61bdcf0e532bf
Author: David Cramer <dcramer@gmail.com>
Date:   Sun Oct 12 08:47:01 2014 -0700

    Avoid queries on project in event list

diff --git a/src/sentry/web/frontend/groups.py b/src/sentry/web/frontend/groups.py
index 8ee5fbb302..18272ae238 100644
--- a/src/sentry/web/frontend/groups.py
+++ b/src/sentry/web/frontend/groups.py
@@ -413,6 +413,9 @@ def group_event_list(request, team, project, group):
     # TODO: we need the event data to bind after we limit
     event_list = group.event_set.all().order_by('-datetime')[:100]
 
+    for event in event_list:
+        event.project = project
+
     Event.objects.bind_nodes(event_list, 'data')
 
     return render_with_group_context(group, 'sentry/groups/event_list.html', {
