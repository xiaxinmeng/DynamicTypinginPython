commit 46b3ad955a860fbc9ecfec5402cb092cfa472016
Author: Markus Unterwaditzer <markus@unterwaditzer.net>
Date:   Mon Jul 1 10:24:16 2019 +0200

    fix: Fix two broken doc links in UI (#13849)

diff --git a/src/sentry/static/sentry/app/views/organizationReleases/list/releaseProgress.jsx b/src/sentry/static/sentry/app/views/organizationReleases/list/releaseProgress.jsx
index 444c81614e..5c0c7f65d7 100644
--- a/src/sentry/static/sentry/app/views/organizationReleases/list/releaseProgress.jsx
+++ b/src/sentry/static/sentry/app/views/organizationReleases/list/releaseProgress.jsx
@@ -15,7 +15,7 @@ import ProgressBar from './progressBar';
 const STEPS = {
   tag: {
     desc: t('Tag an error'),
-    url: 'tag-errors',
+    url: 'configure-sdk',
     msg: 'knowing which errors were introduced in a release, ',
   },
   repo: {
@@ -173,7 +173,7 @@ class ReleaseProgress extends AsyncComponent {
               <span className="pull-right">
                 {t('Next step: ')}
                 <a
-                  href={`https://docs.sentry.io/learn/releases/#${nextStep.url}`}
+                  href={`https://docs.sentry.io/workflow/releases/#${nextStep.url}`}
                   onClick={() => this.recordAnalytics('next', {cta: nextStep.desc})}
                 >
                   {t(`${nextStep.desc}`)}
diff --git a/src/sentry/static/sentry/app/views/settings/project/projectReleaseTracking.jsx b/src/sentry/static/sentry/app/views/settings/project/projectReleaseTracking.jsx
index a40374eaf8..1b143c83fc 100644
--- a/src/sentry/static/sentry/app/views/settings/project/projectReleaseTracking.jsx
+++ b/src/sentry/static/sentry/app/views/settings/project/projectReleaseTracking.jsx
@@ -129,7 +129,7 @@ class ProjectReleaseTracking extends AsyncView {
                 'Start by binding the [release] attribute in your application, take a look at [link] to see how to configure this for the SDK you are using.',
                 {
                   link: (
-                    <a href="https://docs.sentry.io/workflow/releases/?platform=javascript#tag-errors">
+                    <a href="https://docs.sentry.io/workflow/releases/#configure-sdk">
                       our docs
                     </a>
                   ),
