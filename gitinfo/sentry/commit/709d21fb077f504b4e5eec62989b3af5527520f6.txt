commit 709d21fb077f504b4e5eec62989b3af5527520f6
Author: David Cramer <dcramer@gmail.com>
Date:   Wed May 18 15:35:15 2011 -0700

    Ensure we note that mod_wsgi requires a special configuration flag for HTTP Authorization headers (fixes #153)

diff --git a/docs/install/index.rst b/docs/install/index.rst
index 7c181541ab..59513bf404 100644
--- a/docs/install/index.rst
+++ b/docs/install/index.rst
@@ -69,6 +69,10 @@ Sentry to receive template debug information when it hits a syntax error.
 
 Finally, run ``python manage.py syncdb`` to create the database tables.
 
+.. note::
+
+   If you are using mod_wsgi/Apache, you will need to ensure that you set ``WSGIPassAuthorization On`` for the Sentry authentication to work.
+
 .. note::
 
    We recommend using South for migrations. Initial migrations have already been created for Sentry in sentry/migrations/ so you only need to run ``python manage.py migrate sentry`` instead of ``syncdb``
