commit d468cbb169c07991fcb777a7e72ee185d3e276c1
Author: James Cunningham <cunninghamjt09@gmail.com>
Date:   Fri Feb 1 12:03:48 2019 -0800

    fix(buffers): Remove the assertion that we can never decrease partition counts while in flight. (#11856)

diff --git a/src/sentry/buffer/redis.py b/src/sentry/buffer/redis.py
index 841ec721b4..1a5b0b17e2 100644
--- a/src/sentry/buffer/redis.py
+++ b/src/sentry/buffer/redis.py
@@ -96,7 +96,7 @@ class RedisBuffer(Buffer):
         """
         if partition is None:
             return self.pending_key
-        assert partition >= 0 and partition < self.pending_partitions
+        assert partition >= 0
         return '%s:%d' % (self.pending_key, partition)
 
     def _make_pending_key_from_key(self, key):
