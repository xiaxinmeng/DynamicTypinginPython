commit e31b7fd7ca6d617d97defe2b95da40e1abf31a29
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Mon Dec 7 22:48:11 2015 -0800

    Reraise a more useful error message for bad config

diff --git a/src/sentry/runner/importer.py b/src/sentry/runner/importer.py
index 7e4361332e..2afeaa81bc 100644
--- a/src/sentry/runner/importer.py
+++ b/src/sentry/runner/importer.py
@@ -43,7 +43,12 @@ class Importer(object):
             mod = self._load_module(fullname)
         except Exception as e:
             from sentry.utils.settings import reraise_as
-            reraise_as(ConfigurationError(unicode(e)))
+            msg = unicode(e)
+            if msg:
+                msg = '%s: %s' % (type(e).__name__, msg)
+            else:
+                msg = type(e).__name__
+            reraise_as(ConfigurationError(msg))
         else:
             # Install into sys.modules explicitly
             sys.modules[fullname] = mod
