commit 329dcb082743e624be87d8043dffb23da9e745b3
Author: Armin Ronacher <armin.ronacher@active-4.com>
Date:   Tue Mar 8 14:05:36 2016 +0100

    base32 now requires a try/except for bad input

diff --git a/src/sentry/models/group.py b/src/sentry/models/group.py
index 8a284c3940..397be902aa 100644
--- a/src/sentry/models/group.py
+++ b/src/sentry/models/group.py
@@ -60,10 +60,14 @@ class GroupManager(BaseManager):
             raise Group.DoesNotExist()
         callsign, id = match.groups()
         callsign = callsign.upper()
+        try:
+            short_id = base32_decode(id)
+        except ValueError:
+            raise Group.DoesNotExist()
         return Group.objects.get(
             project__organization=org,
             project__callsign=callsign.upper(),
-            short_id=base32_decode(id),
+            short_id=short_id,
         )
 
     def from_kwargs(self, project, **kwargs):
