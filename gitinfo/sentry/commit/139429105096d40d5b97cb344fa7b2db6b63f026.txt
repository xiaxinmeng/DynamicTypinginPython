commit 139429105096d40d5b97cb344fa7b2db6b63f026
Author: Evan Purkhiser <evanpurkhiser@gmail.com>
Date:   Mon Jul 30 16:14:59 2018 -0700

    ref(integrations): Add text to actions (#9245)

diff --git a/src/sentry/static/sentry/app/views/organizationIntegrations/index.jsx b/src/sentry/static/sentry/app/views/organizationIntegrations/index.jsx
index 1803dde193..9a98ffdb4d 100644
--- a/src/sentry/static/sentry/app/views/organizationIntegrations/index.jsx
+++ b/src/sentry/static/sentry/app/views/organizationIntegrations/index.jsx
@@ -122,7 +122,7 @@ export default class OrganizationIntegrations extends AsyncComponent {
             </Box>
             <Box>
               <Button icon="icon-circle-add" size="small" onClick={openModal}>
-                {t('Install')}
+                {installed.length > 0 ? t('Add Another') : t('Install')}
               </Button>
             </Box>
           </PanelItem>
diff --git a/src/sentry/static/sentry/app/views/organizationIntegrations/installedIntegration.jsx b/src/sentry/static/sentry/app/views/organizationIntegrations/installedIntegration.jsx
index 0b97e24036..bebd5929af 100644
--- a/src/sentry/static/sentry/app/views/organizationIntegrations/installedIntegration.jsx
+++ b/src/sentry/static/sentry/app/views/organizationIntegrations/installedIntegration.jsx
@@ -13,6 +13,12 @@ import Tooltip from 'app/components/tooltip';
 
 const CONFIGURABLE_FEATURES = ['commits'];
 
+const removeButton = (
+  <Button size="xsmall" icon="icon-trash">
+    Remove
+  </Button>
+);
+
 export default class InstalledIntegration extends React.Component {
   static propTypes = {
     orgId: PropTypes.string.isRequired,
@@ -66,7 +72,7 @@ export default class InstalledIntegration extends React.Component {
         priority="danger"
         onConfirm={() => this.props.onDisable(integration)}
       >
-        <Button size="xsmall" icon="icon-trash" />
+        {removeButton}
       </Confirm>
     );
   }
@@ -105,7 +111,7 @@ export default class InstalledIntegration extends React.Component {
         priority="danger"
         onConfirm={() => this.props.onRemove(integration)}
       >
-        <Button size="xsmall" icon="icon-trash" />
+        {removeButton}
       </Confirm>
     );
   }
@@ -114,46 +120,46 @@ export default class InstalledIntegration extends React.Component {
     const {integration, provider, orgId} = this.props;
 
     return (
-      <React.Fragment>
-        <PanelItem py={1} px={2} key={integration.id} align="center">
-          <Box flex={1}>
-            <IntegrationItem compact integration={integration} />
-          </Box>
-          <Box mr={1}>
-            {integration.status === 'disabled' && (
-              <AddIntegrationButton
-                size="xsmall"
-                priority="danger"
-                provider={provider}
-                integration={integration}
-                onAddIntegration={this.reinstallIntegration}
-                reinstall={true}
-              />
-            )}
-            {integration.status === 'active' && (
-              <Tooltip
-                disabled={this.hasConfiguration()}
-                tooltipOptions={{placement: 'left'}}
-                title="Integration not configurable"
-              >
-                <span>
-                  <Button
-                    size="xsmall"
-                    icon="icon-settings"
-                    disabled={!this.hasConfiguration()}
-                    to={`/settings/${orgId}/integrations/${provider.key}/${integration.id}/`}
-                  />
-                </span>
-              </Tooltip>
-            )}
-          </Box>
-          <Box>
-            {integration.status === 'active' && integration.provider.canDisable
-              ? this.renderDisableIntegration(integration)
-              : this.renderRemoveIntegration(integration)}
-          </Box>
-        </PanelItem>
-      </React.Fragment>
+      <PanelItem py={1} px={2} key={integration.id} align="center">
+        <Box flex={1}>
+          <IntegrationItem compact integration={integration} />
+        </Box>
+        <Box mr={1}>
+          {integration.status === 'disabled' && (
+            <AddIntegrationButton
+              size="xsmall"
+              priority="danger"
+              provider={provider}
+              integration={integration}
+              onAddIntegration={this.reinstallIntegration}
+              reinstall={true}
+            />
+          )}
+          {integration.status === 'active' && (
+            <Tooltip
+              disabled={this.hasConfiguration()}
+              tooltipOptions={{placement: 'left'}}
+              title="Integration not configurable"
+            >
+              <span>
+                <Button
+                  size="xsmall"
+                  icon="icon-settings"
+                  disabled={!this.hasConfiguration()}
+                  to={`/settings/${orgId}/integrations/${provider.key}/${integration.id}/`}
+                >
+                  Configure
+                </Button>
+              </span>
+            </Tooltip>
+          )}
+        </Box>
+        <Box>
+          {integration.status === 'active' && integration.provider.canDisable
+            ? this.renderDisableIntegration(integration)
+            : this.renderRemoveIntegration(integration)}
+        </Box>
+      </PanelItem>
     );
   }
 }
diff --git a/tests/js/spec/views/settings/organizationIntegrations/__snapshots__/index.spec.jsx.snap b/tests/js/spec/views/settings/organizationIntegrations/__snapshots__/index.spec.jsx.snap
index a06abbfecf..3fc62431bf 100644
--- a/tests/js/spec/views/settings/organizationIntegrations/__snapshots__/index.spec.jsx.snap
+++ b/tests/js/spec/views/settings/organizationIntegrations/__snapshots__/index.spec.jsx.snap
@@ -57,7 +57,7 @@ exports[`OrganizationIntegrations render() with installed integrations Displays
               onClick={[Function]}
               size="small"
             >
-              Install
+              Add Another
             </Button>
           </Box>
         </PanelItem>
@@ -157,7 +157,7 @@ exports[`OrganizationIntegrations render() with installed integrations Displays
               onClick={[Function]}
               size="small"
             >
-              Install
+              Add Another
             </Button>
           </Box>
         </PanelItem>
