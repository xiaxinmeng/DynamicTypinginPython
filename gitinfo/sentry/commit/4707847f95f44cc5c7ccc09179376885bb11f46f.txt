commit 4707847f95f44cc5c7ccc09179376885bb11f46f
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Feb 5 14:53:48 2015 -0800

    Show error state when tags fail to load

diff --git a/src/sentry/static/sentry/app/views/aggregateTags.jsx b/src/sentry/static/sentry/app/views/aggregateTags.jsx
index ad4a25662f..f3df1b7f7a 100644
--- a/src/sentry/static/sentry/app/views/aggregateTags.jsx
+++ b/src/sentry/static/sentry/app/views/aggregateTags.jsx
@@ -17,7 +17,8 @@ var AggregateTags = React.createClass({
   getInitialState() {
     return {
       tagList: null,
-      loading: true
+      loading: true,
+      error: false
     };
   },
 
@@ -30,21 +31,39 @@ var AggregateTags = React.createClass({
 
     this.setState({loading: true});
 
+    this.setState({
+      loading: true,
+      error: false
+    });
+
     api.request('/groups/' + params.aggregateId + '/tags/', {
       success: (data) => {
-        this.setState({tagList: data});
-      },
-      error: () => {
-        // TODO(dcramer):
+        this.setState({
+          tagList: data,
+          error: false,
+          loading: false
+        });
       },
-      complete: () => {
-        this.setState({loading: false});
+      error: (error) => {
+        this.setState({
+          error: true,
+          loading: false
+        });
       }
     });
   },
 
-
   render() {
+    if (this.state.loading) {
+      return <div className="loading"></div>;
+    } else if (this.state.error) {
+      return (
+        <div className="alert alert-error alert-block">
+          <p>There was an error loading data. <a onClick={this.fetchData}>Retry</a></p>
+        </div>
+      );
+    }
+
     var children = [];
 
     if (this.state.tagList) {
