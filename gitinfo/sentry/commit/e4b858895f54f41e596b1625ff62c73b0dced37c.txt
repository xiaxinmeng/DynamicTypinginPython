commit e4b858895f54f41e596b1625ff62c73b0dced37c
Author: Billy Vong <billyvg@users.noreply.github.com>
Date:   Fri Sep 27 15:04:37 2019 -0700

    tests(js): Suppress `console.error` in `<OrganizationContext>` tests (#14843)
    
    Suppress `console.error` messages in a test that is intended to test error state.

diff --git a/tests/js/spec/views/organizationContext.spec.jsx b/tests/js/spec/views/organizationContext.spec.jsx
index 1ea844b39f..f105e20a90 100644
--- a/tests/js/spec/views/organizationContext.spec.jsx
+++ b/tests/js/spec/views/organizationContext.spec.jsx
@@ -121,12 +121,14 @@ describe('OrganizationContext', function() {
       url: '/organizations/org-slug/',
       statusCode: 403,
     });
+    console.error = jest.fn(); // eslint-disable-line no-console
     wrapper = createWrapper();
 
     await tick();
     wrapper.update();
 
     expect(wrapper.find('LoadingError')).toHaveLength(1);
+    console.error.mockRestore(); // eslint-disable-line no-console
   });
 
   it('opens sudo modal for superusers on 403s', async function() {
