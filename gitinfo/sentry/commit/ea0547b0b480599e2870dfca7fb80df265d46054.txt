commit ea0547b0b480599e2870dfca7fb80df265d46054
Author: Billy Vong <billyvg@users.noreply.github.com>
Date:   Tue Oct 29 09:59:52 2019 -0700

    ref(ts): Refactor `app/utils/environment` (#15307)
    
    Refactor functions in `app/utils/environment` to typescript

diff --git a/src/sentry/static/sentry/app/utils/environment.jsx b/src/sentry/static/sentry/app/utils/environment.tsx
similarity index 64%
rename from src/sentry/static/sentry/app/utils/environment.jsx
rename to src/sentry/static/sentry/app/utils/environment.tsx
index b014640e1b..711062938c 100644
--- a/src/sentry/static/sentry/app/utils/environment.jsx
+++ b/src/sentry/static/sentry/app/utils/environment.tsx
@@ -1,12 +1,13 @@
+import {Environment} from 'app/types';
 import {toTitleCase} from 'app/utils';
 
 const DEFAULT_EMPTY_ROUTING_NAME = 'none';
 const DEFAULT_EMPTY_ENV_NAME = '(No Environment)';
 
-export function getUrlRoutingName(env) {
+export function getUrlRoutingName(env: Environment) {
   return encodeURIComponent(env.name) || DEFAULT_EMPTY_ROUTING_NAME;
 }
 
-export function getDisplayName(env) {
+export function getDisplayName(env: Environment) {
   return toTitleCase(env.name) || DEFAULT_EMPTY_ENV_NAME;
 }
