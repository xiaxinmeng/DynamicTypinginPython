commit 9088b5468955cfde35b7fa700fc4ebfd2769b04d
Author: Ben Vinegar <ben@benv.ca>
Date:   Fri Apr 29 17:21:28 2016 -0700

    Add crumb duration, fix message bleeding over timestamp

diff --git a/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbComponents/default.jsx b/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbComponents/default.jsx
index df4c77ce91..293a25d052 100644
--- a/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbComponents/default.jsx
+++ b/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbComponents/default.jsx
@@ -3,6 +3,7 @@ import moment from 'moment';
 import _ from 'underscore';
 
 import Classifier from './classifier';
+import Duration from '../../../duration';
 
 const DefaultCrumbComponent = React.createClass({
   propTypes: {
@@ -42,7 +43,12 @@ const DefaultCrumbComponent = React.createClass({
                 {message &&
                   <pre>
                     <code>{message}</code>
-                    <Classifier value={crumb.classifier} hideIfEmpty={true}/>
+                    {crumb.classifier && <Classifier value={crumb.classifier}/>}
+                    {crumb.duration &&
+                      <span className="timing">
+                        [<Duration seconds={crumb.duration}/>]
+                      </span>
+                    }
                   </pre>
                 }
               </td>
diff --git a/src/sentry/static/sentry/less/group-detail.less b/src/sentry/static/sentry/less/group-detail.less
index 6d1587801b..8672c12663 100644
--- a/src/sentry/static/sentry/less/group-detail.less
+++ b/src/sentry/static/sentry/less/group-detail.less
@@ -1580,7 +1580,8 @@ ul.crumbs {
       right: 20px
     }
 
-    span.crumb-classifier {
+    span.crumb-classifier,
+    span.timing {
       padding-left: 6px;
       color: @gray;
       font-weight: normal;
@@ -1598,7 +1599,7 @@ ul.crumbs {
       }
 
       td {
-        padding: 2px 0 !important;
+        padding: 2px 60px 2px 0 !important;
         font-weight: normal;
       }
 
@@ -1664,11 +1665,6 @@ ul.crumbs {
         padding-top: 4px;
       }
 
-      span.timing {
-        margin-left: 10px;
-        font-size: 90%;
-      }
-
       code {
         padding-left: 0;
         white-space: pre-wrap;
