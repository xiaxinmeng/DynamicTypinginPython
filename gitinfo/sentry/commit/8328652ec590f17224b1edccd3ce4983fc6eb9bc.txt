commit 8328652ec590f17224b1edccd3ce4983fc6eb9bc
Author: Mark Story <mark@sentry.io>
Date:   Fri Nov 1 15:22:13 2019 -0400

    fix(discover2) Don't offer ungraphable aliases. (#15393)
    
    These fields are 'aggregates' but don't produce useful graphs.

diff --git a/src/sentry/static/sentry/app/views/eventsV2/events.tsx b/src/sentry/static/sentry/app/views/eventsV2/events.tsx
index fc0b7b3701..132bbff1f4 100644
--- a/src/sentry/static/sentry/app/views/eventsV2/events.tsx
+++ b/src/sentry/static/sentry/app/views/eventsV2/events.tsx
@@ -87,6 +87,10 @@ export default class Events extends React.Component<EventsProps> {
     const yAxisOptions = uniqBy(
       eventView
         .getAggregateFields()
+        // Exclude last_seen and latest_event as they don't produce useful graphs.
+        .filter(
+          (field: Field) => ['last_seen', 'latest_event'].includes(field.field) === false
+        )
         .map((field: Field) => {
           return {label: field.title, value: field.field};
         })
