commit a804d3f727cc22a3ddf5c03623b25bc395016637
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Feb 4 17:28:51 2015 -0800

    Fix query construction

diff --git a/src/sentry/static/sentry/app/api.jsx b/src/sentry/static/sentry/app/api.jsx
index 4a39a0ecc5..79e33e4820 100644
--- a/src/sentry/static/sentry/app/api.jsx
+++ b/src/sentry/static/sentry/app/api.jsx
@@ -30,8 +30,17 @@ class Client {
       data = JSON.stringify(data);
     }
 
+    var fullUrl = this.baseUrl + path;
+    if (query) {
+      if (fullUrl.indexOf('?') !== -1) {
+        fullUrl += '&' + query;
+      } else {
+        fullUrl += '?' + query;
+      }
+    }
+
     $.ajax({
-      url: this.baseUrl + path + "?" + query,
+      url: fullUrl,
       method: method,
       data: data,
       contentType: 'application/json',
