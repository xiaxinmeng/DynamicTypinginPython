commit dd3cfcb1b3172b3baee6e4080b760b0c0ae3edba
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Fri Oct 2 13:34:52 2015 -0700

    Remove another unnecessary use of a transaction savepoint

diff --git a/src/sentry/features/manager.py b/src/sentry/features/manager.py
index 8449211c6e..e1926ef1dd 100644
--- a/src/sentry/features/manager.py
+++ b/src/sentry/features/manager.py
@@ -43,7 +43,9 @@ class FeatureManager(object):
 
     def _get_plugin_value(self, feature, actor):
         for plugin in plugins.all(version=2):
-            for handler in (safe_execute(plugin.get_feature_hooks) or ()):
+            handlers = safe_execute(plugin.get_feature_hooks,
+                                    _with_transaction=False)
+            for handler in handlers or ():
                 rv = handler(feature, actor)
                 if rv is not None:
                     return rv
