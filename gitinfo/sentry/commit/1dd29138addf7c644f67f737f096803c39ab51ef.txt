commit 1dd29138addf7c644f67f737f096803c39ab51ef
Author: ted kaemming <ted@kaemming.com>
Date:   Mon Oct 17 14:45:49 2016 -0700

    Fix vertical alignment of avatar column in note email. (#4377)
    
    Also makes debug message view for note email random text.

diff --git a/src/sentry/templates/sentry/emails/email-styles.html b/src/sentry/templates/sentry/emails/email-styles.html
index afae1d4137..842fe4ae42 100644
--- a/src/sentry/templates/sentry/emails/email-styles.html
+++ b/src/sentry/templates/sentry/emails/email-styles.html
@@ -467,6 +467,7 @@
   }
   .avatar-column {
     width: 64px;
+    vertical-align: top;
   }
   .notch-column {
     width: 7px;
diff --git a/src/sentry/web/frontend/debug/debug_note_email.py b/src/sentry/web/frontend/debug/debug_note_email.py
index 807ca94724..8a17888765 100644
--- a/src/sentry/web/frontend/debug/debug_note_email.py
+++ b/src/sentry/web/frontend/debug/debug_note_email.py
@@ -2,15 +2,22 @@ from __future__ import absolute_import
 
 from sentry.models import Activity
 
-from .mail import ActivityMailDebugView
+from .mail import ActivityMailDebugView, get_random, make_message
 
 
 class DebugNoteEmailView(ActivityMailDebugView):
     def get_activity(self, request, event):
+        random = get_random(request)
         return {
             'type': Activity.NOTE,
             'user': request.user,
             'data': {
-                'text': 'This is an example note!',
+                'text': make_message(
+                    random,
+                    max(
+                        2,
+                        int(random.weibullvariate(12, 0.4)),
+                    ),
+                )
             },
         }
