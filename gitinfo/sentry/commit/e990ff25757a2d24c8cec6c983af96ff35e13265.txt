commit e990ff25757a2d24c8cec6c983af96ff35e13265
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Feb 11 12:21:15 2015 -0800

    Add URL prefix to api overview

diff --git a/src/sentry/api/views/help_index.py b/src/sentry/api/views/help_index.py
index 7d556e54aa..ac9550eecd 100644
--- a/src/sentry/api/views/help_index.py
+++ b/src/sentry/api/views/help_index.py
@@ -1,5 +1,7 @@
 from __future__ import absolute_import
 
+from django.conf import settings
+
 from sentry.api.views.help_base import ApiHelpBase
 from sentry.web.helpers import render_to_response
 
@@ -11,7 +13,8 @@ class ApiHelpIndexView(ApiHelpBase):
         prefix = '/api/0/'
 
         context = {
-            'section_list': self.get_sections(prefix)
+            'section_list': self.get_sections(prefix),
+            'SENTRY_URL_PREFIX': settings.SENTRY_URL_PREFIX,
         }
 
         return render_to_response('sentry/help/api_index.html', context, request)
diff --git a/src/sentry/templates/sentry/help/api_index.html b/src/sentry/templates/sentry/help/api_index.html
index bc261e344e..cf179e1697 100644
--- a/src/sentry/templates/sentry/help/api_index.html
+++ b/src/sentry/templates/sentry/help/api_index.html
@@ -22,7 +22,7 @@
 
     <p>As an example, to get information about the project which your key is bound to, you might make a request like so:</p>
 
-    <pre>curl -u PUBLIC_KEY:SECRET_KEY /api/0/projects/{project_id}/</pre>
+    <pre>curl -u PUBLIC_KEY:SECRET_KEY {{ SENTRY_URL_PREFIX }}/api/0/projects/{project_id}/</pre>
 
     <p>Note: Currently requests are limited to project-scoped endpoints only.</p>
 
