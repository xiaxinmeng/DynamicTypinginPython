commit 8bbeba14b67af365a285947a052131cdd255a4c9
Author: Armin Ronacher <armin.ronacher@active-4.com>
Date:   Thu Mar 23 14:11:53 2017 +0100

    Fixed existing_app

diff --git a/src/sentry/models/dsymfile.py b/src/sentry/models/dsymfile.py
index 8d681bfbe4..a2afa9c523 100644
--- a/src/sentry/models/dsymfile.py
+++ b/src/sentry/models/dsymfile.py
@@ -60,8 +60,8 @@ class DSymAppManager(BaseManager):
     def create_or_update(self, sync_id, app_id, project, data=None, platform=DSymPlatform.GENERIC):
         if data is None:
             data = {}
-        existing_app = DSymApp.objects.filter(app_id=app_id, project=project)
-        if existing_app:
+        existing_app = DSymApp.objects.filter(app_id=app_id, project=project).first()
+        if existing_app is not None:
             now = timezone.now()
             existing_app.update(
                 sync_id=sync_id,
