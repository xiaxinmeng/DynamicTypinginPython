commit 93f26d810a9617609f700cd64df6777274f6d18a
Author: ckj <chriskjennings@gmail.com>
Date:   Tue Mar 14 14:30:50 2017 -0700

    Link project name and expected to be release text

diff --git a/src/sentry/templates/sentry/emails/activity/release.html b/src/sentry/templates/sentry/emails/activity/release.html
index 449b00b664..6896694803 100644
--- a/src/sentry/templates/sentry/emails/activity/release.html
+++ b/src/sentry/templates/sentry/emails/activity/release.html
@@ -21,9 +21,11 @@
           {% for project, release_link, resolved_issue_count in projects %}
             <tr>
               <td>
-                <h5 style="margin-bottom: 0">{{ project.name }}</h5>
+                <h5 style="margin-bottom: 0">
+                  <a href="{{ release_link }}">{{ project.name }}</a>
+                </h5>
                 <p class="muted" style="margin-bottom: 0">
-                  {{ resolved_issue_count }} Issue{{ resolved_issue_count|pluralize }} expected to be resolved
+                  <a href="{{ release_link }}">{{ resolved_issue_count }} Issue{{ resolved_issue_count|pluralize }} expected to be resolved</a>
                 </p>
               </td>
               <td style="text-align: right">
diff --git a/src/sentry/templates/sentry/emails/email-styles.html b/src/sentry/templates/sentry/emails/email-styles.html
index 10869fa14e..3fcad4a6b1 100644
--- a/src/sentry/templates/sentry/emails/email-styles.html
+++ b/src/sentry/templates/sentry/emails/email-styles.html
@@ -733,6 +733,25 @@
     border-radius: 3px;
   }
 
+  /*
+    ===
+    Release / Deploy email specific styles
+    ===
+  */
+
+  /* Project list */
+
+  .inner table.table.projects h5 a {
+    color: inherit;
+    font-weight: 600;
+  }
+
+  .inner table.table.projects p a {
+    color: inherit;
+  }
+
+  /* Commit list */
+
   .inner table.table.commit-list h4 {
     font-size: 15px;
     margin: 2px 0;
