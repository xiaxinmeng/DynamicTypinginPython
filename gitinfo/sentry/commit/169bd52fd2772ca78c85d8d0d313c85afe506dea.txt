commit 169bd52fd2772ca78c85d8d0d313c85afe506dea
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Dec 26 16:44:57 2012 -0800

    Revert change to restrict what tags are visible on a group details page

diff --git a/src/sentry/models.py b/src/sentry/models.py
index 348fc148c0..815ea623b7 100644
--- a/src/sentry/models.py
+++ b/src/sentry/models.py
@@ -477,11 +477,7 @@ class Group(MessageBase):
 
     def get_tags(self):
         if not hasattr(self, '_tag_cache'):
-            tags = sorted(
-                t for t in
-                self.messagefiltervalue_set.values_list('key', flat=True).distinct()
-                if t in self.project.get_tags()
-            )
+            tags = sorted(self.messagefiltervalue_set.values_list('key', flat=True).distinct())
             self._tag_cache = tags
         return self._tag_cache
 
diff --git a/src/sentry/templates/sentry/groups/tag_list.html b/src/sentry/templates/sentry/groups/tag_list.html
index 61c8a5b3c6..5b5fd992ce 100644
--- a/src/sentry/templates/sentry/groups/tag_list.html
+++ b/src/sentry/templates/sentry/groups/tag_list.html
@@ -4,13 +4,6 @@
 {% load sentry_helpers %}
 
 {% block inner %}
-    <div class="alert alert-block alert-info">
-        <p>
-            {% url sentry-manage-project-tags group.project.slug as link %}
-            {% blocktrans %}You can change what tags you care about in your <a href="{{ link }}">project settings</a>.{% endblocktrans %}
-        </p>
-    </div>
-
     {% if tag_list %}
 
         {% for tag_name, tag_values in tag_list %}
