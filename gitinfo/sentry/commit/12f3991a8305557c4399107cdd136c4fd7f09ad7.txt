commit 12f3991a8305557c4399107cdd136c4fd7f09ad7
Author: Armin Ronacher <armin.ronacher@active-4.com>
Date:   Thu Mar 29 20:00:50 2012 +0100

    Added support back for exceptions in detail pages

diff --git a/sentry/interfaces.py b/sentry/interfaces.py
index a31188ec3b..02b6979cea 100644
--- a/sentry/interfaces.py
+++ b/sentry/interfaces.py
@@ -285,11 +285,16 @@ class Exception(Interface):
         return output
 
     def to_html(self, event):
+        last_frame = None
+        interface = event.interfaces.get('sentry.interfaces.Stacktrace')
+        if interface is not None and interface.frames:
+            last_frame = interface.frames[-1]
         return render_to_string('sentry/partial/interfaces/exception.html', {
             'event': event,
             'exception_value': self.value,
             'exception_type': self.type,
             'exception_module': self.module,
+            'last_frame':   last_frame
         })
 
     def get_search_context(self, event):
diff --git a/sentry/models.py b/sentry/models.py
index 2656dfb45f..656119005d 100644
--- a/sentry/models.py
+++ b/sentry/models.py
@@ -740,4 +740,4 @@ post_save.connect(
     sender=Group,
     dispatch_uid="update_document",
     weak=False,
-)
\ No newline at end of file
+)
diff --git a/sentry/templates/sentry/partial/interfaces/exception.html b/sentry/templates/sentry/partial/interfaces/exception.html
index 9029e1f5b6..00bef3dad5 100644
--- a/sentry/templates/sentry/partial/interfaces/exception.html
+++ b/sentry/templates/sentry/partial/interfaces/exception.html
@@ -1,23 +1,24 @@
-{% comment %}
 <div id="exception" class="module">
   <h2>Exception</h2>
   <div class="module-content">
   <table class="table meta">
+    <colgroup>
+      <col style="width:100px;">
+    </colgroup>
     <tr>
-      <th>Exception Type:</th>
+      <th>Type:</th>
       <td><pre>{{ exception_type|escape }}</pre></td>
     </tr>
     <tr>
-      <th>Exception Value:</th>
+      <th>Value:</th>
       <td><pre>{{ exception_value|escape }}</pre></td>
     </tr>
-    {% if lastframe %}
+    {% if last_frame %}
     <tr>
-      <th>Exception Location:</th>
-      <td>{{ lastframe.filename|escape }} in {{ lastframe.function|escape }}, line {{ lastframe.lineno }}</td>
+      <th>Location:</th>
+      <td>{{ last_frame.filename|escape }} in {{ last_frame.function|escape }}, line {{ last_frame.lineno }}</td>
     </tr>
     {% endif %}
   </table>
   </div>
 </div>
-{% endcomment %}
\ No newline at end of file
