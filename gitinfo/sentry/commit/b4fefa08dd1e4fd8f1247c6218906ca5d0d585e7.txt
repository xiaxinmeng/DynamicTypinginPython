commit b4fefa08dd1e4fd8f1247c6218906ca5d0d585e7
Author: ckj <chriskjennings@gmail.com>
Date:   Mon Dec 7 18:00:37 2015 -0800

    Add bg color and pattern to setup wizard

diff --git a/src/sentry/static/sentry/app/views/installWizard.jsx b/src/sentry/static/sentry/app/views/installWizard.jsx
index ce1966d4f1..0d9c57df72 100644
--- a/src/sentry/static/sentry/app/views/installWizard.jsx
+++ b/src/sentry/static/sentry/app/views/installWizard.jsx
@@ -97,6 +97,11 @@ const InstallWizard = React.createClass({
 
   componentWillMount() {
     this.fetchData();
+    jQuery(document.body).addClass('install-wizard');
+  },
+
+  componentWillUnmount() {
+    jQuery(document.body).removeClass('install-wizard');
   },
 
   remountComponent() {
@@ -161,33 +166,32 @@ const InstallWizard = React.createClass({
     return (
       <DocumentTitle title="Sentry Setup">
         <div className="app">
-          <div className="container">
-            <div className="setup-wizard">
-              <h1>
-                <span>{t('Welcome to Sentry')}</span>
-                <small>{version.current}</small>
-              </h1>
-              {loading ?
-                <LoadingIndicator>
-                  Please wait while we load configuration.
-                </LoadingIndicator>
-              : (error ?
-                <div className="loading-error">
-                  <span className="icon" />
-                  {t('We were unable to load the required configuration from the Sentry server. Please take a look at the service logs.')}
-                </div>
-              :
-                <div>
-                  {submitError &&
-                    <p>{t('We were unable to submit your changes to the Sentry server. Please take a look at the service logs.')}</p>
-                  }
-                  <InstallWizardSettings
-                      options={options}
-                      onSubmit={this.onSubmit}
-                      formDisabled={submitInProgress} />
-                </div>
-              )}
-            </div>
+          <div className="pattern" />
+          <div className="setup-wizard">
+            <h1>
+              <span>{t('Welcome to Sentry')}</span>
+              <small>{version.current}</small>
+            </h1>
+            {loading ?
+              <LoadingIndicator>
+                Please wait while we load configuration.
+              </LoadingIndicator>
+            : (error ?
+              <div className="loading-error">
+                <span className="icon" />
+                {t('We were unable to load the required configuration from the Sentry server. Please take a look at the service logs.')}
+              </div>
+            :
+              <div>
+                {submitError &&
+                  <p>{t('We were unable to submit your changes to the Sentry server. Please take a look at the service logs.')}</p>
+                }
+                <InstallWizardSettings
+                    options={options}
+                    onSubmit={this.onSubmit}
+                    formDisabled={submitInProgress} />
+              </div>
+            )}
           </div>
         </div>
       </DocumentTitle>
diff --git a/src/sentry/static/sentry/images/sentry-pattern.png b/src/sentry/static/sentry/images/sentry-pattern.png
new file mode 100644
index 0000000000..1f7312b5f6
Binary files /dev/null and b/src/sentry/static/sentry/images/sentry-pattern.png differ
diff --git a/src/sentry/static/sentry/less/layout.less b/src/sentry/static/sentry/less/layout.less
index a69923fe24..655dff2100 100644
--- a/src/sentry/static/sentry/less/layout.less
+++ b/src/sentry/static/sentry/less/layout.less
@@ -1,3 +1,7 @@
+html {
+  height: 100%;
+}
+
 body {
   margin: 0;
   font-family: @font-family-base;
diff --git a/src/sentry/static/sentry/less/setup-wizard.less b/src/sentry/static/sentry/less/setup-wizard.less
index 9e263a22ba..4a26512800 100644
--- a/src/sentry/static/sentry/less/setup-wizard.less
+++ b/src/sentry/static/sentry/less/setup-wizard.less
@@ -1,10 +1,37 @@
+/**
+* body.install-wizard
+* ============================================================================
+*/
+
+body.install-wizard {
+  #gradient > .horizontal(#9f73c3 , #6875be);
+  height: 100%;
+
+  .pattern {
+    position: fixed;
+    top: 0;
+    right: 0;
+    bottom: 0;
+    left: 0;
+    background: url(../images/sentry-pattern.png);
+    background-size: 400px;
+    opacity: .8;
+  }
+}
+
+
 .setup-wizard {
   .clearfix;
-  max-width: 600px;
-  margin: 30px auto;
-  border: 1px solid @trim;
-  padding: 20px 20px 0;
+  background: #fff;
+  width: 600px;
+  padding: 40px 40px 20px;
   border-radius: 4px;
+  box-shadow: 0 5px 30px rgba(0,0,0, .3);
+  position: absolute;
+  top: 50%;
+  left: 50%;
+  margin-left: -300px;
+  margin-top: -250px;
 
   h1 {
     .clearfix;
diff --git a/src/sentry/static/sentry/less/shared-components.less b/src/sentry/static/sentry/less/shared-components.less
index 4c4b6b7c99..9810a4eb5a 100644
--- a/src/sentry/static/sentry/less/shared-components.less
+++ b/src/sentry/static/sentry/less/shared-components.less
@@ -601,10 +601,10 @@ table.integrations {
 
   .loading-indicator {
     text-indent: -9999em;
-    border-top: 6px solid rgba(52, 60, 69, .09);
-    border-right: 6px solid rgba(52, 60, 69, .09);
-    border-bottom: 6px solid rgba(52, 60, 69, .09);
-    border-left: 6px solid rgba(52, 60, 69, .4);
+    border-top: 6px solid @white-dark;
+    border-right: 6px solid @white-dark;
+    border-bottom: 6px solid @white-dark;
+    border-left: 6px solid @gray-lightest;
     -webkit-animation: loading .5s infinite linear;
     animation: loading .55s infinite linear;
     border-radius: 50%;
