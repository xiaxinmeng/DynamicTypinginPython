commit 8b2f9c1f8da2ba142677d3301efcdb9221e31d67
Author: Ben Vinegar <ben@benv.ca>
Date:   Mon Sep 26 17:18:47 2016 -0700

    Clicking 'home' should hide other panels

diff --git a/src/sentry/static/sentry/app/components/issueList.jsx b/src/sentry/static/sentry/app/components/issueList.jsx
index 0c6c1411ce..905284583c 100644
--- a/src/sentry/static/sentry/app/components/issueList.jsx
+++ b/src/sentry/static/sentry/app/components/issueList.jsx
@@ -43,6 +43,9 @@ const IssueList = React.createClass({
   componentWillReceiveProps(nextProps) {
     let location = this.props.location;
     let nextLocation = nextProps.location;
+    if (!location)
+      return;
+
     if (location.pathname != nextLocation.pathname || location.search != nextLocation.search) {
       this.remountComponent();
     }
diff --git a/src/sentry/static/sentry/app/components/sidebar/index.jsx b/src/sentry/static/sentry/app/components/sidebar/index.jsx
index 124fbec1e8..639872a437 100644
--- a/src/sentry/static/sentry/app/components/sidebar/index.jsx
+++ b/src/sentry/static/sentry/app/components/sidebar/index.jsx
@@ -166,7 +166,7 @@ const Sidebar = React.createClass({
       {/* Top nav links */}
       <ul className="navbar-nav divider-bottom">
         <li>
-          <Link to={`/${org.slug}/`}>
+          <Link to={`/${org.slug}/`} onClick={()=>this.hidePanel()}>
             <span className="icon icon-home" />
           </Link>
         </li>
