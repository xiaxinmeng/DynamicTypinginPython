commit 45c91ca771768f9708691b209016d9bd8e5d0105
Author: David Cramer <dcramer@gmail.com>
Date:   Fri Oct 30 14:24:09 2015 -0700

    Expand broadcasts UI

diff --git a/src/sentry/static/sentry/app/components/header/broadcasts.jsx b/src/sentry/static/sentry/app/components/header/broadcasts.jsx
index 7d180b13c8..f1d4f27f53 100644
--- a/src/sentry/static/sentry/app/components/header/broadcasts.jsx
+++ b/src/sentry/static/sentry/app/components/header/broadcasts.jsx
@@ -102,20 +102,17 @@ var Broadcasts = React.createClass({
           <li className="empty">No recent broadcasts from the Sentry team.</li>
         :
           broadcasts.map((item) => {
-
-            if(item.link) {
-              return (
-                <li key={item.id} className={!item.hasSeen && 'unseen'}>
-                  <a href="{item.link}">{item.message}</a>
-                </li>
-              );
-            } else {
-              return (
-                <li key={item.id} className={!item.hasSeen && 'unseen'}>
-                  {item.message}
-                </li>
-              );
-            }
+            return (
+              <li key={item.id} className={!item.hasSeen && 'unseen'}>
+                {item.title &&
+                  <h4>{item.title}</h4>
+                }
+                {item.message}
+                {item.link &&
+                  <a href={item.link} className="read-more">Read more</a>
+                }
+              </li>
+            );
           })
         )}
       </DropdownLink>
diff --git a/src/sentry/static/sentry/less/components/broadcasts.less b/src/sentry/static/sentry/less/components/broadcasts.less
index 51320abe62..4a5bcbb887 100644
--- a/src/sentry/static/sentry/less/components/broadcasts.less
+++ b/src/sentry/static/sentry/less/components/broadcasts.less
@@ -29,18 +29,29 @@
     // ideally we could use 90% but due to parent element
     // this doesnt work
     max-height: 500px;
-    min-width: 240px;
+    width: 240px;
     padding-top: 0;
     padding-bottom: 0;
     overflow: auto;
 
     > li {
       color: @gray-dark;
-      padding: 5px 8px;
+      padding: 10px;
       border-bottom: 1px solid #e5e5e5;
       margin-bottom: 0;
       line-height: 1.3;
 
+      h4 {
+        font-size: 16px;
+        font-weight: 600;
+        margin-bottom: 10px;
+      }
+
+      .read-more {
+        display: block;
+        margin-top: 10px;
+      }
+
       a {
         color: @linkColor;
 
