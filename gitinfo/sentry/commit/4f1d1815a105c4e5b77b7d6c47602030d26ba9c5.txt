commit 4f1d1815a105c4e5b77b7d6c47602030d26ba9c5
Author: David Cramer <dcramer@gmail.com>
Date:   Sun Aug 4 14:10:45 2013 -0700

    Remove excess logger statement and correct load call

diff --git a/src/sentry/tasks/fetch_source.py b/src/sentry/tasks/fetch_source.py
index a9e5fb6347..97ef4a1e0c 100644
--- a/src/sentry/tasks/fetch_source.py
+++ b/src/sentry/tasks/fetch_source.py
@@ -136,10 +136,9 @@ def fetch_sourcemap(url):
     if body.startswith(")]}'"):
         body = body.split('\n', 1)[1]
     try:
-        index = sourcemap.load(body)
+        index = sourcemap.loads(body)
     except (JSONDecodeError, ValueError):
-        if logger:
-            logger.warning('Failed parsing sourcemap JSON: %r', body[:15], exc_info=True)
+        pass
     else:
         return index
 
