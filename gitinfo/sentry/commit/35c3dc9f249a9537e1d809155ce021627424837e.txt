commit 35c3dc9f249a9537e1d809155ce021627424837e
Author: filesnate <61479559+filesnate@users.noreply.github.com>
Date:   Wed Feb 26 09:34:54 2020 -0700

    ref(docker): Move entrypoint out of $SENTRY_CONF (#17292)
    
    docker-entrypoint.sh file is not a configuration file, so by moving it out, we can mount a volume onto $SENTRY_CONF containing the custom configuration files.
    
    Co-authored-by: Burak Yigit Kaya <byk@sentry.io>

diff --git a/docker/Dockerfile b/docker/Dockerfile
index 1451d67efe..cbef4bf99a 100644
--- a/docker/Dockerfile
+++ b/docker/Dockerfile
@@ -188,12 +188,13 @@ RUN set -x \
 COPY --from=sdist /dist/*.whl /tmp/dist/
 RUN pip install /tmp/dist/*.whl
 
-COPY ./docker/docker-entrypoint.sh ./docker/sentry.conf.py ./docker/config.yml $SENTRY_CONF/
+COPY ./docker/sentry.conf.py ./docker/config.yml $SENTRY_CONF/
+COPY ./docker/docker-entrypoint.sh /
 
 EXPOSE 9000
 VOLUME /data
 
-ENTRYPOINT exec $SENTRY_CONF/docker-entrypoint.sh $0 $@
+ENTRYPOINT exec /docker-entrypoint.sh $0 $@
 CMD ["run", "web"]
 
 ARG SOURCE_COMMIT
