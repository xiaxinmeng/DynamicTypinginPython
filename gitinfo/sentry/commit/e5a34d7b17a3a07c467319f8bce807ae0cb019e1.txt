commit e5a34d7b17a3a07c467319f8bce807ae0cb019e1
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Feb 14 16:30:24 2013 -0800

    Helps if we commit templates

diff --git a/src/sentry/templates/sentry/partial/client_config/sidekiq.html b/src/sentry/templates/sentry/partial/client_config/sidekiq.html
new file mode 100644
index 0000000000..cac630aac3
--- /dev/null
+++ b/src/sentry/templates/sentry/partial/client_config/sidekiq.html
@@ -0,0 +1,15 @@
+{% extends "sentry/partial/client_config/ruby_base.html" %}
+
+{% load i18n %}
+
+{% block inner %}
+    <p>{% trans "Configure the client within your application:" %}</p>
+
+    <pre>require 'raven'
+
+Raven.configure do |config|
+  config.dsn = '{% if dsn %}{{ dsn }}{% else %}<strong>SENTRY_DSN</strong>{% endif %}'
+end</pre>
+
+    <p>{% trans "That's it! Raven automatically appends middleware to pipe all uncaught exceptions to Sentry." %}</p>
+{% endblock %}
diff --git a/src/sentry/templates/sentry/partial/client_config/sinatra.html b/src/sentry/templates/sentry/partial/client_config/sinatra.html
new file mode 100644
index 0000000000..e2f25d2d2e
--- /dev/null
+++ b/src/sentry/templates/sentry/partial/client_config/sinatra.html
@@ -0,0 +1,21 @@
+{% extends "sentry/partial/client_config/ruby_base.html" %}
+
+{% load i18n %}
+
+{% block inner %}
+    <p>{% trans "Configure the client as and install the Rack middleware:" %}</p>
+
+    <pre>require 'sinatra'
+require 'raven'
+
+Raven.configure do |config|
+  config.dsn = '{% if dsn %}{{ dsn }}{% else %}<strong>SENTRY_DSN</strong>{% endif %}'
+end
+
+use Raven::Rack
+
+get '/' do
+  1 / 0
+end</pre>
+    <p>{% trans "That's it! Raven automatically installs an error handling hook to pipe all uncaught exceptions to Sentry." %}</p>
+{% endblock %}
