commit e3cd6d5481d6fbe19c658a3969a56e07bce279e1
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Jan 15 16:30:39 2015 -0800

    Various additions to assignee selector

diff --git a/src/sentry/static/sentry/app/components/assigneeSelector.jsx b/src/sentry/static/sentry/app/components/assigneeSelector.jsx
index a527ef713d..4cf310dce7 100644
--- a/src/sentry/static/sentry/app/components/assigneeSelector.jsx
+++ b/src/sentry/static/sentry/app/components/assigneeSelector.jsx
@@ -21,14 +21,14 @@ var AssigneeSelector = React.createClass({
     var memberNodes = [];
     this.props.memberList.forEach(function(item){
       memberNodes.push(
-        <li>
-          <a onClick={this.onAssignTo.bind(this, item)}>
+        <li key={item.id}>
+          <a onClick={this.props.onAssignTo.bind(this, item)}>
             <img src={item.avatarUrl} className="avatar" />
             {item.name || item.email}
           </a>
         </li>
       );
-    });
+    }.bind(this));
 
     return (
       <div className={className}>
diff --git a/src/sentry/static/sentry/app/utils/collection.jsx b/src/sentry/static/sentry/app/utils/collection.jsx
index 146ed77dc9..2fe8d87b90 100644
--- a/src/sentry/static/sentry/app/utils/collection.jsx
+++ b/src/sentry/static/sentry/app/utils/collection.jsx
@@ -12,19 +12,19 @@ function Collection(collection, options) {
 
   Array.call(this);
 
-  if (options === undefined) {
+  if (typeof options === "undefined") {
     options = {};
   }
 
   for (i in defaults) {
-    if (options[i] === undefined) {
+    if (typeof options[i] !== "undefined") {
       options[i] = defaults[i];
     }
   }
 
   this.options = options;
 
-  if (collection !== undefined) {
+  if (typeof collection !== "undefined") {
     this.push(collection);
   }
 
diff --git a/src/sentry/templates/sentry/groups/group_list.html b/src/sentry/templates/sentry/groups/group_list.html
index af377d1fdf..85fa1d8a07 100644
--- a/src/sentry/templates/sentry/groups/group_list.html
+++ b/src/sentry/templates/sentry/groups/group_list.html
@@ -10,7 +10,7 @@
       React.render(React.createFactory(Stream)({
         aggList: {% serialize event_list %},
         project: {% serialize project %},
-        memberList: [],
+        memberList: {% serialize member_list %},
         initialQuery: {% convert_to_json query %},
         pageLinks: {% convert_to_json page_links %}
       }), document.getElementById('blk_stream'));
diff --git a/src/sentry/web/frontend/groups.py b/src/sentry/web/frontend/groups.py
index 5ca71b288d..14b7ac0ec8 100644
--- a/src/sentry/web/frontend/groups.py
+++ b/src/sentry/web/frontend/groups.py
@@ -141,16 +141,22 @@ def group_list(request, organization, project):
         params=params,
         user=request.user,
     )
-
     event_list = response.data
     page_links = response['Link']
     query = params['query']
 
+    response = client.get(
+        path='/projects/{}/members/'.format(project.id),
+        user=request.user,
+    )
+    member_list = response.data
+
     return render_to_response('sentry/groups/group_list.html', {
         'team': project.team,
         'organization': organization,
         'project': project,
         'event_list': event_list,
+        'member_list': member_list,
         'page_links': page_links,
         'query': query,
         'SORT_OPTIONS': SORT_OPTIONS,
