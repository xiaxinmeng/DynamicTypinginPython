commit 116b6045339c8ec1ca500b8f0c582909853899a0
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Mar 23 12:21:07 2015 -0700

    More friendly JSON output for stacktraces

diff --git a/src/sentry/interfaces/stacktrace.py b/src/sentry/interfaces/stacktrace.py
index 03e5404734..fcd96618f9 100644
--- a/src/sentry/interfaces/stacktrace.py
+++ b/src/sentry/interfaces/stacktrace.py
@@ -195,6 +195,26 @@ class Frame(Interface):
 
         return cls(**kwargs)
 
+    def to_json(self):
+        return {
+            'filename': self.filename,
+            'absPath': self.abs_path,
+            'module': self.module,
+            'function': self.function,
+            'context': get_context(
+                lineno=self.lineno,
+                context_line=self.context_line,
+                pre_context=self.pre_context,
+                post_context=self.post_context,
+                filename=self.filename or self.module,
+            ),
+            'lineNo': self.lineno,
+            'colNo': self.colno,
+            'inApp': self.in_app,
+            'errors': self.errors,
+
+        }
+
     def is_url(self):
         if not self.abs_path:
             return False
@@ -436,7 +456,7 @@ class Stacktrace(Interface):
     def to_json(self):
         return {
             'frames': [f.to_json() for f in self.frames],
-            'frames_omitted': self.frames_omitted,
+            'framesOmitted': self.frames_omitted,
         }
 
     def get_path(self):
