commit 6dd9ba4168c9a7a17648d48a2a60c55fcec2fae3
Author: David Cramer <dcramer@gmail.com>
Date:   Sat Jun 7 09:24:18 2014 -0400

    Always remove cookie header

diff --git a/src/sentry/interfaces/http.py b/src/sentry/interfaces/http.py
index c762351705..aebe66adeb 100644
--- a/src/sentry/interfaces/http.py
+++ b/src/sentry/interfaces/http.py
@@ -99,8 +99,11 @@ class Http(Interface):
         headers = data.get('headers')
         if headers:
             headers = format_headers(headers)
-            if 'Cookie' in headers and not cookies:
-                cookies = headers.pop('Cookie')
+            if 'Cookie' in headers:
+                if not cookies:
+                    cookies = headers.pop('Cookie')
+                else:
+                    del headers['Cookie']
             headers = trim_dict(headers)
         else:
             headers = {}
