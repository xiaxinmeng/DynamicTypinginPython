commit 7df3c2e6bc328557b5e1613dfeeaf30aa4f58b8c
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Dec 11 13:23:49 2012 -0800

    Correct validation logic in notification settings

diff --git a/src/sentry/web/frontend/accounts.py b/src/sentry/web/frontend/accounts.py
index 4698633d9b..293fe6d613 100644
--- a/src/sentry/web/frontend/accounts.py
+++ b/src/sentry/web/frontend/accounts.py
@@ -169,7 +169,7 @@ def notification_settings(request):
     ] + forms
 
     if request.POST:
-        if all(f.is_valid() for f, h in forms):
+        if all(f.is_valid() for f in forms):
             for form in forms:
                 form.save()
             messages.add_message(request, messages.SUCCESS, 'Your settings were saved.')
