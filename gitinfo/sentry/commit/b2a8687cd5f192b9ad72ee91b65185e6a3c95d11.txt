commit b2a8687cd5f192b9ad72ee91b65185e6a3c95d11
Author: Matej Minar <matej.minar@sentry.io>
Date:   Thu Jan 30 13:41:24 2020 +0100

    ref(ts): Convert <DebugFileFeature> to typescript (#16691)

diff --git a/src/sentry/static/sentry/app/components/debugFileFeature.jsx b/src/sentry/static/sentry/app/components/debugFileFeature.tsx
similarity index 83%
rename from src/sentry/static/sentry/app/components/debugFileFeature.jsx
rename to src/sentry/static/sentry/app/components/debugFileFeature.tsx
index 8cea629f16..d2385a04f6 100644
--- a/src/sentry/static/sentry/app/components/debugFileFeature.jsx
+++ b/src/sentry/static/sentry/app/components/debugFileFeature.tsx
@@ -22,8 +22,13 @@ const FEATURE_TOOLTIPS = {
   ),
 };
 
-function DebugFileFeature({available, feature}) {
-  let icon = null;
+type Props = {
+  feature: 'symtab' | 'debug' | 'unwind' | 'sources';
+  available?: true;
+};
+
+const DebugFileFeature = ({available, feature}: Props) => {
+  let icon: React.ReactNode = null;
 
   if (available === true) {
     icon = <Icon type="success" src="icon-checkmark-sm" />;
@@ -39,14 +44,14 @@ function DebugFileFeature({available, feature}) {
       </Tag>
     </Tooltip>
   );
-}
+};
 
 DebugFileFeature.propTypes = {
   available: PropTypes.bool,
   feature: PropTypes.oneOf(Object.keys(FEATURE_TOOLTIPS)).isRequired,
 };
 
-const Icon = styled(InlineSvg)`
+const Icon = styled(InlineSvg)<{type: 'error' | 'success'}>`
   color: ${p => p.theme.alert[p.type].iconColor};
   margin-right: 1ex;
 `;
