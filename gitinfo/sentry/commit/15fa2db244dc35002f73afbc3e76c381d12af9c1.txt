commit 15fa2db244dc35002f73afbc3e76c381d12af9c1
Author: Daniel Griesser <daniel.griesser.86@gmail.com>
Date:   Thu Nov 10 15:20:13 2016 +0100

    Only change device model leave model_id out

diff --git a/src/sentry/static/sentry/app/utils/deviceNameMapper.jsx b/src/sentry/static/sentry/app/utils/deviceNameMapper.jsx
index 62c40309fa..7a3d6cc6f4 100644
--- a/src/sentry/static/sentry/app/utils/deviceNameMapper.jsx
+++ b/src/sentry/static/sentry/app/utils/deviceNameMapper.jsx
@@ -63,5 +63,9 @@ export default function(model) {
     'Watch1,1': 'Apple Watch',
     'Watch1,2': 'Apple Watch',
   };
-  return deviceLookUpTable[model] === undefined ? model : deviceLookUpTable[model];
+
+  const modelIdentifier = model.split(' ')[0];
+  const modelId = model.split(' ').splice(1).join(' ');
+  const modelName = deviceLookUpTable[modelIdentifier];
+  return modelName === undefined ? model : modelName + ' ' + modelId;
 }
