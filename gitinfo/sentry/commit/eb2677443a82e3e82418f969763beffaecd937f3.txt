commit eb2677443a82e3e82418f969763beffaecd937f3
Author: ckj <chriskjennings@gmail.com>
Date:   Fri Oct 14 13:20:11 2016 -0700

    Fix issue error message rendering

diff --git a/src/sentry/static/sentry/app/components/events/errors.jsx b/src/sentry/static/sentry/app/components/events/errors.jsx
index ab60badf36..bd0df7280f 100644
--- a/src/sentry/static/sentry/app/components/events/errors.jsx
+++ b/src/sentry/static/sentry/app/components/events/errors.jsx
@@ -38,6 +38,7 @@ const EventErrors = React.createClass({
           event={this.props.event}
           type="errors"
           className="errors">
+          <span className="icon icon-alert" />
         <p>
           <a className="pull-right" onClick={this.toggle}>{isOpen ? t('Hide') : t('Show')}</a>
           {
diff --git a/src/sentry/static/sentry/app/views/groupEventDetails.jsx b/src/sentry/static/sentry/app/views/groupEventDetails.jsx
index 41bf288f43..572b73543d 100644
--- a/src/sentry/static/sentry/app/views/groupEventDetails.jsx
+++ b/src/sentry/static/sentry/app/views/groupEventDetails.jsx
@@ -89,7 +89,7 @@ const GroupEventDetails = React.createClass({
                   orgId={params.orgId}
                   projectId={params.projectId} />
             }
-            {group.status &&
+            {group.status != 'unresolved' &&
               <div className="issue-status">
                 {group.status === 'ignored' &&
                   <MutedBox statusDetails={group.statusDetails} />
diff --git a/src/sentry/static/sentry/less/group-detail.less b/src/sentry/static/sentry/less/group-detail.less
index fd99f8a15c..e7af208acf 100644
--- a/src/sentry/static/sentry/less/group-detail.less
+++ b/src/sentry/static/sentry/less/group-detail.less
@@ -1794,10 +1794,20 @@ div.commands {
 }
 
 .box.errors {
-  border-color: #E7C0BC;
-  background: #FDF6F5;
-  color: rgba(0,0,0, .70);
-  font-size: 15px;
+  border-top: 1px solid @alert-danger-border-color !important;
+  background: @alert-danger-bg-color;
+  margin-top: -1px !important;
+  padding-top: 15px !important;
+  font-size: 14px;
+  position: relative;
+  .clearfix;
+
+  .icon-alert {
+    position: absolute;
+    top: 17px;
+    font-size: 16px;
+    color: @red;
+  }
 
   .box-content {
     padding: 10px 20px;
@@ -1811,8 +1821,9 @@ div.commands {
     }
 
     p {
-      margin: 0;
+      margin: 0 0 13px;
       line-height: 1.5;
+      padding-left: 26px;
 
       .btn {
         margin-left: 5px;
@@ -1820,12 +1831,13 @@ div.commands {
     }
 
     ul {
-      border-top: 1px solid #EFE0E1;
+      border-top: 1px solid lighten(@alert-danger-border-color, 4);
       margin: 10px 0 0;
-      padding: 10px 0 0 20px;
+      padding: 10px 0 10px 20px;
 
       li {
         margin-bottom: 5px;
+        word-break: break-word;
       }
     }
 
@@ -1835,6 +1847,10 @@ div.commands {
       margin: 5px 0 0;
     }
   }
+
+  &+ .box, &+ .context-summary {
+    border-top: 1px solid lighten(@alert-danger-border-color, 4);
+  }
 }
 
 
