commit 238f7ba1b8c3c56dc7f1be56814ff2babd06cec9
Author: David Cramer <cramer@dropbox.com>
Date:   Thu Oct 9 14:27:19 2014 -0700

    Add culprit and message to event json data

diff --git a/src/sentry/models/event.py b/src/sentry/models/event.py
index 7bc700e291..870147cf3c 100644
--- a/src/sentry/models/event.py
+++ b/src/sentry/models/event.py
@@ -156,6 +156,8 @@ class Event(Model):
         # We use a SortedDict to keep elements ordered for a potential JSON serializer
         data = SortedDict()
         data['id'] = self.event_id
+        data['culprit'] = self.group.culprit
+        data['message'] = self.message
         data['checksum'] = self.checksum
         data['project'] = self.project.slug
         data['datetime'] = self.datetime
