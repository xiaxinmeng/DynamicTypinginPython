commit 37a9661ba2b45768e9d597df65d685bc2ffa7ded
Author: Steve Pulec <steve@yipit.com>
Date:   Mon Sep 26 20:48:13 2011 -0400

    Adding signal for regression bugs

diff --git a/sentry/signals.py b/sentry/signals.py
new file mode 100644
index 0000000000..46152fa8a1
--- /dev/null
+++ b/sentry/signals.py
@@ -0,0 +1,3 @@
+import django
+
+regression_signal = django.dispatch.Signal(providing_args=["instance"])
diff --git a/sentry/utils/manager.py b/sentry/utils/manager.py
index f73e2ce5e8..a075df2d88 100644
--- a/sentry/utils/manager.py
+++ b/sentry/utils/manager.py
@@ -15,6 +15,7 @@ from django.db import models
 from django.db.models import signals, Sum, F
 
 from sentry.conf import settings
+from sentry.signals import regression_signal
 from sentry.utils import construct_checksum, get_db_engine, should_mail
 from sentry.utils.charts import has_charts
 from sentry.utils.compat.db import connections
@@ -209,6 +210,7 @@ class SentryManager(models.Manager):
                 warnings.warn(u'Unable to process log entry: %s' % (exc,))
         else:
             if mail and should_mail(group):
+                regression_signal.send(sender=GroupedMessage, instance=group)
                 group.mail_admins()
 
             return instance
