commit 857cb0f33a1bba7fe19d99b54d2e6e3aa287c4b0
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Apr 1 18:44:40 2019 -0700

    ref: Bind SENTRY_DEBUGGER only when its set to None

diff --git a/src/sentry/conf/server.py b/src/sentry/conf/server.py
index 83d9a3deac..b8fc351c80 100644
--- a/src/sentry/conf/server.py
+++ b/src/sentry/conf/server.py
@@ -892,7 +892,7 @@ SENTRY_FEATURES = {
 SENTRY_DEFAULT_TIME_ZONE = 'UTC'
 
 # Enable the Sentry Debugger (Beta)
-SENTRY_DEBUGGER = DEBUG
+SENTRY_DEBUGGER = None
 
 SENTRY_IGNORE_EXCEPTIONS = ('OperationalError', )
 
diff --git a/src/sentry/runner/initializer.py b/src/sentry/runner/initializer.py
index ef952e7cd7..b7d668b6f8 100644
--- a/src/sentry/runner/initializer.py
+++ b/src/sentry/runner/initializer.py
@@ -306,6 +306,9 @@ def initialize_app(config, skip_service_validation=False):
         version=settings.ASSET_VERSION,
     )
 
+    if getattr(settings, 'SENTRY_DEBUGGER', None) is None:
+        settings.SENTRY_DEBUGGER = settings.DEBUG
+
     import django
     if hasattr(django, 'setup'):
         # support for Django 1.7+
