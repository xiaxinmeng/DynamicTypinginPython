commit 9af6bd857211f6281dafdda7d9ec15879c0703c4
Author: ckj <chriskjennings@gmail.com>
Date:   Thu Jun 11 14:48:23 2015 -0400

    Fix activity rendering issues

diff --git a/src/sentry/static/sentry/app/views/groupActivity.jsx b/src/sentry/static/sentry/app/views/groupActivity.jsx
index efa6177159..95c85e73bd 100644
--- a/src/sentry/static/sentry/app/views/groupActivity.jsx
+++ b/src/sentry/static/sentry/app/views/groupActivity.jsx
@@ -178,7 +178,11 @@ var GroupActivity = React.createClass({
       } else {
         return (
           <li className="activity-item" key={itemIdx}>
-            {avatar} <span className="activity-author">{authorName}</span> {label} <TimeSince date={item.dateCreated} />
+            {avatar}
+            <TimeSince date={item.dateCreated} />
+            <div className="activity-item-content">
+              <span className="activity-author">{authorName}</span> {label}
+            </div>
           </li>
         );
       }
diff --git a/src/sentry/static/sentry/less/group-detail.less b/src/sentry/static/sentry/less/group-detail.less
index 25d47af0d8..1ef82ad58c 100644
--- a/src/sentry/static/sentry/less/group-detail.less
+++ b/src/sentry/static/sentry/less/group-detail.less
@@ -342,13 +342,15 @@
     margin-bottom: 0;
 
     .activity-note {
+      border-top: 1px solid lighten(@trim, 9);
       position: relative;
       margin-bottom: 15px;
+      padding-top: 15px;
 
       .avatar {
         position: absolute;
         left: 0;
-        top: 0;
+        top: 15px;
         .square(32px);
       }
 
@@ -403,23 +405,22 @@
     }
 
     .activity-item {
-      padding: 15px 0;
-      border-top: 1px solid lighten(@trim, 6);
+      padding: 10px 0;
+      border-top: 1px solid lighten(@trim, 9);
       position: relative;
       font-size: 13px;
-      color: lighten(@gray, 20);
+      color: lighten(@gray, 10);
 
       .avatar {
         width: 20px;
         height: 20px;
-        display: inline-block;
+        position: absolute;
         background-size: 20px 20px;
         background-repeat: no-repeat;
         background-position: center center;
         border-radius: 2px;
-        position: relative;
-        top: -1px;
-        margin-right: 4px;
+        top: 10px;
+        left: 0;
 
         &.sentry {
           color: @gray;
@@ -434,7 +435,12 @@
 
       .activity-author {
         font-weight: 600;
-        color: lighten(@gray, 10);
+        color: @gray;
+      }
+
+      .activity-item-content {
+        padding-left: 42px;
+        margin-right: 100px;
       }
     }
   }
