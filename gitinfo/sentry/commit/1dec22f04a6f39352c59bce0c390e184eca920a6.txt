commit 1dec22f04a6f39352c59bce0c390e184eca920a6
Author: David Cramer <dcramer@gmail.com>
Date:   Thu May 19 16:20:58 2011 -0700

    Ensure we show the same details on a messages page as we do on the group page (fixes GH-155)

diff --git a/sentry/templates/sentry/group/message.html b/sentry/templates/sentry/group/message.html
index f57d2059bd..2b7185dbb4 100644
--- a/sentry/templates/sentry/group/message.html
+++ b/sentry/templates/sentry/group/message.html
@@ -29,20 +29,19 @@
                 <span class="last_seen">{{ message.datetime|timesince }}</span>
             </p>
         </li>
-        
     </ul>
 
-    {% if traceback %}
-
-        <h2>Traceback</h2>
+    {% if message.has_two_part_message %}
+        <h2>{% trans "Full Message" %}</h2>
+        <pre id="full-message">{{ message.message }}</pre>
+    {% endif %}
 
+    {% if traceback %}
         {{ traceback }}
-
     {% endif %}
 
     <div id="requestinfo">
-        <h2>{% trans "More Information" %}</h2>
-
+        <h2>{% trans "Additional Data" %}</h2>
         <table>
             {% for k, v in json_data %}
                 <tr>
@@ -67,7 +66,7 @@
                                 </tbody>
                             </table>
                         {% else %}
-                            {{ v|pprint }}
+                            <pre>{{ v|pprint }}</pre>
                         {% endif %}
                     {% else %}
                         <p>{% blocktrans with k as varname %}No data for {{ varname }}{% endblocktrans %}</p>
