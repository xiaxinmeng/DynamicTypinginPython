commit 737efe1368c7d2becf88c18c795fd80718371578
Author: ckj <chriskjennings@gmail.com>
Date:   Thu May 28 18:57:09 2015 -0400

    Add dummy org avatar to org selector

diff --git a/src/sentry/static/sentry/app/components/organizationSidebar.jsx b/src/sentry/static/sentry/app/components/organizationSidebar.jsx
index 69d9c94564..29af6885f1 100644
--- a/src/sentry/static/sentry/app/components/organizationSidebar.jsx
+++ b/src/sentry/static/sentry/app/components/organizationSidebar.jsx
@@ -32,9 +32,12 @@ var OrganizationSelector = React.createClass({
           onClose={this.onDropdownClose}
           title={activeOrg.name}>
         {this.getOrganizationList().map((org) => {
+          var iconStyle = {
+            backgroundImage: 'url(https://github.com/getsentry.png)' //TODO(dcramer) use actual org avatar
+          };
           return (
-            <MenuItem key={org.slug} to="organizationDetails" params={{orgId: org.slug}}>
-              {org.name}
+            <MenuItem key={org.slug} to="organizationDetails" params={{orgId: org.slug}} iconUrl="http://github.com/getsentry.png">
+              <span className="org-avatar" style={iconStyle}></span> {org.name}
             </MenuItem>
           );
         })}
diff --git a/src/sentry/static/sentry/less/shared-components.less b/src/sentry/static/sentry/less/shared-components.less
index 42f505bad5..c68461b862 100644
--- a/src/sentry/static/sentry/less/shared-components.less
+++ b/src/sentry/static/sentry/less/shared-components.less
@@ -652,11 +652,40 @@ table.integrations {
     top: 62px;
     width: @sidebar-width - 10;
     margin: 0 5px;
-    padding: 10px 0;
+    padding: 0 0 5px;
+
+    .divider {
+      margin: 5px 0 0;
+    }
+
+    li {
+      margin: 5px 0 0;
+      font-size: 16px;
+
+      a {
+        padding: 6px 10px 6px 46px;
+        position: relative;
+      }
+
+      &:last-child {
+        a {
+          padding-left: 15px;
+        }
+      }
+    }
 
     &:after {
       right: 15px !important;
     }
+
+    .org-avatar {
+      position: absolute;
+      top: 5px;
+      left: 15px;
+      background-size: cover;
+      .square(22px);
+      border-radius: 2px;
+    }
   }
 
   .orgs {
@@ -678,12 +707,6 @@ table.integrations {
       }
     }
   }
-
-  &.open {
-    .dropdown-toggle {
-      color: @blue;
-    }
-  }
 }
 
 /**
