commit ed8eed5b986d959a34bbc824592a4d1d60f4c2c9
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Thu Mar 29 12:09:40 2018 -0700

    Revert "feat(buffers): Move process_pending task to it's own queue (#7848)"
    
    This reverts commit c1b195ad1f2bcfefa0127242da368aa6d80f02c8.

diff --git a/src/sentry/conf/server.py b/src/sentry/conf/server.py
index 64c569d99b..1d6aa9cc9f 100644
--- a/src/sentry/conf/server.py
+++ b/src/sentry/conf/server.py
@@ -441,7 +441,6 @@ CELERY_QUEUES = [
     Queue('alerts', routing_key='alerts'),
     Queue('auth', routing_key='auth'),
     Queue('assemble', routing_key='assemble'),
-    Queue('buffers.process_pending', routing_key='buffers.process_pending'),
     Queue('commits', routing_key='commits'),
     Queue('cleanup', routing_key='cleanup'),
     Queue('default', routing_key='default'),
@@ -522,7 +521,7 @@ CELERYBEAT_SCHEDULE = {
         'schedule': timedelta(seconds=10),
         'options': {
             'expires': 10,
-            'queue': 'buffers.process_pending',
+            'queue': 'counters-0',
         }
     },
     'sync-options': {
diff --git a/src/sentry/queue/routers.py b/src/sentry/queue/routers.py
index e5c81ec6f6..2319f200b3 100644
--- a/src/sentry/queue/routers.py
+++ b/src/sentry/queue/routers.py
@@ -7,6 +7,7 @@ from celery import current_app
 
 COUNTER_TASKS = set(
     [
+        'sentry.tasks.process_buffer.process_pending',
         'sentry.tasks.process_buffer.process_incr',
     ]
 )
diff --git a/src/sentry/tasks/process_buffer.py b/src/sentry/tasks/process_buffer.py
index 1fb4d7ed13..75b28dfdf5 100644
--- a/src/sentry/tasks/process_buffer.py
+++ b/src/sentry/tasks/process_buffer.py
@@ -16,10 +16,7 @@ from sentry.utils.locking import UnableToAcquireLock
 logger = logging.getLogger(__name__)
 
 
-@instrumented_task(
-    name='sentry.tasks.process_buffer.process_pending',
-    queue='buffers.process_pending',
-)
+@instrumented_task(name='sentry.tasks.process_buffer.process_pending')
 def process_pending(partition=None):
     """
     Process pending buffers.
