commit e2888c8a43ef1d8e8ec3527b5572c0acbc542894
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Jul 2 15:23:57 2015 -0700

    _is_sudo => _sudo

diff --git a/src/sentry/api/client.py b/src/sentry/api/client.py
index a93b000a1e..07f0a53f71 100644
--- a/src/sentry/api/client.py
+++ b/src/sentry/api/client.py
@@ -30,7 +30,7 @@ class ApiClientHandler(ForceAuthClientHandler):
     def get_response(self, request):
         # This is the simplest place we can hook into to patch the
         # request object.
-        request._is_sudo = self.is_sudo
+        request._sudo = self.is_sudo
         return super(ApiClientHandler, self).get_response(request)
 
 
