commit 40a9cc82d1b0b9537e43dc4c9205e99afd47b344
Author: Evan Purkhiser <evanpurkhiser@gmail.com>
Date:   Mon May 4 12:40:04 2020 -0700

    feat(ui): Allow smart search dropdown to scroll active items into view (#18595)

diff --git a/src/sentry/static/sentry/app/components/smartSearchBar/searchDropdown.tsx b/src/sentry/static/sentry/app/components/smartSearchBar/searchDropdown.tsx
index 430167d316..343bcd2203 100644
--- a/src/sentry/static/sentry/app/components/smartSearchBar/searchDropdown.tsx
+++ b/src/sentry/static/sentry/app/components/smartSearchBar/searchDropdown.tsx
@@ -66,6 +66,7 @@ class SearchDropdown extends React.PureComponent<Props> {
       className={item.active ? 'active' : undefined}
       data-test-id="search-autocomplete-item"
       onClick={this.props.onClick.bind(this, item.value, item)}
+      ref={element => item.active && element?.scrollIntoView?.({block: 'nearest'})}
     >
       <SearchItemTitleWrapper>
         {item.title && item.title + ' · '}
@@ -170,6 +171,7 @@ const SearchItemsList = styled('ul')`
 `;
 
 const SearchListItem = styled(ListItem)`
+  scroll-margin: 40px 0;
   font-size: ${p => p.theme.fontSizeLarge};
   padding: ${space(1)} ${space(2)};
   cursor: pointer;
