commit 2ecdd2feb18ef23610e55242b70b64ce0d6f6fe9
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Jan 15 11:23:28 2013 -0800

    Raise an import error when import_string fails on get_buffer

diff --git a/src/sentry/app.py b/src/sentry/app.py
index 3b6c4329bc..befef7e463 100644
--- a/src/sentry/app.py
+++ b/src/sentry/app.py
@@ -17,6 +17,8 @@ class State(local):
 
 def get_buffer(path, options):
     cls = import_string(path)
+    if cls is None:
+        raise ImportError('Unable to find module %s' % path)
     return cls(**options)
 
 buffer = get_buffer(settings.BUFFER, settings.BUFFER_OPTIONS)
