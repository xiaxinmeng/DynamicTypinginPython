commit 581d90460850ae6e1a444c16fe3871f392f592d0
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Jan 17 18:21:21 2013 -0800

    Add colno to frame spec

diff --git a/src/sentry/interfaces.py b/src/sentry/interfaces.py
index 0b50a55237..03dcc23d3a 100644
--- a/src/sentry/interfaces.py
+++ b/src/sentry/interfaces.py
@@ -209,7 +209,9 @@ class Stacktrace(Interface):
     The following additional attributes are supported:
 
     ``lineno``
-      The lineno of the call
+      The line number of the call
+    ``colno``
+      The column number of the call
     ``abs_path``
       The absolute path to filename
     ``function``
@@ -262,6 +264,10 @@ class Stacktrace(Interface):
             if 'lineno' in frame:
                 frame['lineno'] = int(frame['lineno'])
 
+            # colno should be an int
+            if 'colno' in frame:
+                frame['colno'] = int(frame['colno'])
+
             # in_app should be a boolean
             if 'in_app' in frame:
                 frame['in_app'] = bool(frame['in_app'])
