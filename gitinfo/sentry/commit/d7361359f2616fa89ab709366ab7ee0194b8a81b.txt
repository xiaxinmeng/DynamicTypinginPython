commit d7361359f2616fa89ab709366ab7ee0194b8a81b
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Aug 27 13:24:47 2015 -0700

    prefix form keys

diff --git a/src/sentry/web/frontend/auth_login.py b/src/sentry/web/frontend/auth_login.py
index 90f1a87dd6..9bfcfbba81 100644
--- a/src/sentry/web/frontend/auth_login.py
+++ b/src/sentry/web/frontend/auth_login.py
@@ -42,6 +42,7 @@ class AuthLoginView(BaseView):
         op = request.POST.get('op')
         return AuthenticationForm(
             request, request.POST if op == 'login' else None,
+            prefix='login-',
             captcha=bool(request.session.get('needs_captcha')),
         )
 
@@ -49,6 +50,7 @@ class AuthLoginView(BaseView):
         op = request.POST.get('op')
         return RegistrationForm(
             request.POST if op == 'register' else None,
+            prefix='register-',
             captcha=bool(request.session.get('needs_captcha')),
         )
 
diff --git a/src/sentry/web/frontend/auth_organization_login.py b/src/sentry/web/frontend/auth_organization_login.py
index 67cde016fc..7b8e39ba23 100644
--- a/src/sentry/web/frontend/auth_organization_login.py
+++ b/src/sentry/web/frontend/auth_organization_login.py
@@ -21,6 +21,7 @@ class AuthOrganizationLoginView(BaseView):
         op = request.POST.get('op')
         return AuthenticationForm(
             request, request.POST if op == 'login' else None,
+            prefix='login-',
             captcha=bool(request.session.get('needs_captcha')),
         )
 
@@ -28,6 +29,7 @@ class AuthOrganizationLoginView(BaseView):
         op = request.POST.get('op')
         return RegistrationForm(
             request.POST if op == 'register' else None,
+            prefix='register-',
             captcha=bool(request.session.get('needs_captcha')),
         )
 
