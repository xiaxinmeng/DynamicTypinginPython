commit a0a0eab421fbc01636c44ee09b9e129c6abadf75
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Fri Mar 25 10:38:02 2016 -0700

    devserver reload workers much faster
    
    We don't care about being graceful locally. This default timeout is 60
    seconds, so let's show no mercy and forcefully restart process after 2
    seconds.

diff --git a/src/sentry/runner/commands/devserver.py b/src/sentry/runner/commands/devserver.py
index 825fef0c75..e8c56e24ea 100644
--- a/src/sentry/runner/commands/devserver.py
+++ b/src/sentry/runner/commands/devserver.py
@@ -34,7 +34,10 @@ def devserver(reload, watchers, workers, bind):
         # Make sure we don't try and use uwsgi protocol
         'protocol': 'http',
         # A better log-format for local dev
-        'log-format': '"%(method) %(uri) %(proto)" %(status) %(size)'
+        'log-format': '"%(method) %(uri) %(proto)" %(status) %(size)',
+        # Make sure we reload really quickly for local dev in case it
+        # doesn't want to shut down nicely on it's own, NO MERCY
+        'worker-reload-mercy': 2,
     }
 
     if reload:
