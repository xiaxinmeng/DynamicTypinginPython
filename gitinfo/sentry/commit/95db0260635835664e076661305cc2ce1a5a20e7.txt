commit 95db0260635835664e076661305cc2ce1a5a20e7
Author: Evan Purkhiser <evanpurkhiser@gmail.com>
Date:   Thu Oct 25 17:38:21 2018 -0700

    style(hook): Format Hook component comment (#10278)

diff --git a/src/sentry/static/sentry/app/components/hook.jsx b/src/sentry/static/sentry/app/components/hook.jsx
index 14c11ea62f..a377ecad78 100644
--- a/src/sentry/static/sentry/app/components/hook.jsx
+++ b/src/sentry/static/sentry/app/components/hook.jsx
@@ -7,18 +7,16 @@ import HookStore from 'app/stores/hookStore';
 /**
  * Instead of accessing the HookStore directly, use this.
  *
- * If the hook slot needs to perform anything w/ the hooks, you can pass a function as a child and you will receive an object with a `hooks` key
+ * If the hook slot needs to perform anything w/ the hooks, you can pass a
+ * function as a child and you will receive an object with a `hooks` key
  *
- * example:
- *
- * <Hook name="my-hook">
- * {({hooks}) => hooks.map(hook => (
- *  <Wrapper>
- *    {hook}
- * </Wrapper>
- * ))}
- * </Hook>
+ * Example:
  *
+ *   <Hook name="my-hook">
+ *     {({hooks}) => hooks.map(hook => (
+ *       <Wrapper>{hook}</Wrapper>
+ *     ))}
+ *   </Hook>
  */
 const Hook = createReactClass({
   displayName: 'Hook',
