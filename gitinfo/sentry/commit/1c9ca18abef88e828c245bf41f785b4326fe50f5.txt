commit 1c9ca18abef88e828c245bf41f785b4326fe50f5
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Nov 10 11:55:30 2015 -0800

    Disable pruning of unused files

diff --git a/src/sentry/models/file.py b/src/sentry/models/file.py
index 7bfe3ddd9f..b8fc601b52 100644
--- a/src/sentry/models/file.py
+++ b/src/sentry/models/file.py
@@ -169,10 +169,11 @@ class File(Model):
             )
 
             # if this blob already existed, lets kill the duplicate
-            if self.path != blob.path:
-                get_storage_class(self.storage)(
-                    **self.storage_options
-                ).delete(self.path)
+            # TODO(dcramer): kill data when fully migrated
+            # if self.path != blob.path:
+            #     get_storage_class(self.storage)(
+            #         **self.storage_options
+            #     ).delete(self.path)
 
             self.update(
                 blob=blob,
