commit d9838b69c5bbdef52900e3bfabd35fe9af59c14d
Author: Mark Story <mark@sentry.io>
Date:   Thu Mar 19 16:54:13 2020 -0400

    fix(ui) Fix missing borders on issue details (#17788)
    
    I accidentally removed borders between context sections when doing other
    refactoring. This restores those borders.

diff --git a/src/sentry/static/sentry/app/components/events/contexts.jsx b/src/sentry/static/sentry/app/components/events/contexts.jsx
index c20ff03ab6..633bec0514 100644
--- a/src/sentry/static/sentry/app/components/events/contexts.jsx
+++ b/src/sentry/static/sentry/app/components/events/contexts.jsx
@@ -187,7 +187,7 @@ class ContextsInterface extends React.Component {
       );
     }
 
-    return <div>{children}</div>;
+    return <React.Fragment>{children}</React.Fragment>;
   }
 }
 
diff --git a/src/sentry/static/sentry/app/components/events/styles.tsx b/src/sentry/static/sentry/app/components/events/styles.tsx
index 0f706f41c7..807641e61b 100644
--- a/src/sentry/static/sentry/app/components/events/styles.tsx
+++ b/src/sentry/static/sentry/app/components/events/styles.tsx
@@ -36,7 +36,7 @@ export const BannerContainer = styled('div')<BannerProps>`
   border-bottom: 1px solid ${p => COLORS[p.priority].border};
 
   /* Muted box & processing errors are in different parts of the DOM */
-  & + ${DataSection}, & + div > ${DataSection} {
+  & + ${DataSection}:first-child, & + div > ${DataSection}:first-child {
     border-top: 0;
   }
 `;
