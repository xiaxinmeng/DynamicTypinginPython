commit 6db1326efefc124b3672d1e81bcb3479a7efcecf
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Feb 4 20:13:49 2015 -0800

    Fill in tags

diff --git a/src/sentry/api/serializers/models/event.py b/src/sentry/api/serializers/models/event.py
index 992c1ea367..36f8663364 100644
--- a/src/sentry/api/serializers/models/event.py
+++ b/src/sentry/api/serializers/models/event.py
@@ -36,6 +36,7 @@ class EventSerializer(Serializer):
             'size': obj.size,
             'entries': attrs['entries'],
             'message': obj.message,
+            'tags': obj.get_tags(),
             'platform': obj.platform,
             'dateCreated': obj.datetime,
             'timeSpent': obj.time_spent,
diff --git a/src/sentry/static/sentry/app/views/aggregate/event.jsx b/src/sentry/static/sentry/app/views/aggregate/event.jsx
index 0133406fb0..e514d98401 100644
--- a/src/sentry/static/sentry/app/views/aggregate/event.jsx
+++ b/src/sentry/static/sentry/app/views/aggregate/event.jsx
@@ -19,12 +19,12 @@ var AggregateEvent = React.createClass({
     request: require("./interfaces/request")
   },
 
-  render: function(){
+  render(){
     var agg = this.props.aggregate;
     var evt = this.props.event;
 
     var entries = [];
-    evt.entries.forEach(function(entry, entryIdx){
+    evt.entries.forEach((entry, entryIdx) => {
       try {
         var Component = this.interfaces[entry.type];
         if (!Component) {
@@ -42,7 +42,7 @@ var AggregateEvent = React.createClass({
         // TODO(dcramer): this should log to Sentry
         console.error(ex);
       }
-    }.bind(this));
+    });
 
     return (
       <div>
diff --git a/src/sentry/static/sentry/app/views/aggregate/eventTags.jsx b/src/sentry/static/sentry/app/views/aggregate/eventTags.jsx
index 7f0af37472..90ee0a0d0c 100644
--- a/src/sentry/static/sentry/app/views/aggregate/eventTags.jsx
+++ b/src/sentry/static/sentry/app/views/aggregate/eventTags.jsx
@@ -9,7 +9,17 @@ var AggregateEventTags = React.createClass({
     event: PropTypes.Event.isRequired
   },
 
-  render: function() {
+  render() {
+    var children = this.props.event.tags.map((tag) => {
+      var key = tag[0];
+      var value = tag[1];
+      return (
+        <li>
+          {key} = {value}
+        </li>
+      );
+    });
+
     return (
       <div id="tags" className="box">
         <div className="box-header">
@@ -17,6 +27,7 @@ var AggregateEventTags = React.createClass({
         </div>
         <div className="box-content with-padding">
           <ul className="mini-tag-list">
+            {children}
           </ul>
         </div>
       </div>
