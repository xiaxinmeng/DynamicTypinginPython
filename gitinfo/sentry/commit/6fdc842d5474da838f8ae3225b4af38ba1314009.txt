commit 6fdc842d5474da838f8ae3225b4af38ba1314009
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Feb 5 10:34:24 2015 -0800

    Ensure TagKeys get created in mocks

diff --git a/bin/load-mocks b/bin/load-mocks
index 1438463627..ba56635b44 100755
--- a/bin/load-mocks
+++ b/bin/load-mocks
@@ -127,6 +127,10 @@ def main():
 
     # force the buffer to process any pending incrs
     # (this ensures tags get populated)
+    # TODO(dcramer): we should load a buffer backend which just does incr's in
+    # realtime
+    buffer.process_pending()
+    # Run it once again to handle TagKey incrs
     buffer.process_pending()
 
 
