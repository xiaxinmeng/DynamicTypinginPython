commit 5118802ffa293696b2d41b2582b227f9d7f33a6e
Author: Billy Vong <billyvg@users.noreply.github.com>
Date:   Mon Nov 18 21:22:10 2019 -0800

    fix(test): Fix `<OrganizationContext>` flakey tests with `<Loa… (#15658)
    
    When in doubt, add more ticks

diff --git a/tests/js/spec/views/organizationContext.spec.jsx b/tests/js/spec/views/organizationContext.spec.jsx
index 4ea40e77ed..ac9bb97577 100644
--- a/tests/js/spec/views/organizationContext.spec.jsx
+++ b/tests/js/spec/views/organizationContext.spec.jsx
@@ -120,6 +120,7 @@ describe('OrganizationContext', function() {
     await tick();
     // await resolving api, and updating component
     await tick();
+    await tick();
     wrapper.update();
     expect(wrapper.find('LoadingError')).toHaveLength(1);
     console.error.mockRestore(); // eslint-disable-line no-console
@@ -138,6 +139,7 @@ describe('OrganizationContext', function() {
     await tick();
     // await resolving api, and updating component
     await tick();
+    await tick();
     wrapper.update();
 
     expect(openSudo).toHaveBeenCalled();
