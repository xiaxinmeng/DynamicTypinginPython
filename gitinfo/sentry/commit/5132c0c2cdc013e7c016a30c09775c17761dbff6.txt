commit 5132c0c2cdc013e7c016a30c09775c17761dbff6
Author: ckj <chriskjennings@gmail.com>
Date:   Tue Aug 30 12:46:01 2016 -0700

    Add hoverIntent visual indicator

diff --git a/src/sentry/static/sentry/app/components/header/organizationSelector.jsx b/src/sentry/static/sentry/app/components/header/organizationSelector.jsx
index 9a33027a48..8a1d97b34d 100644
--- a/src/sentry/static/sentry/app/components/header/organizationSelector.jsx
+++ b/src/sentry/static/sentry/app/components/header/organizationSelector.jsx
@@ -25,7 +25,7 @@ const OrganizationSelector = React.createClass({
   //   return (nextProps.organization || {}).id !== (this.props.organization || {}).id;
   // },
 
-  getInitial(orgName) {
+  getOrgInitial(orgName) {
     // TODO: Generate proper letter avatar
 
     let initial = orgName.charAt(0);
@@ -49,12 +49,17 @@ const OrganizationSelector = React.createClass({
     }
 
     let hoverIntent;
+    let hoverTime = 500;
 
     return (
       <div className={classNames}
-        onMouseEnter={()=> {hoverIntent = setTimeout(this.props.onShowPanel, 300);}}
+        onMouseEnter={()=> {hoverIntent = setTimeout(this.props.onShowPanel, hoverTime);}}
         onMouseLeave={()=> {this.props.hidePanel(); clearTimeout(hoverIntent); }}>
 
+        <div className="hover-bar-container">
+          <div className="hover-bar"/>
+        </div>
+
         <a className="active-org" href="/">
           <img src="https://pbs.twimg.com/profile_images/497432038492733440/eW6tXeq3_400x400.png" />
         </a>
@@ -68,7 +73,7 @@ const OrganizationSelector = React.createClass({
                 return (
                   <li className={activeOrg.id === org.id ? "org active" : "org"} key={org.slug}>
                     <Link className="org-avatar" to={`/${org.slug}/`}>
-                      {this.getInitial(org.name)}
+                      {this.getOrgInitial(org.name)}
                     </Link>
                     <h5><Link to={`/${org.slug}/`}>{org.name}</Link></h5>
                     <p>
diff --git a/src/sentry/static/sentry/app/components/sidebarPanel.jsx b/src/sentry/static/sentry/app/components/sidebarPanel.jsx
index fad6c396d6..39ab349da2 100644
--- a/src/sentry/static/sentry/app/components/sidebarPanel.jsx
+++ b/src/sentry/static/sentry/app/components/sidebarPanel.jsx
@@ -10,7 +10,7 @@ const SidebarPanel = React.createClass({
 
   render() {
     return (
-      <div className="sidebar-panel">
+      <div className="sidebar-panel" data-top={this.props.top}>
         <div className="sidebar-panel-header">
           <a className="close pull-right" onClick={this.props.hidePanel}><span className="icon-x" /></a>
           <h2>{this.props.title}</h2>
diff --git a/src/sentry/static/sentry/less/layout.less b/src/sentry/static/sentry/less/layout.less
index 70458f6ce2..074074cce0 100644
--- a/src/sentry/static/sentry/less/layout.less
+++ b/src/sentry/static/sentry/less/layout.less
@@ -138,6 +138,37 @@ body.auth {
     padding: 15px 0;
     position: relative;
 
+    .hover-bar-container {
+      position: absolute;
+      width: 32px;
+      height: 3px;
+      bottom: -2px;
+      border-radius: 2px;
+      overflow: hidden;
+
+      .hover-bar {
+        width: 0;
+        height: 3px;
+        background: rgba(255,255,255, .35);
+        -webkit-transition: width 500ms ease;
+      }
+    }
+
+    &:hover {
+      .hover-bar-container {
+        background: #625c69;
+      }
+      .hover-bar {
+        width: 100%;
+      }
+    }
+
+    &.active:hover {
+      .hover-bar-container {
+        display: none;
+      }
+    }
+
     .org-list {
       > li {
         padding: 15px 20px 10px 68px;
@@ -208,7 +239,7 @@ body.auth {
         text-align: center;
         line-height: 36px;
         color: #fff;
-        
+
         &:hover, &:active, &:focus {
           color: #fff;
         }
@@ -508,6 +539,17 @@ body.auth {
     padding: 0 50px;
     margin-top: (-62px + -16px) / 2; // offset for sidebar-panel-header + font-size
   }
+
+  &:before {
+    position: absolute;
+    display: block;
+    content: "";
+    left: -7px;
+    top: 24px;
+    border-top: 7px solid transparent;
+    border-bottom: 7px solid transparent;
+    border-right: 7px solid #fff;
+  }
 }
 
 .range-picker {
