commit 4f9b0b3a8fd45b02cf9cc76bcb20b91f9ab65a15
Author: Katie Lundsgaard <katie@getsentry.com>
Date:   Fri Aug 19 12:53:48 2016 -0700

    Fix links with hash to scroll to anchor when linked (#3964)
    
    /cc @getsentry/team

diff --git a/src/sentry/static/sentry/app/components/events/eventDataSection.jsx b/src/sentry/static/sentry/app/components/events/eventDataSection.jsx
index f6158e860a..8aeda2f756 100644
--- a/src/sentry/static/sentry/app/components/events/eventDataSection.jsx
+++ b/src/sentry/static/sentry/app/components/events/eventDataSection.jsx
@@ -16,12 +16,20 @@ const GroupEventDataSection = React.createClass({
     };
   },
 
+  componentDidMount() {
+    if (location.hash) {
+        let [, hash] = location.hash.split('#');
+        let anchorElement = hash && document.querySelector('div#' + hash);
+        if (anchorElement) {anchorElement.scrollIntoView(); }
+    }
+  },
+
   render: function() {
     return (
       <div className={(this.props.className || '') + ' box'}>
         {this.props.title &&
-          <div className="box-header">
-            <a name={this.props.type} href={'#' + this.props.type} className="permalink">
+          <div className="box-header" id={this.props.type}>
+            <a href={'#' + this.props.type} className="permalink">
               <em className="icon-anchor" />
             </a>
             {this.props.wrapTitle ?
