commit 7bc65202cc986aeb740be00057acc63d5d6f41f7
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Nov 18 11:20:51 2015 -0800

    Some initial work with Jed

diff --git a/package.json b/package.json
index f0aa06fd99..98ffe7f46a 100644
--- a/package.json
+++ b/package.json
@@ -16,6 +16,7 @@
     "extract-text-webpack-plugin": "0.8.2",
     "file-loader": "0.8.4",
     "history": "1.13.0",
+    "jed": "^1.1.0",
     "jquery": "2.1.4",
     "json-loader": "0.5.3",
     "less": "2.5.3",
diff --git a/src/sentry/static/sentry/app/index.js b/src/sentry/static/sentry/app/index.js
index 21ad26d970..160e551012 100644
--- a/src/sentry/static/sentry/app/index.js
+++ b/src/sentry/static/sentry/app/index.js
@@ -34,6 +34,7 @@ jQuery.ajaxSetup({
 // these get exported to a global variable, which is important as its the only
 // way we can call into scoped objects
 export default {
+  i18n: require("locale"),
   jQuery: jQuery,
   moment: require("moment"),
   Raven: require("raven-js"),
diff --git a/src/sentry/static/sentry/app/locale.jsx b/src/sentry/static/sentry/app/locale.jsx
new file mode 100644
index 0000000000..dea0e6faf3
--- /dev/null
+++ b/src/sentry/static/sentry/app/locale.jsx
@@ -0,0 +1,34 @@
+import Jed from 'jed';
+
+const i18n = new Jed({
+  'domain' : 'sentry',
+
+  // This callback is called when a key is missing
+  'missing_key_callback' : function(key) {
+    // TODO(dcramer): this should log to Sentry
+    console.error(key);
+  },
+
+  'locale_data' : {
+    // This is the domain key
+    'sentry' : {
+      // The empty string key is used as the configuration
+      // block for each domain
+      '' : {
+        // Domain name
+        'domain' : 'sentry',
+
+        // Language code
+        'lang' : 'en',
+
+        // Plural form function for language
+        'plural_forms' : 'nplurals=2; plural=(n != 1);'
+      },
+    }
+  }
+});
+
+export const gettext = i18n.gettext;
+export const ngettext = i18n.ngettext;
+export const _ = i18n.gettext;
+export default i18n;
