commit 1942cb5f17d55bc4711460c5fb1427fc03f5f6d4
Author: DZ <david@nodnod.net>
Date:   Tue Aug 31 14:42:20 2010 -0700

    add static serving for media

diff --git a/sentry/media/styles/global.css b/sentry/media/styles/global.css
new file mode 100644
index 0000000000..8b13789179
--- /dev/null
+++ b/sentry/media/styles/global.css
@@ -0,0 +1 @@
+
diff --git a/sentry/urls.py b/sentry/urls.py
index f595a6a498..b1c68aef19 100644
--- a/sentry/urls.py
+++ b/sentry/urls.py
@@ -1,3 +1,5 @@
+import os
+
 from django.conf import settings
 from django.conf.urls.defaults import *
 from django.utils.hashcompat import md5_constructor
@@ -7,9 +9,13 @@ import views
 
 hashed_secret = md5_constructor(settings.SECRET_KEY).hexdigest()
 
+SENTRY_ROOT = os.path.dirname(__file__) 
+
 urlpatterns = patterns('',
     url(r'^feeds/%s/messages.xml$' % hashed_secret, MessageFeed(), name='sentry-feed-messages'),
     url(r'^feeds/%s/summaries.xml$' % hashed_secret, SummaryFeed(), name='sentry-feed-summaries'),
     url(r'^group/(\d+)$', views.group, name='sentry-group'),
     url(r'^$', views.index, name='sentry'),
+    (r'^_media/(?P<path>.*)$', 'django.views.static.serve',
+        {'document_root': os.path.join(SENTRY_ROOT, 'media')}),
 )
