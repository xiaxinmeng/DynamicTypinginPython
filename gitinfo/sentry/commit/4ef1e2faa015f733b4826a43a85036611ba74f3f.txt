commit 4ef1e2faa015f733b4826a43a85036611ba74f3f
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Nov 5 08:33:52 2014 -0800

    Remove remaining references to SENTRY_KEY

diff --git a/conftest.py b/conftest.py
index 0bdd029c25..37daefe878 100644
--- a/conftest.py
+++ b/conftest.py
@@ -63,7 +63,6 @@ def pytest_configure(config):
         'tests',
     )
     # Need a predictable key for tests that involve checking signatures
-    settings.SENTRY_KEY = 'abc123'
     settings.SENTRY_PUBLIC = False
 
     # This speeds up the tests considerably, pbkdf2 is by design, slow.
diff --git a/docs/quickstart/index.rst b/docs/quickstart/index.rst
index 740a77a928..201f00356d 100644
--- a/docs/quickstart/index.rst
+++ b/docs/quickstart/index.rst
@@ -155,10 +155,6 @@ is not a fully supported database and should not be used in production**.
     # No trailing slash!
     SENTRY_URL_PREFIX = 'http://sentry.example.com'
 
-    # SENTRY_KEY is a unique randomly generated secret key for your server, and it
-    # acts as a signing token
-    SENTRY_KEY = '0123456789abcde'
-
     SENTRY_WEB_HOST = '0.0.0.0'
     SENTRY_WEB_PORT = 9000
     SENTRY_WEB_OPTIONS = {
diff --git a/src/sentry/conf/server.py b/src/sentry/conf/server.py
index 351d52d650..7d71d6e49e 100644
--- a/src/sentry/conf/server.py
+++ b/src/sentry/conf/server.py
@@ -528,8 +528,6 @@ SENTRY_IGNORE_EXCEPTIONS = (
     'OperationalError',
 )
 
-SENTRY_KEY = None
-
 # Absolute URL to the sentry root directory. Should not include a trailing slash.
 SENTRY_URL_PREFIX = ''
 
diff --git a/src/sentry/testutils/cases.py b/src/sentry/testutils/cases.py
index 23216d525c..03ac72b857 100644
--- a/src/sentry/testutils/cases.py
+++ b/src/sentry/testutils/cases.py
@@ -118,13 +118,6 @@ class BaseTestCase(Fixtures, Exam):
     def _makePostMessage(self, data):
         return base64.b64encode(self._makeMessage(data))
 
-    def _postWithKey(self, data, key=None):
-        resp = self.client.post(reverse('sentry-api-store'), {
-            'data': self._makePostMessage(data),
-            'key': settings.SENTRY_KEY,
-        })
-        return resp
-
     def _postWithHeader(self, data, key=None, secret=None):
         if key is None:
             key = self.projectkey.public_key
