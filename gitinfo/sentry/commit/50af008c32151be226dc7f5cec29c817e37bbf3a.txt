commit 50af008c32151be226dc7f5cec29c817e37bbf3a
Author: Chris Jennings <chriskjennings@gmail.com>
Date:   Tue May 8 12:57:09 2018 -0700

    bug(ui): fix issue where long artifact urls break table (#8328)
    
    * bug(ui): fix issue where long artifact urls break table
    
    * number, not string

diff --git a/src/sentry/static/sentry/app/views/releaseArtifacts.jsx b/src/sentry/static/sentry/app/views/releaseArtifacts.jsx
index 7ca778ac7e..97a68bad07 100644
--- a/src/sentry/static/sentry/app/views/releaseArtifacts.jsx
+++ b/src/sentry/static/sentry/app/views/releaseArtifacts.jsx
@@ -126,7 +126,9 @@ const ReleaseArtifacts = createReactClass({
       <div>
         <Panel>
           <PanelHeader>
-            <Flex flex="7">{t('Name')}</Flex>
+            <Flex flex="7" pr={2}>
+              {t('Name')}
+            </Flex>
             <Flex flex="2">{t('Distribution')}</Flex>
             <Flex flex="3">{t('Size')}</Flex>
           </PanelHeader>
@@ -134,7 +136,11 @@ const ReleaseArtifacts = createReactClass({
             {this.state.fileList.map(file => {
               return (
                 <PanelItem key={file.id}>
-                  <Flex flex="7" style={{wordWrap: 'break-word'}}>
+                  <Flex
+                    flex="7"
+                    pr={2}
+                    style={{wordWrap: 'break-word', wordBreak: 'break-all'}}
+                  >
                     <strong>{file.name || '(empty)'}</strong>
                   </Flex>
                   <Flex flex="2">
