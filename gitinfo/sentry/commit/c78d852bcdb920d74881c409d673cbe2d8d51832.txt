commit c78d852bcdb920d74881c409d673cbe2d8d51832
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Thu Oct 1 11:46:53 2015 -0700

    Change up logic a tad to be more accurate to what it was

diff --git a/src/sentry/utils/samples.py b/src/sentry/utils/samples.py
index c52ee2b89a..763ac7528e 100644
--- a/src/sentry/utils/samples.py
+++ b/src/sentry/utils/samples.py
@@ -74,10 +74,8 @@ def create_sample_event(project, platform=None, default=None, raw=True,
     if not platform and not default:
         return
 
-    if platform is None:
-        platform = default
-
-    platform = platform.split('-', 1)[0].split('_', 1)[0]
+    if platform:
+        platform = platform.split('-', 1)[0].split('_', 1)[0]
 
     data = load_data(platform, default)
 
