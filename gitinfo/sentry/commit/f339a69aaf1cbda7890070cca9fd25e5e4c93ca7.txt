commit f339a69aaf1cbda7890070cca9fd25e5e4c93ca7
Author: David Cramer <dcramer@gmail.com>
Date:   Sat Apr 13 19:13:45 2013 -0700

    Correctly validate each exception

diff --git a/src/sentry/interfaces.py b/src/sentry/interfaces.py
index d2d82e6f19..490cd1b9f7 100644
--- a/src/sentry/interfaces.py
+++ b/src/sentry/interfaces.py
@@ -646,6 +646,10 @@ class SingleException(Interface):
         else:
             self.stacktrace = None
 
+    def validate(self):
+        if self.stacktrace:
+            return self.stacktrace.validate()
+
     def serialize(self):
         if self.stacktrace:
             stacktrace = self.stacktrace.serialize()
@@ -746,7 +750,7 @@ class Exception(Interface):
     def validate(self):
         for exception in self.values:
             # ensure we've got the correct required values
-            assert exception.is_valid()
+            exception.validate()
 
     def serialize(self):
         return {
