commit b92e081b7b5c7020bf36abea8981d3ff57c8b0e4
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Oct 19 17:59:53 2011 -0700

    A couple slight tweaks to counts

diff --git a/sentry/templatetags/sentry_helpers.py b/sentry/templatetags/sentry_helpers.py
index ccb4d750c1..b6067b343f 100644
--- a/sentry/templatetags/sentry_helpers.py
+++ b/sentry/templatetags/sentry_helpers.py
@@ -52,12 +52,14 @@ def small_count(v):
     z = [
         (1000000000, _('b')),
         (1000000, _('m')),
-        (10, _('k')),
+        (1000, _('k')),
     ]
     v = int(v)
     for x, y in z:
         o, p = divmod(v, x)
         if o > 1:
+            if len(str(o)) > 2:
+                return '%d%s' % (v, y)
             return '%.1f%s' % (v / float(x), y)
     return v
 
