commit ca23ff95ab87fcc40506732636bd390e2f0f6b14
Author: David Cramer <dcramer@gmail.com>
Date:   Fri Feb 27 13:52:52 2015 -0800

    Fix loading indicator

diff --git a/src/sentry/templates/sentry/organization-members.html b/src/sentry/templates/sentry/organization-members.html
index 618a643f86..015c136e83 100644
--- a/src/sentry/templates/sentry/organization-members.html
+++ b/src/sentry/templates/sentry/organization-members.html
@@ -110,19 +110,23 @@
   });
 
   $('.resend-invite').click(function(){
-    var $this = $(this);
-    $this.replaceWith('<span class="icon-spinner spin"></span>');
+    var $link = $(this);
+    var $loader = $('<span class="icon-spinner spin"></span>');
+
+    $link.replaceWith($loader);
+
     $.ajax({
-      url: app.config.urlPrefix + '/api/0/organizations/' + app.config.organizationId + '/members/' + $this.data('member-id') + '/',
+      url: app.config.urlPrefix + '/api/0/organizations/' + app.config.organizationId + '/members/' + $link.data('member-id') + '/',
       data: JSON.stringify({
         reinvite: 1,
       }),
       contentType: 'application/json',
       method: 'PUT',
       success: function() {
-        $this.replaceWith('<span>Sent!</span>');
+        $loader.replaceWith('<span>Sent!</span>');
       },
       error: function() {
+        $loader.replaceWith($link);
         alert('There was an error while trying to reinvite the member.');
       }
     });
