commit 90eb3d1e7e32be56aff9f0d7e2daa6de6c885062
Author: Armin Ronacher <armin.ronacher@active-4.com>
Date:   Mon Nov 26 16:13:44 2018 +0100

    fix(stacktraces): Correctly pass the timezone info on release creation in stacktraces (#10759)

diff --git a/src/sentry/stacktraces.py b/src/sentry/stacktraces.py
index b2d9b373fd..83630b7d64 100644
--- a/src/sentry/stacktraces.py
+++ b/src/sentry/stacktraces.py
@@ -3,6 +3,7 @@ from __future__ import absolute_import
 import six
 import logging
 from datetime import datetime
+from django.utils import timezone
 
 from collections import namedtuple
 
@@ -118,7 +119,7 @@ class StacktraceProcessor(object):
             return Release.get(project=self.project, version=self.data['release'])
         timestamp = self.data.get('timestamp')
         if timestamp is not None:
-            date = datetime.fromtimestamp(timestamp)
+            date = datetime.fromtimestamp(timestamp).replace(tzinfo=timezone.utc)
         else:
             date = None
         return Release.get_or_create(
