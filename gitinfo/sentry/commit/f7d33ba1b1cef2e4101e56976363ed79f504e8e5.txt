commit f7d33ba1b1cef2e4101e56976363ed79f504e8e5
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Feb 13 15:06:21 2012 -0800

    Only remove the row (realtime) if its not at the same position

diff --git a/sentry/static/scripts/sentry.realtime.js b/sentry/static/scripts/sentry.realtime.js
index cf9efd17e8..2a167ccfe6 100644
--- a/sentry/static/scripts/sentry.realtime.js
+++ b/sentry/static/scripts/sentry.realtime.js
@@ -98,7 +98,6 @@ if (Sentry === undefined) {
             if ($row.attr('data-count') == data.count) {
                 return;
             }
-            $row.remove();
         }
         $row = $(data.html);
 
@@ -115,6 +114,7 @@ if (Sentry === undefined) {
             if (old_pos == pos) {
                 return;
             }
+            $row.remove();
         }
 
         // if the row doesnt outrank any existing elements
