commit 89100bd85f5a14efff17862c5a046f318ebc00d2
Author: Billy Vong <billyvg@users.noreply.github.com>
Date:   Fri May 31 10:00:07 2019 -0700

    fix(metrics): Fix recording wrong http status on API successes (#13472)
    
    This was mistakenly trying to access the `status` key from the response data instead of the xhr response object.

diff --git a/src/sentry/static/sentry/app/api.jsx b/src/sentry/static/sentry/app/api.jsx
index f0feabb7b7..1ed102e0df 100644
--- a/src/sentry/static/sentry/app/api.jsx
+++ b/src/sentry/static/sentry/app/api.jsx
@@ -209,12 +209,12 @@ export class Client {
           Accept: 'application/json; charset=utf-8',
         },
         success: (...args) => {
-          const [resp] = args || [];
+          const [, , xhr] = args || [];
           metric.measure({
             name: 'app.api.request-success',
             start: `api-request-start-${id}`,
             data: {
-              status: resp && resp.status,
+              status: xhr && xhr.status,
             },
           });
           if (!isUndefined(options.success)) {
