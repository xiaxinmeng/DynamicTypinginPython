commit 452b5b2d6b484a06249d04589666a5229cd7e150
Author: Armin Ronacher <armin.ronacher@active-4.com>
Date:   Fri Apr 12 20:24:55 2019 +0200

    feat: Only show location for native events (#12757)

diff --git a/src/sentry/api/serializers/models/group.py b/src/sentry/api/serializers/models/group.py
index 46e4389ec3..a66589d233 100644
--- a/src/sentry/api/serializers/models/group.py
+++ b/src/sentry/api/serializers/models/group.py
@@ -371,6 +371,7 @@ class GroupSerializerBase(Serializer):
             'status': status_label,
             'statusDetails': status_details,
             'isPublic': share_id is not None,
+            'platform': obj.platform,
             'project': {
                 'id': six.text_type(obj.project.id),
                 'name': obj.project.name,
diff --git a/src/sentry/static/sentry/app/components/eventOrGroupHeader.jsx b/src/sentry/static/sentry/app/components/eventOrGroupHeader.jsx
index 06004f2094..d0f261061f 100644
--- a/src/sentry/static/sentry/app/components/eventOrGroupHeader.jsx
+++ b/src/sentry/static/sentry/app/components/eventOrGroupHeader.jsx
@@ -9,6 +9,7 @@ import ProjectLink from 'app/components/projectLink';
 import {Metadata} from 'app/sentryTypes';
 import EventOrGroupTitle from 'app/components/eventOrGroupTitle';
 import Tooltip from 'app/components/tooltip';
+import {isNativePlatform} from 'app/utils/platform';
 
 /**
  * Displays an event or group/issue title (i.e. in Stream)
@@ -63,7 +64,7 @@ class EventOrGroupHeader extends React.Component {
 
   getLocation() {
     const {data} = this.props;
-    if (data.type === 'error') {
+    if (data.type === 'error' && isNativePlatform(data.platform)) {
       const {metadata} = data || {};
       return metadata.filename || null;
     }
diff --git a/src/sentry/static/sentry/app/utils/platform.jsx b/src/sentry/static/sentry/app/utils/platform.jsx
new file mode 100644
index 0000000000..43ff0472c9
--- /dev/null
+++ b/src/sentry/static/sentry/app/utils/platform.jsx
@@ -0,0 +1,12 @@
+export function isNativePlatform(platform) {
+  switch (platform) {
+    case 'cocoa':
+    case 'objc':
+    case 'native':
+    case 'swift':
+    case 'c':
+      return true;
+    default:
+      return false;
+  }
+}
