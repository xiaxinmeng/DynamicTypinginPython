commit 1560ec7a114b01e81ed2b84b51949bde00cea8b6
Author: Katie Lundsgaard <katie@getsentry.com>
Date:   Thu Aug 11 16:17:40 2016 -0700

    Prevent StrictClick event when anchor is clicked allowing external link to open (#3896)
    
    /cc @getsentry/team

diff --git a/src/sentry/static/sentry/app/components/events/interfaces/frame.jsx b/src/sentry/static/sentry/app/components/events/interfaces/frame.jsx
index 8d4c3a8283..5021979a37 100644
--- a/src/sentry/static/sentry/app/components/events/interfaces/frame.jsx
+++ b/src/sentry/static/sentry/app/components/events/interfaces/frame.jsx
@@ -113,6 +113,10 @@ const Frame = React.createClass({
     }
   },
 
+  preventCollapse(evt) {
+    evt.stopPropagation();
+  },
+
   renderDefaultTitle() {
     let data = this.props.data;
     let title = [];
@@ -133,7 +137,7 @@ const Frame = React.createClass({
         </code>
       ));
       if (isUrl(data.absPath)) {
-        title.push(<a href={data.absPath} className="icon-open" key="share" target="_blank" />);
+        title.push(<a href={data.absPath} className="icon-open" key="share" target="_blank" onClick={this.preventCollapse}/>);
       }
       if (defined(data.function)) {
         title.push(<span className="in-at" key="in"> in </span>);
