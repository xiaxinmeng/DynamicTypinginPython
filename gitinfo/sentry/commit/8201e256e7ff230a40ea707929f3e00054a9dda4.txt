commit 8201e256e7ff230a40ea707929f3e00054a9dda4
Author: Michael Rooney <mrooney.github@rowk.com>
Date:   Wed May 16 16:27:24 2012 -0400

    more helpful error message for db backend issues

diff --git a/sentry/utils/models.py b/sentry/utils/models.py
index be5ac05158..d8d6feee31 100644
--- a/sentry/utils/models.py
+++ b/sentry/utils/models.py
@@ -41,6 +41,8 @@ def update(self, using=None, **kwargs):
         return True
     elif affected == 0:
         raise self.DoesNotExist("Cannot update an instance that is not in the database.")
+    elif affected < 0:
+        raise ValueError("Somehow we have updated a negative amount of rows, you seem to have a problem with your db backend.")
     else:
         raise ValueError("Somehow we have updated multiple rows, and you are now royally fucked.")
 
