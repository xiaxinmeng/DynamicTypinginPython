commit 5fabfe34fa02544b5eda9cfe929b7c4297ced276
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Sep 20 13:07:35 2010 -0700

    Somehow slipped with server_name. Fixing passing of settings.CLIENT.

diff --git a/sentry/manager.py b/sentry/manager.py
index 8e8c2b81a7..de0b7a1119 100644
--- a/sentry/manager.py
+++ b/sentry/manager.py
@@ -33,7 +33,7 @@ class SentryManager(models.Manager):
         view = kwargs.pop('view', None)
         logger_name = kwargs.pop('logger', 'root')
         url = kwargs.pop('url', None)
-        server_name = kwargs.pop('server_name', )
+        server_name = kwargs.pop('server_name', settings.CLIENT)
         data = kwargs.pop('data', {}) or {}
 
         if url:
