commit 1d29bcf56d4236718e246dc31cc78b5d7bb9b97a
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Dec 26 18:47:22 2011 -0800

    Dont use utcnow as it fucks up because Python has awful datetime support

diff --git a/sentry/utils/manager.py b/sentry/utils/manager.py
index d9877a22bc..d87a947af5 100644
--- a/sentry/utils/manager.py
+++ b/sentry/utils/manager.py
@@ -226,7 +226,7 @@ class GroupManager(models.Manager, ChartMixin):
         logger_name = kwargs.pop('logger', 'root')
         server_name = kwargs.pop('server_name', None)
         site = kwargs.pop('site', None)
-        date = kwargs.pop('date', None) or datetime.datetime.utcnow()
+        date = kwargs.pop('date', None) or datetime.datetime.now()
         extra = kwargs.pop('extra', None)
         modules = kwargs.pop('modules', None)
 
