commit c28c6220344730a6915ee8cf61c33c38a06a27da
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Thu Oct 22 18:47:41 2015 -0700

    utils/raven: make sure we swallow all errors
    
    There are other reasons why fetching the internal project may fail, and
    we don't want to cause recursion, so just capture them all and return
    gracefully.

diff --git a/src/sentry/utils/raven.py b/src/sentry/utils/raven.py
index 0aeed7e948..d2e4ad2bb6 100644
--- a/src/sentry/utils/raven.py
+++ b/src/sentry/utils/raven.py
@@ -65,6 +65,10 @@ class SentryInternalClient(DjangoClient):
             self.error_logger.error('Internal project (id=%s) does not exist',
                                     settings.SENTRY_PROJECT)
             return
+        except Exception:
+            self.error_logger.error('Unable to fetch internal project for some unknown reason',
+                                    exec_info=True)
+            return
 
         helper.context.bind_project(project)
 
