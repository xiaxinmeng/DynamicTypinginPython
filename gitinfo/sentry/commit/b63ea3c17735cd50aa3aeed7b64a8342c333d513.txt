commit b63ea3c17735cd50aa3aeed7b64a8342c333d513
Author: David Cramer <dcramer@gmail.com>
Date:   Fri Jul 31 13:02:53 2015 -0700

    Block obvious deletion path on sentry user

diff --git a/src/sentry/models/user.py b/src/sentry/models/user.py
index 949975e158..75daf7892c 100644
--- a/src/sentry/models/user.py
+++ b/src/sentry/models/user.py
@@ -58,6 +58,11 @@ class User(BaseModel, AbstractBaseUser):
         verbose_name = _('user')
         verbose_name_plural = _('users')
 
+    def delete(self):
+        if self.username == 'sentry':
+            raise Exception('You cannot delete the "sentry" user as it is required by Sentry.')
+        return super(User, self).delete()
+
     def save(self, *args, **kwargs):
         if not self.username:
             self.username = self.email
