commit 6a0beabcc97475abac22be01e2d97797a5640e7f
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Aug 21 20:24:41 2012 -0700

    If a user is already authenticated, redirect them instead of showing the login form

diff --git a/sentry/web/frontend/accounts.py b/sentry/web/frontend/accounts.py
index b3987e82bd..ce45b3ab84 100644
--- a/sentry/web/frontend/accounts.py
+++ b/sentry/web/frontend/accounts.py
@@ -39,6 +39,9 @@ def login(request):
     from django.contrib.auth import login as login_
     from django.contrib.auth.forms import AuthenticationForm
 
+    if request.user.is_authenticated():
+        return login_redirect(request)
+
     form = AuthenticationForm(request, request.POST or None)
     if form.is_valid():
         login_(request, form.get_user())
