commit 8045583c98d53c74e857fef4f593bebd07dfa002
Author: Mark Story <mark@sentry.io>
Date:   Tue Jan 28 16:57:06 2020 -0500

    feat(discover) Add option for discover2 rollout (#16668)
    
    When we start enabling discover2 for customers we want to do so
    gradually. This option will be read by the feature handlers in getsentry
    to incrementally enable discover2 for accounts on matching plans.

diff --git a/src/sentry/options/defaults.py b/src/sentry/options/defaults.py
index e0d8b083a0..32088618fb 100644
--- a/src/sentry/options/defaults.py
+++ b/src/sentry/options/defaults.py
@@ -205,3 +205,6 @@ register("outcomes.tsdb-in-consumer-sample-rate", default=0.0)
 # Node data save rate
 register("nodedata.cache-sample-rate", default=0.0, flags=FLAG_PRIORITIZE_DISK)
 register("nodedata.cache-on-save", default=False, flags=FLAG_PRIORITIZE_DISK)
+
+# Discover2 incremental rollout rate. Tied to feature handlers in getsentry
+register("discover2.rollout-rate", default=0, flags=FLAG_PRIORITIZE_DISK)
