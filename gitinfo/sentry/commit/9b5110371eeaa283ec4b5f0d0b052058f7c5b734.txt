commit 9b5110371eeaa283ec4b5f0d0b052058f7c5b734
Author: Lauryn Brown <lauryndbrown@gmail.com>
Date:   Fri Jan 26 14:04:52 2018 -0800

    fix(plugin): Changed a relative link to an absolute url in plugins config page. (#7090)
    
    * Changed a relative link to an absolute url in plugins config page.
    
    * Fixes JAVASCRIPT-2WT. Fixes SENTRY-5BF.
    
    * Added third parameter. For plugins.
    
    * Made a small correction

diff --git a/src/sentry/templates/sentry/plugins/bases/issue/not_configured.html b/src/sentry/templates/sentry/plugins/bases/issue/not_configured.html
index f9880ad5c4..ca553e5004 100644
--- a/src/sentry/templates/sentry/plugins/bases/issue/not_configured.html
+++ b/src/sentry/templates/sentry/plugins/bases/issue/not_configured.html
@@ -19,8 +19,9 @@
                 {% endfor %}
             </ul>
         {% else %}
-            {% url 'sentry-configure-project-plugin' organization_slug=project.organization.slug project_slug=project.slug slug=plugin.slug as link %}
-            <p>{% blocktrans %}You still need to <a href="{{ link }}">configure this plugin</a> before you can use it.{% endblocktrans %}</p>
+            <p>{% blocktrans %}You still need to<a href="{% absolute_uri '/{}/{}/settings/plugins/{}/' project.organization.slug project.slug p.slug %}">configure this plugin</a>
+                before you can use it.{% endblocktrans %}
+            </p>
         {% endif %}
     </div>
 {% endblock %}
diff --git a/src/sentry/templates/sentry/projects/manage.html b/src/sentry/templates/sentry/projects/manage.html
index 851bee3624..0368e12bf5 100644
--- a/src/sentry/templates/sentry/projects/manage.html
+++ b/src/sentry/templates/sentry/projects/manage.html
@@ -98,7 +98,7 @@
       </li>
       {% for p in plugins %}
         <li>
-          <a href="{% absolute_uri '/{}/{}/settings/plugins/' project.organization.slug project.slug p.slug %}">{{ p.get_title }}</a>
+          <a href="{% absolute_uri '/{}/{}/settings/plugins/{}/' project.organization.slug project.slug p.slug %}">{{ p.get_title }}</a>
         </li>
       {% endfor %}
     </ul>
