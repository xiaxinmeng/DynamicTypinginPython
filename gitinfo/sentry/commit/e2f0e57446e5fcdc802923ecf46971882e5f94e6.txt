commit e2f0e57446e5fcdc802923ecf46971882e5f94e6
Author: David Cramer <dcramer@gmail.com>
Date:   Sat Jul 18 23:52:29 2015 -0700

    Dont link version on details

diff --git a/src/sentry/static/sentry/app/components/version.jsx b/src/sentry/static/sentry/app/components/version.jsx
index cdbbee09f1..6c30dd547c 100644
--- a/src/sentry/static/sentry/app/components/version.jsx
+++ b/src/sentry/static/sentry/app/components/version.jsx
@@ -7,25 +7,33 @@ var Version = React.createClass({
   },
 
   propTypes: {
-    version: React.PropTypes.string.isRequired
+    version: React.PropTypes.string.isRequired,
+  },
+
+  getDefaultProps() {
+    return {
+      anchor: true
+    };
   },
 
   render() {
     var version = this.props.version;
     var shortVersion = version.length === 40 ? version.substr(0, 12) : version;
     var params = this.context.router.getCurrentParams();
-
-    return (
-      <Router.Link
-          to="releaseDetails"
-          params={{
-            orgId: params.orgId,
-            projectId: params.projectId,
-            version: version,
-          }}>
-        <span title={version}>{shortVersion}</span>
-      </Router.Link>
-    );
+    if (this.props.anchor) {
+      return (
+        <Router.Link
+            to="releaseDetails"
+            params={{
+              orgId: params.orgId,
+              projectId: params.projectId,
+              version: version,
+            }}>
+          <span title={version}>{shortVersion}</span>
+        </Router.Link>
+      );
+    }
+    return <span title={version}>{shortVersion}</span>;
   }
 });
 
diff --git a/src/sentry/static/sentry/app/views/projectReleases.jsx b/src/sentry/static/sentry/app/views/projectReleases.jsx
index 2f7662e7f6..f8515efc05 100644
--- a/src/sentry/static/sentry/app/views/projectReleases.jsx
+++ b/src/sentry/static/sentry/app/views/projectReleases.jsx
@@ -24,21 +24,11 @@ var ReleaseList = React.createClass({
     return (
       <ul className="release-list">
           {this.props.releaseList.map((release) => {
-            var routeParams = {
-              orgId: params.orgId,
-              projectId: params.projectId,
-              version: release.version
-            };
-
             return (
               <li className="release">
                 <div className="row">
                   <div className="col-md-7 col-sm-6 col-xs-6">
-                    <h4>
-                    <Router.Link to="releaseDetails" params={routeParams} className="truncate">
-                      <Version version={release.version} />
-                    </Router.Link>
-                    </h4>
+                    <h4><Version version={release.version} /></h4>
                     <div className="release-meta">
                       <span className="icon icon-clock"></span> <TimeSince date={release.dateCreated} />
                     </div>
diff --git a/src/sentry/static/sentry/app/views/releaseDetails.jsx b/src/sentry/static/sentry/app/views/releaseDetails.jsx
index 8ccdc38cb6..284f12839a 100644
--- a/src/sentry/static/sentry/app/views/releaseDetails.jsx
+++ b/src/sentry/static/sentry/app/views/releaseDetails.jsx
@@ -107,7 +107,7 @@ var ReleaseDetails = React.createClass({
                 <Router.Link to="projectReleases" params={params} className="back-arrow">
                   <span className="icon-arrow-left"></span>
                 </Router.Link>
-                <h3>Release <strong><Version version={release.version} /></strong></h3>
+                <h3>Release <strong><Version version={release.version} anchor={false} /></strong></h3>
                 <div className="release-meta">
                   <span className="icon icon-clock"></span> <TimeSince date={release.dateCreated} />
                 </div>
