commit 8859e2b0f8707c0aa0a228972416e7a87657e13b
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Jul 2 12:51:59 2018 -0700

    fix: Correct pre-commit defaults
    
    - Change order to ensure we're always setting up pre-commit last
    - Set case insensitivity for git

diff --git a/Makefile b/Makefile
index d74c5e7e07..4de3ddf320 100644
--- a/Makefile
+++ b/Makefile
@@ -41,10 +41,11 @@ clean:
 
 setup-git:
 	@echo "--> Installing git hooks"
-	pip install "pre-commit>=1.10.1,<1.11.0"
-	pre-commit install
 	git config branch.autosetuprebase always
+	git config core.ignorecase false
 	cd .git/hooks && ln -sf ../../config/hooks/* ./
+	pip install "pre-commit>=1.10.1,<1.11.0"
+	pre-commit install
 	@echo ""
 
 update-submodules:
