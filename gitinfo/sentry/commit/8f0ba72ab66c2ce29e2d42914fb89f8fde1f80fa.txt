commit 8f0ba72ab66c2ce29e2d42914fb89f8fde1f80fa
Author: Markus Unterwaditzer <markus@unterwaditzer.net>
Date:   Fri Oct 18 18:24:20 2019 +0200

    fix: Swapped arguments for mark_signal_sent (#15175)

diff --git a/src/sentry/ingest/outcomes_consumer.py b/src/sentry/ingest/outcomes_consumer.py
index 5c81de73b0..f32b706b08 100644
--- a/src/sentry/ingest/outcomes_consumer.py
+++ b/src/sentry/ingest/outcomes_consumer.py
@@ -17,6 +17,8 @@ signals to getSentry for these outcomes.
 """
 from __future__ import absolute_import
 
+import six
+
 import datetime
 import time
 import atexit
@@ -55,6 +57,7 @@ def mark_signal_sent(project_id, event_id):
 
     :param project_id: :param event_id: :return:
     """
+    assert isinstance(project_id, six.integer_types)
     key = _get_signal_cache_key(project_id, event_id)
     cache.set(key, True, 3600)
 
diff --git a/src/sentry/tasks/store.py b/src/sentry/tasks/store.py
index cc51dad408..523b193c22 100644
--- a/src/sentry/tasks/store.py
+++ b/src/sentry/tasks/store.py
@@ -523,7 +523,7 @@ def _do_save_event(
         # emitting it for now.
         #
         # XXX(markus): Revisit decision about signals once outcomes consumer is stable.
-        mark_signal_sent(event_id, project_id)
+        mark_signal_sent(project_id, event_id)
         track_outcome(
             project.organization_id,
             project_id,
