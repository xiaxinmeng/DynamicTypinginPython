commit 1a3703079517f00ff54e86ed311d9b251aa949b1
Author: Mark Story <mark@sentry.io>
Date:   Wed Jun 12 09:35:20 2019 -0400

    fix(ui) Fix overflowing tag values (#13648)
    
    Tags with longer values were causing the tag table to break out of the
    modal and overflow out of the viewport. Using `table-layout:fixed`
    solves this along with overflowEllipsis.
    
    Refs SEN-716

diff --git a/src/sentry/static/sentry/app/views/organizationEventsV2/tagsTable.jsx b/src/sentry/static/sentry/app/views/organizationEventsV2/tagsTable.jsx
index 3b02fcd557..bba01713dc 100644
--- a/src/sentry/static/sentry/app/views/organizationEventsV2/tagsTable.jsx
+++ b/src/sentry/static/sentry/app/views/organizationEventsV2/tagsTable.jsx
@@ -6,13 +6,14 @@ import {withRouter} from 'react-router';
 import Link from 'app/components/links/link';
 import {t} from 'app/locale';
 import space from 'app/styles/space';
+import overflowEllipsis from 'app/styles/overflowEllipsis';
 import {getEventTagSearchUrl} from './utils';
 
 const TagsTable = props => {
   return (
     <div>
       <TagHeading>{t('Tags')}</TagHeading>
-      <table>
+      <StyledTable>
         <tbody>
           {props.tags.map(tag => (
             <StyledTr key={tag.key}>
@@ -25,7 +26,7 @@ const TagsTable = props => {
             </StyledTr>
           ))}
         </tbody>
-      </table>
+      </StyledTable>
     </div>
   );
 };
@@ -34,6 +35,12 @@ TagsTable.propTypes = {
   location: PropTypes.object,
 };
 
+const StyledTable = styled('table')`
+  table-layout: fixed;
+  width: 100%;
+  max-width: 100%;
+`;
+
 const TagHeading = styled('h5')`
   text-transform: uppercase;
   color: ${p => p.theme.gray3};
@@ -55,6 +62,7 @@ const TagKey = styled('td')`
 
 const TagValue = styled(TagKey)`
   text-align: right;
+  ${overflowEllipsis};
 `;
 
 export default withRouter(TagsTable);
