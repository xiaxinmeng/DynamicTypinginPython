commit 76cf39a5db99b24322863824d5e2c50311bbb3df
Author: Mark Story <mark@sentry.io>
Date:   Tue Jul 9 09:57:22 2019 -0400

    ref: Improve repr of User (#13896)
    
    Having the user's id in the repr will help diagnosing which user is
    not being serialized properly and is causing API requests to fail.
    
    Refs SENTRY-B2M

diff --git a/src/sentry/models/user.py b/src/sentry/models/user.py
index 19fc990546..6dc968d655 100644
--- a/src/sentry/models/user.py
+++ b/src/sentry/models/user.py
@@ -17,7 +17,12 @@ from django.db import IntegrityError, models, transaction
 from django.utils import timezone
 from django.utils.translation import ugettext_lazy as _
 
-from sentry.db.models import BaseManager, BaseModel, BoundedAutoField
+from sentry.db.models import (
+    BaseManager,
+    BaseModel,
+    BoundedAutoField,
+    sane_repr
+)
 from sentry.models import LostPasswordHash
 from sentry.utils.http import absolute_uri
 
@@ -136,6 +141,8 @@ class User(BaseModel, AbstractBaseUser):
         verbose_name = _('user')
         verbose_name_plural = _('users')
 
+    __repr__ = sane_repr('id')
+
     def delete(self):
         if self.username == 'sentry':
             raise Exception('You cannot delete the "sentry" user as it is required by Sentry.')
