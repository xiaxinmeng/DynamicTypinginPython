commit b628b7173fc6838e7a7b2634ade28d8443fb724e
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Feb 25 14:53:05 2015 -0800

    Fix sqlite check

diff --git a/src/sentry/utils/db.py b/src/sentry/utils/db.py
index a9145ee2f1..0e2c1efee1 100644
--- a/src/sentry/utils/db.py
+++ b/src/sentry/utils/db.py
@@ -29,13 +29,14 @@ def is_mysql(alias='default'):
     return 'mysql' in engine
 
 
-def is_sqlite(alias='sqlite'):
+def is_sqlite(alias='default'):
     engine = get_db_engine(alias)
+    return 'sqlite' in engine
 
 
 def has_charts(db):
     engine = get_db_engine(db)
-    if engine.startswith('sqlite'):
+    if is_sqlite(db):
         return False
     return True
 
