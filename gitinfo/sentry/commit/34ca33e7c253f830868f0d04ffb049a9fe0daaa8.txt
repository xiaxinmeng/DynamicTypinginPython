commit 34ca33e7c253f830868f0d04ffb049a9fe0daaa8
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Mon Feb 22 14:17:43 2016 -0800

    --config should override SENTRY_CONF env variable

diff --git a/src/sentry/runner/__init__.py b/src/sentry/runner/__init__.py
index 7f0413778d..32b44838fc 100755
--- a/src/sentry/runner/__init__.py
+++ b/src/sentry/runner/__init__.py
@@ -23,7 +23,7 @@ else:
 @click.group()
 @click.option(
     '--config',
-    default='~/.sentry',
+    default='',
     envvar='SENTRY_CONF',
     help='Path to configuration files.',
     metavar='PATH')
@@ -39,7 +39,9 @@ def cli(ctx, config):
     """
     # Elevate --config option to SENTRY_CONF env var, and just assume this
     # always will exist down the line
-    os.environ.setdefault('SENTRY_CONF', config)
+    if config:
+        os.environ['SENTRY_CONF'] = config
+    os.environ.setdefault('SENTRY_CONF', '~/.sentry')
 
 
 # TODO(mattrobenolt): Autodiscover commands?
