commit a93f45e8da421540c60404435bbf54fbbeeda073
Author: Evan Purkhiser <evanpurkhiser@gmail.com>
Date:   Fri Jul 6 13:24:52 2018 -0700

    ref(ui): Simplify circleIndicator (#8958)

diff --git a/src/sentry/static/sentry/app/components/circleIndicator.jsx b/src/sentry/static/sentry/app/components/circleIndicator.jsx
index 8e6574430a..825d3bd882 100644
--- a/src/sentry/static/sentry/app/components/circleIndicator.jsx
+++ b/src/sentry/static/sentry/app/components/circleIndicator.jsx
@@ -7,7 +7,6 @@ const getBackgroundColor = p => `
 `;
 
 const getSize = p => `
-  border-radius: ${p.size}px;
   height: ${p.size}px;
   width: ${p.size}px;
 `;
@@ -15,6 +14,7 @@ const getSize = p => `
 const Circle = styled.div`
   display: inline-block;
   position: relative;
+  border-radius: 50%;
   ${getSize} ${getBackgroundColor};
 `;
 
