commit 4e1f2e8073376a996d982c6f8416ac9f706587d1
Author: Matej Minar <matej.minar@sentry.io>
Date:   Fri Apr 3 15:37:40 2020 +0200

    feat(ui): Add beta flag to releases v2 (#18074)

diff --git a/src/sentry/static/sentry/app/components/sidebar/index.tsx b/src/sentry/static/sentry/app/components/sidebar/index.tsx
index 64a972f581..87a7bd38d4 100644
--- a/src/sentry/static/sentry/app/components/sidebar/index.tsx
+++ b/src/sentry/static/sentry/app/components/sidebar/index.tsx
@@ -529,6 +529,7 @@ class Sidebar extends React.Component<Props, State> {
                       label={t('Releases v2')}
                       to={`/organizations/${organization.slug}/releases-v2/`}
                       id="releasesv2"
+                      isBeta
                     />
                   </Feature>
                 </SidebarSection>
diff --git a/src/sentry/static/sentry/app/components/sidebar/sidebarItem.tsx b/src/sentry/static/sentry/app/components/sidebar/sidebarItem.tsx
index 47be1e1f1b..75c5c807ff 100644
--- a/src/sentry/static/sentry/app/components/sidebar/sidebarItem.tsx
+++ b/src/sentry/static/sentry/app/components/sidebar/sidebarItem.tsx
@@ -58,6 +58,10 @@ type Props = ReactRouter.WithRouterProps & {
    * Additional badge letting users know a tab is new.
    */
   isNew?: boolean;
+  /**
+   * Additional badge letting users know a tab is in beta.
+   */
+  isBeta?: boolean;
   /**
    * Sidebar is at "top" or "left" of screen
    */
@@ -75,6 +79,7 @@ const SidebarItem = ({
   active,
   hasPanel,
   isNew,
+  isBeta,
   collapsed,
   className,
   orientation,
@@ -114,6 +119,11 @@ const SidebarItem = ({
                     {t('New')}
                   </StyledTag>
                 )}
+                {isBeta && (
+                  <StyledTag priority="beta" size="small">
+                    {t('Beta')}
+                  </StyledTag>
+                )}
               </LabelHook>
             </SidebarItemLabel>
           )}
