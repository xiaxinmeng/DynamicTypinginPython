commit febaf4157219c8c3a788a807f4f5ade54e323975
Author: Armin Ronacher <armin.ronacher@active-4.com>
Date:   Thu Feb 2 20:34:40 2017 +0100

    format -> % to get unicode coercion in culprit code
    
    Fixes SENTRY-2PS

diff --git a/src/sentry/interfaces/stacktrace.py b/src/sentry/interfaces/stacktrace.py
index ba84c4496e..237955c92a 100644
--- a/src/sentry/interfaces/stacktrace.py
+++ b/src/sentry/interfaces/stacktrace.py
@@ -514,7 +514,9 @@ class Frame(Interface):
         if not fileloc:
             return ''
         elif platform == 'javascript':
-            return '{}({})'.format(self.function or '?', fileloc)
+            # function and fileloc might be unicode here, so let it coerce
+            # to a unicode string if needed.
+            return '%s(%s)' % (self.function or '?', fileloc)
         return '%s in %s' % (
             fileloc,
             self.function or '?',
