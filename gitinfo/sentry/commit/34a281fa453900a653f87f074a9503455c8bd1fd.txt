commit 34a281fa453900a653f87f074a9503455c8bd1fd
Author: Mark Story <mark@sentry.io>
Date:   Wed Jan 22 08:53:35 2020 -0500

    ref(discover) Add hover bar to headers (#16553)
    
    Column resize handles are really hard to find right now, as they don't
    display until you are right on top of them. This change adds a 1px
    border that lets users know where the handle will be.

diff --git a/src/sentry/static/sentry/app/components/gridEditable/styles.tsx b/src/sentry/static/sentry/app/components/gridEditable/styles.tsx
index 85982b295b..4623d93c94 100644
--- a/src/sentry/static/sentry/app/components/gridEditable/styles.tsx
+++ b/src/sentry/static/sentry/app/components/gridEditable/styles.tsx
@@ -138,6 +138,9 @@ export const GridHeadCell = styled('th')`
     border-top-right-radius: ${p => p.theme.borderRadius};
     border-right: none;
   }
+  &:hover {
+    border-right: 1px solid ${p => p.theme.borderDark};
+  }
 `;
 export const GridHeadCellButton = styled('div')<GridEditableProps>`
   min-width: 24px; /* Ensure that edit/remove buttons are never hidden */
