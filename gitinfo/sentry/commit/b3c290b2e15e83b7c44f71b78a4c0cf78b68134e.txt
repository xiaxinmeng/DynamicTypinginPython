commit b3c290b2e15e83b7c44f71b78a4c0cf78b68134e
Author: David Cramer <dcramer@gmail.com>
Date:   Sat Jan 26 15:42:13 2013 -0800

    More general improvements

diff --git a/src/sentry/static/sentry/less/sentry.less b/src/sentry/static/sentry/less/sentry.less
index 741060b7ab..3ee653041b 100644
--- a/src/sentry/static/sentry/less/sentry.less
+++ b/src/sentry/static/sentry/less/sentry.less
@@ -259,7 +259,6 @@ h3 {
     }
 
     .select2-container .select2-choice { 
-      width: 160px;
       height: 28px;
       line-height: 28px;
       top: -3px;
diff --git a/src/sentry/static/sentry/scripts/app.js b/src/sentry/static/sentry/scripts/app.js
index a9146efe81..1063635edd 100644
--- a/src/sentry/static/sentry/scripts/app.js
+++ b/src/sentry/static/sentry/scripts/app.js
@@ -172,7 +172,7 @@
             });
 
             var $event_nav = $('#event_nav');
-            if ($event_nav) {
+            if ($event_nav.length > 0) {
                 var $window = $(window);
                 var $nav_links = $event_nav.find('a[href*=#]');
                 var $nav_targets = [];
@@ -251,8 +251,6 @@
 
             BasePage.prototype.initialize.call(this, data);
 
-            this.el.find('select').select2({width: 'element'});
-
             if (this.options.canSelectTeam && this.options.canCreateTeam) {
                 $('#new_team').hide();
                 $('a[rel="create-new-team"]').click(function(){
diff --git a/src/sentry/static/sentry/scripts/utils.js b/src/sentry/static/sentry/scripts/utils.js
index 5a32abfe92..b31563f86b 100644
--- a/src/sentry/static/sentry/scripts/utils.js
+++ b/src/sentry/static/sentry/scripts/utils.js
@@ -124,6 +124,11 @@
 
     };
 
+    // initialize
+    $(function(){
+        $('select').select2({width: 'element'});
+    });
+
 }(app, jQuery));
 
 /**
diff --git a/src/sentry/templates/sentry/account/appearance.html b/src/sentry/templates/sentry/account/appearance.html
index 5c315741fd..d0d44f535c 100644
--- a/src/sentry/templates/sentry/account/appearance.html
+++ b/src/sentry/templates/sentry/account/appearance.html
@@ -43,10 +43,4 @@
             <button type="submit" class="btn btn-primary">{% trans "Save Changes" %}</button>
         </fieldset>
     </form>
-
-    <script>
-    $('.body select').select2({
-        width: 'element'
-    });
-    </script>
 {% endblock %}
diff --git a/src/sentry/templates/sentry/layout.html b/src/sentry/templates/sentry/layout.html
index 91bcdae68e..de28916137 100644
--- a/src/sentry/templates/sentry/layout.html
+++ b/src/sentry/templates/sentry/layout.html
@@ -168,7 +168,7 @@
             $('input[data-toggle="datepicker"]').datepicker();
             $('.tip').tooltip();
             $('.nav-tabs .active a').tab('show')
-            $('.project-selector').select2().on('change', function(e){
+            $('.project-selector').on('change', function(e){
                 var $el = $(e.target).get(0);
                 var $opt = $($el.options[$el.selectedIndex]);
                 window.location.href = $opt.attr('data-url');
diff --git a/src/sentry/templates/sentry/projects/manage.html b/src/sentry/templates/sentry/projects/manage.html
index 5410690fa1..fd8eabfa60 100644
--- a/src/sentry/templates/sentry/projects/manage.html
+++ b/src/sentry/templates/sentry/projects/manage.html
@@ -68,11 +68,6 @@
         </form>
     {% endblock %}
     </section>
-    <script>
-    $('.body select').select2({
-        width: 'element'
-    });
-    </script>
 {% endblock %}
 
 {% block sidebar %}
