commit b1f434ff7af6b9b189a817735047ff2def4c3791
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Tue Sep 29 09:26:31 2015 -0700

    Guard against NoneType EventAttributeConditions
    
    Fixes GH-2045

diff --git a/src/sentry/rules/conditions/event_attribute.py b/src/sentry/rules/conditions/event_attribute.py
index a835fb6a43..e39152bd6f 100644
--- a/src/sentry/rules/conditions/event_attribute.py
+++ b/src/sentry/rules/conditions/event_attribute.py
@@ -197,7 +197,7 @@ class EventAttributeCondition(EventCondition):
         except KeyError:
             attribute_values = []
 
-        attribute_values = [v.lower() for v in attribute_values]
+        attribute_values = [v.lower() for v in attribute_values if v is not None]
 
         if match == MatchType.EQUAL:
             for a_value in attribute_values:
