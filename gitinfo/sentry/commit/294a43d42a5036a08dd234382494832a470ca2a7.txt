commit 294a43d42a5036a08dd234382494832a470ca2a7
Author: Armin Ronacher <armin.ronacher@active-4.com>
Date:   Mon Jun 29 18:53:04 2015 +0200

    Color repr()s slightly differently than strings if it can be detected.

diff --git a/src/sentry/static/sentry/app/components/contextData.jsx b/src/sentry/static/sentry/app/components/contextData.jsx
index b438a56bbe..98392b9bec 100644
--- a/src/sentry/static/sentry/app/components/contextData.jsx
+++ b/src/sentry/static/sentry/app/components/contextData.jsx
@@ -1,6 +1,21 @@
 var React = require('react');
 var jQuery = require('jquery');
 
+function looksLikeRepr(value) {
+  var a = value[0];
+  var z = value[value.length - 1];
+  if (a == '<' && z == '>') {
+    return true;
+  } else if (a == '[' && z == ']') {
+    return true;
+  } else if (a == '(' && z == ')') {
+    return true;
+  } else if (z == ')' && value.match(/^[\w\d._-]+\(/)) {
+    return true;
+  }
+  return false;
+}
+
 
 var ContextData = React.createClass({
   propTypes: {
@@ -43,8 +58,7 @@ var ContextData = React.createClass({
       } else if (value === true || value === false) {
         return <span className="val-bool">{value ? 'True' : 'False'}</span>;
       } else if (typeof value === 'string' || value instanceof String) {
-        // consider auto quoting?
-        return <span className="val-string">{value}</span>;
+        return <span className={looksLikeRepr(value) ? 'val-repr' : 'val-string'}>{value}</span>;
       } else if (typeof value === 'number' || value instanceof Number) {
         return <span className="val-number">{value}</span>;
       } else if (value instanceof Array) {
diff --git a/src/sentry/static/sentry/less/group-detail.less b/src/sentry/static/sentry/less/group-detail.less
index f51badcd07..ae2d026fec 100644
--- a/src/sentry/static/sentry/less/group-detail.less
+++ b/src/sentry/static/sentry/less/group-detail.less
@@ -986,6 +986,10 @@ pre.val, span.val {
   color: darken(@purple, 5);
 }
 
+.val-repr {
+  color: darken(@blue, 5);
+}
+
 .val-number {
   color: @pink;
 }
