commit 8305d50a5fb866a42f9ee534ead65f20be2a3ca8
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Mar 23 12:32:21 2016 -0700

    Populate event node for issue plugin

diff --git a/src/sentry/plugins/bases/issue.py b/src/sentry/plugins/bases/issue.py
index 5a3ea8c2a4..7457a5390e 100644
--- a/src/sentry/plugins/bases/issue.py
+++ b/src/sentry/plugins/bases/issue.py
@@ -14,6 +14,7 @@ from social_auth.models import UserSocialAuth
 
 from sentry.models import (
     Activity,
+    Event,
     GroupMeta,
 )
 from sentry.plugins import Plugin
@@ -163,6 +164,7 @@ class IssueTrackingPlugin(Plugin):
 
         prefix = self.get_conf_key()
         event = group.get_latest_event()
+        Event.objects.bind_nodes([event], 'data')
 
         form = self.get_new_issue_form(request, group, event)
         if form.is_valid():
