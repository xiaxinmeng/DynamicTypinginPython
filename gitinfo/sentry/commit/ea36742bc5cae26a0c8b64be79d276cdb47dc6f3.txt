commit ea36742bc5cae26a0c8b64be79d276cdb47dc6f3
Author: David Cramer <dcramer@gmail.com>
Date:   Sat Sep 27 16:51:58 2014 -0700

    Default query to is:resolved

diff --git a/src/sentry/web/frontend/groups.py b/src/sentry/web/frontend/groups.py
index 40c061b63a..f63d8ef161 100644
--- a/src/sentry/web/frontend/groups.py
+++ b/src/sentry/web/frontend/groups.py
@@ -48,7 +48,7 @@ def _get_group_list(request, project):
         'project': project,
     }
 
-    status = request.GET.get('status', '0')
+    status = request.GET.get('status')
     if status:
         query_kwargs['status'] = int(status)
 
@@ -104,7 +104,7 @@ def _get_group_list(request, project):
     query_kwargs['offset'] = (page - 1) * EVENTS_PER_PAGE
     query_kwargs['limit'] = EVENTS_PER_PAGE + 1
 
-    query = request.GET.get('query')
+    query = request.GET.get('query', 'is:unresolved')
     if query is not None:
         query_kwargs.update(parse_query(query, request.user))
 
@@ -229,7 +229,7 @@ def group_list(request, team, project):
     except (TypeError, ValueError):
         page = 1
 
-    query = request.GET.get('query')
+    query = request.GET.get('query', 'is:unresolved')
     if query and uuid_re.match(query):
         # Forward to event if it exists
         try:
