commit c1d5cf8919c4f867638da9beb40ac35f66b0f0a7
Author: David Cramer <dcramer@gmail.com>
Date:   Fri Jan 9 14:49:25 2015 -0600

    Correct visibility check on Organization

diff --git a/src/sentry/models/organization.py b/src/sentry/models/organization.py
index 92fac60ce0..141956ad80 100644
--- a/src/sentry/models/organization.py
+++ b/src/sentry/models/organization.py
@@ -51,12 +51,12 @@ class OrganizationManager(BaseManager):
         else:
             qs = OrganizationMember.objects.filter(
                 user=user,
+                organization__status=OrganizationStatus.VISIBLE,
             ).select_related('organization')
             if access is not None:
                 # if we're requesting specific access the member *must* have
                 # global access to teams
                 qs = qs.filter(
-                    organization__status=OrganizationStatus.VISIBLE,
                     type__lte=access,
                     has_global_access=True,
                 )
