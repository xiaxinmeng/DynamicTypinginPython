commit 50c9ab63d972c0d102e10656ac89f6fdf7d9c833
Author: Markus Unterwaditzer <markus@unterwaditzer.net>
Date:   Fri Nov 22 19:36:11 2019 +0100

    fix(outcomes_consumer): Add more reasons for skipping outcomes (#15779)

diff --git a/src/sentry/ingest/outcomes_consumer.py b/src/sentry/ingest/outcomes_consumer.py
index c8c34cfe29..3ff8a23646 100644
--- a/src/sentry/ingest/outcomes_consumer.py
+++ b/src/sentry/ingest/outcomes_consumer.py
@@ -82,6 +82,7 @@ def _process_signal(msg):
 
     event_id = msg.get("event_id")
     if not event_id:
+        metrics.incr("outcomes_consumer.skip_outcome", tags={"reason": "missing_event_id"})
         return
 
     if is_signal_sent(project_id=project_id, event_id=event_id):
@@ -91,6 +92,7 @@ def _process_signal(msg):
     try:
         project = Project.objects.get_from_cache(id=project_id)
     except Project.DoesNotExist:
+        metrics.incr("outcomes_consumer.skip_outcome", tags={"reason": "unknown_project"})
         logger.error("OutcomesConsumer could not find project with id: %s", project_id)
         return
 
