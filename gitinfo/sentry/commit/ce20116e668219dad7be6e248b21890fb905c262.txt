commit ce20116e668219dad7be6e248b21890fb905c262
Author: Dan Fuller <dfuller@sentry.io>
Date:   Mon Apr 1 15:32:42 2019 -0700

    fix: Fix unstable Monitor related tests
    
    I've seen these tests fail randomly a few times, I think related to minute boundaries. Failures were
    like `AssertionError: assert datetime.datetime(2019, 3, 13, 18, 35, tzinfo=<UTC>) ==
    datetime.datetime(2019, 3, 13, 18, 34, tzinfo=<UTC>)`. Just freezing time around these tests so that
    we can hopefully avoid these issues.

diff --git a/tests/sentry/api/endpoints/test_monitor_checkins.py b/tests/sentry/api/endpoints/test_monitor_checkins.py
index 3fccb6c718..fa207cb550 100644
--- a/tests/sentry/api/endpoints/test_monitor_checkins.py
+++ b/tests/sentry/api/endpoints/test_monitor_checkins.py
@@ -2,11 +2,13 @@ from __future__ import absolute_import, print_function
 
 from datetime import timedelta
 from django.utils import timezone
+from freezegun import freeze_time
 
 from sentry.models import CheckInStatus, Monitor, MonitorCheckIn, MonitorStatus, MonitorType
 from sentry.testutils import APITestCase
 
 
+@freeze_time('2019-01-01')
 class CreateMonitorCheckInTest(APITestCase):
     def test_passing(self):
         user = self.create_user()
