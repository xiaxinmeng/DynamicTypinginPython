commit 728f8f8af0351178193aa9ae310db9d1cff7c837
Author: David Cramer <dcramer@gmail.com>
Date:   Fri Jun 7 12:47:28 2013 -0700

    Trim http data to 1024 characters

diff --git a/docs/developer/client/index.rst b/docs/developer/client/index.rst
index 3a9f4126d0..79714a7091 100644
--- a/docs/developer/client/index.rst
+++ b/docs/developer/client/index.rst
@@ -562,3 +562,4 @@ as things like extra data, or tags.
 - Tag values are limited to 200 characters.
 - Culprits are limited to 200 characters.
 - Messages are limited to 1000 characters.
+- Http data (the body) is limited to 1024 characters.
diff --git a/src/sentry/manager.py b/src/sentry/manager.py
index 97894456a8..e6259a0c86 100644
--- a/src/sentry/manager.py
+++ b/src/sentry/manager.py
@@ -482,10 +482,13 @@ class GroupManager(BaseManager, ChartMixin):
 
         if 'sentry.interfaces.Http' in data:
             http_data = data['sentry.interfaces.Http']
-            for key in ('data', 'cookies', 'querystring', 'headers', 'env'):
+            for key in ('cookies', 'querystring', 'headers', 'env', 'url'):
                 value = http_data.get(key)
                 if value:
                     http_data[key] = trim(value)
+            value = http_data.get('data')
+            if value:
+                http_data['data'] = trim(value, 1024)
 
         kwargs = {
             'level': level,
