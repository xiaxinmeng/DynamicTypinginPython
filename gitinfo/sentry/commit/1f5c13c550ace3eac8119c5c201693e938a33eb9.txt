commit 1f5c13c550ace3eac8119c5c201693e938a33eb9
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Sep 6 13:54:34 2010 -0700

    Added SENTRY_REMOTE_TIMEOUT setting

diff --git a/README.rst b/README.rst
index 5836a0d212..ebfb878195 100644
--- a/README.rst
+++ b/README.rst
@@ -108,6 +108,10 @@ And on your main application, you need to add ``sentry.client``::
 	  'sentry.client',
 	]
 
+You may also specify an alternative timeout to the default (which is 5 seconds) for all outgoing logging requests::
+
+	SENTRY_REMOTE_TIMEOUT = 5
+
 #####################
 SENTRY_DATABASE_USING
 #####################
diff --git a/sentry/client/base.py b/sentry/client/base.py
index 7b09e65083..2f88fd807b 100644
--- a/sentry/client/base.py
+++ b/sentry/client/base.py
@@ -47,7 +47,7 @@ class SentryClient(object):
             req = urllib2.Request(settings.REMOTE_URL, urllib.urlencode(data))
 
             try:
-                response = urllib2.urlopen(req).read()
+                response = urllib2.urlopen(req, None, settings.REMOTE_TIMEOUT).read()
             except urllib2.URLError, e:
                 logger.exception('Unable to reach Sentry log server')
             except urllib2.HTTPError, e:
diff --git a/sentry/settings.py b/sentry/settings.py
index 1131ee2a1f..325c23f89a 100644
--- a/sentry/settings.py
+++ b/sentry/settings.py
@@ -31,4 +31,6 @@ LOG_LEVELS = (
 )
 
 # This should be the full URL to sentries store view
-REMOTE_URL = getattr(settings, 'SENTRY_REMOTE_URL', None)
\ No newline at end of file
+REMOTE_URL = getattr(settings, 'SENTRY_REMOTE_URL', None)
+
+REMOTE_TIMEOUT = getattr(settings, 'SENTRY_REMOTE_TIMEOUT', 5)
\ No newline at end of file
