commit d2a41f7d9c0cb08b185b3bce4d9d296cd8b437bd
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Jul 11 09:37:08 2018 -0700

    feat: Support language override in UI

diff --git a/src/sentry/static/sentry/app/stores/configStore.jsx b/src/sentry/static/sentry/app/stores/configStore.jsx
index 3bafc37ec8..918d392488 100644
--- a/src/sentry/static/sentry/app/stores/configStore.jsx
+++ b/src/sentry/static/sentry/app/stores/configStore.jsx
@@ -22,7 +22,7 @@ const ConfigStore = Reflux.createStore({
     return this.config;
   },
 
-  loadInitialData(config) {
+  loadInitialData(config, languageOverride = null) {
     config.features = new Set(config.features || []);
     this.config = config;
 
@@ -30,7 +30,9 @@ const ConfigStore = Reflux.createStore({
     if (config.user) {
       config.user.permissions = new Set(config.user.permissions);
       moment.tz.setDefault(config.user.options.timezone);
-      setLocale(config.user.options.language || 'en');
+      setLocale(languageOverride || config.user.options.language || 'en');
+    } else if (languageOverride) {
+      setLocale(languageOverride);
     }
 
     this.trigger(config);
diff --git a/src/sentry/templates/sentry/layout.html b/src/sentry/templates/sentry/layout.html
index d0c80d3c1b..450f7f8101 100644
--- a/src/sentry/templates/sentry/layout.html
+++ b/src/sentry/templates/sentry/layout.html
@@ -54,7 +54,7 @@
     // if the ads.js file loads below it will mark this variable as false
     window.adblockSuspected = true;
 
-    Sentry.ConfigStore.loadInitialData({% get_react_config %});
+    Sentry.ConfigStore.loadInitialData({% get_react_config %}, {{ request.LANGUAGE_CODE|to_json|safe }});
   </script>
   <script src="{% asset_url 'sentry' 'js/ads.js' %}"></script>
 
