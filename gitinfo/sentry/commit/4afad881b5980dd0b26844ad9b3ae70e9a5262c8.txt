commit 4afad881b5980dd0b26844ad9b3ae70e9a5262c8
Author: ckj <chriskjennings@gmail.com>
Date:   Fri Dec 18 17:08:09 2015 -0800

    Use our own grid for compact issues

diff --git a/src/sentry/static/sentry/app/components/compactIssue.jsx b/src/sentry/static/sentry/app/components/compactIssue.jsx
index f47a16b380..83c852568f 100644
--- a/src/sentry/static/sentry/app/components/compactIssue.jsx
+++ b/src/sentry/static/sentry/app/components/compactIssue.jsx
@@ -145,6 +145,10 @@ const CompactIssue = React.createClass({
 
     className += ' level-' + issue.level;
 
+    if (this.props.statsPeriod) {
+      className += ' with-graph';
+    }
+
     let {id, orgId} = this.props;
     let projectId = issue.project.slug;
 
@@ -152,8 +156,7 @@ const CompactIssue = React.createClass({
 
     return (
       <li className={className} onClick={this.toggleSelect}>
-        <div className="row">
-          <div className={this.props.statsPeriod ? 'col-md-9' : 'col-md-11'}>
+          <div>
             <span className="error-level truncate" title={issue.level}></span>
             <h3 className="truncate">
               <Link to={`/${orgId}/${projectId}/issues/${id}/`}>
@@ -178,11 +181,11 @@ const CompactIssue = React.createClass({
             </div>
           </div>
           {this.props.statsPeriod &&
-            <div className="col-md-2 hidden-sm hidden-xs event-graph align-right">
+            <div className="event-graph">
               <GroupChart id={id} statsPeriod={this.props.statsPeriod} />
             </div>
           }
-          <div className="col-md-1 align-right">
+          <div className="more-menu-container align-right">
             <DropdownLink
               topLevelClasses="more-menu"
               className="more-menu-toggle"
@@ -210,7 +213,6 @@ const CompactIssue = React.createClass({
               }
             </DropdownLink>
           </div>
-        </div>
         {this.props.children}
       </li>
     );
diff --git a/src/sentry/static/sentry/less/stream.less b/src/sentry/static/sentry/less/stream.less
index d32e4ec367..7562a17a45 100644
--- a/src/sentry/static/sentry/less/stream.less
+++ b/src/sentry/static/sentry/less/stream.less
@@ -661,8 +661,9 @@
   box-shadow: 0 1px 3px rgba(0,0,0, .04);
 
   .issue {
-    padding: 12px 15px 5px 35px;
+    padding: 12px 40px 5px 35px;
     border-bottom: 1px solid lighten(@trim, 5);
+    position: relative;
 
     h3 {
       .icon-bookmark, .icon-soundoff {
@@ -710,11 +711,11 @@
     }
 
     .error-level {
-      .square(13px);
+      .square(12px);
       border-radius: 50%;
       position: absolute;
-      top: 2px;
-      left: -8px;
+      top: 13px;
+      left: 12px;
     }
 
     h3 {
@@ -851,7 +852,21 @@
     }
 
     .event-graph {
-      padding-top: 7px;
+      width: 125px;
+      position: absolute;
+      right: 40px;
+      top: 20px;
+    }
+
+    .more-menu-container {
+      position: absolute;
+      width: 20px;
+      right: 14px;
+      top: 13px;
+    }
+
+    &.with-graph {
+      padding-right: 180px;
     }
 
     &:last-child {
