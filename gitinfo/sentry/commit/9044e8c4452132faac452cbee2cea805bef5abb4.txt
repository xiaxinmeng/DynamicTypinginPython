commit 9044e8c4452132faac452cbee2cea805bef5abb4
Author: David Cramer <dcramer@gmail.com>
Date:   Sun Jan 27 13:54:26 2013 -0800

    Restrict Message.message value to 1000 characters or less

diff --git a/src/sentry/interfaces.py b/src/sentry/interfaces.py
index 473005b8bc..3b177b1dfb 100644
--- a/src/sentry/interfaces.py
+++ b/src/sentry/interfaces.py
@@ -153,6 +153,8 @@ class Message(Interface):
     If your message cannot be parameterized, then the message interface
     will serve no benefit.
 
+    - ``message`` must be no more than 1000 characters in length.
+
     >>> {
     >>>     "message": "My raw message with interpreted strings like %s",
     >>>     "params": ["this"]
@@ -160,6 +162,8 @@ class Message(Interface):
     """
 
     def __init__(self, message, params=()):
+        assert len(message) <= 1000
+
         self.message = message
         self.params = params
 
