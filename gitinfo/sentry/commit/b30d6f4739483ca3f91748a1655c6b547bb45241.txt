commit b30d6f4739483ca3f91748a1655c6b547bb45241
Author: Lyn Nagara <lyn.nagara@gmail.com>
Date:   Tue Dec 11 11:38:04 2018 -0800

    feat(sentry10): Add settings link behind feature flag
    
    Adds a settings link to the sidebar if the sentry10 feature switch is
    enabled

diff --git a/src/sentry/static/sentry/app/components/sidebar/index.jsx b/src/sentry/static/sentry/app/components/sidebar/index.jsx
index a99ad26f09..79ba2a991f 100644
--- a/src/sentry/static/sentry/app/components/sidebar/index.jsx
+++ b/src/sentry/static/sentry/app/components/sidebar/index.jsx
@@ -309,6 +309,17 @@ class Sidebar extends React.Component {
                 hidePanel={this.hidePanel}
               />
             </SidebarSection>
+            <Feature features={['sentry10']}>
+              <SidebarSection>
+                <SidebarItem
+                  {...sidebarItemProps}
+                  onClick={this.hidePanel}
+                  icon={<InlineSvg src="icon-settings" />}
+                  label={t('Settings')}
+                  to={`/organizations/${organization.slug}/settings/`}
+                />
+              </SidebarSection>
+            </Feature>
 
             {!horizontal && (
               <SidebarSection noMargin>
