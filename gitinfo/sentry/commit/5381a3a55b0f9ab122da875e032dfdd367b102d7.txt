commit 5381a3a55b0f9ab122da875e032dfdd367b102d7
Author: Evan Purkhiser <evanpurkhiser@gmail.com>
Date:   Wed Mar 11 21:06:36 2020 -0700

    ref(ts): Correctly mark Theme in circleIndicator (#17595)

diff --git a/src/sentry/static/sentry/app/components/circleIndicator.tsx b/src/sentry/static/sentry/app/components/circleIndicator.tsx
index fbe8da9295..d20e026de7 100644
--- a/src/sentry/static/sentry/app/components/circleIndicator.tsx
+++ b/src/sentry/static/sentry/app/components/circleIndicator.tsx
@@ -1,6 +1,8 @@
 import PropTypes from 'prop-types';
 import styled from '@emotion/styled';
 
+import {Theme} from 'app/utils/theme';
+
 const defaultProps = {
   enabled: true,
   size: 14,
@@ -10,10 +12,9 @@ type DefaultProps = Readonly<typeof defaultProps>;
 
 type Props = {
   color?: string;
-  theme?: any;
 } & Partial<DefaultProps>;
 
-const getBackgroundColor = (p: Props) => {
+const getBackgroundColor = (p: Props & {theme: Theme}) => {
   if (p.color) {
     return `background: ${p.color};`;
   }
