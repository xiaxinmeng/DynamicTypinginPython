commit 8089335d1138f66fb6ed25c0d98122ce442a3a9b
Author: Vanč Levstik <vanc.levstik@gmail.com>
Date:   Tue Jul 8 18:42:30 2014 +0200

    Add Pyramid Python as available platform

diff --git a/src/sentry/constants.py b/src/sentry/constants.py
index f553fc8115..d55fd91626 100644
--- a/src/sentry/constants.py
+++ b/src/sentry/constants.py
@@ -87,6 +87,7 @@ PLATFORM_LIST = (
     'javascript',
     'node.js',
     'php',
+    'pyramid',
     'python',
     'r',
     'ruby',
@@ -104,6 +105,7 @@ PLATFORM_ROOTS = {
     'sidekiq': 'ruby',
     'django': 'python',
     'flask': 'python',
+    'pyramid': 'python',
     'tornado': 'python',
     'express': 'node.js',
     'connect': 'node.js',
@@ -122,6 +124,7 @@ PLATFORM_TITLES = {
     'connect': 'Connect (Node.js)',
     'django': 'Django (Python)',
     'flask': 'Flask (Python)',
+    'pyramid': 'Pyramid (Python)',
     'csharp': 'C#',
     'java_log4j': 'Log4j (Java)',
     'java_log4j2': 'Log4j 2.x (Java)',
diff --git a/src/sentry/templates/sentry/partial/client_config/pyramid.html b/src/sentry/templates/sentry/partial/client_config/pyramid.html
new file mode 100644
index 0000000000..3144268ff9
--- /dev/null
+++ b/src/sentry/templates/sentry/partial/client_config/pyramid.html
@@ -0,0 +1,63 @@
+{% extends "sentry/partial/client_config/python_base.html" %}
+
+{% load i18n %}
+
+{% block inner %}
+    <p>{% blocktrans with 'http://pythonpaste.org/deploy' as link %}A filter factory for <a href="{{ link }}">PasteDeploy</a> exists to allow easily inserting Raven into a WSGI pipeline:{% endblocktrans %}</p>
+
+    <pre>
+[pipeline:main]
+pipeline =
+    raven
+    tm
+    MyApp
+
+[filter:raven]
+use = egg:raven#raven
+dsn = http://public:secret@example.com/1
+include_paths = my.package, my.other.package
+exclude_paths = my.package.crud
+    </pre>
+    <p>{% trans " In the <em>[filter:raven]</em> section, you must specify the entry-point for raven with the <em>use =</em> key. All other raven client parameters can be included in this section as well." %}</p>
+
+
+    <p>{% trans "Add the following lines to your project's <em>.ini</em> file to setup <em>SentryHandler</em>:" %}</p>
+
+    <pre>
+[loggers]
+keys = root, sentry
+
+[handlers]
+keys = console, sentry
+
+[formatters]
+keys = generic
+
+[logger_root]
+level = INFO
+handlers = console, sentry
+
+[logger_sentry]
+level = WARN
+handlers = console
+qualname = sentry.errors
+propagate = 0
+
+[handler_console]
+class = StreamHandler
+args = (sys.stderr,)
+level = NOTSET
+formatter = generic
+
+[handler_sentry]
+class = raven.handlers.logging.SentryHandler
+args = ('http://public:secret@example.com/1',)
+level = WARNING
+formatter = generic
+
+[formatter_generic]
+format = %(asctime)s,%(msecs)03d %(levelname)-5.5s [%(name)s] %(message)s
+datefmt = %H:%M:%S
+    </pre>
+
+{% endblock %}
diff --git a/src/sentry/templates/sentry/partial/client_config/python_base.html b/src/sentry/templates/sentry/partial/client_config/python_base.html
index a0a5d68ef3..42540a286e 100644
--- a/src/sentry/templates/sentry/partial/client_config/python_base.html
+++ b/src/sentry/templates/sentry/partial/client_config/python_base.html
@@ -9,13 +9,14 @@
 	<li{% if platform == 'python' %} class="active"{% endif %}><a href="{% url 'sentry-docs-client' project.team.slug project.slug 'python' %}">Python</a></li>
 	<li{% if platform == 'django' %} class="active"{% endif %}><a href="{% url 'sentry-docs-client' project.team.slug project.slug 'django' %}">Django</a></li>
 	<li{% if platform == 'flask' %} class="active"{% endif %}><a href="{% url 'sentry-docs-client' project.team.slug project.slug 'flask' %}">Flask</a></li>
+	<li{% if platform == 'pyramid' %} class="active"{% endif %}><a href="{% url 'sentry-docs-client' project.team.slug project.slug 'pyramid' %}">Pyramid</a></li>
 	<li{% if platform == 'tornado' %} class="active"{% endif %}><a href="{% url 'sentry-docs-client' project.team.slug project.slug 'tornado' %}">Tornado</a></li>
 </ul>
 
 <p>{% blocktrans with 'https://github.com/getsentry/raven-python' as link %}Start by installing <a href="{{ link }}">raven-python</a>:{% endblocktrans %}</p>
 
 <pre>pip install raven</pre>
-    
+
 {% block inner %}
 {% endblock %}
 
