commit 9baf5d2efb08a7e842e077a514fa54edbd8f10a5
Author: David Cramer <dcramer@gmail.com>
Date:   Fri Jan 23 16:24:48 2015 -0800

    Some basic rendering of exception values

diff --git a/src/sentry/static/sentry/app/views/aggregate/event.jsx b/src/sentry/static/sentry/app/views/aggregate/event.jsx
index 26a5f03071..1269f7addb 100644
--- a/src/sentry/static/sentry/app/views/aggregate/event.jsx
+++ b/src/sentry/static/sentry/app/views/aggregate/event.jsx
@@ -25,7 +25,7 @@ var AggregateEvent = React.createClass({
     var evt = this.props.event;
 
     var entries = [];
-    evt.entries.forEach(function(entry, _){
+    evt.entries.forEach(function(entry, entryIdx){
       try {
         var Component = this.interfaces[entry.type];
         if (!Component) {
@@ -34,7 +34,7 @@ var AggregateEvent = React.createClass({
         }
         entries.push(
           <Component
-              key={"entry-" + _}
+              key={"entry-" + entryIdx}
               aggregate={agg}
               event={evt}
               data={entry.data} />
diff --git a/src/sentry/static/sentry/app/views/aggregate/interfaces/exception.jsx b/src/sentry/static/sentry/app/views/aggregate/interfaces/exception.jsx
index 693bb73c84..b8bc8cbae2 100644
--- a/src/sentry/static/sentry/app/views/aggregate/interfaces/exception.jsx
+++ b/src/sentry/static/sentry/app/views/aggregate/interfaces/exception.jsx
@@ -17,12 +17,35 @@ var ExceptionInterface = React.createClass({
   render: function(){
     var agg = this.props.aggregate;
     var evt = this.props.event;
+    var data = this.props.data;
+
+    var children = [];
+    data.values.forEach(function(exc, excIdx){
+      // TODO(dcramer): This is basically completely wrong rendering atm
+
+      var frames = [];
+      exc.stacktrace.frames.forEach(function(frame){
+      });
+
+      children.push(
+        <div className="traceback" key={"exc-" + excIdx}>
+          <h3>
+            <span>{exc.type}</span>
+          </h3>
+          {exc.value &&
+            <pre>{exc.value}</pre>
+          }
+          {frames}
+        </div>
+      );
+    });
 
     return (
       <AggregateEventDataSection
           aggregate={agg}
           event={evt}
           title="Exception">
+        {children}
       </AggregateEventDataSection>
     );
   }
