commit 19a9af7addae34e685b1d4ae3369bc3f61a7e636
Author: ckj <chriskjennings@gmail.com>
Date:   Fri Aug 28 01:37:35 2015 -0400

    Clean up group detail alerts

diff --git a/src/sentry/static/sentry/app/components/eventEntries.jsx b/src/sentry/static/sentry/app/components/eventEntries.jsx
index 9de4ad2d95..775fec8c84 100644
--- a/src/sentry/static/sentry/app/components/eventEntries.jsx
+++ b/src/sentry/static/sentry/app/components/eventEntries.jsx
@@ -62,7 +62,7 @@ var EventErrors = React.createClass({
           type="errors"
           className="errors">
         <p>
-          <a className="btn btn-default btn-sm pull-right" onClick={this.toggle}>{isOpen ? 'Hide' : 'Show'}</a>
+          <a className="pull-right" onClick={this.toggle}>{isOpen ? 'Hide' : 'Show'}</a>
           There {numErrors != 1 ? ('were ' + numErrors + ' errors') : 'was 1 error'} encountered while processing this event.
         </p>
         <ul style={{display: isOpen ? 'block' : 'none'}}>
diff --git a/src/sentry/static/sentry/app/views/groupOverview.jsx b/src/sentry/static/sentry/app/views/groupOverview.jsx
index a3870acec8..cbf1fc7ea6 100644
--- a/src/sentry/static/sentry/app/views/groupOverview.jsx
+++ b/src/sentry/static/sentry/app/views/groupOverview.jsx
@@ -292,11 +292,11 @@ var GroupOverview = React.createClass({
               <LoadingError onRetry={this.fetchData} />
             :
               <div>
-                <div className="box">
-                  <div className="box-header box-header-borderless">
+                <div className="alert alert-block">
+
                     <div className="pull-right">
                     <Router.Link to="groupEventDetails"
-                    className="btn"  
+                    className=""
                     params={{
                       projectId: projectId,
                       orgId: orgId,
@@ -304,8 +304,7 @@ var GroupOverview = React.createClass({
                       eventId: evt.id
                     }}>More Details</Router.Link>
                     </div>
-                    <h3>This summary is based on the most recent event in this aggregate.</h3>
-                  </div>
+                    This summary is based on the most recent event in this aggregate.
                 </div>
                 <GroupEventEntries
                     group={group}
diff --git a/src/sentry/static/sentry/less/group-detail.less b/src/sentry/static/sentry/less/group-detail.less
index 1ac11d95b2..c0885c8698 100644
--- a/src/sentry/static/sentry/less/group-detail.less
+++ b/src/sentry/static/sentry/less/group-detail.less
@@ -1089,20 +1089,33 @@ div.commands {
 }
 
 .box.errors {
-  border-color: @red;
-  background: #fff;
-  color: #000;
+  border-color: #DAB9BB;
+  background: #FFF7F7;
+  color: #381618;
+  font-size: 15px;
 
   .box-content {
-    p {
-      color: #d21c25;
+    padding: 10px 20px;
+
+    a {
       font-weight: bold;
+      color: #6D292D;
+      &:hover {
+        color: darken(#6D292D, 10%);
+      }
+    }
+
+    p {
+      margin: 0;
+      color: #9A3A40;
+      line-height: 1.5;
+
       .btn {
         margin-left: 5px;
       }
     }
     ul {
-      margin-bottom: 20px;
+      margin: 10px 0;
       li {
         margin-bottom: 5px;
       }
diff --git a/src/sentry/static/sentry/less/shared-components.less b/src/sentry/static/sentry/less/shared-components.less
index 14cdaa3b6f..4e5a0c8ec1 100644
--- a/src/sentry/static/sentry/less/shared-components.less
+++ b/src/sentry/static/sentry/less/shared-components.less
@@ -1735,15 +1735,16 @@ pre.clippy {
 }
 
 .alert {
-  background: @gray;
-  color: #fff;
-  border: 0;
+  background: #FDFBF1;
+  color: #866938;
+  border: 1px solid #E2D2AD;
   font-weight: 400;
   padding: 10px 20px;
   border-radius: 0;
   margin: 0;
   font-size: 15px;
   line-height: 1.4;
+  box-shadow: 0 1px 1px rgba(0,0,0, .03);
 
   p:last-child {
     margin: 0;
@@ -1776,7 +1777,8 @@ pre.clippy {
 
     &:hover {
       box-shadow: none;
-      color: #fff;
+      font-weight: bold;
+      color: #66512c;
       opacity: 1;
     }
   }
@@ -1797,6 +1799,9 @@ pre.clippy {
 
 .alert-error {
   background: @red;
+  color: #fff;
+  border: 0;
+
   .icon:before {
     content: "\e615";
   }
@@ -1804,6 +1809,9 @@ pre.clippy {
 
 .alert-info {
   background: @blue;
+  color: #fff;
+  border: 0;
+
   .icon {
     display: none;
   }
diff --git a/src/sentry/static/sentry/less/variables.less b/src/sentry/static/sentry/less/variables.less
index 778a9690db..9f8f1c86b4 100644
--- a/src/sentry/static/sentry/less/variables.less
+++ b/src/sentry/static/sentry/less/variables.less
@@ -2,7 +2,7 @@
 @bgColor: lighten(#f5f8fa, .75);
 @bgColorDarker: #f6f9fc;
 @baseFontSize: 13px;
-@gray: lighten(#455060, 8);
+@gray: lighten(#4A5361, 10);
 @gray-light: lighten(@gray, 24);
 @gray-dark: #343B3E;
 @navbarInverseLinkBackgroundActive: #333;
