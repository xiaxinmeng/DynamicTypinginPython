commit 4456d5a46891eea97288bc501818e461b5cd9f2c
Author: Harm Verhagen <harm.verhagen@gmail.com>
Date:   Tue Mar 8 12:06:20 2011 +0000

    fix: issue 60 sentry permissions not working
    https://github.com/dcramer/django-sentry/issues#issue/60

diff --git a/sentry/views.py b/sentry/views.py
index 4d5487957f..79aeadb13a 100644
--- a/sentry/views.py
+++ b/sentry/views.py
@@ -55,7 +55,7 @@ def login_required(func):
         if not conf.PUBLIC:
             if not request.user.is_authenticated():
                 return HttpResponseRedirect(reverse('sentry-login'))
-            if not request.user.has_perm('sentry_groupedmessage.can_view'):
+            if not request.user.has_perm('sentry.can_view'):
                 return HttpResponseRedirect(reverse('sentry-login'))
         return func(request, *args, **kwargs)
     wrapped.__doc__ = func.__doc__
