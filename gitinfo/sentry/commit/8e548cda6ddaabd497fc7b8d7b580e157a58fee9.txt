commit 8e548cda6ddaabd497fc7b8d7b580e157a58fee9
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Feb 24 14:16:33 2015 -0800

    Correct handling of Response type

diff --git a/src/sentry/web/frontend/organization_auth_settings.py b/src/sentry/web/frontend/organization_auth_settings.py
index ae78b9fad4..6acfeec54f 100644
--- a/src/sentry/web/frontend/organization_auth_settings.py
+++ b/src/sentry/web/frontend/organization_auth_settings.py
@@ -11,6 +11,7 @@ from sentry.auth.helper import AuthHelper
 from sentry.models import (
     AuditLogEntry, AuditLogEntryEvent, AuthProvider, OrganizationMemberType
 )
+from sentry.plugins import Response
 from sentry.utils.http import absolute_uri
 from sentry.web.frontend.base import OrganizationView
 
@@ -46,6 +47,12 @@ class OrganizationAuthSettingsView(OrganizationView):
         response = view(request, organization, auth_provider)
         if isinstance(response, HttpResponse):
             return response
+        elif isinstance(response, Response):
+            response = response.render(request, {
+                'auth_provider': auth_provider,
+                'organization': organization,
+                'provider': provider,
+            })
 
         context = {
             'login_url': absolute_uri(reverse('sentry-organization-home', args=[organization.slug])),
