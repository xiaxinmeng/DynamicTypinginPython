commit 50a9ee6e60200f799ca0f8d41728e28079fbba7d
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Dec 26 18:56:50 2011 -0800

    Default logger of javascript for js client

diff --git a/sentry/static/scripts/client.js b/sentry/static/scripts/client.js
index c2377cb2f4..a7a5588692 100644
--- a/sentry/static/scripts/client.js
+++ b/sentry/static/scripts/client.js
@@ -13,7 +13,8 @@ if (Sentry === undefined) {
         fetchHeaders: false,
         publicKey: null,
         server: '/sentry/store/', // JS client only supports a single server
-        projectId: 1
+        projectId: 1,
+        logger: 'javascript'
     };
 
     Sentry.client.config = function(data){
@@ -60,6 +61,7 @@ if (Sentry === undefined) {
 
     Sentry.client.process = function(data) {
         data.project = self.options.projectId;
+        data.logger = self.options.logger;
 
         var req = new XMLHttpRequest();
         // req.setRequestHeader('User-Agent', 'Sentry:JS/1.0');
