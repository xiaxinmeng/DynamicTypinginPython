commit 9d1680ee2f0cb8e1ff9d5ebf35d7f5df11518ea8
Author: Evan Purkhiser <evanpurkhiser@gmail.com>
Date:   Thu Nov 7 01:56:03 2019 -0800

    chore(ts): Convert notFound errors (#15483)

diff --git a/src/sentry/static/sentry/app/components/errors/notFound.jsx b/src/sentry/static/sentry/app/components/errors/notFound.jsx
deleted file mode 100644
index 83a0807e31..0000000000
--- a/src/sentry/static/sentry/app/components/errors/notFound.jsx
+++ /dev/null
@@ -1,38 +0,0 @@
-import React from 'react';
-import {t} from 'app/locale';
-
-class NotFound extends React.Component {
-  render() {
-    // TODO(chrissy): use scale for margin values
-    return (
-      <div className="alert alert-block alert-error" style={{margin: '30px 0 10px'}}>
-        <div style={{fontSize: 24, marginBottom: 10}}>
-          <span className="icon-exclamation" style={{fontSize: 20, marginRight: 10}} />
-          <span>{t('Page Not Found')}</span>
-        </div>
-        <p>The page you are looking for was not found.</p>
-        <p>You may wish to try the following:</p>
-        <ul>
-          <li>
-            If you entered the address manually, double check the path. Did you forget a
-            trailing slash?
-          </li>
-          <li>
-            If you followed a link here, try hitting back and reloading the page. It's
-            possible the resource was moved out from under you.
-          </li>
-          <li>
-            If all else fails,{' '}
-            <a href="http://github.com/getsentry/sentry/issues">create an issue</a> with
-            more details.
-          </li>
-        </ul>
-        <p>
-          Not sure what to do? <a href="/">Return to the dashboard</a>
-        </p>
-      </div>
-    );
-  }
-}
-
-export default NotFound;
diff --git a/src/sentry/static/sentry/app/components/errors/notFound.tsx b/src/sentry/static/sentry/app/components/errors/notFound.tsx
new file mode 100644
index 0000000000..b5f54984a7
--- /dev/null
+++ b/src/sentry/static/sentry/app/components/errors/notFound.tsx
@@ -0,0 +1,35 @@
+import React from 'react';
+
+import {t} from 'app/locale';
+
+// TODO(chrissy): use scale for margin values
+const NotFound = () => (
+  <div className="alert alert-block alert-error" style={{margin: '30px 0 10px'}}>
+    <div style={{fontSize: 24, marginBottom: 10}}>
+      <span className="icon-exclamation" style={{fontSize: 20, marginRight: 10}} />
+      <span>{t('Page Not Found')}</span>
+    </div>
+    <p>The page you are looking for was not found.</p>
+    <p>You may wish to try the following:</p>
+    <ul>
+      <li>
+        If you entered the address manually, double check the path. Did you forget a
+        trailing slash?
+      </li>
+      <li>
+        If you followed a link here, try hitting back and reloading the page. It's
+        possible the resource was moved out from under you.
+      </li>
+      <li>
+        If all else fails,{' '}
+        <a href="http://github.com/getsentry/sentry/issues">create an issue</a> with more
+        details.
+      </li>
+    </ul>
+    <p>
+      Not sure what to do? <a href="/">Return to the dashboard</a>
+    </p>
+  </div>
+);
+
+export default NotFound;
diff --git a/src/sentry/static/sentry/app/views/routeNotFound.jsx b/src/sentry/static/sentry/app/views/routeNotFound.tsx
similarity index 100%
rename from src/sentry/static/sentry/app/views/routeNotFound.jsx
rename to src/sentry/static/sentry/app/views/routeNotFound.tsx
