commit 4b1228c6d3ed0f4b7b332f767646b29cfc373c0e
Author: David Cramer <dcramer@gmail.com>
Date:   Fri Jul 31 13:42:31 2015 -0700

    Handle HTML in notes (Fixes GH-1669)

diff --git a/src/sentry/static/sentry/app/views/groupActivity.jsx b/src/sentry/static/sentry/app/views/groupActivity.jsx
index 70aa6c9aef..2608067ac7 100644
--- a/src/sentry/static/sentry/app/views/groupActivity.jsx
+++ b/src/sentry/static/sentry/app/views/groupActivity.jsx
@@ -163,13 +163,14 @@ var GroupActivity = React.createClass({
       var label = formatActivity(item);
 
       if (item.type === 'note') {
+        var noteBody = utils.nl2br(utils.urlize(utils.escape(item.data.text)));
         return (
           <li className="activity-note" key={itemIdx}>
             {avatar}
             <div className="activity-bubble">
               <TimeSince date={item.dateCreated} />
               <div className="activity-author">{authorName}</div>
-              <p>{ utils.nl2br(utils.urlize(utils.escape(item.data.text))) }</p>
+              <p dangerouslySetInnerHTML={{__html: noteBody}} />
             </div>
           </li>
         );
