commit 327b59d763f1c50692cd5fa1944d529179db769f
Author: David Cramer <dcramer@gmail.com>
Date:   Tue May 7 21:23:24 2013 -0700

    minor tweaks to ProjectOption usage

diff --git a/src/sentry/models.py b/src/sentry/models.py
index 391d6ddcbb..39d789bf09 100644
--- a/src/sentry/models.py
+++ b/src/sentry/models.py
@@ -385,9 +385,7 @@ class ProjectOption(Model):
     key = models.CharField(max_length=64)
     value = PickledObjectField()
 
-    objects = InstanceMetaManager('project', cache_fields=[
-        'project_id',
-    ])
+    objects = InstanceMetaManager('project')
 
     class Meta:
         db_table = 'sentry_projectoptions'
diff --git a/src/sentry/tasks/check_alerts.py b/src/sentry/tasks/check_alerts.py
index 507e303cd5..ea09a2847c 100644
--- a/src/sentry/tasks/check_alerts.py
+++ b/src/sentry/tasks/check_alerts.py
@@ -65,10 +65,10 @@ def check_project_alerts(project_id, when, count, **kwargs):
 
     # TODO: make this use the cache
     try:
-        threshold, min_events = ProjectOption.objects.get(
+        threshold, min_events = ProjectOption.objects.get_value(
             project=project_id,
             key='alert:threshold',
-        ).value
+        )
     except ProjectOption.DoesNotExist:
         threshold, min_events = settings.DEFAULT_ALERT_PROJECT_THRESHOLD
 
