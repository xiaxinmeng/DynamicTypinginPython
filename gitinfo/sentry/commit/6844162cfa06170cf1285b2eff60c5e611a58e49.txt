commit 6844162cfa06170cf1285b2eff60c5e611a58e49
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Feb 4 12:42:43 2013 -0800

    Disable Pygments lexing until we determine a more efficient means

diff --git a/src/sentry/interfaces.py b/src/sentry/interfaces.py
index bacc0d776a..9e986a4a44 100644
--- a/src/sentry/interfaces.py
+++ b/src/sentry/interfaces.py
@@ -61,10 +61,11 @@ def get_context(lineno, context_line, pre_context=None, post_context=None, filen
         filename = 'index.html'
 
     if format:
-        try:
-            lexer = get_lexer_for_filename(filename)
-        except ClassNotFound:
-            lexer = TextLexer()
+        # try:
+        #     lexer = get_lexer_for_filename(filename)
+        # except ClassNotFound:
+        #     lexer = TextLexer()
+        lexer = TextLexer()
 
         formatter = HtmlFormatter()
 
