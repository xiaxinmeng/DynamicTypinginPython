commit 7065f7b4e3f5b5cac4396ef993fafc2bdc65b911
Author: ckj <chriskjennings@gmail.com>
Date:   Thu Jul 30 02:41:49 2015 -0400

    Style selectize in rule forms

diff --git a/src/sentry/static/sentry/app/components/selectize.jsx b/src/sentry/static/sentry/app/components/selectize.jsx
index 12610299fc..1f90f903a3 100644
--- a/src/sentry/static/sentry/app/components/selectize.jsx
+++ b/src/sentry/static/sentry/app/components/selectize.jsx
@@ -141,7 +141,7 @@ var Selectize = React.createClass({
   render() {
     var opts = {
         ref: 'select',
-        className: "form-control " + (this.props.className || '')
+        className: " " + (this.props.className || '')
     };
     if (this.props.disabled) {
       opts.disabled = true;
@@ -150,11 +150,9 @@ var Selectize = React.createClass({
       opts.multiple = true;
     }
     return (
-      <div className="form-group">
-        <select {...opts}>
-          {this.props.children}
-        </select>
-      </div>
+      <select {...opts}>
+        {this.props.children}
+      </select>
     );
   }
 });
diff --git a/src/sentry/static/sentry/less/project-settings.less b/src/sentry/static/sentry/less/project-settings.less
index 7600d30f5b..ff10fce137 100644
--- a/src/sentry/static/sentry/less/project-settings.less
+++ b/src/sentry/static/sentry/less/project-settings.less
@@ -3,17 +3,37 @@
 * ============================================================================
 */
 
-.selectize-inline-block {
+.rule-form {
+  vertical-align: middle;
+  height: 48px;
+}
+
+.rule-form input[type="text"], .rule-form input[type="number"] {
+  .form-control;
+  display: inline-block;
+  padding: 3px 8px;
+  width: auto;
+  vertical-align: middle;
+  position: relative;
+  top: -1px;
+  line-height: 1.5;
+  margin: 0 6px;
+}
+
+.rule-form input[type="text"] + .selectize-control .selectize-input {
+  margin: 0;
+}
+
+.selectize-inline, .rule-form .selectize-control {
   display: inline-block;
   padding: 0 6px;
-  margin-bottom: 5px !important;
+  margin-bottom: 0 !important;
+  vertical-align: middle;
 
   .selectize-input {
     font-size: 90%;
     padding: 6px 15px 5px 8px;
     min-width: 70px;
-    position: relative;
-    top: 8px;
 
     &:after {
       top: 46% !important;
@@ -25,11 +45,11 @@
   .selectize-dropdown {
     z-index: 5000;
     padding: 0 !important;
+    font-size: 95%;
   }
 }
 
 .rules-list, .rule-detail {
-
   h6 {
     font-weight: 600;
     select {
@@ -70,7 +90,7 @@
     }
   }
 
-  select {
+  .selectize-control {
     margin-bottom: 20px;
   }
 
