commit 044cc543903d4d381226aa0b999e9c4849850e87
Author: James Cunningham <cunninghamjt09@gmail.com>
Date:   Tue Jan 29 12:27:22 2019 -0700

    Track internal increments based on their key. (#11756)
    
    * Track internal increments based on their key.
    
    * Remove instance assignment and rely on key alone.
    
    * Explicitly zero-out tags.
    
    * Explicitly only incr by 1.

diff --git a/src/sentry/utils/metrics.py b/src/sentry/utils/metrics.py
index 94ab99bf66..585ffe3ac2 100644
--- a/src/sentry/utils/metrics.py
+++ b/src/sentry/utils/metrics.py
@@ -89,6 +89,8 @@ def incr(key, amount=1, instance=None, tags=None, skip_internal=True):
         internal.incr(key, instance, tags, amount)
     try:
         backend.incr(key, instance, tags, amount, sample_rate)
+        if not skip_internal:
+            backend.incr('internal_metrics.incr', key, None, 1, sample_rate)
     except Exception:
         logger = logging.getLogger('sentry.errors')
         logger.exception('Unable to record backend metric')
