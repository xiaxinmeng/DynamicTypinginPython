commit 247ef82a69c2b83926043f92e795d5fa904d990f
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Feb 18 15:50:42 2015 -0800

    Remove redirect in favor of internal query

diff --git a/src/sentry/api/endpoints/group_events_latest.py b/src/sentry/api/endpoints/group_events_latest.py
index 622c5a7ee6..1214560dde 100644
--- a/src/sentry/api/endpoints/group_events_latest.py
+++ b/src/sentry/api/endpoints/group_events_latest.py
@@ -1,8 +1,6 @@
 from __future__ import absolute_import
 
-from django.core.urlresolvers import reverse
-from django.http import HttpResponseRedirect
-
+from sentry.api import client
 from sentry.api.bases.group import GroupEndpoint
 
 
@@ -10,6 +8,4 @@ class GroupEventsLatestEndpoint(GroupEndpoint):
     def get(self, request, group):
         event = group.get_latest_event()
 
-        return HttpResponseRedirect(reverse('sentry-api-0-event-details', kwargs={
-            'event_id': event.id,
-        }))
+        return client.get('/events/{}/'.format(event.id), request.user, request.auth)
diff --git a/tests/sentry/api/endpoints/test_group_events_latest.py b/tests/sentry/api/endpoints/test_group_events_latest.py
index 4d7ec020c5..761f38cf96 100644
--- a/tests/sentry/api/endpoints/test_group_events_latest.py
+++ b/tests/sentry/api/endpoints/test_group_events_latest.py
@@ -1,3 +1,5 @@
+from __future__ import absolute_import
+
 from datetime import datetime
 from django.core.urlresolvers import reverse
 
@@ -25,9 +27,5 @@ class GroupEventsLatestTest(APITestCase):
         })
         response = self.client.get(url, format='json')
 
-        assert response.status_code == 302, response.content
-        assert response['Location'] == 'http://testserver{0}'.format(
-            reverse('sentry-api-0-event-details', kwargs={
-                'event_id': event_2.id,
-            })
-        )
+        assert response.status_code == 200
+        assert response.data['id'] == str(event_2.id)
