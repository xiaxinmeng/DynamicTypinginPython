commit 55e3eb12a6857b868fd7022ddd13e56299d8b94f
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Dec 13 11:10:29 2010 -0800

    Update test (revert actually) so that it checks for the string of the value, rather than the proper (but not present) unicode value

diff --git a/sentry/tests/tests.py b/sentry/tests/tests.py
index 9272249dc4..2795f7492a 100644
--- a/sentry/tests/tests.py
+++ b/sentry/tests/tests.py
@@ -265,7 +265,7 @@ class SentryTestCase(TestCase):
 
         error = get_client().create_from_text(value)
         self.assertEquals(Message.objects.count(), cnt+1)
-        self.assertEquals(error.message, force_unicode(value))
+        self.assertEquals(error.message, value)
 
         logging.info(value)
         self.assertEquals(Message.objects.count(), cnt+2)
