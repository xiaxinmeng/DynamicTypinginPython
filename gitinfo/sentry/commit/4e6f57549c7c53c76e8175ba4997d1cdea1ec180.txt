commit 4e6f57549c7c53c76e8175ba4997d1cdea1ec180
Author: Markus Unterwaditzer <markus@unterwaditzer.net>
Date:   Tue Jun 2 14:46:28 2020 +0200

    fix(metaProxy): Do not reset _meta to root if local _meta becomes falsey (#19133)
    
    Also remove first parameter to MetaProxy as it is now entirely unused.
    
    closes: #19128

diff --git a/src/sentry/static/sentry/app/components/events/meta/metaProxy.jsx b/src/sentry/static/sentry/app/components/events/meta/metaProxy.jsx
index be2144995a..d7dffb89fc 100644
--- a/src/sentry/static/sentry/app/components/events/meta/metaProxy.jsx
+++ b/src/sentry/static/sentry/app/components/events/meta/metaProxy.jsx
@@ -12,11 +12,8 @@ function isAnnotated(meta) {
 }
 
 export class MetaProxy {
-  constructor(root, local) {
-    // entire meta object
-    this._meta = root;
-
-    this.local = !local ? root : local;
+  constructor(local) {
+    this.local = local;
   }
 
   get(obj, prop, receiver) {
@@ -51,7 +48,7 @@ export class MetaProxy {
 
     // Make sure we apply proxy to all children (objects and arrays)
     // Do we need to check for annotated inside of objects?
-    return new Proxy(value, new MetaProxy(this._meta, this.local && this.local[prop]));
+    return new Proxy(value, new MetaProxy(this.local && this.local[prop]));
   }
 }
 
