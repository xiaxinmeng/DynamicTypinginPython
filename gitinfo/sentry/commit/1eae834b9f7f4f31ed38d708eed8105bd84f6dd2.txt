commit 1eae834b9f7f4f31ed38d708eed8105bd84f6dd2
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Jan 4 14:45:04 2016 -0800

    Improve activity items in feed

diff --git a/src/sentry/models/groupassignee.py b/src/sentry/models/groupassignee.py
index 5cc46867f0..8af69fb871 100644
--- a/src/sentry/models/groupassignee.py
+++ b/src/sentry/models/groupassignee.py
@@ -49,6 +49,7 @@ class GroupAssigneeManager(BaseManager):
                 user=acting_user,
                 data={
                     'assignee': str(assigned_to.id),
+                    'assigneeEmail': assigned_to.email,
                 }
             )
             activity.send_notification()
diff --git a/src/sentry/static/sentry/app/components/activity/item.jsx b/src/sentry/static/sentry/app/components/activity/item.jsx
index fabc559930..71e3572c1d 100644
--- a/src/sentry/static/sentry/app/components/activity/item.jsx
+++ b/src/sentry/static/sentry/app/components/activity/item.jsx
@@ -107,6 +107,13 @@ const ActivityItem = React.createClass({
             link: <Link to={`/${orgId}/${project.slug}/issues/${issue.id}/`} />
           });
         }
+        else if (data.assigneeEmail) {
+          return tct('[author] assigned [link:an issue] to [assignee]', {
+            author: author,
+            assignee: data.assigneeEmail,
+            link: <Link to={`/${orgId}/${project.slug}/issues/${issue.id}/`} />
+          });
+        }
         return tct('[author] assigned [link:an issue] to an [help:unknown user]', {
           author: author,
           help: <span title={data.assignee} />,
