commit b2ae7ff755cf6255adff6e07e48721996e710e36
Author: cclauss <cclauss@bluewin.ch>
Date:   Mon Sep 24 14:03:51 2018 +0100

    xrange() --> six.moves.xrange() for Python 3

diff --git a/src/sentry/api/serializers/snuba.py b/src/sentry/api/serializers/snuba.py
index 51d586ea15..305afca370 100644
--- a/src/sentry/api/serializers/snuba.py
+++ b/src/sentry/api/serializers/snuba.py
@@ -145,7 +145,7 @@ def zerofill(data, start, end, rollup):
     start = ((int(to_timestamp(start)) / rollup) * rollup) - rollup
     end = ((int(to_timestamp(end)) / rollup) * rollup) + rollup
     i = 0
-    for key in xrange(start, end, rollup):
+    for key in six.moves.xrange(start, end, rollup):
         try:
             if data[i][0] == key:
                 rv.append(data[i])
