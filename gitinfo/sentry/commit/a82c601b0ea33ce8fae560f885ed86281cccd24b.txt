commit a82c601b0ea33ce8fae560f885ed86281cccd24b
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Jan 26 11:35:55 2012 -0800

    Move module versions to below additional data on event page

diff --git a/sentry/templates/sentry/groups/event.html b/sentry/templates/sentry/groups/event.html
index 99d4d6805b..205cb2aaf5 100644
--- a/sentry/templates/sentry/groups/event.html
+++ b/sentry/templates/sentry/groups/event.html
@@ -38,23 +38,6 @@
         <pre id="full-message">{{ event.message }}</pre>
     {% endif %}
 
-
-    {% if version_data %}
-        <div id="versioninfo">
-            <div class="page-header">
-                <h2>{% trans "Module Versions" %}</h2>
-            </div>
-            <table class="table inline">
-                {% for k, v in version_data.iteritems|as_sorted %}
-                    <tr>
-                        <th class="key">{{ k }}</th>
-                        <td class="values"><code>{{ v }}</code></td>
-                    </tr>
-                {% endfor %}
-            </table>
-        </div>
-    {% endif %}
-
     {% for interface in interface_list %}
         {{ interface }}
     {% endfor %}
@@ -99,4 +82,20 @@
             </table>
         </div>
     {% endif %}
+
+    {% if version_data %}
+        <div id="versioninfo">
+            <div class="page-header">
+                <h2>{% trans "Module Versions" %}</h2>
+            </div>
+            <table class="table table-bordered">
+                {% for k, v in version_data.iteritems|as_sorted %}
+                    <tr>
+                        <th class="key">{{ k }}</th>
+                        <td class="values"><code>{{ v }}</code></td>
+                    </tr>
+                {% endfor %}
+            </table>
+        </div>
+    {% endif %}
 {% endblock %}
\ No newline at end of file
