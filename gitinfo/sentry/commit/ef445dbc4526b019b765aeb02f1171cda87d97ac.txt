commit ef445dbc4526b019b765aeb02f1171cda87d97ac
Author: David Cramer <dcramer@gmail.com>
Date:   Wed May 25 11:59:35 2011 -0700

    Mock transactions if we're running on a remote server, as we dont need to be forcing database configuration

diff --git a/sentry/client/models.py b/sentry/client/models.py
index 5302da1c4d..1970e8b74d 100644
--- a/sentry/client/models.py
+++ b/sentry/client/models.py
@@ -5,12 +5,26 @@ import logging
 import warnings
 
 from django.core.signals import got_request_exception
-from django.db import  transaction
 
 from sentry.conf import settings
 
 logger = logging.getLogger('sentry.errors')
 
+if settings.REMOTE_URL:
+    class MockTransaction(object):
+        def commit_on_success(self, func):
+            return func
+        
+        def is_dirty(self):
+            return False
+    
+        def rollback(self):
+            pass
+    
+    transaction = MockTransaction()
+else:
+    from django.db import transaction
+
 _client = (None, None)
 def get_client():
     global _client
