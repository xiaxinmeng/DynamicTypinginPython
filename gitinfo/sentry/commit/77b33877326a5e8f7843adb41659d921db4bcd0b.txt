commit 77b33877326a5e8f7843adb41659d921db4bcd0b
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Fri Jan 29 13:55:48 2016 -0800

    Fix boolean logic for disabled <MemberItem>
    
    Pretty sure fixes GH-2606

diff --git a/src/sentry/static/sentry/app/components/assigneeSelector.jsx b/src/sentry/static/sentry/app/components/assigneeSelector.jsx
index c201782f3b..bc39287298 100644
--- a/src/sentry/static/sentry/app/components/assigneeSelector.jsx
+++ b/src/sentry/static/sentry/app/components/assigneeSelector.jsx
@@ -181,7 +181,7 @@ const AssigneeSelector = React.createClass({
     let memberNodes = members.map((item) => {
       return (
         <MenuItem key={item.id}
-                  disabled={!loading}
+                  disabled={loading}
                   onSelect={this.assignTo.bind(this, item)} >
           <Gravatar email={item.email} className="avatar"
                     size={48} />
