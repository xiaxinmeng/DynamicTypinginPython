commit 271c5cfbdf3e31f605a1797a30eb9bc97b721007
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Tue Dec 1 14:08:23 2015 -0800

    Move api-generator to use `sentry start` instead of `devserver`

diff --git a/api-docs/generator.py b/api-docs/generator.py
index ab0bdaedb5..187faa2c7e 100644
--- a/api-docs/generator.py
+++ b/api-docs/generator.py
@@ -69,10 +69,8 @@ def launch_redis():
 
 def spawn_sentry():
     report('sentry', 'Launching sentry server')
-    cl = Popen(['sentry', '--config=' + SENTRY_CONFIG, 'devserver',
-                '-v', '0', '--noreload', '--nothreading',
-                '--no-watchers', '--traceback',
-                '127.0.0.1:%s' % settings.SENTRY_APIDOCS_WEB_PORT])
+    cl = Popen(['sentry', '--config=' + SENTRY_CONFIG, 'start',
+                '-w', '1', '--bind', '127.0.0.1:%s' % settings.SENTRY_APIDOCS_WEB_PORT])
     return cl
 
 
