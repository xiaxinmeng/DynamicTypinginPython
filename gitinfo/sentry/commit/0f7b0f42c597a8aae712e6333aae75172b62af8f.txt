commit 0f7b0f42c597a8aae712e6333aae75172b62af8f
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Tue Oct 6 17:28:31 2015 -0700

    Log our "safe" errors under a `sentry.safe` logger
    
    This makes them easier to distinguish

diff --git a/src/sentry/utils/logging.py b/src/sentry/utils/logging.py
index 231aac805f..d9783ab46c 100644
--- a/src/sentry/utils/logging.py
+++ b/src/sentry/utils/logging.py
@@ -30,6 +30,6 @@ class WithoutRavenManager(object):
 
     def __exit__(self, exc_type, exc_value, exc_traceback):
         if all([exc_type, exc_value, exc_traceback]):
-            logger = logging.getLogger('sentry')
+            logger = logging.getLogger('sentry.safe')
             logger.exception(unicode(exc_value))
         return True
diff --git a/src/sentry/utils/safe.py b/src/sentry/utils/safe.py
index 356bc62e40..a95654858e 100644
--- a/src/sentry/utils/safe.py
+++ b/src/sentry/utils/safe.py
@@ -35,14 +35,9 @@ def safe_execute(func, *args, **kwargs):
         func_name = getattr(func, '__name__', str(func))
         cls_name = cls.__name__
 
-        logger = logging.getLogger('sentry')
+        logger = logging.getLogger('sentry.safe')
         logger.error(
             'Error processing %r on %r: %s', func_name, cls_name, exc,
-            extra={
-                'func_module': cls.__module__,
-                'func_args': args,
-                'func_kwargs': kwargs,
-            },
             exc_info=True,
         )
     else:
