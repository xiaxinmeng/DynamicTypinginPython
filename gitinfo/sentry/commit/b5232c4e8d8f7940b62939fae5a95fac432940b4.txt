commit b5232c4e8d8f7940b62939fae5a95fac432940b4
Author: Dan Fuller <dfuller@sentry.io>
Date:   Thu Oct 17 14:58:02 2019 -0700

    fix: Fix unstable plugin test
    
    More events without explicit timestamps set

diff --git a/tests/sentry/lang/java/test_plugin.py b/tests/sentry/lang/java/test_plugin.py
index c0dcbe5b39..88253b1656 100644
--- a/tests/sentry/lang/java/test_plugin.py
+++ b/tests/sentry/lang/java/test_plugin.py
@@ -8,6 +8,7 @@ from django.core.files.uploadedfile import SimpleUploadedFile
 
 from sentry import eventstore
 from sentry.testutils import TestCase
+from sentry.testutils.helpers.datetime import before_now, iso_format
 from sentry.utils import json
 
 
@@ -85,6 +86,7 @@ class BasicResolvingIntegrationTest(TestCase):
                     }
                 ]
             },
+            "timestamp": iso_format(before_now(seconds=1)),
         }
 
         # We do a preflight post, because there are many queries polluting the array
@@ -177,6 +179,7 @@ class BasicResolvingIntegrationTest(TestCase):
                     }
                 ]
             },
+            "timestamp": iso_format(before_now(seconds=1)),
         }
 
         resp = self._postWithHeader(event_data)
