commit 7ceb8dc47e9b313b6dde66bfc3874f5ccdbc19c7
Author: Brett Hoerner <brett@bretthoerner.com>
Date:   Wed Oct 10 21:54:44 2018 -0500

    fix(eventstream): Gracefully handle/capture Kafka produce failures (#10080)

diff --git a/src/sentry/eventstream/kafka.py b/src/sentry/eventstream/kafka.py
index 8d4fbd9d23..7783390efc 100644
--- a/src/sentry/eventstream/kafka.py
+++ b/src/sentry/eventstream/kafka.py
@@ -104,8 +104,8 @@ class KafkaEventStream(EventStream):
                 on_delivery=self.delivery_callback,
             )
         except Exception as error:
-            logger.warning('Could not publish message: %s', error, exc_info=True)
-            raise
+            logger.error('Could not publish message: %s', error, exc_info=True)
+            return
 
         if not asynchronous:
             # flush() is a convenience method that calls poll() until len() is zero
