commit ae058dd3b1bc2236aabec35942adfb30dc8b4780
Author: Mark Story <mark@sentry.io>
Date:   Wed Oct 9 14:25:11 2019 -0400

    fix(discover2) Fix grammar mistake (#15012)

diff --git a/src/sentry/api/event_search.py b/src/sentry/api/event_search.py
index 448929d469..07a19a68b1 100644
--- a/src/sentry/api/event_search.py
+++ b/src/sentry/api/event_search.py
@@ -736,7 +736,7 @@ def resolve_orderby(orderby, fields, aggregations):
     if len(validated) == len(orderby):
         return validated
 
-    raise InvalidSearchQuery("Cannot order by an field that is not selected.")
+    raise InvalidSearchQuery("Cannot order by a field that is not selected.")
 
 
 def get_aggregate_alias(match):
