commit b7d36fbd670fd6d4a58fc9600d1102d67272c197
Author: Ben Vinegar <ben@benv.ca>
Date:   Fri Sep 30 17:44:37 2016 -0700

    Close panel if navigated somewhere

diff --git a/src/sentry/static/sentry/app/components/sidebar/index.jsx b/src/sentry/static/sentry/app/components/sidebar/index.jsx
index 21c1feb4d4..4e766f03bd 100644
--- a/src/sentry/static/sentry/app/components/sidebar/index.jsx
+++ b/src/sentry/static/sentry/app/components/sidebar/index.jsx
@@ -89,6 +89,16 @@ const Sidebar = React.createClass({
     loadIncidents();
   },
 
+  componentWillReceiveProps(nextProps, nextContext) {
+    let {location} = this.context;
+    let nextLocation = nextContext.location;
+
+    // Close active panel if we navigated anywhere
+    if (location.pathname != nextLocation.pathname) {
+      this.hidePanel();
+    }
+  },
+
   componentWillUnmount() {
     $(window).off('hashchange', this.hashChangeHandler);
     $(document).off('click', this.documentClickHandler);
@@ -133,7 +143,6 @@ const Sidebar = React.createClass({
       this.showPanel(panel);
   },
 
-
   renderBody() {
     let org = this.getOrganization();
 
