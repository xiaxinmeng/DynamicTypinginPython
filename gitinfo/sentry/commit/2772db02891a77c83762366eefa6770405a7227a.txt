commit 2772db02891a77c83762366eefa6770405a7227a
Author: ckj <chriskjennings@gmail.com>
Date:   Fri Dec 12 01:26:51 2014 -0500

    Flesh out 'Create a team' flow

diff --git a/src/sentry/static/sentry/less/sentry.less b/src/sentry/static/sentry/less/sentry.less
index 5582122b89..a61924e8f1 100644
--- a/src/sentry/static/sentry/less/sentry.less
+++ b/src/sentry/static/sentry/less/sentry.less
@@ -1730,6 +1730,7 @@ small {
 .help-block {
   font-size: 12px;
   font-weight: normal;
+  color: #808690;
 }
 
 
@@ -2793,3 +2794,52 @@ table.vars {
 pre {
   word-break: normal;
 }
+
+
+.create-flow {
+  width: 575px;
+  margin: 0 auto;
+  padding: 45px 0 50px;
+
+  .help-block {
+    font-size: 14px;
+    color: @gray;
+    line-height: 1.4;
+    margin-bottom: 20px;
+  }
+
+  .control-group {
+    margin-bottom: 20px;
+  }
+
+  input[type="text"] {
+    width: 400px;
+  }
+
+  .steps {
+    .clearfix();
+    list-style-type: none;
+    padding: 0;
+    margin: 0 0 30px;
+
+    li {
+      width: 33.33%;
+      float: left;
+      text-align: center;
+      font-size: 22px;
+      color: #D4D6D9;
+
+      &.active {
+        color: @gray;
+      }
+
+      &:first-child {
+        text-align: left;
+      }
+
+      &:last-child {
+        text-align: right;
+      }
+    }
+  }
+}
diff --git a/src/sentry/static/sentry/styles/global.min.css b/src/sentry/static/sentry/styles/global.min.css
index 4d3e1ad469..59c74f6cd8 100644
--- a/src/sentry/static/sentry/styles/global.min.css
+++ b/src/sentry/static/sentry/styles/global.min.css
@@ -8198,6 +8198,7 @@ small {
 .help-block {
   font-size: 12px;
   font-weight: normal;
+  color: #808690;
 }
 .controls > .inputs-list label:first-child,
 .controls > .inputs-list label:first-child {
@@ -9235,3 +9236,51 @@ table.vars table.vars th {
 pre {
   word-break: normal;
 }
+.create-flow {
+  width: 575px;
+  margin: 0 auto;
+  padding: 45px 0 50px;
+}
+.create-flow .help-block {
+  font-size: 14px;
+  color: #465262;
+  line-height: 1.4;
+  margin-bottom: 20px;
+}
+.create-flow .control-group {
+  margin-bottom: 20px;
+}
+.create-flow input[type="text"] {
+  width: 400px;
+}
+.create-flow .steps {
+  *zoom: 1;
+  list-style-type: none;
+  padding: 0;
+  margin: 0 0 30px;
+}
+.create-flow .steps:before,
+.create-flow .steps:after {
+  display: table;
+  content: "";
+  line-height: 0;
+}
+.create-flow .steps:after {
+  clear: both;
+}
+.create-flow .steps li {
+  width: 33.33%;
+  float: left;
+  text-align: center;
+  font-size: 22px;
+  color: #D4D6D9;
+}
+.create-flow .steps li.active {
+  color: #465262;
+}
+.create-flow .steps li:first-child {
+  text-align: left;
+}
+.create-flow .steps li:last-child {
+  text-align: right;
+}
diff --git a/src/sentry/templates/sentry/create-team.html b/src/sentry/templates/sentry/create-team.html
index 813ad71354..a3d267f952 100644
--- a/src/sentry/templates/sentry/create-team.html
+++ b/src/sentry/templates/sentry/create-team.html
@@ -7,45 +7,49 @@
 
 {% block main %}
   <section class="body">
-    <div class="page-header">
-      <div class="pull-right">
-        <a href="{% url 'sentry-organization-home' organization.slug %}">Back to Organization Home</a>
-      </div>
-      <h2>{% trans "Create a New Team" %}</h2>
-    </div>
-
-    <br>
-
-    <form class="form-stacked" action="" method="post">
-      {% csrf_token %}
-      {{ form|as_crispy_errors }}
-
-      <fieldset>
-        <h3>Team</h3>
-
-        <div class="help-block">
-          A team generally represents a group of people within your organization. It might be something like <strong>"Platform"</strong> or <strong>"API"</strong>, or it could just be a specific product that may (or may not) have sub-projects. You'll be able to select which teams a member in your organization has access to so they also act as ACLs.
-        </div>
-
-        {{ team_form.name|as_crispy_field }}
-      </fieldset>
-
-      <fieldset>
-        <h3>Project</h3>
-
-        <div class="help-block">
-          Projects generally pair up well with repositories. In addition to thinking about each repository being a project, also consider your environments. You might have something like <strong>"Backend"</strong> or <strong>"Frontend (Staging)"</strong>.
+    <div class="create-flow">
+      <ul class="steps">
+        <li class="active">1. {% trans "Create a team" %}</li>
+        <li>2. {% trans "Invite members" %}</li>
+        <li>3. {% trans "Add a project" %}</li>
+      </ul>
+
+      <form class="form-stacked" action="" method="post">
+        {% csrf_token %}
+        {{ form|as_crispy_errors }}
+
+        <fieldset>
+          <div class="help-block">
+            {% comment %}
+            A team generally represents a group of people within your organization. It might be something like <strong>"Platform"</strong> or <strong>"API"</strong>, or it could just be a specific product that may (or may not) have sub-projects. You'll be able to select which teams a member in your organization has access to so they also act as ACLs.
+            {% endcomment %}
+            Teams group members' access to a specific focus, e.g. a major product or application that may have sub-projects.
+            <strong>Let’s add our first one:</strong>
+          </div>
+
+          {{ team_form.name|as_crispy_field }}
+        </fieldset>
+
+        {% comment %}
+        <fieldset>
+          <h3>Project</h3>
+
+          <div class="help-block">
+            Projects generally pair up well with repositories. In addition to thinking about each repository being a project, also consider your environments. You might have something like <strong>"Backend"</strong> or <strong>"Frontend (Staging)"</strong>.
+          </div>
+
+          {{ project_form.name|as_crispy_field }}
+
+          {{ project_form.platform|as_crispy_field }}
+
+        </fieldset>
+        {% endcomment%}
+
+        <div class="actions">
+          <button type="submit" class="btn btn-primary">{% trans "Create Team" %}</button>
+          <a href="#" class="btn btn-default">{% trans "Skip this step" %}</a>
         </div>
-
-        {{ project_form.name|as_crispy_field }}
-
-        {{ project_form.platform|as_crispy_field }}
-
-      </fieldset>
-
-      <div class="actions">
-        <button type="submit" class="btn btn-primary">{% trans "Create Team" %}</button>
-      </div>
-    </form>
+      </form>
+    </div>
   </section>
 {% endblock %}
diff --git a/src/sentry/web/frontend/create_team.py b/src/sentry/web/frontend/create_team.py
index 2b30733217..12aa9c6d4f 100644
--- a/src/sentry/web/frontend/create_team.py
+++ b/src/sentry/web/frontend/create_team.py
@@ -17,7 +17,7 @@ BLANK_CHOICE = [("", "")]
 
 class NewTeamForm(forms.ModelForm):
     name = forms.CharField(label=_('Name'), max_length=200,
-        widget=forms.TextInput(attrs={'placeholder': _('e.g. Website')}))
+        widget=forms.TextInput(attrs={'placeholder': _('E.g. Platform, API, Website, ...')}))
 
     class Meta:
         fields = ('name',)
