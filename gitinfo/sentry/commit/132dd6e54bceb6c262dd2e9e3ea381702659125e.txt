commit 132dd6e54bceb6c262dd2e9e3ea381702659125e
Author: Mark Story <mark@sentry.io>
Date:   Mon May 4 10:24:55 2020 -0400

    fix(discover) Only fetch saved query data when it changes (#18569)
    
    Don't fetch saved queries each time the view changes. Instead only do it
    when the saved query id is updated.

diff --git a/src/sentry/static/sentry/app/views/eventsV2/resultsHeader.tsx b/src/sentry/static/sentry/app/views/eventsV2/resultsHeader.tsx
index abcea3fda4..12eaedc444 100644
--- a/src/sentry/static/sentry/app/views/eventsV2/resultsHeader.tsx
+++ b/src/sentry/static/sentry/app/views/eventsV2/resultsHeader.tsx
@@ -41,7 +41,11 @@ class ResultsHeader extends React.Component<Props, State> {
   }
 
   componentDidUpdate(prevProps: Props) {
-    if (prevProps.eventView !== this.props.eventView) {
+    if (
+      prevProps.eventView &&
+      this.props.eventView &&
+      prevProps.eventView.id !== this.props.eventView.id
+    ) {
       this.fetchData();
     }
   }
