commit 6ed5ac9d322dfd740fc2327fc5a8af2230edc1ef
Author: unsatisfiedpopcorn <jefferykwoh@gmail.com>
Date:   Wed Mar 25 02:07:21 2020 +0800

    chore(workflow):Add proj flag for alerts targeting

diff --git a/src/sentry/models/project.py b/src/sentry/models/project.py
index a8eb3fc505..3b64c02c74 100644
--- a/src/sentry/models/project.py
+++ b/src/sentry/models/project.py
@@ -103,7 +103,12 @@ class Project(Model, PendingDeletionMixin):
     # will have their first_event field set to date_added
     first_event = models.DateTimeField(null=True)
     flags = BitField(
-        flags=(("has_releases", "This Project has sent release data"),), default=0, null=True
+        flags=(
+            ("has_releases", "This Project has sent release data"),
+            ("has_issue_alerts_targeting", "This Project has issue alerts targeting"),
+        ),
+        default=0,
+        null=True,
     )
 
     objects = ProjectManager(cache_fields=["pk"])
