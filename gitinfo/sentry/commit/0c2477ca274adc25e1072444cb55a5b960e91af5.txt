commit 0c2477ca274adc25e1072444cb55a5b960e91af5
Author: Billy Vong <billyvg@users.noreply.github.com>
Date:   Thu Mar 19 11:16:51 2020 -0700

    feat(workflow): Add extra info about Time Window (#17776)
    
    This adds extra information about the evaluation interval for the Time Window field when building a Metric Alert.

diff --git a/src/sentry/static/sentry/app/views/settings/incidentRules/ruleConditionsForm.tsx b/src/sentry/static/sentry/app/views/settings/incidentRules/ruleConditionsForm.tsx
index e359477144..e8f8789b49 100644
--- a/src/sentry/static/sentry/app/views/settings/incidentRules/ruleConditionsForm.tsx
+++ b/src/sentry/static/sentry/app/views/settings/incidentRules/ruleConditionsForm.tsx
@@ -138,7 +138,16 @@ class RuleConditionsForm extends React.PureComponent<Props, State> {
           <SelectField
             name="timeWindow"
             label={t('Time Window')}
-            help={t('The time window to use when evaluating the Metric')}
+            help={
+              <React.Fragment>
+                <div>{t('The time window to use when evaluating the Metric')}</div>
+                <div>
+                  {t(
+                    'Note: Triggers are evaluated every minute regardless of this value.'
+                  )}
+                </div>
+              </React.Fragment>
+            }
             choices={Object.entries(TIME_WINDOW_MAP)}
             required
             isDisabled={disabled}
