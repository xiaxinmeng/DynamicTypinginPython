commit 323ddd5facea4e64ad6ccfbf0effbd3913f41962
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Wed Oct 19 15:15:52 2016 -0700

    Dont cache a non-200 response for React mappings (#4389)

diff --git a/src/sentry/lang/javascript/errormapping.py b/src/sentry/lang/javascript/errormapping.py
index 49cd0a3af0..a992b13d8f 100644
--- a/src/sentry/lang/javascript/errormapping.py
+++ b/src/sentry/lang/javascript/errormapping.py
@@ -58,6 +58,8 @@ class Processor(object):
                 allow_redirects=True,
                 timeout=settings.SENTRY_SOURCE_FETCH_TIMEOUT,
             )
+            # Make sure we only get a 2xx to prevent caching bad data
+            response.raise_for_status()
             data = response.json()
             cache.set(key, json.dumps([time.time(), data]), HARD_TIMEOUT)
         except Exception:
