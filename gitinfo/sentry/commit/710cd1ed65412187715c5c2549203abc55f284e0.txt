commit 710cd1ed65412187715c5c2549203abc55f284e0
Author: ckj <chris.jennings@github.com>
Date:   Sat Jun 14 03:23:18 2014 -0400

    Style event browser

diff --git a/src/sentry/static/sentry/less/sentry.less b/src/sentry/static/sentry/less/sentry.less
index 3069ca3be1..669c6194b6 100644
--- a/src/sentry/static/sentry/less/sentry.less
+++ b/src/sentry/static/sentry/less/sentry.less
@@ -1402,6 +1402,15 @@ ol.context-line {
   }
 }
 
+.event-toolbar {
+  text-align: center;
+  margin-bottom: 30px;
+
+  h4 {
+    margin: 5px 0 0;
+  }
+}
+
 // Sparkline grid
 
 .innerColumn(@columnSpan: 1) {
diff --git a/src/sentry/static/sentry/styles/global.min.css b/src/sentry/static/sentry/styles/global.min.css
index f5693c792d..c120288e65 100644
--- a/src/sentry/static/sentry/styles/global.min.css
+++ b/src/sentry/static/sentry/styles/global.min.css
@@ -7702,6 +7702,13 @@ ol.context-line > li > span {
 .mini-tag-list > li span {
   padding-right: 8px;
 }
+.event-toolbar {
+  text-align: center;
+  margin-bottom: 30px;
+}
+.event-toolbar h4 {
+  margin: 5px 0 0;
+}
 #chart {
   margin-bottom: 10px;
   overflow: hidden;
diff --git a/src/sentry/static/sentry/styles/sentry.css b/src/sentry/static/sentry/styles/sentry.css
index f5693c792d..c120288e65 100644
--- a/src/sentry/static/sentry/styles/sentry.css
+++ b/src/sentry/static/sentry/styles/sentry.css
@@ -7702,6 +7702,13 @@ ol.context-line > li > span {
 .mini-tag-list > li span {
   padding-right: 8px;
 }
+.event-toolbar {
+  text-align: center;
+  margin-bottom: 30px;
+}
+.event-toolbar h4 {
+  margin: 5px 0 0;
+}
 #chart {
   margin-bottom: 10px;
   overflow: hidden;
diff --git a/src/sentry/templates/sentry/groups/details.html b/src/sentry/templates/sentry/groups/details.html
index 1f3d9995ba..946309640d 100644
--- a/src/sentry/templates/sentry/groups/details.html
+++ b/src/sentry/templates/sentry/groups/details.html
@@ -63,8 +63,8 @@
 
             <div class="btn-toolbar event-toolbar">
                 <!-- We switch the ordering of events here as it makes more sense visually -->
-                <a class="btn pull-left prev {% if not next_event %} disabled{% endif %}"{% if next_event %} href="{% url 'sentry-group-event' group.team.slug group.project.slug group.id next_event.id %}"{% endif %}><span></span> {% trans "Newer Event" %}</a>
-                <a class="btn pull-right next {% if not prev_event %} disabled{% endif %}"{% if prev_event %} href="{% url 'sentry-group-event' group.team.slug group.project.slug group.id prev_event.id %}"{% endif %}>{% trans "Older Event" %} <span></span></a>
+                <a class="btn btn-default btn-lg pull-right prev {% if not next_event %} disabled{% endif %}"{% if next_event %} href="{% url 'sentry-group-event' group.team.slug group.project.slug group.id next_event.id %}"{% endif %}><span></span> {% trans "Newer Event" %}</a>
+                <a class="btn btn-default btn-lg pull-left next {% if not prev_event %} disabled{% endif %}"{% if prev_event %} href="{% url 'sentry-group-event' group.team.slug group.project.slug group.id prev_event.id %}"{% endif %}>{% trans "Older Event" %} <span></span></a>
                 <h4>
                 Event at <span title="{{ event.datetime }}">{% localized_datetime event.datetime %}</span> [{{ event.size|filesizeformat }}]<br><small>ID: {{ event.event_id }}</small></h4>
             </div>
