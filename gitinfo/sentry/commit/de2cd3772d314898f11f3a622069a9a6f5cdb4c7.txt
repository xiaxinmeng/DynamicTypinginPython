commit de2cd3772d314898f11f3a622069a9a6f5cdb4c7
Author: Lyn Nagara <lyn.nagara@gmail.com>
Date:   Tue Oct 16 11:46:29 2018 -0700

    fix(discover): Stop showing the in progress state on failed request (#10127)
    
    Required to allow the user to submit a subsequent request

diff --git a/src/sentry/static/sentry/app/views/organizationDiscover/discover.jsx b/src/sentry/static/sentry/app/views/organizationDiscover/discover.jsx
index 87459e6697..210ba00cd8 100644
--- a/src/sentry/static/sentry/app/views/organizationDiscover/discover.jsx
+++ b/src/sentry/static/sentry/app/views/organizationDiscover/discover.jsx
@@ -165,6 +165,7 @@ export default class OrganizationDiscover extends React.Component {
       .catch(err => {
         const message = (err && err.message) || t('An error occurred');
         addErrorMessage(message);
+        this.setState({isFetchingQuery: false});
       });
   };
 
