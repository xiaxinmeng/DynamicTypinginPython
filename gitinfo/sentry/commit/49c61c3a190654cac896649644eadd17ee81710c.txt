commit 49c61c3a190654cac896649644eadd17ee81710c
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Feb 5 11:03:38 2015 -0800

    Render tag top values

diff --git a/src/sentry/static/sentry/app/views/aggregateTags.jsx b/src/sentry/static/sentry/app/views/aggregateTags.jsx
index 38bf6f75e7..ad4a25662f 100644
--- a/src/sentry/static/sentry/app/views/aggregateTags.jsx
+++ b/src/sentry/static/sentry/app/views/aggregateTags.jsx
@@ -4,6 +4,7 @@ var React = require("react");
 var Router = require("react-router");
 
 var api = require("../api");
+var Count = require("../components/count");
 var PropTypes = require("../proptypes");
 
 var AggregateTags = React.createClass({
@@ -47,19 +48,38 @@ var AggregateTags = React.createClass({
     var children = [];
 
     if (this.state.tagList) {
-      children = this.state.tagList.map((tag) => {
+      children = this.state.tagList.map((tag, tagIdx) => {
+        var valueChildren = tag.topValues.map((tagValue, tagValueIdx) => {
+          return (
+            <li key={tagValueIdx}>
+              <a href="">
+                {tagValue.value}
+                <span><Count value={tagValue.count} /></span>
+              </a>
+            </li>
+          );
+        });
+
         return (
-          <li>{tag.name}</li>
+          <div className="span6" key={tagIdx}>
+            <div className="page-header">
+              <span className="pull-right">
+                <a href="">More Details</a>
+              </span>
+              <h4>{tag.name} <small><Count value={tag.totalValues} /></small></h4>
+            </div>
+
+            <ul>
+              {valueChildren}
+            </ul>
+          </div>
         );
       });
     }
 
     return (
       <div>
-        Tags
-        <ul>
-          {children}
-        </ul>
+        {children}
       </div>
     );
   }
