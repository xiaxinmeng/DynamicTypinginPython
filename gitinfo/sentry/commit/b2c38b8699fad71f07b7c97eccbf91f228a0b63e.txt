commit b2c38b8699fad71f07b7c97eccbf91f228a0b63e
Author: Billy Vong <billyvg@users.noreply.github.com>
Date:   Mon Mar 12 19:42:51 2018 -0700

    fix(ui): Fix join/leave team (#7590)
    
    * fix barcharts on org dashboard

diff --git a/src/sentry/static/sentry/app/actionCreators/teams.jsx b/src/sentry/static/sentry/app/actionCreators/teams.jsx
index cd8c8c9c9f..26d3752237 100644
--- a/src/sentry/static/sentry/app/actionCreators/teams.jsx
+++ b/src/sentry/static/sentry/app/actionCreators/teams.jsx
@@ -64,7 +64,7 @@ export function joinTeam(api, params, options) {
     method: 'POST',
     data: params.data,
     success: data => {
-      TeamActions.updateSuccess(id, params.teamId, data);
+      TeamActions.updateSuccess(params.teamId, data);
       doCallback(options, 'success', data);
     },
     error: error => {
@@ -84,7 +84,7 @@ export function leaveTeam(api, params, options) {
   return api.request(endpoint, {
     method: 'DELETE',
     success: data => {
-      TeamActions.updateSuccess(id, params.teamId, data);
+      TeamActions.updateSuccess(params.teamId, data);
       doCallback(options, 'success', data);
     },
     error: error => {
diff --git a/src/sentry/static/sentry/less/stream.less b/src/sentry/static/sentry/less/stream.less
index 6b075342ca..a8a45efff9 100644
--- a/src/sentry/static/sentry/less/stream.less
+++ b/src/sentry/static/sentry/less/stream.less
@@ -923,6 +923,10 @@
       position: absolute;
       right: 40px;
       top: 20px;
+
+      .barchart {
+        height: 24px;
+      }
     }
 
     .more-menu-container {
