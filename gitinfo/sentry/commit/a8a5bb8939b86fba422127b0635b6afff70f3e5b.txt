commit a8a5bb8939b86fba422127b0635b6afff70f3e5b
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Wed Oct 26 14:35:51 2016 -0700

    cli: make `sentry plugins list` show more useful information
    
    With `sentry-plugins`, this output was extremely vague and not useful.
    This fixes any case where one python package would provide multiple
    entry points.

diff --git a/src/sentry/runner/commands/plugins.py b/src/sentry/runner/commands/plugins.py
index 1ba80ac197..d5b6fb6a70 100644
--- a/src/sentry/runner/commands/plugins.py
+++ b/src/sentry/runner/commands/plugins.py
@@ -8,7 +8,6 @@ sentry.runner.commands.plugins
 from __future__ import absolute_import, print_function
 
 import click
-import six
 
 
 @click.group()
@@ -21,4 +20,9 @@ def list():
     "List all installed plugins"
     from pkg_resources import iter_entry_points
     for ep in iter_entry_points('sentry.plugins'):
-        click.echo(six.text_type(ep.dist))
+        click.echo(u'%s: %s %s (%s)' % (
+            ep.name,
+            ep.dist.project_name,
+            ep.dist.version,
+            ep.dist.location,
+        ))
