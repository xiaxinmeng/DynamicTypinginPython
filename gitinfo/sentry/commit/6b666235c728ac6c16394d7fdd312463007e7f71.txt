commit 6b666235c728ac6c16394d7fdd312463007e7f71
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Dec 18 12:21:28 2014 -0800

    Add event data to API

diff --git a/src/sentry/api/endpoints/group_events.py b/src/sentry/api/endpoints/group_events.py
index da23da68bf..df11ad1ed6 100644
--- a/src/sentry/api/endpoints/group_events.py
+++ b/src/sentry/api/endpoints/group_events.py
@@ -28,10 +28,14 @@ class GroupEventsEndpoint(Endpoint):
             group=group
         )
 
+        def on_results(event_list):
+            Event.objects.bind_nodes(event_list, 'data')
+            return serialize(event_list, request.user)
+
         return self.paginate(
             request=request,
             queryset=events,
             # TODO(dcramer): we want to sort by datetime
             order_by='-id',
-            on_results=lambda x: serialize(x, request.user),
+            on_results=on_results,
         )
diff --git a/src/sentry/api/serializers/models/event.py b/src/sentry/api/serializers/models/event.py
index 34f633673a..4a26a5cdaa 100644
--- a/src/sentry/api/serializers/models/event.py
+++ b/src/sentry/api/serializers/models/event.py
@@ -15,6 +15,7 @@ class EventSerializer(Serializer):
                 'name': obj.project.name,
                 'slug': obj.project.slug,
             },
+            'data': dict(obj.data),
             'message': obj.message,
             'checksum': obj.checksum,
             'platform': obj.platform,
