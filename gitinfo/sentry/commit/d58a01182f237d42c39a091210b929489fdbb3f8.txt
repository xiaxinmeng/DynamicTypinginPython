commit d58a01182f237d42c39a091210b929489fdbb3f8
Author: David Cramer <dcramer@gmail.com>
Date:   Sun Jul 20 14:05:49 2014 +0200

    Improve add member page

diff --git a/src/sentry/templates/sentry/teams/members/new.html b/src/sentry/templates/sentry/teams/members/new.html
index 1dd0a19a2c..cf0bdcf6b6 100644
--- a/src/sentry/templates/sentry/teams/members/new.html
+++ b/src/sentry/templates/sentry/teams/members/new.html
@@ -4,12 +4,6 @@
 
 {% block title %}{% trans "Add Team Member" %} | {{ block.super }}{% endblock %}
 
-{% block sidebar %}
-    {{ block.super }}
-    <h4>{% trans "Access Levels" %}</h4>
-    <p>{% blocktrans %}The primary difference between access levels is team management. Any member with admin access is treated almost identical to the team owner.{% endblocktrans %}</p>
-{% endblock %}
-
 {% block inner %}
     <div class="box">
         <div class="box-header">
@@ -19,6 +13,12 @@
             <p>{% trans "Invite a member to join this team via their email address. If they do not already have an account, they will first be asked to create one." %}</p>
 
             {% include "sentry/partial/_form.html" %}
+
+            <br><br>
+
+            <h4>{% trans "Access Levels" %}</h4>
+
+            <p>{% blocktrans %}The primary difference between access levels is team management. Any member with admin access is treated almost identical to the team owner. Members with user-level access are able to take action on events, but will be restricted from modifying any team or project-level settings.{% endblocktrans %}</p>
         </div>
     </div>
 {% endblock %}
diff --git a/src/sentry/web/forms/teams.py b/src/sentry/web/forms/teams.py
index 055dfd2ece..f7df381750 100644
--- a/src/sentry/web/forms/teams.py
+++ b/src/sentry/web/forms/teams.py
@@ -60,8 +60,10 @@ EditTeamMemberForm = BaseTeamMemberForm
 
 
 class InviteTeamMemberForm(BaseTeamMemberForm):
+    type = forms.ChoiceField(label=_('Access Type'), choices=MEMBER_TYPES)
+
     class Meta:
-        fields = ('type', 'email')
+        fields = ('email', 'type')
         model = PendingTeamMember
 
     def clean_email(self):
