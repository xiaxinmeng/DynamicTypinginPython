commit 07f613f92404331d1ef68db0cc7332348db31d9f
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Jan 30 16:29:23 2013 -0800

    Extra dev and test requirements

diff --git a/Makefile b/Makefile
index 5634e9820d..61d0c0ad06 100644
--- a/Makefile
+++ b/Makefile
@@ -8,7 +8,7 @@ LESS = node_modules/less/bin/lessc
 
 develop: update-submodules
 	npm install
-	pip install "flake8>=1.6" --use-mirrors
+	pip install "file://`pwd`#egg=sentry[dev]"
 	pip install -e . --use-mirrors
 
 build: static locale
@@ -29,11 +29,14 @@ static:
 	${LESS} --strict-imports ${STATIC_DIR}/less/sentry.less ${STATIC_DIR}/styles/sentry.css
 	@echo "Static assets successfully built! - `date`";
 
+install-test-requirements:
+	pip install "file://`pwd`#egg=sentry[tests]"
+
 update-submodules:
 	git submodule init
 	git submodule update
 
-test: lint test-js test-python
+test: install-test-requirements lint test-js test-python
 
 test-js:
 	@echo "Running JavaScript tests"
diff --git a/setup.py b/setup.py
index c7bdcc064e..7715a54559 100755
--- a/setup.py
+++ b/setup.py
@@ -34,6 +34,10 @@ for m in ('multiprocessing', 'billiard'):
     except ImportError:
         pass
 
+dev_requires = [
+    'flake8>=1.6',
+]
+
 tests_require = [
     'exam>=0.5.1',
     'eventlet',
@@ -84,7 +88,10 @@ setup(
     packages=find_packages('src'),
     zip_safe=False,
     install_requires=install_requires,
-    tests_require=tests_require,
+    extras_require={
+        'tests': tests_require,
+        'dev': dev_requires,
+    },
     test_suite='runtests.runtests',
     license='BSD',
     include_package_data=True,
