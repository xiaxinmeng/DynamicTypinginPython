commit cd59cba5f3153bfff76afb24793fa5f7f866ce9c
Author: josh <josh@jrl.ninja>
Date:   Thu Oct 3 16:43:17 2019 -0700

    py3(drf): bump djangorestframework to 3.2.x (#14893)

diff --git a/requirements-base.txt b/requirements-base.txt
index 42a19c8f64..3ff7645305 100644
--- a/requirements-base.txt
+++ b/requirements-base.txt
@@ -13,7 +13,7 @@ django-jsonfield>=0.9.13,<0.9.14
 django-picklefield>=0.3.0,<1.1.0
 django-sudo>=2.1.0,<3.0.0
 Django>=1.8,<1.9
-djangorestframework==3.1.3
+djangorestframework==3.2.5
 email-reply-parser>=0.2.0,<0.3.0
 enum34>=1.1.6,<1.2.0
 exam>=0.5.1
diff --git a/src/sentry/api/endpoints/auth_login.py b/src/sentry/api/endpoints/auth_login.py
index 6f47eb91c3..7ec862b2c8 100644
--- a/src/sentry/api/endpoints/auth_login.py
+++ b/src/sentry/api/endpoints/auth_login.py
@@ -21,12 +21,12 @@ class AuthLoginEndpoint(Endpoint, OrganizationMixin):
         Process a login request via username/password. SSO login is handled
         elsewhere.
         """
-        login_form = AuthenticationForm(request, request.DATA)
+        login_form = AuthenticationForm(request, request.data)
 
         # Rate limit logins
         is_limited = ratelimiter.is_limited(
             u"auth:login:username:{}".format(
-                md5_text(request.DATA.get("username").lower()).hexdigest()
+                md5_text(request.data.get("username").lower()).hexdigest()
             ),
             limit=10,
             window=60,  # 10 per minute should be enough for anyone
diff --git a/src/sentry/api/endpoints/user_details.py b/src/sentry/api/endpoints/user_details.py
index da3f273412..0107bf0ecd 100644
--- a/src/sentry/api/endpoints/user_details.py
+++ b/src/sentry/api/endpoints/user_details.py
@@ -99,7 +99,6 @@ class SuperuserUserSerializer(BaseUserSerializer):
         # no idea wtf is up with django rest framework, but we need is_active
         # and isActive
         fields = ("name", "username", "isActive", "isStaff", "isSuperuser")
-        # write_only_fields = ('password',)
 
 
 class DeleteUserSerializer(serializers.Serializer):
