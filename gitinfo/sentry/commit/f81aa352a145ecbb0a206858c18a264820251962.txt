commit f81aa352a145ecbb0a206858c18a264820251962
Author: David Cramer <dcramer@gmail.com>
Date:   Sat Dec 29 13:52:25 2012 -0800

    Improve client documentation

diff --git a/src/sentry/static/sentry/images/github-icons.png b/src/sentry/static/sentry/images/github-icons.png
new file mode 100644
index 0000000000..031e304b01
Binary files /dev/null and b/src/sentry/static/sentry/images/github-icons.png differ
diff --git a/src/sentry/static/sentry/less/github.less b/src/sentry/static/sentry/less/github.less
new file mode 100644
index 0000000000..951b4416c5
--- /dev/null
+++ b/src/sentry/static/sentry/less/github.less
@@ -0,0 +1,86 @@
+.github-btn {
+    height: 20px;
+    overflow: hidden;
+    font: bold 11px/14px "Helvetica Neue", Helvetica, Arial, sans-serif;
+    text-rendering: optimizeLegibility;
+}
+.gh-btn,
+.gh-count,
+.gh-ico {
+    float: left;
+}
+.gh-btn,
+.gh-count {
+    padding: 2px 5px 2px 4px;
+    color: #666;
+    text-decoration: none;
+    text-shadow: 0 1px 0 #fff;
+    white-space: nowrap;
+    cursor: pointer;
+    .border-radius(3px);
+}
+.gh-btn {
+    background-color: #e6e6e6;
+    background-image: -webkit-gradient(linear, 0 0, 0 100%, from(#ffffff), color-stop(25%, #ffffff), to(#e6e6e6));
+    background-image: -webkit-linear-gradient(#ffffff, #ffffff 25%, #e6e6e6);
+    background-image: -moz-linear-gradient(top, #ffffff, #ffffff 25%, #e6e6e6);
+    background-image: -ms-linear-gradient(#ffffff, #ffffff 25%, #e6e6e6);
+    background-image: -o-linear-gradient(#ffffff, #ffffff 25%, #e6e6e6);
+    background-image: linear-gradient(#ffffff, #ffffff 25%, #e6e6e6);
+    background-repeat: no-repeat;
+    border: 1px solid #ccc;
+    border-bottom-color: #bbb;
+    .box-shadow(inset 0 1px 0 rgba(255,255,255,.2));
+
+    &:hover, &:focus {
+        color: #333;
+        text-decoration: none;
+        background-position: 0 -10px;
+        .gh-icon {
+            .opacity(75);
+        }
+    }
+    &:active {
+        .box-shadow(inset 0 2px 4px rgba(0,0,0,.15));
+    }
+}
+.gh-ico {
+    width: 14px;
+    height: 14px;
+    margin-right: 4px;
+    vertical-align: middle;
+    background: url(../images/github-icons.png) no-repeat 0 0;
+    .opacity(65);
+}
+.gh-count {
+    display: none; /* hidden to start */
+    margin-left: 2px;
+    background-color: #fff;
+    border: 1px solid #ddd;
+
+    &:hover, &:focus {
+        color: #333;
+        text-decoration: none;
+        background-color: #f5f5f5;
+        border-color: #d5d5d5;
+    }
+}
+
+.github-btn-large {
+    height: 30px;
+
+    .gh-btn, .gh-count {
+        padding: 3px 10px 3px 8px;
+        font-size: 16px;
+        line-height: 22px;
+        .border-radius(5px);
+    }
+    .gh-ico {
+        width: 22px;
+        height: 22px;
+        background-position: 0 -20px;
+    }
+    .gh-count {
+        margin-left: 4px;
+    }
+}
\ No newline at end of file
diff --git a/src/sentry/static/sentry/less/sentry.less b/src/sentry/static/sentry/less/sentry.less
index 11a0aa8f66..c75f8c212f 100644
--- a/src/sentry/static/sentry/less/sentry.less
+++ b/src/sentry/static/sentry/less/sentry.less
@@ -1,5 +1,6 @@
 @import url("../bootstrap/less/bootstrap.less");
 @import url("datepicker.less");
+@import url("github.less");
 
 @baseFontSize: 13px;
 @grayDark: #404f60;
diff --git a/src/sentry/templates/sentry/partial/client_config/connect.html b/src/sentry/templates/sentry/partial/client_config/connect.html
index 1f3f230020..cfe824ba71 100644
--- a/src/sentry/templates/sentry/partial/client_config/connect.html
+++ b/src/sentry/templates/sentry/partial/client_config/connect.html
@@ -1,22 +1,28 @@
+{% extends "sentry/partial/client_config/node_base.html" %}
+
 {% load i18n %}
 
-<p>{% blocktrans with 'https://github.com/mattrobenolt/raven-node' as link %}Start by installing <a href="{{ link }}">raven-node</a>:{% endblocktrans %}</p>
+{% block inner %}
+
+	<p>{% blocktrans with 'https://github.com/mattrobenolt/raven-node' as link %}Start by installing <a href="{{ link }}">raven-node</a>:{% endblocktrans %}</p>
 
-<pre>npm install raven</pre>
+	<pre>npm install raven</pre>
 
-<p>{% trans "Register an instance of the Connect middleware <strong>after your main application handler</strong>:" %}</p>
+	<p>{% trans "Register an instance of the Connect middleware <strong>after your main application handler</strong>:" %}</p>
 
-<pre>connect(
+	<pre>connect(
   connect.bodyParser(),
   connect.cookieParser(),
   mainHandler,
   raven.middleware.connect('{% if dsn %}{{ dsn }}{% else %}<strong>SENTRY_DSN</strong>{% endif %}'),
 ).listen(3000);</pre>
 
-<p>{% trans "Ensure Node is run with <code>NODE_ENV=production</code>:" %}</p>
+	<p>{% trans "Ensure Node is run with <code>NODE_ENV=production</code>:" %}</p>
+
+	<pre>NODE_ENV=production node script.js</pre>
 
-<pre>NODE_ENV=production node script.js</pre>
+	<p>{% trans "That's it! Raven automatically installs an error handling hook to pipe all uncaught exceptions to Sentry." %}</p>
 
-<p>{% trans "That's it! Raven automatically installs an error handling hook to pipe all uncaught exceptions to Sentry." %}</p>
+	<p>{% blocktrans with 'https://github.com/mattrobenolt/raven-node' as link %}For more information on other uses of Raven with Node.js, please see the <a href="{{ link }}">official documentation for raven-node</a>.{% endblocktrans %}</p>
 
-<p>{% blocktrans with 'https://github.com/mattrobenolt/raven-node' as link %}For more information on other uses of Raven with Node.js, please see the <a href="{{ link }}">official documentation for raven-node</a>.{% endblocktrans %}</p>
+{% endblock %}
\ No newline at end of file
diff --git a/src/sentry/templates/sentry/partial/client_config/django.html b/src/sentry/templates/sentry/partial/client_config/django.html
index 864df2529e..f779656ae4 100644
--- a/src/sentry/templates/sentry/partial/client_config/django.html
+++ b/src/sentry/templates/sentry/partial/client_config/django.html
@@ -1,12 +1,16 @@
+{% extends "sentry/partial/client_config/python_base.html" %}
+
 {% load i18n %}
 
-<p>{% blocktrans with 'https://github.com/getsentry/raven-python' as link %}Start by installing <a href="{{ link }}">raven-python</a>:{% endblocktrans %}</p>
+{% block inner %}
+
+	<p>{% blocktrans with 'https://github.com/getsentry/raven-python' as link %}Start by installing <a href="{{ link }}">raven-python</a>:{% endblocktrans %}</p>
 
-<pre>pip install raven</pre>
+	<pre>pip install raven</pre>
 
-<p>{% trans "Then simply modify your Django configuration:" %}</p>
+	<p>{% trans "Then simply modify your Django configuration:" %}</p>
 
-<pre># {% trans "Set your DSN value" %}
+	<pre># {% trans "Set your DSN value" %}
 SENTRY_DSN = '{% if dsn %}{{ dsn }}{% else %}<strong>SENTRY_DSN</strong>{% endif %}'
 
 # {% trans "Add raven to the list of installed apps" %}
@@ -15,6 +19,8 @@ INSTALLED_APPS = INSTALLED_APPS + (
 'raven.contrib.django',
 )</pre>
 
-<p>{% trans "That's it! Raven automatically installs an error handling hook to pipe all uncaught exceptions to Sentry." %}</p>
+	<p>{% trans "That's it! Raven automatically installs an error handling hook to pipe all uncaught exceptions to Sentry." %}</p>
+
+	<p>{% blocktrans with 'http://raven.readthedocs.org' as link %}For more information on other uses of Raven with Django, please see the <a href="{{ link }}">official documentation for raven-python</a>.{% endblocktrans %}</p>
 
-<p>{% blocktrans with 'http://raven.readthedocs.org' as link %}For more information on other uses of Raven with Django, please see the <a href="{{ link }}">official documentation for raven-python</a>.{% endblocktrans %}</p>
+{% endblock %}
\ No newline at end of file
diff --git a/src/sentry/templates/sentry/partial/client_config/express.html b/src/sentry/templates/sentry/partial/client_config/express.html
index 1a98b96981..f45194ef0c 100644
--- a/src/sentry/templates/sentry/partial/client_config/express.html
+++ b/src/sentry/templates/sentry/partial/client_config/express.html
@@ -1,19 +1,25 @@
+{% extends "sentry/partial/client_config/node_base.html" %}
+
 {% load i18n %}
 
-<p>{% blocktrans with 'https://github.com/mattrobenolt/raven-node' as link %}Start by installing <a href="{{ link }}">raven-node</a>:{% endblocktrans %}</p>
+{% block inner %}
+
+	<p>{% blocktrans with 'https://github.com/mattrobenolt/raven-node' as link %}Start by installing <a href="{{ link }}">raven-node</a>:{% endblocktrans %}</p>
 
-<pre>npm install raven</pre>
+	<pre>npm install raven</pre>
 
-<p>{% trans "Register an instance of the Express middleware:" %}</p>
+	<p>{% trans "Register an instance of the Express middleware:" %}</p>
 
-<pre>var app = require('express').createServer();
+	<pre>var app = require('express').createServer();
 
 app.error(raven.middleware.express('{% if dsn %}{{ dsn }}{% else %}<strong>SENTRY_DSN</strong>{% endif %}'));</pre>
 
-<p>{% trans "Ensure Node is run with <code>NODE_ENV=production</code>:" %}</p>
+	<p>{% trans "Ensure Node is run with <code>NODE_ENV=production</code>:" %}</p>
+
+	<pre>NODE_ENV=production node script.js</pre>
 
-<pre>NODE_ENV=production node script.js</pre>
+	<p>{% trans "That's it! Raven automatically installs an error handling hook to pipe all uncaught exceptions to Sentry." %}</p>
 
-<p>{% trans "That's it! Raven automatically installs an error handling hook to pipe all uncaught exceptions to Sentry." %}</p>
+	<p>{% blocktrans with 'https://github.com/mattrobenolt/raven-node' as link %}For more information on other uses of Raven with Node.js, please see the <a href="{{ link }}">official documentation for raven-node</a>.{% endblocktrans %}</p>
 
-<p>{% blocktrans with 'https://github.com/mattrobenolt/raven-node' as link %}For more information on other uses of Raven with Node.js, please see the <a href="{{ link }}">official documentation for raven-node</a>.{% endblocktrans %}</p>
+{% endblock %}
\ No newline at end of file
diff --git a/src/sentry/templates/sentry/partial/client_config/flask.html b/src/sentry/templates/sentry/partial/client_config/flask.html
index f43b62939b..4790570493 100644
--- a/src/sentry/templates/sentry/partial/client_config/flask.html
+++ b/src/sentry/templates/sentry/partial/client_config/flask.html
@@ -1,18 +1,22 @@
+{% extends "sentry/partial/client_config/python_base.html" %}
+
 {% load i18n %}
 
-<p>{% blocktrans with 'https://github.com/getsentry/raven-python' as link %}Start by installing <a href="{{ link }}">raven-python</a> and Blinker:{% endblocktrans %}</p>
+{% block inner %}
+	<p>{% blocktrans with 'https://github.com/getsentry/raven-python' as link %}Start by installing <a href="{{ link }}">raven-python</a> and Blinker:{% endblocktrans %}</p>
 
-<pre>pip install raven blinker</pre>
+	<pre>pip install raven blinker</pre>
 
-<p><em>{% trans "Blinker is required for signals to work within Flask." %}</em></p>
+	<p><em>{% trans "Blinker is required for signals to work within Flask." %}</em></p>
 
-<p>{% trans "Add the required configuration in your application setup:" %}</p>
+	<p>{% trans "Add the required configuration in your application setup:" %}</p>
 
-<pre>from raven.contrib.flask import Sentry
+	<pre>from raven.contrib.flask import Sentry
 
 app.config['SENTRY_DSN'] = '{% if dsn %}{{ dsn }}{% else %}<strong>SENTRY_DSN</strong>{% endif %}'
 sentry = Sentry(app)</pre>
 
-<p>{% trans "That's it! Raven automatically installs an error handling hook to pipe all uncaught exceptions to Sentry." %}</p>
+	<p>{% trans "That's it! Raven automatically installs an error handling hook to pipe all uncaught exceptions to Sentry." %}</p>
 
-<p>{% blocktrans with 'http://raven.readthedocs.org' as link %}For more information on other uses of Raven with Flask, please see the <a href="{{ link }}">official documentation for raven-python</a>.{% endblocktrans %}</p>
+	<p>{% blocktrans with 'http://raven.readthedocs.org' as link %}For more information on other uses of Raven with Flask, please see the <a href="{{ link }}">official documentation for raven-python</a>.{% endblocktrans %}</p>
+{% endblock %}
diff --git a/src/sentry/templates/sentry/partial/client_config/node.js.html b/src/sentry/templates/sentry/partial/client_config/node.js.html
index 5a7bfb95a4..40cd4355c7 100644
--- a/src/sentry/templates/sentry/partial/client_config/node.js.html
+++ b/src/sentry/templates/sentry/partial/client_config/node.js.html
@@ -1,29 +1,35 @@
+{% extends "sentry/partial/client_config/node_base.html" %}
+
 {% load i18n %}
 
-<p>{% blocktrans with 'https://github.com/mattrobenolt/raven-node' as link %}Start by installing <a href="{{ link }}">raven-node</a>:{% endblocktrans %}</p>
+{% block inner %}
+
+	<p>{% blocktrans with 'https://github.com/mattrobenolt/raven-node' as link %}Start by installing <a href="{{ link }}">raven-node</a>:{% endblocktrans %}</p>
 
-<pre>npm install raven</pre>
+	<pre>npm install raven</pre>
 
-<p>{% trans "Create an instance of the client:" %}</p>
+	<p>{% trans "Create an instance of the client:" %}</p>
 
-<pre>var raven = require('raven');
+	<pre>var raven = require('raven');
 
 var client = new raven.Client('{% if dsn %}{{ dsn }}{% else %}<strong>SENTRY_DSN</strong>{% endif %}');</pre>
 
-<p>{% trans "You can also optionally install a default error handler to catch all exceptions:" %}</p>
+	<p>{% trans "You can also optionally install a default error handler to catch all exceptions:" %}</p>
 
-<pre>client.patchGlobal();</pre>
+	<pre>client.patchGlobal();</pre>
 
-<p>{% trans "Ensure Node is run with <code>NODE_ENV=production</code>:" %}</p>
+	<p>{% trans "Ensure Node is run with <code>NODE_ENV=production</code>:" %}</p>
 
-<pre>NODE_ENV=production node script.js</pre>
+	<pre>NODE_ENV=production node script.js</pre>
 
-<p>{% trans "Now call out to the raven client to capture events:" %}</p>
+	<p>{% trans "Now call out to the raven client to capture events:" %}</p>
 
-<pre>// {% trans "record a simple message" %}
+	<pre>// {% trans "record a simple message" %}
 client.captureMessage('hello world!')
 
 // {% trans "capture an exception" %}
 client.captureError(new Error('Uh oh!!'));</pre>
 
-<p>{% blocktrans with 'https://github.com/mattrobenolt/raven-node' as link %}For more information on other uses of Raven with Node.js, please see the <a href="{{ link }}">official documentation for raven-node</a>.{% endblocktrans %}</p>
+	<p>{% blocktrans with 'https://github.com/mattrobenolt/raven-node' as link %}For more information on other uses of Raven with Node.js, please see the <a href="{{ link }}">official documentation for raven-node</a>.{% endblocktrans %}</p>
+
+{% endblock %}
\ No newline at end of file
diff --git a/src/sentry/templates/sentry/partial/client_config/node_base.html b/src/sentry/templates/sentry/partial/client_config/node_base.html
new file mode 100644
index 0000000000..daab8254c2
--- /dev/null
+++ b/src/sentry/templates/sentry/partial/client_config/node_base.html
@@ -0,0 +1,14 @@
+{% load sentry_helpers %}
+
+<div class="pull-right">
+	{% github_button 'mattrobenolt' 'raven-node' %}
+</div>
+
+<ul class="nav nav-tabs">
+	<li{% if platform == 'node.js' %} class="active"{% endif %}><a href="{% url sentry-docs-client project.slug 'node.js' %}">Node.js</a></li>
+	<li{% if platform == 'express' %} class="active"{% endif %}><a href="{% url sentry-docs-client project.slug 'express' %}">Express</a></li>
+	<li{% if platform == 'connect' %} class="active"{% endif %}><a href="{% url sentry-docs-client project.slug 'connect' %}">Connect</a></li>
+</ul>
+
+{% block inner %}
+{% endblock %}
\ No newline at end of file
diff --git a/src/sentry/templates/sentry/partial/client_config/python.html b/src/sentry/templates/sentry/partial/client_config/python.html
index 2cbf931231..5eb4e01557 100644
--- a/src/sentry/templates/sentry/partial/client_config/python.html
+++ b/src/sentry/templates/sentry/partial/client_config/python.html
@@ -1,18 +1,22 @@
+{% extends "sentry/partial/client_config/python_base.html" %}
+
 {% load i18n %}
 
-<p>{% blocktrans with 'https://github.com/getsentry/raven-python' as link %}Start by installing <a href="{{ link }}">raven-python</a>:{% endblocktrans %}</p>
+{% block inner %}
+
+	<p>{% blocktrans with 'https://github.com/getsentry/raven-python' as link %}Start by installing <a href="{{ link }}">raven-python</a>:{% endblocktrans %}</p>
 
-<pre>pip install raven</pre>
+	<pre>pip install raven</pre>
 
-<p>{% trans "Create an instance of the client:" %}</p>
+	<p>{% trans "Create an instance of the client:" %}</p>
 
-<pre>from raven import Client
+	<pre>from raven import Client
 
 client = Client('{% if dsn %}{{ dsn }}{% else %}<strong>SENTRY_DSN</strong>{% endif %}')</pre>
 
-<p>{% trans "Now call out to the raven client to capture events:" %}</p>
+	<p>{% trans "Now call out to the raven client to capture events:" %}</p>
 
-<pre># {% trans "record a simple message" %}
+	<pre># {% trans "record a simple message" %}
 client.captureMessage('hello world!')
 
 # {% trans "capture an exception" %}
@@ -21,10 +25,12 @@ try:
 except DivisionError:
     client.captureException()</pre>
 
-<p>{% trans "If you want to wrap a WSGI app to record uncaught exceptions, Raven provides a middleware for that:" %}</p>
+	<p>{% trans "If you want to wrap a WSGI app to record uncaught exceptions, Raven provides a middleware for that:" %}</p>
 
-<pre>from raven.middleware import Sentry
+	<pre>from raven.middleware import Sentry
 
 application = Sentry(application)</pre>
 
-<p>{% blocktrans with 'http://raven.readthedocs.org' as link %}For more information on other uses of Raven with Python, please see the <a href="{{ link }}">official documentation for raven-python</a>.{% endblocktrans %}</p>
+	<p>{% blocktrans with 'http://raven.readthedocs.org' as link %}For more information on other uses of Raven with Python, please see the <a href="{{ link }}">official documentation for raven-python</a>.{% endblocktrans %}</p>
+
+{% endblock %}
\ No newline at end of file
diff --git a/src/sentry/templates/sentry/partial/client_config/python_base.html b/src/sentry/templates/sentry/partial/client_config/python_base.html
new file mode 100644
index 0000000000..18245481b4
--- /dev/null
+++ b/src/sentry/templates/sentry/partial/client_config/python_base.html
@@ -0,0 +1,14 @@
+{% load sentry_helpers %}
+
+<div class="pull-right">
+	{% github_button 'getsentry' 'raven-python' %}
+</div>
+
+<ul class="nav nav-tabs">
+	<li{% if platform == 'python' %} class="active"{% endif %}><a href="{% url sentry-docs-client project.slug 'python' %}">Python</a></li>
+	<li{% if platform == 'django' %} class="active"{% endif %}><a href="{% url sentry-docs-client project.slug 'django' %}">Django</a></li>
+	<li{% if platform == 'flask' %} class="active"{% endif %}><a href="{% url sentry-docs-client project.slug 'flask' %}">Flask</a></li>
+</ul>
+
+{% block inner %}
+{% endblock %}
\ No newline at end of file
diff --git a/src/sentry/templates/sentry/partial/client_config/rails3.html b/src/sentry/templates/sentry/partial/client_config/rails3.html
index 174c534338..cd9379ba87 100644
--- a/src/sentry/templates/sentry/partial/client_config/rails3.html
+++ b/src/sentry/templates/sentry/partial/client_config/rails3.html
@@ -1,21 +1,27 @@
+{% extends "sentry/partial/client_config/ruby_base.html" %}
+
 {% load i18n %}
 
-<p>{% blocktrans with 'https://github.com/getsentry/raven-ruby' as link %}Start by installing <a href="{{ link }}">raven-ruby</a>:{% endblocktrans %}</p>
+{% block inner %}
+
+	<p>{% blocktrans with 'https://github.com/getsentry/raven-ruby' as link %}Start by installing <a href="{{ link }}">raven-ruby</a>:{% endblocktrans %}</p>
 
-<pre>gem install sentry-raven</pre>
+	<pre>gem install sentry-raven</pre>
 
-<p>{% trans "Or add it to your <code>Gemfile</code>:" %}</p>
+	<p>{% trans "Or add it to your <code>Gemfile</code>:" %}</p>
 
-<pre>gem "sentry-raven", :git => "https://github.com/getsentry/raven-ruby.git"</pre>
+	<pre>gem "sentry-raven", :git => "https://github.com/getsentry/raven-ruby.git"</pre>
 
-<p>{% trans "Add a <code>config/initializers/raven.rb</code> containing:" %}</p>
+	<p>{% trans "Add a <code>config/initializers/raven.rb</code> containing:" %}</p>
 
-<pre>require 'raven'
+	<pre>require 'raven'
 
 Raven.configure do |config|
   config.dsn = '{% if dsn %}{{ dsn }}{% else %}<strong>SENTRY_DSN</strong>{% endif %}'
 end</pre>
 
-<p>{% trans "That's it! Raven automatically installs an error handling hook to pipe all uncaught exceptions to Sentry." %}</p>
+	<p>{% trans "That's it! Raven automatically installs an error handling hook to pipe all uncaught exceptions to Sentry." %}</p>
+
+	<p>{% blocktrans with 'https://github.com/getsentry/raven-ruby' as link %}For more information on other uses of Raven with Ruby, please see the <a href="{{ link }}">official documentation for raven-ruby</a>.{% endblocktrans %}</p>
 
-<p>{% blocktrans with 'https://github.com/getsentry/raven-ruby' as link %}For more information on other uses of Raven with Ruby, please see the <a href="{{ link }}">official documentation for raven-ruby</a>.{% endblocktrans %}</p>
+{% endblock %}
diff --git a/src/sentry/templates/sentry/partial/client_config/ruby.html b/src/sentry/templates/sentry/partial/client_config/ruby.html
index 577e423f84..4f4d0ba875 100644
--- a/src/sentry/templates/sentry/partial/client_config/ruby.html
+++ b/src/sentry/templates/sentry/partial/client_config/ruby.html
@@ -1,25 +1,31 @@
+{% extends "sentry/partial/client_config/ruby_base.html" %}
+
 {% load i18n %}
 
-<p>{% blocktrans with 'https://github.com/getsentry/raven-ruby' as link %}Start by installing <a href="{{ link }}">raven-ruby</a>:{% endblocktrans %}</p>
+{% block inner %}
+
+	<p>{% blocktrans with 'https://github.com/getsentry/raven-ruby' as link %}Start by installing <a href="{{ link }}">raven-ruby</a>:{% endblocktrans %}</p>
 
-<pre>gem install sentry-raven</pre>
+	<pre>gem install sentry-raven</pre>
 
-<p>{% trans "Or add it to your <code>Gemfile</code>:" %}</p>
+	<p>{% trans "Or add it to your <code>Gemfile</code>:" %}</p>
 
-<pre>gem "sentry-raven", :git => "https://github.com/getsentry/raven-ruby.git"</pre>
+	<pre>gem "sentry-raven", :git => "https://github.com/getsentry/raven-ruby.git"</pre>
 
-<p>{% trans "Create an instance of the client:" %}</p>
+	<p>{% trans "Create an instance of the client:" %}</p>
 
-<pre>require 'raven'
+	<pre>require 'raven'
 
 Raven.configure do |config|
   config.dsn = '{% if dsn %}{{ dsn }}{% else %}<strong>SENTRY_DSN</strong>{% endif %}'
 end</pre>
 
-<p>{% trans "Now wrap your application to capture events:" %}</p>
+	<p>{% trans "Now wrap your application to capture events:" %}</p>
 
-<pre>Raven.capture do
+	<pre>Raven.capture do
     MyApp.run
 end</pre>
 
-<p>{% blocktrans with 'https://github.com/getsentry/raven-ruby' as link %}For more information on other uses of Raven with Ruby, please see the <a href="{{ link }}">official documentation for raven-ruby</a>.{% endblocktrans %}</p>
+	<p>{% blocktrans with 'https://github.com/getsentry/raven-ruby' as link %}For more information on other uses of Raven with Ruby, please see the <a href="{{ link }}">official documentation for raven-ruby</a>.{% endblocktrans %}</p>
+
+{% endblock %}
\ No newline at end of file
diff --git a/src/sentry/templates/sentry/partial/client_config/ruby_base.html b/src/sentry/templates/sentry/partial/client_config/ruby_base.html
new file mode 100644
index 0000000000..eb8f3f0956
--- /dev/null
+++ b/src/sentry/templates/sentry/partial/client_config/ruby_base.html
@@ -0,0 +1,13 @@
+{% load sentry_helpers %}
+
+<div class="pull-right">
+	{% github_button 'getsentry' 'raven-ruby' %}
+</div>
+
+<ul class="nav nav-tabs">
+	<li{% if platform == 'ruby' %} class="active"{% endif %}><a href="{% url sentry-docs-client project.slug 'ruby' %}">Ruby</a></li>
+	<li{% if platform == 'rails3' %} class="active"{% endif %}><a href="{% url sentry-docs-client project.slug 'rails3' %}">Rails 3</a></li>
+</ul>
+
+{% block inner %}
+{% endblock %}
\ No newline at end of file
diff --git a/src/sentry/templates/sentry/partial/github_button.html b/src/sentry/templates/sentry/partial/github_button.html
new file mode 100644
index 0000000000..6cefddfeeb
--- /dev/null
+++ b/src/sentry/templates/sentry/partial/github_button.html
@@ -0,0 +1,8 @@
+<span class="github" data-user="{{ user }}" data-repo="{{ repo }}">
+	<span id="github-btn" class="github-btn">
+		<a id="gh-btn" href="https://github.com/{{ user }}/{{ repo }}" target="_blank" class="gh-btn">
+			<span class="gh-ico"></span>
+			<span id="gh-text" class="gh-text">{{ repo }}</span>
+		</a>
+	</span>
+</span>
\ No newline at end of file
diff --git a/src/sentry/templates/sentry/projects/manage.html b/src/sentry/templates/sentry/projects/manage.html
index 9e22ea9d19..53a381b668 100644
--- a/src/sentry/templates/sentry/projects/manage.html
+++ b/src/sentry/templates/sentry/projects/manage.html
@@ -101,6 +101,12 @@
         <li{% if page == 'client_help_ruby' %} class="active"{% endif %}>
             <a href="{% url sentry-docs-client project.slug "ruby" %}">{% trans "Ruby" %}</a>
         </li>
+        <li{% if page == 'client_help_node.js' %} class="active"{% endif %}>
+            <a href="{% url sentry-docs-client project.slug "node.js" %}">{% trans "Node.js" %}</a>
+        </li>
+        <li{% if page == 'client_help_ios' %} class="active"{% endif %}>
+            <a href="{% url sentry-docs-client project.slug "ios" %}">{% trans "iOS" %}</a>
+        </li>
     </ul>
     {% with project|get_plugins as plugins %}
         {% if plugins %}
diff --git a/src/sentry/templatetags/sentry_helpers.py b/src/sentry/templatetags/sentry_helpers.py
index fd2da2e6a5..4e6b8b6bde 100644
--- a/src/sentry/templatetags/sentry_helpers.py
+++ b/src/sentry/templatetags/sentry_helpers.py
@@ -365,3 +365,11 @@ def get_rendered_interfaces(event):
             continue
         interface_list.append((interface, mark_safe(html)))
     return sorted(interface_list, key=lambda x: x[0].get_display_score(), reverse=True)
+
+
+@register.inclusion_tag('sentry/partial/github_button.html')
+def github_button(user, repo):
+    return {
+        'user': user,
+        'repo': repo,
+    }
