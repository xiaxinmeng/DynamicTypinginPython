commit d75bed5ad57c2735abccb51301cec9beac526ef5
Author: David Cramer <dcramer@gmail.com>
Date:   Sat Mar 24 13:43:34 2012 -0700

    Heroku blurbs arent very useful

diff --git a/sentry/templates/sentry/partial/client_config/connect.html b/sentry/templates/sentry/partial/client_config/connect.html
index 6f3cda8ecd..950be85f6e 100644
--- a/sentry/templates/sentry/partial/client_config/connect.html
+++ b/sentry/templates/sentry/partial/client_config/connect.html
@@ -13,10 +13,10 @@
   raven.middleware.connect('{% if dsn %}{{ dsn }}{% else %}<strong>SENTRY_DSN</strong>{% endif %}'),
 ).listen(3000);</pre>
 
-<p>{% trans "If you're using Heroku, we'll pick up the DSN from your environment automatically." %}</p>
-
 <p>{% trans "Ensure Node is run with <code>NODE_ENV=production</code>:" %}</p>
 
 <pre>NODE_ENV=production node script.js</pre>
 
+<p>{% trans "That's it! Raven automatically installs an error handling hook to pipe all uncaught exceptions to Sentry." %}</p>
+
 <p>{% blocktrans with 'https://github.com/mattrobenolt/raven-node' as link %}For more information on other uses of Raven with Node.js, please see the <a href="{{ link }}">official documentation for raven-node</a>.{% endblocktrans %}</p>
diff --git a/sentry/templates/sentry/partial/client_config/django.html b/sentry/templates/sentry/partial/client_config/django.html
index 918b02ba9d..68657cc1a7 100644
--- a/sentry/templates/sentry/partial/client_config/django.html
+++ b/sentry/templates/sentry/partial/client_config/django.html
@@ -15,8 +15,6 @@ INSTALLED_APPS = INSTALLED_APPS + (
 'raven.contrib.django',
 )</pre>
 
-<p>{% trans "If you're using Heroku, we'll pick up the DSN from your environment automatically." %}</p>
-
 <p>{% trans "That's it! Raven automatically installs an error handling hook to pipe all uncaught exceptions to Sentry." %}</p>
 
 <p>{% blocktrans with 'http://raven.readthedocs.org' as link %}For more information on other uses of Raven with Django, please see the <a href="{{ link }}">official documentation for raven-python</a>.{% endblocktrans %}</p>
diff --git a/sentry/templates/sentry/partial/client_config/express.html b/sentry/templates/sentry/partial/client_config/express.html
index 58c106aed7..11659805e6 100644
--- a/sentry/templates/sentry/partial/client_config/express.html
+++ b/sentry/templates/sentry/partial/client_config/express.html
@@ -10,8 +10,6 @@
 
 app.error(raven.middleware.express('{% if dsn %}{{ dsn }}{% else %}<strong>SENTRY_DSN</strong>{% endif %}'));</pre>
 
-<p>{% trans "If you're using Heroku, we'll pick up the DSN from your environment automatically." %}</p>
-
 <p>{% trans "Ensure Node is run with <code>NODE_ENV=production</code>:" %}</p>
 
 <pre>NODE_ENV=production node script.js</pre>
diff --git a/sentry/templates/sentry/partial/client_config/flask.html b/sentry/templates/sentry/partial/client_config/flask.html
index c28f0632cd..d98b82f309 100644
--- a/sentry/templates/sentry/partial/client_config/flask.html
+++ b/sentry/templates/sentry/partial/client_config/flask.html
@@ -13,8 +13,6 @@
 app.config['SENTRY_DSN'] = '{% if dsn %}{{ dsn }}{% else %}<strong>SENTRY_DSN</strong>{% endif %}'
 sentry = Sentry(app)</pre>
 
-<p>{% trans "If you're using Heroku, we'll pick up the DSN from your environment automatically." %}</p>
-
 <p>{% trans "That's it! Raven automatically installs an error handling hook to pipe all uncaught exceptions to Sentry." %}</p>
 
 <p>{% blocktrans with 'http://raven.readthedocs.org' as link %}For more information on other uses of Raven with Flask, please see the <a href="{{ link }}">official documentation for raven-python</a>.{% endblocktrans %}</p>
diff --git a/sentry/templates/sentry/partial/client_config/java.html b/sentry/templates/sentry/partial/client_config/java.html
index d286aaa838..b07c4c9e56 100644
--- a/sentry/templates/sentry/partial/client_config/java.html
+++ b/sentry/templates/sentry/partial/client_config/java.html
@@ -6,8 +6,6 @@
 
 <pre>RavenClient client = new RavenClient("{% if dsn %}{{ dsn }}{% else %}<strong>SENTRY_DSN</strong>{% endif %}");</pre>
 
-<p>{% trans "If you're using Heroku, we'll pick up the DSN from your environment automatically." %}</p>
-
 <p>{% trans "Now call out to the raven client to capture events:" %}</p>
 
 <pre>// {% trans "record a simple message" %}
diff --git a/sentry/templates/sentry/partial/client_config/node.js.html b/sentry/templates/sentry/partial/client_config/node.js.html
index 5c6b637d12..f30a7945c1 100644
--- a/sentry/templates/sentry/partial/client_config/node.js.html
+++ b/sentry/templates/sentry/partial/client_config/node.js.html
@@ -10,8 +10,6 @@
 
 var client = new raven.Client('{% if dsn %}{{ dsn }}{% else %}<strong>SENTRY_DSN</strong>{% endif %}');</pre>
 
-<p>{% trans "If you're using Heroku, we'll pick up the DSN from your environment automatically." %}</p>
-
 <p>{% trans "You can also optionally install a default error handler to catch all exceptions:" %}</p>
 
 <pre>client.patchGlobal();</pre>
diff --git a/sentry/templates/sentry/partial/client_config/php.html b/sentry/templates/sentry/partial/client_config/php.html
index 79b653d293..f42a170c6e 100644
--- a/sentry/templates/sentry/partial/client_config/php.html
+++ b/sentry/templates/sentry/partial/client_config/php.html
@@ -12,8 +12,6 @@ Raven_Autoloader::register();</pre>
 
 <pre>$client = new Raven_Client('{% if dsn %}{{ dsn }}{% else %}<strong>SENTRY_DSN</strong>{% endif %}');</pre>
 
-<p>{% trans "If you're using Heroku, we'll pick up the DSN from your environment automatically." %}</p>
-
 <p>{% trans "Now call out to the raven client to capture events:" %}</p>
 
 <pre>// {% trans "record a simple message" %}
diff --git a/sentry/templates/sentry/partial/client_config/python.html b/sentry/templates/sentry/partial/client_config/python.html
index 83ceaba30e..7f3e26603c 100644
--- a/sentry/templates/sentry/partial/client_config/python.html
+++ b/sentry/templates/sentry/partial/client_config/python.html
@@ -10,8 +10,6 @@
 
 client = Client('{% if dsn %}{{ dsn }}{% else %}<strong>SENTRY_DSN</strong>{% endif %}')</pre>
 
-<p>{% trans "If you're using Heroku, we'll pick up the DSN from your environment automatically." %}</p>
-
 <p>{% trans "Now call out to the raven client to capture events:" %}</p>
 
 <pre># {% trans "record a simple message" %}
