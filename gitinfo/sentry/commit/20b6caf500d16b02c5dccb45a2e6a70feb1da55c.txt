commit 20b6caf500d16b02c5dccb45a2e6a70feb1da55c
Author: Lior Sion <liorsion@the-macbook-air.(none)>
Date:   Wed Apr 27 09:30:42 2011 +0300

    added default value to sort in order to avoid exception

diff --git a/sentry/views.py b/sentry/views.py
index b63b7aecea..1a84589381 100644
--- a/sentry/views.py
+++ b/sentry/views.py
@@ -126,7 +126,7 @@ def search(request):
     else:
         message_list = GroupedMessage.objects.none()
     
-    sort = request.GET.get('sort')
+    sort = request.GET.get('sort', 'relevance')
     if sort == 'date':
         message_list = message_list.order_by('-last_seen')
     elif sort == 'new':
