commit 7279ced6c7fbe40c0cc2f8a9958c09885906552b
Author: Daniel Griesser <daniel.griesser.86@gmail.com>
Date:   Wed Sep 13 08:23:45 2017 +0200

    feat(react): Make nav issues button reload page

diff --git a/src/sentry/static/sentry/app/views/stream.jsx b/src/sentry/static/sentry/app/views/stream.jsx
index 6907f0360f..159cc1cc3c 100644
--- a/src/sentry/static/sentry/app/views/stream.jsx
+++ b/src/sentry/static/sentry/app/views/stream.jsx
@@ -97,9 +97,6 @@ const Stream = React.createClass({
     this.fetchSavedSearches();
     this.fetchProcessingIssues();
     this.fetchTags();
-    if (!this.state.loading) {
-      this.fetchData();
-    }
   },
 
   componentWillReceiveProps(nextProps) {
@@ -109,6 +106,8 @@ const Stream = React.createClass({
       return;
     }
 
+    this.fetchData();
+
     let searchIdChanged = this.state.isDefaultSearch
       ? nextProps.params.searchId
       : nextProps.params.searchId !== this.state.searchId;
