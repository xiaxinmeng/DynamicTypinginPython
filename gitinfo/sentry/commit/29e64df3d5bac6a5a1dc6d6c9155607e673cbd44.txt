commit 29e64df3d5bac6a5a1dc6d6c9155607e673cbd44
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Oct 12 17:54:03 2015 -0700

    Persist role

diff --git a/src/sentry/models/organizationmember.py b/src/sentry/models/organizationmember.py
index b7693dcf82..59f11cd2d8 100644
--- a/src/sentry/models/organizationmember.py
+++ b/src/sentry/models/organizationmember.py
@@ -130,10 +130,10 @@ class OrganizationMember(Model):
     def save(self, *args, **kwargs):
         assert self.user_id or self.email, \
             'Must set user or email'
+        self.role = self._compute_role()
         super(OrganizationMember, self).save(*args, **kwargs)
         if not self.counter:
             self._set_counter()
-        self.role = self._compute_role()
 
     @transaction.atomic
     def delete(self, *args, **kwargs):
