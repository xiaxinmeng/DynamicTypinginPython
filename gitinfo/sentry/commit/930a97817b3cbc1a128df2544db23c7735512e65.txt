commit 930a97817b3cbc1a128df2544db23c7735512e65
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Apr 29 10:11:24 2013 -0700

    Dont limit password resets

diff --git a/src/sentry/web/frontend/accounts.py b/src/sentry/web/frontend/accounts.py
index 715c57800e..0a3e605d40 100644
--- a/src/sentry/web/frontend/accounts.py
+++ b/src/sentry/web/frontend/accounts.py
@@ -110,13 +110,9 @@ def recover(request):
             user=form.cleaned_data['user']
         )
         if not password_hash.is_valid():
-            created = True
             password_hash.date_added = timezone.now()
             password_hash.set_hash()
 
-        if not created:
-            form.errors['__all__'] = ['A password reset was already attempted for this account within the last 24 hours.']
-
     if form.is_valid():
         password_hash.send_recover_mail()
 
