commit a4a6d822ca5c1e9f8418ffd7ad55f88eb8593b9b
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Feb 22 20:51:51 2012 -0800

    Dont run eventlet patcher in gunicorn as it causes the world to turn to shit

diff --git a/sentry/services/http.py b/sentry/services/http.py
index d8a44aec03..1ff229f428 100644
--- a/sentry/services/http.py
+++ b/sentry/services/http.py
@@ -32,9 +32,6 @@ class SentryApplication(DjangoApplication):
 
     def load(self):
         from sentry.wsgi import application
-        import eventlet.patcher
-
-        eventlet.patcher.monkey_patch()
 
         self.validate()
         self.activate_translation()
@@ -68,7 +65,7 @@ class SentryHTTPServer(Service):
             'worker_class': 'eventlet',
             'debug': debug,
             'daemon': False,
-            'timeout': 3,
+            'timeout': 30,
             # 'post_fork': post_fork,
         }
         options.update(settings.WEB_OPTIONS or {})
