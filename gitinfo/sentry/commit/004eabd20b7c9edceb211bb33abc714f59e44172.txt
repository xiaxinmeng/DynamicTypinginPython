commit 004eabd20b7c9edceb211bb33abc714f59e44172
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Mar 14 11:34:40 2019 -0700

    fix(feedback): Lowercase event ID to align with normalization behavior

diff --git a/src/sentry/api/endpoints/project_user_reports.py b/src/sentry/api/endpoints/project_user_reports.py
index 7ff99e0cf6..703422facd 100644
--- a/src/sentry/api/endpoints/project_user_reports.py
+++ b/src/sentry/api/endpoints/project_user_reports.py
@@ -135,6 +135,8 @@ class ProjectUserReportsEndpoint(ProjectEndpoint, EnvironmentMixin):
             return self.respond(serializer.errors, status=400)
 
         report = serializer.object
+        # XXX(dcramer): enforce case insensitivty by coercing this to a lowercase string
+        report.event_id = report.event_id.lower()
         report.project = project
 
         # TODO(dcramer): we should probably create the user if they dont
diff --git a/src/sentry/web/frontend/error_page_embed.py b/src/sentry/web/frontend/error_page_embed.py
index c57c64a1d4..9a3c995f17 100644
--- a/src/sentry/web/frontend/error_page_embed.py
+++ b/src/sentry/web/frontend/error_page_embed.py
@@ -123,6 +123,9 @@ class ErrorPageEmbedView(View):
             return self._smart_response(
                 request, {'eventId': 'Missing or invalid parameter.'}, status=400)
 
+        # XXX(dcramer): enforce case insensitivty by coercing this to a lowercase string
+        event_id = event_id.lower()
+
         key = self._get_project_key(request)
         if not key:
             return self._smart_response(
