commit b7cd8851547cccf948a3027d1a64002ba81596bb
Author: Nikolay Panov <github@npanov.com>
Date:   Thu May 23 11:28:50 2019 -0700

    Fixed syntax error in 'exec' command handler (#13360)
    
    The `startswith` method accepts a tuple as a first argument, without a tuple the following error appears:
    
    ```
    TypeError: slice indices must be integers or None or have an __index__ method
    ```

diff --git a/src/sentry/runner/commands/exec.py b/src/sentry/runner/commands/exec.py
index b9dca435f4..7f0daeca51 100644
--- a/src/sentry/runner/commands/exec.py
+++ b/src/sentry/runner/commands/exec.py
@@ -86,7 +86,7 @@ def exec_(c, file):
         for line in c.splitlines():
             if line.startswith('from __future__'):
                 state = 1
-            elif line and not line.startswith('#', '"', "'") and state == 1:
+            elif line and not line.startswith(('#', '"', "'")) and state == 1:
                 state = 2
             if state == 2:
                 body.append(line)
