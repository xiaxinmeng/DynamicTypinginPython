commit 8438836addc45144ba493a859d0214849139ff9e
Author: Lyn Nagara <lyn.nagara@gmail.com>
Date:   Tue May 8 12:53:23 2018 -0700

    chore: Add mock data for release project environment model (#8359)

diff --git a/bin/load-mocks b/bin/load-mocks
index 4500d739ac..4b0d1c97b6 100755
--- a/bin/load-mocks
+++ b/bin/load-mocks
@@ -23,8 +23,8 @@ from sentry.models import (
     Activity, Broadcast, Commit, CommitAuthor, CommitFileChange, Deploy, Event,
     Environment, File, Group, GroupMeta, GroupRelease, GroupTombstone, Organization,
     OrganizationAccessRequest, OrganizationMember, Project, Release,
-    ReleaseCommit, ReleaseEnvironment, ReleaseFile, Repository, Team,
-    TOMBSTONE_FIELDS_FROM_GROUP, User, UserReport
+    ReleaseCommit, ReleaseEnvironment, ReleaseProjectEnvironment, ReleaseFile, Repository,
+    Team, TOMBSTONE_FIELDS_FROM_GROUP, User, UserReport
 )
 from sentry.signals import mocks_loaded
 from sentry.similarity import features
@@ -487,6 +487,13 @@ def main(num_events=1, extra_events=False):
                 authors=[six.text_type(a.id) for a in authors],
             )
 
+            ReleaseProjectEnvironment.objects.create_or_update(
+                project=project,
+                environment=environment,
+                release=release,
+                defaults={'last_deploy_id': deploy.id}
+            )
+
             Activity.objects.create(
                 type=Activity.DEPLOY,
                 project=project,
