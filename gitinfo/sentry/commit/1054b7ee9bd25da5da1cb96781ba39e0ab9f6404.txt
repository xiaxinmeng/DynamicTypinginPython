commit 1054b7ee9bd25da5da1cb96781ba39e0ab9f6404
Author: evanh <evanh@users.noreply.github.com>
Date:   Fri Apr 17 16:07:03 2020 -0400

    fix(discover) Return proper type for aggregate dates (#18285)
    
    We were returning the number type for aggregates on dates, which would result in
    the column not displaying. Correctly assign the right type for the frontend.

diff --git a/src/sentry/api/event_search.py b/src/sentry/api/event_search.py
index bd5ffd0171..26f4c1c5be 100644
--- a/src/sentry/api/event_search.py
+++ b/src/sentry/api/event_search.py
@@ -1149,13 +1149,11 @@ FUNCTIONS = {
         "name": "min",
         "args": [NumericColumnNoLookup("column")],
         "aggregate": ["min", u"{column}", None],
-        "result_type": "number",
     },
     "max": {
         "name": "max",
         "args": [NumericColumnNoLookup("column")],
         "aggregate": ["max", u"{column}", None],
-        "result_type": "number",
     },
     "avg": {
         "name": "avg",
diff --git a/tests/snuba/api/endpoints/test_organization_events_v2.py b/tests/snuba/api/endpoints/test_organization_events_v2.py
index 2504d2023d..731c19153a 100644
--- a/tests/snuba/api/endpoints/test_organization_events_v2.py
+++ b/tests/snuba/api/endpoints/test_organization_events_v2.py
@@ -1029,6 +1029,7 @@ class OrganizationEventsV2EndpointTest(APITestCase, SnubaTestCase):
 
         assert response.status_code == 200, response.content
         assert len(response.data["data"]) == 2
+        response.data["meta"]["max_timestamp"] == "date"
         data = response.data["data"]
         assert data[0]["issue.id"] == event.group_id
 
