commit ec50b449ecf1d9db1fe18238b3728f84730768c1
Author: David Cramer <dcramer@gmail.com>
Date:   Tue May 31 16:40:28 2011 -0700

    Ensure TEMPLATE_DIRS are set by default

diff --git a/sentry/conf/defaults.py b/sentry/conf/defaults.py
index 262500733f..e9fe372a1d 100644
--- a/sentry/conf/defaults.py
+++ b/sentry/conf/defaults.py
@@ -79,6 +79,13 @@ INTERNAL_IPS = set()
 
 SERVER_EMAIL = 'root@localhost'
 
+TEMPLATE_DIRS = (
+    # Put strings here, like "/home/html/django_templates" or "C:/www/django/templates".
+    # Always use forward slashes, even on Windows.
+    # Don't forget to use absolute paths, not relative paths.
+    os.path.join(ROOT, 'templates'),
+)
+
 ## The following settings refer to the built-in webserver
 
 WEB_HOST = 'localhost'
