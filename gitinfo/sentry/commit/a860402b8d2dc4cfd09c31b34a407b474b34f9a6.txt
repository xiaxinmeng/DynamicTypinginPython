commit a860402b8d2dc4cfd09c31b34a407b474b34f9a6
Author: Armin Ronacher <armin.ronacher@active-4.com>
Date:   Mon Mar 7 00:59:43 2016 +0100

    Fixed a potential bug with callsigns for empty project names.

diff --git a/src/sentry/web/forms/add_project.py b/src/sentry/web/forms/add_project.py
index 691c9b7375..16f56b338b 100644
--- a/src/sentry/web/forms/add_project.py
+++ b/src/sentry/web/forms/add_project.py
@@ -39,7 +39,7 @@ class AddProjectForm(forms.ModelForm):
     def clean_callsign(self):
         callsign = self.cleaned_data.get('callsign')
         if not callsign:
-            it = iter_callsign_choices(self.cleaned_data['name'])
+            it = iter_callsign_choices(self.cleaned_data.get('name') or '')
             for potential_callsign in it:
                 try:
                     Project.objects.get(
