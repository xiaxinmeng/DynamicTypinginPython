commit 5959bd3ed9feabcf8e47d93b679f93b5105f416d
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Feb 2 02:57:10 2012 -0800

    Some template fixes :(

diff --git a/CHANGES b/CHANGES
index 613d38d3ba..21bc679e99 100644
--- a/CHANGES
+++ b/CHANGES
@@ -1,3 +1,10 @@
+Version 2.4.1
+-------------
+
+* Change appearance of resolve states.
+* Adjust user management to link username (which is required) and not
+  optional fields.
+
 Version 2.4.0
 -------------
 
diff --git a/sentry/templates/sentry/admin/users/list.html b/sentry/templates/sentry/admin/users/list.html
index 5230b2e8d6..29e4980ad1 100644
--- a/sentry/templates/sentry/admin/users/list.html
+++ b/sentry/templates/sentry/admin/users/list.html
@@ -41,17 +41,19 @@
                     <tr>
                         <td>
                             <a href="{% url sentry-admin-edit-user user.pk %}">
-                                {% if user.first_name %}
-                                    {{ user.first_name }} ({{ user.email }})
-                                {% else %}
-                                    {{ user.email }}
-                                {% endif %}
+                                {{ user.username }}
                             </a>
                             {% if not user.is_active %}
                                 <em>inactive account</em>
                             {% endif %}
                             <br/>
-                            <small>{{ user.username }}</small>
+                            <small>
+                                {% if user.first_name %}
+                                    {{ user.first_name }} ({{ user.email }})
+                                {% else %}
+                                    {{ user.email }}
+                                {% endif %}
+                            </small>
                         </td>
                         <td style="text-align:center; vertical-align:middle;">
                             {{ user.date_joined|date }}
diff --git a/sentry/templates/sentry/groups/details.html b/sentry/templates/sentry/groups/details.html
index ca1547c55d..8e49cfd7e4 100644
--- a/sentry/templates/sentry/groups/details.html
+++ b/sentry/templates/sentry/groups/details.html
@@ -42,7 +42,7 @@
 {% block main %}
     {% handle_before_events request group %}
     <ul class="events" id="event_list">
-        <li class="event level-{{ group.level }}{% if group.status == 0 %} resolved{% endif %}" id="group_{{ group.pk|safe }}" data-count="{{ group.times_seen }}" data-score="{{ group.sort_value }}">
+        <li class="event level-{{ group.level }}{% if group.status == 1 %} resolved{% endif %}" id="group_{{ group.pk|safe }}" data-count="{{ group.times_seen }}" data-score="{{ group.sort_value }}">
             <div class="count"><span>{{ group.times_seen|small_count }}</span></div>
             <div class="details">
                 <h3>
diff --git a/sentry/templates/sentry/partial/_group.html b/sentry/templates/sentry/partial/_group.html
index 62462b661c..2a6f4c738e 100644
--- a/sentry/templates/sentry/partial/_group.html
+++ b/sentry/templates/sentry/partial/_group.html
@@ -1,6 +1,6 @@
 {% load sentry_helpers %}
 
-<li class="event level-{{ group.level }}{% if group.status == 0 %} resolved{% endif %}" id="group_{{ group.pk|safe }}" data-count="{{ group.times_seen }}" data-score="{{ group.sort_value }}">
+<li class="event level-{{ group.level }}{% if group.status == 1 %} resolved{% endif %}" id="group_{{ group.pk|safe }}" data-count="{{ group.times_seen }}" data-score="{{ group.sort_value }}">
     <div class="count"><span>{{ group.times_seen|small_count }}</span></div>
     <div class="details">
         <h3>
diff --git a/setup.py b/setup.py
index 6794528c96..e7ddfc998c 100755
--- a/setup.py
+++ b/setup.py
@@ -45,7 +45,7 @@ install_requires = [
 
 setup(
     name='sentry',
-    version='2.4.0',
+    version='2.4.1',
     author='David Cramer',
     author_email='dcramer@gmail.com',
     url='http://github.com/dcramer/sentry',
