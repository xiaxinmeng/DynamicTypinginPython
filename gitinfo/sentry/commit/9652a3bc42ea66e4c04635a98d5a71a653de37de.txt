commit 9652a3bc42ea66e4c04635a98d5a71a653de37de
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Sun Mar 6 13:56:14 2016 -0800

    Add additional mimetypes for serving all of our static files
    
    We use lots of types that don't generally exist by default on systems,
    so we help jumpstart the mimetypes list by adding the known types we
    have.

diff --git a/src/sentry/runner/settings.py b/src/sentry/runner/settings.py
index 961fce59aa..3db46a55ca 100644
--- a/src/sentry/runner/settings.py
+++ b/src/sentry/runner/settings.py
@@ -280,6 +280,20 @@ def configure(ctx, py, yaml, skip_backend_validation=False):
     import warnings
     warnings.filterwarnings('default', '', Warning, r'^sentry')
 
+    # Add in additional mimetypes that are useful for our static files
+    # which aren't common in default system registries
+    import mimetypes
+    for type, ext in (
+        ('application/json', 'map'),
+        ('application/font-woff', 'woff'),
+        ('application/font-woff2', 'woff2'),
+        ('application/vnd.ms-fontobject', 'eot'),
+        ('application/x-font-ttf', 'ttf'),
+        ('application/x-font-ttf', 'ttc'),
+        ('font/opentype', 'otf'),
+    ):
+        mimetypes.add_type(type, '.' + ext)
+
     from .importer import install
 
     if yaml is None:
