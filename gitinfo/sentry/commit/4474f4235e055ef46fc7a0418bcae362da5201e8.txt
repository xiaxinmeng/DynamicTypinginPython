commit 4474f4235e055ef46fc7a0418bcae362da5201e8
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Jul 15 14:59:06 2013 -0700

    Add column headers for data values

diff --git a/src/sentry/templates/sentry/partial/data_values.html b/src/sentry/templates/sentry/partial/data_values.html
index d49ee0c1e7..284eeda155 100644
--- a/src/sentry/templates/sentry/partial/data_values.html
+++ b/src/sentry/templates/sentry/partial/data_values.html
@@ -6,10 +6,22 @@
         <colgroup>
             <col style="width:100px;">
         </colgroup>
+        <thead>
+            <tr>
+                <th>
+                    {% if is_list %}
+                        {% trans "Index" %}
+                    {% else %}
+                        {% trans "Key" %}
+                    {% endif %}
+                </th>
+                <th>{% trans "Value" %}</th>
+            </tr>
+        </thead>
         <tbody>
             {% for k, v in value_before_expand %}
                 <tr>
-                    <th>{{ k }}</th>
+                    <td>{{ k }}</td>
                     <td class="values">
                         {% render_values v %}
                     </td>
@@ -17,7 +29,7 @@
             {% endfor %}
             {% for k, v in value_after_expand %}
                 <tr class="js-expand" style="display:none">
-                    <th>{{ k }}</th>
+                    <td>{{ k }}</td>
                     <td class="values">
                         {% render_values v %}
                     </td>
diff --git a/src/sentry/templatetags/sentry_helpers.py b/src/sentry/templatetags/sentry_helpers.py
index f4488b5970..2459cdfcbf 100644
--- a/src/sentry/templatetags/sentry_helpers.py
+++ b/src/sentry/templatetags/sentry_helpers.py
@@ -390,13 +390,14 @@ def github_button(user, repo):
 @register.inclusion_tag('sentry/partial/data_values.html')
 def render_values(value, threshold=5, collapse_to=3):
     if isinstance(value, (list, tuple)):
-        value = dict(("[%04d]" % (idx,), val) for idx, val in enumerate(value))
-        is_dict = True
+        value = dict(enumerate(value))
+        is_list, is_dict = True, True
     else:
-        is_dict = isinstance(value, dict)
+        is_list, is_dict = False, isinstance(value, dict)
 
     context = {
         'is_dict': is_dict,
+        'is_list': is_list,
         'threshold': threshold,
         'collapse_to': collapse_to,
     }
