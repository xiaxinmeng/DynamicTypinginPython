commit 4a9204d5a5b22fcc35db47be4dae0e6da6df7ee3
Author: David Cramer <dcramer@gmail.com>
Date:   Sun Feb 12 20:05:13 2017 -0800

    Explicit user list test for commit hook

diff --git a/src/sentry/receivers/releases.py b/src/sentry/receivers/releases.py
index 7e225ab31d..54b7df2162 100644
--- a/src/sentry/receivers/releases.py
+++ b/src/sentry/receivers/releases.py
@@ -77,18 +77,18 @@ def resolved_in_commit(instance, created, **kwargs):
                 user_list = list(instance.author.find_users())
             else:
                 user_list = ()
-
-            for user in user_list:
-                Activity.objects.create(
-                    project_id=group.project_id,
-                    group=group,
-                    type=Activity.SET_RESOLVED_IN_COMMIT,
-                    ident=instance.id,
-                    user=user,
-                    data={
-                        'commit': instance.id,
-                    }
-                )
+            if user_list:
+                for user in user_list:
+                    Activity.objects.create(
+                        project_id=group.project_id,
+                        group=group,
+                        type=Activity.SET_RESOLVED_IN_COMMIT,
+                        ident=instance.id,
+                        user=user,
+                        data={
+                            'commit': instance.id,
+                        }
+                    )
             else:
                 Activity.objects.create(
                     project_id=group.project_id,
