commit cb439b1797288dd3fd5a04969a98712b17d751ca
Author: David Cramer <dcramer@gmail.com>
Date:   Fri May 6 11:04:46 2016 -0500

    Add auth and data to event_received

diff --git a/src/sentry/web/api.py b/src/sentry/web/api.py
index ebcc6d78b1..22a861b068 100644
--- a/src/sentry/web/api.py
+++ b/src/sentry/web/api.py
@@ -284,7 +284,12 @@ class StoreView(APIView):
         metrics.incr('events.total')
 
         remote_addr = request.META['REMOTE_ADDR']
-        event_received.send_robust(ip=remote_addr, sender=type(self))
+        event_received.send_robust(
+            ip=remote_addr,
+            auth=auth,
+            data=data,
+            sender=type(self),
+        )
 
         if not is_valid_ip(remote_addr, project):
             app.tsdb.incr_multi([
