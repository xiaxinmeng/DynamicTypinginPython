commit 53ee2ec917da5c627d5077ac517361462b961159
Author: David Cramer <dcramer@gmail.com>
Date:   Sat Mar 7 11:41:47 2015 -0800

    Update rails integration docs to match current

diff --git a/src/sentry/templates/sentry/partial/client_config/rails3.html b/src/sentry/templates/sentry/partial/client_config/rails3.html
index def680e85c..4c5a0b38e3 100644
--- a/src/sentry/templates/sentry/partial/client_config/rails3.html
+++ b/src/sentry/templates/sentry/partial/client_config/rails3.html
@@ -3,13 +3,17 @@
 {% load i18n %}
 
 {% block inner %}
-	<p>{% trans "Add a <code>config/initializers/raven.rb</code> containing:" %}</p>
+  <p>{% trans "Add a <code>config/initializers/raven.rb</code> containing:" %}</p>
 
-	<pre>require 'raven'
+  <pre>require 'raven'
 
 Raven.configure do |config|
   config.dsn = '{% if dsn %}{{ dsn }}{% else %}<strong class="dsn">SENTRY_DSN</strong>{% endif %}'
 end</pre>
 
-	<p>{% trans "That's it! Raven automatically installs an error handling hook to pipe all uncaught exceptions to Sentry." %}</p>
+  <p>You'll want to ensure you've disabled anything that would prevent errors from being propagated to the Raven::Rack middleware, like ActionDispatch::ShowExceptions:</p>
+
+  <pre>config.action_dispatch.show_exceptions = false # this is the default setting in production</pre>
+
+  <p>{% trans "That's it! Raven automatically installs an error handling hook to pipe all uncaught exceptions to Sentry." %}</p>
 {% endblock %}
diff --git a/src/sentry/templates/sentry/partial/client_config/rails4.html b/src/sentry/templates/sentry/partial/client_config/rails4.html
index 8a8259b101..a34f1cbd5b 100644
--- a/src/sentry/templates/sentry/partial/client_config/rails4.html
+++ b/src/sentry/templates/sentry/partial/client_config/rails4.html
@@ -1,15 +1 @@
-{% extends "sentry/partial/client_config/ruby_base.html" %}
-
-{% load i18n %}
-
-{% block inner %}
-  <p>{% trans "Add a <code>config/initializers/raven.rb</code> containing:" %}</p>
-
-  <pre>require 'raven'
-
-Raven.configure do |config|
-  config.dsn = '{% if dsn %}{{ dsn }}{% else %}<strong class="dsn">SENTRY_DSN</strong>{% endif %}'
-end</pre>
-
-  <p>{% trans "That's it! Raven automatically installs an error handling hook to pipe all uncaught exceptions to Sentry." %}</p>
-{% endblock %}
+{% extends "sentry/partial/client_config/rails3.html" %}
