commit af51a242f7c75335e0b6616779e859aafe5d1904
Author: ckj <chriskjennings@gmail.com>
Date:   Sat Mar 28 01:34:41 2015 -0400

    Spruce up stacktrace block

diff --git a/src/sentry/static/sentry/app/views/groupDetails/interfaces/stacktraceContent.jsx b/src/sentry/static/sentry/app/views/groupDetails/interfaces/stacktraceContent.jsx
index a8788350e7..4f3bfd9bd4 100644
--- a/src/sentry/static/sentry/app/views/groupDetails/interfaces/stacktraceContent.jsx
+++ b/src/sentry/static/sentry/app/views/groupDetails/interfaces/stacktraceContent.jsx
@@ -74,7 +74,7 @@ var Frame = React.createClass({
         title.push(<a href={data.absPath} className="icon-share" key="share" />);
       }
       if (this.isDefined(data.function)) {
-        title.push(<span key="in"> in </span>);
+        title.push(<span className="in-at" key="in"> in </span>);
       }
     }
 
@@ -83,7 +83,7 @@ var Frame = React.createClass({
     }
 
     if (this.isDefined(data.lineNo)) {
-      title.push(<span key="at"> at line </span>);
+      title.push(<span className="in-at" key="at"> at line </span>);
       if (this.isDefined(data.colNo)) {
         title.push(<code key="line">{data.lineNo}:{data.colNo}</code>);
       } else {
diff --git a/src/sentry/static/sentry/less/group-detail.less b/src/sentry/static/sentry/less/group-detail.less
index d2b0f8cc91..0c78d2bafd 100644
--- a/src/sentry/static/sentry/less/group-detail.less
+++ b/src/sentry/static/sentry/less/group-detail.less
@@ -413,6 +413,10 @@
     }
   }
 
+  .in-at {
+    opacity: .7;
+  }
+
   code {
     padding: 0;
     background: #fff;
@@ -506,6 +510,7 @@ ol.context {
     background-color: #f6f7f8;
     min-height: 22px;
     list-style-type: none;
+    border-radius: 3px;
 
     pre {
       color: @grayDark;
@@ -513,11 +518,11 @@ ol.context {
   }
 
   > li:first-child {
-      border-radius: 4px 4px 0 0;
+      border-radius: 3px 3px 0 0;
   }
 
-  > li:last-child {
-      border-radius: 0 0 4px 4px;
+  > li:last-of-type {
+      border-radius: 0 0 3px 3px;
   }
 
   li.closed {
@@ -533,6 +538,7 @@ ol.context {
       background-color: @blue;
       color: #fff;
       list-style-type: inherit;
+      border-radius: 0;
     }
   }
 }
diff --git a/src/sentry/static/sentry/less/type.less b/src/sentry/static/sentry/less/type.less
index 95a88782d7..25f7894997 100644
--- a/src/sentry/static/sentry/less/type.less
+++ b/src/sentry/static/sentry/less/type.less
@@ -111,7 +111,7 @@ pre, code {
   border: 0;
   background-color: #f6f7f8;
   color: @gray;
-  border-radius: 2px;
+  border-radius: 3px;
 
   span {
     display: inline-block;
