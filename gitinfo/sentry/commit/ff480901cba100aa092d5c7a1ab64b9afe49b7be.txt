commit ff480901cba100aa092d5c7a1ab64b9afe49b7be
Author: Burak Yigit Kaya <byk@sentry.io>
Date:   Thu Jan 9 05:53:46 2020 +0300

    ui(footer): Add build information to footer version (#16331)
    
    This is #16303 done properly.

diff --git a/docker/Dockerfile b/docker/Dockerfile
index 0933c90f19..a40a10d115 100644
--- a/docker/Dockerfile
+++ b/docker/Dockerfile
@@ -184,5 +184,6 @@ ENTRYPOINT exec $SENTRY_CONF/docker-entrypoint.sh $0 $@
 CMD ["run", "web"]
 
 ARG SOURCE_COMMIT
+ENV SENTRY_BUILD=${SOURCE_COMMIT:-unknown}
 LABEL org.opencontainers.image.revision=$SOURCE_COMMIT
 LABEL org.opencontainers.image.licenses="https://github.com/getsentry/sentry/blob/${SOURCE_COMMIT:-master}/LICENSE"
diff --git a/src/sentry/static/sentry/app/components/footer.tsx b/src/sentry/static/sentry/app/components/footer.tsx
index 7c4dd2b805..b4da7aed53 100644
--- a/src/sentry/static/sentry/app/components/footer.tsx
+++ b/src/sentry/static/sentry/app/components/footer.tsx
@@ -37,6 +37,12 @@ const Footer = () => {
               fixed: 'Acceptance Test',
               value: config.version.current,
             })}
+            {' ('}
+            {getDynamicText({
+              fixed: 'test',
+              value: config.version.build,
+            })}
+            {')'}
           </div>
         )}
         <a href="/" tabIndex={-1} className="icon-sentry-logo" />
