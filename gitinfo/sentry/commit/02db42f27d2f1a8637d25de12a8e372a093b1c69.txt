commit 02db42f27d2f1a8637d25de12a8e372a093b1c69
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Jan 3 02:56:41 2012 -0800

    Added views and processors to status page

diff --git a/sentry/templates/sentry/status.html b/sentry/templates/sentry/status.html
index d943bdad81..3db7604c5b 100644
--- a/sentry/templates/sentry/status.html
+++ b/sentry/templates/sentry/status.html
@@ -68,19 +68,63 @@
                 <div class="page-header">
                     <h2>Extensions</h2>
                 </div>
-                <table class="vars bordered-table zebra-striped">
-                    <colgroup>
-                        <col style="width:220px">
-                    </colgroup>
-                    <tbody>
-                        {% for key, value in extensions %}
-                            <tr>
-                                <th>{{ key }}</th>
-                                <td>{{ value }}</td>
-                            </tr>
-                        {% endfor %}
-                    </tbody>
-                </table>
+                {% if extensions %}
+                    <table class="vars bordered-table zebra-striped">
+                        <colgroup>
+                            <col style="width:220px">
+                        </colgroup>
+                        <tbody>
+                            {% for key, value in extensions %}
+                                <tr>
+                                    <th>{{ key }}</th>
+                                    <td>{{ value }}</td>
+                                </tr>
+                            {% endfor %}
+                        </tbody>
+                    </table>
+                {% else %}
+                    <p>No extensions registered.</p>
+                {% endif %}
+                <div class="page-header">
+                    <h2>Views</h2>
+                </div>
+                {% if views %}
+                    <table class="vars bordered-table zebra-striped">
+                        <colgroup>
+                            <col style="width:220px">
+                        </colgroup>
+                        <tbody>
+                            {% for key, value in views %}
+                                <tr>
+                                    <th>{{ key }}</th>
+                                    <td>{{ value }}</td>
+                                </tr>
+                            {% endfor %}
+                        </tbody>
+                    </table>
+                {% else %}
+                    <p>No views registered.</p>
+                {% endif %}
+                <div class="page-header">
+                    <h2>Processors</h2>
+                </div>
+                {% if processors %}
+                    <table class="vars bordered-table zebra-striped">
+                        <colgroup>
+                            <col style="width:220px">
+                        </colgroup>
+                        <tbody>
+                            {% for key, value in processors %}
+                                <tr>
+                                    <th>{{ key }}</th>
+                                    <td>{{ value }}</td>
+                                </tr>
+                            {% endfor %}
+                        </tbody>
+                    </table>
+                {% else %}
+                    <p>No event processors registered.</p>
+                {% endif %}
                 <div class="page-header">
                     <h2>Modules</h2>
                 </div>
diff --git a/sentry/web/frontend/generic.py b/sentry/web/frontend/generic.py
index dea173a068..5478f60e66 100644
--- a/sentry/web/frontend/generic.py
+++ b/sentry/web/frontend/generic.py
@@ -34,6 +34,9 @@ def dashboard(request):
 
 @login_required
 def status(request):
+    from sentry.views import View
+    from sentry.processors import Processor
+
     config = []
     for k in sorted(dir(settings)):
         if k == 'KEY':
@@ -62,6 +65,8 @@ def status(request):
         'python_version': sys.version,
         'modules': sorted([(p.project_name, p.version) for p in pkg_resources.working_set]),
         'extensions': [(cls.title, cls.__module__.rsplit('.', 1)[0]) for cls in GroupActionProvider.plugins.itervalues()],
+        'views': [(x.__class__.__name__, x.__module__) for x in View.handlers.all()],
+        'processors': [(x.__class__.__name__, x.__module__) for x in Processor.handlers.all()],
         'pending_tasks': pending_tasks,
         'worker_status': worker_status,
     }, request)
