commit 14bd39ed54f4a712a65347c1559126f4fc560495
Author: David Cramer <dcramer@gmail.com>
Date:   Sat May 30 14:15:45 2015 +0200

    Ensure pageLinks are updated as part of polling

diff --git a/src/sentry/static/sentry/app/utils/cursorPoller.jsx b/src/sentry/static/sentry/app/utils/cursorPoller.jsx
index 03b67ab75c..0b53ecdda7 100644
--- a/src/sentry/static/sentry/app/utils/cursorPoller.jsx
+++ b/src/sentry/static/sentry/app/utils/cursorPoller.jsx
@@ -56,7 +56,7 @@ class CursorPoller {
 
         this._pollingEndpoint = links.previous.href;
 
-        this.options.success(data);
+        this.options.success(data, jqXHR.getResponseHeader('Link'));
       },
       complete: () => {
         if (this._active) {
diff --git a/src/sentry/static/sentry/app/views/stream.jsx b/src/sentry/static/sentry/app/views/stream.jsx
index afed0b0ea8..31d6815220 100644
--- a/src/sentry/static/sentry/app/views/stream.jsx
+++ b/src/sentry/static/sentry/app/views/stream.jsx
@@ -136,8 +136,13 @@ var Stream = React.createClass({
     }
   },
 
-  onRealtimePoll(data) {
+  onRealtimePoll(data, links) {
     this._streamManager.unshift(data);
+    if (!utils.valueIsEqual(this.state.pageLinks, links, true)) {
+      this.setState({
+        pageLinks: links,
+      });
+    }
   },
 
   onGroupChange() {
