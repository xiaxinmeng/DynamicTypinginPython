commit 32f65aa73b2f0e9193463d2fb9edd22d55b9c111
Author: John Manhart <jmanhart@sentry.io>
Date:   Fri Aug 9 15:56:03 2019 -0500

    Bug fix on the on the pill icon margin (#14337)
    
    Bug fix for the margin issue on the pill icon.

diff --git a/src/sentry/static/sentry/app/components/pill.jsx b/src/sentry/static/sentry/app/components/pill.jsx
index b2ecc68fe7..edb01cc0ff 100644
--- a/src/sentry/static/sentry/app/components/pill.jsx
+++ b/src/sentry/static/sentry/app/components/pill.jsx
@@ -64,7 +64,6 @@ const PillName = styled('span')`
 `;
 
 const PillValue = styled(PillName)`
-
   /* .true - good values */
   ${p =>
     p.type === 'true' &&
@@ -89,6 +88,7 @@ const PillValue = styled(PillName)`
   p.theme.button.borderRadius} 0;
   font-family: ${p => p.theme.text.familyMono};
   max-width: 100%;
+
   > a {
     max-width: 100%;
     text-overflow: ellipsis;
@@ -97,6 +97,8 @@ const PillValue = styled(PillName)`
     display: inline-block;
     vertical-align: text-bottom;
   }
+
+  .pill-icon,
   .external-icon {
     display: inline;
     margin: 0 0 0 ${space(1)};
diff --git a/src/sentry/static/sentry/app/components/pills.jsx b/src/sentry/static/sentry/app/components/pills.jsx
index 8dc833b885..a6ceb371b6 100644
--- a/src/sentry/static/sentry/app/components/pills.jsx
+++ b/src/sentry/static/sentry/app/components/pills.jsx
@@ -3,7 +3,7 @@ import styled from 'react-emotion';
 const Pills = styled('div')`
   display: flex;
   flex-wrap: wrap;
-  font-size: ${p => p.theme.fontSizeSmall};
+  font-size: 13px;
 `;
 
 export default Pills;
