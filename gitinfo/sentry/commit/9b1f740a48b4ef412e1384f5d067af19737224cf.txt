commit 9b1f740a48b4ef412e1384f5d067af19737224cf
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Tue Mar 18 02:33:25 2014 -0700

    Don't assume _sentry_sudo exists

diff --git a/src/sentry/middleware/sudo.py b/src/sentry/middleware/sudo.py
index 03fbc55477..902d6780ee 100644
--- a/src/sentry/middleware/sudo.py
+++ b/src/sentry/middleware/sudo.py
@@ -14,7 +14,7 @@ class SudoMiddleware(object):
         request.is_sudo = lambda: has_sudo_privileges(request)
 
     def process_response(self, request, response):
-        is_sudo = request._sentry_sudo
+        is_sudo = getattr(request, '_sentry_sudo', None)
 
         if is_sudo is None:
             return response
