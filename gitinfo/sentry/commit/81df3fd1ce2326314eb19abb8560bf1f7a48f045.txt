commit 81df3fd1ce2326314eb19abb8560bf1f7a48f045
Author: Lyn Nagara <lyn.nagara@gmail.com>
Date:   Wed May 29 18:07:58 2019 -0700

    test(events-v2): Fix dynamic values in Percy (#13463)

diff --git a/src/sentry/static/sentry/app/views/organizationEventsV2/events.jsx b/src/sentry/static/sentry/app/views/organizationEventsV2/events.jsx
index 091846ea33..25f0925cce 100644
--- a/src/sentry/static/sentry/app/views/organizationEventsV2/events.jsx
+++ b/src/sentry/static/sentry/app/views/organizationEventsV2/events.jsx
@@ -9,6 +9,7 @@ import AsyncComponent from 'app/components/asyncComponent';
 import Pagination from 'app/components/pagination';
 import {Panel} from 'app/components/panels';
 import EventsChart from 'app/views/organizationEvents/eventsChart';
+import getDynamicText from 'app/utils/getDynamicText';
 
 import {getParams} from 'app/views/organizationEvents/utils/getParams';
 
@@ -70,14 +71,19 @@ class Events extends AsyncComponent {
     return (
       <React.Fragment>
         <Panel>
-          <EventsChart
-            router={router}
-            query={query}
-            organization={organization}
-            onZoom={this.handleZoom}
-            showLegend
-            yAxisOptions={CHART_AXIS_OPTIONS}
-          />
+          {getDynamicText({
+            value: (
+              <EventsChart
+                router={router}
+                query={query}
+                organization={organization}
+                onZoom={this.handleZoom}
+                showLegend
+                yAxisOptions={CHART_AXIS_OPTIONS}
+              />
+            ),
+            fixed: 'events chart',
+          })}
         </Panel>
         <StyledSearchBar
           organization={organization}
