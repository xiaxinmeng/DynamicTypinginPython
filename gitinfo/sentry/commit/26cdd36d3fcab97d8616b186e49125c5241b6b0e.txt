commit 26cdd36d3fcab97d8616b186e49125c5241b6b0e
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Thu Dec 3 19:46:30 2015 -0800

    Declare flags like a sane person

diff --git a/src/sentry/options/manager.py b/src/sentry/options/manager.py
index 48ff96495f..bcc7ddef07 100644
--- a/src/sentry/options/manager.py
+++ b/src/sentry/options/manager.py
@@ -20,14 +20,14 @@ class UnknownOption(KeyError):
     pass
 
 
-DEFAULT_FLAGS = 0b000
+DEFAULT_FLAGS = 1 << 0
 # Value can't be changed at runtime
-FLAG_IMMUTABLE = 0b001
+FLAG_IMMUTABLE = 1 << 1
 # Don't check/set in the datastore. Option only exists from file.
-FLAG_NOSTORE = 0b010
+FLAG_NOSTORE = 1 << 2
 # Values that should only exist in datastore, and shouldn't exist in
 # config files.
-FLAG_STOREONLY = 0b100
+FLAG_STOREONLY = 1 << 3
 
 # How long will a cache key exist in local memory before being evicted
 DEFAULT_KEY_TTL = 10
