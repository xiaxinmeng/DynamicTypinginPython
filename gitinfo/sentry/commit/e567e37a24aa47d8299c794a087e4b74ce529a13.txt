commit e567e37a24aa47d8299c794a087e4b74ce529a13
Author: josh <josh@jrl.ninja>
Date:   Tue Feb 11 18:30:07 2020 +0000

    build: enable light build for install-sentry-dev (#16891)

diff --git a/Makefile b/Makefile
index cce7c17aaa..64a749f35e 100644
--- a/Makefile
+++ b/Makefile
@@ -77,7 +77,10 @@ install-yarn-pkgs: node-version-check
 
 install-sentry-dev:
 	@echo "--> Installing Sentry (for development)"
-	$(PIP) install -e ".[dev]"
+	# SENTRY_LIGHT_BUILD=1 disables webpacking during setup.py.
+	# Webpacked assets are only necessary for devserver (which does it lazily anyways)
+	# and acceptance tests, which webpack automatically if run.
+	SENTRY_LIGHT_BUILD=1 $(PIP) install -e ".[dev]"
 
 build-js-po: node-version-check
 	mkdir -p build
