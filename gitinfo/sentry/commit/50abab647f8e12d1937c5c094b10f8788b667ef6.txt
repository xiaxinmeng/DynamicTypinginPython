commit 50abab647f8e12d1937c5c094b10f8788b667ef6
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Aug 27 17:53:36 2015 -0700

    Add basic logging for event entry rendering errors

diff --git a/.eslintrc b/.eslintrc
index 4308c84fb1..109e9bd220 100644
--- a/.eslintrc
+++ b/.eslintrc
@@ -12,7 +12,8 @@
   "globals": {
     "require": false,
     "expect": false,
-    "sinon": false
+    "sinon": false,
+    "Raven": true
   },
   "plugins": [
     "react"
diff --git a/src/sentry/static/sentry/app/components/eventEntries.jsx b/src/sentry/static/sentry/app/components/eventEntries.jsx
index ebacb2084d..832cf9949a 100644
--- a/src/sentry/static/sentry/app/components/eventEntries.jsx
+++ b/src/sentry/static/sentry/app/components/eventEntries.jsx
@@ -2,6 +2,7 @@ import React from "react";
 
 import ClippedBox from "./clippedBox";
 import ContextData from "./contextData";
+import {logException} from "../utils/logging";
 import EventDataSection from "./eventDataSection";
 import EventTags from "./eventTags";
 import PropTypes from "../proptypes";
@@ -233,7 +234,7 @@ var EventEntries = React.createClass({
             isShare={isShare} />
         );
       } catch (ex) {
-        // TODO(dcramer): this should log to Sentry
+        logException(ex);
         return (
           <EventDataSection
               group={group}
diff --git a/src/sentry/static/sentry/app/utils/logging.jsx b/src/sentry/static/sentry/app/utils/logging.jsx
new file mode 100644
index 0000000000..ae6678777b
--- /dev/null
+++ b/src/sentry/static/sentry/app/utils/logging.jsx
@@ -0,0 +1,7 @@
+export function logException(ex, context) {
+  Raven.captureException(ex, {
+    extra: context
+  });
+  /*eslint no-console:0*/
+  console.error(ex);
+}
diff --git a/webpack.config.js b/webpack.config.js
index 796e64e4e6..4f8add4814 100644
--- a/webpack.config.js
+++ b/webpack.config.js
@@ -54,7 +54,8 @@ var config = {
       $: 'jquery',
       jQuery: 'jquery',
       "window.jQuery": "jquery",
-      "root.jQuery": "jquery"
+      "root.jQuery": "jquery",
+      Raven: "raven-js"
     })
   ],
   resolve: {
