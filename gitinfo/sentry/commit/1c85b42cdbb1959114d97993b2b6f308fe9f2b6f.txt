commit 1c85b42cdbb1959114d97993b2b6f308fe9f2b6f
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Jul 1 11:43:43 2015 -0700

    Dont use cached version of interfaces in EventManager

diff --git a/src/sentry/event_manager.py b/src/sentry/event_manager.py
index d18c6ccbfd..6c63c18624 100644
--- a/src/sentry/event_manager.py
+++ b/src/sentry/event_manager.py
@@ -62,7 +62,7 @@ def md5_from_hash(hash_bits):
 
 
 def get_hashes_for_event(event):
-    interfaces = event.interfaces
+    interfaces = event.get_interfaces()
     for interface in interfaces.itervalues():
         result = interface.compute_hashes(event.platform)
         if not result:
diff --git a/src/sentry/models/event.py b/src/sentry/models/event.py
index aac4ae08c3..0151d1b65e 100644
--- a/src/sentry/models/event.py
+++ b/src/sentry/models/event.py
@@ -135,8 +135,7 @@ class Event(Model):
 
         return None
 
-    @memoize
-    def interfaces(self):
+    def get_interfaces(self):
         result = []
         for key, data in self.data.iteritems():
             try:
@@ -152,6 +151,8 @@ class Event(Model):
 
         return OrderedDict((k, v) for k, v in sorted(result, key=lambda x: x[1].get_score(), reverse=True))
 
+    interfaces = memoize(get_interfaces)
+
     def get_tags(self, with_internal=True):
         try:
             return [
