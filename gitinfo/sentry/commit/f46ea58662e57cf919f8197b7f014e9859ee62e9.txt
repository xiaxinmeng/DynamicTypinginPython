commit f46ea58662e57cf919f8197b7f014e9859ee62e9
Author: Max Bittker <maxbittker@gmail.com>
Date:   Thu Mar 29 12:46:59 2018 -0700

    feat(eventDetails): link to release from event release tag (#7758)

diff --git a/src/sentry/static/sentry/app/components/events/eventTags.jsx b/src/sentry/static/sentry/app/components/events/eventTags.jsx
index 91ba524246..af5aee50a9 100644
--- a/src/sentry/static/sentry/app/components/events/eventTags.jsx
+++ b/src/sentry/static/sentry/app/components/events/eventTags.jsx
@@ -10,6 +10,9 @@ import {isUrl, deviceNameMapper} from '../../utils';
 import {t} from '../../locale';
 import Pills from '../pills';
 import Pill from '../pill';
+import VersionHoverCard from '../versionHoverCard';
+import InlineSvg from '../inlineSvg';
+import TextLink from '../textLink';
 
 class EventTags extends React.Component {
   static propTypes = {
@@ -49,6 +52,23 @@ class EventTags extends React.Component {
                     <em className="icon-open" />
                   </a>
                 )}
+                {tag.key == 'release' && (
+                  <VersionHoverCard
+                    version={tag.value}
+                    orgId={orgId}
+                    projectId={projectId}
+                  >
+                    <TextLink
+                      style={{
+                        color: '#625471',
+                        paddingLeft: '4px',
+                      }}
+                      to={`/${orgId}/${projectId}/releases/${tag.value}/`}
+                    >
+                      <InlineSvg src="icon-circle-info" size="14px" />
+                    </TextLink>
+                  </VersionHoverCard>
+                )}
               </Pill>
             );
           })}
