commit d26ee786e77e8c4799347666f7cb836383021648
Author: Mark Story <mark@sentry.io>
Date:   Mon Jun 22 09:53:51 2020 -0400

    fix(ui) Fix stacking of option selectors (#19487)
    
    When not open the higher z-index places these buttons above the search
    filter dropdown which looks bad.

diff --git a/src/sentry/static/sentry/app/components/charts/optionSelector.tsx b/src/sentry/static/sentry/app/components/charts/optionSelector.tsx
index 4d83ba4e8c..a8bca10a51 100644
--- a/src/sentry/static/sentry/app/components/charts/optionSelector.tsx
+++ b/src/sentry/static/sentry/app/components/charts/optionSelector.tsx
@@ -74,7 +74,7 @@ const StyledDropdownButton = styled(DropdownButton)`
   padding: ${space(1)} ${space(2)};
   font-weight: normal;
   color: ${p => p.theme.gray600};
-  z-index: ${p => p.theme.zIndex.dropdownAutocomplete.actor};
+  z-index: ${p => (p.isOpen ? p.theme.zIndex.dropdownAutocomplete.actor : 'auto')};
 
   &:hover,
   &:focus,
