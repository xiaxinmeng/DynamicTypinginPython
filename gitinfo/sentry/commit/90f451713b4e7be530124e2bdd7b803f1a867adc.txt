commit 90f451713b4e7be530124e2bdd7b803f1a867adc
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Jan 14 14:28:02 2013 -0800

    Correctly handle existing team choice (fixes GH-720)

diff --git a/src/sentry/web/forms/projects.py b/src/sentry/web/forms/projects.py
index dccc560c02..b8a68640db 100644
--- a/src/sentry/web/forms/projects.py
+++ b/src/sentry/web/forms/projects.py
@@ -121,10 +121,13 @@ class EditProjectForm(forms.ModelForm):
 
     def clean_team(self):
         value = self.cleaned_data.get('team')
-        if not value:
+        if not value or value is -1:
             return
 
-        return self.team_list[int(value)]
+        if value == self.instance.team.id:
+            return self.instance.team
+
+        return self.team_list[value]
 
 
 class EditProjectAdminForm(EditProjectForm):
