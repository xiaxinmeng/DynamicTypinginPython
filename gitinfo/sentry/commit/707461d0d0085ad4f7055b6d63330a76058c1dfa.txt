commit 707461d0d0085ad4f7055b6d63330a76058c1dfa
Author: David Cramer <dcramer@gmail.com>
Date:   Thu May 1 15:24:51 2014 -0300

    Notes for each flag

diff --git a/src/sentry/models/projectkey.py b/src/sentry/models/projectkey.py
index 2843264e5a..31080596e6 100644
--- a/src/sentry/models/projectkey.py
+++ b/src/sentry/models/projectkey.py
@@ -25,7 +25,10 @@ class ProjectKey(Model):
     secret_key = models.CharField(max_length=32, unique=True, null=True)
     user = models.ForeignKey(settings.AUTH_USER_MODEL, null=True)
     roles = BitField(flags=(
+        # access to post events to the store endpoint
         'store',
+
+        # read/write access to rest API
         'api',
     ), default=['store'])
 
