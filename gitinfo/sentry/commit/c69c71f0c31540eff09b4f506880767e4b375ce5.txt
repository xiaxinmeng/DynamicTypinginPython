commit c69c71f0c31540eff09b4f506880767e4b375ce5
Author: David Cramer <dcramer@gmail.com>
Date:   Sun Dec 6 12:40:35 2015 -0800

    Add indicators for resolved, snoozed, bookmarked

diff --git a/src/sentry/static/sentry/app/components/compactIssue.jsx b/src/sentry/static/sentry/app/components/compactIssue.jsx
index 22d48e86d3..e6a4a590d6 100644
--- a/src/sentry/static/sentry/app/components/compactIssue.jsx
+++ b/src/sentry/static/sentry/app/components/compactIssue.jsx
@@ -161,8 +161,8 @@ const CompactIssue = React.createClass({
           <span className="error-level truncate" title={issue.level}></span>
           <h3 className="truncate">
             <Link to={`/${orgId}/${projectId}/issues/${id}/`}>
-              <span className="icon icon-soundoff hidden"></span>
-              <span className="icon icon-bookmark hidden"></span>
+              <span className="icon icon-soundoff" />
+              <span className="icon icon-bookmark" />
               {issue.title}
             </Link>
           </h3>
@@ -172,7 +172,7 @@ const CompactIssue = React.createClass({
                 <Link to={`/${orgId}/${projectId}/`}>{issue.project.name}</Link>
               </li>
               <li className="hidden">
-                <span className="icon icon-clock"></span>
+                <span className="icon icon-clock" />
                 <TimeSince date={issue.lastSeen} />
                 &nbsp;&mdash;&nbsp;
                 <TimeSince date={issue.firstSeen} suffix="old" />
@@ -180,7 +180,7 @@ const CompactIssue = React.createClass({
               {issue.numComments !== 0 &&
                 <li>
                   <Link to={`/${orgId}/${projectId}/issues/${id}/activity/`} className="comments">
-                    <span className="icon icon-comments"></span>
+                    <span className="icon icon-comments" />
                     <span className="tag-count">{issue.numComments}</span>
                   </Link>
                 </li>
diff --git a/src/sentry/static/sentry/app/components/issueList.jsx b/src/sentry/static/sentry/app/components/issueList.jsx
index 85932fdbbd..22d2a120bb 100644
--- a/src/sentry/static/sentry/app/components/issueList.jsx
+++ b/src/sentry/static/sentry/app/components/issueList.jsx
@@ -95,7 +95,7 @@ const IssueList = React.createClass({
       );
     }
     else
-      body = this.renderEmpty();
+      body = (this.props.renderEmpty || this.renderEmpty)();
 
     return body;
   },
diff --git a/src/sentry/static/sentry/less/stream.less b/src/sentry/static/sentry/less/stream.less
index 408710037c..7fd785e262 100644
--- a/src/sentry/static/sentry/less/stream.less
+++ b/src/sentry/static/sentry/less/stream.less
@@ -544,11 +544,63 @@
     padding: 12px 0 6px 20px;
     border-bottom: 1px solid lighten(@trim, 5);
 
+    .icon-bookmark, .icon-soundoff {
+      padding-right: 5px;
+      font-size: 12px;
+      position: relative;
+      .transition(all .2s ease-in-out);
+      width: auto;
+    }
+
+    .icon-bookmark {
+      top: 1px;
+      display: none;
+      color: @purple;
+      overflow: hidden;
+    }
+
+    .icon-soundoff {
+      display: none;
+      top: 1px;
+      color: @red;
+      width: auto;
+      overflow: hidden;
+      padding-left: 1px;
+    }
+
+    &.isBookmarked {
+      .icon-bookmark {
+        display: inline-block;
+      }
+    }
+
+    &.isMuted {
+      .icon-soundoff {
+        display: inline-block;
+      }
+    }
+
+    &.selected {
+      background: lighten(@blue, 20);
+      box-shadow: inset 0 1px 0 rgba(52, 60, 69, 0.08);
+
+      .event-details:after {
+        background: @blue
+      }
+    }
+
+    &.isResolved {
+      h3 {
+        text-decoration: line-through;
+        color: @gray;
+      }
+    }
+
     .error-level {
       .square(13px);
       border-radius: 50%;
       position: absolute;
-      top: 1px;
+      top: 2px;
       left: -8px;
     }
 
