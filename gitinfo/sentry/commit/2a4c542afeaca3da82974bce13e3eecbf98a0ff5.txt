commit 2a4c542afeaca3da82974bce13e3eecbf98a0ff5
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Wed Nov 11 07:52:53 2015 -0800

    Add robots.txt

diff --git a/src/sentry/web/api.py b/src/sentry/web/api.py
index f505bb2136..5e19ac4858 100644
--- a/src/sentry/web/api.py
+++ b/src/sentry/web/api.py
@@ -449,6 +449,11 @@ class CspReportView(StoreView):
         return HttpResponse(status=201)
 
 
+@cache_control(max_age=3600, public=True)
+def robots_txt(request):
+    return HttpResponse("User-agent: *\nDisallow: /\n", content_type='text/plain')
+
+
 @cache_control(max_age=3600, public=True)
 def crossdomain_xml_index(request):
     response = render_to_response('sentry/crossdomain_index.xml')
diff --git a/src/sentry/web/urls.py b/src/sentry/web/urls.py
index 6b51071695..fa16afd20d 100644
--- a/src/sentry/web/urls.py
+++ b/src/sentry/web/urls.py
@@ -338,6 +338,9 @@ urlpatterns += patterns(
     url(r'^$', HomeView.as_view(),
         name='sentry'),
 
+    url(r'^robots\.txt$', api.robots_txt,
+        name='sentry-api-robots-txt'),
+
     # crossdomain.xml
     url(r'^crossdomain\.xml$', api.crossdomain_xml_index,
         name='sentry-api-crossdomain-xml-index'),
diff --git a/tests/sentry/web/api/tests.py b/tests/sentry/web/api/tests.py
index 4119d34d47..97d9130ee2 100644
--- a/tests/sentry/web/api/tests.py
+++ b/tests/sentry/web/api/tests.py
@@ -195,3 +195,14 @@ class CrossDomainXmlIndexTest(TestCase):
         self.assertEquals(resp['Content-Type'], 'application/xml')
         self.assertTemplateUsed(resp, 'sentry/crossdomain_index.xml')
         assert '<site-control permitted-cross-domain-policies="all" />' in resp.content
+
+
+class RobotsTxtTest(TestCase):
+    @fixture
+    def path(self):
+        return reverse('sentry-api-robots-txt')
+
+    def test_robots(self):
+        resp = self.client.get(self.path)
+        assert resp.status_code == 200
+        assert resp['Content-Type'] == 'text/plain'
