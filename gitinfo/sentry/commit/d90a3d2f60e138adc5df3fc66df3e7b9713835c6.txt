commit d90a3d2f60e138adc5df3fc66df3e7b9713835c6
Author: ckj <chriskjennings@gmail.com>
Date:   Fri Jun 24 15:43:26 2016 -0700

    Fix lettars

diff --git a/src/sentry/static/sentry/app/components/events/contextSummary.jsx b/src/sentry/static/sentry/app/components/events/contextSummary.jsx
index c8c57c202e..b0edc70c61 100644
--- a/src/sentry/static/sentry/app/components/events/contextSummary.jsx
+++ b/src/sentry/static/sentry/app/components/events/contextSummary.jsx
@@ -13,7 +13,7 @@ const NoSummary = React.createClass({
   render() {
     return (
       <div className="context-item">
-        <span className="icon" />
+        <span className="context-item-icon" />
         <h3>{this.props.title}</h3>
       </div>
     );
@@ -37,7 +37,7 @@ const GenericSummary = React.createClass({
 
     return (
       <div className={`context-item ${className}`}>
-        <span className="icon" />
+        <span className="context-item-icon" />
         <h3>{data.name}</h3>
         <p><strong>{t('Version:')}</strong> {data.version || t('Unknown Version')}</p>
       </div>
@@ -63,7 +63,7 @@ const UserSummary = React.createClass({
 
     return (
       <div className="context-item user">
-        <Avatar user={user} size={48} className="icon" />
+        <Avatar user={user} size={48} className="context-item-icon" />
         <h3>{userTitle}</h3>
         {user.id && user.id !== userTitle ?
           <p><strong>{t('ID:')}</strong> {user.id}</p>
diff --git a/src/sentry/static/sentry/less/group-detail.less b/src/sentry/static/sentry/less/group-detail.less
index fdaa440d44..a3b03d8593 100644
--- a/src/sentry/static/sentry/less/group-detail.less
+++ b/src/sentry/static/sentry/less/group-detail.less
@@ -711,7 +711,7 @@
     padding-left: 78px;
     position: relative;
 
-    .icon, .avatar {
+    .context-item-icon {
       position: absolute;
       left: 15px;
       top: 15px;
@@ -738,22 +738,22 @@
       }
     }
 
-    &.ipad .icon {
+    &.ipad .context-item-icon {
       background-image: url(../images/icons/context/ipad.svg);
     }
 
     &.ipod, &.iphone {
-      .icon {
+      .context-item-icon {
         background-image: url(../images/icons/context/iphone.svg);
       }
     }
 
-    &.ios .icon {
+    &.ios .context-item-icon {
       background-image: url(../images/icons/context/ios.svg);
       top: 13px;
     }
 
-    &.android .icon {
+    &.android .context-item-icon {
       background-image: url(../images/icons/context/android.svg);
       top: 14px;
     }
