commit 42f8db2a672ccdab91a6c8d9c00c07dfdb3cbb0a
Author: Armin Ronacher <armin.ronacher@active-4.com>
Date:   Thu Feb 25 16:26:56 2016 +0100

    Install locally with dsym

diff --git a/Makefile b/Makefile
index eb3a02fb39..e478d4e03e 100644
--- a/Makefile
+++ b/Makefile
@@ -6,7 +6,7 @@ install-python:
 	pip install "setuptools>=0.9.8"
 	# order matters here, base package must install first
 	pip install -e .
-	pip install "file://`pwd`#egg=sentry[dev]"
+	pip install "file://`pwd`#egg=sentry[dev,dsym]"
 
 install-npm:
 	@echo "--> Installing Node dependencies"
