commit 30b6596162bb80e5688f1606c21c4897947a30ba
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Dec 18 10:33:02 2014 -0800

    Always show team

diff --git a/src/sentry/templates/sentry/projects/manage.html b/src/sentry/templates/sentry/projects/manage.html
index c8a9e999f8..a37f02119a 100644
--- a/src/sentry/templates/sentry/projects/manage.html
+++ b/src/sentry/templates/sentry/projects/manage.html
@@ -26,9 +26,7 @@
                 {{ form.name|as_crispy_field }}
                 {{ form.slug|as_crispy_field }}
                 {{ form.platform|as_crispy_field }}
-                {% if form.team %}
-                    {{ form.team|as_cripys_field %}}
-                {% endif %}
+                {{ form.team|as_crispy_field %}}
             </fieldset>
             <fieldset id="events">
                 <div><legend>{% trans "Event Settings" %}</legend></div>
diff --git a/src/sentry/web/frontend/project_settings.py b/src/sentry/web/frontend/project_settings.py
index 8688443fab..37c618ceff 100644
--- a/src/sentry/web/frontend/project_settings.py
+++ b/src/sentry/web/frontend/project_settings.py
@@ -91,11 +91,8 @@ class EditProjectForm(forms.ModelForm):
 
         if not can_set_public_projects(request.user):
             del self.fields['public']
-        if len(team_list) == 1 and instance.team == team_list[0]:
-            del self.fields['team']
-        else:
-            self.fields['team'].choices = self.get_team_choices(team_list, instance.team)
-            self.fields['team'].widget.choices = self.fields['team'].choices
+        self.fields['team'].choices = self.get_team_choices(team_list, instance.team)
+        self.fields['team'].widget.choices = self.fields['team'].choices
 
     def get_team_label(self, team):
         return '%s (%s)' % (team.name, team.slug)
