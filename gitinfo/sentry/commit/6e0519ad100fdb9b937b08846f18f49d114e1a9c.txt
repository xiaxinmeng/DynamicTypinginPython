commit 6e0519ad100fdb9b937b08846f18f49d114e1a9c
Author: Alberto Leal <mail4alberto@gmail.com>
Date:   Tue Feb 25 15:19:05 2020 -0500

    fix(discover2): Pass yAxis explicitly to the query card's graph (#17277)

diff --git a/src/sentry/static/sentry/app/views/eventsV2/miniGraph.tsx b/src/sentry/static/sentry/app/views/eventsV2/miniGraph.tsx
index a06da4d99a..a4936a7c77 100644
--- a/src/sentry/static/sentry/app/views/eventsV2/miniGraph.tsx
+++ b/src/sentry/static/sentry/app/views/eventsV2/miniGraph.tsx
@@ -54,6 +54,7 @@ class MiniGraph extends React.Component<Props> {
       period,
       project: eventView.project,
       environment: eventView.environment,
+      yAxis: eventView.getYAxis(),
     };
   }
 
@@ -67,6 +68,7 @@ class MiniGraph extends React.Component<Props> {
       organization,
       project,
       environment,
+      yAxis,
     } = this.getRefreshProps(this.props);
 
     return (
@@ -81,6 +83,7 @@ class MiniGraph extends React.Component<Props> {
         project={project as number[]}
         environment={environment as string[]}
         includePrevious={false}
+        yAxis={yAxis}
       >
         {({loading, timeseriesData, errored}) => {
           if (errored) {
