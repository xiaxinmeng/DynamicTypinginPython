commit 0972399c4584454d198acb8b5a00738577cb0d05
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Sep 10 20:42:31 2012 -0700

    Add plugin meta box on configuration pages

diff --git a/src/sentry.less b/src/sentry.less
index a751d40eb1..fe2277e0c6 100644
--- a/src/sentry.less
+++ b/src/sentry.less
@@ -891,3 +891,10 @@ legend {
 .icon-white {
   background-image: url(../images/glyphicons-halflings-white.png);
 }
+
+
+.plugin-meta {
+  .description {
+    p { font-size: 13px; }
+  }
+}
\ No newline at end of file
diff --git a/src/sentry/plugins/base.py b/src/sentry/plugins/base.py
index 72602c224d..e30b8032e5 100644
--- a/src/sentry/plugins/base.py
+++ b/src/sentry/plugins/base.py
@@ -284,6 +284,19 @@ class IPlugin(local):
         """
         return self.description
 
+    def get_resource_links(self):
+        """
+        Returns a list of tuples pointing to various resources for this plugin.
+
+        >>> def get_resource_links(self):
+        >>>     return [
+        >>>         ('Documentation', 'http://sentry.readthedocs.org'),
+        >>>         ('Bug Tracker', 'https://github.com/getsentry/sentry/issues'),
+        >>>         ('Source', 'https://github.com/getsentry/sentry'),
+        >>>     ]
+        """
+        return []
+
     def get_view_response(self, request, group):
         from sentry.permissions import can_admin_group
 
diff --git a/src/sentry/static/sentry/styles/global.css b/src/sentry/static/sentry/styles/global.css
index a18745797d..b7ae613ecf 100644
--- a/src/sentry/static/sentry/styles/global.css
+++ b/src/sentry/static/sentry/styles/global.css
@@ -4735,3 +4735,6 @@ legend {
 .icon-white {
   background-image: url(../images/glyphicons-halflings-white.png);
 }
+.plugin-meta .description p {
+  font-size: 13px;
+}
diff --git a/src/sentry/static/sentry/styles/global.min.css b/src/sentry/static/sentry/styles/global.min.css
index 63a5ecae53..a94cf5c3ed 100644
--- a/src/sentry/static/sentry/styles/global.min.css
+++ b/src/sentry/static/sentry/styles/global.min.css
@@ -779,3 +779,4 @@ legend{margin-bottom:18px;}
 .alert-block>ul{margin-bottom:9px;}
 [class*="icon-"]{background-image:url(../images/glyphicons-halflings.png);}
 .icon-white{background-image:url(../images/glyphicons-halflings-white.png);}
+.plugin-meta .description p{font-size:13px;}
diff --git a/src/sentry/templates/sentry/plugins/project_configuration.html b/src/sentry/templates/sentry/plugins/project_configuration.html
index 9851dcc8cf..ef7309e9ba 100644
--- a/src/sentry/templates/sentry/plugins/project_configuration.html
+++ b/src/sentry/templates/sentry/plugins/project_configuration.html
@@ -1,14 +1,48 @@
 {% load i18n %}
+{% load sentry_helpers %}
 
-{% if plugin_description %}
-    {{ plugin_description|linebreaks }}
-{% endif %}
+<div class="row-fluid">
+    <div class="span8">
+        {% if request.GET.success %}
+            <div class="alert alert-success">
+                {% trans "Changes to your project were saved successfully." %}
+                <a class="close" data-dismiss="alert" href="javascript:void(0)">&times;</a>
+            </div>
+        {% endif %}
 
-{% if request.GET.success %}
-    <div class="alert alert-success">
-        {% trans "Changes to your project were saved successfully." %}
-        <a class="close" data-dismiss="alert" href="javascript:void(0)">&times;</a>
+        {% include "sentry/partial/_form.html" %}
     </div>
-{% endif %}
-
-{% include "sentry/partial/_form.html" %}
\ No newline at end of file
+    <div class="span4">
+        <div class="well plugin-meta">
+            <h6>Plugin Information</h6>
+            <dl class="flat">
+                <dt>Name:</dt>
+                <dd>{{ plugin.get_title }}</dd>
+                <dt>Author</dt>
+                <dd>{{ plugin.author }}</dd>
+                {% if plugin.author_url %}
+                    <dt>URL</dt>
+                    <dd><a href="{{ plugin.author_url }}">{{ plugin.author_url|trim_schema }}</a></dd>
+                {% endif %}
+                <dt>Version</dt>
+                <dd>{{ plugin.version }}</dd>
+            </dl>
+            {% if plugin_description %}
+                <h6>Description</h6>
+                <div class="description">
+                    {{ plugin_description|linebreaks }}
+                </div>
+            {% endif %}
+            {% with plugin.get_resource_links as resource_links %}
+                {% if resource_links %}
+                    <ul class="nav nav-list" style="padding:0;">
+                        <li class="nav-header">Resources</li>
+                        {% for label, link in resource_links %}
+                            <li><a href="{{ link }}">{{ label }}</a></li>
+                        {% endfor %}
+                    </ul>
+                {% endif %}
+            {% endwith %}
+        </div>
+    </div>
+</div>
\ No newline at end of file
diff --git a/src/sentry/templatetags/sentry_helpers.py b/src/sentry/templatetags/sentry_helpers.py
index 1ab9fc580c..266c9b97b6 100644
--- a/src/sentry/templatetags/sentry_helpers.py
+++ b/src/sentry/templatetags/sentry_helpers.py
@@ -328,3 +328,8 @@ def gravatar_url(context, email, sizevar=None, defaultvar='mm'):
         gravatar_url += "?" + urllib.urlencode(properties)
 
     return gravatar_url
+
+
+@register.filter
+def trim_schema(value):
+    return value.split('//', 1)[-1]
diff --git a/src/sentry/web/helpers.py b/src/sentry/web/helpers.py
index 3701cbafea..0f12c7591a 100644
--- a/src/sentry/web/helpers.py
+++ b/src/sentry/web/helpers.py
@@ -185,5 +185,6 @@ def plugin_config(plugin, project, request):
     return ('display', mark_safe(render_to_string(template, {
             'form': form,
             'request': request,
+            'plugin': plugin,
             'plugin_description': plugin.get_description() or '',
         }, context_instance=RequestContext(request))))
