commit 3f7d95ec398a83a2c87ea5dbccc1f7b9435f1125
Author: David Cramer <dcramer@gmail.com>
Date:   Fri Feb 8 12:23:17 2013 -0800

    Improve client guides

diff --git a/src/sentry/templates/sentry/get_started.html b/src/sentry/templates/sentry/get_started.html
index f4c83947d5..4c1e222d2c 100644
--- a/src/sentry/templates/sentry/get_started.html
+++ b/src/sentry/templates/sentry/get_started.html
@@ -1,6 +1,7 @@
 {% extends "sentry/layout.html" %}
 
 {% load i18n %}
+{% load sentry_helpers %}
 
 {% block title %}{{ project.name }} | {{ block.super }}{% endblock %}
 
@@ -18,8 +19,6 @@
     		<h2>{% trans "Getting Started" %}</h2>
     	</div>
 
-		<p>{% trans "To start sending events to Sentry select a platform:" %}</p>
-
-		{% include "sentry/partial/_client_config.html" %}
-	</seciton>
+		{% client_help request.user project %}
+	</section>
 {% endblock %}
diff --git a/src/sentry/templates/sentry/partial/_client_config.html b/src/sentry/templates/sentry/partial/_client_config.html
index 6c5cde4782..1f2cc23021 100644
--- a/src/sentry/templates/sentry/partial/_client_config.html
+++ b/src/sentry/templates/sentry/partial/_client_config.html
@@ -38,6 +38,19 @@
     display: block;
 }
 </style>
+{% if key %}
+    <p>{% trans "To start sending events, configure your client with the following DSN value:" %}</p>
+
+    <pre class="clippy">{{ dsn }}</pre>
+
+    <p>{% blocktrans with 'https://github.com/getsentry/raven-js' as link %}If this is a client that only needs public access (like <a href="{{ link }}">raven-js</a>), you can use the following public DSN:{% endblocktrans %}</p>
+
+    <pre class="clippy">{{ dsn_public }}</pre>
+{% else %}
+    {% url sentry-project-keys project.slug as link %}
+    <p>{% blocktrans %}There is no API key assigned to your account. Generate a new key or select one from the <a href="{{ link }}">API Keys page</a>.{% endblocktrans %}</p>
+{% endif %}
+
 <div class="page-header">
     <h3>{% trans "Platforms" %}</h3>
 </div>
@@ -118,3 +131,5 @@
         </a>
     </li>
 </ul>
+
+<p>{% blocktrans with 'http://sentry.readthedocs.org' as link %}For more information on developing clients and the Sentry API, see the <a href="{{ link }}">documentation</a>.{% endblocktrans %}</p>
diff --git a/src/sentry/templates/sentry/projects/client_help.html b/src/sentry/templates/sentry/projects/client_help.html
index 3700238591..6f1aa6ab7e 100644
--- a/src/sentry/templates/sentry/projects/client_help.html
+++ b/src/sentry/templates/sentry/projects/client_help.html
@@ -1,40 +1,20 @@
 {% extends "sentry/projects/manage.html" %}
 
 {% load i18n %}
+{% load sentry_helpers %}
 
 {% block title %}{% trans "Client Configuration" %} | {{ block.super }}{% endblock %}
 
 {% block breadcrumb %}
     {{ block.super }}
     <li class="divider"></li>
-    <li><a href="{% url sentry-project-client-help project.slug %}">{% trans "Client Configuration" %}</a></li>
+    <li><a href="{% url sentry-project-client-help project.team.slug project.slug %}">{% trans "Client Configuration" %}</a></li>
 {% endblock %}
 
 {% block inner %}
     <div class="page-header">
         <h2>{% trans "Client Configuration" %}</h2>
     </div>
-
-    {% if key %}
-        <p>{% trans "To start sending events, configure your client with the following DSN value:" %}</p>
-
-        <pre class="clippy">{{ dsn }}</pre>
-
-        <p>{% blocktrans with 'https://github.com/getsentry/raven-js' as link %}If this is a client that only needs public access (like <a href="{{ link }}">raven-js</a>), you can use the following public DSN:{% endblocktrans %}</p>
-
-        <pre class="clippy">{{ dsn_public }}</pre>
-    {% else %}
-        {% url sentry-project-keys project.slug as link %}
-        <p>{% blocktrans %}There is no API key assigned to your account. Generate a new key or select one from the <a href="{{ link }}">API Keys page</a>.{% endblocktrans %}</p>
-    {% endif %}
-
-    <div class="page-header">
-        <h3>{% trans "Platform Instructions" %}</h3>
-    </div>
-
-    <p>{% trans "Choose your platform for quick setup instructions:" %}</p>
-    {% include "sentry/partial/_client_config.html" %}
-
-    <p>{% blocktrans with 'http://sentry.readthedocs.org' as link %}For more information on developing clients and the Sentry API, see the <a href="{{ link }}">documentation</a>.{% endblocktrans %}</p>
+    {% client_help request.user project %}
 {% endblock %}
 
diff --git a/src/sentry/templatetags/sentry_helpers.py b/src/sentry/templatetags/sentry_helpers.py
index 604513b015..604f2e4dfc 100644
--- a/src/sentry/templatetags/sentry_helpers.py
+++ b/src/sentry/templatetags/sentry_helpers.py
@@ -381,3 +381,12 @@ def render_values(value, threshold=5, collapse_to=3):
         context['value'] = value
 
     return context
+
+
+@register.inclusion_tag('sentry/partial/_client_config.html')
+def client_help(user, project):
+    from sentry.web.frontend.docs import get_key_context
+
+    context = get_key_context(user, project)
+    context['project'] = project
+    return context
