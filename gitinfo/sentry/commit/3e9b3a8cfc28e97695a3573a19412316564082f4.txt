commit 3e9b3a8cfc28e97695a3573a19412316564082f4
Author: Markus Unterwaditzer <markus@unterwaditzer.net>
Date:   Mon Sep 23 16:20:22 2019 +0200

    fix(relay/config): Send subscope as str (#14804)

diff --git a/src/sentry/quotas/redis.py b/src/sentry/quotas/redis.py
index c0740410ac..3a0856b722 100644
--- a/src/sentry/quotas/redis.py
+++ b/src/sentry/quotas/redis.py
@@ -113,8 +113,8 @@ class BasicRedisQuota(object):
     def to_json(self):
         return prune_empty_keys(
             {
-                "prefix": self.prefix,
-                "subscope": self.subscope,
+                "prefix": six.text_type(self.prefix),
+                "subscope": six.text_type(self.subscope) if self.subscope is not None else None,
                 "limit": self.limit,
                 "window": self.window,
                 "reasonCode": self.reason_code,
