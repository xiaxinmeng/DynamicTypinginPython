commit 8872a0907411c5e0de5248b6173636c8a166bc8e
Author: Evan Purkhiser <evanpurkhiser@gmail.com>
Date:   Mon Mar 16 10:58:27 2020 -0700

    chore(ts): Convert Qrcode (#17678)

diff --git a/src/sentry/static/sentry/app/components/qrcode.jsx b/src/sentry/static/sentry/app/components/qrcode.jsx
deleted file mode 100644
index 472193aa17..0000000000
--- a/src/sentry/static/sentry/app/components/qrcode.jsx
+++ /dev/null
@@ -1,47 +0,0 @@
-import React from 'react';
-import PropTypes from 'prop-types';
-import styled from '@emotion/styled';
-
-const Cell = styled('td')`
-  height: 6px;
-  width: 6px;
-  padding: 0;
-`;
-const BlackCell = styled(Cell)`
-  background-color: black;
-`;
-const WhiteCell = styled(Cell)`
-  background-color: white;
-`;
-
-const Table = styled('table')`
-  margin: 0;
-`;
-
-class Qrcode extends React.Component {
-  static propTypes = {
-    /**
-     * Takes a multidimensional array representing a 2-bit, 2d matrix
-     */
-    code: PropTypes.arrayOf(PropTypes.arrayOf(PropTypes.number)),
-  };
-
-  render() {
-    const {code} = this.props;
-    return (
-      <Table>
-        <tbody>
-          {code.map((row, i) => (
-            <tr key={i}>
-              {row.map((cell, j) =>
-                cell ? <BlackCell key={j} /> : <WhiteCell key={j} />
-              )}
-            </tr>
-          ))}
-        </tbody>
-      </Table>
-    );
-  }
-}
-
-export default Qrcode;
diff --git a/src/sentry/static/sentry/app/components/qrcode.tsx b/src/sentry/static/sentry/app/components/qrcode.tsx
new file mode 100644
index 0000000000..7ddcceb1d9
--- /dev/null
+++ b/src/sentry/static/sentry/app/components/qrcode.tsx
@@ -0,0 +1,38 @@
+import React from 'react';
+import styled from '@emotion/styled';
+
+type Props = {
+  code: (1 | 0)[][];
+};
+
+const Qrcode = ({code}: Props) => (
+  <Table>
+    <tbody>
+      {code.map((row, i) => (
+        <tr key={i}>
+          {row.map((cell, j) => (cell ? <BlackCell key={j} /> : <WhiteCell key={j} />))}
+        </tr>
+      ))}
+    </tbody>
+  </Table>
+);
+
+const Cell = styled('td')`
+  height: 6px;
+  width: 6px;
+  padding: 0;
+`;
+
+const BlackCell = styled(Cell)`
+  background-color: black;
+`;
+
+const WhiteCell = styled(Cell)`
+  background-color: white;
+`;
+
+const Table = styled('table')`
+  margin: 0;
+`;
+
+export default Qrcode;
