commit 83831d97b82bbfd655e6ef2a71d5a8a09e2ddbde
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Oct 11 19:17:03 2011 -0700

    Change the defaults to use sqlite

diff --git a/sentry/scripts/runner.py b/sentry/scripts/runner.py
index 5ecc3d5f71..75485f78da 100644
--- a/sentry/scripts/runner.py
+++ b/sentry/scripts/runner.py
@@ -27,9 +27,10 @@ ROOT = os.path.dirname(__file__)
 DATABASES = {
     'default': {
         # You can swap out the engine for MySQL easily by changing this value
-        # to ``django.db.backends.mysql``
-        'ENGINE': 'django.db.backends.postgresql_psycopg2',
-        'NAME': 'sentry',
+        # to ``django.db.backends.mysql`` or to PostgreSQL with
+        # ``django.db.backends.postgresql_psycopg2``
+        'ENGINE': 'django.db.backends.sqlite3',
+        'NAME': os.path.join(ROOT, 'sentry.db'),
         'USER': 'postgres',
         'PASSWORD': '',
         'HOST': '',
