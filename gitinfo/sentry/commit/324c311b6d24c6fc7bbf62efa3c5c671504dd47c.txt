commit 324c311b6d24c6fc7bbf62efa3c5c671504dd47c
Author: Jess MacQueen <macqueen@users.noreply.github.com>
Date:   Thu Apr 27 14:00:28 2017 -0700

    link to github commit page from activity feed (#5291)
    
    * link to github commit page from activity feed
    
    * make inline commit look nice

diff --git a/src/sentry/static/sentry/app/components/activity/item.jsx b/src/sentry/static/sentry/app/components/activity/item.jsx
index 8d76efdc63..364507c11f 100644
--- a/src/sentry/static/sentry/app/components/activity/item.jsx
+++ b/src/sentry/static/sentry/app/components/activity/item.jsx
@@ -1,6 +1,7 @@
 import marked from 'marked';
 import React from 'react';
 
+import {CommitLink} from '../../views/releases/releaseCommits';
 import Duration from '../../components/duration';
 import Avatar from '../../components/avatar';
 import {Link} from 'react-router';
@@ -93,7 +94,13 @@ const ActivityItem = React.createClass({
       case 'set_resolved_in_commit':
         return tct('[author] marked [issue] as fixed in [version]', {
           author: author,
-          version: data.commit.id.substr(0, 12),
+          version: (
+            <CommitLink
+              inline={true}
+              commitId={data.commit.id}
+              repository={data.commit.repository}
+            />
+          ),
           issue: issueLink
         });
       case 'set_unresolved':
diff --git a/src/sentry/static/sentry/app/views/groupActivity/index.jsx b/src/sentry/static/sentry/app/views/groupActivity/index.jsx
index c6b8125b76..0b03d2899f 100644
--- a/src/sentry/static/sentry/app/views/groupActivity/index.jsx
+++ b/src/sentry/static/sentry/app/views/groupActivity/index.jsx
@@ -3,6 +3,7 @@ import React from 'react';
 import ApiMixin from '../../mixins/apiMixin';
 import GroupState from '../../mixins/groupState';
 
+import {CommitLink} from '../../views/releases/releaseCommits';
 import Duration from '../../components/duration';
 import Avatar from '../../components/avatar';
 import TimeSince from '../../components/timeSince';
@@ -50,7 +51,13 @@ const GroupActivity = React.createClass({
       case 'set_resolved_in_commit':
         return t('%(author)s marked this issue as fixed in %(version)s', {
           author: author,
-          version: data.commit.id.substr(0, 12)
+          version: (
+            <CommitLink
+              inline={true}
+              commitId={data.commit.id}
+              repository={data.commit.repository}
+            />
+          )
         });
       case 'set_unresolved':
         return t('%s marked this issue as unresolved', author);
diff --git a/src/sentry/static/sentry/app/views/releases/releaseCommits.jsx b/src/sentry/static/sentry/app/views/releases/releaseCommits.jsx
index 9016c6f4ce..31f5920a83 100644
--- a/src/sentry/static/sentry/app/views/releases/releaseCommits.jsx
+++ b/src/sentry/static/sentry/app/views/releases/releaseCommits.jsx
@@ -13,7 +13,8 @@ import {t} from '../../locale';
 const CommitLink = React.createClass({
   propTypes: {
     commitId: React.PropTypes.string,
-    repository: React.PropTypes.object
+    repository: React.PropTypes.object,
+    inline: React.PropTypes.bool
   },
 
   getCommitUrl() {
@@ -29,10 +30,13 @@ const CommitLink = React.createClass({
     let shortId = this.props.commitId.slice(0, 7);
 
     return commitUrl
-      ? <a className="btn btn-default btn-sm" href={commitUrl} target="_blank">
+      ? <a
+          className={this.props.inline ? 'inline-commit' : 'btn btn-default btn-sm'}
+          href={commitUrl}
+          target="_blank">
           <span className={'icon-mark-' + this.props.repository.provider.id} />
           &nbsp;
-          {' '}
+          {this.props.inline ? '' : ' '}
           {shortId}
         </a>
       : <span>{shortId}</span>;
diff --git a/src/sentry/static/sentry/less/group-detail.less b/src/sentry/static/sentry/less/group-detail.less
index 472af13925..ade54274e5 100644
--- a/src/sentry/static/sentry/less/group-detail.less
+++ b/src/sentry/static/sentry/less/group-detail.less
@@ -777,6 +777,20 @@
   }
 }
 
+/**
+* Commit Activity
+* ============================================================================
+*/
+.activity-item {
+  .inline-commit {
+    color: @gray-light;
+    padding-left: 3px;
+    &:hover {
+      color: @gray-dark;
+    }
+  }
+}
+
 /**
 * Event Details
 * ============================================================================
