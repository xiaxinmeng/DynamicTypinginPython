commit f2842ee589774a9e60c091aa146ae7ace5139ba1
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Jul 2 14:13:52 2015 -0700

    Handle error on latest event

diff --git a/src/sentry/api/client.py b/src/sentry/api/client.py
index 328b1e521a..6acd3fb0cd 100644
--- a/src/sentry/api/client.py
+++ b/src/sentry/api/client.py
@@ -15,6 +15,10 @@ class ApiError(Exception):
     def __unicode__(self):
         return 'status=%s body=%s' % (self.status_code, self.body)
 
+    @property
+    def status(self):
+        return self.status_code
+
 
 class ApiClient(object):
     prefix = '/api/0'
diff --git a/src/sentry/api/endpoints/group_events_latest.py b/src/sentry/api/endpoints/group_events_latest.py
index 8629e109cb..5de9d1b38b 100644
--- a/src/sentry/api/endpoints/group_events_latest.py
+++ b/src/sentry/api/endpoints/group_events_latest.py
@@ -1,5 +1,7 @@
 from __future__ import absolute_import
 
+from rest_framework.response import Response
+
 from sentry.api import client
 from sentry.api.base import DocSection
 from sentry.api.bases.group import GroupEndpoint
@@ -19,4 +21,7 @@ class GroupEventsLatestEndpoint(GroupEndpoint):
         """
         event = group.get_latest_event()
 
-        return client.get('/events/{}/'.format(event.id), request.user, request.auth)
+        try:
+            return client.get('/events/{}/'.format(event.id), request.user, request.auth)
+        except client.ApiError as e:
+            return Response(e.body, status=e.status)
