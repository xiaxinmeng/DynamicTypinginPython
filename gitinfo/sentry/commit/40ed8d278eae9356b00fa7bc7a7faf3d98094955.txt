commit 40ed8d278eae9356b00fa7bc7a7faf3d98094955
Author: David Cramer <dcramer@gmail.com>
Date:   Fri Mar 25 16:13:00 2011 -0700

    Add dependancy link for django-haystack (thanks to @carljm)

diff --git a/setup.py b/setup.py
index 0f18146958..5dedfc6ba2 100644
--- a/setup.py
+++ b/setup.py
@@ -11,7 +11,7 @@ tests_require = [
     'django',
     'django-celery',
     'south',
-    # also requires the disqus fork of haystack
+    'django-haystack',
 ]
 
 setup(
@@ -28,6 +28,9 @@ setup(
         'django-indexer==0.2.1',
         'uuid',
     ],
+    dependency_links=[
+        'https://github.com/disqus/django-haystack/tarball/master#egg=django-haystack',
+        ],
     tests_require=tests_require,
     extras_require={'test': tests_require},
     test_suite='sentry.runtests.runtests',
