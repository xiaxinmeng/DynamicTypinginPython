commit 888cff4bd2dc13489f352ed06253de968974bbea
Author: Brett Hoerner <brett@bretthoerner.com>
Date:   Thu Aug 31 11:46:17 2017 -0500

    test: add test for GroupTagKeySerializer (#6010)
    
    This would have caught SENTRY-4C0 (GH-6006).

diff --git a/tests/sentry/api/serializers/test_grouptagkey.py b/tests/sentry/api/serializers/test_grouptagkey.py
new file mode 100644
index 0000000000..932b950c3f
--- /dev/null
+++ b/tests/sentry/api/serializers/test_grouptagkey.py
@@ -0,0 +1,26 @@
+from __future__ import absolute_import
+
+import six
+
+from sentry.api.serializers import serialize
+from sentry.models import GroupTagKey, TagKey
+from sentry.testutils import TestCase
+
+
+class GroupTagKeySerializerTest(TestCase):
+    def test(self):
+        user = self.create_user()
+        project = self.create_project()
+        tagkey = TagKey.objects.create(
+            project_id=project.id,
+            key='key'
+        )
+        grouptagkey = GroupTagKey.objects.create(
+            project_id=project.id,
+            group_id=self.create_group(project=project).id,
+            key=tagkey.key
+        )
+
+        result = serialize(grouptagkey, user)
+        assert result['id'] == six.text_type(grouptagkey.id)
+        assert result['key'] == 'key'
