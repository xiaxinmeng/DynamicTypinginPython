commit 112ead8823f1113fb607209e82c789e7c6d043f8
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Oct 29 10:19:07 2018 -0700

    ref(permissions): Move known permissions into settings

diff --git a/src/sentry/conf/server.py b/src/sentry/conf/server.py
index 20ae626520..2a0f1927e0 100644
--- a/src/sentry/conf/server.py
+++ b/src/sentry/conf/server.py
@@ -1431,3 +1431,9 @@ JS_SDK_LOADER_DEFAULT_SDK_URL = ''
 # block domains which are generally used by spammers -- keep this configurable in case an onpremise
 # install wants to allow it
 INVALID_EMAIL_ADDRESS_PATTERN = re.compile(r'\@qq\.com$', re.I)
+
+# This is customizable for sentry.io, but generally should only be additive
+# (currently the values not used anymore so this is more for documentation purposes)
+SENTRY_USER_PERMISSIONS = (
+    'broadcasts.admin',
+)
diff --git a/src/sentry/models/userpermission.py b/src/sentry/models/userpermission.py
index 035fd4fa14..e17360315c 100644
--- a/src/sentry/models/userpermission.py
+++ b/src/sentry/models/userpermission.py
@@ -4,10 +4,6 @@ from django.db import models
 
 from sentry.db.models import Model, FlexibleForeignKey, sane_repr
 
-KNOWN_PERMISSIONS = (
-    'broadcasts.admin',
-)
-
 
 class UserPermission(Model):
     __core__ = True
