commit 6bbb0dc48fbd4e242a75b032a81ea0f1f508b1d8
Author: Dan Fuller <dfuller@sentry.io>
Date:   Wed May 8 17:25:07 2019 -0700

    refs: South fixes for Django 1.8

diff --git a/src/south/modelsinspector.py b/src/south/modelsinspector.py
index a2c5e6e820..4234c0238b 100644
--- a/src/south/modelsinspector.py
+++ b/src/south/modelsinspector.py
@@ -40,7 +40,9 @@ def convert_on_delete_handler(value):
         else:
             # This is totally dependent on the implementation of django.db.models.deletion.SET
             func_name = getattr(value, '__name__', None)
-            if func_name == 'set_on_delete':
+            if func_name == 'SET_NULL':
+                return "%s.SET_NULL" % (django_db_models_module)
+            elif func_name == 'set_on_delete':
                 # we must inspect the function closure to see what parameters were passed in
                 closure_contents = value.__closure__[0].cell_contents
                 if closure_contents is None:
diff --git a/src/south/utils/__init__.py b/src/south/utils/__init__.py
index bebce14295..ba137b8168 100644
--- a/src/south/utils/__init__.py
+++ b/src/south/utils/__init__.py
@@ -37,6 +37,7 @@ def get_attribute(item, attribute):
     """
     value = item
     for part in attribute.split("."):
+        part = 'model_name' if part == 'module_name' else part
         value = getattr(value, part)
     return value
 
