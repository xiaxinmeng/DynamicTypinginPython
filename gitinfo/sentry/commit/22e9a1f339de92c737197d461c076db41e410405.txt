commit 22e9a1f339de92c737197d461c076db41e410405
Author: David Cramer <dcramer@gmail.com>
Date:   Fri Feb 22 05:25:33 2013 -0800

    Some basic tests for Exception interface

diff --git a/tests/sentry/interfaces/exception/__init__.py b/tests/sentry/interfaces/exception/__init__.py
new file mode 100644
index 0000000000..e69de29bb2
diff --git a/tests/sentry/interfaces/exception/tests.py b/tests/sentry/interfaces/exception/tests.py
new file mode 100644
index 0000000000..3785fc5f17
--- /dev/null
+++ b/tests/sentry/interfaces/exception/tests.py
@@ -0,0 +1,42 @@
+# -*- coding: utf-8 -*-
+
+from __future__ import absolute_import
+
+from sentry.testutils import TestCase, fixture
+
+
+class ExceptionTest(TestCase):
+    @fixture
+    def interface(self):
+        from sentry.interfaces import Exception
+
+        return Exception(
+            type='ValueError',
+            value='hello world',
+            module='foo.bar',
+        )
+
+    def test_serialize_behavior(self):
+        assert self.interface.serialize() == {
+            'type': self.interface.type,
+            'value': self.interface.value,
+            'module': self.interface.module,
+        }
+
+    def test_get_hash(self):
+        assert self.interface.get_hash() == [
+            self.interface.type,
+            self.interface.value,
+        ]
+
+    def test_get_hash_without_type(self):
+        self.interface.type = None
+        assert self.interface.get_hash() == [
+            self.interface.value,
+        ]
+
+    def test_get_hash_without_value(self):
+        self.interface.value = None
+        assert self.interface.get_hash() == [
+            self.interface.type,
+        ]
