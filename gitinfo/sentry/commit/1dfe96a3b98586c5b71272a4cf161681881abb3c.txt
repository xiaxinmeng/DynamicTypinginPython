commit 1dfe96a3b98586c5b71272a4cf161681881abb3c
Author: David Cramer <dcramer@gmail.com>
Date:   Fri Oct 19 13:53:53 2012 -0700

    Force line numbers to ints

diff --git a/src/sentry/interfaces.py b/src/sentry/interfaces.py
index 82b8299b0e..990537e961 100644
--- a/src/sentry/interfaces.py
+++ b/src/sentry/interfaces.py
@@ -212,12 +212,17 @@ class Stacktrace(Interface):
             else:
                 context_vars = []
 
+            if frame.get('lineno') is not None:
+                lineno = int(frame['lineno'])
+            else:
+                lineno = None
+
             frames.append({
                 'abs_path': frame.get('abs_path'),
                 'filename': frame['filename'],
                 'function': frame.get('function'),
                 'start_lineno': start_lineno,
-                'lineno': frame.get('lineno'),
+                'lineno': lineno,
                 'context': context,
                 'vars': context_vars,
             })
@@ -449,7 +454,7 @@ class Template(Interface):
             'event': event,
             'abs_path': self.abs_path,
             'filename': self.filename,
-            'lineno': self.lineno,
+            'lineno': int(self.lineno),
             'start_lineno': context[0][0],
             'context': context,
             'template': self.get_traceback(event, context),
