commit e2e739051e32cbcb7feba1441aade39cd0de529c
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Jan 24 13:02:03 2013 -0800

    Remove null values for colno and lineno from stacktraces

diff --git a/src/sentry/interfaces.py b/src/sentry/interfaces.py
index f9e8df957e..8bf914e13e 100644
--- a/src/sentry/interfaces.py
+++ b/src/sentry/interfaces.py
@@ -295,11 +295,17 @@ class Stacktrace(Interface):
 
             # lineno should be an int
             if 'lineno' in frame:
-                frame['lineno'] = int(frame['lineno'])
+                if frame['lineno'] is None:
+                    del frame['lineno']
+                else:
+                    frame['lineno'] = int(frame['lineno'])
 
             # colno should be an int
             if 'colno' in frame:
-                frame['colno'] = int(frame['colno'])
+                if frame['colno'] is None:
+                    del frame['colno']
+                else:
+                    frame['colno'] = int(frame['colno'])
 
             # in_app should be a boolean
             if 'in_app' in frame:
