commit a584ac649e14112e92e380e2bc04a67397d8fc59
Author: adhiraj <693121+adhiraj@users.noreply.github.com>
Date:   Thu Oct 4 14:24:15 2018 -0700

    feat(analytics): Log issue page view (#10018)

diff --git a/src/sentry/static/sentry/app/views/groupDetails.jsx b/src/sentry/static/sentry/app/views/groupDetails.jsx
index 96bb0c70cb..a77078dba8 100644
--- a/src/sentry/static/sentry/app/views/groupDetails.jsx
+++ b/src/sentry/static/sentry/app/views/groupDetails.jsx
@@ -5,6 +5,7 @@ import Reflux from 'reflux';
 import {browserHistory} from 'react-router';
 import DocumentTitle from 'react-document-title';
 
+import analytics from 'app/utils/analytics';
 import ApiMixin from 'app/mixins/apiMixin';
 import GroupHeader from 'app/views/groupDetails/header';
 import GroupStore from 'app/stores/groupStore';
@@ -27,6 +28,10 @@ const GroupDetails = createReactClass({
     environment: SentryTypes.Environment,
   },
 
+  contextTypes: {
+    organization: SentryTypes.Organization,
+  },
+
   childContextTypes: {
     group: SentryTypes.Group,
     location: PropTypes.object,
@@ -61,6 +66,13 @@ const GroupDetails = createReactClass({
     this.fetchData();
   },
 
+  componentDidMount() {
+    analytics('issue_page.viewed', {
+      group_id: parseInt(this.props.params.groupId, 10),
+      org_id: parseInt(this.context.organization.id, 10),
+    });
+  },
+
   componentWillReceiveProps(nextProps) {
     if (nextProps.params.groupId !== this.props.params.groupId) {
       this.remountComponent();
