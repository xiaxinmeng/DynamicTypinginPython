commit 3ff0fb3d4a066bed82d2286722f16be429d1dcea
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Jan 26 17:03:20 2016 -0800

    Dont show message bodies when enhanced privacy is enabled (refs GH-2600)

diff --git a/src/sentry/templates/sentry/emails/error.html b/src/sentry/templates/sentry/emails/error.html
index 648bcd0fda..480b7f9e48 100644
--- a/src/sentry/templates/sentry/emails/error.html
+++ b/src/sentry/templates/sentry/emails/error.html
@@ -26,16 +26,16 @@
         {% endif %}
     </h2>
 
-    <div class="event">
-      <pre class="message">{{ event.message }}</pre>
-      <div class="event-id">ID: {{ event.event_id }}</div>
-      <div class="event-date">{{ event.datetime }} UTC</div>
-    </div>
-
     {% if enhanced_privacy %}
       <div class="notice">Details about this issue are not shown in this notification since enhanced privacy
         controls are enabled. For more details about this issue, <a href="{{ link }}">view this issue on Sentry</a>.</div>
     {% else %}
+      <div class="event">
+        <pre class="message">{{ event.message }}</pre>
+        <div class="event-id">ID: {{ event.event_id }}</div>
+        <div class="event-date">{{ event.datetime }} UTC</div>
+      </div>
+
       {% for label, html, _ in interfaces %}
       <div class="interface">
           <h3 class="title">{{ label }}</h3>
diff --git a/src/sentry/templates/sentry/emails/error.txt b/src/sentry/templates/sentry/emails/error.txt
index 3fe978a071..dd7e6c348e 100644
--- a/src/sentry/templates/sentry/emails/error.txt
+++ b/src/sentry/templates/sentry/emails/error.txt
@@ -1,5 +1,14 @@
 {% spaceless %}
 {% autoescape off %}
+{% if enhanced_privacy %}
+Details about this issue are not shown in this notification since enhanced
+privacy controls are enabled. For more details about this issue, view this
+issue on Sentry.
+Details
+-------
+
+{{ link }}
+{% else %}
 A new event has been recorded in Sentry:
 
     {{ event.message }}
@@ -8,11 +17,6 @@ Details
 -------
 
 {{ link }}
-{% if enhanced_privacy %}
-Details about this issue are not shown in this notification since enhanced
-privacy controls are enabled. For more details about this issue, view this
-issue on Sentry.
-{% else %}
 Tags
 ----
 {% for tag_key, tag_value in tags %}
