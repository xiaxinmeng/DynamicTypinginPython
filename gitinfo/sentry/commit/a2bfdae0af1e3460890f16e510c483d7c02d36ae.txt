commit a2bfdae0af1e3460890f16e510c483d7c02d36ae
Author: ckj <chriskjennings@gmail.com>
Date:   Tue Oct 11 12:09:05 2016 -0700

    Update label and slider hover color

diff --git a/src/sentry/static/sentry/app/views/organizationRateLimits/index.jsx b/src/sentry/static/sentry/app/views/organizationRateLimits/index.jsx
index da9ad006cc..fc2d43df4e 100644
--- a/src/sentry/static/sentry/app/views/organizationRateLimits/index.jsx
+++ b/src/sentry/static/sentry/app/views/organizationRateLimits/index.jsx
@@ -77,7 +77,7 @@ const RateLimitEditor = React.createClass({
         </p>
 
         <RangeField
-            label={t('Maximum amount a single project may send')}
+            label={t('Max percentage a single project may send')}
             defaultValue={savedProjectLimit}
             onChange={this.onProjectLimitChange}
             formatLabel={(value) => { return `${value}%`; }} />
diff --git a/src/sentry/static/sentry/less/includes/simple-slider.less b/src/sentry/static/sentry/less/includes/simple-slider.less
index e0feec70b3..403bfac2f1 100644
--- a/src/sentry/static/sentry/less/includes/simple-slider.less
+++ b/src/sentry/static/sentry/less/includes/simple-slider.less
@@ -13,7 +13,7 @@
   }
 
   > .dragger:hover {
-    background: #4593DE;
+    background: @purple-dark;
   }
 
   > .track {
@@ -26,7 +26,7 @@
   }
 
   > .value {
-    top: -28px;
+    top: -29px;
     position: absolute;
     right: -(@slider-width / 2);
     // width: 80px;
