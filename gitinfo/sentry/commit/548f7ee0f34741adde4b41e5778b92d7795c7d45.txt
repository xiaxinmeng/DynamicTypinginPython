commit 548f7ee0f34741adde4b41e5778b92d7795c7d45
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Sep 28 19:49:49 2015 -0700

    Dont coerce to string

diff --git a/src/sentry/models/eventuser.py b/src/sentry/models/eventuser.py
index 574b31e8eb..18d4693006 100644
--- a/src/sentry/models/eventuser.py
+++ b/src/sentry/models/eventuser.py
@@ -51,13 +51,13 @@ class EventUser(Model):
             'No identifying value found for user'
 
         if self.ident:
-            return 'id:{}'.format(self.ident)
+            return u'id:{}'.format(self.ident)
         if self.email:
-            return 'email:{}'.format(self.email)
+            return u'email:{}'.format(self.email)
         if self.username:
-            return 'username:{}'.format(self.username)
+            return u'username:{}'.format(self.username)
         if self.ip_address:
-            return 'ip:{}'.format(self.ip_address)
+            return u'ip:{}'.format(self.ip_address)
 
     def get_label(self):
         return self.email or self.username or self.ident or self.ip_address
