commit 2c8ee0d15555d1068f1105131ee0713f492826c0
Author: David Cramer <dcramer@gmail.com>
Date:   Sun Aug 4 14:47:20 2013 -0700

    Revert "Correct discover call"
    
    This reverts commit 26a2aae51f1e56260f087dcb708a950a6f2c70c8.

diff --git a/src/sentry/tasks/fetch_source.py b/src/sentry/tasks/fetch_source.py
index 38d8de6786..793889a231 100644
--- a/src/sentry/tasks/fetch_source.py
+++ b/src/sentry/tasks/fetch_source.py
@@ -70,7 +70,7 @@ def discover_sourcemap(result):
     # all keys become lowercase so they're normalized
     map_path = result.headers.get('sourcemap', result.headers.get('x-sourcemap'))
     if not map_path:
-        map_path = sourcemap.discover(result.body)
+        map_path = sourcemap.discover(result)
 
     if map_path:
         # ensure url is absolute
