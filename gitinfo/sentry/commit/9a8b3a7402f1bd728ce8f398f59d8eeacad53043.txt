commit 9a8b3a7402f1bd728ce8f398f59d8eeacad53043
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Aug 31 18:02:20 2015 -0700

    Improve tag values
    
    - Link url-like tags (refs GH-1855)
    - Remove invalid container styling

diff --git a/src/sentry/static/sentry/app/utils.jsx b/src/sentry/static/sentry/app/utils.jsx
index 795a2e6052..49bc2ecbc3 100644
--- a/src/sentry/static/sentry/app/utils.jsx
+++ b/src/sentry/static/sentry/app/utils.jsx
@@ -160,6 +160,10 @@ export default {
     return str.replace(/(?:\r\n|\r|\n)/g, '<br />');
   },
 
+  isUrl(str) {
+    return (str.indexOf('http://') !== -1) || (str.indexOf('https://') !== -1);
+  },
+
   escape(str) {
     return str.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
   },
diff --git a/src/sentry/static/sentry/app/views/groupTagValues.jsx b/src/sentry/static/sentry/app/views/groupTagValues.jsx
index 83c934660a..3772d31bba 100644
--- a/src/sentry/static/sentry/app/views/groupTagValues.jsx
+++ b/src/sentry/static/sentry/app/views/groupTagValues.jsx
@@ -8,6 +8,7 @@ import LoadingError from "../components/loadingError";
 import LoadingIndicator from "../components/loadingIndicator";
 import Pagination from "../components/pagination";
 import TimeSince from "../components/timeSince";
+import {isUrl} from "../utils";
 
 var GroupTagValues = React.createClass({
   mixins: [GroupState],
@@ -98,12 +99,16 @@ var GroupTagValues = React.createClass({
         <td>{pct}%</td>
           <td>
             <Router.Link
-                className="tag-bar"
                 to="stream"
                 params={params}
                 query={{query: tagKey.key + ':' + '"' + tagValue.value + '"'}}>
               {tagValue.name}
             </Router.Link>
+            {isUrl(tagValue.value) &&
+              <a href={tagValue.value} className="external">
+                <em className="icon-browser" />
+              </a>
+            }
           </td>
           <td>
             <TimeSince date={tagValue.lastSeen} />
diff --git a/src/sentry/static/sentry/less/shared-components.less b/src/sentry/static/sentry/less/shared-components.less
index 95cb1e1a49..3090f2e35e 100644
--- a/src/sentry/static/sentry/less/shared-components.less
+++ b/src/sentry/static/sentry/less/shared-components.less
@@ -331,6 +331,13 @@ pre.plain {
   word-break: break-all;
 }
 
+a.external {
+  color: inherit;
+  font-size: 11px;
+  padding: 0 5px;
+  color: @gray-dark;
+}
+
 table.vars {
   margin-bottom: 0;
   border: 0;
