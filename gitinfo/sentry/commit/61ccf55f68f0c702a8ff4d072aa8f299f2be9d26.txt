commit 61ccf55f68f0c702a8ff4d072aa8f299f2be9d26
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Feb 2 01:38:40 2012 -0800

    Clean up resolve actions

diff --git a/bootstrap/sentry.less b/bootstrap/sentry.less
index 4d1aa17a86..2eb48f7547 100644
--- a/bootstrap/sentry.less
+++ b/bootstrap/sentry.less
@@ -383,6 +383,9 @@ footer {
         .checked {
           color: orange;
         }
+        .disabled {
+          color: @gray;
+        }
       }
     }
 
diff --git a/sentry/static/styles/global.css b/sentry/static/styles/global.css
index 05ba74c471..9a53772818 100644
--- a/sentry/static/styles/global.css
+++ b/sentry/static/styles/global.css
@@ -2495,6 +2495,9 @@ footer a {
 .events .event .actions li .checked {
   color: orange;
 }
+.events .event .actions li .disabled {
+  color: #6d757e;
+}
 .events .event .row_link {
   top: 0;
   left: 0;
diff --git a/sentry/static/styles/global.min.css b/sentry/static/styles/global.min.css
index b7387f3073..704fad9779 100644
--- a/sentry/static/styles/global.min.css
+++ b/sentry/static/styles/global.min.css
@@ -387,6 +387,7 @@ footer a{color:#fff;font-weight:bold;}
 .events .event .message{margin-bottom:0;font-size:14px;padding-right:14px;}.events .event .message .tag{margin-left:5px;font-size:12px;float:right;-webkit-border-radius:6px;-moz-border-radius:6px;border-radius:6px;background:#fff;padding:2px 6px;color:#aec9d7;}
 .events .event .meta{position:absolute;right:40px;top:20px;}
 .events .event .actions{position:absolute;right:15px;top:20px;list-style-type:none;margin:0;padding:0;}.events .event .actions li{margin-bottom:12px;font-size:14px;line-height:18px;}.events .event .actions li .checked{color:orange;}
+.events .event .actions li .disabled{color:#6d757e;}
 .events .event .row_link{top:0;left:0;width:100%;height:100%;position:absolute;}
 .events .event.level-20 .count{background:#ffa101;}
 .events .event.level-30 .count{background:#ffa101;}
diff --git a/sentry/templates/sentry/groups/details.html b/sentry/templates/sentry/groups/details.html
index 0777520ab9..04a0c0a0f4 100644
--- a/sentry/templates/sentry/groups/details.html
+++ b/sentry/templates/sentry/groups/details.html
@@ -42,7 +42,50 @@
 {% block main %}
     {% handle_before_events request group %}
     <ul class="events" id="event_list">
-        {% include "sentry/partial/_group.html" %}
+        <li class="event level-{{ group.level }}" id="group_{{ group.pk|safe }}" data-count="{{ group.times_seen }}" data-score="{{ group.sort_value }}">
+            <div class="count"><span>{{ group.times_seen|small_count }}</span></div>
+            <div class="details">
+                <h3>
+                    <a href="{% url sentry-group group.project_id group.pk %}">
+                        {% if group.view %}
+                            {{ group.view }}
+                        {% else %}
+                            {{ group.message_top|truncatechars:100 }}
+                        {% endif %}
+                    </a>
+                </h3>
+                <p class="message" title="{{ group.message }}">
+                    <span class="tag tag-logger">{{ group.logger }}</span>
+                    {% with group.get_version as version %}
+                        {% if version %}
+                            <span class="tag tag-version">{{ version.0 }} {{ version.1 }}</span>
+                        {% endif %}
+                    {% endwith %}
+                    {% for tag in group|get_tags:request %}<span class="tag">{{ tag }}</span> {% endfor %}
+                    {{ group.error }}
+                </p>
+                <div class="meta">
+                    <span class="last-seen pretty-date" title="{{ group.last_seen|date:"c" }}Z">{{ group.last_seen|timesince }}</span>
+                    {% if group.avg_time_spent %}
+                        <span class="time-spent">{{ group.avg_time_spent|duration }}</span>
+                    {% endif %}
+                </div>
+                <ul class="actions">
+                    <li>
+                        {% if group.status == 0 %}
+                            <a href="javascript:void(0)" onclick="Sentry.stream.resolve({{ group.project_id|safe }}, {{ group.pk|safe }}, false);" class="resolve">&#10003;</a>
+                        {% else %}
+                            <a href="javascript:void(0)" class="resolve disabled">&#10003;</a>
+                        {% endif %}
+                    </li>
+                    {% if request.user.is_authenticated %}
+                        <li><a href="javascript:void(0)" onclick="Sentry.stream.bookmark({{ group.project_id|safe }}, {{ group.pk|safe }}, this);" class="bookmark{% if is_bookmarked %} checked{% endif %}">&#9829;</a></li>
+                    {% endif %}
+                </ul>
+        <!--         <a href="{% url sentry-group group.project_id group.pk %}" class="row_link"></a>
+         -->
+            </div>
+        </li>
     </ul>
 
     <section class="body">
@@ -60,11 +103,6 @@
                 <a class="dropdown-toggle" href="#" data-toggle="dropdown">{% trans "Actions" %} <span class="caret"></span></a>
                 <ul class="dropdown-menu">
                     {# We're going to move these to the resolve action location #}
-                    {% if group.status == 0 %}
-                        <li><a href="javascript:void(0)" onclick="Sentry.stream.resolve({{ group.project_id|safe }}, {{ group.pk|safe }});">{% trans "&#10003; Mark as Resolved" %}</a></li>
-                    {% else %}
-                        <li class="disabled"><a>{% trans "&#10003; Already Resolved" %}</a></li>
-                    {% endif %}
                     <li><a href="{% url sentry-group-json project_id=project.pk group_id=group.pk %}">{% trans "Raw JSON Data" %}</a></li>
                     {% for label, link, is_active in group|get_actions:request %}
                         {% if forloop.first %}
