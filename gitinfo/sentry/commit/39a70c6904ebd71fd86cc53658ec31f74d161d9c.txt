commit 39a70c6904ebd71fd86cc53658ec31f74d161d9c
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Jan 3 23:47:18 2012 -0800

    Dont send view_id if its not set

diff --git a/sentry/static/scripts/global.js b/sentry/static/scripts/global.js
index 4eec113357..1e0841b810 100644
--- a/sentry/static/scripts/global.js
+++ b/sentry/static/scripts/global.js
@@ -109,12 +109,12 @@ if (Sentry === undefined) {
     };
 
     Sentry.realtime.refresh = function(){
-        data = getQueryParams();
-        data.view_id = Sentry.realtime.options.viewId;
-        data.op = 'poll';
         if (!Sentry.realtime.status) {
             return;
         }
+        data = getQueryParams();
+        data.view_id = Sentry.realtime.options.viewId || undefined;
+        data.op = 'poll';
         $.ajax({
             url: Sentry.options.apiUrl,
             type: 'get',
