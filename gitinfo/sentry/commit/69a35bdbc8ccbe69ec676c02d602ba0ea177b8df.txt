commit 69a35bdbc8ccbe69ec676c02d602ba0ea177b8df
Author: Jess MacQueen <jessmacqueen@gmail.com>
Date:   Tue Feb 21 13:01:14 2017 -0800

    fix IntegrityError in load-mocks when creating commit author (#4932)

diff --git a/bin/load-mocks b/bin/load-mocks
index 90724a3313..a563838eec 100755
--- a/bin/load-mocks
+++ b/bin/load-mocks
@@ -448,8 +448,8 @@ def main(num_events=1, extra_events=False):
                 defaults={
                     'author': CommitAuthor.objects.get_or_create(
                         organization_id=org.id,
-                        name=user.name,
                         email=user.email,
+                        defaults={'name': user.name}
                     )[0],
                     'message': 'Ooops!\nFixes {}'.format(event5.group.qualified_short_id),
                 },
