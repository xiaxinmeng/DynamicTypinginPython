commit d379c2bceebc6e98c47143fa56919362c8898ea0
Author: Anton Ovchinnikov <anton@tonyo.info>
Date:   Tue Jan 29 17:11:59 2019 +0100

    Revert "fix: Skip too large events in workerrelay" (#11764)
    
    * Revert "fix: Skip too large events in workerrelay (#11763)"
    
    This reverts commit cec8c3c5e39c209da7985e932a5e793bc802a401.

diff --git a/src/sentry/eventstream/kafka/protocol.py b/src/sentry/eventstream/kafka/protocol.py
index c041dd53da..494967276f 100644
--- a/src/sentry/eventstream/kafka/protocol.py
+++ b/src/sentry/eventstream/kafka/protocol.py
@@ -92,11 +92,6 @@ def get_task_kwargs_for_message(value):
     can be applied to a post-processing task, or ``None`` if no task should be
     dispatched.
     """
-
-    if len(value) > 10000000:
-        logger.debug("Event payload too large: %d", len(value))
-        return None
-
     payload = json.loads(value)
 
     try:
