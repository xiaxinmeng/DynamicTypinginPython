commit 175034c252fb98c3af8b72b3af82940b634314c2
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Feb 23 13:04:51 2011 -0800

    Sentry object was not being setup correctly

diff --git a/sentry/media/scripts/global.js b/sentry/media/scripts/global.js
index 6ca2503b19..0132f23e45 100644
--- a/sentry/media/scripts/global.js
+++ b/sentry/media/scripts/global.js
@@ -277,9 +277,9 @@ var Sentry = {};
             }
             return cookieValue;
         }
-        if (!(/^http:.*/.test(settings.url) || /^https:.*/.test(settings.url))) {
+        if (!(/^http:.*/.test(settings.url) || (/^https:.*/.test(settings.url)))) {
             // Only send the token to relative URLs i.e. locally.
             xhr.setRequestHeader("X-CSRFToken", getCookie('csrftoken'));
         }
     });
-});
+}());
