commit 8019a3d23f941424e6003fee32c13a5633923663
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Sep 19 16:28:16 2016 -0700

    Dont apply validators with empty value

diff --git a/src/sentry/plugins/config.py b/src/sentry/plugins/config.py
index 5c3bba7807..a531a85f47 100644
--- a/src/sentry/plugins/config.py
+++ b/src/sentry/plugins/config.py
@@ -2,6 +2,7 @@ from __future__ import absolute_import
 
 __all__ = ['PluginConfigMixin']
 
+from sentry.exceptions import PluginError
 from sentry.utils.forms import form_to_config
 
 from .validators import DEFAULT_VALIDATORS
@@ -42,6 +43,11 @@ class PluginConfigMixin(object):
             if config['name'] != name:
                 continue
 
+            if value is None:
+                if config.get('required'):
+                    raise PluginError('Field is required')
+                return value
+
             for validator in DEFAULT_VALIDATORS.get(config['type'], ()):
                 value = validator(project=project, value=value, actor=actor)
 
