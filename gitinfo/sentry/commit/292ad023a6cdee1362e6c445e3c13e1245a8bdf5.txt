commit 292ad023a6cdee1362e6c445e3c13e1245a8bdf5
Author: James Cunningham <cunninghamjt09@gmail.com>
Date:   Fri Jul 22 11:55:37 2016 -1000

    Drop unparseable sourcemaps into debug logging. (#3774)
    
    * Drop unparseable sourcemaps into debug logging.
    
    * Add exc_info.

diff --git a/src/sentry/lang/javascript/processor.py b/src/sentry/lang/javascript/processor.py
index d8e1a08a35..833024b5e5 100644
--- a/src/sentry/lang/javascript/processor.py
+++ b/src/sentry/lang/javascript/processor.py
@@ -411,7 +411,8 @@ def fetch_sourcemap(url, project=None, release=None, allow_scraping=True):
     try:
         return sourcemap_to_index(body)
     except Exception as exc:
-        logger.warn(unicode(exc))
+        # This is in debug because the product shows an error already.
+        logger.debug(unicode(exc), exc_info=True)
         raise UnparseableSourcemap({
             'url': expose_url(url),
         })
