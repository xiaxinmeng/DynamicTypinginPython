commit f03a4aa9af893382573a470a4204c39bc72a5284
Author: Stephen Cefali <scefali@sentry.io>
Date:   Wed Jun 24 12:42:11 2020 -0700

    logging(integrations): adds warning if there is a pipeline error (#19517)

diff --git a/src/sentry/pipeline/__init__.py b/src/sentry/pipeline/__init__.py
index 8aeba1c4c5..53c651ed7a 100644
--- a/src/sentry/pipeline/__init__.py
+++ b/src/sentry/pipeline/__init__.py
@@ -226,6 +226,14 @@ class Pipeline(object):
 
     def error(self, message):
         context = {"error": message}
+        extra = {
+            "organization_id": self.organization.id if self.organization else None,
+            "provider": self.provider.key,
+            "error": message,
+        }
+        logger = self.get_logger()
+        # log error
+        logger.error("pipeline error", extra=extra)
         return render_to_response("sentry/pipeline-error.html", context, self.request)
 
     def next_step(self, step_size=1):
