commit 17dd53c9e28d4896bb5cfa4f38af9a11cc9e2c6c
Author: David Cramer <dcramer@gmail.com>
Date:   Fri Feb 27 11:31:59 2015 -0800

    Minor improvements to test configurations

diff --git a/src/sentry/web/helpers.py b/src/sentry/web/helpers.py
index 0775f915fe..abeb4a295c 100644
--- a/src/sentry/web/helpers.py
+++ b/src/sentry/web/helpers.py
@@ -168,11 +168,12 @@ def plugin_config(plugin, project, request):
             try:
                 test_results = plugin.test_configuration(project)
             except Exception as exc:
+                logging.exception('Plugin(%s) raised an error during test', plugin_key)
                 if hasattr(exc, 'read') and callable(exc.read):
                     test_results = '%s\n%s' % (exc, exc.read())
                 else:
                     test_results = exc
-            if test_results is None:
+            if not test_results:
                 test_results = 'No errors returned'
         else:
             for field, value in form.cleaned_data.iteritems():
