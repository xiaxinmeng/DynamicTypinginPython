commit fff7d5c68e38345a4b9f7b5f8384a82f791ed4fd
Author: David Cramer <dcramer@gmail.com>
Date:   Sun Mar 1 11:18:33 2015 -0800

    Support parameters in redirect_uri

diff --git a/src/sentry/auth/providers/oauth2.py b/src/sentry/auth/providers/oauth2.py
index 32df4778e3..76ce247a57 100644
--- a/src/sentry/auth/providers/oauth2.py
+++ b/src/sentry/auth/providers/oauth2.py
@@ -54,7 +54,11 @@ class OAuth2Login(AuthView):
             redirect_uri=absolute_uri(helper.get_redirect_url()),
         )
 
-        redirect_uri = self.get_authorize_url() + '?' + urlencode(params)
+        redirect_uri = self.get_authorize_url()
+        if '?' in redirect_uri:
+            redirect_uri = '{}&{}'.format(redirect_uri, urlencode(params))
+        else:
+            redirect_uri = '{}?{}'.format(redirect_uri, urlencode(params))
 
         helper.bind_state('state', state)
 
