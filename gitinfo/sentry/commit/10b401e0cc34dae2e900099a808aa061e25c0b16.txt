commit 10b401e0cc34dae2e900099a808aa061e25c0b16
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Aug 22 12:30:33 2018 -0700

    Fix optional app argument

diff --git a/src/sentry/receivers/users.py b/src/sentry/receivers/users.py
index e21b068d2d..a344a27638 100644
--- a/src/sentry/receivers/users.py
+++ b/src/sentry/receivers/users.py
@@ -6,7 +6,7 @@ from django.db.models.signals import post_syncdb
 from sentry.models import User
 
 
-def create_first_user(app, created_models, verbosity, db, **kwargs):
+def create_first_user(created_models, verbosity, db, app=None, **kwargs):
     # this is super confusing
     if app and app.__name__ != 'sentry.models':
         return
