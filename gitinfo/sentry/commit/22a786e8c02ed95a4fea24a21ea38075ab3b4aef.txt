commit 22a786e8c02ed95a4fea24a21ea38075ab3b4aef
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Jan 31 01:46:43 2012 -0800

    Change sentry admin to be /manage/ to avoid any unintentional conflict with the Django admin

diff --git a/CHANGES b/CHANGES
index 8fff8761b6..fe1586c551 100644
--- a/CHANGES
+++ b/CHANGES
@@ -1,3 +1,10 @@
+Version 2.2.2
+-------------
+
+* Corrected event_id key to contain (project_id, event_id).
+* Adjusted project form to default the user to the current user
+  when accessed by an admin.
+
 Version 2.2.1
 -------------
 
diff --git a/sentry/web/forms.py b/sentry/web/forms.py
index 5ef14e0b1d..3cd6a35ce6 100644
--- a/sentry/web/forms.py
+++ b/sentry/web/forms.py
@@ -87,7 +87,7 @@ class NewProjectAdminForm(forms.ModelForm):
         attrs={
             'placeholder': 'username',
         }
-    ))
+    ), required=False)
 
     class Meta:
         fields = ('name', 'owner')
diff --git a/sentry/web/frontend/projects.py b/sentry/web/frontend/projects.py
index 13c5c58e86..41b61cd736 100644
--- a/sentry/web/frontend/projects.py
+++ b/sentry/web/frontend/projects.py
@@ -29,10 +29,14 @@ def new_project(request):
 
     if request.user.has_perm('sentry.can_admin_project'):
         form_cls = NewProjectAdminForm
+        initial = {
+            'owner': request.user.username,
+        }
     else:
         form_cls = NewProjectForm
+        initial = {}
 
-    form = form_cls(request.POST or None)
+    form = form_cls(request.POST or None, initial=initial)
     if form.is_valid():
         project = form.save(commit=False)
         if not project.owner:
diff --git a/sentry/web/urls.py b/sentry/web/urls.py
index 0c6dc1a088..9b0118f7f7 100644
--- a/sentry/web/urls.py
+++ b/sentry/web/urls.py
@@ -56,8 +56,8 @@ urlpatterns = patterns('',
     # Global
 
     url(r'^$', generic.dashboard, name='sentry'),
-    url(r'^admin/status/$', admin.status, name='sentry-admin-status'),
-    url(r'^admin/plugins/(?P<slug>[\w_-]+)/$', admin.configure_plugin, name='sentry-admin-configure-plugin'),
+    url(r'^manage/status/$', admin.status, name='sentry-admin-status'),
+    url(r'^manage/plugins/(?P<slug>[\w_-]+)/$', admin.configure_plugin, name='sentry-admin-configure-plugin'),
 
     # API / JS
 
