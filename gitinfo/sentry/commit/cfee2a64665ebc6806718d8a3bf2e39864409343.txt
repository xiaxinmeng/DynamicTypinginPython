commit cfee2a64665ebc6806718d8a3bf2e39864409343
Author: Ted Kaemming <ted@kaemming.com>
Date:   Wed Sep 30 14:44:59 2015 -0700

    A couple template changes.

diff --git a/src/sentry/plugins/sentry_mail/models.py b/src/sentry/plugins/sentry_mail/models.py
index f5dc06f0ef..303b865029 100644
--- a/src/sentry/plugins/sentry_mail/models.py
+++ b/src/sentry/plugins/sentry_mail/models.py
@@ -200,11 +200,11 @@ class MailPlugin(NotificationPlugin):
         # TODO: Maybe project should just be an attribute of the digest?
         records = list(itertools.chain.from_iterable(digest.values()))
         # TODO: Pluralization
-        subject = '[{project}] {n} events from {first.datetime} to {last.datetime}]'.format(
+        subject = '[{project}] {n} events from {oldest.datetime} to {newest.datetime}]'.format(
             project=project.get_full_name().encode('utf-8'),
             n=len(records),
-            first=records[0],
-            last=records[-1],
+            oldest=records[-1],
+            newest=records[0],
         )
 
         self._send_mail(
diff --git a/src/sentry/templates/sentry/emails/digests/body.html b/src/sentry/templates/sentry/emails/digests/body.html
index 60c4c7bbf0..70e3434ec0 100644
--- a/src/sentry/templates/sentry/emails/digests/body.html
+++ b/src/sentry/templates/sentry/emails/digests/body.html
@@ -4,7 +4,7 @@
 
 {% block main %}
 
-<h2>{{ project }}</h2>
+<h2>{{ project.name }}</h2>
 
 {% for group, records in digest.items %}
     {% url 'sentry-group' group.organization.slug group.project.slug group.id as group_link %}
@@ -13,8 +13,8 @@
     <ul>
         {% for record in records %}
             <li>
-                {{ record.value.event.message }}
-                at {{ record.datetime }}
+                <strong>{{ record.value.event.message }}</strong>
+                <small>at {{ record.datetime }}</small>
             </li>
         {% endfor %}
     </ul>
