commit 1d7be7faa7201e842d465c2c5d83853949d8c4f2
Author: David Cramer <dcramer@gmail.com>
Date:   Tue May 7 20:58:05 2013 -0700

    Add postgres_pypy

diff --git a/setup.py b/setup.py
index 33ebe820ef..8ccd596261 100755
--- a/setup.py
+++ b/setup.py
@@ -84,6 +84,10 @@ postgres_requires = [
     'psycopg2>=2.4.0,<2.5.0',
 ]
 
+postgres_pypy_requires = [
+    'psycopg2cffi',
+]
+
 mysql_requires = [
     'MySQL-python>=1.2.0,<1.3.0',
 ]
@@ -105,6 +109,7 @@ setup(
         'tests': tests_require,
         'dev': dev_requires,
         'postgres': install_requires + postgres_requires,
+        'postgres_pypy': install_requires + postgres_pypy_requires,
         'mysql': install_requires + mysql_requires,
     },
     test_suite='runtests.runtests',
