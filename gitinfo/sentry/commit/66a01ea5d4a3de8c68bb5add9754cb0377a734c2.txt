commit 66a01ea5d4a3de8c68bb5add9754cb0377a734c2
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Jun 2 16:11:07 2015 +0100

    Switch nav to be more similar to tabbed style

diff --git a/src/sentry/static/sentry/app/components/organizationHomeSidebar.jsx b/src/sentry/static/sentry/app/components/organizationHomeSidebar.jsx
index df108e9907..bd84c52ee1 100644
--- a/src/sentry/static/sentry/app/components/organizationHomeSidebar.jsx
+++ b/src/sentry/static/sentry/app/components/organizationHomeSidebar.jsx
@@ -29,28 +29,36 @@ var OrganizationHomeSidebar = React.createClass({
 
     return (
       <div>
+        <h6>Organization</h6>
         <ul className="nav nav-stacked">
           <ListLink to="organizationTeams" params={orgParams}>Teams</ListLink>
           <ListLink to="organizationProjects" params={orgParams}>Projects</ListLink>
           {access.has('org:read') &&
             <ListLink to="organizationStats" params={orgParams}>Stats</ListLink>
           }
-          {access.has('org:write') &&
-            <li><a href={urlPrefix + '/audit-log/'}>Audit Log</a></li>
-          }
-          {access.has('org:read') &&
-            <li><a href={urlPrefix + '/members/'}>Members</a></li>
-          }
-          {features.has('sso') && access.has('org:write') &&
-            <li><a href={urlPrefix + '/auth/'}>Auth</a></li>
-          }
-          {access.has('org:write') &&
-            <li><a href={urlPrefix + '/api-keys/'}>API Keys</a></li>
-          }
-          {access.has('org:write') &&
-            <li><a href={urlPrefix + '/settings/'}>Settings</a></li>
-          }
         </ul>
+        {access.has('org:read') &&
+          <div>
+            <h6>Admin</h6>
+            <ul className="nav nav-stacked">
+              {access.has('org:write') &&
+                <li><a href={urlPrefix + '/audit-log/'}>Audit Log</a></li>
+              }
+              {features.has('sso') && access.has('org:write') &&
+                <li><a href={urlPrefix + '/auth/'}>Auth</a></li>
+              }
+              {access.has('org:write') &&
+                <li><a href={urlPrefix + '/api-keys/'}>API Keys</a></li>
+              }
+              {access.has('org:read') &&
+                <li><a href={urlPrefix + '/members/'}>Members</a></li>
+              }
+              {access.has('org:write') &&
+                <li><a href={urlPrefix + '/settings/'}>Settings</a></li>
+              }
+            </ul>
+          </div>
+        }
       </div>
     );
   }
diff --git a/src/sentry/static/sentry/less/shared-components.less b/src/sentry/static/sentry/less/shared-components.less
index 5893e5b1cf..b831380e04 100644
--- a/src/sentry/static/sentry/less/shared-components.less
+++ b/src/sentry/static/sentry/less/shared-components.less
@@ -1643,21 +1643,26 @@ header ~ #messages {
   margin-bottom: 6px;
 
   > a {
-    background: lighten(#f1f2f3, 2);
-    font-weight: 600;
-    padding: 8px 14px;
-    border-radius: 4px;
-    color: @gray;
+    font-size: 15px;
+    padding: 4px 14px;
+    border: 0;
+    background: none;
+    color: rgba(52, 60, 69, .5);
 
-    &:hover {
-    background: #f1f2f3;
+    &:hover, &:active, &:focus {
+      background: none;
+      color: rgba(52, 60, 69, 1);
     }
   }
 
   &.active {
-    a, a:hover {
-    color: #fff;
-    background: @blue;
+    a, a:hover, a:focus {
+      border: 0;
+      border-left: 3px solid @blue;
+      padding-left: 11px;
+      background: none;
+      color: rgba(52, 60, 69, 1);
+      font-weight: 600;
     }
   }
 }
