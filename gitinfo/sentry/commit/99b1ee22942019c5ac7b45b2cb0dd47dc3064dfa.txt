commit 99b1ee22942019c5ac7b45b2cb0dd47dc3064dfa
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Mar 18 21:07:22 2013 -0700

    Better error handling in import_string

diff --git a/src/sentry/app.py b/src/sentry/app.py
index 891c176f00..9f104cf4bd 100644
--- a/src/sentry/app.py
+++ b/src/sentry/app.py
@@ -18,8 +18,6 @@ class State(local):
 
 def get_instance(path, options):
     cls = import_string(path)
-    if cls is None:
-        raise ImportError('Unable to find module %s' % path)
     return cls(**options)
 
 buffer = get_instance(settings.BUFFER, settings.BUFFER_OPTIONS)
diff --git a/src/sentry/utils/imports.py b/src/sentry/utils/imports.py
index b7bf7e36e2..a24522d629 100644
--- a/src/sentry/utils/imports.py
+++ b/src/sentry/utils/imports.py
@@ -37,5 +37,5 @@ def import_string(path):
     """
     result = _cache[path]
     if result is None:
-        raise ImportError(path)
+        raise ImportError('Unable to load module %s' % path)
     return result
