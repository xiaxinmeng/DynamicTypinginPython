commit 49525b242abdca9fb6d39d057199127275986763
Author: ckj <chriskjennings@gmail.com>
Date:   Thu Oct 15 12:20:47 2015 -0700

    Extend base for link identity emails

diff --git a/src/sentry/templates/sentry/emails/auth-link-identity.html b/src/sentry/templates/sentry/emails/auth-link-identity.html
index bc6d6f8fb5..a91ca70001 100644
--- a/src/sentry/templates/sentry/emails/auth-link-identity.html
+++ b/src/sentry/templates/sentry/emails/auth-link-identity.html
@@ -1,6 +1,9 @@
-SSO has been configured for the {{ organization_name }} organization.
+{% extends "sentry/emails/base.html" %}
 
-We'll need you to link your existing account with the SSO provider by visiting the following url:
+{% load i18n %}
 
-    {{ url }}
+{% block main %}
+  <p>SSO has been configured for the <strong>{{ organization_name }}</strong> organization.</p>
 
+  <p>We'll need you to link your existing account with the SSO provider by visiting the following url: {{ url }}</p>
+{% endblock %}
diff --git a/src/sentry/templates/sentry/emails/request-team-access.html b/src/sentry/templates/sentry/emails/request-team-access.html
index 3bfb7b5e2f..c58a4bac41 100644
--- a/src/sentry/templates/sentry/emails/request-team-access.html
+++ b/src/sentry/templates/sentry/emails/request-team-access.html
@@ -7,5 +7,4 @@
     <p><strong>{{ name }}</strong> is requesting access to the {{ team.name }} team on Sentry.</p>
 
     <p>Accept or decline this request in the <a href="{{ url }}">pending requests panel</a>.</p>
-
 {% endblock %}
