commit ffe8fd827cbe6c72e5bf010960f6fae217f27559
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Jan 1 20:49:00 2015 -0800

    Implement styles for group state

diff --git a/src/sentry/static/sentry/app-react/components/stream.jsx b/src/sentry/static/sentry/app-react/components/stream.jsx
index c7f60d3690..306d455dfa 100644
--- a/src/sentry/static/sentry/app-react/components/stream.jsx
+++ b/src/sentry/static/sentry/app-react/components/stream.jsx
@@ -459,11 +459,14 @@ var Aggregate = React.createClass({
     }
 
     var className = "group";
-    if (this.props.isBookmarked) {
-      className += ' isBookmarked';
+    if (data.isBookmarked) {
+      className += " isBookmarked";
     }
-    if (this.props.hasSeen) {
-      className += ' hasSeen';
+    if (data.hasSeen) {
+      className += " hasSeen";
+    }
+    if (data.status === "resolved") {
+      className += " isResolved";
     }
 
     return (
diff --git a/src/sentry/static/sentry/less/sentry.less b/src/sentry/static/sentry/less/sentry.less
index 3871779089..c9a0b7c2c5 100644
--- a/src/sentry/static/sentry/less/sentry.less
+++ b/src/sentry/static/sentry/less/sentry.less
@@ -1572,7 +1572,7 @@ a.icon-share {
     position: relative;
 
     .icon-bookmark {
-      display: inline-block;
+      display: none;
       font-size: 95%;
       position: relative;
       top: 2px;
@@ -1582,6 +1582,11 @@ a.icon-share {
       overflow: hidden;
       .transition(all .2s ease-in-out);
     }
+    &.isBookmarked {
+      .icon-bookmark {
+        display: inline-block;
+      }
+    }
 
     .event-details:after {
       position: absolute;
@@ -1596,13 +1601,13 @@ a.icon-share {
       .transition(all .2s ease-in-out);
     }
 
-    &.seen {
+    &.hasSeen {
       .event-details:after {
         background: transparent;
       }
     }
 
-    &.resolved {
+    &.isResolved {
       h3 {
         text-decoration: line-through;
         color: #949ea4;
