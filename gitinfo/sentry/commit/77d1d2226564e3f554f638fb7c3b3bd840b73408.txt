commit 77d1d2226564e3f554f638fb7c3b3bd840b73408
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Jun 27 16:26:00 2016 -0700

    Improve icon accessibility

diff --git a/src/sentry/static/sentry/app/components/events/contextSummary.jsx b/src/sentry/static/sentry/app/components/events/contextSummary.jsx
index 449dc6932e..c45c4b8db0 100644
--- a/src/sentry/static/sentry/app/components/events/contextSummary.jsx
+++ b/src/sentry/static/sentry/app/components/events/contextSummary.jsx
@@ -71,7 +71,7 @@ const UserSummary = React.createClass({
           <Avatar user={user} size={48} className="context-item-icon"
                   gravatar={false} />
         :
-          <span className="icon" />
+          <span className="context-item-icon" />
         }
         <h3>{userTitle}</h3>
         {user.id && user.id !== userTitle ?
@@ -97,11 +97,11 @@ const DeviceSummary = React.createClass({
     }
 
     // TODO(dcramer): we need a better way to parse it
-    let className = data.model.split(/\d/)[0].toLowerCase();
+    let className = data.model.split(/\d/)[0].toLowerCase().replace(/[^a-z0-9\-]+/, '-');
 
     return (
       <div className={`context-item ${className}`}>
-        <span className="icon" />
+        <span className="context-item-icon" />
         <h3>{data.model}</h3>
         <p>{data.arch || data.model_id || ''}</p>
       </div>
