commit e315db2eafcceb740e1e0104934bc7d4037bc005
Author: Helmut Januschka <helmut@januschka.com>
Date:   Wed Jul 11 20:07:01 2018 +0200

    fix(ui): linefeeds of text selection (#8992)
    
    Closes #8131

diff --git a/src/sentry/static/sentry/app/utils/selectText.jsx b/src/sentry/static/sentry/app/utils/selectText.jsx
index 28d1921146..8150fa2da3 100644
--- a/src/sentry/static/sentry/app/utils/selectText.jsx
+++ b/src/sentry/static/sentry/app/utils/selectText.jsx
@@ -1,5 +1,7 @@
 export function selectText(node) {
-  if (document.selection) {
+  if (node.type && node.type === 'text') {
+    node.select();
+  } else if (document.selection) {
     let range = document.body.createTextRange();
     range.moveToElementText(node);
     range.select();
