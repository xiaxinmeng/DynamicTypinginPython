commit 16c3f2b6289b574b063fdeab1111535631dbf984
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Apr 20 09:39:50 2016 -0700

    Filter out in-active projects for team project list
    
    /cc @getsentry/api

diff --git a/src/sentry/api/endpoints/team_project_index.py b/src/sentry/api/endpoints/team_project_index.py
index 4706dc5d7d..01ce7f51bd 100644
--- a/src/sentry/api/endpoints/team_project_index.py
+++ b/src/sentry/api/endpoints/team_project_index.py
@@ -6,7 +6,7 @@ from rest_framework.response import Response
 from sentry.api.base import DocSection
 from sentry.api.bases.team import TeamEndpoint
 from sentry.api.serializers import serialize
-from sentry.models import Project, AuditLogEntryEvent
+from sentry.models import Project, ProjectStatus, AuditLogEntryEvent
 from sentry.utils.apidocs import scenario, attach_scenarios
 
 
@@ -56,7 +56,11 @@ class TeamProjectIndexEndpoint(TeamEndpoint):
             results = list(Project.objects.get_for_user(
                 team=team, user=request.user))
         else:
-            results = list(Project.objects.filter(team=team))
+            # TODO(dcramer): status should be selectable
+            results = list(Project.objects.filter(
+                team=team,
+                status=ProjectStatus.visible,
+            ))
 
         return Response(serialize(results, request.user))
 
