commit 139142afbeb41aff9905258dcc62aa2260593d22
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Wed Dec 16 16:54:59 2015 -0800

    Only lower() once

diff --git a/src/sentry/options/types.py b/src/sentry/options/types.py
index 4b8cc91aa2..7265ecba3d 100644
--- a/src/sentry/options/types.py
+++ b/src/sentry/options/types.py
@@ -72,9 +72,10 @@ class BoolType(OptionType):
     expected_types = (bool,)
 
     def convert(self, value):
-        if value.lower() in ('y', 'yes', 't', 'true', '1'):
+        value = value.lower()
+        if value in ('y', 'yes', 't', 'true', '1'):
             return True
-        if value.lower() in ('n', 'no', 'f', 'false', '0'):
+        if value in ('n', 'no', 'f', 'false', '0'):
             return False
 
 
