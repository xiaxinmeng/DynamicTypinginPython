commit 18c1fe10f135cfdcf41efca4ce78cf3c5015d881
Author: David Cramer <dcramer@gmail.com>
Date:   Fri Nov 4 16:53:10 2011 -0700

    Change the dashboard to just be project selection (for now)

diff --git a/sentry/templates/sentry/dashboard.html b/sentry/templates/sentry/dashboard.html
index e69de29bb2..ead69eb8c9 100644
--- a/sentry/templates/sentry/dashboard.html
+++ b/sentry/templates/sentry/dashboard.html
@@ -0,0 +1,18 @@
+{% extends "sentry/layout.html" %}
+
+{% load i18n %}
+
+{% block title %}{% trans "Select a Project" %} | {{ block.super }}{% endblock %}
+
+{% block heading %}{% trans "Select a Project" %}{% endblock %}
+
+{% block content %}
+    <div id="body">
+        <p>Select a project...</p>
+        <ul>
+            {% for project in project_list.itervalues %}
+                <li><a href="{% url sentry project.pk %}">{{ project.name }}</a></li>
+            {% endfor %}
+        </ul>
+    </div>
+{% endblock %}
diff --git a/sentry/web/views.py b/sentry/web/views.py
index 06e38726ea..a23f8ae763 100644
--- a/sentry/web/views.py
+++ b/sentry/web/views.py
@@ -284,7 +284,11 @@ def search(request, project):
 
 @login_required
 def dashboard(request):
+    project_list = get_project_list(request.user)
+    if len(project_list) == 1:
+        return HttpResponseRedirect(reverse('sentry', kwargs={'project_id': project_list.keys()[0]}))
     return render_to_response('sentry/dashboard.html', {
+        'project_list': project_list,
         'request': request,
     })
 
