commit 4921b46c9d623e13d8f105971172fbd92952723e
Author: David Cramer <dcramer@gmail.com>
Date:   Wed May 23 12:46:01 2012 -0700

    Signatures are no longer required on messages

diff --git a/sentry/coreapi.py b/sentry/coreapi.py
index f737b922ee..0b694b1a5f 100644
--- a/sentry/coreapi.py
+++ b/sentry/coreapi.py
@@ -84,7 +84,7 @@ def extract_auth_vars(request):
         return None
 
 
-def project_from_auth_vars(auth_vars, data, require_signature=True):
+def project_from_auth_vars(auth_vars, data, require_signature=False):
     api_key = auth_vars.get('sentry_key')
     if api_key:
         try:
diff --git a/sentry/web/api.py b/sentry/web/api.py
index 782519aa81..1f207258f0 100644
--- a/sentry/web/api.py
+++ b/sentry/web/api.py
@@ -94,8 +94,6 @@ def store(request, project=None):
     logger.debug('Inbound %r request from %r', request.method, request.META['REMOTE_ADDR'])
     client = '<unknown client>'
 
-    origin = request.META.get('HTTP_ORIGIN', None)
-
     response = HttpResponse()
 
     if request.method == 'POST':
@@ -122,7 +120,7 @@ def store(request, project=None):
                 # We only require a signature if a referrer was not set
                 # (this is restricted via the CORS headers)
                 project_ = project_from_auth_vars(auth_vars, data,
-                    require_signature=bool(origin is None))
+                    require_signature=False)
 
                 if not project:
                     project = project_
diff --git a/setup.py b/setup.py
index d9f2d34dd8..d3cf22dfe3 100755
--- a/setup.py
+++ b/setup.py
@@ -71,7 +71,7 @@ dependency_links = [
 
 setup(
     name='sentry',
-    version='4.4.5',
+    version='4.4.6',
     author='David Cramer',
     author_email='dcramer@gmail.com',
     url='http://github.com/dcramer/sentry',
