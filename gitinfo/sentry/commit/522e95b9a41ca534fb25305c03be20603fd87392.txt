commit 522e95b9a41ca534fb25305c03be20603fd87392
Author: alex-hofsteede <alex@hofsteede.com>
Date:   Fri Feb 22 08:56:58 2019 -0800

    fix: use BoundedBigIntegerField
    
    I am a little concerned that this might break on the import because
    the *definition* of this class is behind a flag which could be false?

diff --git a/src/sentry/db/models/manager.py b/src/sentry/db/models/manager.py
index 915e399593..c7e4d7c133 100644
--- a/src/sentry/db/models/manager.py
+++ b/src/sentry/db/models/manager.py
@@ -22,7 +22,7 @@ from django.db.models.signals import (post_save, post_delete, post_init, class_p
 from django.utils.encoding import smart_text
 
 from sentry import nodestore
-from sentry.db.models.fields import BoundedIntegerField
+from sentry.db.models.fields import BoundedBigIntegerField
 from sentry.utils.cache import cache
 from sentry.utils.hashlib import md5_text
 from sentry.utils.validators import is_event_id
@@ -351,7 +351,7 @@ class EventManager(BaseManager):
         # TODO (alexh) deprecate lookup by id so we can move to snuba.
 
         event = None
-        if id_or_event_id.isdigit() and int(id_or_event_id) <= BoundedIntegerField.MAX_VALUE:
+        if id_or_event_id.isdigit() and int(id_or_event_id) <= BoundedBigIntegerField.MAX_VALUE:
             # If its a numeric string, check if it's an event Primary Key first
             try:
                 if project_id is None:
