commit 3f9c39db6db9c1fdb2a16a3a30202541516d3ece
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Apr 16 17:28:14 2015 -0700

    Ensure we can fetch from all versions

diff --git a/src/sentry/plugins/base/manager.py b/src/sentry/plugins/base/manager.py
index 1eca4a9ac7..3103f31e30 100644
--- a/src/sentry/plugins/base/manager.py
+++ b/src/sentry/plugins/base/manager.py
@@ -43,7 +43,10 @@ class PluginManager(InstanceManager):
             yield plugin
 
     def get(self, slug):
-        for plugin in self.all():
+        for plugin in self.all(version=1):
+            if plugin.slug == slug:
+                return plugin
+        for plugin in self.all(version=2):
             if plugin.slug == slug:
                 return plugin
         raise KeyError(slug)
