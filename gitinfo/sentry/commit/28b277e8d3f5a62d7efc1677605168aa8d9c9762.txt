commit 28b277e8d3f5a62d7efc1677605168aa8d9c9762
Author: David Cramer <dcramer@gmail.com>
Date:   Fri Sep 24 18:41:35 2010 -0700

    All sentry errors should now go to sentry.errors

diff --git a/sentry/client/base.py b/sentry/client/base.py
index 63bc7b82d9..3b9fa28363 100644
--- a/sentry/client/base.py
+++ b/sentry/client/base.py
@@ -24,9 +24,6 @@ class SentryClient(object):
     def process(self, **kwargs):
         from sentry.helpers import get_filters
 
-        for filter_ in get_filters():
-            kwargs = filter_(None).process(kwargs) or kwargs
-
         kwargs.setdefault('level', logging.ERROR)
         kwargs.setdefault('server_name', settings.NAME)
 
@@ -38,6 +35,9 @@ class SentryClient(object):
             if not added and cache.incr(cache_key) > settings.THRASHING_LIMIT:
                 return
 
+        for filter_ in get_filters():
+            kwargs = filter_(None).process(kwargs) or kwargs
+
         return self.send(**kwargs)
 
     def send(self, **kwargs):
diff --git a/sentry/client/models.py b/sentry/client/models.py
index a1a6782e0a..a9c3c1f889 100644
--- a/sentry/client/models.py
+++ b/sentry/client/models.py
@@ -9,7 +9,7 @@ from django.http import Http404
 from sentry import settings
 from sentry.client.base import SentryClient
 
-logger = logging.getLogger('sentry')
+logger = logging.getLogger('sentry.errors')
 
 _client = (None, None)
 def get_client():
diff --git a/sentry/manager.py b/sentry/manager.py
index 40f2e08161..fa702737f0 100644
--- a/sentry/manager.py
+++ b/sentry/manager.py
@@ -13,7 +13,7 @@ from sentry.helpers import construct_checksum, transform, varmap
 
 assert not settings.DATABASE_USING or django.VERSION >= (1, 2), 'The `SENTRY_DATABASE_USING` setting requires Django >= 1.2'
 
-logger = logging.getLogger('sentry')
+logger = logging.getLogger('sentry.errors')
 
 class SentryManager(models.Manager):
     use_for_related_fields = True
