commit 4123755bd1182e226312631677d22daac5d831b8
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Nov 15 01:53:52 2012 -0800

    Correct tests and extend behavior

diff --git a/src/sentry/static/sentry/coffee/views.coffee b/src/sentry/static/sentry/coffee/views.coffee
index e1ce5c23a2..75861167ca 100644
--- a/src/sentry/static/sentry/coffee/views.coffee
+++ b/src/sentry/static/sentry/coffee/views.coffee
@@ -43,7 +43,7 @@ jQuery ->
 
         extend: (data) ->
             for item in data
-                @addMember(data)
+                @addMember(item)
 
         addMember: (member) ->
             if not @hasMember(member)
diff --git a/src/sentry/static/sentry/scripts/site.js b/src/sentry/static/sentry/scripts/site.js
index 16d863d405..75607cb82e 100644
--- a/src/sentry/static/sentry/scripts/site.js
+++ b/src/sentry/static/sentry/scripts/site.js
@@ -478,7 +478,7 @@
         _results = [];
         for (_i = 0, _len = data.length; _i < _len; _i++) {
           item = data[_i];
-          _results.push(this.addMember(data));
+          _results.push(this.addMember(item));
         }
         return _results;
       };
diff --git a/tests/js/spec/OrderedElementsViewSpec.js b/tests/js/spec/OrderedElementsViewSpec.js
index 2d35e02763..d2f560f330 100644
--- a/tests/js/spec/OrderedElementsViewSpec.js
+++ b/tests/js/spec/OrderedElementsViewSpec.js
@@ -33,8 +33,8 @@ describe("OrderedElementsView", function() {
     view.$parent = $('<ul></ul>');
   });
 
-  it("should bind a collection", function() {
-    expect(view.collection);
+  it("should suggest its loading", function() {
+    expect(view.loaded).toBe(false);
   });
 
   describe(".extend", function() {
@@ -45,8 +45,9 @@ describe("OrderedElementsView", function() {
       view.addMember = sinon.spy();
       view.extend([group1, group2]);
       expect(view.addMember.callCount).toBe(2);
-      expect(view.addMember.calledWith(group1));
-      expect(view.addMember.calledWith(group2));
+      console.log(view.addMember.getCall(0));
+      expect(view.addMember.calledWithExactly(group1)).toBe(true);
+      expect(view.addMember.calledWithExactly(group2)).toBe(true);
     });
   });
 
