commit 89f9865d8290a265bad0af9d2a6363632be96ac7
Author: Evan Purkhiser <evanpurkhiser@gmail.com>
Date:   Thu Aug 29 13:10:00 2019 -0700

    ref(2fa): Correct proptypes warning for qrcode (#14536)

diff --git a/src/sentry/utils/otp.py b/src/sentry/utils/otp.py
index 2aeb74c48a..c16f751b6c 100644
--- a/src/sentry/utils/otp.py
+++ b/src/sentry/utils/otp.py
@@ -102,4 +102,6 @@ class TOTP(object):
     def get_provision_qrcode(self, user, issuer=None):
         qr = qrcode.QRCode(border=0)
         qr.add_data(self.get_provision_url(user, issuer=issuer))
-        return qr.get_matrix()
+
+        # Frontend expects the matrix to be serialized as 1/0, not True/False
+        return [[int(c) for c in row] for row in qr.get_matrix()]
