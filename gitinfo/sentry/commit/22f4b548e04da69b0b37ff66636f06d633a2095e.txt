commit 22f4b548e04da69b0b37ff66636f06d633a2095e
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Fri Mar 1 15:08:24 2019 -0800

    mocks: fix when using RedisSnubaTSDB backend
    
    These models are no longer supported in production so cause issues

diff --git a/bin/load-mocks b/bin/load-mocks
index e05d982fa2..5017249235 100755
--- a/bin/load-mocks
+++ b/bin/load-mocks
@@ -246,11 +246,6 @@ def create_sample_time_series(event, release=None):
         ), now, int(count * 0.1))
 
         frequencies = [
-            (tsdb.models.frequent_projects_by_organization, {
-                project.organization_id: {
-                    project.id: count,
-                },
-            }),
             (tsdb.models.frequent_issues_by_project, {
                 project.id: {
                     group.id: count,
@@ -293,11 +288,6 @@ def create_sample_time_series(event, release=None):
         ), now, int(count * 0.1))
 
         frequencies = [
-            (tsdb.models.frequent_projects_by_organization, {
-                project.organization_id: {
-                    project.id: count,
-                },
-            }),
             (tsdb.models.frequent_issues_by_project, {
                 project.id: {
                     group.id: count,
