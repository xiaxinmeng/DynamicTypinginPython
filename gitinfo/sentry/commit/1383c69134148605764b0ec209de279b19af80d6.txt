commit 1383c69134148605764b0ec209de279b19af80d6
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Mon Feb 1 11:31:22 2016 -0800

    Add `sentry config generate-secret-key`

diff --git a/docs/config.rst b/docs/config.rst
index 2c8fea88fb..52f29e7186 100644
--- a/docs/config.rst
+++ b/docs/config.rst
@@ -64,13 +64,9 @@ General
 
         system.secret-key: 'a-really-long-secret-value'
 
-    To generate a new value, you can use Python:
+    To generate a new value, we've provided a helper:
 
-    .. code-block:: python
-
-        # $ sentry shell
-        from sentry.runner.settings import generate_secret_key
-        print(generate_secret_key())
+        $ sentry config generate-secret-key
 
 
 Authentication
diff --git a/src/sentry/runner/commands/config.py b/src/sentry/runner/commands/config.py
index 1a37581e63..486ee59bec 100644
--- a/src/sentry/runner/commands/config.py
+++ b/src/sentry/runner/commands/config.py
@@ -70,3 +70,10 @@ def delete(option, no_input):
         options.delete(option)
     except UnknownOption:
         raise click.ClickException('unknown option: %s' % option)
+
+
+@config.command(name='generate-secret-key')
+def generate_secret_key():
+    "Generate a new cryptographically secure secret key value."
+    from sentry.runner.settings import generate_secret_key
+    click.echo(generate_secret_key())
