commit 16a4c177de3f63055c5f0252c3f8ba202175fb41
Author: Brett Hoerner <brett@bretthoerner.com>
Date:   Wed Oct 10 17:51:47 2018 -0500

    fix(eventstream): Fix message types for delete_groups and merge (#10072)

diff --git a/src/sentry/eventstream/kafka.py b/src/sentry/eventstream/kafka.py
index 375d6b8da6..0e10baddbd 100644
--- a/src/sentry/eventstream/kafka.py
+++ b/src/sentry/eventstream/kafka.py
@@ -149,7 +149,7 @@ class KafkaEventStream(EventStream):
 
         self._send(
             project_id,
-            'delete_groups',
+            'start_delete_groups',
             extra_data=(state,),
             asynchronous=False
         )
@@ -180,7 +180,7 @@ class KafkaEventStream(EventStream):
 
         self._send(
             project_id,
-            'merge',
+            'start_merge',
             extra_data=(state,),
             asynchronous=False
         )
@@ -190,7 +190,7 @@ class KafkaEventStream(EventStream):
         state['datetime'] = datetime.now(tz=pytz.utc)
         self._send(
             state['project_id'],
-            'merge',
+            'end_merge',
             extra_data=(state,),
             asynchronous=False
         )
