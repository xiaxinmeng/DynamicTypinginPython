commit 6d816cb1653b8bca95f4b6f9429a1ccaf2bc7484
Author: Mark Story <mark@mark-story.com>
Date:   Thu Jan 31 14:40:25 2019 -0500

    ref(issues) Update saved search selector to use styled components (#11808)
    
    Remove the less styles for the saved search selector. I've had to retain
    some of the nested classname rules as the dropdownlink and dropdownmenu
    components still use classnames as well.

diff --git a/src/sentry/static/sentry/app/views/stream/savedSearchSelector.jsx b/src/sentry/static/sentry/app/views/stream/savedSearchSelector.jsx
index 28d14b187d..18e20e4fb8 100644
--- a/src/sentry/static/sentry/app/views/stream/savedSearchSelector.jsx
+++ b/src/sentry/static/sentry/app/views/stream/savedSearchSelector.jsx
@@ -247,8 +247,8 @@ const SavedSearchSelector = withApi(
         );
       });
       return (
-        <div className="saved-search-selector">
-          <DropdownLink
+        <Container>
+          <StyledDropdownLink
             title={
               <span>
                 <span>{this.getTitle()}</span>
@@ -288,13 +288,44 @@ const SavedSearchSelector = withApi(
                 </span>
               </Tooltip>
             </ButtonBar>
-          </DropdownLink>
-        </div>
+          </StyledDropdownLink>
+        </Container>
       );
     }
   }
 );
 
+const Container = styled.div`
+  & .dropdown-menu {
+    max-width: 350px;
+    min-width: 275px;
+  }
+`;
+
+const StyledDropdownLink = styled(DropdownLink)`
+  display: inline-block;
+  font-size: 22px;
+  color: ${p => p.theme.gray5};
+  line-height: 36px;
+  margin-right: 10px;
+  max-width: 100%;
+  text-overflow: ellipsis;
+  white-space: nowrap;
+  overflow: hidden;
+
+  & :hover,
+  & :focus {
+    color: ${p => p.theme.gray5};
+  }
+
+  & .icon-arrow-down {
+    display: inline-block;
+    margin-left: 5px;
+    top: 0;
+    vertical-align: middle;
+  }
+`;
+
 const EmptyItem = styled.li`
   padding: 8px 10px 5px;
   font-style: italic;
diff --git a/src/sentry/static/sentry/less/stream.less b/src/sentry/static/sentry/less/stream.less
index c9f5828abb..10d7c90bbe 100644
--- a/src/sentry/static/sentry/less/stream.less
+++ b/src/sentry/static/sentry/less/stream.less
@@ -857,41 +857,6 @@
   font-size: 20px;
 }
 
-/**
-* Stream Saved Searches
-* ============================================================================
-*/
-
-.saved-search-selector {
-  .dropdown-toggle {
-    display: inline-block;
-    font-size: 22px;
-    line-height: 36px;
-    margin-right: 10px;
-    color: @gray-darker;
-    max-width: 100%;
-    text-overflow: ellipsis;
-    white-space: nowrap;
-    overflow: hidden;
-
-    &:hover {
-      color: @gray-darkest;
-    }
-
-    .icon-arrow-down {
-      display: inline-block;
-      margin-left: 5px;
-      top: 0;
-      vertical-align: middle;
-    }
-  }
-
-  .dropdown-menu {
-    max-width: 350px;
-    min-width: 275px;
-  }
-}
-
 .stream-sort {
   margin-right: 5px;
   z-index: 1001;
