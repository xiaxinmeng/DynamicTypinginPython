commit 78b51778f1f9671bdea480b08781a174ec81fe91
Author: Burak Yigit Kaya <byk@sentry.io>
Date:   Sat Jul 6 00:21:10 2019 +0300

    ci(travis): Move docker-sentry builds to Dockes Hub autobuild (#13901)

diff --git a/.travis.yml b/.travis.yml
index 8873f98610..a8799f1a59 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -65,25 +65,8 @@ after_script:
   # Trigger a build for the `git` image on docker-sentry
   - >
       if [ "$TRAVIS_PULL_REQUEST" == "false" -a "$TRAVIS_BRANCH" == "master" ]; then
-      curl -s -X POST
-      -H "Content-Type: application/json"
-      -H "Accept: application/json"
-      -H "Travis-API-Version: 3"
-      -H "Authorization: token $TRAVIS_TOKEN"
-      -d '{
-        "request": {
-          "branch": "master",
-          "config": {
-            "env": {
-              "matrix": [
-                "VERSION=git"
-              ]
-            }
-          },
-          "message": "Build for getsentry/sentry@'"$TRAVIS_COMMIT"'"
-        }
-      }'
-      https://api.travis-ci.org/repo/getsentry%2Fdocker-sentry/requests
+        curl -s -X POST "$DOCKER_SENTRY_BUILD_TRIGGER_URL";
+      fi
 
 base_postgres: &postgres_default
   python: 2.7
