commit da7ac24484b75c8ea29c300aa46c42f0fdd9519d
Author: Eric Feng <eric@getsentry.com>
Date:   Fri Feb 26 12:14:02 2016 -0800

    fix for sentry-113947841

diff --git a/src/sentry/receivers/onboarding.py b/src/sentry/receivers/onboarding.py
index 50054f7a46..1a2f702e23 100644
--- a/src/sentry/receivers/onboarding.py
+++ b/src/sentry/receivers/onboarding.py
@@ -96,7 +96,7 @@ def record_first_event(project, group, **kwargs):
             return
 
         # Only counts if it's a new project and platform
-        if oot.project_id != project.id and oot.data['platform'] != group.platform:
+        if oot.project_id != project.id and oot.data.get('platform', group.platform) != group.platform:
             OrganizationOnboardingTask.objects.create_or_update(
                 organization=project.organization,
                 task=OnboardingTask.SECOND_PLATFORM,
