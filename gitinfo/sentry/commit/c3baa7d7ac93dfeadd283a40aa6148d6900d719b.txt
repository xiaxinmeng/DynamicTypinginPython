commit c3baa7d7ac93dfeadd283a40aa6148d6900d719b
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Fri Sep 2 16:43:35 2016 -0700

    Environment names may have unicodes, so don't blow up (#4075)
    
    Fixes SENTRY-1X6

diff --git a/src/sentry/models/grouprelease.py b/src/sentry/models/grouprelease.py
index 8d7b17aa2d..b24ace9d48 100644
--- a/src/sentry/models/grouprelease.py
+++ b/src/sentry/models/grouprelease.py
@@ -32,7 +32,7 @@ class GroupRelease(Model):
     def get_cache_key(cls, group_id, release_id, environment):
         return 'grouprelease:1:{}:{}'.format(
             group_id,
-            md5_text('{}:{}'.format(release_id, environment)).hexdigest(),
+            md5_text(u'{}:{}'.format(release_id, environment)).hexdigest(),
         )
 
     @classmethod
