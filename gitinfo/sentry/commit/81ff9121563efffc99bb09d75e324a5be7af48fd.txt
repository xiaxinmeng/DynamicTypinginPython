commit 81ff9121563efffc99bb09d75e324a5be7af48fd
Author: Max Bittker <maxbittker@gmail.com>
Date:   Wed Aug 2 15:53:47 2017 -0700

    gmt -> utc (#5802)

diff --git a/src/sentry/web/forms/accounts.py b/src/sentry/web/forms/accounts.py
index 8eb0360c88..45af0999fa 100644
--- a/src/sentry/web/forms/accounts.py
+++ b/src/sentry/web/forms/accounts.py
@@ -33,7 +33,7 @@ def _get_timezone_choices():
     for tz in pytz.common_timezones:
         now = datetime.now(pytz.timezone(tz))
         offset = now.strftime('%z')
-        results.append((int(offset), tz, '(GMT%s) %s' % (offset, tz)))
+        results.append((int(offset), tz, '(UTC%s) %s' % (offset, tz)))
     results.sort()
 
     for i in range(len(results)):
