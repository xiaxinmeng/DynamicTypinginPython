commit c7b832252dc7e02e9279125561e445dc6a6a0f42
Author: David Cramer <dcramer@gmail.com>
Date:   Tue May 24 13:59:02 2011 -0700

    Correctly handle 'message id not found' when search is enabled

diff --git a/sentry/web/views.py b/sentry/web/views.py
index aecb2c2894..bbb17480d7 100644
--- a/sentry/web/views.py
+++ b/sentry/web/views.py
@@ -122,6 +122,8 @@ def search(request):
             except Message.DoesNotExist:
                 if not has_search:
                     return render_to_response('sentry/invalid_message_id.html')
+                else:
+                    message_list = get_search_query_set(query)
             else:
                 return HttpResponseRedirect(message.get_absolute_url())
         elif not has_search:
