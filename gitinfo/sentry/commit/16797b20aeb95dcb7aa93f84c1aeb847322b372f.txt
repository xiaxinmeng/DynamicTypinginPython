commit 16797b20aeb95dcb7aa93f84c1aeb847322b372f
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Mar 3 15:51:59 2015 -0800

    Dont mark event as seen until loaded / fail silently

diff --git a/src/sentry/static/sentry/app/api.jsx b/src/sentry/static/sentry/app/api.jsx
index 308ebe74bd..844420f24d 100644
--- a/src/sentry/static/sentry/app/api.jsx
+++ b/src/sentry/static/sentry/app/api.jsx
@@ -84,6 +84,9 @@ class Client {
         GroupActions.updateSuccess(id, params.itemIds, response);
       },
       error: (error) => {
+        if (params.failSilently) {
+          return;
+        }
         GroupActions.updateError(id, params.itemIds, error);
       }
     });
diff --git a/src/sentry/static/sentry/app/views/groupOverview.jsx b/src/sentry/static/sentry/app/views/groupOverview.jsx
index bf65b47a42..f217071c10 100644
--- a/src/sentry/static/sentry/app/views/groupOverview.jsx
+++ b/src/sentry/static/sentry/app/views/groupOverview.jsx
@@ -53,15 +53,6 @@ var GroupOverview = React.createClass({
     this.fetchData();
   },
 
-  componentDidMount() {
-    api.bulkUpdate({
-      orgId: this.getOrganization().slug,
-      projectId: this.getProject().slug,
-      itemIds: [this.getGroup().id],
-      data: {hasSeen: true}
-    });
-  },
-
   routeDidChange(prevPath) {
     this.fetchData();
   },
@@ -85,6 +76,14 @@ var GroupOverview = React.createClass({
           error: false,
           loading: false
         });
+
+        api.bulkUpdate({
+          orgId: this.getOrganization().slug,
+          projectId: this.getProject().slug,
+          itemIds: [this.getGroup().id],
+          failSilently: true,
+          data: {hasSeen: true}
+        });
       },
       error: () => {
         this.setState({
