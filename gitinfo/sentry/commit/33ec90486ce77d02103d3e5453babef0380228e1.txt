commit 33ec90486ce77d02103d3e5453babef0380228e1
Author: Ben Vinegar <benvinegar@users.noreply.github.com>
Date:   Mon Jan 9 12:59:40 2017 -0800

    Remove UglifyJS warnings from travis output (#4735)

diff --git a/webpack.config.js b/webpack.config.js
index 64a6bee136..dd719a008c 100644
--- a/webpack.config.js
+++ b/webpack.config.js
@@ -185,16 +185,23 @@ var config = {
     '#cheap-module-eval-source-map'
 };
 
-// This compression-webpack-plugin generates pre-compressed files
-// ending in .gz, to be picked up and served by our internal static media
-// server as well as nginx when paired with the gzip_static module.
 if (IS_PRODUCTION) {
+  // This compression-webpack-plugin generates pre-compressed files
+  // ending in .gz, to be picked up and served by our internal static media
+  // server as well as nginx when paired with the gzip_static module.
   config.plugins.push(new (require('compression-webpack-plugin'))({
     algorithm: function(buffer, options, callback) {
       require('zlib').gzip(buffer, callback);
     },
     regExp: /\.(js|map|css|svg|html|txt|ico|eot|ttf)$/,
   }));
+
+  // Disable annoying UglifyJS warnings that pollute Travis log output
+  config.plugins.push(new webpack.optimize.UglifyJsPlugin({
+    compress: {
+      warnings: false
+    }
+  }));
 }
 
 module.exports = config;
