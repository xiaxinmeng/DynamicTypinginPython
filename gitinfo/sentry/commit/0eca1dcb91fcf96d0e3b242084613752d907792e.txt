commit 0eca1dcb91fcf96d0e3b242084613752d907792e
Author: Mark Story <mark@sentry.io>
Date:   Tue Jul 30 13:35:57 2019 -0400

    fix(ui) Remove scroll lock correctly (#14205)
    
    The body element doesn't have a default overflow value most of the time.
    The conditional was preventing the scroll lock from being released.

diff --git a/src/sentry/static/sentry/app/components/modalDialog.jsx b/src/sentry/static/sentry/app/components/modalDialog.jsx
index 67dd194e6c..78b7eade22 100644
--- a/src/sentry/static/sentry/app/components/modalDialog.jsx
+++ b/src/sentry/static/sentry/app/components/modalDialog.jsx
@@ -80,9 +80,7 @@ class ModalDialog extends React.Component {
     document.removeEventListener('keydown', this.handleKeyDown);
 
     // Restore body scrolling.
-    if (this.previousOverflow) {
-      document.body.style.overflow = this.previousOverflow;
-    }
+    document.body.style.overflow = this.previousOverflow;
   }
 
   handleClose = event => {
