commit 1d6edb0c7d1515c7e59ffe64aced756ad0bdd0a4
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Aug 1 14:40:10 2011 -0700

    Simplify map example

diff --git a/docs/config/index.rst b/docs/config/index.rst
index 24c0f9255e..31da68820a 100644
--- a/docs/config/index.rst
+++ b/docs/config/index.rst
@@ -56,7 +56,7 @@ Older Versions
 
     logger = logging.getLogger()
     # ensure we havent already registered the handler
-    if SentryHandler not in map(lambda x: x.__class__, logger.handlers):
+    if SentryHandler not in map(type, logger.handlers):
         logger.addHandler(SentryHandler())
 
         # Add StreamHandler to sentry's default so you can catch missed exceptions
