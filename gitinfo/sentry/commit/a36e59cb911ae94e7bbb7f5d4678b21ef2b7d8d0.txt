commit a36e59cb911ae94e7bbb7f5d4678b21ef2b7d8d0
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Sep 2 21:33:49 2014 -0700

    dont send an empty value for timeout

diff --git a/src/sentry/cache/redis.py b/src/sentry/cache/redis.py
index 528e5e6da0..496c429946 100644
--- a/src/sentry/cache/redis.py
+++ b/src/sentry/cache/redis.py
@@ -36,7 +36,8 @@ class RedisCache(local):
     def set(self, key, value, timeout):
         with self.conn.map() as conn:
             conn.set(key, json.dumps(value))
-            conn.expire(key, timeout)
+            if timeout:
+                conn.expire(key, timeout)
 
     def delete(self, key):
         self.conn.delete(key)
