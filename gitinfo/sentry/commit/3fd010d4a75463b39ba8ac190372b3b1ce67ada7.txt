commit 3fd010d4a75463b39ba8ac190372b3b1ce67ada7
Author: Danny Lee <dlee@sentry.io>
Date:   Tue Sep 3 15:21:47 2019 -0700

    fix(ui): Fix Pill component creating bad links for tags with spaces (#14565)

diff --git a/src/sentry/static/sentry/app/components/events/eventTags.tsx b/src/sentry/static/sentry/app/components/events/eventTags.tsx
index 6ac2f96a5e..a8bceae57b 100644
--- a/src/sentry/static/sentry/app/components/events/eventTags.tsx
+++ b/src/sentry/static/sentry/app/components/events/eventTags.tsx
@@ -42,7 +42,10 @@ class EventTags extends React.Component<EventTagsProps> {
         query.project = tag.value;
         break;
       default:
-        query.query = `${query.query} ${tag.key}:${tag.value}`;
+        query.query =
+          tag.value.indexOf(' ') > -1
+            ? `${query.query} ${tag.key}:"${tag.value}"`
+            : `${query.query} ${tag.key}:${tag.value}`;
     }
 
     const locationSearch = `?${queryString.stringify(query)}`;
