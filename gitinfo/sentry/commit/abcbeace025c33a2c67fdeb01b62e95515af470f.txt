commit abcbeace025c33a2c67fdeb01b62e95515af470f
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Feb 14 23:22:58 2012 -0800

    Remove LOG_DIR and RUN_DIR settings

diff --git a/sentry/conf/defaults.py b/sentry/conf/defaults.py
index 67f7416d72..5e281d7519 100644
--- a/sentry/conf/defaults.py
+++ b/sentry/conf/defaults.py
@@ -93,12 +93,6 @@ MINUTE_NORMALIZATION = 15
 # The number of events to display per page
 MESSAGES_PER_PAGE = 15
 
-# Directory to stash log files in
-LOG_DIR = os.path.join(ROOT, 'log')
-
-# Directory to stash pid files in
-RUN_DIR = os.path.join(ROOT, 'run')
-
 # Web Service
 WEB_HOST = 'localhost'
 WEB_PORT = 9000
diff --git a/sentry/management/commands/start.py b/sentry/management/commands/start.py
index 29cddb4f6c..ff148a3efd 100644
--- a/sentry/management/commands/start.py
+++ b/sentry/management/commands/start.py
@@ -10,11 +10,8 @@ from django.core.management.base import BaseCommand, CommandError
 
 from optparse import make_option
 
-from sentry.conf import settings
 from sentry.services import http, worker, udp
 
-import os
-
 services = {
     'http': http.SentryHTTPServer,
     'worker': worker.SentryWorker,
@@ -34,12 +31,6 @@ class Command(BaseCommand):
     )
 
     def handle(self, service_name='http', **options):
-        if not os.path.exists(settings.LOG_DIR):
-            os.makedirs(settings.LOG_DIR)
-
-        if not os.path.exists(settings.RUN_DIR):
-            os.makedirs(settings.RUN_DIR)
-
         # Ensure we perform an upgrade before starting any service
         print "Performing upgrade before service startup..."
         call_command('upgrade', verbosity=0)
diff --git a/sentry/utils/runner.py b/sentry/utils/runner.py
index 08aee064d7..e0ee2c4b91 100644
--- a/sentry/utils/runner.py
+++ b/sentry/utils/runner.py
@@ -34,8 +34,6 @@ SENTRY_PUBLIC = True
 
 SENTRY_WEB_HOST = '0.0.0.0'
 SENTRY_WEB_PORT = 9000
-SENTRY_LOG_DIR = os.path.abspath(os.path.join(ROOT, 'log'))
-SENTRY_RUN_DIR = os.path.abspath(os.path.join(ROOT, 'run'))
 """
 
 
