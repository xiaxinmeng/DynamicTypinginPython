commit 1e61625bb3f2f4c90ab61b5b99c0b5b7eb8b420f
Author: Markus Unterwaditzer <markus@unterwaditzer.net>
Date:   Wed Jan 30 18:05:14 2019 +0100

    ref: Split up event size metrics by rust vs python (#11796)
    
    * ref: Split up event size metrics by rust vs python
    
    * fix: Defensive access of event data

diff --git a/src/sentry/tasks/post_process.py b/src/sentry/tasks/post_process.py
index f35522289a..a1258827f8 100644
--- a/src/sentry/tasks/post_process.py
+++ b/src/sentry/tasks/post_process.py
@@ -45,7 +45,10 @@ def _capture_stats(event, is_new):
     if not platform:
         return
     platform = platform.split('-', 1)[0].split('_', 1)[0]
-    tags = {'platform': platform}
+    tags = {
+        'platform': platform,
+        'use_rust_normalize': event.data.get('use_rust_normalize', 'unknown')
+    }
 
     if is_new:
         metrics.incr('events.unique', tags=tags, skip_internal=False)
