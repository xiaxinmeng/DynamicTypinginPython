commit e9eab2f9efa226c0f404abc4188c5bc2417684ed
Author: Matej Minar <matej.minar@sentry.io>
Date:   Wed May 20 13:21:59 2020 +0200

    feat(ui): Adjust grouping info hint size (#18931)

diff --git a/src/sentry/static/sentry/app/components/events/groupingInfo/groupingComponent.tsx b/src/sentry/static/sentry/app/components/events/groupingInfo/groupingComponent.tsx
index 3c5465fa52..a5896fce19 100644
--- a/src/sentry/static/sentry/app/components/events/groupingInfo/groupingComponent.tsx
+++ b/src/sentry/static/sentry/app/components/events/groupingInfo/groupingComponent.tsx
@@ -25,7 +25,7 @@ const GroupingComponent = ({component, showNonContributing}: Props) => {
     <GroupingComponentWrapper isContributing={component.contributes}>
       <span>
         {component.name || component.id}
-        {component.hint && <small>{` (${component.hint})`}</small>}
+        {component.hint && <GroupingHint>{` (${component.hint})`}</GroupingHint>}
       </span>
 
       <GroupingComponentList isInline={shouldInlineValue}>
@@ -77,4 +77,8 @@ const GroupingComponentWrapper = styled('div')<{isContributing: boolean}>`
   }
 `;
 
+const GroupingHint = styled('small')`
+  font-size: 0.8em;
+`;
+
 export default GroupingComponent;
