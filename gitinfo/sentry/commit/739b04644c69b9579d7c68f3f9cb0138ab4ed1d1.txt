commit 739b04644c69b9579d7c68f3f9cb0138ab4ed1d1
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Feb 19 19:35:39 2013 -0800

    Add link to notification settings in error emails

diff --git a/src/sentry/plugins/sentry_mail/models.py b/src/sentry/plugins/sentry_mail/models.py
index 1a93ed3973..22995c0c61 100644
--- a/src/sentry/plugins/sentry_mail/models.py
+++ b/src/sentry/plugins/sentry_mail/models.py
@@ -197,6 +197,8 @@ class MailProcessor(NotificationPlugin):
             'event': event,
             'link': link,
             'interfaces': interface_list,
+            'settings_link': '%s%s' % (settings.URL_PREFIX,
+                reverse('sentry-account-settings-notifications')),
         })).run()
 
     def get_option(self, key, *args, **kwargs):
diff --git a/src/sentry/templates/sentry/emails/error.html b/src/sentry/templates/sentry/emails/error.html
index b2368249ad..44776b1007 100644
--- a/src/sentry/templates/sentry/emails/error.html
+++ b/src/sentry/templates/sentry/emails/error.html
@@ -48,4 +48,6 @@
         <pre>{{ text }}</pre>
     </div>
     {% endfor %}
+
+    <p><a href="{{ settings_link }}">Notification Settings</a></p>
 {% endblock %}
