commit a86b3139668adc70322fa7dbd0adca4bed9fc143
Author: Billy Vong <billyvg@users.noreply.github.com>
Date:   Mon Aug 5 12:46:18 2019 -0700

    ref(ui): Remove `grid-emotion` from `<FeatureDisabledMessage>` (#14273)

diff --git a/src/sentry/static/sentry/app/components/acl/featureDisabled.jsx b/src/sentry/static/sentry/app/components/acl/featureDisabled.jsx
index eaa1f7f2ae..b63064cf00 100644
--- a/src/sentry/static/sentry/app/components/acl/featureDisabled.jsx
+++ b/src/sentry/static/sentry/app/components/acl/featureDisabled.jsx
@@ -1,4 +1,3 @@
-import {Box, Flex} from 'grid-emotion';
 import PropTypes from 'prop-types';
 import React from 'react';
 import styled from 'react-emotion';
@@ -74,7 +73,7 @@ class FeatureDisabled extends React.Component {
 
     const featureDisabled = (
       <React.Fragment>
-        <Flex justify="space-between" data-test-id="feature-message">
+        <FeatureDisabledMessage>
           {message}
           {!hideHelpToggle && (
             <HelpButton
@@ -86,7 +85,7 @@ class FeatureDisabled extends React.Component {
               {t('Help')}
             </HelpButton>
           )}
-        </Flex>
+        </FeatureDisabledMessage>
         {showDescription && (
           <HelpDescription>
             <p>
@@ -121,11 +120,16 @@ class FeatureDisabled extends React.Component {
   }
 }
 
+const FeatureDisabledMessage = styled('div')`
+  display: flex;
+  justify-content: space-between;
+`;
+
 const HelpButton = styled(Button)`
   font-size: 0.8em;
 `;
 
-const HelpDescription = styled(Box)`
+const HelpDescription = styled('div')`
   font-size: 0.9em;
   margin-top: ${space(1)};
 
diff --git a/tests/js/spec/components/acl/featureDisabled.spec.jsx b/tests/js/spec/components/acl/featureDisabled.spec.jsx
index 2fc82806bd..cb7054d95d 100644
--- a/tests/js/spec/components/acl/featureDisabled.spec.jsx
+++ b/tests/js/spec/components/acl/featureDisabled.spec.jsx
@@ -18,7 +18,7 @@ describe('FeatureDisabled', function() {
 
     expect(
       wrapper
-        .find('[data-test-id="feature-message"]')
+        .find('FeatureDisabledMessage')
         .first()
         .text()
     ).toEqual(
@@ -40,7 +40,7 @@ describe('FeatureDisabled', function() {
 
     expect(
       wrapper
-        .find('[data-test-id="feature-message"]')
+        .find('FeatureDisabledMessage')
         .first()
         .text()
     ).toEqual(expect.stringContaining(customMessage));
