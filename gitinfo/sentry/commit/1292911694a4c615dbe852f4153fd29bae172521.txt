commit 1292911694a4c615dbe852f4153fd29bae172521
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Mar 3 15:54:11 2015 -0800

    Move failSilently logic into alert handler

diff --git a/src/sentry/static/sentry/app/api.jsx b/src/sentry/static/sentry/app/api.jsx
index 844420f24d..988d1d15f3 100644
--- a/src/sentry/static/sentry/app/api.jsx
+++ b/src/sentry/static/sentry/app/api.jsx
@@ -84,10 +84,7 @@ class Client {
         GroupActions.updateSuccess(id, params.itemIds, response);
       },
       error: (error) => {
-        if (params.failSilently) {
-          return;
-        }
-        GroupActions.updateError(id, params.itemIds, error);
+        GroupActions.updateError(id, params.itemIds, error, params.failSilently);
       }
     });
   }
diff --git a/src/sentry/static/sentry/app/stores/groupStore.jsx b/src/sentry/static/sentry/app/stores/groupStore.jsx
index d3f1408614..9514c2f643 100644
--- a/src/sentry/static/sentry/app/stores/groupStore.jsx
+++ b/src/sentry/static/sentry/app/stores/groupStore.jsx
@@ -193,12 +193,14 @@ var GroupListStore = Reflux.createStore({
     this.trigger();
   },
 
-  onUpdateError(changeId, itemIds, error) {
+  onUpdateError(changeId, itemIds, error, failSilently) {
     this.pendingChanges.remove(changeId);
     itemIds.forEach(itemId => {
       this.clearStatus(itemId, 'update');
     });
-    AlertActions.addAlert(ERR_UPDATE, 'error');
+    if (failSilently) {
+      AlertActions.addAlert(ERR_UPDATE, 'error');
+    }
     this.trigger();
   },
 
