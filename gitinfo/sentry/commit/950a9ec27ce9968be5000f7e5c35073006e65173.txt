commit 950a9ec27ce9968be5000f7e5c35073006e65173
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Feb 7 18:02:35 2013 -0800

    Revert "Dont pull in context without pre_context or post_context being set"
    
    This reverts commit 8ec022ec40ec800b84c0d40fef182d0829664aa8.

diff --git a/src/sentry/interfaces.py b/src/sentry/interfaces.py
index bb0bdf0f54..dcbb0d420e 100644
--- a/src/sentry/interfaces.py
+++ b/src/sentry/interfaces.py
@@ -390,9 +390,7 @@ class Stacktrace(Interface):
         system_frames = 0
         frames = []
         for frame in self.frames:
-            if (frame.get('context_line') and frame.get('lineno') is not None
-                and (frame.get('pre_context') or frame.get('post_context'))):
-
+            if frame.get('context_line') and frame.get('lineno') is not None:
                 context = get_context(
                     lineno=frame['lineno'],
                     context_line=frame['context_line'],
