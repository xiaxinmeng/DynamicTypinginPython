commit cce49a9d35321eeeacacd2b0a46f93213a9a1a35
Author: ted kaemming <t.kaemming+github@gmail.com>
Date:   Fri Dec 18 17:53:41 2015 -0800

    Clean up digest Redis backend instantiation.
    
    - Validating to ensure no extra options are provided to `RedisBackend` results
      in a false positive warning since mutations to keyword arguments provided to
      the superclass are not reflected in `**kwargs` passed by the subclass.
    - Drop the Redis validation message log level to DEBUG.

diff --git a/src/sentry/digests/backends/redis.py b/src/sentry/digests/backends/redis.py
index 0424f85ff9..71a6d9c46d 100644
--- a/src/sentry/digests/backends/redis.py
+++ b/src/sentry/digests/backends/redis.py
@@ -144,11 +144,8 @@ class RedisBackend(Backend):
         # too early.
         self.ttl = options.pop('ttl', 60 * 60)
 
-        if options:
-            logger.warning('Discarding invalid options: %r', options)
-
     def validate(self):
-        logger.info('Validating Redis version...')
+        logger.debug('Validating Redis version...')
         check_cluster_versions(
             self.cluster,
             Version((2, 8, 9)),
