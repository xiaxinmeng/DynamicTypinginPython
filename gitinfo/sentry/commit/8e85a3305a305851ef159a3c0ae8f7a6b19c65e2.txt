commit 8e85a3305a305851ef159a3c0ae8f7a6b19c65e2
Author: Dan Fuller <dfuller@sentry.io>
Date:   Fri Apr 5 12:50:56 2019 -0700

    refs: Remove MySQL test run
    
    We no longer support MySQL or sqlite. We rely on the sqlite tests to handle riak, so leaving them in
    place for now.

diff --git a/.travis.yml b/.travis.yml
index c0ad00e00b..405395a02a 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -151,22 +151,6 @@ matrix:
       before_script:
         - psql -c 'create database sentry;' -U postgres
 
-    - python: 2.7
-      name: 'Backend [MySQL]'
-      env: TEST_SUITE=mysql DB=mysql
-      services:
-        - memcached
-        - mysql
-        - redis-server
-      install:
-        - python setup.py install_egg_info
-        - pip install -e ".[dev,tests,optional]"
-        # 1.3.14 causes test failures. Pinning to 1.3.13 for now, hopefully
-        # a later release resolves this.
-        - pip install mysqlclient==1.3.13
-      before_script:
-        - mysql -u root -e 'create database sentry;'
-
     - python: 2.7
       name: 'Acceptance'
       env: TEST_SUITE=acceptance USE_SNUBA=1
