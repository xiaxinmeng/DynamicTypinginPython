commit 644dbe519f9345f37130ba9e0bea1e3e5326c557
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Apr 2 22:44:29 2013 -0700

    Clarify that SENTRY_URL_PREFIX must be configured

diff --git a/src/sentry/utils/runner.py b/src/sentry/utils/runner.py
index 1acd2d24b7..a0bc527c3b 100644
--- a/src/sentry/utils/runner.py
+++ b/src/sentry/utils/runner.py
@@ -74,9 +74,8 @@ DATABASES = {
 
 SENTRY_KEY = %(default_key)r
 
-# You should configure the absolute URI to Sentry. It will attempt to guess it if you don't
-# but proxies may interfere with this.
-# SENTRY_URL_PREFIX = 'http://sentry.example.com'  # No trailing slash!
+# You MUST configure the absolute URI root for Sentry:
+SENTRY_URL_PREFIX = 'http://sentry.example.com'  # No trailing slash!
 
 SENTRY_WEB_HOST = '0.0.0.0'
 SENTRY_WEB_PORT = 9000
