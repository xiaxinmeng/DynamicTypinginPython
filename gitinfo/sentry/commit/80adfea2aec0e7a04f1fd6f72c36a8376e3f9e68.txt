commit 80adfea2aec0e7a04f1fd6f72c36a8376e3f9e68
Author: Lyn Nagara <lyn.nagara@gmail.com>
Date:   Wed Jan 22 10:38:21 2020 -0800

    chore: Remove nodedata warning (#16555)
    
    Remove the warning since nodedata no longer needs to be populated before
    access, we now load lazily in most (non get_multi) cases.

diff --git a/src/sentry/db/models/fields/node.py b/src/sentry/db/models/fields/node.py
index 1a7fb7424e..6583eee156 100644
--- a/src/sentry/db/models/fields/node.py
+++ b/src/sentry/db/models/fields/node.py
@@ -4,7 +4,6 @@ from base64 import b64encode
 import collections
 import logging
 import six
-import warnings
 from uuid import uuid4
 
 from django.db.models.signals import post_delete
@@ -108,7 +107,6 @@ class NodeData(collections.MutableMapping):
             return self._node_data
 
         elif self.id:
-            warnings.warn("You should populate node data before accessing it.")
             self.bind_data(nodestore.get(self.id) or {})
             return self._node_data
 
