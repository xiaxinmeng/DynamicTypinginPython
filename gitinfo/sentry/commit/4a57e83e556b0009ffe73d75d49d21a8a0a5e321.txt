commit 4a57e83e556b0009ffe73d75d49d21a8a0a5e321
Author: William Mak <william@wmak.io>
Date:   Tue Feb 18 11:45:37 2020 -0500

    fix(discover) - Error states not going away (#17013)
    
    * fix(discover) - Error states not going away
    
    - This removes error states if a new correct query is used instead

diff --git a/src/sentry/static/sentry/app/views/events/utils/eventsRequest.tsx b/src/sentry/static/sentry/app/views/events/utils/eventsRequest.tsx
index fb666eef4f..7f5887e880 100644
--- a/src/sentry/static/sentry/app/views/events/utils/eventsRequest.tsx
+++ b/src/sentry/static/sentry/app/views/events/utils/eventsRequest.tsx
@@ -211,6 +211,7 @@ class EventsRequest extends React.PureComponent<EventsRequestProps, EventsReques
 
     this.setState(state => ({
       reloading: state.timeseriesData !== null,
+      errored: false,
     }));
 
     try {
diff --git a/src/sentry/static/sentry/app/views/eventsV2/table/index.tsx b/src/sentry/static/sentry/app/views/eventsV2/table/index.tsx
index 53a7da8905..1d032424a4 100644
--- a/src/sentry/static/sentry/app/views/eventsV2/table/index.tsx
+++ b/src/sentry/static/sentry/app/views/eventsV2/table/index.tsx
@@ -18,7 +18,7 @@ type TableProps = {
   eventView: EventView;
   organization: Organization;
   tags: {[key: string]: Tag};
-  setError: (msg: string) => void;
+  setError: (msg: string | undefined) => void;
   title: string;
 };
 
@@ -79,6 +79,7 @@ class Table extends React.PureComponent<TableProps, TableState> {
     const url = `/organizations/${organization.slug}/eventsv2/`;
     const tableFetchID = Symbol('tableFetchID');
     const apiPayload = eventView.getEventsAPIPayload(location);
+    setError(undefined);
 
     this.setState({isLoading: true, tableFetchID});
 
