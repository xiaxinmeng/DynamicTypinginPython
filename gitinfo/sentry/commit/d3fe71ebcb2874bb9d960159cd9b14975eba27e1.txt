commit d3fe71ebcb2874bb9d960159cd9b14975eba27e1
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Mon Aug 31 14:52:36 2015 -0700

    Make sure headers is a list in replay_event

diff --git a/src/sentry/interfaces/http.py b/src/sentry/interfaces/http.py
index aa8b5e0adc..b67145b748 100644
--- a/src/sentry/interfaces/http.py
+++ b/src/sentry/interfaces/http.py
@@ -132,7 +132,7 @@ class Http(Interface):
             if not cookies and cookie_header:
                 cookies = cookie_header
         else:
-            headers = {}
+            headers = ()
 
         body = data.get('data')
         if isinstance(body, dict):
diff --git a/src/sentry/web/frontend/events.py b/src/sentry/web/frontend/events.py
index fd901a5f2c..32d22d6c8a 100644
--- a/src/sentry/web/frontend/events.py
+++ b/src/sentry/web/frontend/events.py
@@ -39,7 +39,7 @@ def replay_event(request, organization, project, group, event_id):
     # TODO(mattrobenolt): Add Cookie as a header
     http = interfaces['sentry.interfaces.Http']
     if http.headers:
-        headers = '\n'.join('%s: %s' % (k, v) for k, v in http.headers.iteritems() if k[0].upper() == k[0])
+        headers = '\n'.join('%s: %s' % (k, v) for k, v in http.headers if k[0].upper() == k[0])
     else:
         headers = ''
 
