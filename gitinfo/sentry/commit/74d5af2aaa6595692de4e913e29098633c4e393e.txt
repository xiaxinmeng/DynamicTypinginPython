commit 74d5af2aaa6595692de4e913e29098633c4e393e
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Tue Sep 27 10:20:28 2016 -0700

    Remove trailing command from releasefile cache key
    
    This causes cache_key to be a tuple instead of a string.
    
    In reality, this is fine, it just geenrates weird cache key names like:
    
    :1:('releasefile:v1:1839585:b4b02b1e1406fc0d84a91fd028d1d934',)

diff --git a/src/sentry/lang/javascript/processor.py b/src/sentry/lang/javascript/processor.py
index eec1e9d14c..40181a88fa 100644
--- a/src/sentry/lang/javascript/processor.py
+++ b/src/sentry/lang/javascript/processor.py
@@ -218,7 +218,7 @@ def fetch_release_file(filename, release):
     cache_key = 'releasefile:v1:%s:%s' % (
         release.id,
         md5_text(filename).hexdigest(),
-    ),
+    )
 
     filename_path = None
     if filename is not None:
