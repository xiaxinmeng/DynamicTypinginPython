commit 395958c6bca69daacf939a191e097518fa4c4667
Author: Evan Purkhiser <evanpurkhiser@gmail.com>
Date:   Tue Mar 24 10:18:47 2020 -0700

    chore(ts): Convert utils/promptsActivity (#17862)

diff --git a/src/sentry/static/sentry/app/utils/promptsActivity.jsx b/src/sentry/static/sentry/app/utils/promptsActivity.jsx
deleted file mode 100644
index 6b00844503..0000000000
--- a/src/sentry/static/sentry/app/utils/promptsActivity.jsx
+++ /dev/null
@@ -1,13 +0,0 @@
-import moment from 'moment';
-
-/**
- * Given a snoozed unix timestamp in seconds,
- * returns the number of days since the prompt was snoozed
- *
- * @param {Number} snoozedTs Snoozed timestamp
- */
-export function snoozedDays(snoozedTs) {
-  const now = moment.utc();
-  const snoozedDay = moment.unix(snoozedTs).utc();
-  return now.diff(snoozedDay, 'days');
-}
diff --git a/src/sentry/static/sentry/app/utils/promptsActivity.tsx b/src/sentry/static/sentry/app/utils/promptsActivity.tsx
new file mode 100644
index 0000000000..b14e7cded3
--- /dev/null
+++ b/src/sentry/static/sentry/app/utils/promptsActivity.tsx
@@ -0,0 +1,13 @@
+import moment from 'moment';
+
+/**
+ * Given a snoozed unix timestamp in seconds, returns the number of days since
+ * the prompt was snoozed.
+ *
+ * @param snoozedTs Snoozed timestamp
+ */
+export function snoozedDays(snoozedTs: number) {
+  const now = moment.utc();
+  const snoozedDay = moment.unix(snoozedTs).utc();
+  return now.diff(snoozedDay, 'days');
+}
