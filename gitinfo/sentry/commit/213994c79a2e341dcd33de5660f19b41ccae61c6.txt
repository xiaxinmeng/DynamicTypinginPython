commit 213994c79a2e341dcd33de5660f19b41ccae61c6
Author: Armin Ronacher <armin.ronacher@active-4.com>
Date:   Thu Jun 23 08:43:34 2016 +0200

    Add battery level, orientation and root status

diff --git a/src/sentry/static/sentry/app/components/events/contexts/device.jsx b/src/sentry/static/sentry/app/components/events/contexts/device.jsx
index 951c316df0..5c353bcab3 100644
--- a/src/sentry/static/sentry/app/components/events/contexts/device.jsx
+++ b/src/sentry/static/sentry/app/components/events/contexts/device.jsx
@@ -1,6 +1,7 @@
 import React from 'react';
 
 import ContextBlock from './contextBlock';
+import {defined} from '../../../utils';
 
 const DeviceContextType = React.createClass({
   propTypes: {
@@ -9,14 +10,18 @@ const DeviceContextType = React.createClass({
   },
 
   render() {
-    let {name, model, model_id, arch, ...data} = this.props.data;
+    let {name, model, model_id, arch, battery_level, orientation,
+      ...data} = this.props.data;
     return (
       <ContextBlock
         data={data}
         knownData={[
           ['Name', name],
           ['Model', model + (model_id ? ` (${model_id})` : '')],
-          ['Architecture', arch]
+          ['Architecture', arch],
+          ['Battery Level', defined(battery_level)
+            ? `${battery_level}%` : null],
+          ['Orientation', orientation],
         ]}
         alias={this.props.alias} />
     );
diff --git a/src/sentry/static/sentry/app/components/events/contexts/os.jsx b/src/sentry/static/sentry/app/components/events/contexts/os.jsx
index c761850c6f..a247c9d7f2 100644
--- a/src/sentry/static/sentry/app/components/events/contexts/os.jsx
+++ b/src/sentry/static/sentry/app/components/events/contexts/os.jsx
@@ -1,6 +1,7 @@
 import React from 'react';
 
 import ContextBlock from './contextBlock';
+import {defined} from '../../../utils';
 
 const OsContextType = React.createClass({
   propTypes: {
@@ -9,7 +10,7 @@ const OsContextType = React.createClass({
   },
 
   render() {
-    let {name, version, build, kernel_version, ...data} = this.props.data;
+    let {name, version, build, kernel_version, rooted, ...data} = this.props.data;
     return (
       <ContextBlock
         data={data}
@@ -17,6 +18,7 @@ const OsContextType = React.createClass({
           ['Name', name],
           ['Version', version + (build ? ` (${build})` : '')],
           ['Kernel Version', kernel_version],
+          ['Rooted', defined(rooted) ? (rooted ? 'yes' : 'no') : null],
         ]}
         alias={this.props.alias} />
     );
