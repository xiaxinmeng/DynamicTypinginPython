commit 9aeb16ff89a45bfd6b1fa9a0a754577dbe71cacd
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Wed Oct 7 02:48:55 2015 -0700

    Make sure we run latest pip

diff --git a/.travis.yml b/.travis.yml
index 99c7517620..60fb318758 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -12,7 +12,7 @@ python:
 cache:
   directories:
     - node_modules
-    - .pip_download_cache
+    - $HOME/.cache/pip
     - "$HOME/virtualenv/python2.7.9"
 env:
   matrix:
@@ -20,10 +20,10 @@ env:
     - DB=mysql
     - DB=postgres
   global:
-    - PIP_DOWNLOAD_CACHE=".pip_download_cache"
     - SENTRY_LIGHT_BUILD=1
     - SKIP_BACKEND_VALIDATION=1
 install:
+  - python -m pip install --upgrade pip==7.1.2
   - time make develop dev-postgres dev-mysql
 before_script:
   - mysql -e 'create database sentry;'
