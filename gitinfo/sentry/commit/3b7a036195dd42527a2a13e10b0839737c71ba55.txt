commit 3b7a036195dd42527a2a13e10b0839737c71ba55
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Sep 9 17:25:46 2015 -0500

    Remove date restriction on tags

diff --git a/src/sentry/models/grouptagvalue.py b/src/sentry/models/grouptagvalue.py
index 79950f3d3f..2f8b291207 100644
--- a/src/sentry/models/grouptagvalue.py
+++ b/src/sentry/models/grouptagvalue.py
@@ -64,7 +64,7 @@ class GroupTagValue(Model):
                     FROM sentry_messagefiltervalue
                     WHERE group_id = %s
                     AND key = %s
-                    AND last_seen > NOW() - INTERVAL '7 days'
+                    ORDER BY last_seen DESC
                     LIMIT 10000
                 ) as a
             """, [group_id, key])
@@ -89,7 +89,7 @@ class GroupTagValue(Model):
                     FROM sentry_messagefiltervalue
                     WHERE group_id = %%s
                     AND key = %%s
-                    AND last_seen > NOW() - INTERVAL '7 days'
+                    ORDER BY last_seen DESC
                     LIMIT 10000
                 ) as a
                 ORDER BY times_seen DESC
diff --git a/src/sentry/static/sentry/app/views/groupTags.jsx b/src/sentry/static/sentry/app/views/groupTags.jsx
index 320cab2829..878f39adf3 100644
--- a/src/sentry/static/sentry/app/views/groupTags.jsx
+++ b/src/sentry/static/sentry/app/views/groupTags.jsx
@@ -119,11 +119,8 @@ var GroupTags = React.createClass({
     }
 
     return (
-      <div>
-        <h5>Data is based on events seen in the last 7 days.</h5>
-        <div className="row">
-          {children}
-        </div>
+      <div className="row">
+        {children}
       </div>
     );
   }
