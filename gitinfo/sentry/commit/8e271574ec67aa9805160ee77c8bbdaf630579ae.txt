commit 8e271574ec67aa9805160ee77c8bbdaf630579ae
Author: Markus Unterwaditzer <markus@unterwaditzer.net>
Date:   Wed Mar 25 21:06:42 2020 +0100

    fix: Fix Relay usage for users of ALLOWED_HOSTS (#17900)
    
    Forward the Host header so ALLOWED_HOSTS actually works (and does not
    require the user to add "sentry" explicitly)
    
    This still requires the user to add `host.docker.internal` explicitly,
    which we can unlikely fix.

diff --git a/config/reverse_proxy/nginx.conf b/config/reverse_proxy/nginx.conf
index 013af061d4..536d2d0198 100644
--- a/config/reverse_proxy/nginx.conf
+++ b/config/reverse_proxy/nginx.conf
@@ -63,5 +63,7 @@ http {
         location / {
            proxy_pass http://sentry;
         }
+
+        proxy_set_header Host $host;
     }
 }
