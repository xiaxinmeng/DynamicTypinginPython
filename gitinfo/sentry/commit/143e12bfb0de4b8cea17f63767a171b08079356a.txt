commit 143e12bfb0de4b8cea17f63767a171b08079356a
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Tue Aug 22 17:17:09 2017 -0700

    buffers: remove a dead code path

diff --git a/src/sentry/buffer/base.py b/src/sentry/buffer/base.py
index ebaafb77f1..37eb06c273 100644
--- a/src/sentry/buffer/base.py
+++ b/src/sentry/buffer/base.py
@@ -61,13 +61,6 @@ class Buffer(Service):
         if extra:
             update_kwargs.update(extra)
 
-        # TODO(mattrobenolt): Remove in 8.18
-        if model.__name__ == 'GroupTagValue':
-            try:
-                update_kwargs['project_id'] = update_kwargs.pop('project')
-            except KeyError:
-                pass
-
         _, created = model.objects.create_or_update(values=update_kwargs, **filters)
 
         buffer_incr_complete.send_robust(
