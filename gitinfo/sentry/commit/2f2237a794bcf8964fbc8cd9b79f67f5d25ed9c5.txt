commit 2f2237a794bcf8964fbc8cd9b79f67f5d25ed9c5
Author: Mark Story <mark@sentry.io>
Date:   Thu Feb 13 12:48:06 2020 -0500

    fix(ui) Fix error in sidebar (#17012)
    
    Sometimes `organization.features` is undefined. It shouldn't be but it
    is, and we have to account for that.
    
    Fixes JAVASCRIPT-21G1

diff --git a/src/sentry/static/sentry/app/components/sidebar/index.jsx b/src/sentry/static/sentry/app/components/sidebar/index.jsx
index 604159b28c..a82e3581e5 100644
--- a/src/sentry/static/sentry/app/components/sidebar/index.jsx
+++ b/src/sentry/static/sentry/app/components/sidebar/index.jsx
@@ -240,7 +240,7 @@ class Sidebar extends React.Component {
     };
 
     // Bail as we can't do any more checks.
-    if (!organization) {
+    if (!organization || !organization.features) {
       return sidebarState;
     }
     const optState = localStorage.getItem('discover:version');
