commit 27c93c81bb9da7dc5c01a6184835da6e54aa19e7
Author: ted kaemming <ted@kaemming.com>
Date:   Thu Apr 26 15:12:06 2018 -0700

    Revert "ref(services): Log all exceptions thrown in executor (#8148)" (#8223)
    
    This reverts commit 71983fac8303653b65c3bd845f2c7dd900d5ac48.

diff --git a/src/sentry/utils/services.py b/src/sentry/utils/services.py
index 3658523b72..5870feeb30 100644
--- a/src/sentry/utils/services.py
+++ b/src/sentry/utils/services.py
@@ -288,10 +288,6 @@ class ServiceDelegator(Service):
                 active_backends[base] = backend
                 try:
                     return getattr(backend, attribute_name)(*args, **kwargs)
-                except Exception as error:
-                    logger.warning('Caught %s in executor while calling %r on %r: %s',
-                                   type(error).__name__, attribute_name, backend, error, exc_info=True)
-                    raise
                 finally:
                     # Unmark the backend as active.
                     assert active_backends[base] is backend
