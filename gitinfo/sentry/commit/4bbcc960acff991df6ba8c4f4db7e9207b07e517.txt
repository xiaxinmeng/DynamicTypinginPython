commit 4bbcc960acff991df6ba8c4f4db7e9207b07e517
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Aug 21 00:31:32 2014 -0700

    Change tag conditions to base case insensitive

diff --git a/src/sentry/rules/conditions/tagged_event.py b/src/sentry/rules/conditions/tagged_event.py
index 4ff3d7502f..5c69e89871 100644
--- a/src/sentry/rules/conditions/tagged_event.py
+++ b/src/sentry/rules/conditions/tagged_event.py
@@ -47,7 +47,9 @@ class TaggedEventCondition(EventCondition):
         if not (key and match and value):
             return False
 
-        tags = (v for k, v in event.get_tags() if k == key)
+        value = value.lower()
+
+        tags = (v.lower() for k, v in event.get_tags() if k == key)
 
         if match == MatchType.EQUAL:
             for t_value in tags:
