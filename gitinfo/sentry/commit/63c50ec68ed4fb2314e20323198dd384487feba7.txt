commit 63c50ec68ed4fb2314e20323198dd384487feba7
Author: adhiraj <693121+adhiraj@users.noreply.github.com>
Date:   Wed Apr 25 18:04:18 2018 -0700

    feat(orgdash): record pageview and clicks on orgdash resources (#8189)

diff --git a/src/sentry/static/sentry/app/components/resourceCard.jsx b/src/sentry/static/sentry/app/components/resourceCard.jsx
index 4138427027..13b5f9f1a7 100644
--- a/src/sentry/static/sentry/app/components/resourceCard.jsx
+++ b/src/sentry/static/sentry/app/components/resourceCard.jsx
@@ -4,6 +4,7 @@ import styled from 'react-emotion';
 
 import ConfigStore from '../stores/configStore';
 import ExternalLink from '../components/externalLink';
+import HookStore from '../stores/hookStore';
 
 const StyledTitle = styled('div')`
   color: #493e54;
@@ -19,12 +20,23 @@ export default class ResourceCard extends React.Component {
     imgUrl: PropTypes.string.isRequired,
   };
 
+  recordClick = () => {
+    let {link, title} = this.props;
+    HookStore.get('analytics:event').forEach(cb =>
+      cb('orgdash.resource_clicked', {link, title})
+    );
+  };
+
   render() {
     const mediaUrl = ConfigStore.get('mediaUrl');
     let {title, link, imgUrl} = this.props;
 
     return (
-      <div className="flex box p-x-2 p-y-1" style={{flexGrow: '1', alignItems: 'center'}}>
+      <div
+        className="flex box p-x-2 p-y-1"
+        style={{flexGrow: '1', alignItems: 'center'}}
+        onClick={this.recordClick}
+      >
         <ExternalLink href={link}>
           <div className="m-b-1">
             <img src={mediaUrl + imgUrl} alt={title} />
diff --git a/src/sentry/static/sentry/app/views/organizationDashboard/resources.jsx b/src/sentry/static/sentry/app/views/organizationDashboard/resources.jsx
index fa842e08ff..b1fd114fbf 100644
--- a/src/sentry/static/sentry/app/views/organizationDashboard/resources.jsx
+++ b/src/sentry/static/sentry/app/views/organizationDashboard/resources.jsx
@@ -5,6 +5,7 @@ import SentryTypes from '../../proptypes';
 import ErrorRobot from '../../components/errorRobot';
 import {Panel} from '../../components/panels';
 import {t} from '../../locale';
+import HookStore from '../../stores/hookStore';
 
 export default class Resources extends React.Component {
   static propTypes = {
@@ -12,6 +13,10 @@ export default class Resources extends React.Component {
     project: SentryTypes.Project,
   };
 
+  componentDidMount() {
+    HookStore.get('analytics:event').forEach(cb => cb('orgdash.resources_shown'));
+  }
+
   render() {
     return (
       <React.Fragment>
