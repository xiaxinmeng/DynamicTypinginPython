commit cb358fec41c4b1a6432bf8d2a345e289b06b6361
Author: MeredithAnya <meredith.a.heller@gmail.com>
Date:   Mon Oct 2 15:19:40 2017 -0700

    check the user id against request (#6237)
    
    * check the user id against request
    
    * add message

diff --git a/src/sentry/web/frontend/accept_project_transfer.py b/src/sentry/web/frontend/accept_project_transfer.py
index f2a8e59231..6fb5ef1896 100644
--- a/src/sentry/web/frontend/accept_project_transfer.py
+++ b/src/sentry/web/frontend/accept_project_transfer.py
@@ -67,6 +67,14 @@ class AcceptProjectTransferView(BaseView):
         user_id = data['user_id']
         transaction_id = data['transaction_id']
         from_organization_id = data['from_organization_id']
+        if user_id != request.user.id:
+            messages.add_message(
+                request, messages.ERROR,
+                _(u'Invalid permissions!')
+            )
+            return HttpResponseRedirect(
+                reverse('sentry')
+            )
 
         # check if user is still an owner
         if not OrganizationMember.objects.filter(
