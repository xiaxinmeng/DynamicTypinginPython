commit d2791de0c85c130bfa48549dc5538d8afe2f677b
Author: Alex Hofsteede <alex@hofsteede.com>
Date:   Thu Apr 12 16:48:45 2018 -0700

    fix(tsdb): Some methods pass key lists as tuples (#8034)

diff --git a/src/sentry/tsdb/snuba.py b/src/sentry/tsdb/snuba.py
index 9069ffbd4d..a7e923790c 100644
--- a/src/sentry/tsdb/snuba.py
+++ b/src/sentry/tsdb/snuba.py
@@ -160,7 +160,7 @@ class SnubaTSDB(BaseTSDB):
         top level or a `{level1_key: [level2_key, ...]}` dictionary->list map.
         """
         # Flatten keys
-        if isinstance(items, list):
+        if isinstance(items, (list, tuple)):
             return (items, None)
         elif isinstance(items, dict):
             return (items.keys(), list(set.union(*(set(v) for v in items.values()))))
