commit 221402833bb4bd29c1b58f1d8444700b89bbc42b
Author: Mark Story <mark@sentry.io>
Date:   Fri May 1 10:18:03 2020 -0400

    fix(ui) Handle cancelled errors and 500s better (#18553)
    
    Don't fail to generate an error message when handling a cancelled
    request or a 500 from the server.
    
    Fixes JAVASCRIPT-20W9

diff --git a/src/sentry/static/sentry/app/views/eventsV2/table/index.tsx b/src/sentry/static/sentry/app/views/eventsV2/table/index.tsx
index 194b5cd6ae..8fc89210fd 100644
--- a/src/sentry/static/sentry/app/views/eventsV2/table/index.tsx
+++ b/src/sentry/static/sentry/app/views/eventsV2/table/index.tsx
@@ -3,6 +3,7 @@ import {Location} from 'history';
 import styled from '@emotion/styled';
 
 import {Client} from 'app/api';
+import {t} from 'app/locale';
 import {Organization, Tag} from 'app/types';
 import {metric} from 'app/utils/analytics';
 import withApi from 'app/utils/withApi';
@@ -121,14 +122,15 @@ class Table extends React.PureComponent<TableProps, TableState> {
             status: err.status,
           },
         });
+        const message = err?.responseJSON?.detail || t('An unknown error occurred.');
         this.setState({
           isLoading: false,
           tableFetchID: undefined,
-          error: err.responseJSON.detail,
+          error: message,
           pageLinks: null,
           tableData: null,
         });
-        setError(err.responseJSON.detail);
+        setError(message);
       });
   };
 
