commit 7ff391a96bd52d7f3ff4d9a18c8b6e5a1b1cac97
Author: Ted Kaemming <ted@kaemming.com>
Date:   Tue Sep 29 15:50:17 2015 -0700

    Don't need the test script any more (the preview takes care of that.)

diff --git a/digest.py b/digest.py
deleted file mode 100644
index 9d7a650c5a..0000000000
--- a/digest.py
+++ /dev/null
@@ -1,38 +0,0 @@
-#!/usr/bin/env python
-
-from sentry.utils.runner import configure
-
-configure()
-
-import itertools
-import sys
-
-from sentry.app import digests
-from sentry.digests.notifications import (
-    build_digest,
-    event_to_record,
-)
-from sentry.models import (
-    Event,
-    Project,
-)
-
-
-def reserialize(value):
-    return digests.codec.decode(digests.codec.encode(value))
-
-
-project = Project.objects.get(id=int(sys.argv[1]))
-events = project.event_set.all()[:int(sys.argv[2])]
-Event.objects.bind_nodes(events, 'data')
-records = itertools.imap(reserialize, itertools.imap(event_to_record, events))
-
-print '{project.organization} / {project}'.format(project=project)
-print ''
-
-digest = build_digest(project, records)
-for group, records in digest.groups.iteritems():
-    print '*', group.message_short
-    print ' ', group.culprit
-    print ' ', group.get_absolute_url()
-    print ' ', len(records), 'events from', min(r.timestamp for r in records), 'to', max(r.timestamp for r in records)
