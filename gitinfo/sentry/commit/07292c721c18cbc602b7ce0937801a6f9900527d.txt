commit 07292c721c18cbc602b7ce0937801a6f9900527d
Author: Mark Story <mark@sentry.io>
Date:   Thu Sep 5 13:29:41 2019 -0400

    fix(tests) Stabilize another test (#14590)
    
    This one has been failing occasionally.

diff --git a/tests/js/spec/stores/discoverSavedQueriesStore.spec.jsx b/tests/js/spec/stores/discoverSavedQueriesStore.spec.jsx
index d0b4e5c7de..caa2ed633d 100644
--- a/tests/js/spec/stores/discoverSavedQueriesStore.spec.jsx
+++ b/tests/js/spec/stores/discoverSavedQueriesStore.spec.jsx
@@ -39,10 +39,6 @@ describe('DiscoverSavedQueriesStore', function() {
         },
       ],
     });
-    Client.addMockResponse({
-      url: '/organizations/org-1/discover/saved/1/',
-      method: 'DELETE',
-    });
   });
 
   afterEach(async function() {
@@ -148,11 +144,18 @@ describe('DiscoverSavedQueriesStore', function() {
   });
 
   it('deleting a query updates the store', async function() {
+    Client.addMockResponse({
+      url: '/organizations/org-1/discover/saved/1/',
+      method: 'DELETE',
+      statusCode: 200,
+    });
+
     fetchSavedQueries(api, 'org-1');
     await tick();
 
     deleteSavedQuery(api, 'org-1', '1');
     await tick();
+    await tick();
 
     const state = DiscoverSavedQueriesStore.get();
     expect(state.isLoading).toEqual(false);
