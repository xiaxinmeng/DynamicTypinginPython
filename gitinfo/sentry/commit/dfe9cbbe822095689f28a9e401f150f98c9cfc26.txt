commit dfe9cbbe822095689f28a9e401f150f98c9cfc26
Author: Armin Ronacher <armin.ronacher@active-4.com>
Date:   Tue Nov 10 19:49:42 2015 +0100

    Remove bad test which assumed incorrect behavior for quoting of versions.

diff --git a/tests/sentry/api/endpoints/test_release_details.py b/tests/sentry/api/endpoints/test_release_details.py
index 23fa4b428d..d47b9bfb89 100644
--- a/tests/sentry/api/endpoints/test_release_details.py
+++ b/tests/sentry/api/endpoints/test_release_details.py
@@ -26,25 +26,6 @@ class ReleaseDetailsTest(APITestCase):
         assert response.status_code == 200, response.content
         assert response.data['version'] == release.version
 
-    def test_with_slash_in_version(self):
-        self.login_as(user=self.user)
-
-        project = self.create_project(name='foo')
-        release = Release.objects.create(
-            project=project,
-            version='release/1',
-        )
-
-        url = reverse('sentry-api-0-release-details', kwargs={
-            'organization_slug': project.organization.slug,
-            'project_slug': project.slug,
-            'version': 'release%2F1',
-        })
-        response = self.client.get(url)
-
-        assert response.status_code == 200, response.content
-        assert response.data['version'] == release.version
-
 
 class UpdateReleaseDetailsTest(APITestCase):
     def test_simple(self):
