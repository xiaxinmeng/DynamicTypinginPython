commit 7f0971219d833435293da7328b256620a8119825
Author: Markus Unterwaditzer <markus@unterwaditzer.net>
Date:   Tue Feb 11 19:06:17 2020 +0100

    ref: Bump pytest-sentry (#16938)
    
    * ref: Bump pytest-sentry
    
    * fix: Add pytest-rerunfailures
    
    * fix: Use PYTEST_SENTRY_ALWAYS_REPORT

diff --git a/.travis.yml b/.travis.yml
index 4307bb81b5..841ae70839 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -39,6 +39,7 @@ env:
 
 before_install:
   - &pip_install pip install --no-cache-dir "pip>=20.0.2"
+  - '[ "$TRAVIS_BRANCH" != "master" ] || export PYTEST_SENTRY_ALWAYS_REPORT=1'
 
 script:
   # certain commands require sentry init to be run, but this is only true for
diff --git a/requirements-dev.txt b/requirements-dev.txt
index 63915c7349..25cd0632b7 100644
--- a/requirements-dev.txt
+++ b/requirements-dev.txt
@@ -8,7 +8,8 @@ pytest==4.6.5
 pytest-cov==2.5.1
 pytest-django==3.5.1
 pytest-html==1.22.0
-pytest-sentry==0.1.0
+pytest-sentry==0.1.1
+pytest-rerunfailures==8.0
 responses>=0.8.1,<0.9.0
 sentry-flake8==0.1.1
 werkzeug==0.15.5
