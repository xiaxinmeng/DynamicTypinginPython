commit ab37256073cfec216b9df39f52df65c4aa24c093
Author: Tisza Gergő <gtisza@gmail.com>
Date:   Thu Jan 29 19:20:44 2015 -0800

    Fix typo in comment

diff --git a/src/sentry/conf/server.py b/src/sentry/conf/server.py
index b742a346ee..9d7cccb243 100644
--- a/src/sentry/conf/server.py
+++ b/src/sentry/conf/server.py
@@ -608,7 +608,7 @@ SENTRY_ENABLE_EXPLORE_USERS = True
 # size in characters
 SENTRY_MAX_VARIABLE_SIZE = 512
 
-# Prevent varabiesl within extra context from exceeding this size in
+# Prevent variables within extra context from exceeding this size in
 # characters
 SENTRY_MAX_EXTRA_VARIABLE_SIZE = 4096
 
