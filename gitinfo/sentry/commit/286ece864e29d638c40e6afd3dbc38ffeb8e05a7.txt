commit 286ece864e29d638c40e6afd3dbc38ffeb8e05a7
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Sun May 14 15:08:17 2017 -0700

    db/cleanup: use timestamptz instead of timestamp to be explicit
    
    Also formats it the same way that Django does

diff --git a/src/sentry/db/deletion.py b/src/sentry/db/deletion.py
index ee91c3de39..18441407da 100644
--- a/src/sentry/db/deletion.py
+++ b/src/sentry/db/deletion.py
@@ -21,7 +21,7 @@ class BulkDeleteQuery(object):
 
         where = []
         if self.dtfield and self.days is not None:
-            where.append("{} < timestamp '{}'".format(
+            where.append("{} < '{}'::timestamptz".format(
                 quote_name(self.dtfield),
                 (timezone.now() - timedelta(days=self.days)).isoformat(),
                 self.days,
