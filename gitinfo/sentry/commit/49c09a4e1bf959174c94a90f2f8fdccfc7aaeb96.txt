commit 49c09a4e1bf959174c94a90f2f8fdccfc7aaeb96
Author: Simon Boudrias <admin@simonboudrias.com>
Date:   Thu Oct 25 14:52:18 2012 -0300

    Line too long is sidebar break layout
    
    When you checkout an error, it happen that there's a line to long to fit inside the sidebar, and this break the layout.
    
    Just added an `overflow: hidden` to prevent this from happening. Adding a `title` attributes to those links could help too.

diff --git a/src/sentry.less b/src/sentry.less
index 41c04c2eaf..30747d0e10 100644
--- a/src/sentry.less
+++ b/src/sentry.less
@@ -285,6 +285,7 @@ h3 {
     float: left;
     padding: 18px 20px;
     margin-right: -300px;
+    overflow: hidden;
 
     h6 {
       display: block;
