commit 8e443c8349ec7242db5772b423fbc5c90726b261
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Feb 4 20:26:35 2015 -0800

    Add events view

diff --git a/src/sentry/static/sentry/app/routes.jsx b/src/sentry/static/sentry/app/routes.jsx
index 7abd8f8b44..09d49871ed 100644
--- a/src/sentry/static/sentry/app/routes.jsx
+++ b/src/sentry/static/sentry/app/routes.jsx
@@ -6,6 +6,7 @@ var Route = Router.Route;
 var DefaultRoute = Router.DefaultRoute;
 
 var Aggregate = require("./views/aggregate");
+var AggregateEvents = require("./views/aggregateEvents");
 var AggregateTags = require("./views/aggregateTags");
 var AggregateOverview = require("./views/aggregateOverview");
 var ProjectDetails = require("./views/projectDetails");
@@ -26,6 +27,7 @@ var routes = (
       <Route name="aggregateDetails" path="group/:aggregateId/" handler={Aggregate}>
         <DefaultRoute name="aggregateOverview" handler={AggregateOverview} />
         <Route name="aggregateTags" path="tags/" handler={AggregateTags} />
+        <Route name="aggregateEvents" path="events/" handler={AggregateEvents} />
       </Route>
     </Route>
   </Route>
diff --git a/src/sentry/static/sentry/app/views/aggregate/header.jsx b/src/sentry/static/sentry/app/views/aggregate/header.jsx
index 5acbe9c338..e94061cfd6 100644
--- a/src/sentry/static/sentry/app/views/aggregate/header.jsx
+++ b/src/sentry/static/sentry/app/views/aggregate/header.jsx
@@ -150,7 +150,11 @@ var AggregateHeader = React.createClass({
               Tags
             </Router.Link>
           </li>
-          <li><a href="#">Similar Events</a></li>
+          <li>
+            <Router.Link to="aggregateEvents" params={aggRouteParams}>
+              Similar Events
+            </Router.Link>
+          </li>
         </ul>
       </div>
     );
diff --git a/src/sentry/static/sentry/app/views/aggregateEvents.jsx b/src/sentry/static/sentry/app/views/aggregateEvents.jsx
new file mode 100644
index 0000000000..b5801113cc
--- /dev/null
+++ b/src/sentry/static/sentry/app/views/aggregateEvents.jsx
@@ -0,0 +1,24 @@
+/*** @jsx React.DOM */
+
+var React = require("react");
+var Router = require("react-router");
+
+var PropTypes = require("../proptypes");
+
+var AggregateEvents = React.createClass({
+  mixins: [Router.State],
+
+  propTypes: {
+    aggregate: PropTypes.Aggregate.isRequired
+  },
+
+  render() {
+    return (
+      <div>
+      Events
+      </div>
+    );
+  }
+});
+
+module.exports = AggregateEvents;
diff --git a/src/sentry/static/sentry/app/views/aggregateTags.jsx b/src/sentry/static/sentry/app/views/aggregateTags.jsx
index 9157b2dff1..bd6187ffda 100644
--- a/src/sentry/static/sentry/app/views/aggregateTags.jsx
+++ b/src/sentry/static/sentry/app/views/aggregateTags.jsx
@@ -13,8 +13,6 @@ var AggregateTags = React.createClass({
   },
 
   render() {
-    var agg = this.props.aggregate;
-
     return (
       <div>
       Tags
diff --git a/src/sentry/web/urls.py b/src/sentry/web/urls.py
index 6b57c4bceb..9cf411dea3 100644
--- a/src/sentry/web/urls.py
+++ b/src/sentry/web/urls.py
@@ -348,7 +348,7 @@ urlpatterns += patterns('',
 
     url(r'^(?P<organization_slug>[\w_-]+)/(?P<project_id>[\w_-]+)/group/(?P<group_id>\d+)/$', ReactPageView.as_view(),
         name='sentry-group'),
-    url(r'^(?P<organization_slug>[\w_-]+)/(?P<project_id>[\w_-]+)/group/(?P<group_id>\d+)/events/$', groups.group_event_list,
+    url(r'^(?P<organization_slug>[\w_-]+)/(?P<project_id>[\w_-]+)/group/(?P<group_id>\d+)/events/$', ReactPageView.as_view(),
         name='sentry-group-events'),
     url(r'^(?P<organization_slug>[\w_-]+)/(?P<project_id>[\w_-]+)/group/(?P<group_id>\d+)/events/(?P<event_id>\d+)/$', groups.group,
         name='sentry-group-event'),
