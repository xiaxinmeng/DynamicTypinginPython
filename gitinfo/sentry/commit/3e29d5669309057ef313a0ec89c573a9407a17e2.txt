commit 3e29d5669309057ef313a0ec89c573a9407a17e2
Author: David Cramer <dcramer@gmail.com>
Date:   Sun Oct 12 07:43:25 2014 -0700

    Remove unused is_muted filter
    
    Conflicts:
            src/sentry/templatetags/sentry_helpers.py

diff --git a/src/sentry/templatetags/sentry_helpers.py b/src/sentry/templatetags/sentry_helpers.py
index 6608bf0806..2751a82f0a 100644
--- a/src/sentry/templatetags/sentry_helpers.py
+++ b/src/sentry/templatetags/sentry_helpers.py
@@ -32,7 +32,7 @@ import six
 from six.moves import range
 
 from sentry import options
-from sentry.constants import STATUS_MUTED, EVENTS_PER_PAGE
+from sentry.constants import EVENTS_PER_PAGE
 from sentry.models import Organization
 from sentry.web.helpers import group_is_public
 from sentry.utils import to_unicode
@@ -369,11 +369,6 @@ def titlize(value):
     return value.replace('_', ' ').title()
 
 
-@register.filter
-def is_muted(value):
-    return value == STATUS_MUTED
-
-
 @register.filter
 def split(value, delim=''):
     return value.split(delim)
