commit d59b495bc214470dbe80a6841e4a233e2ddd4bc6
Author: David Cramer <dcramer@gmail.com>
Date:   Sat May 7 13:45:42 2011 -0700

    Added helper configure() method

diff --git a/sentry/helpers.py b/sentry/helpers.py
index 30a5a7b33d..e54e5ffeb5 100644
--- a/sentry/helpers.py
+++ b/sentry/helpers.py
@@ -2,6 +2,7 @@ import hmac
 import logging
 import sys
 import uuid
+import warnings
 from types import ClassType, TypeType
 
 import django
@@ -258,4 +259,10 @@ def get_auth_header(signature, timestamp, client):
     )
 
 def parse_auth_header(header):
-    return dict(map(lambda x: x.strip().split('='), header.split(' ', 1)[1].split(',')))
\ No newline at end of file
+    return dict(map(lambda x: x.strip().split('='), header.split(' ', 1)[1].split(',')))
+
+def configure(**kwargs):
+    for k, v in kwargs.iteritems():
+        if not hasattr(conf, k):
+            warnings.warn('Setting %k which is not defined by Sentry' % k)
+        setattr(conf, k, v)
\ No newline at end of file
