commit 1a9ab3595a78e58a4f19889774c55d926730e424
Author: David Cramer <dcramer@gmail.com>
Date:   Fri Oct 15 11:45:33 2010 -0700

    Clean up some wrapping issues in CSS. Added logger name to list of tags.

diff --git a/sentry/media/styles/global.css b/sentry/media/styles/global.css
index 7574dd2bd2..7af02b8b31 100644
--- a/sentry/media/styles/global.css
+++ b/sentry/media/styles/global.css
@@ -279,15 +279,25 @@ dl.flat dd {
 	border-radius: 6px;
 	padding: 4px; line-height: 19px; color: #fff; display: inline-block; margin-right: 5px;
 }
-.messages h3 { height: 20px; line-height: 20px; display: inline-block; margin: 0; white-space: nowrap; overflow: hidden; }
+.messages h3 {
+    height: 20px;
+    line-height: 20px;
+    display: inline-block;
+    margin: 0;
+    white-space: nowrap;
+    overflow: hidden;
+    max-width: 80%;
+}
 .messages h3 a { text-decoration: none; }
 .messages .message {
 	color: #666;
 	margin-top: 8px;
 	font-size: 0.9em;
+	overflow: hidden;
+	white-space: nowrap;
 }
 
-.messages .tag { display: inline-block; padding: 2px 5px; background: #d5d5d5; margin-right: 5px;}
+.messages .tag { display: inline-block; padding: 2px 5px; background: #eaeaea; margin-right: 5px;}
 .messages .traceback { display: none; }
 .messages li { padding: 8px; position: relative; padding-right: 50px; overflow: hidden;}
 .messages .row1 { background: #f9f9f9; }
@@ -358,7 +368,7 @@ dl.flat dd {
 	background:-moz-linear-gradient(center top , #efd57c, #e0ae00) repeat scroll 0 0 transparent;
 	background:-webkit-gradient(linear, 0% 0%, 0% 100%, from(#efd57c), to(#e0ae00));
 }
-.messages .last_seen { color: #bbb; font-size: 0.9em; margin-left: 5px; }
+.messages .last_seen { color: #bbb; font-size: 0.9em; margin-left: 5px; white-space: nowrap; }
 .messages .status { margin-left: 5px; text-transform: uppercase; font-size: 0.9em; }
 .messages .status-0 { display: none; }
 .messages .status-1 { color: red; }
@@ -368,6 +378,7 @@ dl.flat dd {
 	-moz-border-radius: 6px;
 	border-radius: 6px;
 }
+.messages li:hover .tag { background: #fff; }
 .messages li:hover h3 a {
     color: #006CA3;
 }
diff --git a/sentry/templates/sentry/partial/_group.html b/sentry/templates/sentry/partial/_group.html
index 12ea901839..eb1d041700 100644
--- a/sentry/templates/sentry/partial/_group.html
+++ b/sentry/templates/sentry/partial/_group.html
@@ -5,8 +5,12 @@
 	<h3><a href="{% url sentry-group group.pk %}">{% if group.view %}{{ group.view }}{% else %}{{ group.message }}{% endif %}</a></h3>
 	<span class="last_seen">{{ group.last_seen|timesince }} ago</span>
 	<span class="status status-{{ group.status }}">{{ group.get_status_display }}</span>
-	<p class="message"><span class="tag tag-level">{{ group.get_level_display }}</span> {{ group.error }}</p>
-    <a href="{% url sentry-group group.pk %}" class="row_link"></a>
+	<p class="message">
+		<span class="tag tag-level">{{ group.get_level_display }}</span> 
+		<span class="tag tag-logger">{{ group.logger }}</span> 
+		{{ group.error }}
+	</p>
+	<a href="{% url sentry-group group.pk %}" class="row_link"></a>
 	{% if group.status == 0 %}
 		<a href="{% url sentry-ajax %}?op=resolve&amp;gid={{ group.pk }}" onclick="sentryResolve({{ group.pk }});returnfalse;" class="button resolve_button hidden">&#10003;</a>
 	{% endif %}
