commit 290c0d43194f86751cac9c4a37153f3fde476431
Author: Ben Vinegar <ben@benv.ca>
Date:   Mon May 2 15:22:48 2016 -0700

    Add 'crumb-user' for 'ui.*' crumb categories

diff --git a/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbComponents/crumb.jsx b/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbComponents/crumb.jsx
index 32e58ac62d..baba98071c 100644
--- a/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbComponents/crumb.jsx
+++ b/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbComponents/crumb.jsx
@@ -20,6 +20,11 @@ const CrumbComponent = React.createClass({
     if (crumb.type !== 'default') {
       rv += ' crumb-' + crumb.type.replace(/[\s_]+/g, '-').toLowerCase();
     }
+    // special case for 'ui.' category breadcrumbs
+    // TODO: find a better way to customize UI around non-schema data
+    if (crumb.category && crumb.category.slice(0, 3) === 'ui.') {
+      rv += ' crumb-user';
+    }
     return rv;
   },
 
