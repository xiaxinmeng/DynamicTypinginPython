commit adf41f6d13e5c3c9217b32e96838aa1b468db9e2
Author: Jess MacQueen <macqueen@users.noreply.github.com>
Date:   Tue Oct 4 13:24:02 2016 -0700

    add support for multiple to Select2Field (#4259)

diff --git a/src/sentry/static/sentry/app/components/forms/select2Field.jsx b/src/sentry/static/sentry/app/components/forms/select2Field.jsx
index c34a69ae96..2a245ceebf 100644
--- a/src/sentry/static/sentry/app/components/forms/select2Field.jsx
+++ b/src/sentry/static/sentry/app/components/forms/select2Field.jsx
@@ -13,6 +13,7 @@ class Select2Field extends InputField {
           onChange={this.onChange.bind(this)}
           disabled={this.props.disabled}
           required={this.props.required}
+          multiple={this.props.multiple || false}
           value={this.state.value}>
         {this.props.choices.map((choice) => {
           return (
@@ -26,6 +27,25 @@ class Select2Field extends InputField {
     );
   }
 
+  onChange(e) {
+    if (this.props.multiple) {
+      let options = e.target.options;
+      let value = [];
+      for (let i = 0; i < options.length; i++) {
+        if (options[i].selected) {
+          value.push(options[i].value);
+        }
+      }
+      this.setState({
+        value: value,
+      }, () => {
+        this.props.onChange(this.state.value);
+      });
+      return;
+    }
+    super.onChange(e);
+  }
+
   componentDidMount() {
     jQuery(this.refs.input).select2().on('change', this.onChange);
 
