commit 9906a3080b718b41cfb768c591e8315e1b734aa4
Author: Armin Ronacher <armin.ronacher@active-4.com>
Date:   Wed May 4 18:22:09 2016 +0200

    Implemented better rendering for error types

diff --git a/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbComponents/crumb.jsx b/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbComponents/crumb.jsx
index baba98071c..f1b00ed50e 100644
--- a/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbComponents/crumb.jsx
+++ b/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbComponents/crumb.jsx
@@ -2,10 +2,12 @@ import React from 'react';
 import moment from 'moment';
 
 import HttpRenderer from './httpRenderer';
+import ErrorRenderer from './errorRenderer';
 import DefaultRenderer from './defaultRenderer';
 
 const CUSTOM_RENDERERS = {
   http: HttpRenderer,
+  error: ErrorRenderer,
 };
 
 
diff --git a/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbComponents/errorRenderer.jsx b/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbComponents/errorRenderer.jsx
new file mode 100644
index 0000000000..ba132b69bf
--- /dev/null
+++ b/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbComponents/errorRenderer.jsx
@@ -0,0 +1,53 @@
+import React from 'react';
+
+import CrumbTable from './crumbTable';
+import SummaryLine from './summaryLine';
+
+
+const ErrorRenderer = React.createClass({
+  propTypes: {
+    crumb: React.PropTypes.object.isRequired,
+  },
+
+  renderUrl(url) {
+    return (
+      url.match(/^https?:\/\//)
+        ? <a href={url}>{url}</a>
+        : <em>{url}</em>
+    );
+  },
+
+  render() {
+    let {crumb} = this.props;
+    let {type, value, ...extra} = crumb.data || {};
+    let messages = [];
+
+    if (value) {
+      messages.push(value);
+    }
+    if (crumb.message) {
+      messages.push(crumb.message);
+    }
+
+    let summary = (
+      <SummaryLine crumb={crumb}>
+        <pre>
+          <code>
+            {type && <strong>{type}: </strong>}
+            {messages.join('. ')}
+          </code>
+        </pre>
+      </SummaryLine>
+    );
+
+    return (
+      <CrumbTable
+        title="Error"
+        summary={summary}
+        kvData={extra}
+        {...this.props} />
+    );
+  }
+});
+
+export default ErrorRenderer;
diff --git a/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbs.jsx b/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbs.jsx
index fa39bfda85..083c179fba 100644
--- a/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbs.jsx
+++ b/src/sentry/static/sentry/app/components/events/interfaces/breadcrumbs.jsx
@@ -95,7 +95,10 @@ const BreadcrumbsInterface = React.createClass({
         type: 'error',
         level: 'error',
         category: moduleToCategory(module || null),
-        message: type + ': ' + value,
+        data: {
+          type: type,
+          value: value
+        },
         timestamp: evt.dateCreated
       }]);
     }
