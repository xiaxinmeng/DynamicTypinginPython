commit 1680423c8c85189a21c4197f22d92bda87e9df81
Author: David Cramer <cramer@dropbox.com>
Date:   Sat Dec 28 18:24:46 2013 -0800

    Tag project in save_data

diff --git a/src/sentry/manager.py b/src/sentry/manager.py
index c28d0b8b29..96b93b101b 100644
--- a/src/sentry/manager.py
+++ b/src/sentry/manager.py
@@ -24,7 +24,9 @@ from django.db.models import Sum
 from django.utils import timezone
 from django.utils.datastructures import SortedDict
 
+from raven.base import Raven
 from raven.utils.encoding import to_string
+
 from sentry import app
 from sentry.constants import (
     STATUS_RESOLVED, STATUS_UNRESOLVED, MINUTE_NORMALIZATION,
@@ -341,6 +343,8 @@ class GroupManager(BaseManager, ChartMixin):
 
         project = Project.objects.get_from_cache(id=project)
 
+        Raven.tags_context({'project': project.id})
+
         # First we pull out our top-level (non-data attr) kwargs
         event_id = data.pop('event_id')
         message = data.pop('message')
