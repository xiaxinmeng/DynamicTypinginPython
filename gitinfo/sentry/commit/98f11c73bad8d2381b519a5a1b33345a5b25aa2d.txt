commit 98f11c73bad8d2381b519a5a1b33345a5b25aa2d
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Sun Aug 24 22:23:52 2014 +0000

    Move conditional

diff --git a/src/sentry/tasks/fetch_source.py b/src/sentry/tasks/fetch_source.py
index 83b074b4f0..a5279bbe95 100644
--- a/src/sentry/tasks/fetch_source.py
+++ b/src/sentry/tasks/fetch_source.py
@@ -59,10 +59,10 @@ def trim_line(line, column=0):
     """
     line = line.strip('\n')
     ll = len(line)
-    if column > ll:
-        column = ll
     if ll <= 150:
         return line
+    if column > ll:
+        column = ll
     start = max(column - 60, 0)
     # Round down if it brings us close to the edge
     if start < 5:
