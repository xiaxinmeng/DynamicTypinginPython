commit df559a9fc8067010021391f67b4ebdc5c0d10aaa
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Aug 10 00:52:38 2015 -0700

    Dont duplicate name in emails

diff --git a/src/sentry/models/project.py b/src/sentry/models/project.py
index 4ae8124a43..b3ad4bdab9 100644
--- a/src/sentry/models/project.py
+++ b/src/sentry/models/project.py
@@ -232,3 +232,8 @@ class Project(Model):
             'public': self.public,
             'platform': self.platform,
         }
+
+    def get_full_name(self):
+        if self.team.name not in self.name:
+            return '%s %s' % (self.team.name, self.name)
+        return self.name
diff --git a/src/sentry/plugins/sentry_mail/models.py b/src/sentry/plugins/sentry_mail/models.py
index 80c27b4b17..fca93b2a88 100644
--- a/src/sentry/plugins/sentry_mail/models.py
+++ b/src/sentry/plugins/sentry_mail/models.py
@@ -164,6 +164,7 @@ class MailPlugin(NotificationPlugin):
             rules.append((rule.label, rule_link))
 
         context = {
+            'project_label': project.get_full_name(),
             'group': group,
             'event': event,
             'tags': event.get_tags(),
diff --git a/src/sentry/templates/sentry/emails/error.html b/src/sentry/templates/sentry/emails/error.html
index 495c81ec4a..e1642401fd 100644
--- a/src/sentry/templates/sentry/emails/error.html
+++ b/src/sentry/templates/sentry/emails/error.html
@@ -12,9 +12,9 @@
     <h2>
         {% url 'sentry-stream' group.project.organization.slug group.project.slug as project_link %}
         {% if group.times_seen == 1 %}
-            New Event on <a href="{% absolute_uri project_link %}" class="highlight">{{ group.team.name }} {{ group.project.name }}</a>
+            New Event on <a href="{% absolute_uri project_link %}" class="highlight">{{ project_label }}</a>
         {% else %}
-            Regression on <a href="{% absolute_uri project_link %}" class="highlight">{{ group.team.name }} {{ group.project.name }}</a>
+            Regression on <a href="{% absolute_uri project_link %}" class="highlight">{{ project_label }}</a>
         {% endif %}
     </h2>
 
