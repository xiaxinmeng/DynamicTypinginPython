commit bf16b0714f09ba052f96397823723ad37ea61897
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Oct 15 00:45:55 2012 -0700

    Change get_plugins helpers to return lists

diff --git a/src/sentry/templatetags/sentry_plugins.py b/src/sentry/templatetags/sentry_plugins.py
index 35806b81db..2e66fa2b5f 100644
--- a/src/sentry/templatetags/sentry_plugins.py
+++ b/src/sentry/templatetags/sentry_plugins.py
@@ -110,10 +110,9 @@ def handle_before_events(request, event_list):
 
 @register.filter
 def get_plugins(project):
-    return plugins.for_project(project)
+    return list(plugins.for_project(project))
 
 
 @register.filter
 def get_plugins_with_status(project):
-    for plugin in plugins.all():
-        yield plugin, safe_execute(plugin.is_enabled, project)
+    return [(plugin, safe_execute(plugin.is_enabled, project)) for plugin in plugins.all()]
