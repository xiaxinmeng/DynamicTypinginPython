commit fc1cecddb102318bef9c97852114334b9ab70d7e
Author: Mark Story <mark@sentry.io>
Date:   Thu Aug 30 16:50:30 2018 -0400

    chore(ui) Remove components with no imports.
    
    Using some grep/awk I found two components that have no imports. These
    should be safe to remove.

diff --git a/src/sentry/static/sentry/app/components/userInfo.jsx b/src/sentry/static/sentry/app/components/userInfo.jsx
deleted file mode 100644
index 0fd3d48b4f..0000000000
--- a/src/sentry/static/sentry/app/components/userInfo.jsx
+++ /dev/null
@@ -1,39 +0,0 @@
-import PropTypes from 'prop-types';
-import React from 'react';
-
-function getUserDisplayName(name) {
-  let parts = name.split(/@/);
-  if (parts.length == 1) {
-    return parts[0];
-  }
-  return parts[0].toLowerCase().replace(/[\.-_]+/, ' ');
-}
-
-class UserInfo extends React.Component {
-  static propTypes = {
-    user: PropTypes.any.isRequired,
-  };
-
-  render() {
-    // XXX(dcramer): not supported by babel
-    // var {user, ...other} = this.props;
-    let user = this.props.user;
-    let other = {};
-    for (let key in this.props) {
-      if (key !== 'user') {
-        other[key] = this.props[key];
-      }
-    }
-
-    let name = user.name || user.email;
-    let displayName = getUserDisplayName(name);
-
-    return (
-      <span title={name} {...other}>
-        {displayName}
-      </span>
-    );
-  }
-}
-
-export default UserInfo;
diff --git a/src/sentry/static/sentry/app/views/settings/project/projectOwnership/ownerInputStyles.js b/src/sentry/static/sentry/app/views/settings/project/projectOwnership/ownerInputStyles.js
deleted file mode 100644
index 66c808e7d2..0000000000
--- a/src/sentry/static/sentry/app/views/settings/project/projectOwnership/ownerInputStyles.js
+++ /dev/null
@@ -1,53 +0,0 @@
-let styles = {
-  control: {
-    backgroundColor: '#fff',
-    fontSize: 15,
-    fontWeight: 'normal',
-  },
-
-  input: {
-    margin: 0,
-    fontFamily: 'Monaco, Consolas, "Courier New", monospace',
-    wordBreak: 'break-all',
-    whiteSpace: 'pre-wrap',
-  },
-
-  '&multiLine': {
-    control: {
-      fontFamily: 'Monaco, Consolas, "Courier New", monospace',
-    },
-
-    highlighter: {
-      padding: 20,
-    },
-
-    input: {
-      padding: '5px 5px 0',
-      minHeight: 140,
-      overflow: 'auto',
-      outline: 0,
-      border: '1 solid',
-    },
-  },
-
-  suggestions: {
-    list: {
-      maxHeight: 150,
-      overflow: 'auto',
-      backgroundColor: 'white',
-      border: '1px solid rgba(0,0,0,0.15)',
-      fontSize: 15,
-    },
-
-    item: {
-      padding: '5px 15px',
-      borderBottom: '1px solid rgba(0,0,0,0.15)',
-
-      '&focused': {
-        backgroundColor: '#f8f6f9',
-      },
-    },
-  },
-};
-
-export default styles;
