commit 5a6b633eabbfbcbe732f4fe622ad95b131e5253c
Author: David Cramer <dcramer@gmail.com>
Date:   Sat Jan 26 17:26:29 2013 -0800

    Improve how activity is displayed

diff --git a/src/sentry/static/sentry/less/sentry.less b/src/sentry/static/sentry/less/sentry.less
index 24dfc4748e..e363d8d542 100644
--- a/src/sentry/static/sentry/less/sentry.less
+++ b/src/sentry/static/sentry/less/sentry.less
@@ -466,8 +466,8 @@ h3 {
     padding: 15px 0;
   }
   section.body .event-toolbar {
-    padding: 5px 30px 15px;
-    margin: 0 -30px 15px;
+    padding: 0;
+    margin: 0 0 20px;
     border-bottom: 0;
     text-align: center;
 
@@ -477,6 +477,15 @@ h3 {
       color: #aaa;
     }
   }
+  .activity {
+    margin-bottom: 20px;
+
+    .sep, .time {
+      font-size: 12px;
+      color: #aaa;
+    }
+  }
+
   .sidebar {
     position: relative;
     width: 240px;
@@ -1322,7 +1331,7 @@ td.code {
   width: ((@gridColumnWidth) * @columnSpan) + (@gridGutterWidth * (@columnSpan - 1)) - 10;
 }
 
-#chart { margin-bottom: 15px; overflow: hidden; }
+#chart { margin-bottom: 10px; overflow: hidden; }
 
 div.pastebin textarea { padding: 10px; background: #f9f9f9; border-color: #ddd; }
 
@@ -1551,7 +1560,6 @@ section.body .page-header:first-child {
 #event_nav {
   position: relative;
   margin-bottom: 15px;
-  margin-top: -15px;
 
   .event_nav_wrap {
     position: relative;
diff --git a/src/sentry/templates/sentry/groups/details.html b/src/sentry/templates/sentry/groups/details.html
index 43816deeb2..cd7ed3e5db 100644
--- a/src/sentry/templates/sentry/groups/details.html
+++ b/src/sentry/templates/sentry/groups/details.html
@@ -1,6 +1,7 @@
 {% extends "sentry/groups/group_list.html" %}
 
 {% load i18n %}
+{% load sentry_activity %}
 {% load sentry_helpers %}
 {% load sentry_plugins %}
 
@@ -34,6 +35,19 @@
                     </div>
                 {% endif %}
 
+
+                {% if activity %}
+                    <ul class="activity">
+                        {% for item in activity %}
+                            {% with item|render_activity as out %}
+                                {% if out %}
+                                    <li class="item item-{{ item.get_type_display }}">{{ out }}</li>
+                                {% endif %}
+                            {% endwith %}
+                        {% endfor %}
+                    </ul>
+                {% endif %}
+
                 <div class="btn-toolbar event-toolbar">
                     <!-- We switch the ordering of events here as it makes more sense visually -->
                     <a class="btn pull-left {% if not next_event %} disabled{% endif %}"{% if next_event %} href="{% url sentry-group-event group.team.slug group.project.slug group.id next_event.id %}"{% endif %}><span>{% trans "Newer Event" %}</span></a>
diff --git a/src/sentry/templates/sentry/partial/_event_details.html b/src/sentry/templates/sentry/partial/_event_details.html
index fcd5cabad7..4feb70761a 100644
--- a/src/sentry/templates/sentry/partial/_event_details.html
+++ b/src/sentry/templates/sentry/partial/_event_details.html
@@ -1,21 +1,6 @@
 {% load i18n %}
-{% load sentry_activity %}
 {% load sentry_helpers %}
 
-{% if activity %}
-    <hr>
-    <ul class="activity">
-        {% for item in activity %}
-            {% with item|render_activity as out %}
-                {% if out %}
-                    <li class="item item-{{ item.get_type_display }}">{{ out }}</li>
-                {% endif %}
-            {% endwith %}
-        {% endfor %}
-    </ul>
-    <hr>
-{% endif %}
-
 {% for interface, interface_html in interface_list %}
     <div id="{{ interface.get_slug }}">
         {{ interface_html }}
diff --git a/src/sentry/templatetags/sentry_activity.py b/src/sentry/templatetags/sentry_activity.py
index 2cbee99df7..b75cad39bc 100644
--- a/src/sentry/templatetags/sentry_activity.py
+++ b/src/sentry/templatetags/sentry_activity.py
@@ -43,7 +43,7 @@ def render_activity(item):
     else:
         output += 'The system %s' % (action_str,)
 
-    output += ' &mdash; %s</p>' % (timesince(item.datetime),)
+    output += ' <span class="sep">&mdash;</span> <span class="time">%s</span></p>' % (timesince(item.datetime),)
 
     if item.type == Activity.COMMENT:
         output += linebreaks(item.data['body'])
