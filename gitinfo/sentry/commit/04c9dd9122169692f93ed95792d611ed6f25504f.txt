commit 04c9dd9122169692f93ed95792d611ed6f25504f
Author: Jess MacQueen <jessmacqueen@gmail.com>
Date:   Thu Feb 1 14:52:55 2018 -0800

    feat(projects): Add link to create new team from project teams page

diff --git a/src/sentry/static/sentry/app/views/settings/project/projectTeams.jsx b/src/sentry/static/sentry/app/views/settings/project/projectTeams.jsx
index b927df9117..c75f6f398b 100644
--- a/src/sentry/static/sentry/app/views/settings/project/projectTeams.jsx
+++ b/src/sentry/static/sentry/app/views/settings/project/projectTeams.jsx
@@ -162,6 +162,7 @@ class ProjectTeams extends AsyncView {
   }
 
   renderBody() {
+    let {orgId} = this.props.params;
     let body;
 
     if (this.state.projectTeams.length > 0) body = this.renderResults();
@@ -195,6 +196,16 @@ class ProjectTeams extends AsyncView {
                   <a>{t('No available teams')}</a>
                 </MenuItem>
               )}
+              <MenuItem divider={true} />
+              <div style={{textAlign: 'center', padding: '5px 0px'}}>
+                <Button
+                  to={`/organizations/${orgId}/teams/new/`}
+                  priority="primary"
+                  size="small"
+                >
+                  {t('Create a new team')}
+                </Button>
+              </div>
             </DropdownLink>
           }
         />
diff --git a/tests/js/spec/views/__snapshots__/projectTeamSettings.spec.jsx.snap b/tests/js/spec/views/__snapshots__/projectTeamSettings.spec.jsx.snap
index e002f6afdc..fb0f2b2a2a 100644
--- a/tests/js/spec/views/__snapshots__/projectTeamSettings.spec.jsx.snap
+++ b/tests/js/spec/views/__snapshots__/projectTeamSettings.spec.jsx.snap
@@ -24,6 +24,26 @@ exports[`ProjectTeamsSettings render() renders 1`] = `
               Team Name 2
             </a>
           </MenuItem>
+          <MenuItem
+            divider={true}
+          />
+          <div
+            style={
+              Object {
+                "padding": "5px 0px",
+                "textAlign": "center",
+              }
+            }
+          >
+            <Button
+              disabled={false}
+              priority="primary"
+              size="small"
+              to="/organizations/org-slug/teams/new/"
+            >
+              Create a new team
+            </Button>
+          </div>
         </DropdownLink>
       }
       title="Teams"
diff --git a/tests/js/spec/views/projectTeamSettings.spec.jsx b/tests/js/spec/views/projectTeamSettings.spec.jsx
index 0b1c22e693..3a23b3421f 100644
--- a/tests/js/spec/views/projectTeamSettings.spec.jsx
+++ b/tests/js/spec/views/projectTeamSettings.spec.jsx
@@ -78,10 +78,13 @@ describe('ProjectTeamsSettings', function() {
       expect(mock).not.toHaveBeenCalled();
 
       // open modal
-      wrapper.find('Button').simulate('click');
+      wrapper.find('.button-default').simulate('click');
 
-      // click confrim
-      wrapper.find('button.button-primary').simulate('click');
+      // click confirm
+      wrapper
+        .find('.modal-footer')
+        .find('button.button-primary')
+        .simulate('click');
 
       expect(mock).toHaveBeenCalledWith(
         endpoint,
