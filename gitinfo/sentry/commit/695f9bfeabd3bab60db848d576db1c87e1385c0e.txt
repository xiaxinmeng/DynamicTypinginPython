commit 695f9bfeabd3bab60db848d576db1c87e1385c0e
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Mar 5 16:27:38 2018 -0800

    feat(owners): Support windows paths

diff --git a/src/sentry/utils/committers.py b/src/sentry/utils/committers.py
index fbff6eca91..edeb7e0f67 100644
--- a/src/sentry/utils/committers.py
+++ b/src/sentry/utils/committers.py
@@ -14,10 +14,14 @@ from itertools import izip
 from collections import defaultdict
 from six.moves import reduce
 
+PATH_SEPERATORS = frozenset(['/', '\\'])
+
 
 def tokenize_path(path):
-    # TODO(maxbittker) tokenize in a smarter crossplatform way.
-    return reversed(path.split('/'))
+    for sep in PATH_SEPERATORS:
+        if sep in path:
+            return reversed(path.split(sep))
+    return [path]
 
 
 def score_path_match_length(path_a, path_b):
diff --git a/tests/sentry/utils/test_committers.py b/tests/sentry/utils/test_committers.py
index 2e9c3dcb64..c42f018eeb 100644
--- a/tests/sentry/utils/test_committers.py
+++ b/tests/sentry/utils/test_committers.py
@@ -1,6 +1,6 @@
 from __future__ import absolute_import
 
-from sentry.utils.committers import score_path_match_length
+from sentry.utils.committers import score_path_match_length, tokenize_path
 
 
 def test_score_path_match_length():
@@ -9,3 +9,9 @@ def test_score_path_match_length():
     assert score_path_match_length('foo/bar/baz', 'baz') == 1
     assert score_path_match_length('foo/bar/baz', 'foo') == 0
     assert score_path_match_length('./foo/bar/baz', 'foo/bar/baz') == 3
+
+
+def test_tokenize_path():
+    assert list(tokenize_path('foo/bar')) == ['bar', 'foo']
+    assert list(tokenize_path('foo\\bar')) == ['bar', 'foo']
+    assert list(tokenize_path('foo.bar')) == ['foo.bar']
