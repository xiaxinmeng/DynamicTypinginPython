commit 4abaa7e137e60105f025b13e71a704705507ab0d
Author: Armin Ronacher <armin.ronacher@active-4.com>
Date:   Thu Nov 19 14:59:49 2015 +0100

    Load translations in i18n module

diff --git a/src/sentry/static/sentry/app/locale.jsx b/src/sentry/static/sentry/app/locale.jsx
index 399268f083..d92b6d171d 100644
--- a/src/sentry/static/sentry/app/locale.jsx
+++ b/src/sentry/static/sentry/app/locale.jsx
@@ -1,4 +1,5 @@
 import Jed from 'jed';
+import { getTranslations } from './translations';
 
 const i18n = new Jed({
   'domain' : 'sentry',
@@ -8,22 +9,9 @@ const i18n = new Jed({
     // TODO(dcramer): this should log to Sentry
   },
 
-  'locale_data' : {
-    // This is the domain key
-    'sentry' : {
-      // The empty string key is used as the configuration
-      // block for each domain
-      '' : {
-        // Domain name
-        'domain' : 'sentry',
-
-        // Language code
-        'lang' : 'en',
-
-        // Plural form function for language
-        'plural_forms' : 'nplurals=2; plural=(n != 1);'
-      },
-    }
+  'locale_data': {
+    // XXX: configure language here
+    'sentry': getTranslations('en')
   }
 });
 
