commit e18a0f5c0ec56ec307bf083522c0de84eb08edb3
Author: David Cramer <dcramer@gmail.com>
Date:   Sun Jan 29 12:05:51 2012 -0800

    Only send regression signal if is_new is True

diff --git a/sentry/manager.py b/sentry/manager.py
index 25e0aa7488..62273253ad 100644
--- a/sentry/manager.py
+++ b/sentry/manager.py
@@ -343,7 +343,8 @@ class GroupManager(models.Manager, ChartMixin):
         if not is_sample:
             event.save()
 
-        regression_signal.send(sender=self.model, instance=group)
+        if is_new:
+            regression_signal.send(sender=self.model, instance=group)
 
         send_group_processors(group=group, event=event, is_new=is_new, is_sample=is_sample)
 
