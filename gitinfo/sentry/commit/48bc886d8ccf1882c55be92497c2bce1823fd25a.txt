commit 48bc886d8ccf1882c55be92497c2bce1823fd25a
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Aug 1 14:14:01 2011 -0700

    useless cleanup

diff --git a/runtests.py b/runtests.py
index 2f33fb66c4..c97312cf82 100644
--- a/runtests.py
+++ b/runtests.py
@@ -74,9 +74,15 @@ def runtests(*test_args, **kwargs):
 
     if not test_args:
         test_args = ['tests']
-    parent = dirname(abspath(__file__))
-    sys.path.insert(0, parent)
-    failures = run_tests(test_args, verbosity=kwargs.get('verbosity', 1), interactive=kwargs.get('interactive', False), failfast=kwargs.get('failfast'))
+
+    sys.path.insert(0, dirname(abspath(__file__)))
+
+    failures = run_tests(test_args,
+        verbosity=kwargs.get('verbosity', 1),
+        interactive=kwargs.get('interactive', False),
+        failfast=kwargs.get('failfast'),
+    )
+
     sys.exit(failures)
 
 if __name__ == '__main__':
