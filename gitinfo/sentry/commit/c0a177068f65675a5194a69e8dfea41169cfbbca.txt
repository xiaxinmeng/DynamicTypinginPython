commit c0a177068f65675a5194a69e8dfea41169cfbbca
Author: David Cramer <dcramer@gmail.com>
Date:   Tue May 6 23:54:42 2014 -0700

    Remove concept of logger + culprit in constraint

diff --git a/src/sentry/models/group.py b/src/sentry/models/group.py
index ba627507eb..483d002e33 100644
--- a/src/sentry/models/group.py
+++ b/src/sentry/models/group.py
@@ -100,7 +100,7 @@ class Group(Model):
         return float(self.time_spent_total) / self.time_spent_count
 
     def natural_key(self):
-        return (self.project, self.logger, self.culprit, self.checksum)
+        return (self.project, self.checksum)
 
     def is_over_resolve_age(self):
         resolve_age = self.project.get_option('sentry:resolve_age', None)
diff --git a/src/sentry/models/project.py b/src/sentry/models/project.py
index e3c9ca4db6..266543fd2c 100644
--- a/src/sentry/models/project.py
+++ b/src/sentry/models/project.py
@@ -85,8 +85,6 @@ class Project(Model):
             try:
                 other = Group.objects.get(
                     project=project,
-                    logger=group.logger,
-                    culprit=group.culprit,
                     checksum=group.checksum,
                 )
             except Group.DoesNotExist:
