commit d527eac4a35bfb10c5f27c828c068478f6edb361
Author: Keegan Carruthers-Smith <keegan.csmith@gmail.com>
Date:   Fri May 27 00:40:52 2011 +0200

    Make setup.py exclude the example_project package
    
    Previously setup.py was using the exclude keyword incorrectly for find_packages,
    which caused it to not exclude any packages.

diff --git a/setup.py b/setup.py
index c227c01714..0e259848d8 100644
--- a/setup.py
+++ b/setup.py
@@ -44,7 +44,7 @@ setup(
     author_email='dcramer@gmail.com',
     url='http://github.com/dcramer/django-sentry',
     description = 'Exception Logging to a Database in Django',
-    packages=find_packages(exclude="example_project"),
+    packages=find_packages(exclude=("example_project",)),
     zip_safe=False,
     install_requires=install_requires,
     dependency_links=[
