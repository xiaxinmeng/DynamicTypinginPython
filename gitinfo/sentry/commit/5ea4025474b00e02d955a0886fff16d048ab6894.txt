commit 5ea4025474b00e02d955a0886fff16d048ab6894
Author: Billy Vong <billyvg@users.noreply.github.com>
Date:   Wed May 8 13:14:35 2019 -0700

    feat(ui): Add "Activity" to `SentryTypes` (#13103)

diff --git a/src/sentry/static/sentry/app/sentryTypes.jsx b/src/sentry/static/sentry/app/sentryTypes.jsx
index 2a8d1f343d..a7fe539a57 100644
--- a/src/sentry/static/sentry/app/sentryTypes.jsx
+++ b/src/sentry/static/sentry/app/sentryTypes.jsx
@@ -414,6 +414,16 @@ export const Incident = PropTypes.shape({
   isSubscribed: PropTypes.bool.isRequired,
 });
 
+export const Activity = PropTypes.shape({
+  id: PropTypes.string.isRequired,
+  type: PropTypes.string.isRequired,
+  dateCreated: PropTypes.string.isRequired,
+  user: User,
+  data: PropTypes.shape({
+    text: PropTypes.string,
+  }),
+});
+
 export const GlobalSelection = PropTypes.shape({
   projects: PropTypes.arrayOf(PropTypes.number),
   environments: PropTypes.arrayOf(PropTypes.string),
@@ -932,6 +942,7 @@ const SentryTypes = {
     id: PropTypes.string.isRequired,
   }),
   Actor,
+  Activity,
   AuthProvider,
   Config,
   Deploy,
