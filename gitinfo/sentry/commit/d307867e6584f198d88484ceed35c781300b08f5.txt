commit d307867e6584f198d88484ceed35c781300b08f5
Author: Priscila Oliveira <priscila.oliveira@sentry.io>
Date:   Thu Mar 5 17:43:13 2020 +0100

    ref(webpack): updated tsloader use logic (#17475)

diff --git a/webpack.config.js b/webpack.config.js
index c5554f8efc..87227468c4 100644
--- a/webpack.config.js
+++ b/webpack.config.js
@@ -237,7 +237,7 @@ let appConfig = {
         exclude: /(vendor|node_modules|dist)/,
         // Make sure we typecheck in CI, but not for local dev since that is run with
         // the fork-ts plugin
-        use: SHOULD_FORK_TS ? [babelLoaderConfig, tsLoaderConfig] : babelLoaderConfig,
+        use: SHOULD_FORK_TS ? babelLoaderConfig : [babelLoaderConfig, tsLoaderConfig],
       },
       {
         test: /\.po$/,
