commit 65b9457626c3c19091674ed0c5c0daf69e73fe4c
Author: ckj <chriskjennings@gmail.com>
Date:   Fri Jan 23 02:25:29 2015 -0500

    Stub out non-react org switcher

diff --git a/src/sentry/static/sentry/less/sentry.less b/src/sentry/static/sentry/less/sentry.less
index c6de0c1f44..00fc9148a0 100644
--- a/src/sentry/static/sentry/less/sentry.less
+++ b/src/sentry/static/sentry/less/sentry.less
@@ -353,15 +353,55 @@ ul, ol {
 
 .org-selector {
   border-bottom: 1px solid rgba(36, 39, 43, .8);
-  padding: 0 20px;
+  padding: 0;
   margin: 0 -20px 20px;
   height: 66px;
-  line-height: 70px;
+  position: relative;
 
   .active-org {
     font-size: 18px;
     font-weight: 600;
     line-height: 1;
+    padding: 24px 20px;
+    display: block;
+    position: relative;
+  }
+
+  .dropdown-toggle {
+    position: absolute;
+    top: 0;
+    right: 0;
+    padding: 22px 20px 20px 5px;
+    z-index: 10;
+  }
+
+  .dropdown-menu {
+    background: #fff;
+    border-radius: 0;
+    top: 66px;
+    width: @sidebar-width;
+    margin: 0;
+    padding: 0;
+  }
+
+  .orgs {
+    line-height: 1;
+    .list-unstyled();
+    margin-bottom: 0;
+
+    li {
+      a {
+        display: block;
+        padding: 20px;
+        font-size: 18px;
+        box-shadow: inset -1px 0 0 rgba(0,0,0, .12);
+
+        &:hover {
+          background: @blue;
+          color: #fff;
+        }
+      }
+    }
   }
 }
 
diff --git a/src/sentry/templates/sentry/layout.html b/src/sentry/templates/sentry/layout.html
index 85b75fc01b..5b3fa8252b 100644
--- a/src/sentry/templates/sentry/layout.html
+++ b/src/sentry/templates/sentry/layout.html
@@ -75,20 +75,20 @@
     <body class="{% block wrapperclass %}{% endblock %} {% if request.user.is_authenticated %}show-sidebar{% endif %}" ng-app="sentry">
       {% if request.user.is_authenticated %}
       <div class="app-sidebar">
-        <div class="org-selector">
+        <div class="org-selector dropdown">
           <a href="#" class="active-org">Sentry</a>
-          <a href="#" class="dropdown-toggle"></a>
+          <a href="#" class="dropdown-toggle" data-toggle="dropdown" onclick="$(this).parent('.dropdown').toggleClass('open');"><span class="icon-arrow-down"></span></a>
           {% if list_organizations|length > 1 %}
             <div class="dropdown-menu">
-              <ul>
+              <ul class="orgs">
                 {% for o in request.user|list_organizations %}
-                <li{% if o.id == organization.id %} class="active"{% endif %}>
-                  <a href="{% url 'sentry-organization-home' o.slug %}">
+                  {% if o.id != organization.id %}
+                  <li>
                     <a href="{% url 'sentry-organization-home' o.slug %}">
-                      {{ o.name }}
+                        {{ o.name }}
                     </a>
-                  </a>
-                </li>
+                  </li>
+                  {% endif %}
                 {% endfor %}
               </ul>
             </div>
