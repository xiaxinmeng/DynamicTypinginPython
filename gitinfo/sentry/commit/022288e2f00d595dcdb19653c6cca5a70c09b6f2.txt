commit 022288e2f00d595dcdb19653c6cca5a70c09b6f2
Author: Armin Ronacher <armin.ronacher@active-4.com>
Date:   Tue Jul 9 15:41:26 2019 +0200

    feat(loader): Make the default for new js projects v5 of js sdk (#13327)
    
    * feat(loader): Make the default for new js projects v5 of js sdk
    
    * fix: Tests

diff --git a/src/sentry/projectoptions/defaults.py b/src/sentry/projectoptions/defaults.py
index 86353ce63a..6ee00851db 100644
--- a/src/sentry/projectoptions/defaults.py
+++ b/src/sentry/projectoptions/defaults.py
@@ -54,6 +54,7 @@ register(
     key='sentry:default_loader_version',
     epoch_defaults={
         1: '4.x',
+        2: '5.x',
     }
 )
 
diff --git a/tests/sentry/web/frontend/test_js_sdk_loader.py b/tests/sentry/web/frontend/test_js_sdk_loader.py
index 583897938b..5a30be7d75 100644
--- a/tests/sentry/web/frontend/test_js_sdk_loader.py
+++ b/tests/sentry/web/frontend/test_js_sdk_loader.py
@@ -71,7 +71,7 @@ class JavaScriptSdkLoaderTest(TestCase):
         self.assertIn('s-maxage', resp['Cache-Control'])
         self.assertIn('max-age', resp['Cache-Control'])
         self.assertIn('project/%s' % self.projectkey.project_id, resp['Surrogate-Key'])
-        self.assertIn('sdk/%s' % mocked_version, resp['Surrogate-Key'])
+        self.assertIn('sdk/', resp['Surrogate-Key'])
         self.assertIn('sdk-loader', resp['Surrogate-Key'])
         assert 'Content-Encoding' not in resp
         assert 'Set-Cookie' not in resp
