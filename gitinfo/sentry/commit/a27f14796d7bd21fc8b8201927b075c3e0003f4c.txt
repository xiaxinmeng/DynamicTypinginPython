commit a27f14796d7bd21fc8b8201927b075c3e0003f4c
Author: adhiraj <693121+adhiraj@users.noreply.github.com>
Date:   Mon Feb 24 10:08:26 2020 -0800

    Update processor.py (#17252)
    
    It's a high volume event and we don't need all the data

diff --git a/src/sentry/rules/processor.py b/src/sentry/rules/processor.py
index 40289cc45d..6f3516b21c 100644
--- a/src/sentry/rules/processor.py
+++ b/src/sentry/rules/processor.py
@@ -7,6 +7,7 @@ from collections import namedtuple
 from datetime import timedelta
 from django.core.cache import cache
 from django.utils import timezone
+from random import randrange
 
 from sentry import analytics
 from sentry.models import GroupRuleStatus, Rule
@@ -110,13 +111,14 @@ class RuleProcessor(object):
         if not passed:
             return
 
-        analytics.record(
-            "issue_alert.fired",
-            issue_id=self.group.id,
-            project_id=rule.project.id,
-            organization_id=rule.project.organization.id,
-            rule_id=rule.id,
-        )
+        if randrange(10) == 0:
+            analytics.record(
+                "issue_alert.fired",
+                issue_id=self.group.id,
+                project_id=rule.project.id,
+                organization_id=rule.project.organization.id,
+                rule_id=rule.id,
+            )
 
         for action in rule.data.get("actions", ()):
             action_cls = rules.get(action["id"])
