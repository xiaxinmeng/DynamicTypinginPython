commit 69288cf00d0b5c2595d79e40cba72a13fc28ad84
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Mar 26 10:47:12 2013 -0700

    Allow users to see project global keys if SENTRY_PUBLIC is enabled

diff --git a/src/sentry/web/frontend/docs.py b/src/sentry/web/frontend/docs.py
index f62c009fa5..756941becf 100644
--- a/src/sentry/web/frontend/docs.py
+++ b/src/sentry/web/frontend/docs.py
@@ -8,6 +8,7 @@ sentry.web.frontend.projects
 from django.core.urlresolvers import reverse
 from django.http import HttpResponseRedirect
 
+from sentry.conf import settings
 from sentry.constants import (MEMBER_OWNER, PLATFORM_LIST, PLATFORM_TITLES,
     PLATFORM_ROOTS)
 from sentry.models import ProjectKey
@@ -18,6 +19,8 @@ from sentry.web.helpers import render_to_response, render_to_string
 def can_see_global_keys(user, project):
     if user.is_superuser:
         return True
+    if settings.PUBLIC:
+        return True
     if not project.team:
         return False
     if not project.team.member_set.filter(user=user, type=MEMBER_OWNER).exists():
