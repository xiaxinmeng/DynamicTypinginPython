commit b2e1d981cb80a90c2d0810ab5d9ca00003497d19
Author: ckj <chriskjennings@gmail.com>
Date:   Wed Sep 28 16:00:53 2016 -0700

    Polish event-level alerts

diff --git a/src/sentry/static/sentry/app/components/events/eventEntries.jsx b/src/sentry/static/sentry/app/components/events/eventEntries.jsx
index e2e41acaac..e8f1e35bc4 100644
--- a/src/sentry/static/sentry/app/components/events/eventEntries.jsx
+++ b/src/sentry/static/sentry/app/components/events/eventEntries.jsx
@@ -117,10 +117,11 @@ const EventEntries = React.createClass({
         }
         {!utils.objectIsEmpty(evt.sdk) && evt.sdk.upstream.isNewer &&
           <div className="alert-block alert-info box">
+            <span className="icon-exclamation"/>
             {t('This event was reported with an old version of the %s SDK.', evt.platform)}
             {evt.sdk.upstream.url &&
               <a href={evt.sdk.upstream.url}
-                 style={{marginLeft: 10}}>{t('Learn More')}</a>
+                 className="btn btn-sm btn-default">{t('Learn More')}</a>
             }
           </div>
         }
diff --git a/src/sentry/static/sentry/less/group-detail.less b/src/sentry/static/sentry/less/group-detail.less
index cd8b3fc8cb..25c8078688 100644
--- a/src/sentry/static/sentry/less/group-detail.less
+++ b/src/sentry/static/sentry/less/group-detail.less
@@ -789,6 +789,14 @@
       padding-top: 15px;
       padding-bottom: 15px;
 
+      .icon-exclamation {
+        font-size: 18px;
+        color: desaturate(darken(@alert-info-border-color, 15), 20%);
+        margin-right: 8px;
+        position: relative;
+        top: 2px;
+      }
+
       &.alert-info, &.alert-info + .box, &.alert-info + .context-summary {
         border-top: 1px solid @alert-info-border-color;
       }
diff --git a/src/sentry/static/sentry/less/shared-components.less b/src/sentry/static/sentry/less/shared-components.less
index ed824f4b31..e434119af5 100644
--- a/src/sentry/static/sentry/less/shared-components.less
+++ b/src/sentry/static/sentry/less/shared-components.less
@@ -2118,7 +2118,6 @@ ul.faces {
   p:last-child {
     margin: 0;
   }
-
   .icon {
     float: left;
     margin: 3px 8px 0 3px;
@@ -2169,6 +2168,12 @@ ul.faces {
   }
 }
 
+.alert-block {
+  .btn {
+    float: right;
+  }
+}
+
 .alert-block ul {
   padding-left: 20px;
 }
@@ -2187,6 +2192,10 @@ ul.faces {
   .icon:before {
     content: "\e615";
   }
+
+  .btn-default {
+    border-color: darken(@alert-danger-border-color, 5);
+  }
 }
 
 .alert-info {
@@ -2197,6 +2206,10 @@ ul.faces {
   .icon {
     display: none;
   }
+
+  .btn-default {
+    border-color: desaturate(darken(@alert-info-border-color, 10), 10);
+  }
 }
 
 .alert-success {
@@ -2206,6 +2219,10 @@ ul.faces {
   .icon:before {
     content: "\e60a";
   }
+
+  .btn-default {
+    border-color: darken(@alert-success-border-color, 5);
+  }
 }
 
 .active-alerts {
