commit 1c016d906d1596faf3b891bc909f68bed064b03f
Author: David Cramer <dcramer@gmail.com>
Date:   Sun Jan 8 12:49:11 2012 -0800

    Dont stringify the message twice

diff --git a/sentry/static/scripts/client.js b/sentry/static/scripts/client.js
index bfa0acea44..b06a54f314 100644
--- a/sentry/static/scripts/client.js
+++ b/sentry/static/scripts/client.js
@@ -100,7 +100,6 @@ if (Sentry === undefined) {
         var traceback;
         var stack;
         var headers;
-        var message = e.toString();
         var fileurl;
 
         if (e.line) { // WebKit
@@ -123,7 +122,7 @@ if (Sentry === undefined) {
         }
 
         var urlparts = self.parseUrl(url);
-        var label = e.name + ": " + message;
+        var label = e.toString();
         if (lineno) {
             label = label + " at " + lineno;
         }
@@ -150,7 +149,7 @@ if (Sentry === undefined) {
             "sentry.interfaces.Stacktrace": stack || undefined,
             "sentry.interfaces.Exception": {
                 "type": e.name,
-                "value": message
+                "value": e.message
             },
             "sentry.interfaces.Http": {
                 "url": urlparts.url,
