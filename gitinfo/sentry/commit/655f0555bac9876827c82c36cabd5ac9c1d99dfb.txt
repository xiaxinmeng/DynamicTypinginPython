commit 655f0555bac9876827c82c36cabd5ac9c1d99dfb
Author: Lyn Nagara <lyn.nagara@gmail.com>
Date:   Fri Jun 7 16:41:15 2019 -0700

    ref: Fix invalid jsdoc syntax (#13598)

diff --git a/src/sentry/static/sentry/app/stores/formSearchStore.jsx b/src/sentry/static/sentry/app/stores/formSearchStore.jsx
index 33d899846e..0cf8ef6995 100644
--- a/src/sentry/static/sentry/app/stores/formSearchStore.jsx
+++ b/src/sentry/static/sentry/app/stores/formSearchStore.jsx
@@ -22,7 +22,7 @@ const FormSearchStore = Reflux.createStore({
   /**
    * Adds to search map
    *
-   * @param Array searchMap array of objects: {route, field}
+   * @param {Array} searchMap array of objects: {route, field}
    */
   onLoadSearchMap(searchMap) {
     // Only load once
diff --git a/src/sentry/static/sentry/app/utils/getPeriod.jsx b/src/sentry/static/sentry/app/utils/getPeriod.jsx
index d02096102b..4ed361ec74 100644
--- a/src/sentry/static/sentry/app/utils/getPeriod.jsx
+++ b/src/sentry/static/sentry/app/utils/getPeriod.jsx
@@ -13,7 +13,7 @@ import {getUtcDateString} from 'app/utils/dates';
  * @param {Date} dateObj.end Ending date object
  * @param {Object} options Options
  * @param {Boolean} [options.shouldDoublePeriod] Doubles the given period (useful for getting previous period data)
- * @return Object Returns an object with either a period or start/end dates ({statsPeriod: string} or {start: string, end: string})
+ * @return {Object} Returns an object with either a period or start/end dates ({statsPeriod: string} or {start: string, end: string})
  */
 export const getPeriod = ({period, start, end}, {shouldDoublePeriod} = {}) => {
   if (!period && !start && !end) {
