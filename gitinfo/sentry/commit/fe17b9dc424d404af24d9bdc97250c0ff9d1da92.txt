commit fe17b9dc424d404af24d9bdc97250c0ff9d1da92
Author: Daniel Griesser <daniel.griesser.86@gmail.com>
Date:   Wed Aug 23 10:43:41 2017 -0700

    Adjust styling of discard button

diff --git a/src/sentry/static/sentry/app/views/projectProcessingIssues.jsx b/src/sentry/static/sentry/app/views/projectProcessingIssues.jsx
index f06b597984..1fc1ba598c 100644
--- a/src/sentry/static/sentry/app/views/projectProcessingIssues.jsx
+++ b/src/sentry/static/sentry/app/views/projectProcessingIssues.jsx
@@ -341,6 +341,16 @@ const ProjectProcessingIssues = React.createClass({
     return (
       <div>
         {fixLinkBlock}
+        <h3>
+          {t('Pending issues')}
+          <a
+            className="btn btn-default btn-sm pull-right"
+            onClick={() => {
+              this.discardEvents();
+            }}>
+            {t('Discard all')}
+          </a>
+        </h3>
         <div className="panel panel-default">
           <div className="panel-heading panel-heading-bold hidden-xs">
             <div className="row">
@@ -379,18 +389,6 @@ const ProjectProcessingIssues = React.createClass({
                 </div>
               );
             })}
-            <div className="list-group-item">
-              {t(
-                `In case you want to discard all events with pending processing issues: `
-              )}
-              <a
-                className="btn btn-sm btn-danger"
-                onClick={() => {
-                  this.discardEvents();
-                }}>
-                Discard all pending Events
-              </a>
-            </div>
           </div>
         </div>
       </div>
