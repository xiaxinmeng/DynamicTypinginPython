commit 50deb513fd605ea55c69e38f4e40a0430cfacc0c
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Sep 28 11:12:54 2015 -0700

    Dont render active user in seenBy
    
    This restores old functionaly from v7 which didn't render the current user
    in seenBy, and thus ensures doesnt render empty seenBy.
    
    Fixes GH-2014

diff --git a/src/sentry/static/sentry/app/views/groupDetails/seenBy.jsx b/src/sentry/static/sentry/app/views/groupDetails/seenBy.jsx
index 8614a159d6..22560ff732 100644
--- a/src/sentry/static/sentry/app/views/groupDetails/seenBy.jsx
+++ b/src/sentry/static/sentry/app/views/groupDetails/seenBy.jsx
@@ -1,6 +1,7 @@
 import React from "react";
 import moment from "moment";
 
+import ConfigStore from "../../stores/configStore";
 import Gravatar from "../../components/gravatar";
 import GroupState from "../../mixins/groupState";
 import {userDisplayName} from "../../utils/formatters";
@@ -16,9 +17,12 @@ var GroupSeenBy = React.createClass({
   ],
 
   render() {
-    var group = this.getGroup();
+    var activeUser = ConfigStore.get('user');
+    let group = this.getGroup();
 
-    var seenByNodes = group.seenBy.map((user, userIdx) => {
+    let seenByNodes = group.seenBy.filter((user, userIdx) => {
+      return activeUser.id !== user.id;
+    }).map((user, userIdx) => {
       let title = userDisplayName(user) + '<br/>' + moment(user.lastSeen).format("LL");
       return (
         <li key={userIdx} className="tip" data-title={title}>
@@ -27,7 +31,7 @@ var GroupSeenBy = React.createClass({
       );
     });
 
-    if (!seenByNodes) {
+    if (seenByNodes.length === 0) {
       return null;
     }
 
