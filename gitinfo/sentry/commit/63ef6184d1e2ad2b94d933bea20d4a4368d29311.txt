commit 63ef6184d1e2ad2b94d933bea20d4a4368d29311
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Sep 12 12:58:29 2012 -0700

    Correct bool filter on add_tags to check the value, not the tuple

diff --git a/src/sentry/manager.py b/src/sentry/manager.py
index 03e93d9d36..0a49fbe6c3 100644
--- a/src/sentry/manager.py
+++ b/src/sentry/manager.py
@@ -616,7 +616,7 @@ class GroupManager(BaseManager, ChartMixin):
         project = group.project
         date = group.last_seen
 
-        for key, value in itertools.ifilter(bool, tags):
+        for key, value in itertools.ifilter(lambda x: bool(x[1]), tags):
             if len(value) > MAX_TAG_LENGTH:
                 continue
 
