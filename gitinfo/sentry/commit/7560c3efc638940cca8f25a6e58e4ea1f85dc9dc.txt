commit 7560c3efc638940cca8f25a6e58e4ea1f85dc9dc
Author: David Cramer <dcramer@gmail.com>
Date:   Sat Dec 22 23:38:50 2012 -0800

    Change Status filters to build from constant

diff --git a/src/sentry/filters/builtins.py b/src/sentry/filters/builtins.py
index 8675a13ff2..4819afc5d7 100644
--- a/src/sentry/filters/builtins.py
+++ b/src/sentry/filters/builtins.py
@@ -8,6 +8,7 @@ sentry.filters.base
 from django.utils.datastructures import SortedDict
 from django.utils.translation import ugettext_lazy as _
 from sentry.conf import settings
+from sentry.constants import STATUS_LEVELS
 from .base import Filter, GroupFilter
 
 __all__ = ('StatusFilter', 'LoggerFilter', 'LevelFilter')
@@ -17,12 +18,10 @@ class StatusFilter(GroupFilter):
     label = _('Status')
     column = 'status'
     default = '0'
+    choices = SortedDict(STATUS_LEVELS)
 
     def get_choices(self):
-        return SortedDict([
-            (0, _('Unresolved')),
-            (1, _('Resolved')),
-        ])
+        return self.choices
 
 
 class LoggerFilter(Filter):
