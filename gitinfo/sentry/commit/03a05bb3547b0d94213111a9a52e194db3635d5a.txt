commit 03a05bb3547b0d94213111a9a52e194db3635d5a
Author: Brandon Adams <badams@opinionlab.com>
Date:   Tue Apr 17 20:03:25 2012 -0500

    patterns in sentry.conf/urls.py are first in line
    
    Reorder urlpatterns such that those found in sentry/conf/urls.py
    are processed before those in sentry/web/urls.py
    
    Resolves https://github.com/dcramer/sentry/issues/432

diff --git a/sentry/conf/urls.py b/sentry/conf/urls.py
index 8c4f00e73b..b41e97c51a 100644
--- a/sentry/conf/urls.py
+++ b/sentry/conf/urls.py
@@ -38,10 +38,10 @@ def handler500(request):
     t = loader.get_template('sentry/500.html')
     return HttpResponseServerError(t.render(Context(context)))
 
-urlpatterns += patterns('',
+urlpatterns = patterns('',
     (r'^i18n/', include('django.conf.urls.i18n')),
     (r'^admin/', include(admin.site.urls)),
     url(r'^_admin_media/(?P<path>.*)$', generic.static_media,
         kwargs={'root': admin_media_dir},
         name='admin-media'),
-)
+) + urlpatterns
