commit ff0b21dc4b7e09e128ea50c6bde0a6afda122b6f
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Sep 6 16:08:12 2011 -0700

    Use pop not get

diff --git a/sentry/client/base.py b/sentry/client/base.py
index a44feff82d..5dd57efc37 100644
--- a/sentry/client/base.py
+++ b/sentry/client/base.py
@@ -296,7 +296,7 @@ class SentryClient(object):
         if record.exc_info and all(record.exc_info):
             return self.create_from_exception(record.exc_info, **kwargs)
 
-        data = kwargs.get('data', {}) or {}
+        data = kwargs.pop('data', {}) or {}
         data['__sentry__'] = {}
         if getattr(record, 'stack', settings.AUTO_LOG_STACKS):
             stack = []
