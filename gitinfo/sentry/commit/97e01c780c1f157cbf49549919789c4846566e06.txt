commit 97e01c780c1f157cbf49549919789c4846566e06
Author: adhiraj <693121+adhiraj@users.noreply.github.com>
Date:   Fri Mar 13 16:02:09 2020 -0700

    chore(alerts) Update empty state text (#17551)
    
    * chore(alerts) update empty state in alert stream
    
    * Update

diff --git a/src/sentry/static/sentry/app/views/alerts/list/index.tsx b/src/sentry/static/sentry/app/views/alerts/list/index.tsx
index a0480c218d..5cce5021cc 100644
--- a/src/sentry/static/sentry/app/views/alerts/list/index.tsx
+++ b/src/sentry/static/sentry/app/views/alerts/list/index.tsx
@@ -108,7 +108,7 @@ class IncidentsList extends AsyncComponent<Props, State & AsyncComponent['state'
   renderEmpty() {
     return (
       <EmptyStateWarning>
-        <p>{t("You don't have any Alerts yet")}</p>
+        <p>{t("You don't have any Metric Alerts yet")}</p>
       </EmptyStateWarning>
     );
   }
diff --git a/tests/js/spec/views/incidents/list/index.spec.jsx b/tests/js/spec/views/incidents/list/index.spec.jsx
index c1c5cbe67a..ba24145a01 100644
--- a/tests/js/spec/views/incidents/list/index.spec.jsx
+++ b/tests/js/spec/views/incidents/list/index.spec.jsx
@@ -93,7 +93,7 @@ describe('IncidentsList', function() {
 
     wrapper = await createWrapper();
     expect(wrapper.find('PanelItem')).toHaveLength(0);
-    expect(wrapper.text()).toContain("You don't have any Alerts yet");
+    expect(wrapper.text()).toContain("You don't have any Metric Alerts yet");
   });
 
   it('toggles all/open', async function() {
