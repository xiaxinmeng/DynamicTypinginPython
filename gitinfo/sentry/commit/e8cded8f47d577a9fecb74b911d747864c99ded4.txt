commit e8cded8f47d577a9fecb74b911d747864c99ded4
Author: jpellerin <jpellerin@jpdesk.(none)>
Date:   Tue Jan 11 09:31:26 2011 -0500

    Updated group view to handle display of messages where __sentry__ does not contain exc (for instance, messages from the logging handler).

diff --git a/sentry/views.py b/sentry/views.py
index bbd5674bb4..13d3ae587f 100644
--- a/sentry/views.py
+++ b/sentry/views.py
@@ -235,7 +235,7 @@ def group(request, group_id):
     group = get_object_or_404(GroupedMessage, pk=group_id)
 
     obj = group.message_set.all().order_by('-id')[0]
-    if '__sentry__' in obj.data:
+    if '__sentry__' in obj.data and 'exc' in obj.data['__sentry__']:
         module, args, frames = obj.data['__sentry__']['exc']
         obj.class_name = str(obj.class_name)
         # We fake the exception class due to many issues with imports/builtins/etc
