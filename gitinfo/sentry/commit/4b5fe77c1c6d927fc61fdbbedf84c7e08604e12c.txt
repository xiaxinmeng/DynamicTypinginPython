commit 4b5fe77c1c6d927fc61fdbbedf84c7e08604e12c
Author: Chris Clark <christopherchristopherclark@gmail.com>
Date:   Thu Mar 14 10:55:35 2019 -0700

    fix(alerts): panel alert borders (#12398)
    
    * fix panel alert borders
    
    * don't need calc

diff --git a/src/sentry/static/sentry/app/components/panels/panelAlert.jsx b/src/sentry/static/sentry/app/components/panels/panelAlert.jsx
index cdbee9b53e..a25847db52 100644
--- a/src/sentry/static/sentry/app/components/panels/panelAlert.jsx
+++ b/src/sentry/static/sentry/app/components/panels/panelAlert.jsx
@@ -13,9 +13,9 @@ const DEFAULT_ICONS = {
 
 // Margin bottom should probably be a different prop
 const PanelAlert = styled(({type, icon, ...props}) => (
-  <Alert {...props} icon={icon || DEFAULT_ICONS[type]} type={type} />
+  <Alert {...props} icon={icon || DEFAULT_ICONS[type]} type={type} system={true} />
 ))`
-  margin: -1px;
+  margin: 1px 0;
   border-radius: 0;
 `;
 
