commit e3353204bffe1d72466b74bcdc00d8e693a22fbc
Author: Philippe Lammerts <P.M.Lammerts@student.tudelft.nl>
Date:   Fri Apr 10 17:18:16 2020 +0200

    fix(views): Add warning for empty alert rule name (#18179)
    
    Add a PanelAlert above the empty rule name field.
    Triggers when the user tries to save a new alert rule.
    
    Fixes #18153

diff --git a/src/sentry/static/sentry/app/views/settings/projectAlerts/issueEditor/index.tsx b/src/sentry/static/sentry/app/views/settings/projectAlerts/issueEditor/index.tsx
index 2d918fb3a8..8387f2958b 100644
--- a/src/sentry/static/sentry/app/views/settings/projectAlerts/issueEditor/index.tsx
+++ b/src/sentry/static/sentry/app/views/settings/projectAlerts/issueEditor/index.tsx
@@ -458,6 +458,11 @@ class IssueRuleEditor extends AsyncView<Props, State> {
 
           <Panel>
             <PanelHeader>{t('Give your rule a name')}</PanelHeader>
+
+            {this.hasError('name') && (
+              <PanelAlert type="error">{t('Must enter a rule name')}</PanelAlert>
+            )}
+
             <PanelBody>
               <TextField
                 label={t('Rule name')}
