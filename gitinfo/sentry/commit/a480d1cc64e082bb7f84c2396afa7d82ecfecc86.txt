commit a480d1cc64e082bb7f84c2396afa7d82ecfecc86
Author: Evan Purkhiser <evanpurkhiser@gmail.com>
Date:   Thu May 30 13:23:39 2019 -0700

    ref(onboarding): Improve alerts from wizard docs (#13469)

diff --git a/src/sentry/static/sentry/app/views/onboarding/projectSetup/projectDocs.jsx b/src/sentry/static/sentry/app/views/onboarding/projectSetup/projectDocs.jsx
index 6124e464fd..2f7651517c 100644
--- a/src/sentry/static/sentry/app/views/onboarding/projectSetup/projectDocs.jsx
+++ b/src/sentry/static/sentry/app/views/onboarding/projectSetup/projectDocs.jsx
@@ -245,9 +245,10 @@ AnimatedPlatformHeading.propTypes = {
   platform: PropTypes.string.isRequired,
 };
 
+const getAlertClass = type => (type === 'default' ? 'alert' : `alert-${type}`);
 const mapAlertStyles = p => type =>
   css`
-    .alert-${type} {
+    .${getAlertClass(type)} {
       ${alertStyles({theme: p.theme, type})};
       display: block;
     }
@@ -284,6 +285,13 @@ const DocsWrapper = styled(posed.div(docsTransition))`
     margin-bottom: 1rem;
   }
 
+  /**
+   * XXX(epurkhiser): This comes from the doc styles and avoids bottom margin issues in alerts
+   */
+  .content-flush-bottom *:last-child {
+    margin-bottom: 0;
+  }
+
   ${p => Object.keys(p.theme.alert).map(mapAlertStyles(p))}
 `;
 
