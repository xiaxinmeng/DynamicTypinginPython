commit 63e5ada9095aaab1532578a4e3a87b2ec8284cf0
Author: Armin Ronacher <armin.ronacher@active-4.com>
Date:   Thu Apr 20 01:05:20 2017 +0200

    Use two null bytes to separate filename from dist for hashing

diff --git a/src/sentry/models/releasefile.py b/src/sentry/models/releasefile.py
index f52d31528b..0c4aa3a9f3 100644
--- a/src/sentry/models/releasefile.py
+++ b/src/sentry/models/releasefile.py
@@ -55,5 +55,5 @@ class ReleaseFile(Model):
     @classmethod
     def get_ident(cls, name, dist=None):
         if dist is not None:
-            return sha1_text(name + '@@' + dist).hexdigest()
+            return sha1_text(name + '\x00\x00' + dist).hexdigest()
         return sha1_text(name).hexdigest()
