commit bb4f6afd34114de82a7b227b4faba8234ebb4026
Author: Esben Sparre Andreasen <42067045+esben-semmle@users.noreply.github.com>
Date:   Mon Jan 28 21:21:01 2019 +0100

    fix: Ensure the `fetchData` method uses the right value of `this` (#11671)
    
    When the `this.fetchData` methods are invoked as `onRetry` callbacks
    below, they will be invoked as function calls, making `this` have an
    unexpected value.
    
    This is a similar fix to the one in: https://github.com/getsentry/sentry/pull/11566/commits/e3436678970b0bc973057b11c3a7e68135d4f46e

diff --git a/src/sentry/static/sentry/app/views/groupDetails/shared/groupTags.jsx b/src/sentry/static/sentry/app/views/groupDetails/shared/groupTags.jsx
index 3ae18bbd55..d172fce801 100644
--- a/src/sentry/static/sentry/app/views/groupDetails/shared/groupTags.jsx
+++ b/src/sentry/static/sentry/app/views/groupDetails/shared/groupTags.jsx
@@ -43,7 +43,7 @@ class GroupTags extends React.Component {
     }
   }
 
-  fetchData() {
+  fetchData = () => {
     const {api, query, group} = this.props;
     this.setState({
       loading: true,
@@ -66,7 +66,7 @@ class GroupTags extends React.Component {
         });
       },
     });
-  }
+  };
 
   getTagsDocsUrl() {
     return 'https://docs.sentry.io/hosted/learn/context/';
diff --git a/src/sentry/static/sentry/app/views/releases/detail/shared/releaseArtifacts.jsx b/src/sentry/static/sentry/app/views/releases/detail/shared/releaseArtifacts.jsx
index 972ddf8671..361a8ec829 100644
--- a/src/sentry/static/sentry/app/views/releases/detail/shared/releaseArtifacts.jsx
+++ b/src/sentry/static/sentry/app/views/releases/detail/shared/releaseArtifacts.jsx
@@ -51,7 +51,7 @@ class ReleaseArtifacts extends React.Component {
       : `/organizations/${orgId}/releases/${encodedVersion}/files/`;
   }
 
-  fetchData() {
+  fetchData = () => {
     this.setState({
       loading: true,
       error: false,
@@ -75,7 +75,7 @@ class ReleaseArtifacts extends React.Component {
         });
       },
     });
-  }
+  };
 
   handleRemove(id) {
     let loadingIndicator = IndicatorStore.add(t('Removing artifact..'));
