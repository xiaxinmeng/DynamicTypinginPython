commit 724ec6fa752f92dca38ac9c533e2ba4a9f838e44
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Dec 26 16:48:27 2011 -0800

    Handle non dict post bodies in replay form

diff --git a/sentry/web/frontend/events.py b/sentry/web/frontend/events.py
index cf4a1bd039..3a5a7ddb31 100644
--- a/sentry/web/frontend/events.py
+++ b/sentry/web/frontend/events.py
@@ -65,11 +65,16 @@ def replay_event(request, project_id, event_id):
     else:
         headers = ''
 
+    if isinstance(http.data, dict):
+        data = urllib.urlencode(http.data)
+    else:
+        data = http.data
+
     initial = {
         'url': http.url,
         'method': http.method,
         'headers': headers,
-        'data': urllib.urlencode(http.data),
+        'data': data,
     }
 
     form = ReplayForm(request.POST or None, initial=initial)
