commit 723726fba36f634f4ec8665659dcdcbe20ccaa52
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Sep 16 01:49:17 2014 -0700

    Add status and assignment queries

diff --git a/src/sentry/api/endpoints/project_group_index.py b/src/sentry/api/endpoints/project_group_index.py
index f0f59c7ddc..67c9d38c01 100644
--- a/src/sentry/api/endpoints/project_group_index.py
+++ b/src/sentry/api/endpoints/project_group_index.py
@@ -100,7 +100,7 @@ class ProjectGroupIndexEndpoint(Endpoint):
 
         query = request.GET.get('query')
         if query is not None:
-            query_kwargs.update(parse_query(query))
+            query_kwargs.update(parse_query(query, request.user))
 
         results = search.query(**query_kwargs)
 
diff --git a/src/sentry/search/utils.py b/src/sentry/search/utils.py
index 2790d51bca..d3f55f98aa 100644
--- a/src/sentry/search/utils.py
+++ b/src/sentry/search/utils.py
@@ -1,17 +1,36 @@
 from __future__ import absolute_import, division, print_function
 
+from sentry.constants import STATUS_CHOICES
+from sentry.utils.auth import find_users
 
-def parse_query(query):
+
+def parse_query(query, user):
     # TODO(dcramer): make this better
     tokens = query.split(' ')
 
     results = {'tags': {}, 'query': []}
     for token in tokens:
-        if ':' in token:
-            key, value = token.split(':', 1)
-            results['tags'][key] = value
-        else:
+        if ':' not in token:
             results['query'].append(token)
+            continue
+
+        key, value = token.split(':', 1)
+
+        if key == 'is':
+            try:
+                results['status'] = STATUS_CHOICES[value]
+            except KeyError:
+                pass
+        elif key == 'assigned':
+            if value == 'me':
+                results['assigned_to'] = user
+            else:
+                try:
+                    results['assigned_to'] = find_users(value)[0]
+                except IndexError:
+                    pass
+        else:
+            results['tags'][key] = value
 
     results['query'] = ' '.join(results['query'])
 
diff --git a/src/sentry/web/frontend/groups.py b/src/sentry/web/frontend/groups.py
index 9c4c0e2f30..40c061b63a 100644
--- a/src/sentry/web/frontend/groups.py
+++ b/src/sentry/web/frontend/groups.py
@@ -106,7 +106,7 @@ def _get_group_list(request, project):
 
     query = request.GET.get('query')
     if query is not None:
-        query_kwargs.update(parse_query(query))
+        query_kwargs.update(parse_query(query, request.user))
 
     results = app.search.query(**query_kwargs)
 
