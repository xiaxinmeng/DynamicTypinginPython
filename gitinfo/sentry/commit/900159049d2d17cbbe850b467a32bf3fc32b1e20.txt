commit 900159049d2d17cbbe850b467a32bf3fc32b1e20
Author: David Cramer <dcramer@gmail.com>
Date:   Thu Jan 17 20:15:21 2013 -0800

    Dont fetch javascript sources that already have provided context

diff --git a/src/sentry/tasks/fetch_source.py b/src/sentry/tasks/fetch_source.py
index 1d29113c3e..daac208421 100644
--- a/src/sentry/tasks/fetch_source.py
+++ b/src/sentry/tasks/fetch_source.py
@@ -69,7 +69,9 @@ def fetch_javascript_source(event, **kwargs):
 
     # build list of frames that we can actually grab source for
     frames = [f for f in stacktrace['frames']
-        if f.get('lineno') is not None and f.get('abs_path', '').startswith(('http://', 'https://'))]
+        if f.get('lineno') is not None
+            and f.get('abs_path', '').startswith(('http://', 'https://')
+            and not f.get('context_line'))]
     if not frames:
         logger.info('Event %r has no frames with enough context to fetch remote source', event.id)
         return
