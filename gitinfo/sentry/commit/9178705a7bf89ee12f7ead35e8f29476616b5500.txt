commit 9178705a7bf89ee12f7ead35e8f29476616b5500
Author: Lyn Nagara <lyn.nagara@gmail.com>
Date:   Wed Aug 7 17:18:45 2019 -0700

    ref(ts): Convert formatters.jsx to TypeScript (#14302)
    
    Add the User and CommitAuthor type

diff --git a/src/sentry/static/sentry/app/types/index.tsx b/src/sentry/static/sentry/app/types/index.tsx
index 4ff6245a38..4894086ea8 100644
--- a/src/sentry/static/sentry/app/types/index.tsx
+++ b/src/sentry/static/sentry/app/types/index.tsx
@@ -52,3 +52,15 @@ export type EventsStats = {
   data: EventsStatsData;
   totals?: {count: number};
 };
+
+export type User = {
+  id: string;
+  name: string;
+  username: string;
+  email: string;
+};
+
+export type CommitAuthor = {
+  email?: string;
+  name?: string;
+};
diff --git a/src/sentry/static/sentry/app/utils/formatters.jsx b/src/sentry/static/sentry/app/utils/formatters.tsx
similarity index 69%
rename from src/sentry/static/sentry/app/utils/formatters.jsx
rename to src/sentry/static/sentry/app/utils/formatters.tsx
index 488a42ee95..bc4b0e4518 100644
--- a/src/sentry/static/sentry/app/utils/formatters.jsx
+++ b/src/sentry/static/sentry/app/utils/formatters.tsx
@@ -1,8 +1,9 @@
 import {get} from 'lodash';
 
 import {t} from 'app/locale';
+import {CommitAuthor, User} from 'app/types';
 
-export function userDisplayName(user) {
+export function userDisplayName(user: User | CommitAuthor): string {
   let displayName = String(get(user, 'name', t('Unknown author'))).trim();
 
   if (displayName.length <= 0) {
@@ -12,7 +13,7 @@ export function userDisplayName(user) {
   const email = String(get(user, 'email', '')).trim();
 
   if (email.length > 0 && email !== displayName) {
-    displayName += ' (' + user.email + ')';
+    displayName += ' (' + email + ')';
   }
   return displayName;
 }
