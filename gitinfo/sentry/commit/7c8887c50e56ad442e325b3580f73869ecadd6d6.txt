commit 7c8887c50e56ad442e325b3580f73869ecadd6d6
Author: David Cramer <dcramer@gmail.com>
Date:   Sat Aug 3 13:21:35 2013 -0700

    Generate a sample event when a new project is created

diff --git a/src/sentry/web/frontend/teams.py b/src/sentry/web/frontend/teams.py
index 822be389ca..e5e162b1d2 100644
--- a/src/sentry/web/frontend/teams.py
+++ b/src/sentry/web/frontend/teams.py
@@ -18,6 +18,7 @@ from sentry.permissions import (can_add_team_member, can_remove_team, can_create
     can_create_teams, can_edit_team_member, can_remove_team_member,
     Permissions)
 from sentry.plugins import plugins
+from sentry.utils.samples import create_sample_event
 from sentry.web.decorators import login_required, has_access
 from sentry.web.forms.teams import (NewTeamForm, NewTeamAdminForm,
     EditTeamForm, EditTeamAdminForm, EditTeamMemberForm, NewTeamMemberForm,
@@ -411,6 +412,8 @@ def create_new_team_project(request, team):
             project.owner = request.user
         project.save()
 
+        create_sample_event(project)
+
         if project.platform not in (None, 'other'):
             return HttpResponseRedirect(reverse('sentry-docs-client', args=[project.team.slug, project.slug, project.platform]))
         return HttpResponseRedirect(reverse('sentry-get-started', args=[project.team.slug, project.slug]))
