commit 39d2a0f627ccc204e4e64920044eb3fc9e743401
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Aug 5 21:29:49 2014 -0700

    Merge in times_seen total

diff --git a/src/sentry/tasks/merge.py b/src/sentry/tasks/merge.py
index 41ab58a6d0..147920905a 100644
--- a/src/sentry/tasks/merge.py
+++ b/src/sentry/tasks/merge.py
@@ -9,6 +9,7 @@ sentry.tasks.merge
 from __future__ import absolute_import
 
 from django.db import IntegrityError, transaction
+from django.db.models import F
 
 from sentry.tasks.base import instrumented_task, retry
 
@@ -46,6 +47,10 @@ def merge_group(from_object_id, to_object_id, **kwargs):
         )
         return
 
+    new_group.update(
+        times_seen=F('times_seen') + group.times_seen,
+    )
+
     group.delete()
 
 
