commit 3a69ed330b42b516e3df10533cf28907503260a2
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Wed Feb 24 09:54:20 2016 -0800

    Guard frame rendering against poorly shaped context data
    
    The root cause is the data is being sent from the client in an
    unexpected shape. This just makes the rendering of the page not fail.
    Need to fix yet why the data is being sent this way.
    
    Sorta fixes GH-2745

diff --git a/src/sentry/static/sentry/app/components/events/interfaces/frame.jsx b/src/sentry/static/sentry/app/components/events/interfaces/frame.jsx
index abe50ec6a4..4862d78757 100644
--- a/src/sentry/static/sentry/app/components/events/interfaces/frame.jsx
+++ b/src/sentry/static/sentry/app/components/events/interfaces/frame.jsx
@@ -151,7 +151,7 @@ const Frame = React.createClass({
 
             let lineWs;
             let lineCode;
-            if (defined(line[1])) {
+            if (defined(line[1]) && line[1].match) {
               [, lineWs, lineCode] = line[1].match(/^(\s*)(.*?)$/m);
             } else {
               lineWs = '';
