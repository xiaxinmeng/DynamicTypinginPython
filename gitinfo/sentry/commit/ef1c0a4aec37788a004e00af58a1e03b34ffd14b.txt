commit ef1c0a4aec37788a004e00af58a1e03b34ffd14b
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Apr 14 17:09:21 2015 -0700

    Update identity on login

diff --git a/src/sentry/auth/helper.py b/src/sentry/auth/helper.py
index d7bcb480e4..9a05c86cce 100644
--- a/src/sentry/auth/helper.py
+++ b/src/sentry/auth/helper.py
@@ -340,6 +340,14 @@ class AuthHelper(object):
                     'identity': identity,
                 })
         else:
+            # TODO(dcramer): this is very similar to attach
+            now = timezone.now()
+            auth_identity.update(
+                data=identity.get('data', {}),
+                last_verified=now,
+                last_synced=now,
+            )
+
             member = OrganizationMember.objects.get(
                 user=auth_identity.user,
                 organization=self.organization,
