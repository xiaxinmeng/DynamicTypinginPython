commit e12142878908a7dc3de62e7f62163682b04a4887
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Aug 24 19:34:55 2015 -0700

    Add tooltip for resolve/bookmark actions

diff --git a/src/sentry/static/sentry/app/views/stream/actionLink.jsx b/src/sentry/static/sentry/app/views/stream/actionLink.jsx
index 614a048b33..75cb8cd093 100644
--- a/src/sentry/static/sentry/app/views/stream/actionLink.jsx
+++ b/src/sentry/static/sentry/app/views/stream/actionLink.jsx
@@ -3,9 +3,16 @@ import OverlayMixin from "react-bootstrap/OverlayMixin";
 var PureRenderMixin = require('react/addons').addons.PureRenderMixin;
 import React from "react";
 import SelectedGroupStore from "../../stores/selectedGroupStore";
+import TooltipMixin from "../../mixins/tooltip";
 
 var ActionLink = React.createClass({
-  mixins: [OverlayMixin, PureRenderMixin],
+  mixins: [
+    OverlayMixin, PureRenderMixin,
+    TooltipMixin({
+      html: false,
+      container: 'body'
+    })
+  ],
 
   propTypes: {
     actionLabel: React.PropTypes.string,
@@ -68,8 +75,12 @@ var ActionLink = React.createClass({
     if (this.props.disabled) {
       className += ' disabled';
     }
+    className += ' tip';
     return (
-      <a title={this.props.buttonTitle} className={className} disabled={this.props.disabled} onClick={this.handleToggle}>
+      <a title={this.props.tooltip || this.props.buttonTitle}
+         className={className}
+         disabled={this.props.disabled}
+         onClick={this.handleToggle}>
         {this.props.children}
       </a>
     );
diff --git a/src/sentry/static/sentry/app/views/stream/actions.jsx b/src/sentry/static/sentry/app/views/stream/actions.jsx
index 36fae6a990..dafedbd132 100644
--- a/src/sentry/static/sentry/app/views/stream/actions.jsx
+++ b/src/sentry/static/sentry/app/views/stream/actions.jsx
@@ -148,6 +148,7 @@ var StreamActions = React.createClass({
                onAction={this.onUpdate.bind(this, {status: "resolved"})}
                buttonTitle="Resolve"
                confirmLabel="Resolve"
+               tooltip="Set Status to Resolved"
                canActionAll={true}
                onlyIfBulk={true}
                selectAllActive={this.state.selectAllActive}
@@ -162,6 +163,7 @@ var StreamActions = React.createClass({
                neverConfirm={true}
                buttonTitle="Bookmark"
                confirmLabel="Bookmark"
+               tooltip="Add to Bookmarks"
                canActionAll={false}
                onlyIfBulk={true}
                selectAllActive={this.state.selectAllActive}
