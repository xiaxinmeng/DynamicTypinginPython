commit 980acb0704af4517f13e8cda84f4516cdbf53921
Author: ckj <chriskjennings@gmail.com>
Date:   Mon Oct 12 17:31:56 2015 -0700

    Experiment with hidden pre-header for client summaries

diff --git a/src/sentry/templates/sentry/emails/activity/assigned.html b/src/sentry/templates/sentry/emails/activity/assigned.html
index 7a038a4510..342b27845b 100644
--- a/src/sentry/templates/sentry/emails/activity/assigned.html
+++ b/src/sentry/templates/sentry/emails/activity/assigned.html
@@ -1,5 +1,9 @@
 {% extends "sentry/emails/base.html" %}
 
+{% block preheader %}
+  {{ author }} assigned you an issue in {{ group.organization.name }}/{{ group.project.name }}.
+{% endblock %}
+
 {% block header %}
   <a href="{{ link }}" class="btn">View on Sentry</a>
   {{ block.super }}
@@ -9,7 +13,7 @@
   <h2>Assigned</h2>
 
   <blockquote>
-    <strong>{{ author }}</strong> assigned this event to you.
+    <strong>{{ author }}</strong> assigned this issue to you in {{ group.organization.name }}/{{ group.project.name }}.
   </blockquote>
 
   {% include "sentry/emails/group_header.html" %}
diff --git a/src/sentry/templates/sentry/emails/activity/note.html b/src/sentry/templates/sentry/emails/activity/note.html
index 5323ebe71c..66f0c9f0ff 100644
--- a/src/sentry/templates/sentry/emails/activity/note.html
+++ b/src/sentry/templates/sentry/emails/activity/note.html
@@ -1,12 +1,16 @@
 {% extends "sentry/emails/base.html" %}
 
+{% block preheader %}
+  New note by {{ author }} in {{ group.organization.name }}/{{ group.project.name }}.
+{% endblock %}
+
 {% block header %}
   <a href="{{ activity_link }}" class="btn">View on Sentry</a>
   {{ block.super }}
 {% endblock %}
 
 {% block main %}
-  <h2>New Note by <span class="highlight">{{ author }}</span></h2>
+  <h2>New Note by <span class="highlight">{{ author }}</span> in {{ group.organization.name }}/{{ group.project.name }}</h2>
 
   <div class="note-body">{{ data.text|urlize|linebreaks }}</div>
 
diff --git a/src/sentry/templates/sentry/emails/base.html b/src/sentry/templates/sentry/emails/base.html
index b5bd4f9b77..edf270424c 100644
--- a/src/sentry/templates/sentry/emails/base.html
+++ b/src/sentry/templates/sentry/emails/base.html
@@ -68,6 +68,16 @@
     text-align: left;
     padding: 0 20px;
   }
+
+  .preheader {
+    display: none;
+    font-size: 0;
+    max-height: 0;
+    line-height: 0;
+    padding: 0; (optional)
+    mso-hide: all; /* hide elements in Outlook 2007-2013 */
+  }
+
   .header {
     padding: 25px 0;
     font-size: 14px;
@@ -294,6 +304,7 @@
   </style>
 </head>
 <body{% block bodyclass %}{% endblock %}>
+<div class="preheader">{% block preheader %}{% endblock %}</div>
 <table class="main">
   <tr>
     <td>
