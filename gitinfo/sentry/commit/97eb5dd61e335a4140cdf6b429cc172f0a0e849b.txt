commit 97eb5dd61e335a4140cdf6b429cc172f0a0e849b
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Thu Sep 10 10:14:00 2015 -0700

    plugins/sentry_mail: decode subject as utf8 to prevent UnicodeDecodeError
    
    Fixes GH-1937

diff --git a/src/sentry/plugins/sentry_mail/models.py b/src/sentry/plugins/sentry_mail/models.py
index 79e78fcdb1..8ff658f1f1 100644
--- a/src/sentry/plugins/sentry_mail/models.py
+++ b/src/sentry/plugins/sentry_mail/models.py
@@ -11,6 +11,7 @@ import sentry
 
 from django.conf import settings
 from django.core.urlresolvers import reverse
+from django.utils.encoding import force_text
 from django.utils.safestring import mark_safe
 
 from sentry.plugins import register
@@ -40,6 +41,8 @@ class MailPlugin(NotificationPlugin):
             return
 
         subject_prefix = self.get_option('subject_prefix', project) or self.subject_prefix
+        subject_prefix = force_text(subject_prefix)
+        subject = force_text(subject)
 
         msg = MessageBuilder(
             subject='%s%s' % (subject_prefix, subject),
