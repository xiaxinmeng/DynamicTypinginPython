commit 06eec65f1448afb2bec0d2b3f6e14e0617e20659
Author: Katie Byers <lobsterkatie@gmail.com>
Date:   Mon Mar 25 11:32:44 2019 -0700

    fix(global issue stream): Re-enable resolve menu options (#12516)
    
    The enabled/disabled status was erroneously depending on org features rather than whether or not the project had any associated releases.

diff --git a/src/sentry/static/sentry/app/views/organizationStream/overview.jsx b/src/sentry/static/sentry/app/views/organizationStream/overview.jsx
index 7e0c74eadd..e34f355215 100644
--- a/src/sentry/static/sentry/app/views/organizationStream/overview.jsx
+++ b/src/sentry/static/sentry/app/views/organizationStream/overview.jsx
@@ -646,7 +646,7 @@ const OrganizationStream = createReactClass({
     const projects = this.getGlobalSearchProjects();
 
     if (selectedProject) {
-      hasReleases = this.getFeatures().has('releases');
+      hasReleases = new Set(selectedProject.features).has('releases');
       latestRelease = selectedProject.latestRelease;
       projectId = selectedProject.slug;
     } else if (projects.length == 1) {
