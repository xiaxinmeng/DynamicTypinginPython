commit cded7c0ab58312492b7b6976cb14b73dcc9093f4
Author: David Cramer <dcramer@gmail.com>
Date:   Sat Jan 26 17:47:25 2013 -0800

    Remove paragraph tags from activity stream

diff --git a/src/sentry/static/sentry/less/sentry.less b/src/sentry/static/sentry/less/sentry.less
index 19b4636a04..f3cf61af1e 100644
--- a/src/sentry/static/sentry/less/sentry.less
+++ b/src/sentry/static/sentry/less/sentry.less
@@ -1859,14 +1859,15 @@ section.body {
   margin: 0 0 20px;
 
   > li {
-    line-height: 20px;
+    margin-bottom: 5px;
+    line-height: 26px;
     position: relative;
     padding-left: 20px;
 
     .avatar {
       position: absolute;
       left: 0;
-      top: 2px;
+      top: 4px;
       width: 16px;
       height: 16px;
       display: block;
diff --git a/src/sentry/templatetags/sentry_activity.py b/src/sentry/templatetags/sentry_activity.py
index fd919a7863..259dc7d403 100644
--- a/src/sentry/templatetags/sentry_activity.py
+++ b/src/sentry/templatetags/sentry_activity.py
@@ -35,7 +35,7 @@ def render_activity(item):
 
     action_str = ACTIVITY_ACTION_STRINGS[item.type]
 
-    output = '<p>'
+    output = ''
 
     if item.user:
         user = item.user
@@ -46,7 +46,7 @@ def render_activity(item):
         output += '<span class="avatar sentry"></span> '
         output += 'The system %s' % (action_str,)
 
-    output += ' <span class="sep">&mdash;</span> <span class="time">%s</span></p>' % (timesince(item.datetime),)
+    output += ' <span class="sep">&mdash;</span> <span class="time">%s</span>' % (timesince(item.datetime),)
 
     if item.type == Activity.COMMENT:
         output += linebreaks(item.data['body'])
