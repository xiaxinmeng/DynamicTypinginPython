commit 58885ae21f7795d3acf76b068b2c726a020cf2bb
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Thu May 19 13:30:42 2016 -0700

    Hide celery app version check when CELERY_ALWAYS_EAGER is on

diff --git a/src/sentry/status_checks/celery_app_version.py b/src/sentry/status_checks/celery_app_version.py
index 4331de3211..862c43b388 100644
--- a/src/sentry/status_checks/celery_app_version.py
+++ b/src/sentry/status_checks/celery_app_version.py
@@ -2,6 +2,7 @@ from __future__ import absolute_import
 
 import sentry
 
+from django.conf import settings
 from sentry import options
 
 from .base import StatusCheck, Problem
@@ -9,6 +10,9 @@ from .base import StatusCheck, Problem
 
 class CeleryAppVersionCheck(StatusCheck):
     def check(self):
+        # There is no queue, and celery is not running, so never show error
+        if settings.CELERY_ALWAYS_EAGER:
+            return []
         version = options.get('sentry:last_worker_version')
         if not version:
             return []
