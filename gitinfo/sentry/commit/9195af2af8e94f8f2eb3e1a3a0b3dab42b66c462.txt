commit 9195af2af8e94f8f2eb3e1a3a0b3dab42b66c462
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Dec 26 06:29:49 2011 -0800

    Clean up charts and various bits

diff --git a/sentry/templates/sentry/groups/details.html b/sentry/templates/sentry/groups/details.html
index 5ff9128b64..64a34cc249 100644
--- a/sentry/templates/sentry/groups/details.html
+++ b/sentry/templates/sentry/groups/details.html
@@ -102,6 +102,20 @@
             <pre id="full-message">{{ group.message }}</pre>
         {% endif %}
 
+        {% if group|has_charts %}
+            <div class="module">
+                <div class="page-header">
+                    <h2>{% trans "Frequency" %}</h2>
+                </div>
+                <div class="module-content">
+                    <div id="chart">
+                        <noscript>{% trans "Get yourself some JavaScripts dood" %}</noscript>
+                        <p>Loading...</p>
+                    </div>
+                </div>
+            </div>
+        {% endif %}
+
         {% if version_data %}
             <div id="versioninfo" class="module">
                 <div class="page-header">
@@ -124,20 +138,6 @@
             {{ interface }}
         {% endfor %}
 
-        {% if group|has_charts %}
-            <div class="module">
-                <div class="page-header">
-                    <h2>{% trans "Frequency" %}</h2>
-                </div>
-                <div class="module-content">
-                    <div id="chart">
-                        <noscript>{% trans "Get yourself some JavaScripts dood" %}</noscript>
-                        <p>Loading...</p>
-                    </div>
-                </div>
-            </div>
-        {% endif %}
-
         {% if json_data %}
         <div id="requestinfo" class="module">
             <h2>{% trans "Additional Data" %}</h2>
@@ -223,7 +223,7 @@
                 for (var i=0; i<data.length; i++) {
                     pairs.push([start + (3600 * 1000) * i, data[i]]);
                 }
-                $('#chart').height(200);
+                $('#chart').height(120);
                 $.plot($("#chart"), [
                     {
                         data: pairs,
@@ -240,9 +240,9 @@
                        mode: "time"
                     },
                     grid: {
-                        borderColor: '#aaaaaa',
+                        borderColor: '#dddddd',
                         borderWidth: 1,
-                        backgroundColor: '#eeeeee'
+                        backgroundColor: '#F5F5F5'
                     },
                     lines: { show: false }
 
diff --git a/sentry/templates/sentry/groups/group_list.html b/sentry/templates/sentry/groups/group_list.html
index 3a7fc439aa..eea8326bc4 100644
--- a/sentry/templates/sentry/groups/group_list.html
+++ b/sentry/templates/sentry/groups/group_list.html
@@ -125,7 +125,7 @@
                     },
                     grid: {
                         show: false,
-                        backgroundColor: '#eeeeee'
+                        backgroundColor: '#F5F5F5'
                     },
                     lines: { show: false }
 
diff --git a/sentry/web/frontend/generic.py b/sentry/web/frontend/generic.py
index 867a652387..eaef4367c6 100644
--- a/sentry/web/frontend/generic.py
+++ b/sentry/web/frontend/generic.py
@@ -12,8 +12,7 @@ def dashboard(request):
     project_list = get_project_list(request.user)
     if len(project_list) == 1:
         return HttpResponseRedirect(reverse('sentry', kwargs={'project_id': project_list.keys()[0]}))
-    return render_to_response('sentry/dashboard.html', {
-    }, request)
+    return render_to_response('sentry/dashboard.html', request=request)
 
 
 def static_media(request, path):
diff --git a/sentry/web/helpers.py b/sentry/web/helpers.py
index ab20112135..f04c224aca 100644
--- a/sentry/web/helpers.py
+++ b/sentry/web/helpers.py
@@ -55,7 +55,9 @@ def iter_data(obj):
         yield k, v
 
 
-def render_to_string(template, context={}, request=None):
+def render_to_string(template, context=None, request=None):
+    if context is None:
+        context = {}
     context.update({
         'has_search': False,
         'MESSAGES_PER_PAGE': settings.MESSAGES_PER_PAGE,
@@ -69,7 +71,7 @@ def render_to_string(template, context={}, request=None):
     return loader.render_to_string(template, context)
 
 
-def render_to_response(template, context={}, request=None, status=200):
+def render_to_response(template, context=None, request=None, status=200):
     response = HttpResponse(render_to_string(template, context, request))
     response.status_code = status
 
