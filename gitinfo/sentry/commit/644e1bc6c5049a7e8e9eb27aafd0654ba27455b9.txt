commit 644e1bc6c5049a7e8e9eb27aafd0654ba27455b9
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Feb 13 12:22:15 2013 -0800

    Start of work on alerts list

diff --git a/src/sentry/templates/sentry/alerts/list.html b/src/sentry/templates/sentry/alerts/list.html
new file mode 100644
index 0000000000..3d4e8da8e6
--- /dev/null
+++ b/src/sentry/templates/sentry/alerts/list.html
@@ -0,0 +1,32 @@
+{% extends "sentry/layout.html" %}
+
+{% load i18n %}
+{% load sentry_helpers %}
+{% load sentry_plugins %}
+
+{% block title %}{% trans "Alerts" %} | {{ project.name }} | {{ block.super }}{% endblock %}
+
+{% block bodyclass %}{% endblock %}
+
+{% block breadcrumb %}
+    {{ block.super }}
+    <li class="divider"></li>
+    <li><a href="{% url sentry-project-alerts project.team.slug project.slug %}">{% trans "Alerts" %}</a></li>
+{% endblock %}
+
+{% block main %}
+    <section class="body">
+        <div class="page-header">
+            <h2>{% trans "Recent Alerts" %}</h2>
+        </div>
+
+        <ul>
+            {% for alert in alert_list %}
+                <li>
+                    <p>{{ alert.message }}</p>
+                    <small>{{ alert.datetime|timesince }} ({{ alert.datetime }})</small>
+                </li>
+            {% endfor %}
+        </ul>
+    </section>
+{% endblock %}
diff --git a/src/sentry/web/frontend/groups.py b/src/sentry/web/frontend/groups.py
index 4d64f7a1fa..22dd20ec17 100644
--- a/src/sentry/web/frontend/groups.py
+++ b/src/sentry/web/frontend/groups.py
@@ -27,7 +27,8 @@ from sentry.constants import (SORT_OPTIONS, SEARCH_SORT_OPTIONS,
     SCORE_CLAUSES, MYSQL_SCORE_CLAUSES, SQLITE_SCORE_CLAUSES,
     ORACLE_SORT_CLAUSES, ORACLE_SCORE_CLAUSES)
 from sentry.filters import get_filters
-from sentry.models import Project, Group, Event, SearchDocument, Activity
+from sentry.models import (Project, Group, Event, SearchDocument, Activity,
+    Alert)
 from sentry.permissions import can_admin_group, can_create_projects
 from sentry.plugins import plugins
 from sentry.utils import json
@@ -307,6 +308,19 @@ def search(request, team, project):
     }, request)
 
 
+@login_required
+@has_access
+def alert_list(request, team, project):
+    alert_list = Alert.objects.filter(project=project, group__isnull=True).order_by('-datetime')
+
+    return render_to_response('sentry/alerts/list.html', {
+        'team': team,
+        'project': project,
+        'alert_list': alert_list,
+        'SECTION': 'events',
+    }, request)
+
+
 @login_required
 @has_access
 def group_list(request, team, project):
diff --git a/src/sentry/web/urls.py b/src/sentry/web/urls.py
index 0eb050e37c..c1e6900e40 100644
--- a/src/sentry/web/urls.py
+++ b/src/sentry/web/urls.py
@@ -204,12 +204,11 @@ urlpatterns = patterns('',
     url(r'^(?P<team_slug>[\w_-]+)/(?P<project_id>[\w_-]+)/group/(?P<group_id>\d+)/actions/(?P<slug>[\w_-]+)/', groups.group_plugin_action, name='sentry-group-plugin-action'),
     url(r'^(?P<team_slug>[\w_-]+)/(?P<project_id>[\w_-]+)/group/(?P<group_id>\d+)/tags/$', groups.group_tag_list, name='sentry-group-tags'),
     url(r'^(?P<team_slug>[\w_-]+)/(?P<project_id>[\w_-]+)/group/(?P<group_id>\d+)/tags/(?P<tag_name>[^/]+)/$', groups.group_tag_details, name='sentry-group-tag-details'),
-
     url(r'^(?P<team_slug>[\w_-]+)/(?P<project_id>[\w_-]+)/events/$', events.event_list, name='sentry-events'),
     url(r'^(?P<team_slug>[\w_-]+)/(?P<project_id>[\w_-]+)/events/(?P<event_id>\d+)/replay/$', events.replay_event, name='sentry-replay'),
-
     url(r'^(?P<team_slug>[\w_-]+)/(?P<project_id>[\w_-]+)/search/$', groups.search, name='sentry-search'),
-
+    url(r'^(?P<team_slug>[\w_-]+)/(?P<project_id>[\w_-]+)/alerts/$', groups.alert_list,
+        name='sentry-project-alerts'),
     url(r'^(?P<team_slug>[\w_-]+)/(?P<project_id>[\w_-]+)/stream/$', groups.group_list),
     url(r'^(?P<team_slug>[\w_-]+)/(?P<project_id>[\w_-]+)/$', groups.group_list, name='sentry-stream'),
 
