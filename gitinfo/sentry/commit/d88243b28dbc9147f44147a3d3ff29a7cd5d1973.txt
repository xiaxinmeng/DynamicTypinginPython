commit d88243b28dbc9147f44147a3d3ff29a7cd5d1973
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Dec 26 20:09:51 2012 -0800

    Improve how values are rendered in additional data

diff --git a/src/sentry/templates/sentry/partial/_event_details.html b/src/sentry/templates/sentry/partial/_event_details.html
index f52de2200a..3bbdd51c6c 100644
--- a/src/sentry/templates/sentry/partial/_event_details.html
+++ b/src/sentry/templates/sentry/partial/_event_details.html
@@ -14,30 +14,26 @@
             {% for k, v in json_data.iteritems|as_sorted %}
                 <tr>
                     <th class="key">{{ k }}</th>
-                    <td class="values{% if v and not v|is_dict %} code{% endif %}">
-                    {% if v %}
-                        {% if v|is_dict %}
-                            {% if v|length > 5 %}
-                                <a href="#" onclick="return varToggle(this, '#vExtra{{ forloop.counter }}')"><span>&#x25b6;</span> {% trans "Expand" %}</a>
-                            {% endif %}
-                            <table class="table vars" id="vExtra{{ forloop.counter }}"{% if v|length > 5 %} style="display:none;"{% endif %}>
-                                <colgroup>
-                                    <col style="width:100px;">
-                                </colgroup>
-                                <tbody>
-                                    {% for k_, v_ in v.iteritems|as_sorted %}
-                                        <tr>
-                                            <th>{{ k_ }}</th>
-                                            <td class="code"><pre>{{ v_|pprint }}</pre></td>
-                                        </tr>
-                                    {% endfor %}
-                                </tbody>
-                            </table>
-                        {% else %}
-                            <pre>{{ v|pprint }}</pre>
+                    <td class="values{% if not v|is_dict %} code{% endif %}">
+                    {% if v|is_dict %}
+                        {% if v|length > 5 %}
+                            <a href="#" onclick="return varToggle(this, '#vExtra{{ forloop.counter }}')"><span>&#x25b6;</span> {% trans "Expand" %}</a>
                         {% endif %}
+                        <table class="table vars" id="vExtra{{ forloop.counter }}"{% if v|length > 5 %} style="display:none;"{% endif %}>
+                            <colgroup>
+                                <col style="width:100px;">
+                            </colgroup>
+                            <tbody>
+                                {% for k_, v_ in v.iteritems|as_sorted %}
+                                    <tr>
+                                        <th>{{ k_ }}</th>
+                                        <td class="code"><pre>{{ v_|pprint }}</pre></td>
+                                    </tr>
+                                {% endfor %}
+                            </tbody>
+                        </table>
                     {% else %}
-                        <p>{% blocktrans with k as varname %}No data for {{ varname }}{% endblocktrans %}</p>
+                        <pre>{{ v|pprint }}</pre>
                     {% endif %}
                     </td>
                 </tr>
