commit 9c5b3d6dba020c7987e95c8727b0a44bec778898
Author: Armin Ronacher <armin.ronacher@active-4.com>
Date:   Tue Mar 8 13:06:45 2016 +0100

    Simplify by_qualified_short_id to not look up project separately

diff --git a/src/sentry/models/group.py b/src/sentry/models/group.py
index af618be526..b1ab03cc47 100644
--- a/src/sentry/models/group.py
+++ b/src/sentry/models/group.py
@@ -57,19 +57,12 @@ class GroupManager(BaseManager):
     def by_qualified_short_id(self, org, short_id):
         match = _short_id_re.match(short_id.strip())
         if match is None:
-            raise Group.DoesNotExist
-        from sentry.models import Project
+            raise Group.DoesNotExist()
         callsign, id = match.groups()
         callsign = callsign.upper()
-        try:
-            project = Project.objects.get(
-                organization=org,
-                callsign=callsign
-            )
-        except Project.DoesNotExist:
-            raise Group.DoesNotExist
         return Group.objects.get(
-            project=project,
+            project__organization=org,
+            project__callsign=callsign.upper(),
             short_id=base36_decode(id),
         )
 
