commit 00c79cce224ad51920ee2b8cc35e84b0d0b8c6cf
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Apr 13 17:31:59 2015 -0700

    Merge users by default

diff --git a/src/sentry/auth/helper.py b/src/sentry/auth/helper.py
index c02b669a3f..dddba054a2 100644
--- a/src/sentry/auth/helper.py
+++ b/src/sentry/auth/helper.py
@@ -291,11 +291,14 @@ class AuthHelper(object):
                 ident=identity['id'],
             )
         except AuthIdentity.DoesNotExist:
-            user = User.objects.create(
-                email=identity['email'],
-                first_name=identity.get('name', ''),
-                is_managed=True,
-            )
+            if request.user.is_authenticated():
+                user = request.user
+            else:
+                user = User.objects.create(
+                    email=identity['email'],
+                    first_name=identity.get('name', ''),
+                    is_managed=True,
+                )
 
             auth_identity = AuthIdentity.objects.create(
                 auth_provider=auth_provider,
