commit 5f254776035fe394e3098bba511a3e0bf0454a3a
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Mar 30 13:33:11 2015 -0700

    Add SENTRY_DEFAULT_TIME_ZONE setting (fixes GH-1472)

diff --git a/src/sentry/conf/server.py b/src/sentry/conf/server.py
index 1e3b8cffd2..908b04c517 100644
--- a/src/sentry/conf/server.py
+++ b/src/sentry/conf/server.py
@@ -542,6 +542,10 @@ SENTRY_FEATURES = {
     'teams:create': True,
 }
 
+# Default time zone for localization in the UI.
+# http://en.wikipedia.org/wiki/List_of_tz_zones_by_name
+SENTRY_DEFAULT_TIME_ZONE = 'UTC'
+
 SENTRY_FILTERS = (
     'sentry.filters.StatusFilter',
 )
diff --git a/src/sentry/templatetags/sentry_helpers.py b/src/sentry/templatetags/sentry_helpers.py
index 45c8453e80..8bc1670155 100644
--- a/src/sentry/templatetags/sentry_helpers.py
+++ b/src/sentry/templatetags/sentry_helpers.py
@@ -464,7 +464,7 @@ def localized_datetime(context, dt, format='DATETIME_FORMAT'):
     request = context['request']
     timezone = getattr(request, 'timezone', None)
     if not timezone:
-        timezone = pytz.timezone(settings.TIME_ZONE)
+        timezone = pytz.timezone(settings.SENTRY_DEFAULT_TIME_ZONE)
 
     dt = dt.astimezone(timezone)
 
diff --git a/src/sentry/web/frontend/accounts.py b/src/sentry/web/frontend/accounts.py
index 95412c3ba1..d2bc54a64b 100644
--- a/src/sentry/web/frontend/accounts.py
+++ b/src/sentry/web/frontend/accounts.py
@@ -190,7 +190,7 @@ def appearance_settings(request):
     form = AppearanceSettingsForm(request.user, request.POST or None, initial={
         'language': options.get('language') or request.LANGUAGE_CODE,
         'stacktrace_order': int(options.get('stacktrace_order', -1) or -1),
-        'timezone': options.get('timezone') or settings.TIME_ZONE,
+        'timezone': options.get('timezone') or settings.SENTRY_DEFAULT_TIME_ZONE,
     })
     if form.is_valid():
         form.save()
