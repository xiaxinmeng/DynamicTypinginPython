commit 4b4ad8671023440ab2fd4e5b2933135ed4787f72
Author: Tobias von Klipstein <tobias@uxebu.com>
Date:   Tue Sep 14 11:44:13 2010 +0200

    Catching general exception if we convert to unicode.
    In some cases a different exception than UnicodeDecodeError occurs.

diff --git a/sentry/client/base.py b/sentry/client/base.py
index 95a3cf04b5..b5dce8dab0 100644
--- a/sentry/client/base.py
+++ b/sentry/client/base.py
@@ -118,6 +118,8 @@ class SentryClient(object):
                     f = smart_unicode(f)
                 except (UnicodeEncodeError, UnicodeDecodeError):
                     f = '(Error decoding value)'
+                except Exception: # in some cases we get a different exception
+                    f = smart_unicode(type(f))
             return f
 
         def shorten(var):
