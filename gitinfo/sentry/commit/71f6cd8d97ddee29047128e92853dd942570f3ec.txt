commit 71f6cd8d97ddee29047128e92853dd942570f3ec
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Mon Oct 28 12:19:24 2019 -0700

    build: maxminddb isn't needed to be explicitly installed (#15266)
    
    This is coming in from requirements-base.txt now.
    
    See: f9267a11769be041ba5464548e1a8c16fc4f584f

diff --git a/.travis.yml b/.travis.yml
index d3b459278a..758cbe14b7 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -19,7 +19,7 @@ addons:
     update: true
     packages:
       - libxmlsec1-dev
-      - libgeoip-dev
+      - libmaxminddb-dev
   chrome: stable
 
 env:
diff --git a/docker/Dockerfile b/docker/Dockerfile
index 728a3ce9e9..19d878378c 100644
--- a/docker/Dockerfile
+++ b/docker/Dockerfile
@@ -138,7 +138,7 @@ RUN set -x \
     # Separate these due to https://git.io/fjyz6
     # Otherwise librabbitmq will install the latest amqp version,
     # violating kombu's amqp<2.0 constraint.
-    && pip install librabbitmq==1.6.1 maxminddb==1.4.1 \
+    && pip install librabbitmq==1.6.1 \
     && mkdir /tmp/uwsgi-dogstatsd \
     && wget -O - https://github.com/eventbrite/uwsgi-dogstatsd/archive/filters-and-tags.tar.gz | \
        tar -xzf - -C /tmp/uwsgi-dogstatsd --strip-components=1 \
