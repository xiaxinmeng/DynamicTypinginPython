commit f69385c34c4350a7df219fbb077243601383861e
Author: Evan Purkhiser <evanpurkhiser@gmail.com>
Date:   Wed Sep 19 14:34:34 2018 -0700

    fix(integrations): Cleanup repo dropdown (#9819)

diff --git a/src/sentry/static/sentry/app/views/organizationIntegrations/integrationRepos.jsx b/src/sentry/static/sentry/app/views/organizationIntegrations/integrationRepos.jsx
index 38a8fe1db9..7d54212791 100644
--- a/src/sentry/static/sentry/app/views/organizationIntegrations/integrationRepos.jsx
+++ b/src/sentry/static/sentry/app/views/organizationIntegrations/integrationRepos.jsx
@@ -212,6 +212,8 @@ export default class IntegrationRepos extends AsyncComponent {
         onChange={onChange}
         menuHeader={menuHeader}
         emptyMessage={t('No repositories available')}
+        noResultsMessage={t('No repositories found')}
+        busy={this.state.dropdownBusy}
       >
         {({isOpen}) => (
           <DropdownButton isOpen={isOpen} size="xsmall" busy={this.state.adding}>
@@ -305,7 +307,6 @@ const StyledReposLabel = styled('div')`
 `;
 
 const StyledListElement = styled('div')`
-  font-size: 0.875em;
   display: flex;
   align-items: center;
   padding: ${space(0.5)};
