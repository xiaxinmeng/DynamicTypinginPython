commit 1328d58d0ac53c322d481cef0016f070a51b2604
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Sep 1 16:43:06 2010 -0700

    Make sure the query only returns distinct results

diff --git a/sentry/views.py b/sentry/views.py
index 2ce30f2c97..8b8b6af8a9 100644
--- a/sentry/views.py
+++ b/sentry/views.py
@@ -36,7 +36,7 @@ def index(request):
         select={
             'score': 'times_seen / (pow((floor(extract(epoch from now() - last_seen) / 3600) + 2), 1.25) + 1)',
         }
-    ).order_by('-score', '-last_seen')
+    ).order_by('-score', '-last_seen').distinct()
     
     any_filter = False
     for filter_ in filters:
