commit 594b4e7c63b38d5ee64371d4ab1a336022e3d1ca
Author: ted kaemming <t.kaemming+github@gmail.com>
Date:   Thu Sep 15 16:21:43 2016 -0700

    Make it clear we're taking a four week average for report aggregates. (#4149)

diff --git a/src/sentry/tasks/reports.py b/src/sentry/tasks/reports.py
index ccc8805e0f..ed448bee3c 100644
--- a/src/sentry/tasks/reports.py
+++ b/src/sentry/tasks/reports.py
@@ -702,7 +702,7 @@ def to_context(report, fetch_groups=None):
         },
         'comparisons': [
             ('last week', change(aggregates[-1], aggregates[-2])),
-            ('last month', change(
+            ('four week average', change(
                 aggregates[-1],
                 mean(aggregates) if all(v is not None for v in aggregates) else None,
             )),
diff --git a/src/sentry/templates/sentry/emails/reports/body.html b/src/sentry/templates/sentry/emails/reports/body.html
index 1002eccc82..298e80948d 100644
--- a/src/sentry/templates/sentry/emails/reports/body.html
+++ b/src/sentry/templates/sentry/emails/reports/body.html
@@ -85,7 +85,7 @@
                 {% if percentage >= 1000 %}{{ percentage|floatformat:"0" }}{% else %}{{ percentage|floatformat:"-1" }}{% endif %}%
             {% endwith %}
             </div>
-            <small>{% if change >= 0 %}more{% else %}fewer{% endif %} events than {{ label }}</small>
+            <small>{% if change >= 0 %}more{% else %}less{% endif %} than {{ label }}</small>
           {% else %}
             <small class="empty">There is not enough data to compare to {{ label }}.</small>
           {% endif %}
