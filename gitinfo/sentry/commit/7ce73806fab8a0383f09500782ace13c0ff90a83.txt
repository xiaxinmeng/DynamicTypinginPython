commit 7ce73806fab8a0383f09500782ace13c0ff90a83
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Wed May 7 01:57:47 2014 -0700

    Don't assume all VLQ objects reference a src.
    
    Fixes https://app.getsentry.com/sentry/sentry/group/18815426/

diff --git a/src/sentry/utils/sourcemaps.py b/src/sentry/utils/sourcemaps.py
index b852b91e74..efc164d2a0 100644
--- a/src/sentry/utils/sourcemaps.py
+++ b/src/sentry/utils/sourcemaps.py
@@ -124,7 +124,11 @@ def sourcemap_to_index(sourcemap):
     for state in parse_sourcemap(smap):
         state_list.append(state)
         key_list.append((state.dst_line, state.dst_col))
-        src_list.add(state.src)
+
+        # Apparently it's possible to not have a src
+        # specified in the vlq segments
+        if state.src is not None:
+            src_list.add(state.src)
 
     return SourceMapIndex(state_list, key_list, src_list, content)
 
