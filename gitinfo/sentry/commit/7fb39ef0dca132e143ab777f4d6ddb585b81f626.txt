commit 7fb39ef0dca132e143ab777f4d6ddb585b81f626
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Mon Mar 28 16:59:31 2016 -0700

    Don't set chunked Transfer-Encoding header on streaming responses
    
    For some reason, this breaks some streaming responses from Django, so
    it's better to not even set it.

diff --git a/src/sentry/middleware/proxy.py b/src/sentry/middleware/proxy.py
index 25786ad422..973bb34bb4 100644
--- a/src/sentry/middleware/proxy.py
+++ b/src/sentry/middleware/proxy.py
@@ -23,8 +23,7 @@ class ContentLengthHeaderMiddleware(object):
         if 'Transfer-Encoding' in response or 'Content-Length' in response:
             return response
 
-        if response.streaming:
-            response['Transfer-Encoding'] = 'chunked'
-        else:
+        if not response.streaming:
             response['Content-Length'] = str(len(response.content))
+
         return response
