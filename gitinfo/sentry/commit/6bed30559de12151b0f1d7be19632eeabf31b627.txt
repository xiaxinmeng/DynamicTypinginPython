commit 6bed30559de12151b0f1d7be19632eeabf31b627
Author: Ben Vinegar <ben@benv.ca>
Date:   Thu Dec 10 17:31:49 2015 -0800

    Show message when no users matching assignee selector text (fixes #2428)

diff --git a/src/sentry/static/sentry/app/components/assigneeSelector.jsx b/src/sentry/static/sentry/app/components/assigneeSelector.jsx
index 06d00263ef..c94a26a7e6 100644
--- a/src/sentry/static/sentry/app/components/assigneeSelector.jsx
+++ b/src/sentry/static/sentry/app/components/assigneeSelector.jsx
@@ -12,6 +12,7 @@ import LoadingIndicator from '../components/loadingIndicator';
 import {userDisplayName} from '../utils/formatters';
 import {valueIsEqual} from '../utils';
 import TooltipMixin from '../mixins/tooltip';
+import {t} from '../locale';
 
 const AssigneeSelector = React.createClass({
   propTypes: {
@@ -169,6 +170,12 @@ const AssigneeSelector = React.createClass({
       );
     });
 
+    if (memberNodes.length === 0) {
+      memberNodes = [
+        <li className="not-found" key="no-user"><span>{t('No matching users found.')}</span></li>
+      ];
+    }
+
     let tooltipTitle = null;
     if (assignedTo) {
       tooltipTitle = userDisplayName(assignedTo);
diff --git a/src/sentry/static/sentry/less/shared-components.less b/src/sentry/static/sentry/less/shared-components.less
index 228555a449..9a893c51f7 100644
--- a/src/sentry/static/sentry/less/shared-components.less
+++ b/src/sentry/static/sentry/less/shared-components.less
@@ -1519,6 +1519,13 @@ table.integrations {
     }
 
     li {
+      &.not-found span {
+        padding: 5px 8px;
+        padding-left: 10px;
+        display: block;
+        font-style: italic;
+      }
+
       strong {
         font-weight: normal;
         background: lighten(@yellow, 15);
