commit 05a9b12d6464b0c6b29b3d1a806413e71d180f95
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Fri Apr 21 09:09:53 2017 -0700

    petname 2.0
    
    The wordlist has been refined drastically, removing lots of garbage
    things.
    
    See: https://twitter.com/stiggler/status/855438926202494977

diff --git a/setup.py b/setup.py
index d9d2bbe484..178da470d1 100755
--- a/setup.py
+++ b/setup.py
@@ -117,7 +117,7 @@ install_requires = [
     'mmh3>=2.3.1,<2.4',
     'oauth2>=1.5.167',
     'percy>=0.2.5',
-    'petname>=1.7,<1.8',
+    'petname>=2.0,<2.1',
     'Pillow>=3.2.0,<3.3.0',
     'progressbar2>=3.10,<3.11',
     'psycopg2>=2.6.0,<2.7.0',
diff --git a/src/sentry/models/apiapplication.py b/src/sentry/models/apiapplication.py
index 4933564f68..4056a1625a 100644
--- a/src/sentry/models/apiapplication.py
+++ b/src/sentry/models/apiapplication.py
@@ -32,7 +32,7 @@ class ApiApplication(Model):
     owner = FlexibleForeignKey('sentry.User')
     name = models.CharField(
         max_length=64, blank=True,
-        default=lambda: petname.Generate(2, ' ').title())
+        default=lambda: petname.Generate(2, ' ', letters=1).title())
     status = BoundedPositiveIntegerField(default=0, choices=(
         (ApiApplicationStatus.active, _('Active')),
         (ApiApplicationStatus.inactive, _('Inactive')),
diff --git a/src/sentry/models/projectkey.py b/src/sentry/models/projectkey.py
index 85f31a3be8..5d0bf01eb2 100644
--- a/src/sentry/models/projectkey.py
+++ b/src/sentry/models/projectkey.py
@@ -128,7 +128,7 @@ class ProjectKey(Model):
         if not self.secret_key:
             self.secret_key = ProjectKey.generate_api_key()
         if not self.label:
-            self.label = petname.Generate(2, ' ').title()
+            self.label = petname.Generate(2, ' ', letters=1).title()
         super(ProjectKey, self).save(*args, **kwargs)
 
     def get_dsn(self, domain=None, secure=True, public=False):
diff --git a/src/sentry/testutils/fixtures.py b/src/sentry/testutils/fixtures.py
index 5fc54baaa4..5414b808ad 100644
--- a/src/sentry/testutils/fixtures.py
+++ b/src/sentry/testutils/fixtures.py
@@ -196,7 +196,7 @@ class Fixtures(object):
 
     def create_organization(self, **kwargs):
         if not kwargs.get('name'):
-            kwargs['name'] = petname.Generate(2, ' ').title()
+            kwargs['name'] = petname.Generate(2, ' ', letters=1).title()
 
         owner = kwargs.pop('owner', -1)
         if owner is -1:
@@ -226,7 +226,7 @@ class Fixtures(object):
 
     def create_team(self, **kwargs):
         if not kwargs.get('name'):
-            kwargs['name'] = petname.Generate(2, ' ').title()
+            kwargs['name'] = petname.Generate(2, ' ', letters=1).title()
         if not kwargs.get('slug'):
             kwargs['slug'] = slugify(six.text_type(kwargs['name']))
         if not kwargs.get('organization'):
@@ -236,7 +236,7 @@ class Fixtures(object):
 
     def create_project(self, **kwargs):
         if not kwargs.get('name'):
-            kwargs['name'] = petname.Generate(2, ' ').title()
+            kwargs['name'] = petname.Generate(2, ' ', letters=1).title()
         if not kwargs.get('slug'):
             kwargs['slug'] = slugify(six.text_type(kwargs['name']))
         if not kwargs.get('team'):
diff --git a/src/sentry/web/frontend/accounts_twofactor.py b/src/sentry/web/frontend/accounts_twofactor.py
index 8e5fe380d3..3f9d075de2 100644
--- a/src/sentry/web/frontend/accounts_twofactor.py
+++ b/src/sentry/web/frontend/accounts_twofactor.py
@@ -30,7 +30,7 @@ class SmsForm(forms.Form):
 class U2fForm(forms.Form):
     device_name = forms.CharField(
         label=_('Device name'), max_length=60, required=False,
-        initial=lambda: petname.Generate(2, ' ').title(),
+        initial=lambda: petname.Generate(2, ' ', letters=1).title(),
     )
 
 
