commit 6c18eef910ba8475900310867da67d7296cdd4bc
Author: Daniel Griesser <daniel.griesser.86@gmail.com>
Date:   Mon Nov 28 16:43:56 2016 +0100

    Fix iOS device name for related events list

diff --git a/src/sentry/static/sentry/app/views/groupEvents.jsx b/src/sentry/static/sentry/app/views/groupEvents.jsx
index 60897f844e..9bd525beaa 100644
--- a/src/sentry/static/sentry/app/views/groupEvents.jsx
+++ b/src/sentry/static/sentry/app/views/groupEvents.jsx
@@ -10,6 +10,7 @@ import LoadingIndicator from '../components/loadingIndicator';
 import Pagination from '../components/pagination';
 import SearchBar from '../components/searchBar.jsx';
 import {t} from '../locale';
+import {deviceNameMapper} from '../utils';
 
 const GroupEvents = React.createClass({
   mixins: [
@@ -160,7 +161,7 @@ const GroupEvents = React.createClass({
           {tagList.map((tag) => {
             return (
               <td key={tag.key}>
-                {tagMap[tag.key]}
+                {deviceNameMapper(tagMap[tag.key])}
               </td>
             );
           })}
