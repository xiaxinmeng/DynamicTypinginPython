commit 57a6ace7e1b0f76aba6ff01bb4a60cd96d4361b2
Author: Burak Yigit Kaya <byk@sentry.io>
Date:   Sat Feb 8 08:53:35 2020 +0300

    fix(docker): Fix broken entrypoint after #16852 (#16896)

diff --git a/docker/docker-entrypoint.sh b/docker/docker-entrypoint.sh
index 222bf9b5f0..a15bd0ca22 100755
--- a/docker/docker-entrypoint.sh
+++ b/docker/docker-entrypoint.sh
@@ -17,7 +17,7 @@ if [ "$1" = 'sentry' ]; then
 	if [ "$(id -u)" = '0' ]; then
 		mkdir -p /data/files
 		sentry_uid=$(id -u sentry)
-		if [ "$(stat -c %u /data)" != "$sentry_uid" || "$(stat -c %u /data/files)" != "$sentry_uid" ]; then
+		if [ "$(stat -c %u /data)" != "$sentry_uid" ] || [ "$(stat -c %u /data/files)" != "$sentry_uid" ]; then
 			find /data ! -user sentry -exec chown sentry {} \;
 		fi
 		set -- gosu sentry "$@"
