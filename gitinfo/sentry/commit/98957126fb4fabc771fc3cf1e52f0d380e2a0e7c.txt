commit 98957126fb4fabc771fc3cf1e52f0d380e2a0e7c
Author: Billy Vong <billyvg@users.noreply.github.com>
Date:   Wed Feb 19 15:32:18 2020 -0800

    fix(workflow): Change Metric Alert Rule trigger time window rate string (#17116)
    
    Trigger time window is in minutes, not seconds

diff --git a/src/sentry/static/sentry/app/views/alerts/details/body.tsx b/src/sentry/static/sentry/app/views/alerts/details/body.tsx
index f28481921b..ad81a82b59 100644
--- a/src/sentry/static/sentry/app/views/alerts/details/body.tsx
+++ b/src/sentry/static/sentry/app/views/alerts/details/body.tsx
@@ -145,7 +145,9 @@ export default class DetailsBody extends React.Component<Props> {
         )}
 
         <span>{t('Time Window')}</span>
-        <span>{incident && <Duration seconds={incident.alertRule.timeWindow} />}</span>
+        <span>
+          {incident && <Duration seconds={incident.alertRule.timeWindow * 60} />}
+        </span>
       </RuleDetails>
     );
   }
diff --git a/src/sentry/static/sentry/app/views/settings/projectAlerts/ruleRowNew.tsx b/src/sentry/static/sentry/app/views/settings/projectAlerts/ruleRowNew.tsx
index c7912b46af..f2dafa6461 100644
--- a/src/sentry/static/sentry/app/views/settings/projectAlerts/ruleRowNew.tsx
+++ b/src/sentry/static/sentry/app/views/settings/projectAlerts/ruleRowNew.tsx
@@ -114,7 +114,7 @@ class RuleRow extends React.Component<Props, State> {
                     <div>
                       {data.aggregations[0] === 0 ? t('Events') : t('Users')}{' '}
                       {trigger.thresholdType === 0 ? t('above') : t('below')}{' '}
-                      {trigger.alertThreshold}/{data.timeWindow}s
+                      {trigger.alertThreshold}/{data.timeWindow}min
                     </div>
                   </Trigger>
                   <div>
