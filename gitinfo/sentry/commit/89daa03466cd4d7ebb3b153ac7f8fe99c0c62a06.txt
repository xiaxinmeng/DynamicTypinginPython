commit 89daa03466cd4d7ebb3b153ac7f8fe99c0c62a06
Author: Armin Ronacher <armin.ronacher@active-4.com>
Date:   Mon Jun 20 20:52:14 2016 +0200

    Add a way to specific an entire app-id for u2f

diff --git a/src/sentry/models/authenticator.py b/src/sentry/models/authenticator.py
index 16752ee251..511a03c652 100644
--- a/src/sentry/models/authenticator.py
+++ b/src/sentry/models/authenticator.py
@@ -437,7 +437,8 @@ class U2fInterface(AuthenticatorInterface):
 
     @classproperty
     def u2f_app_id(cls):
-        return absolute_uri(reverse('sentry-u2f-app-id'))
+        rv = options.get('u2f.app-id')
+        return rv or absolute_uri(reverse('sentry-u2f-app-id'))
 
     @classproperty
     def u2f_facets(cls):
diff --git a/src/sentry/options/defaults.py b/src/sentry/options/defaults.py
index e97ca2e9fe..c8963a9e00 100644
--- a/src/sentry/options/defaults.py
+++ b/src/sentry/options/defaults.py
@@ -71,5 +71,6 @@ register('sms.twilio-token', default='', flags=FLAG_ALLOW_EMPTY | FLAG_PRIORITIZ
 register('sms.twilio-number', default='', flags=FLAG_ALLOW_EMPTY | FLAG_PRIORITIZE_DISK)
 
 # U2F
+register('u2f.app-id', default='', flags=FLAG_ALLOW_EMPTY | FLAG_PRIORITIZE_DISK)
 register('u2f.facets', default=(), type=Sequence,
          flags=FLAG_ALLOW_EMPTY | FLAG_PRIORITIZE_DISK)
