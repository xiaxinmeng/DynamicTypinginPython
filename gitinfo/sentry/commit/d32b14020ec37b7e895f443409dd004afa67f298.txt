commit d32b14020ec37b7e895f443409dd004afa67f298
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Tue Mar 1 13:02:23 2016 -0800

    Ignore any exception raised from pkg_resources
    
    Newer versions of setuptools or whatever started raising different
    exceptions, so let's just catch everything.

diff --git a/src/sentry/debug/utils/packages.py b/src/sentry/debug/utils/packages.py
index 74748ac6b2..e3d06d4575 100644
--- a/src/sentry/debug/utils/packages.py
+++ b/src/sentry/debug/utils/packages.py
@@ -17,7 +17,7 @@ def get_package_version(module_name, app):
         # pull version from pkg_resources if distro exists
         try:
             return pkg_resources.get_distribution(module_name).version
-        except pkg_resources.DistributionNotFound:
+        except Exception:
             pass
 
     if hasattr(app, 'get_version'):
