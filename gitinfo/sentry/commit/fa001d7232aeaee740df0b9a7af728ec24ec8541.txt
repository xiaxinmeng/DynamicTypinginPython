commit fa001d7232aeaee740df0b9a7af728ec24ec8541
Author: Stephen Cefali <scefali@sentry.io>
Date:   Mon Oct 7 15:41:45 2019 -0700

    adds logging on handle delete (#14970)

diff --git a/src/sentry/integrations/github/webhook.py b/src/sentry/integrations/github/webhook.py
index 6a78b7b974..1cdc08a439 100644
--- a/src/sentry/integrations/github/webhook.py
+++ b/src/sentry/integrations/github/webhook.py
@@ -127,6 +127,16 @@ class InstallationEventWebhook(Webhook):
     def _handle_delete(self, event, integration):
 
         organizations = integration.organizations.all()
+
+        logger.info(
+            "InstallationEventWebhook._handle_delete",
+            extra={
+                "external_id": event["installation"]["id"],
+                "integration_id": integration.id,
+                "organization_id_list": organizations.values_list("id", flat=True),
+            },
+        )
+
         integration.update(status=ObjectStatus.DISABLED)
 
         Repository.objects.filter(
