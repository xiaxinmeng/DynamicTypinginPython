commit 657e060622f56247af478d7792a5520e1e2f89c4
Author: ckj <chriskjennings@gmail.com>
Date:   Thu Aug 27 14:32:11 2015 -0400

    Disable welcome modal completely. Refs GH-1787.

diff --git a/src/sentry/templates/sentry/layout.html b/src/sentry/templates/sentry/layout.html
index c567715494..95fd7040ca 100644
--- a/src/sentry/templates/sentry/layout.html
+++ b/src/sentry/templates/sentry/layout.html
@@ -404,12 +404,12 @@
       showSlide(1);
     }
 
-    $( "body" ).keypress(function( event ) {
-      if ( event.which == 96 ) {
-         event.preventDefault();
-         $(".welcome-modal").fadeIn();
-      }
-    });
+    // $( "body" ).keypress(function( event ) {
+    //   if ( event.which == 96 ) {
+    //      event.preventDefault();
+    //      $(".welcome-modal").fadeIn();
+    //   }
+    // });
 
     slideShow();
   });
