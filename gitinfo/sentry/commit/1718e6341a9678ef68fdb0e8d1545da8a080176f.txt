commit 1718e6341a9678ef68fdb0e8d1545da8a080176f
Author: David Cramer <dcramer@gmail.com>
Date:   Fri Aug 10 23:28:13 2012 -0700

    Correct permissions for showing new project option

diff --git a/sentry/templates/sentry/teams/manage.html b/sentry/templates/sentry/teams/manage.html
index 749a16a71a..b6232fb508 100644
--- a/sentry/templates/sentry/teams/manage.html
+++ b/sentry/templates/sentry/teams/manage.html
@@ -39,7 +39,7 @@
                 </fieldset>
             </form>
             <div class="page-header">
-                {% if can_add_member %}
+                {% if can_add_project %}
                     <a href="{% url sentry-new-team-project team.slug %}" class="btn pull-right btn-primary">{% trans "New Project" %}</a>
                 {% endif %}
                 <h2>{% trans "Projects" %}</h2>
diff --git a/sentry/web/frontend/teams.py b/sentry/web/frontend/teams.py
index 52ab91f02e..77bc2c7ce7 100644
--- a/sentry/web/frontend/teams.py
+++ b/sentry/web/frontend/teams.py
@@ -78,6 +78,7 @@ def manage_team(request, team):
 
     context = csrf(request)
     context.update({
+        'can_add_project': can_create_projects(request.user, team),
         'can_add_member': can_add_team_member(request.user, team),
         'can_remove_team': can_remove_team(request.user, team),
         'page': 'details',
