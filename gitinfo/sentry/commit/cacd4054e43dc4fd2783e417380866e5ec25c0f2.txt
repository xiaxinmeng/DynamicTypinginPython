commit cacd4054e43dc4fd2783e417380866e5ec25c0f2
Author: Ted Kaemming <ted@kaemming.com>
Date:   Thu Mar 31 14:08:00 2016 -0700

    Don't unlink preview file, also make the filename clearer.

diff --git a/src/sentry/utils/email.py b/src/sentry/utils/email.py
index 855b6f75c1..2dfd78fc05 100644
--- a/src/sentry/utils/email.py
+++ b/src/sentry/utils/email.py
@@ -406,15 +406,12 @@ class PreviewBackend(BaseEmailBackend):
             content = str(message.message())
             preview = tempfile.NamedTemporaryFile(
                 delete=False,
-                prefix='preview-',
+                prefix='sentry-email-preview-',
                 suffix='.eml',
             )
             preview.write(content)
             preview.flush()
             preview.close()
-            try:
-                subprocess.check_call(('open', preview.name))
-            finally:
-                os.unlink(preview.name)
+            subprocess.check_call(('open', preview.name))
 
         return len(email_messages)
