commit e2ad49a3679e1d7b891afa33905fc02d735fb7bf
Author: Matej Minar <matej.minar@sentry.io>
Date:   Mon Jan 27 09:13:26 2020 +0100

    ref(ts): Convert <Truncate> to typescript (#16577)

diff --git a/src/sentry/static/sentry/app/components/truncate.jsx b/src/sentry/static/sentry/app/components/truncate.tsx
similarity index 81%
rename from src/sentry/static/sentry/app/components/truncate.jsx
rename to src/sentry/static/sentry/app/components/truncate.tsx
index f0297ade3a..c34b6e5a02 100644
--- a/src/sentry/static/sentry/app/components/truncate.jsx
+++ b/src/sentry/static/sentry/app/components/truncate.tsx
@@ -1,7 +1,18 @@
-import PropTypes from 'prop-types';
 import React from 'react';
+import PropTypes from 'prop-types';
+
+type Props = {
+  value: string;
+  maxLength: number;
+  leftTrim: boolean;
+  className?: string;
+};
+
+type State = {
+  isExpanded: boolean;
+};
 
-class Truncate extends React.Component {
+class Truncate extends React.Component<Props, State> {
   static propTypes = {
     value: PropTypes.string.isRequired,
     leftTrim: PropTypes.bool,
@@ -9,18 +20,15 @@ class Truncate extends React.Component {
   };
 
   static defaultProps = {
-    leftTrim: false,
     maxLength: 50,
+    leftTrim: false,
   };
 
-  constructor(...args) {
-    super(...args);
-    this.state = {
-      isExpanded: false,
-    };
-  }
+  state = {
+    isExpanded: false,
+  };
 
-  onFocus = e => {
+  onFocus = () => {
     const {value, maxLength} = this.props;
     if (value.length <= maxLength) {
       return;
@@ -28,7 +36,7 @@ class Truncate extends React.Component {
     this.setState({isExpanded: true});
   };
 
-  onBlur = e => {
+  onBlur = () => {
     if (this.state.isExpanded) {
       this.setState({isExpanded: false});
     }
@@ -37,7 +45,7 @@ class Truncate extends React.Component {
   render() {
     const {leftTrim, maxLength, value} = this.props;
     const isTruncated = value.length > maxLength;
-    let shortValue = '';
+    let shortValue: React.ReactNode = '';
 
     if (isTruncated) {
       if (leftTrim) {
