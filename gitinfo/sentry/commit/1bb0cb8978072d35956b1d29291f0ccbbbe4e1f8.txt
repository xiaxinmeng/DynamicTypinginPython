commit 1bb0cb8978072d35956b1d29291f0ccbbbe4e1f8
Author: Brett Hoerner <brett@bretthoerner.com>
Date:   Mon Oct 22 14:56:24 2018 -0700

    ref: Remove unused delete_group task (#10216)

diff --git a/src/sentry/tasks/deletion.py b/src/sentry/tasks/deletion.py
index 08192c1966..a35c8c258c 100644
--- a/src/sentry/tasks/deletion.py
+++ b/src/sentry/tasks/deletion.py
@@ -240,33 +240,6 @@ def delete_project(object_id, transaction_id=None, **kwargs):
         )
 
 
-@instrumented_task(
-    name='sentry.tasks.deletion.delete_group',
-    queue='cleanup',
-    default_retry_delay=60 * 5,
-    max_retries=MAX_RETRIES
-)
-@retry(exclude=(DeleteAborted, ))
-def delete_group(object_id, transaction_id=None, **kwargs):
-    from sentry import deletions
-    from sentry.models import Group
-
-    task = deletions.get(
-        model=Group,
-        query={
-            'id': object_id,
-        },
-        transaction_id=transaction_id or uuid4().hex,
-    )
-    has_more = task.chunk()
-    if has_more:
-        delete_group.apply_async(
-            kwargs={'object_id': object_id,
-                    'transaction_id': transaction_id},
-            countdown=15,
-        )
-
-
 @instrumented_task(
     name='sentry.tasks.deletion.delete_groups',
     queue='cleanup',
