commit f04514ab5cb890a639c3a506688d18c13f1f892e
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Apr 24 01:11:58 2012 -0700

    Cleanup and activation of the embedded R docs

diff --git a/sentry/templates/sentry/partial/_client_config.html b/sentry/templates/sentry/partial/_client_config.html
index a547f0a1a2..a832f3c3ae 100644
--- a/sentry/templates/sentry/partial/_client_config.html
+++ b/sentry/templates/sentry/partial/_client_config.html
@@ -15,4 +15,5 @@
             <li><a href="{% url sentry-docs-client project.slug 'connect' %}" data-toggle="modal">Connect</a></li>
         </ul>
     </li>
+    <li><a href="{% url sentry-docs-client project.slug 'r' %}" data-toggle="modal">R</a></li>
 </ul>
diff --git a/sentry/templates/sentry/partial/client_config/r.html b/sentry/templates/sentry/partial/client_config/r.html
index e07f6b283d..7728193533 100644
--- a/sentry/templates/sentry/partial/client_config/r.html
+++ b/sentry/templates/sentry/partial/client_config/r.html
@@ -2,20 +2,20 @@
 
 <p>{% blocktrans with 'https://r-forge.r-project.org/projects/logging/' as link %}Start by installing <a href="{{ link }}">r-logging</a>:{% endblocktrans %}</p>
 
-<p>{% trans "load the packages in the namespace:" %}</p>
+<p>{% trans "Load the logging package:" %}</p>
 
 <pre>library(logging)
 library(logging.handlers)</pre>
 
-<p>{% trans "associate a sentry handler to a logger:" %}</p>
+<p>{% trans "Create a Sentry handler and add it to a logger:" %}</p>
 
-<pre>rl <- getLogger() # for example the root logger
-rl$addHandler(sentryHandler, dsn=DSN)
+<pre>rl &lt;- getLogger() # for example the root logger
+rl$addHandler(sentryHandler, dsn='{% if dsn %}{{ dsn }}{% else %}<strong>SENTRY_DSN</strong>{% endif %}'')
 </pre>
 
-<p>{% trans "logging records going through the logger will be handled:" %}</p>
+<p>{% trans "Send events via the standard logging interface:" %}</p>
 
-<pre>sol <- getLogger('some.other.logger')
+<pre>sol &lt;- getLogger('some.other.logger')
 sol$error(MESSAGE)
 sol$warn(MESSAGE)
 sol$info(MESSAGE)
diff --git a/sentry/web/frontend/docs.py b/sentry/web/frontend/docs.py
index 2e90420439..7a095e4abc 100644
--- a/sentry/web/frontend/docs.py
+++ b/sentry/web/frontend/docs.py
@@ -21,6 +21,7 @@ PLATFORM_LIST = (
     'node.js',
     'express',
     'connect',
+    'r',
 )
 
 
