commit da726a2dfc8900829777873542cac92b63b3387f
Author: ckj <chriskjennings@gmail.com>
Date:   Thu Apr 23 17:06:29 2015 -0400

    Style pending access list

diff --git a/src/sentry/static/sentry/less/sentry.less b/src/sentry/static/sentry/less/sentry.less
index e61a37632c..6718ed5bad 100644
--- a/src/sentry/static/sentry/less/sentry.less
+++ b/src/sentry/static/sentry/less/sentry.less
@@ -2511,6 +2511,36 @@ table.simple-list {
   }
 }
 
+.access-request-list {
+  border: 1px solid #e1e2e3;
+  border-radius: 3px;
+  margin-bottom: 20px;
+
+  h3 {
+    font-weight: 500;
+    font-size: 14px;
+    padding: 6px 15px;
+    margin: 0;
+  }
+
+  ul {
+    margin: 0;
+    padding: 0;
+  }
+
+  li {
+    font-size: 13px;
+    list-style: none;
+    line-height: 28px;
+    padding: 9px 14px;
+    border-top: 1px solid #e1e2e3;
+  }
+
+  .btn-small {
+    padding: 2px 8px;
+  }
+}
+
 table.member-list {
 
   .status strong {
diff --git a/src/sentry/templates/sentry/organization-members.html b/src/sentry/templates/sentry/organization-members.html
index 92eb62eb95..dffaa4091b 100644
--- a/src/sentry/templates/sentry/organization-members.html
+++ b/src/sentry/templates/sentry/organization-members.html
@@ -18,21 +18,22 @@
   <br>
 
   {% if request_list %}
-    <h3>Pending Accessing Requests</h3>
-
-    <ul class="access-request-list">
-      {% for access_request in request_list %}
-        <li>
-          <div class="pull-right">
-            <a href="#" class="btn btn-primary"
-               data-action="approve" data-id="{{ access_request.id }}">Approve</a>
-            <a href="#" class="btn btn-default"
-               data-action="deny" data-id="{{ access_request.id }}">Deny</a>
-          </div>
-          <strong>{{ access_request.member.user.get_display_name }}</strong> requests access to the <strong>{{ access_request.team.name }}</strong> team.
-        </li>
-      {% endfor %}
-    </ul>
+    <div class="access-request-list">
+      <h3>Pending Accessing Requests</h3>
+      <ul>
+        {% for access_request in request_list %}
+          <li>
+            <div class="pull-right">
+              <a href="#" class="btn btn-primary btn-small"
+                 data-action="approve" data-id="{{ access_request.id }}">Approve</a>
+              <a href="#" class="btn btn-default btn-small"
+                 data-action="deny" data-id="{{ access_request.id }}">Deny</a>
+            </div>
+            <strong>{{ access_request.member.user.get_display_name }}</strong> requests access to the <strong>{{ access_request.team.name }}</strong> team.
+          </li>
+        {% endfor %}
+      </ul>
+    </div>
 
     <script>
     $(function(){
