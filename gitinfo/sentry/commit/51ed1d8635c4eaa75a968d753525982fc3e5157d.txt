commit 51ed1d8635c4eaa75a968d753525982fc3e5157d
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Nov 10 10:26:34 2015 -0800

    Add filestore.blob-size metric

diff --git a/src/sentry/models/file.py b/src/sentry/models/file.py
index 2b4d48a6d1..3660b6ba0d 100644
--- a/src/sentry/models/file.py
+++ b/src/sentry/models/file.py
@@ -19,6 +19,7 @@ from uuid import uuid4
 from sentry.db.models import (
     BoundedPositiveIntegerField, FlexibleForeignKey, Model
 )
+from sentry.utils import metrics
 from sentry.utils.cache import Lock
 
 ONE_DAY = 60 * 60 * 24
@@ -107,6 +108,7 @@ class FileBlob(Model):
             storage = self.get_storage()
             storage.save(self.path, fileobj)
             self.save()
+            metrics.timing('filestore.blob-size', self.size)
 
     def getfile(self):
         """
