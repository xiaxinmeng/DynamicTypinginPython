commit 7629de1102973e4a3930487a3bf126a2f13c6850
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Feb 25 09:23:24 2015 -0800

    Only configure settings if not already so (refs GH-1438)

diff --git a/src/sentry/wsgi.py b/src/sentry/wsgi.py
index 1f55fd671b..59fdb2319c 100644
--- a/src/sentry/wsgi.py
+++ b/src/sentry/wsgi.py
@@ -15,11 +15,13 @@ import sys
 sys.path.insert(0, os.path.join(os.path.dirname(__file__), os.pardir))
 sys.stdout = sys.stderr
 
-# Configure the application (Logan)
-from sentry.utils.runner import configure
-configure()
-
+# Configure the application (Logan) only if it seemingly isnt already
+# configured
 from django.conf import settings
+if not settings.configured:
+    from sentry.utils.runner import configure
+    configure()
+
 if settings.SESSION_FILE_PATH and not os.path.exists(settings.SESSION_FILE_PATH):
     try:
         os.makedirs(settings.SESSION_FILE_PATH)
