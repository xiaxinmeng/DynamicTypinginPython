commit 9b726430f2cc8347ea5c6c8d243289bd81f3c416
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Jun 4 16:56:44 2014 -0700

    Only fire conditions if values are set

diff --git a/src/sentry/rules/conditions/event_frequency.py b/src/sentry/rules/conditions/event_frequency.py
index fd2cf41a06..bcf8c8454c 100644
--- a/src/sentry/rules/conditions/event_frequency.py
+++ b/src/sentry/rules/conditions/event_frequency.py
@@ -52,6 +52,9 @@ class EventFrequencyCondition(EventCondition):
         interval = self.get_option('interval')
         value = int(self.get_option('value'))
 
+        if not (interval and value):
+            return False
+
         current_value = self.get_rate(event, interval)
 
         return current_value > value
diff --git a/src/sentry/rules/conditions/tagged_event.py b/src/sentry/rules/conditions/tagged_event.py
index 75e2e6add2..2b224faf2a 100644
--- a/src/sentry/rules/conditions/tagged_event.py
+++ b/src/sentry/rules/conditions/tagged_event.py
@@ -44,6 +44,9 @@ class TaggedEventCondition(EventCondition):
         match = self.get_option('match')
         value = self.get_option('value')
 
+        if not (key and match and value):
+            return False
+
         tags = (v for k, v in event.get_tags() if k == key)
 
         if match == MatchType.EQUAL:
