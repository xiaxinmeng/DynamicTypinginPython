commit d52c6f9bdd510a3c5f77ee46cf20c1304adbbe00
Author: Chris Faulkner <thefaulkner@gmail.com>
Date:   Wed May 23 01:46:24 2012 +1000

    Fix project-based CORS settings lookup.

diff --git a/sentry/web/api.py b/sentry/web/api.py
index d3aebddc63..1a1611afc6 100644
--- a/sentry/web/api.py
+++ b/sentry/web/api.py
@@ -80,7 +80,7 @@ def store(request, project_id=None):
 
     if 'HTTP_ORIGIN' in request.META:
         origin = request.META.get('HTTP_ORIGIN', '')
-        if not is_valid_origin(origin):
+        if not is_valid_origin(origin, project):
             raise APIError('Invalid origin')
     else:
         origin = None
