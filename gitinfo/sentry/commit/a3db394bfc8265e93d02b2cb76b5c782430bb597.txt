commit a3db394bfc8265e93d02b2cb76b5c782430bb597
Author: Priscila Oliveira <priscilawebdev@gmail.com>
Date:   Tue Mar 31 08:29:40 2020 +0200

    fix(ui): Fixed login/register with non-unique ids (#17976)

diff --git a/src/sentry/templates/sentry/organization-login.html b/src/sentry/templates/sentry/organization-login.html
index 2af0ab945c..7c3a877ae5 100644
--- a/src/sentry/templates/sentry/organization-login.html
+++ b/src/sentry/templates/sentry/organization-login.html
@@ -11,7 +11,7 @@
   SentryApp.passwordStrength.load(function(passwordStrength) {
     $(function() {
       passwordStrength.attachTo({
-        input: document.querySelector('#register #id_password'),
+        input: document.querySelector('#id_registration_password'),
         element: document.querySelector('.password-strength'),
       });
     })
diff --git a/src/sentry/web/frontend/auth_login.py b/src/sentry/web/frontend/auth_login.py
index 40ed0cece5..f7fb436bcd 100644
--- a/src/sentry/web/frontend/auth_login.py
+++ b/src/sentry/web/frontend/auth_login.py
@@ -73,7 +73,12 @@ class AuthLoginView(BaseView):
 
     def get_register_form(self, request, initial=None):
         op = request.POST.get("op")
-        return RegistrationForm(request.POST if op == "register" else None, initial=initial)
+        return RegistrationForm(
+            request.POST if op == "register" else None,
+            initial=initial,
+            # Custom auto_id to avoid ID collision with AuthenticationForm.
+            auto_id="id_registration_%s"
+        )
 
     def can_register(self, request):
         return bool(auth.has_user_registration() or request.session.get("can_register"))
