commit c8b7ce067f7c214b29f958b6c8a93ed9db448cd6
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Aug 1 13:49:46 2011 -0700

    Require importlib and uuid under certain python versions only

diff --git a/setup.py b/setup.py
index 08db8e4570..16af6262bc 100755
--- a/setup.py
+++ b/setup.py
@@ -1,5 +1,7 @@
 #!/usr/bin/env python
 
+import sys
+
 try:
     from setuptools import setup, find_packages, Command
 except ImportError:
@@ -31,10 +33,10 @@ install_requires = [
     'django-templatetag-sugar>=0.1.0',
 ]
 
-try:
-    __import__('uuid')
-except ImportError:
-    # Older versions of Python did not include uuid
+if sys.version_info[:2] < (2, 7):
+    install_requires.append('importlib')
+
+if sys.version_info[:2] < (2, 5):
     install_requires.append('uuid')
 
 setup(
