commit 53e1fbec0d41b8ab2b39ffac4749d58fd5830295
Author: David Cramer <dcramer@gmail.com>
Date:   Sat May 3 13:36:20 2014 -0300

    Improve/fix mocks

diff --git a/bin/load-mocks b/bin/load-mocks
index 44cf6cc250..bdaf9c4f66 100755
--- a/bin/load-mocks
+++ b/bin/load-mocks
@@ -3,9 +3,26 @@
 from sentry.utils.runner import configure
 configure()
 
-from sentry.models import User, Project, Team
+from datetime import datetime, timedelta
+from pytz import utc
+from random import randint
+
+from sentry.app import tsdb
+from sentry.models import Activity, User, Project, Team
 from sentry.utils.samples import create_sample_event
 
+def create_sample_time_series(group):
+    now = datetime.utcnow().replace(tzinfo=utc)
+
+    for _ in xrange(60):
+        tsdb.incr(tsdb.models.group, group.id, now, randint(1, 100))
+        now = now - timedelta(seconds=1)
+
+    for _ in xrange(24 * 30):
+        tsdb.incr(tsdb.models.group, group.id, now, randint(1, 100))
+        now = now - timedelta(hours=1)
+
+
 user = User.objects.filter(is_superuser=True)[0]
 
 team, _ = Team.objects.get_or_create(
@@ -22,15 +39,19 @@ project, _ = Project.objects.get_or_create(
     owner=team.owner,
 )
 
-create_sample_event(project, 'python')
-create_sample_event(project, 'javascript')
+group1 = create_sample_event(project, 'python')
+create_sample_time_series(group1)
+
+group2 = create_sample_event(project, 'javascript')
+create_sample_time_series(group2)
 
 Activity.objects.create(
     type=Activity.DEPLOY,
-    project=project,,
+    project=project,
     ident='4f38b65c62c4565aa94bba391ff8946922a8eed4',
     user=user,
     data={'version': '4f38b65c62c4565aa94bba391ff8946922a8eed4'},
 )
 
-create_sample_event(project, 'java')
+group3 = create_sample_event(project, 'java')
+create_sample_time_series(group3)
