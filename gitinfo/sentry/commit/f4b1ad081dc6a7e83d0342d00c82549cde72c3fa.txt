commit f4b1ad081dc6a7e83d0342d00c82549cde72c3fa
Author: David Cramer <dcramer@gmail.com>
Date:   Fri Feb 10 16:31:48 2017 -0800

    [api] improve user search capabilities

diff --git a/src/sentry/api/endpoints/user_index.py b/src/sentry/api/endpoints/user_index.py
index fd6db310e6..08c20697c7 100644
--- a/src/sentry/api/endpoints/user_index.py
+++ b/src/sentry/api/endpoints/user_index.py
@@ -31,6 +31,10 @@ class UserIndexEndpoint(Endpoint):
                         Q(email__icontains=value) |
                         Q(emails__email__icontains=value)
                     )
+                elif key == 'id':
+                    queryset = queryset.filter(
+                        id__in=value,
+                    )
                 elif key == 'name':
                     queryset = queryset.filter(
                         in_iexact('name', value)
@@ -43,6 +47,8 @@ class UserIndexEndpoint(Endpoint):
                     queryset = queryset.filter(
                         in_iexact('username', value)
                     )
+                else:
+                    queryset = queryset.none()
 
         status = request.GET.get('status')
         if status == 'active':
