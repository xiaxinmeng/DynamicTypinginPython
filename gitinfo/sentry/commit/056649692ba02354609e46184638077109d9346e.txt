commit 056649692ba02354609e46184638077109d9346e
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Feb 4 16:19:44 2013 -0800

    Some minor tweaks to logging in api

diff --git a/src/sentry/coreapi.py b/src/sentry/coreapi.py
index dec2584a3d..d946fc5e0c 100644
--- a/src/sentry/coreapi.py
+++ b/src/sentry/coreapi.py
@@ -329,7 +329,7 @@ def validate_data(project, data, client=None):
             continue
 
         if not data[k]:
-            logger.warning('Ignored empty interface value: %s', k, **client_metadata(client))
+            logger.info('Ignored empty interface value: %s', k, **client_metadata(client))
             del data[k]
             continue
 
@@ -353,10 +353,10 @@ def validate_data(project, data, client=None):
             data[import_path] = inst.serialize()
         except Exception, e:
             if isinstance(e, AssertionError):
-                func = logger.warning
+                log = logger.warning
             else:
-                func = logger.error
-            func('Invalid value for interface: %s', k,
+                log = logger.error
+            log('Invalid value for interface: %s', k,
                 **client_metadata(client, exception=e, extra={'value': value}))
             raise InvalidData('Unable to validate interface, %r: %s' % (k, e))
 
diff --git a/src/sentry/web/api.py b/src/sentry/web/api.py
index 6c230dadaf..f503212c1e 100644
--- a/src/sentry/web/api.py
+++ b/src/sentry/web/api.py
@@ -112,13 +112,13 @@ class APIView(BaseView):
             response['X-Sentry-Error'] = response.content[:200]  # safety net on content length
 
             if response.status_code == 500:
-                func = logger.error
+                log = logger.error
                 exc_info = True
             else:
-                func = logger.info
+                log = logger.info
                 exc_info = None
 
-            func('status=%s project_id=%s %s', response.status_code, project_id,
+            log('status=%s project_id=%s %s', response.status_code, project_id,
                 response['X-Sentry-Error'], extra={
                     'request': request,
                 }, exc_info=exc_info,
