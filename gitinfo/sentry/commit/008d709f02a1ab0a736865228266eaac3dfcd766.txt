commit 008d709f02a1ab0a736865228266eaac3dfcd766
Author: Ben Vinegar <ben@benv.ca>
Date:   Wed Sep 9 17:54:00 2015 -0700

    Sort tags on group details (refs #1899)

diff --git a/src/sentry/static/sentry/app/components/events/eventTags.jsx b/src/sentry/static/sentry/app/components/events/eventTags.jsx
index 285bc05ad6..3470bbfba0 100644
--- a/src/sentry/static/sentry/app/components/events/eventTags.jsx
+++ b/src/sentry/static/sentry/app/components/events/eventTags.jsx
@@ -1,6 +1,9 @@
 import React from "react";
-import PropTypes from "../../proptypes";
 import Router from "react-router";
+import _ from "underscore";
+
+import PropTypes from "../../proptypes";
+
 import EventDataSection from "./eventDataSection";
 import {isUrl} from "../../utils";
 
@@ -15,44 +18,41 @@ var EventTags = React.createClass({
   },
 
   render() {
-    var params = this.context.router.getCurrentParams(),
-      children = [],
-      value;
-
-    var {group, event} = this.props;
-
-    for (var key in this.props.event.tags) {
-      value = this.props.event.tags[key];
-
-      children.push(
-        <li key={key}>
-          {key} = <Router.Link
-            to="stream"
-            params={params}
-            query={{query: key + ':' + '"' + value + '"'}}>
-            {value}
-          </Router.Link>
-          {isUrl(value) &&
-            <a href={value} className="external">
-              <em className="icon-open" />
-            </a>
-          }
-        </li>
-      );
-    }
-
-    if (children.length === 0) {
+    var params = this.context.router.getCurrentParams();
+
+    let tags = this.props.event.tags;
+    if (_.isEmpty(tags))
       return null;
-    }
+
+    let sortedTags = _.chain(tags)
+      .map((val, key) => [key, val])
+      .sortBy(([key,]) => key)
+      .value();
 
     return (
       <EventDataSection
-          group={group}
-          event={event}
+          group={this.props.group}
+          event={this.props.event}
           title="Tags"
           type="tags">
         <ul className="mini-tag-list">
-          {children}
+          {sortedTags.map(([key, value]) => {
+            return (
+              <li key={key}>
+                {key} = <Router.Link
+                  to="stream"
+                  params={params}
+                  query={{query: `${key}:"${value}"`}}>
+                  {value}
+                </Router.Link>
+                {isUrl(value) &&
+                  <a href={value} className="external">
+                    <em className="icon-open" />
+                  </a>
+                }
+              </li>
+            );
+          })}
         </ul>
       </EventDataSection>
     );
