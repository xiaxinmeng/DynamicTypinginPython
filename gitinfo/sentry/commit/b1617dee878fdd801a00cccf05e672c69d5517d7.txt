commit b1617dee878fdd801a00cccf05e672c69d5517d7
Author: David Cramer <dcramer@gmail.com>
Date:   Tue Jun 2 00:04:47 2015 +0100

    Remove invalid task

diff --git a/src/sentry/conf/server.py b/src/sentry/conf/server.py
index 0506cd4eeb..1c107475a8 100644
--- a/src/sentry/conf/server.py
+++ b/src/sentry/conf/server.py
@@ -360,7 +360,6 @@ CELERY_DEFAULT_ROUTING_KEY = "default"
 CELERY_CREATE_MISSING_QUEUES = True
 CELERY_IMPORTS = (
     'sentry.tasks.beacon',
-    'sentry.tasks.check_alerts',
     'sentry.tasks.check_auth',
     'sentry.tasks.cleanup',
     'sentry.tasks.deletion',
@@ -409,14 +408,6 @@ CELERYBEAT_SCHEDULE = {
             'queue': 'auth',
         }
     },
-    'check-alerts': {
-        'task': 'sentry.tasks.check_alerts',
-        'schedule': timedelta(minutes=1),
-        'options': {
-            'expires': 60,
-            'queue': 'alerts',
-        }
-    },
     'send-beacon': {
         'task': 'sentry.tasks.send_beacon',
         'schedule': timedelta(hours=1),
