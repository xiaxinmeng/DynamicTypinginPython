commit 03342d488d0ccb438e7aca580fa08051cbdfc4b9
Author: ckj <chriskjennings@gmail.com>
Date:   Tue Mar 29 11:39:17 2016 -0700

    Stub out non-functioning epm module

diff --git a/src/sentry/static/sentry/app/views/organizationDashboard.jsx b/src/sentry/static/sentry/app/views/organizationDashboard.jsx
index a92a2c9b2c..7b071852d5 100644
--- a/src/sentry/static/sentry/app/views/organizationDashboard.jsx
+++ b/src/sentry/static/sentry/app/views/organizationDashboard.jsx
@@ -184,6 +184,27 @@ const Activity = React.createClass({
   },
 });
 
+const EPM = React.createClass({
+
+  mixins: [OrganizationState],
+
+  getEndpoint() {
+    return `/organizations/${this.props.params.orgId}/activity/`;
+  },
+
+  render() {
+    let org = this.getOrganization();
+
+    return (
+      <div>
+        <Link className="btn-sidebar-header" to={`/organizations/${org.slug}/stats/`}>View Stats</Link>
+        <h6 className="nav-header">Events Per Minute</h6>
+
+      </div>
+    );
+  },
+});
+
 const OrganizationDashboard = React.createClass({
   mixins: [
     ApiMixin,
@@ -235,6 +256,8 @@ const OrganizationDashboard = React.createClass({
             <Activity {...this.props} />
           </div>
           <div className="col-md-4">
+            <EPM />
+            <hr />
             <ProjectList {...this.props} teams={this.state.teams} />
           </div>
         </div>
diff --git a/src/sentry/static/sentry/less/organization.less b/src/sentry/static/sentry/less/organization.less
index b85e3e3c64..93b98acbf6 100644
--- a/src/sentry/static/sentry/less/organization.less
+++ b/src/sentry/static/sentry/less/organization.less
@@ -63,7 +63,7 @@
 
     .activity-item {
       margin: 0;
-      padding: 10px 15px 10px 60px;
+      padding: 10px 15px 10px 62px;
       border-bottom: 1px solid lighten(@trim, 5);
       line-height: 1.4;
       font-size: 15px;
@@ -74,6 +74,7 @@
 
       .activity-item-content {
         display: block;
+        padding: 0;
       }
 
       .activity-item-bubble {
