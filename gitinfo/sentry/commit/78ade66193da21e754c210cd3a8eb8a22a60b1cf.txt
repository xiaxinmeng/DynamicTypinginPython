commit 78ade66193da21e754c210cd3a8eb8a22a60b1cf
Author: David Cramer <dcramer@gmail.com>
Date:   Mon Apr 6 18:01:48 2015 -0700

    [stream] Reduce limit to 50 results

diff --git a/src/sentry/static/sentry/app/views/stream.jsx b/src/sentry/static/sentry/app/views/stream.jsx
index 2a276b7db8..fcb39cf183 100644
--- a/src/sentry/static/sentry/app/views/stream.jsx
+++ b/src/sentry/static/sentry/app/views/stream.jsx
@@ -109,6 +109,7 @@ var Stream = React.createClass({
   getGroupListEndpoint() {
     var params = this.getParams();
     var queryParams = this.getQuery();
+    queryParams.limit = 50;
     var querystring = $.param(queryParams);
 
     return '/projects/' + params.orgId + '/' + params.projectId + '/groups/?' + querystring;
