commit 23d132b835dab17b52249808b6c324ca4f115da5
Author: James Cunningham <cunninghamjt09@gmail.com>
Date:   Fri Aug 26 12:14:41 2016 -1000

    Missed the other bulk delete log line. (#4023)
    
    * Missed the other bulk delete log line.
    
    * Actually typecast it.

diff --git a/src/sentry/utils/query.py b/src/sentry/utils/query.py
index 371dad489d..dc15e51eee 100644
--- a/src/sentry/utils/query.py
+++ b/src/sentry/utils/query.py
@@ -276,12 +276,6 @@ def bulk_delete_objects(model, limit=10000, transaction_id=None, logger=None, **
         query.append('%s = %%s' % (quote_name(column),))
         params.append(value)
 
-    if logger is not None:
-        logger.info('%s.remove.deleted' % model.__name__.lower(), extra={
-            column: value,
-            'transaction_id': transaction_id,
-        })
-
     if db.is_postgres():
         query = """
             delete from %(table)s
@@ -317,4 +311,11 @@ def bulk_delete_objects(model, limit=10000, transaction_id=None, logger=None, **
 
     cursor = connection.cursor()
     cursor.execute(query, params)
+
+    if logger is not None:
+        logger.info('object.delete.bulk_executed', extra=dict(filters.items() + [
+            ('model', model.__name__),
+            ('transaction_id', transaction_id),
+        ]))
+
     return cursor.rowcount > 0
