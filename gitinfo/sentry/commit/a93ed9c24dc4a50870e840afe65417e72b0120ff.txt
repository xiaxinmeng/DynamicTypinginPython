commit a93ed9c24dc4a50870e840afe65417e72b0120ff
Author: David Cramer <dcramer@gmail.com>
Date:   Fri Mar 27 15:26:31 2015 -0700

    Ensure plaintext emails dont render html bodies

diff --git a/src/sentry/plugins/sentry_mail/models.py b/src/sentry/plugins/sentry_mail/models.py
index 7e0a904cb2..80c27b4b17 100644
--- a/src/sentry/plugins/sentry_mail/models.py
+++ b/src/sentry/plugins/sentry_mail/models.py
@@ -144,7 +144,10 @@ class MailPlugin(NotificationPlugin):
             body = interface.to_email_html(event)
             if not body:
                 continue
-            interface_list.append((interface.get_title(), mark_safe(body)))
+            text_body = interface.to_string(event)
+            interface_list.append(
+                (interface.get_title(), mark_safe(body), text_body)
+            )
 
         subject = group.get_email_subject()
 
diff --git a/src/sentry/templates/sentry/emails/error.html b/src/sentry/templates/sentry/emails/error.html
index 971285dd8e..0f2d205049 100644
--- a/src/sentry/templates/sentry/emails/error.html
+++ b/src/sentry/templates/sentry/emails/error.html
@@ -32,10 +32,10 @@
     {% endfor %}
     </ul>
 
-    {% for label, text in interfaces %}
+    {% for label, html, _ in interfaces %}
     <div class="interface">
         <h3 class="title">{{ label }}</h3>
-		{{ text }}
+		{{ html }}
     </div>
     {% endfor %}
 
diff --git a/src/sentry/templates/sentry/emails/error.txt b/src/sentry/templates/sentry/emails/error.txt
index 11c30d6613..f1e184ded1 100644
--- a/src/sentry/templates/sentry/emails/error.txt
+++ b/src/sentry/templates/sentry/emails/error.txt
@@ -11,7 +11,7 @@ Details
 
 First Seen: {{ group.first_seen|date }}
 
-{% if interfaces %}{% for label, text in interfaces %}
+{% if interfaces %}{% for label, _, text in interfaces %}
 {{ label }}
 -----------
 
