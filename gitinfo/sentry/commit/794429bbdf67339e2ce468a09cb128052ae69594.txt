commit 794429bbdf67339e2ce468a09cb128052ae69594
Author: Dora <dora.l.chan@gmail.com>
Date:   Mon Apr 27 08:00:33 2020 -0700

    feat(perf-subtitle): change to transactions (#18447)
    
    * rename to slowest transactions
    
    * updating right hand scores
    
    Co-authored-by: Dora Chan <dora.lchan@gmail.com>

diff --git a/src/sentry/static/sentry/app/views/performance/transactionSummary/table.tsx b/src/sentry/static/sentry/app/views/performance/transactionSummary/table.tsx
index 511b480b1c..9beb524de4 100644
--- a/src/sentry/static/sentry/app/views/performance/transactionSummary/table.tsx
+++ b/src/sentry/static/sentry/app/views/performance/transactionSummary/table.tsx
@@ -150,7 +150,7 @@ class SummaryContentTable extends React.Component<Props> {
   render() {
     const {eventView, organization, isLoading, tableData} = this.props;
 
-    let title = t('Slowest Requests');
+    let title = t('Slowest Transactions');
     const parsed = tokenizeSearch(eventView.query);
     if (parsed['transaction.duration']) {
       title = t('Requests %s and %s in duration', ...parsed['transaction.duration']);
diff --git a/src/sentry/static/sentry/app/views/performance/transactionSummary/userStats.tsx b/src/sentry/static/sentry/app/views/performance/transactionSummary/userStats.tsx
index 5221d8d576..d23dc53448 100644
--- a/src/sentry/static/sentry/app/views/performance/transactionSummary/userStats.tsx
+++ b/src/sentry/static/sentry/app/views/performance/transactionSummary/userStats.tsx
@@ -77,14 +77,14 @@ class UserStats extends React.Component<Props> {
 
           return (
             <Container>
-              <div>
-                <StatTitle>{t('User Impact')}</StatTitle>
-                <StatNumber>{stats['impact()']}</StatNumber>
-              </div>
               <div>
                 <StatTitle>{t('Apdex Score')}</StatTitle>
                 <StatNumber>{stats['apdex()']}</StatNumber>
               </div>
+              <div>
+                <StatTitle>{t('User Impact')}</StatTitle>
+                <StatNumber>{stats['impact()']}</StatNumber>
+              </div>
             </Container>
           );
         }}
@@ -94,11 +94,8 @@ class UserStats extends React.Component<Props> {
 }
 
 const Container = styled('div')`
-  margin-bottom: ${space(3)};
+  margin-bottom: ${space(4)};
   display: flex;
-
-  color: ${p => p.theme.gray3};
-
   > * + * {
     margin-left: ${space(4)};
   }
@@ -106,13 +103,12 @@ const Container = styled('div')`
 
 const StatTitle = styled('h4')`
   font-size: ${p => p.theme.fontSizeMedium};
-  margin-top: ${space(1)};
-  margin-bottom: ${space(0.5)};
+  color: ${p => p.theme.gray3};
+  margin: ${space(1)} 0 ${space(1.5)} 0;
 `;
 
 const StatNumber = styled('div')`
   font-size: 32px;
-  line-height: 40px;
   color: ${p => p.theme.gray4};
 `;
 
