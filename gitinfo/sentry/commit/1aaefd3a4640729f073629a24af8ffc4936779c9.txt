commit 1aaefd3a4640729f073629a24af8ffc4936779c9
Author: Matt Robenolt <matt@ydekproductions.com>
Date:   Thu Jan 16 19:30:31 2014 -0800

    Don't need to double check for Re

diff --git a/src/sentry/utils/email.py b/src/sentry/utils/email.py
index 6021381d94..f84bf865a7 100644
--- a/src/sentry/utils/email.py
+++ b/src/sentry/utils/email.py
@@ -163,8 +163,7 @@ class MessageBuilder(object):
         if self.reply_to_id is not None:
             headers.setdefault('In-Reply-To', self.reply_to_id)
             headers.setdefault('References', self.reply_to_id)
-            if subject[:4] != 'Re: ':
-                subject = 'Re: %s' % subject
+            subject = 'Re: %s' % subject
 
         msg = EmailMultiAlternatives(
             subject,
