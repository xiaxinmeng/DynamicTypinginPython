commit a3ba30af6a1b46a1cc0d9f4ef792af788df9888a
Author: Lyn Nagara <lyn.nagara@gmail.com>
Date:   Mon Sep 30 13:47:58 2019 -0700

    ref: Clean up sampling related code (#14855)
    
    Since the sampling feature has been removed we can now delete the remaining unused parts of the sampling related code.

diff --git a/src/sentry/conf/server.py b/src/sentry/conf/server.py
index 1021ff8752..65d17e9673 100644
--- a/src/sentry/conf/server.py
+++ b/src/sentry/conf/server.py
@@ -935,23 +935,6 @@ SENTRY_CELERYBEAT_MONITORS = {
     # 'scheduled-name': 'monitor_guid',
 }
 
-# Only store a portion of all messages per unique group.
-SENTRY_SAMPLE_DATA = False
-
-# The following values control the sampling rates
-SENTRY_SAMPLE_RATES = (
-    # up until N events, store 1 in M
-    (50, 1),
-    (1000, 2),
-    (10000, 10),
-    (100000, 50),
-    (1000000, 300),
-    (10000000, 2000),
-)
-SENTRY_MAX_SAMPLE_RATE = 10000
-SENTRY_SAMPLE_TIMES = ((3600, 1), (360, 10), (60, 60))
-SENTRY_MAX_SAMPLE_TIME = 10000
-
 # Web Service
 SENTRY_WEB_HOST = "localhost"
 SENTRY_WEB_PORT = 9000
diff --git a/src/sentry/eventstream/base.py b/src/sentry/eventstream/base.py
index c95f475a36..8da6811688 100644
--- a/src/sentry/eventstream/base.py
+++ b/src/sentry/eventstream/base.py
@@ -60,7 +60,6 @@ class EventStream(Service):
         is_new_group_environment,
         primary_hash,
         skip_consume=False,
-        is_sample=False,  # TODO: Remove once this is no longer passed
     ):
         self._dispatch_post_process_group_task(
             event, is_new, is_regression, is_new_group_environment, primary_hash, skip_consume
diff --git a/src/sentry/eventstream/snuba.py b/src/sentry/eventstream/snuba.py
index 5750ce513f..96bd11270e 100644
--- a/src/sentry/eventstream/snuba.py
+++ b/src/sentry/eventstream/snuba.py
@@ -84,7 +84,6 @@ class SnubaProtocolEventStream(EventStream):
         is_new_group_environment,
         primary_hash,
         skip_consume=False,
-        is_sample=False,  # TODO: Remove once this is no longer passed
     ):
         project = event.project
         retention_days = quotas.get_event_retention(organization=project.organization)
@@ -251,7 +250,6 @@ class SnubaEventStream(SnubaProtocolEventStream):
         is_new_group_environment,
         primary_hash,
         skip_consume=False,
-        is_sample=False,  # TODO: Remove once this is no longer passed
     ):
         super(SnubaEventStream, self).insert(
             group,
