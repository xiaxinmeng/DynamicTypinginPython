commit 98fefc27105641ff695d6e965873c7075ff8e90f
Author: Mark Story <mark@sentry.io>
Date:   Tue Aug 27 10:09:42 2019 -0400

    ref(ui) Convert Well to typescript (#14505)
    
    Small and simple component to update.

diff --git a/src/sentry/static/sentry/app/components/well.jsx b/src/sentry/static/sentry/app/components/well.tsx
similarity index 62%
rename from src/sentry/static/sentry/app/components/well.jsx
rename to src/sentry/static/sentry/app/components/well.tsx
index 6f7071cd3a..19e274f4f6 100644
--- a/src/sentry/static/sentry/app/components/well.jsx
+++ b/src/sentry/static/sentry/app/components/well.tsx
@@ -1,7 +1,17 @@
+import React from 'react';
 import styled from 'react-emotion';
 import PropTypes from 'prop-types';
 
-const Well = styled('div')`
+type Props = {
+  hasImage: boolean | undefined;
+  centered: boolean | undefined;
+  children: React.ReactNode;
+  theme: any;
+};
+
+type WellProps = Omit<React.HTMLProps<HTMLDivElement>, keyof Props> & Props;
+
+const Well = styled('div')<WellProps>`
   border: 1px solid ${p => p.theme.borderLight};
   box-shadow: none;
   background: ${p => p.theme.whiteDark};
@@ -16,9 +26,4 @@ Well.propTypes = {
   centered: PropTypes.bool,
 };
 
-Well.defaultProps = {
-  hasImage: false,
-  centered: false,
-};
-
 export default Well;
