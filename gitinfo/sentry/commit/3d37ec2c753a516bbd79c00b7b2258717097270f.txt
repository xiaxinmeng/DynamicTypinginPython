commit 3d37ec2c753a516bbd79c00b7b2258717097270f
Author: Ben Vinegar <ben@benv.ca>
Date:   Mon Sep 19 16:16:46 2016 -0700

    Render new sidebar on django-rendered pages

diff --git a/src/sentry/static/sentry/app/index.js b/src/sentry/static/sentry/app/index.js
index e44fcdd134..325b7fd740 100644
--- a/src/sentry/static/sentry/app/index.js
+++ b/src/sentry/static/sentry/app/index.js
@@ -80,6 +80,7 @@ export default {
     ProjectIssueTracking: require('./views/projectIssueTracking'),
     ProjectSelector: require('./components/projectHeader/projectSelector'),
     RuleEditor: require('./views/ruleEditor'),
+    Sidebar: require('./components/sidebar'),
     StackedBarChart: require('./components/stackedBarChart'),
     TimeSince: require('./components/timeSince'),
     TodoList: require('./components/todos'),
diff --git a/src/sentry/templates/sentry/layout.html b/src/sentry/templates/sentry/layout.html
index 2852edcdb1..d5ccac2cac 100644
--- a/src/sentry/templates/sentry/layout.html
+++ b/src/sentry/templates/sentry/layout.html
@@ -71,11 +71,39 @@
         {% show_system_status %}
     {% endif %}
 
+    {% if request.user.is_authenticated %}
+    <div id="blk_sidebar"></div>
+    <script>
+    $(function(){
+      var SidebarWrapper = React.createClass({
+        propTypes: {
+          organization: React.PropTypes.object
+        },
+        getChildContext: function () {
+          return {
+            organization: this.props.organization,
+          }
+        },
+        render: function () {
+          return React.createElement(Sentry.Sidebar);
+        }
+      });
+      SidebarWrapper.childContextTypes = {
+        organization: React.PropTypes.object
+      };
+      ReactDOM.render(React.createFactory(SidebarWrapper)({
+        organization: {% serialize_detailed_org organization %},
+      }), document.getElementById('blk_sidebar'));
+    });
+    </script>
+    {% endif %}
+
     {% block alerts %}
     {% include "sentry/partial/alerts.html" %}
     {% endblock %}
 
     {% block header %}
+    {% comment %}
     <header>
       <div class="container">
         {% if request.user.is_authenticated %}
@@ -139,6 +167,7 @@
         {% endif %}
       </div>
     </header>
+    {% endcomment %}
     {% endblock %}
 
     {% if organization and ACCESS.org_read %}
@@ -148,9 +177,6 @@
         <div class="pull-right">
           {% block header_action %}{% endblock %}
         </div>
-        <div class="org-name">
-            <a href="{% url 'sentry-organization-home' organization.slug %}">{{ organization.name }}</a>
-        </div>
         <div id="blk_projectselect"></div>
         <script>
         $(function(){
