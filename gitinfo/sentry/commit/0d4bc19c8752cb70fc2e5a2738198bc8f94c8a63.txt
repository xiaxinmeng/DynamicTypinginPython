commit 0d4bc19c8752cb70fc2e5a2738198bc8f94c8a63
Author: David Cramer <dcramer@gmail.com>
Date:   Sat Feb 4 00:22:44 2012 -0800

    Minor useless cleanup

diff --git a/sentry/manager.py b/sentry/manager.py
index ae670a7fa5..0d124ad659 100644
--- a/sentry/manager.py
+++ b/sentry/manager.py
@@ -349,7 +349,6 @@ class GroupManager(models.Manager, ChartMixin):
             except Exception, e:
                 transaction.rollback_unless_managed(using=group._state.db)
                 logger.exception(u'Error indexing document: %s', e)
-                import traceback; traceback.print_exc()
 
         if is_new:
             try:
diff --git a/sentry/queue/client.py b/sentry/queue/client.py
index 07c862e023..736f927864 100644
--- a/sentry/queue/client.py
+++ b/sentry/queue/client.py
@@ -30,6 +30,7 @@ class Broker(object):
                 maybe_declare(queue, producer.channel)
 
         return self._connection
+
     connection = property(_get_connection)
 
     def delay(self, func, *args, **kwargs):
