commit 0c4deede7194cec914226abc1a355f98aab2f0ef
Author: David Cramer <dcramer@gmail.com>
Date:   Wed Jun 4 20:38:07 2014 -0700

    Various scheduled task tweaks

diff --git a/src/sentry/conf/server.py b/src/sentry/conf/server.py
index 4ed4c8ec32..fb1bfe0f14 100644
--- a/src/sentry/conf/server.py
+++ b/src/sentry/conf/server.py
@@ -310,14 +310,25 @@ CELERYBEAT_SCHEDULE = {
     'check-alerts': {
         'task': 'sentry.tasks.check_alerts',
         'schedule': timedelta(minutes=1),
+        'options': {
+            'expires': 60,
+            'queue': 'alerts',
+        }
     },
     'check-version': {
         'task': 'sentry.tasks.check_update',
         'schedule': timedelta(hours=1),
+        'options': {
+            'expires': 3600,
+        },
     },
     'flush-buffers': {
         'task': 'sentry.tasks.process_buffer.process_pending',
         'schedule': timedelta(seconds=10),
+        'options': {
+            'expires': 10,
+            'queue': 'counters-0',
+        }
     },
 }
 
