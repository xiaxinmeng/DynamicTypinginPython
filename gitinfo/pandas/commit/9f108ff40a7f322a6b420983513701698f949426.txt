commit 9f108ff40a7f322a6b420983513701698f949426
Author: Jeff Reback <jeff@reback.net>
Date:   Sat Sep 5 19:45:09 2015 -0400

    TST: avoid some numpy comparison warnings

diff --git a/pandas/src/testing.pyx b/pandas/src/testing.pyx
index 1abc75855..9839c9f41 100644
--- a/pandas/src/testing.pyx
+++ b/pandas/src/testing.pyx
@@ -1,7 +1,7 @@
 import numpy as np
 
 from pandas import compat
-from pandas.core.common import isnull
+from pandas.core.common import isnull, array_equivalent
 
 cdef NUMERIC_TYPES = (
     bool,
@@ -120,7 +120,7 @@ cpdef assert_almost_equal(a, b, bint check_less_precise=False,
                 raise_assert_detail(obj, '{0} shapes are different'.format(obj),
                                     a.shape, b.shape)
             try:
-                if np.array_equal(a, b):
+                if array_equivalent(a, b, strict_nan=True):
                     return True
             except:
                 pass
