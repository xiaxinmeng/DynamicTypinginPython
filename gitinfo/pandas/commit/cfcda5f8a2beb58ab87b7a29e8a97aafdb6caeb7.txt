commit cfcda5f8a2beb58ab87b7a29e8a97aafdb6caeb7
Author: Angelos Evripiotis <jevripiotis@bloomberg.net>
Date:   Sat Nov 29 11:39:09 2014 +0000

    CLN: move import to top of file
    
    For consistency with [PEP8][1]:
    
        Imports are always put at the top of the file, just after any module
        comments and docstrings, and before module globals and constants.
    
    [1]: https://www.python.org/dev/peps/pep-0008#id17

diff --git a/pandas/core/config.py b/pandas/core/config.py
index 60dc1d7d0..6768e0af0 100644
--- a/pandas/core/config.py
+++ b/pandas/core/config.py
@@ -51,6 +51,7 @@ Implementation
 import re
 
 from collections import namedtuple
+from contextlib import contextmanager
 import warnings
 from pandas.compat import map, lmap, u
 import pandas.compat as compat
@@ -681,8 +682,6 @@ def pp_options_list(keys, width=80, _print=False):
 #
 # helpers
 
-from contextlib import contextmanager
-
 
 @contextmanager
 def config_prefix(prefix):
