commit 313b8e4a3fbbf747ed4e57b8ade465775b48677e
Author: Jeff Reback <jeff@reback.net>
Date:   Thu Nov 17 12:54:23 2016 -0500

    BLD: use miniconda3 as the miniconda dir

diff --git a/ci/install_travis.sh b/ci/install_travis.sh
index 39f6808b9..bdd2c01f6 100755
--- a/ci/install_travis.sh
+++ b/ci/install_travis.sh
@@ -31,7 +31,7 @@ edit_init
 home_dir=$(pwd)
 echo "home_dir: [$home_dir]"
 
-MINICONDA_DIR="$HOME/miniconda"
+MINICONDA_DIR="$HOME/miniconda3"
 
 if [ -d "$MINICONDA_DIR" ] && [ -e "$MINICONDA_DIR/bin/conda" ] && [ "$USE_CACHE" ]; then
     echo "Miniconda install already present from cache: $MINICONDA_DIR"
