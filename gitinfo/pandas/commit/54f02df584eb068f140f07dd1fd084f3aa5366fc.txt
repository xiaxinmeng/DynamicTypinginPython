commit 54f02df584eb068f140f07dd1fd084f3aa5366fc
Author: jreback <jeff@reback.net>
Date:   Sun Aug 23 10:41:24 2015 -0400

    COMPAT: value_counts always return int64 dtype, xref #10876

diff --git a/pandas/core/categorical.py b/pandas/core/categorical.py
index 78c9d264c..9951024ff 100644
--- a/pandas/core/categorical.py
+++ b/pandas/core/categorical.py
@@ -1046,7 +1046,7 @@ class Categorical(PandasObject):
         ix = Categorical(ix, categories=cat,
                 ordered=obj.ordered, fastpath=True)
 
-        return Series(count, index=CategoricalIndex(ix))
+        return Series(count, index=CategoricalIndex(ix), dtype='int64')
 
     def get_values(self):
         """ Return the values.
