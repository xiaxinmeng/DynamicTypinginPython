commit 5834ccd0bfb495a8044e62e39e0bfcd9cc6a255a
Author: Adam Klein <adamklein@gmail.com>
Date:   Tue Mar 6 17:34:10 2012 -0500

    BUG: close #859, clearer err msg on failed slicing situations

diff --git a/pandas/core/generic.py b/pandas/core/generic.py
index 780e7dda2..5272aa308 100644
--- a/pandas/core/generic.py
+++ b/pandas/core/generic.py
@@ -279,10 +279,16 @@ class NDFrame(PandasObject):
         try:
             return cache[item]
         except Exception:
-            values = self._data.get(item)
-            res = self._box_item_values(item, values)
-            cache[item] = res
-            return res
+            try:
+                values = self._data.get(item)
+                res = self._box_item_values(item, values)
+                cache[item] = res
+                return res
+            except Exception:
+                from pandas.core.frame import DataFrame 
+                if isinstance(item, DataFrame):
+                    raise ValueError('Cannot slice using dataframe')
+                raise
 
     def _box_item_values(self, key, values):
         raise NotImplementedError
diff --git a/pandas/core/indexing.py b/pandas/core/indexing.py
index cdb1d2c85..f844981d1 100644
--- a/pandas/core/indexing.py
+++ b/pandas/core/indexing.py
@@ -168,6 +168,9 @@ class _NDFrameIndexer(object):
         elif _is_list_like(key) and not (isinstance(key, tuple) and
                                          isinstance(labels, MultiIndex)):
 
+            if hasattr(key, 'ndim') and key.ndim > 1:
+                raise ValueError('Cannot slice with multidimensional input')
+ 
             return self._getitem_iterable(key, axis=axis)
         elif axis == 0:
             is_int_index = _is_integer_index(labels)
diff --git a/pandas/tests/test_frame.py b/pandas/tests/test_frame.py
index 1d1c0a09d..094ce1e37 100644
--- a/pandas/tests/test_frame.py
+++ b/pandas/tests/test_frame.py
@@ -395,6 +395,8 @@ class CheckIndexing(object):
         exp.values[5:10] = 5
         assert_frame_equal(f, exp)
 
+        self.assertRaises(ValueError, ix.__getitem__, f > 0.5)
+
     def test_slice_floats(self):
         index = [52195.504153, 52196.303147, 52198.369883]
         df = DataFrame(np.random.rand(3, 2), index=index)
