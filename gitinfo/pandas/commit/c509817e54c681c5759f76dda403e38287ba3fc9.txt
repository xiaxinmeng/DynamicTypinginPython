commit c509817e54c681c5759f76dda403e38287ba3fc9
Author: Dieter Vandenbussche <dvandenbussche@axioma.com>
Date:   Thu Feb 9 11:03:46 2012 -0500

    Add sheet_names property to ExcelFile, with tests

diff --git a/pandas/io/parsers.py b/pandas/io/parsers.py
index d3bef75c6..834e22e04 100644
--- a/pandas/io/parsers.py
+++ b/pandas/io/parsers.py
@@ -669,6 +669,14 @@ class ExcelFile(object):
 
         return parser.get_chunk()
 
+    @property
+    def sheet_names(self):
+        if self.use_xlsx:
+            return self.book.get_sheet_names()
+        else:
+            return self.book.sheet_names()
+
+
 def _trim_excel_header(row):
     # trim header row so auto-index inference works
     while len(row) > 0 and row[0] == '':
diff --git a/pandas/tests/test_frame.py b/pandas/tests/test_frame.py
index dd7710365..176d408f5 100644
--- a/pandas/tests/test_frame.py
+++ b/pandas/tests/test_frame.py
@@ -2352,6 +2352,10 @@ class TestDataFrame(unittest.TestCase, CheckIndexing,
             assert_frame_equal(self.frame, recons)
             recons = reader.parse('test2',index_col=0)
             assert_frame_equal(self.tsframe, recons)
+            np.testing.assert_equal(2, len(reader.sheet_names))
+            np.testing.assert_equal('test1', reader.sheet_names[0])
+            np.testing.assert_equal('test2', reader.sheet_names[1])
+
 
             os.remove(path)
 
