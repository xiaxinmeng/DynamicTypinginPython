commit 775dd4072f5403694e7596b6395eafa5978db2af
Author: Marc Garcia <garcia.marc@gmail.com>
Date:   Wed Dec 4 13:21:44 2019 +0000

    CI: Fix print skipped tests (#30044)

diff --git a/ci/print_skipped.py b/ci/print_skipped.py
index 51a2460e0..243f390fd 100755
--- a/ci/print_skipped.py
+++ b/ci/print_skipped.py
@@ -5,12 +5,12 @@ import xml.etree.ElementTree as et
 
 def main(filename):
     if not os.path.isfile(filename):
-        return
+        raise RuntimeError(f"Could not find junit file {filename!r}")
 
     tree = et.parse(filename)
     root = tree.getroot()
     current_class = ""
-    for el in root.findall("testcase"):
+    for el in root.iter("testcase"):
         cn = el.attrib["classname"]
         for sk in el.findall("skipped"):
             old_class = current_class
