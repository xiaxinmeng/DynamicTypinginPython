commit 5a152bdfdd044c6f53c76b361065b1784dc73b61
Author: Chang She <changshe@gmail.com>
Date:   Wed Nov 7 18:03:38 2012 -0500

    Revert 866217640c false alarm

diff --git a/pandas/core/internals.py b/pandas/core/internals.py
index 76550a0b5..10a85c559 100644
--- a/pandas/core/internals.py
+++ b/pandas/core/internals.py
@@ -1325,8 +1325,7 @@ def _stack_arrays(tuples, ref_items, dtype):
     names, arrays = zip(*tuples)
 
     # index may box values
-    names = Index(names)
-    items = names[names.isin(ref_items)]
+    items = ref_items[ref_items.isin(names)]
 
     first = arrays[0]
     shape = (len(arrays),) + _shape_compat(first)
diff --git a/pandas/tests/test_frame.py b/pandas/tests/test_frame.py
index d728511c2..5c5fd1902 100644
--- a/pandas/tests/test_frame.py
+++ b/pandas/tests/test_frame.py
@@ -2319,13 +2319,6 @@ class TestDataFrame(unittest.TestCase, CheckIndexing,
                           [('a',[8]),('a',[5]), ('b', [6])],
                           columns=['b', 'a','a'])
 
-        #additional test for #2079
-        vals = [[1, -1, 2.], [2, -2, 3.]]
-        rs = DataFrame(vals, columns=['A', 'A', 'B'])
-        xp = DataFrame(vals)
-        xp.columns = ['A', 'A', 'B']
-        assert_frame_equal(rs, xp)
-
     def test_new_empty_index(self):
         df1 = DataFrame(randn(0, 3))
         df2 = DataFrame(randn(0, 3))
