commit 6c6ddcfaf9832dcda4ee43654202685b3e749635
Author: Chang She <chang@lambdafoundry.com>
Date:   Tue Apr 10 09:05:18 2012 -0400

    TST: scatter matrix plot
    DOC: corrected misleading doc string in scatter_matrix

diff --git a/pandas/tests/test_graphics.py b/pandas/tests/test_graphics.py
index 426cc5dd0..325928cd4 100644
--- a/pandas/tests/test_graphics.py
+++ b/pandas/tests/test_graphics.py
@@ -9,6 +9,7 @@ import pandas.util.testing as tm
 import numpy as np
 
 from numpy.testing.decorators import slow
+import pandas.tools.plotting as plotting
 
 class TestSeriesPlots(unittest.TestCase):
 
@@ -124,6 +125,16 @@ class TestDataFramePlots(unittest.TestCase):
         _check_plot_works(df.hist)
         _check_plot_works(df.hist, grid=False)
 
+    @slow
+    def test_scatter(self):
+        df = DataFrame(np.random.randn(100, 4))
+        import pandas.tools.plotting as plt
+        def scat(**kwds):
+            return plt.scatter_matrix(df, **kwds)
+        _check_plot_works(scat)
+        _check_plot_works(scat, marker='+')
+        _check_plot_works(scat, vmin=0)
+
     @slow
     def test_plot_int_columns(self):
         df = DataFrame(np.random.randn(100, 4)).cumsum()
diff --git a/pandas/tools/plotting.py b/pandas/tools/plotting.py
index 7e9d565a7..f80b69f21 100644
--- a/pandas/tools/plotting.py
+++ b/pandas/tools/plotting.py
@@ -19,7 +19,7 @@ def scatter_matrix(frame, **kwds):
     Examples
     --------
     >>> df = DataFrame(np.random.randn(1000, 4), columns=['A','B','C','D'])
-    >>> df.scatter_matrix(alpha=0.2)
+    >>> scatter_matrix(df, alpha=0.2)
     """
     df = frame._get_numeric_data()
     n = df.columns.size
