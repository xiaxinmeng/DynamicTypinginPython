commit 16c56d6b7868605f76dcb1021fb75e51117c14b2
Author: jbrockmendel <jbrockmendel@gmail.com>
Date:   Wed Jan 1 17:02:23 2020 -0800

    CLN: remove itemsize, should have gone in #29918 (#30606)

diff --git a/pandas/core/arrays/categorical.py b/pandas/core/arrays/categorical.py
index 53051baa8..5f4bd8014 100644
--- a/pandas/core/arrays/categorical.py
+++ b/pandas/core/arrays/categorical.py
@@ -302,7 +302,7 @@ class Categorical(ExtensionArray, PandasObject):
     __array_priority__ = 1000
     _dtype = CategoricalDtype(ordered=False)
     # tolist is not actually deprecated, just suppressed in the __dir__
-    _deprecations = PandasObject._deprecations | frozenset(["tolist", "itemsize"])
+    _deprecations = PandasObject._deprecations | frozenset(["tolist"])
     _typ = "categorical"
 
     def __init__(
diff --git a/pandas/core/indexes/category.py b/pandas/core/indexes/category.py
index 531014e4a..5552efefa 100644
--- a/pandas/core/indexes/category.py
+++ b/pandas/core/indexes/category.py
@@ -361,11 +361,6 @@ class CategoricalIndex(Index, accessor.PandasDelegate):
         """ return the underlying data, which is a Categorical """
         return self._data
 
-    @property
-    def itemsize(self):
-        # Size of the items in categories, not codes.
-        return self.values.itemsize
-
     def _wrap_setop_result(self, other, result):
         name = get_op_result_name(self, other)
         return self._shallow_copy(result, name=name)
