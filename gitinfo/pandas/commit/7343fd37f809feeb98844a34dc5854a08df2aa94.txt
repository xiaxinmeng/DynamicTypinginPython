commit 7343fd37f809feeb98844a34dc5854a08df2aa94
Author: Thiviyan Thanapalasingam <thiviyan.t@gmail.com>
Date:   Wed Sep 26 20:41:11 2018 +0100

    DOC: Handle whitespace in pathnames (#20880)

diff --git a/doc/make.py b/doc/make.py
index d85747458..cab5fa0ed 100755
--- a/doc/make.py
+++ b/doc/make.py
@@ -233,10 +233,10 @@ class DocBuilder:
                      '-b{}'.format(kind),
                      '-{}'.format(
                          'v' * self.verbosity) if self.verbosity else '',
-                     '-d{}'.format(os.path.join(BUILD_PATH, 'doctrees')),
+                     '-d"{}"'.format(os.path.join(BUILD_PATH, 'doctrees')),
                      '-Dexclude_patterns={}'.format(self.exclude_patterns),
-                     SOURCE_PATH,
-                     os.path.join(BUILD_PATH, kind))
+                     '"{}"'.format(SOURCE_PATH),
+                     '"{}"'.format(os.path.join(BUILD_PATH, kind)))
 
     def _open_browser(self):
         base_url = os.path.join('file://', DOC_PATH, 'build', 'html')
