commit 744a814383deb7a0d9fcde5e209f6447d68c9ceb
Author: Skipper Seabold <jsseabold@gmail.com>
Date:   Sun Feb 19 18:51:42 2012 -0500

    ENH: Add explicit init with month check to YearEnd

diff --git a/pandas/core/datetools.py b/pandas/core/datetools.py
index 25455dc9d..475e4b769 100644
--- a/pandas/core/datetools.py
+++ b/pandas/core/datetools.py
@@ -655,6 +655,14 @@ class YearEnd(DateOffset, CacheableOffset):
     """DateOffset increments between calendar year ends"""
     _normalizeFirst = True
 
+    def __init__(self, n=1, **kwds):
+        self.month = kwds.get('month', 12)
+
+        if self.month < 1 or self.month > 12:
+            raise Exception('Month must go from 1 to 12')
+
+        DateOffset.__init__(self, n=n, **kwds)
+
     def apply(self, other):
         n = self.n
         if other.month != 12 or other.day != 31:
