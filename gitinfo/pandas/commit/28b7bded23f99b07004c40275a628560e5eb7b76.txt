commit 28b7bded23f99b07004c40275a628560e5eb7b76
Author: Jan Schulz <jasc@gmx.net>
Date:   Fri Nov 13 00:34:01 2015 +0100

    TST: Allow for more than one acessor on a Series
    
    `.dt` and `.str` can be available when the Series is of type
    category, which makes `.cat` available.

diff --git a/pandas/core/series.py b/pandas/core/series.py
index cab231e8f..29e9a81d1 100644
--- a/pandas/core/series.py
+++ b/pandas/core/series.py
@@ -2704,12 +2704,10 @@ class Series(base.IndexOpsMixin, strings.StringAccessorMixin, generic.NDFrame,):
 
     def _dir_additions(self):
         rv = set()
-        # these accessors are mutually exclusive, so break loop when one exists
         for accessor in self._accessors:
             try:
                 getattr(self, accessor)
                 rv.add(accessor)
-                break
             except AttributeError:
                 pass
         return rv
