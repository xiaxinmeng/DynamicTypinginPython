commit 1bebaa12832efb1139d3e0a720489e219c78fccf
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Fri Feb 24 15:50:08 2012 -0500

    BUG: use Cython take_2d instead of ndarray.take due to Fortran order performance problem, GH #817

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index b5d34a193..5c55e3f67 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -2050,7 +2050,9 @@ class DataFrame(NDFrame):
                 new_columns = self.columns.take(indices)
                 return self.reindex(columns=new_columns)
         else:
-            new_values = self.values.take(indices, axis=axis)
+            new_values = com.take_2d(self.values,
+                                     com._ensure_int32(indices),
+                                     axis=axis)
             if axis == 0:
                 new_columns = self.columns
                 new_index = self.index.take(indices)
