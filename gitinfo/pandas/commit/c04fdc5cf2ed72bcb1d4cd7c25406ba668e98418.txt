commit c04fdc5cf2ed72bcb1d4cd7c25406ba668e98418
Author: Chang She <chang@lambdafoundry.com>
Date:   Tue May 22 18:12:33 2012 -0400

    TST: test length of filtered series

diff --git a/pandas/tseries/tests/test_timeseries.py b/pandas/tseries/tests/test_timeseries.py
index 8a58487de..c7791a9dd 100644
--- a/pandas/tseries/tests/test_timeseries.py
+++ b/pandas/tseries/tests/test_timeseries.py
@@ -595,6 +595,13 @@ class TestTimeSeries(unittest.TestCase):
         close_open = itertools.product([True, False], [True, False])
         for inc_start, inc_end in close_open:
             filtered = ts.between_time(stime, etime, inc_start, inc_end)
+            exp_len = 13 * 4 + 1
+            if not inc_start:
+                exp_len -= 5
+            if not inc_end:
+                exp_len -= 4
+
+            self.assert_(len(filtered) == exp_len)
             for rs in filtered.index:
                 t = rs.time()
                 if inc_start:
