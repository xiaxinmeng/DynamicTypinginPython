commit d82e5aaad5c6b70538f0da7caf1ee37ed9f5e83a
Author: y-p <yoval@gmx.com>
Date:   Wed Apr 3 13:38:55 2013 +0300

    BUG: Log scale on bar chart hides bars GH3247

diff --git a/pandas/tools/plotting.py b/pandas/tools/plotting.py
index 2b41acf28..875376b4d 100644
--- a/pandas/tools/plotting.py
+++ b/pandas/tools/plotting.py
@@ -1370,7 +1370,7 @@ class BarPlot(MPLPlot):
             if self.subplots:
                 ax = self._get_ax(i)  # self.axes[i]
                 rect = bar_f(ax, self.ax_pos, y,
-                             self.bar_width, start=pos_prior, **kwds)
+                             self.bar_width, **kwds)
                 ax.set_title(label)
             elif self.stacked:
                 mask = y > 0
@@ -1381,7 +1381,7 @@ class BarPlot(MPLPlot):
                 neg_prior = neg_prior + np.where(mask, 0, y)
             else:
                 rect = bar_f(ax, self.ax_pos + i * 0.75 / K, y, 0.75 / K,
-                             start=pos_prior, label=label, **kwds)
+                              label=label, **kwds)
             rects.append(rect)
             labels.append(label)
 
