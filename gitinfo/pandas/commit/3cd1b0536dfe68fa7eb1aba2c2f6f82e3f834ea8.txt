commit 3cd1b0536dfe68fa7eb1aba2c2f6f82e3f834ea8
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Thu Feb 23 20:32:34 2012 -0500

    TST: script testing groupby iteration performance GH close #817

diff --git a/scripts/groupby_speed.py b/scripts/groupby_speed.py
new file mode 100644
index 000000000..37d153d38
--- /dev/null
+++ b/scripts/groupby_speed.py
@@ -0,0 +1,32 @@
+from pandas import *
+
+rng = DateRange('1/3/2011', '11/30/2011', offset=datetools.Minute())
+rng5 = DateRange('1/3/2011', '11/30/2011', offset=datetools.Minute(5))
+
+df = DataFrame(np.random.randn(len(rng), 5), index=rng,
+               columns=list('OHLCV'))
+
+gp = rng5.asof
+
+grouped = df.groupby(gp)
+
+def get1(dt):
+    k = gp(dt)
+    return grouped.get_group(k)
+
+def get2(dt):
+    k = gp(dt)
+    return df.ix[grouped.groups[k]]
+
+def f():
+    for i, date in enumerate(df.index):
+        if i % 10000 == 0:
+            print i
+        get1(date)
+
+def g():
+    for i, date in enumerate(df.index):
+        if i % 10000 == 0:
+            print i
+        get2(date)
+
