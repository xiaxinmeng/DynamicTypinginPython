commit 4a5b75b44b00483df9ff4816b6a39b5043f9e2d3
Author: y-p <yoval@gmx.com>
Date:   Sun Nov 11 01:31:09 2012 +0200

    BUG: modify df.iteritems to support duplicate column labels #2219

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index 2bd465537..e4e9705e5 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -633,7 +633,8 @@ class DataFrame(NDFrame):
 
     def iteritems(self):
         """Iterator over (column, series) pairs"""
-        return ((k, self[k]) for k in self.columns)
+        for i, k in enumerate(self.columns):
+            yield (k,self.take([i],axis=1)[k])
 
     def iterrows(self):
         """
