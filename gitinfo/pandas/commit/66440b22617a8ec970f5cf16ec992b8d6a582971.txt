commit 66440b22617a8ec970f5cf16ec992b8d6a582971
Author: iamshwin <23633545+iamshwin@users.noreply.github.com>
Date:   Tue Feb 11 02:01:48 2020 +0000

    DOC: Specify use of google cloud storage for CSVs (#31860)

diff --git a/pandas/io/parsers.py b/pandas/io/parsers.py
index 8bc8470ae..4e26ceef0 100755
--- a/pandas/io/parsers.py
+++ b/pandas/io/parsers.py
@@ -89,7 +89,7 @@ Parameters
 ----------
 filepath_or_buffer : str, path object or file-like object
     Any valid string path is acceptable. The string could be a URL. Valid
-    URL schemes include http, ftp, s3, and file. For file URLs, a host is
+    URL schemes include http, ftp, s3, gs, and file. For file URLs, a host is
     expected. A local file could be: file://localhost/path/to/table.csv.
 
     If you want to pass in a path object, pandas accepts any ``os.PathLike``.
