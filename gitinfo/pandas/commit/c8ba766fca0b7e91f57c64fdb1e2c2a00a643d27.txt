commit c8ba766fca0b7e91f57c64fdb1e2c2a00a643d27
Author: William Ayd <william.ayd@icloud.com>
Date:   Tue Mar 19 02:06:11 2019 -0700

    DOC: Fix typo in docstring of DataFrame.memory_usage  (#25770)
    
    Co-authored-by: Peter Leimbigler <pjleimbigler@gmail.com>

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index 99e5950f0..b75680a2b 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -2468,8 +2468,8 @@ class DataFrame(NDFrame):
         ----------
         index : bool, default True
             Specifies whether to include the memory usage of the DataFrame's
-            index in returned Series. If ``index=True`` the memory usage of the
-            index the first item in the output.
+            index in returned Series. If ``index=True``, the memory usage of
+            the index is the first item in the output.
         deep : bool, default False
             If True, introspect the data deeply by interrogating
             `object` dtypes for system-level memory consumption, and include
