commit ac12c1944ea5afd1a01568d78ca000e178ed112e
Author: y-p <yoval@gmx.com>
Date:   Sun Apr 7 22:55:07 2013 +0300

    BLD: setup.py, proper init of arg in cython cache code

diff --git a/setup.py b/setup.py
index 8b0a2edbc..8cc08c2a3 100755
--- a/setup.py
+++ b/setup.py
@@ -395,6 +395,7 @@ class CompilationCacheExtMixin(CompilationCacheMixin):
 
     def _hash_file(self, fname):
         from hashlib import sha1
+        f= None
         try:
             hash = sha1()
             hash.update(self.build_lib.encode('utf-8'))
@@ -416,7 +417,8 @@ class CompilationCacheExtMixin(CompilationCacheMixin):
                 raise
                 return None
             finally:
-                f.close()
+                if f:
+                    f.close()
 
         except IOError:
             return None
