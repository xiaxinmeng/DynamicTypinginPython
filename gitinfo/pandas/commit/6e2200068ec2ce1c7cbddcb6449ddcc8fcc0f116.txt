commit 6e2200068ec2ce1c7cbddcb6449ddcc8fcc0f116
Author: robbuckley <rob@buckleynet.uk>
Date:   Sat Jan 5 18:32:17 2019 +0000

    CLN: remove redundant mac wheel-build code (#24644)

diff --git a/setup.py b/setup.py
index 6cd359b28..7ba4f5ba3 100755
--- a/setup.py
+++ b/setup.py
@@ -401,20 +401,6 @@ class DummyBuildSrc(Command):
 cmdclass.update({'clean': CleanCommand,
                  'build': build})
 
-try:
-    from wheel.bdist_wheel import bdist_wheel
-
-    class BdistWheel(bdist_wheel):
-        def get_tag(self):
-            tag = bdist_wheel.get_tag(self)
-            repl = 'macosx_10_6_intel.macosx_10_9_intel.macosx_10_9_x86_64'
-            if tag[2] == 'macosx_10_6_intel':
-                tag = (tag[0], tag[1], repl)
-            return tag
-    cmdclass['bdist_wheel'] = BdistWheel
-except ImportError:
-    pass
-
 if cython:
     suffix = '.pyx'
     cmdclass['build_ext'] = CheckingBuildExt
