commit 9d528bf2bd9cdf766cd7f171079205159fa83f96
Author: MomIsBestFriend <50263213+MomIsBestFriend@users.noreply.github.com>
Date:   Sun Dec 1 19:45:22 2019 +0200

    CI: added unwanted pattern test case for "foo.__class__" instead of type(foo) (#29937)

diff --git a/ci/code_checks.sh b/ci/code_checks.sh
index 4e25fd0dd..ceb13c52d 100755
--- a/ci/code_checks.sh
+++ b/ci/code_checks.sh
@@ -194,6 +194,10 @@ if [[ -z "$CHECK" || "$CHECK" == "patterns" ]]; then
     invgrep -R --include="*.py" -P '# type: (?!ignore)' pandas
     RET=$(($RET + $?)) ; echo $MSG "DONE"
 
+    MSG='Check for use of foo.__class__ instead of type(foo)' ; echo $MSG
+    invgrep -R --include=*.{py,pyx} '\.__class__' pandas
+    RET=$(($RET + $?)) ; echo $MSG "DONE"
+
     MSG='Check that no file in the repo contains trailing whitespaces' ; echo $MSG
     INVGREP_APPEND=" <- trailing whitespaces found"
     invgrep -RI --exclude=\*.{svg,c,cpp,html,js} --exclude-dir=env "\s$" *
