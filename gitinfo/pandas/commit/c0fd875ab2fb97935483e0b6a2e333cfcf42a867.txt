commit c0fd875ab2fb97935483e0b6a2e333cfcf42a867
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Wed Aug 17 12:24:12 2011 -0400

    BUG: agg methods should return NaN when all NaN

diff --git a/pandas/core/series.py b/pandas/core/series.py
index 4b984d907..03d1c6424 100644
--- a/pandas/core/series.py
+++ b/pandas/core/series.py
@@ -452,6 +452,8 @@ class Series(np.ndarray, PandasObject):
 #-------------------------------------------------------------------------------
 # Statistics, overridden ndarray methods
 
+    # TODO: integrate bottleneck
+
     def count(self):
         """
         Return number of observations of Series.
@@ -480,6 +482,8 @@ class Series(np.ndarray, PandasObject):
 
         if isnull(retVal):
             arr = remove_na(arr)
+            if len(arr) == 0:
+                return np.nan
             retVal = getattr(arr, funcname)()
 
         return retVal
