commit a6b5e167a4d3ac6e8bd6e041df85f85294cd972b
Author: Steffen Rehberg <steffen.rehberg@googlemail.com>
Date:   Fri Jul 3 16:14:18 2020 +0200

    DOC: Fix code formatting and typos in Series.tz_localize (#35110)

diff --git a/pandas/core/generic.py b/pandas/core/generic.py
index a66cade3b..d892e2487 100644
--- a/pandas/core/generic.py
+++ b/pandas/core/generic.py
@@ -9580,8 +9580,9 @@ class NDFrame(PandasObject, SelectionMixin, indexing.IndexingMixin):
         dtype: int64
 
         If the DST transition causes nonexistent times, you can shift these
-        dates forward or backwards with a timedelta object or `'shift_forward'`
-        or `'shift_backwards'`.
+        dates forward or backward with a timedelta object or `'shift_forward'`
+        or `'shift_backward'`.
+
         >>> s = pd.Series(range(2),
         ...               index=pd.DatetimeIndex(['2015-03-29 02:30:00',
         ...                                       '2015-03-29 03:30:00']))
