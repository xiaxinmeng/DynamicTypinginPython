commit c6b09e418873003c6913d3ae3b60e496a44c3d59
Author: Prabakaran Kumaresshan <k_prabakaran@hotmail.com>
Date:   Sat Jan 5 20:24:33 2019 +0530

    DOC: update applymap docstring in pandas/core/frame.py (#24612) (#24633)

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index 7659f0696..aa78fc6c8 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -6501,6 +6501,14 @@ class DataFrame(NDFrame):
         --------
         DataFrame.apply : Apply a function along input axis of DataFrame.
 
+        Notes
+        -----
+        In the current implementation applymap calls `func` twice on the
+        first column/row to decide whether it can take a fast or slow
+        code path. This can lead to unexpected behavior if `func` has
+        side-effects, as they will take effect twice for the first
+        column/row.
+
         Examples
         --------
         >>> df = pd.DataFrame([[1, 2.12], [3.356, 4.567]])
