commit 2b46f7ed56fb4048c65f575c52b3ada2616cc11a
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Mon Nov 22 01:21:15 2010 -0500

    unit test to raise DeprecationWarning so I don't forget

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index 33912576c..536ab727d 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -1019,7 +1019,7 @@ class DataFrame(Picklable, Groupable):
 
         indexer, mask = common.get_indexer(self.index, index, method)
 
-        # Maybe this is a bit much? Wish I had unit tests...
+        # Maybe this is a bit much? Wish I had more unit tests...
         typeHierarchy = [
             (float, float),
             (int, float),
diff --git a/pandas/core/tests/test_frame.py b/pandas/core/tests/test_frame.py
index f838cdf30..d1f0154ce 100644
--- a/pandas/core/tests/test_frame.py
+++ b/pandas/core/tests/test_frame.py
@@ -626,6 +626,9 @@ class TestDataFrame(unittest.TestCase):
 
         filled = rule_monthly.asfreq('WEEKDAY', method='pad')
 
+        # don't forget!
+        filled_dep = rule_monthly.asfreq('WEEKDAY', fillMethod='pad')
+
         # test does not blow up on length-0 DataFrame
         zero_length = self.tsframe.reindex([])
         result = zero_length.asfreq('EOM')
diff --git a/pandas/core/tests/test_series.py b/pandas/core/tests/test_series.py
index 8e1fc74c1..de66c7624 100644
--- a/pandas/core/tests/test_series.py
+++ b/pandas/core/tests/test_series.py
@@ -623,6 +623,9 @@ class TestSeries(unittest.TestCase):
         # corner case: pad empty series
         reindexed = self.empty.reindex(self.ts.index, method='pad')
 
+        # don't forget to fix this
+        reindexed_dep = self.empty.reindex(self.ts.index, fillMethod='pad')
+
         # pass non-Index
         reindexed = self.ts.reindex(list(self.ts.index))
         assert_series_equal(self.ts, reindexed)
