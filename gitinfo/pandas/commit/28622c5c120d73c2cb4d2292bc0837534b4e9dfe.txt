commit 28622c5c120d73c2cb4d2292bc0837534b4e9dfe
Author: Jan Novotný <30341479+HonzaOsobne@users.noreply.github.com>
Date:   Sat Jul 22 03:21:50 2017 +0200

    Fixed 'add_methods' when the 'select' argument is specified. (#17045)

diff --git a/pandas/core/ops.py b/pandas/core/ops.py
index 017afcd69..bc201be26 100644
--- a/pandas/core/ops.py
+++ b/pandas/core/ops.py
@@ -149,13 +149,15 @@ def _create_methods(arith_method, comp_method, bool_method,
 def add_methods(cls, new_methods, force, select, exclude):
     if select and exclude:
         raise TypeError("May only pass either select or exclude")
-    methods = new_methods
+
     if select:
         select = set(select)
         methods = {}
         for key, method in new_methods.items():
             if key in select:
                 methods[key] = method
+        new_methods = methods
+
     if exclude:
         for k in exclude:
             new_methods.pop(k, None)
