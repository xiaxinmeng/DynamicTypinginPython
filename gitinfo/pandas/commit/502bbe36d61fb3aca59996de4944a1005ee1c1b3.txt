commit 502bbe36d61fb3aca59996de4944a1005ee1c1b3
Author: Thomas Kluyver <takowl@gmail.com>
Date:   Sat Jan 28 21:48:22 2012 +0000

    Don't use unnecessary UnicodeReader on Python 3.

diff --git a/pandas/core/common.py b/pandas/core/common.py
index cd93bd4ea..82d565dd1 100644
--- a/pandas/core/common.py
+++ b/pandas/core/common.py
@@ -854,6 +854,8 @@ class UnicodeReader:
     """
     A CSV reader which will iterate over lines in the CSV file "f",
     which is encoded in the given encoding.
+    
+    On Python 3, this is replaced (below) by csv.reader, which handles unicode.
     """
 
     def __init__(self, f, dialect=csv.excel, encoding="utf-8", **kwds):
@@ -867,3 +869,6 @@ class UnicodeReader:
     def __iter__(self):
         return self
 
+if py3compat.PY3:
+    UnicodeReader = csv.reader
+
