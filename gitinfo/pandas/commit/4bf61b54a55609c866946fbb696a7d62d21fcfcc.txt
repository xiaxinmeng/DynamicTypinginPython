commit 4bf61b54a55609c866946fbb696a7d62d21fcfcc
Author: Thomas Kluyver <takowl@gmail.com>
Date:   Mon Apr 23 22:13:44 2012 +0100

    Use next() builtin rather than method.

diff --git a/pandas/core/common.py b/pandas/core/common.py
index 838b22dc9..b705ab569 100644
--- a/pandas/core/common.py
+++ b/pandas/core/common.py
@@ -519,7 +519,7 @@ def iterpairs(seq):
     # input may not be sliceable
     seq_it = iter(seq)
     seq_it_next = iter(seq)
-    _ = seq_it_next.next()
+    _ = next(seq_it_next)
 
     return itertools.izip(seq_it, seq_it_next)
 
diff --git a/pandas/io/parsers.py b/pandas/io/parsers.py
index 8c12861df..c9b3eb4b5 100644
--- a/pandas/io/parsers.py
+++ b/pandas/io/parsers.py
@@ -489,9 +489,9 @@ class TextParser(object):
                 raise StopIteration
         else:
             while self.pos in self.skiprows:
-                self.data.next()
+                next(self.data)
                 self.pos += 1
-            line = self.data.next()
+            line = next(self.data)
         self.pos += 1
         self.buf.append(line)
 
@@ -682,10 +682,10 @@ class TextParser(object):
             try:
                 if rows is not None:
                     for _ in xrange(rows):
-                        lines.append(source.next())
+                        lines.append(next(source))
                 else:
                     while True:
-                        lines.append(source.next())
+                        lines.append(next(source))
             except StopIteration:
                 if len(lines) == 0:
                     raise
@@ -757,10 +757,13 @@ class FixedWidthReader(object):
             assert isinstance(colspec[1], int)
 
     def next(self):
-        line = self.f.next()
+        line = next(self.f)
         # Note: 'colspecs' is a sequence of half-open intervals.
         return [line[fromm:to].strip(self.filler or ' ')
                 for (fromm, to) in self.colspecs]
+    
+    # Iterator protocol in Python 3 uses __next__()
+    __next__ = next
 
 
 class FixedWidthFieldParser(TextParser):
diff --git a/setup.py b/setup.py
index 3d9e53487..190ea2399 100755
--- a/setup.py
+++ b/setup.py
@@ -40,6 +40,8 @@ if sys.version_info[0] >= 3:
                          'zip_safe': False,
                          'install_requires': ['python-dateutil >= 2',
                                               'numpy >= 1.4'],
+                         'use_2to3_exclude_fixers': ['lib2to3.fixes.fix_next',
+                                                    ],
                         }
     if not _have_setuptools:
         sys.exit("need setuptools/distribute for Py3k"
