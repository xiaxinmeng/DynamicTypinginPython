commit 96cc68ae8a758a24072815e334a1148980d76780
Author: y-p <yoval@gmx.com>
Date:   Sat Dec 15 10:18:32 2012 +0200

    CLN: remove argument and deprecation warning from internal function

diff --git a/pandas/core/format.py b/pandas/core/format.py
index 82c8ce99f..e185049b8 100644
--- a/pandas/core/format.py
+++ b/pandas/core/format.py
@@ -233,14 +233,10 @@ class DataFrameFormatter(TableFormatter):
         else:
             self.columns = frame.columns
 
-    def _to_str_columns(self, force_unicode=None):
+    def _to_str_columns(self):
         """
         Render a DataFrame to a list of columns (as lists of strings).
         """
-        import warnings
-        if force_unicode is not None:  # pragma: no cover
-            warnings.warn("force_unicode is deprecated, it will have no effect",
-                          FutureWarning)
 
         # may include levels names also
         str_index = self._get_formatted_index()
