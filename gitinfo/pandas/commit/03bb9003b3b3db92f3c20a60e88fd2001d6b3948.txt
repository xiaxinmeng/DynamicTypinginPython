commit 03bb9003b3b3db92f3c20a60e88fd2001d6b3948
Author: Jeff Reback <jeff@reback.net>
Date:   Tue Feb 14 20:44:44 2017 -0500

    TST: incorrect skip in when --skip-network is run
    
    closes #15407

diff --git a/pandas/conftest.py b/pandas/conftest.py
index b3683de3a..623feb99e 100644
--- a/pandas/conftest.py
+++ b/pandas/conftest.py
@@ -17,5 +17,5 @@ def pytest_runtest_setup(item):
     if 'slow' not in item.keywords and item.config.getoption("--only-slow"):
         pytest.skip("skipping due to --only-slow")
 
-    if 'skip' in item.keywords and item.config.getoption("--skip-network"):
+    if 'network' in item.keywords and item.config.getoption("--skip-network"):
         pytest.skip("skipping due to --skip-network")
