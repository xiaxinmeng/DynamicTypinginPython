commit ad1bd4481fb9ad69df8c07a0b38b46c0987f98e2
Author: y-p <yoval@gmx.com>
Date:   Thu Nov 21 14:35:29 2013 +0200

    ENH: short-circuit config lookup when exact key given GH5147

diff --git a/pandas/core/config.py b/pandas/core/config.py
index ac48232ec..bb591947d 100644
--- a/pandas/core/config.py
+++ b/pandas/core/config.py
@@ -512,6 +512,11 @@ def _select_options(pat):
     if pat=="all", returns all registered options
     """
 
+    # short-circuit for exact key
+    if pat in _registered_options:
+        return [pat]
+
+    # else look through all of them
     keys = sorted(_registered_options.keys())
     if pat == 'all':  # reserved key
         return keys
