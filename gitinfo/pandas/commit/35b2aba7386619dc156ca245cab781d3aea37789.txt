commit 35b2aba7386619dc156ca245cab781d3aea37789
Author: Paul Reidy <paul_reidy@outlook.com>
Date:   Fri Jan 5 00:53:19 2018 +0000

    DOC: Add information on kind parameter to resample docstring (#19084)

diff --git a/pandas/core/generic.py b/pandas/core/generic.py
index 1ab7c50d8..2b5e4b912 100644
--- a/pandas/core/generic.py
+++ b/pandas/core/generic.py
@@ -5724,6 +5724,10 @@ class NDFrame(PandasObject, SelectionMixin):
         convention : {'start', 'end', 's', 'e'}
             For PeriodIndex only, controls whether to use the start or end of
             `rule`
+        kind: {'timestamp', 'period'}, optional
+            Pass 'timestamp' to convert the resulting index to a
+            ``DateTimeIndex`` or 'period' to convert it to a ``PeriodIndex``.
+            By default the input representation is retained.
         loffset : timedelta
             Adjust the resampled time labels
         base : int, default 0
