commit 0eee324c2684693edae2b0a5ebad3c1619128db4
Author: Rohit Sanjay <sanjay.rohit2@gmail.com>
Date:   Sun Oct 13 22:14:42 2019 +0530

    TST: add test_series_any_timedelta for GH17667 (#28942)

diff --git a/pandas/tests/test_multilevel.py b/pandas/tests/test_multilevel.py
index b9a33d130..e641d6f84 100644
--- a/pandas/tests/test_multilevel.py
+++ b/pandas/tests/test_multilevel.py
@@ -1065,6 +1065,23 @@ Thur,Lunch,Yes,51.51,17"""
         ex = DataFrame({"data": [False, False]}, index=["one", "two"])
         tm.assert_frame_equal(result, ex)
 
+    def test_series_any_timedelta(self):
+        # GH 17667
+        df = DataFrame(
+            {
+                "a": Series([0, 0]),
+                "t": Series([pd.to_timedelta(0, "s"), pd.to_timedelta(1, "ms")]),
+            }
+        )
+
+        result = df.any(axis=0)
+        expected = Series(data=[False, True], index=["a", "t"])
+        tm.assert_series_equal(result, expected)
+
+        result = df.any(axis=1)
+        expected = Series(data=[False, True])
+        tm.assert_series_equal(result, expected)
+
     def test_std_var_pass_ddof(self):
         index = MultiIndex.from_arrays(
             [np.arange(5).repeat(10), np.tile(np.arange(10), 5)]
