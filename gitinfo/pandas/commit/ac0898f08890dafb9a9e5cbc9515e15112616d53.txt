commit ac0898f08890dafb9a9e5cbc9515e15112616d53
Author: y-p <yoval@gmx.com>
Date:   Sun Nov 11 18:45:28 2012 +0200

    TST: series tidy_repr with unicode data values

diff --git a/pandas/tests/test_series.py b/pandas/tests/test_series.py
index a906489e6..db0a37978 100644
--- a/pandas/tests/test_series.py
+++ b/pandas/tests/test_series.py
@@ -1043,6 +1043,11 @@ class TestSeries(unittest.TestCase, CheckNameIntegration):
         rep_str = repr(ser)
         self.assert_("Name: 0" in rep_str)
 
+    def test_tidy_repr(self):
+        a=Series([u"\u05d0"]*1000)
+        a.name= 'title1'
+        repr(a)         # should not raise exception
+
     def test_repr_bool_fails(self):
         s = Series([DataFrame(np.random.randn(2,2)) for i in range(5)])
 
