commit a8380f80f67783f0f36dbc1bba723d71448f6f45
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Mon Dec 12 20:59:23 2011 -0500

    BUG: handling of single-grouping keys in general apply

diff --git a/pandas/core/groupby.py b/pandas/core/groupby.py
index 897e9a166..ecbd78c94 100644
--- a/pandas/core/groupby.py
+++ b/pandas/core/groupby.py
@@ -1225,7 +1225,12 @@ def _make_concat_multiindex(indexes, keys, groupings):
     # construct labels
     labels = []
 
-    for hlevel, ping in zip(zip(*keys), groupings):
+    if len(groupings) == 1:
+        zipped = [keys]
+    else:
+        zipped = zip(*keys)
+
+    for hlevel, ping in zip(zipped, groupings):
         get_id = ping.reverse_ids.__getitem__
         mapped = [get_id(x) for x in hlevel]
         labels.append(np.repeat(mapped, n))
diff --git a/pandas/tests/test_groupby.py b/pandas/tests/test_groupby.py
index c334af977..2a3fd57e8 100644
--- a/pandas/tests/test_groupby.py
+++ b/pandas/tests/test_groupby.py
@@ -139,6 +139,10 @@ class TestGroupBy(unittest.TestCase):
         assert_frame_equal(grouped.agg(np.sum), DataFrame({}))
         assert_frame_equal(grouped.apply(np.sum), DataFrame({}))
 
+    def test_apply_describe_bug(self):
+        grouped = self.mframe.groupby(level='first')
+        result = grouped.describe() # it works!
+
     def test_len(self):
         df = tm.makeTimeDataFrame()
         grouped = df.groupby([lambda x: x.year,
