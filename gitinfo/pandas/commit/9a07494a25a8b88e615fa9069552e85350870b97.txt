commit 9a07494a25a8b88e615fa9069552e85350870b97
Author: Chang She <chang@lambdafoundry.com>
Date:   Wed May 23 19:11:34 2012 -0400

    DOC: fixed bug in auto_dev_build in docs make.py

diff --git a/doc/make.py b/doc/make.py
index 5568e4a5b..2d07af91c 100755
--- a/doc/make.py
+++ b/doc/make.py
@@ -105,7 +105,7 @@ def auto_dev_build(debug=False):
         html()
         step = 'upload dev'
         upload_dev()
-        if not debug():
+        if not debug:
             sendmail(step)
 
         step = 'latex'
@@ -115,8 +115,8 @@ def auto_dev_build(debug=False):
         if not debug:
             sendmail(step)
     except (Exception, SystemExit), inst:
-        msg += str(inst) + '\n'
-        sendmail(step, msg)
+        msg = str(inst) + '\n'
+        sendmail(step, '[ERROR] ' + msg)
 
 def sendmail(step=None, err_msg=None):
     from_name, to_name = _get_config()
