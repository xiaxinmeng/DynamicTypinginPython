commit d9897f39300c9579c7b9a6a31bbfc9bfaaea33bf
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Wed Jun 6 21:17:27 2012 -0400

    BUG: preserve name in PeriodIndex.to_timestamp

diff --git a/pandas/tseries/period.py b/pandas/tseries/period.py
index 08953225a..bd54ad550 100644
--- a/pandas/tseries/period.py
+++ b/pandas/tseries/period.py
@@ -781,7 +781,7 @@ class PeriodIndex(Int64Index):
             raise ValueError('Only mult == 1 supported')
 
         new_data = plib.periodarr_to_dt64arr(new_data.values, base)
-        return DatetimeIndex(new_data, freq='infer')
+        return DatetimeIndex(new_data, freq='infer', name=self.name)
 
     def shift(self, n):
         """
diff --git a/pandas/tseries/tests/test_period.py b/pandas/tseries/tests/test_period.py
index 0bfe60cc5..875058a92 100644
--- a/pandas/tseries/tests/test_period.py
+++ b/pandas/tseries/tests/test_period.py
@@ -986,6 +986,14 @@ class TestPeriodIndex(TestCase):
         expected = DatetimeIndex([x.to_timestamp('D', 'end') for x in pindex])
         self.assert_(stamps.equals(expected))
 
+    def test_to_timestamp_preserve_name(self):
+        index = PeriodIndex(freq='A', start='1/1/2001', end='12/1/2009',
+                            name='foo')
+        self.assertEquals(index.name, 'foo')
+
+        conv = index.to_timestamp('D')
+        self.assertEquals(conv.name, 'foo')
+
     def test_as_frame_columns(self):
         rng = period_range('1/1/2000', periods=5)
         df = DataFrame(randn(10, 5), columns=rng)
