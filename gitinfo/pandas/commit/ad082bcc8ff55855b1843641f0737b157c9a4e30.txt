commit ad082bcc8ff55855b1843641f0737b157c9a4e30
Author: y-p <yoval@gmx.com>
Date:   Tue Mar 19 18:33:24 2013 +0200

    TST: fix test to test dupe columns, instead of null op

diff --git a/pandas/tests/test_frame.py b/pandas/tests/test_frame.py
index 1247325e8..b8d7284c6 100644
--- a/pandas/tests/test_frame.py
+++ b/pandas/tests/test_frame.py
@@ -4544,14 +4544,13 @@ class TestDataFrame(unittest.TestCase, CheckIndexing,
         for nrows in [10,N-2,N-1,N,N+1,N+2]:
             df = mkdf(nrows, 10)
             cols = list(df.columns)
-            cols[:1] = ["dupe","dupe"]
-            cols[-1:] = ["dupe","dupe"]
+            cols[:2] = ["dupe","dupe"]
+            cols[-2:] = ["dupe","dupe"]
             ix = list(df.index)
             ix[:2] = ["rdupe","rdupe"]
             ix[-2:] = ["rdupe","rdupe"]
-            print( nrows)
-
             df.index=ix
+            df.columns=cols
             _do_test(df,path)
 
         for r_idx_type in ['i', 'f','s','u','dt']:
