commit 814c51caf2fcd377f7b9e17c7f5e19fcb1be201d
Author: jbrockmendel <jbrockmendel@gmail.com>
Date:   Wed Oct 23 04:46:41 2019 -0700

    Stop catching TypeError in _aggregate_item_by_item (#29177)

diff --git a/pandas/core/groupby/generic.py b/pandas/core/groupby/generic.py
index 9c9598273..c766fcaa4 100644
--- a/pandas/core/groupby/generic.py
+++ b/pandas/core/groupby/generic.py
@@ -1099,10 +1099,7 @@ class DataFrameGroupBy(GroupBy):
 
             cast = self._transform_should_cast(func)
             try:
-
                 result[item] = colg.aggregate(func, *args, **kwargs)
-                if cast:
-                    result[item] = self._try_cast(result[item], data)
 
             except ValueError as err:
                 if "Must produce aggregated value" in str(err):
@@ -1111,10 +1108,10 @@ class DataFrameGroupBy(GroupBy):
                     raise
                 cannot_agg.append(item)
                 continue
-            except TypeError as e:
-                cannot_agg.append(item)
-                errors = e
-                continue
+
+            else:
+                if cast:
+                    result[item] = self._try_cast(result[item], data)
 
         result_columns = obj.columns
         if cannot_agg:
