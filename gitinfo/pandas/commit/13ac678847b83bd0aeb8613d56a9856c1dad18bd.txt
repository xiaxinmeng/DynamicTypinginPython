commit 13ac678847b83bd0aeb8613d56a9856c1dad18bd
Author: Chang She <changshe@gmail.com>
Date:   Sun Nov 13 10:32:44 2011 -0500

    Test case for Series.cov

diff --git a/pandas/tests/test_series.py b/pandas/tests/test_series.py
index a19a318a1..36181abf4 100644
--- a/pandas/tests/test_series.py
+++ b/pandas/tests/test_series.py
@@ -833,6 +833,16 @@ class TestSeries(unittest.TestCase, CheckNameIntegration):
 
         # additional checks?
 
+    def test_cov(self):
+        # full overlap
+        self.assertAlmostEqual(self.ts.cov(self.ts), self.ts.std()**2)
+
+        # partial overlap
+        self.assertAlmostEqual(self.ts[:15].cov(self.ts[5:]), self.ts[5:15].std()**2)
+
+        # No overlap
+        self.assert_(np.isnan(self.ts[::2].cov(self.ts[1::2])))
+
     def test_copy(self):
         ts = self.ts.copy()
 
