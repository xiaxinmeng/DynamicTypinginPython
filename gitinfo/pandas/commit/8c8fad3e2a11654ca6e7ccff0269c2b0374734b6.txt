commit 8c8fad3e2a11654ca6e7ccff0269c2b0374734b6
Author: Chang She <chang@lambdafoundry.com>
Date:   Wed May 2 19:24:22 2012 -0400

    BUG: overriding ptp in Series to work with np.ptp. GH #1166

diff --git a/pandas/core/series.py b/pandas/core/series.py
index ea321e0ab..c6baa753e 100644
--- a/pandas/core/series.py
+++ b/pandas/core/series.py
@@ -1269,6 +1269,9 @@ copy : boolean, default False
             return np.nan
         return _quantile(valid_values, q * 100)
 
+    def ptp(self, axis=None, out=None):
+        return self.values.ptp(axis, out)
+
     def describe(self, percentile_width=50):
         """
         Generate various summary statistics of Series, excluding NaN
diff --git a/pandas/tests/test_series.py b/pandas/tests/test_series.py
index d151db187..7a63c0114 100644
--- a/pandas/tests/test_series.py
+++ b/pandas/tests/test_series.py
@@ -1922,6 +1922,12 @@ class TestSeries(unittest.TestCase, CheckNameIntegration):
                           before=self.ts.index[-1] + offset,
                           after=self.ts.index[0] - offset)
 
+    def test_ptp(self):
+        N = 1000
+        arr = np.random.randn(N)
+        ser = Series(arr)
+        self.assertEqual(np.ptp(ser), np.ptp(arr))
+
     def test_asof(self):
         # array or list or dates
         N = 50
