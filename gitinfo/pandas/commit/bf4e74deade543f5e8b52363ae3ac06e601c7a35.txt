commit bf4e74deade543f5e8b52363ae3ac06e601c7a35
Author: jbrockmendel <jbrockmendel@gmail.com>
Date:   Sun Feb 9 14:17:19 2020 -0800

    CLN: Use self.loc for Series __getitem__ with IntervalIndex (#31830)

diff --git a/pandas/core/series.py b/pandas/core/series.py
index 75ac48fa8..fbf16d7a3 100644
--- a/pandas/core/series.py
+++ b/pandas/core/series.py
@@ -916,11 +916,12 @@ class Series(base.IndexOpsMixin, generic.NDFrame):
         # Note: The key_type == "boolean" case should be caught by the
         #  com.is_bool_indexer check in __getitem__
         if key_type == "integer":
+            # We need to decide whether to treat this as a positional indexer
+            #  (i.e. self.iloc) or label-based (i.e. self.loc)
             if self.index.is_integer() or self.index.is_floating():
                 return self.loc[key]
             elif isinstance(self.index, IntervalIndex):
-                indexer = self.index.get_indexer_for(key)
-                return self.iloc[indexer]
+                return self.loc[key]
             else:
                 return self.iloc[key]
 
