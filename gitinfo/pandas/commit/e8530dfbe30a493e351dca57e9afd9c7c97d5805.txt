commit e8530dfbe30a493e351dca57e9afd9c7c97d5805
Author: Andy Hayden <andyhayden1@gmail.com>
Date:   Sun Aug 25 20:39:00 2013 +0100

    TST another groupby as_index test

diff --git a/pandas/tests/test_groupby.py b/pandas/tests/test_groupby.py
index a11d53921..4bd44fcf2 100644
--- a/pandas/tests/test_groupby.py
+++ b/pandas/tests/test_groupby.py
@@ -1202,6 +1202,10 @@ class TestGroupBy(unittest.TestCase):
         res_not_as = g_not_as.apply(lambda x: x.head(2)).index
         assert_index_equal(res_not_as, exp_not_as)
 
+        ind = Index(list('abcde'))
+        df = DataFrame([[1, 2], [2, 3], [1, 4], [1, 5], [2, 6]], index=ind)
+        res = df.groupby(0, as_index=False).apply(lambda x: x).index
+        assert_index_equal(res, ind)
 
     def test_groupby_multiple_key(self):
         df = tm.makeTimeDataFrame()
