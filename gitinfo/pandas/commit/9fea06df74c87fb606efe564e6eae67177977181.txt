commit 9fea06df74c87fb606efe564e6eae67177977181
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Thu Feb 9 15:34:49 2012 -0500

    BUG: actually pass encoding to UnicodeWriter

diff --git a/RELEASE.rst b/RELEASE.rst
index 93a85f8c8..d764c904b 100644
--- a/RELEASE.rst
+++ b/RELEASE.rst
@@ -301,6 +301,8 @@ pandas 0.7.0
   - Allow subclasses of dicts in DataFrame constructor, with tests
   - Fix problem whereby set_index destroys column multiindex (GH #764)
   - Hack around bug in generating DateRange from naive DateOffset (GH #770)
+  - Fix bug in DateRange.intersection causing incorrect results with some
+    overlapping ranges (GH #771)
 
 Thanks
 ------
diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index e7aba5f74..a855b24b5 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -843,7 +843,7 @@ class DataFrame(NDFrame):
     to_wide = deprecate('to_wide', to_panel)
 
     def _helper_csvexcel(self, writer, na_rep=None, cols=None, header=True,
-                         index=True, index_label=None, encoding=None):
+                         index=True, index_label=None):
         if cols is None:
             cols = self.columns
 
@@ -938,12 +938,12 @@ class DataFrame(NDFrame):
         try:
             if encoding is not None:
                 csvout = com.UnicodeWriter(f, lineterminator='\n',
-                                           delimiter=sep)
+                                           delimiter=sep, encoding=encoding)
             else:
                 csvout = csv.writer(f, lineterminator='\n', delimiter=sep)
             self._helper_csvexcel(csvout, na_rep=na_rep, cols=cols,
                                   header=header, index=index,
-                                  index_label=index_label, encoding=encoding)
+                                  index_label=index_label)
         finally:
             if close:
                 f.close()
@@ -990,7 +990,7 @@ class DataFrame(NDFrame):
         excel_writer.cur_sheet = sheet_name
         self._helper_csvexcel(excel_writer, na_rep=na_rep, cols=cols,
                               header=header, index=index,
-                              index_label=index_label, encoding=None)
+                              index_label=index_label)
         if need_save:
             excel_writer.save()
 
