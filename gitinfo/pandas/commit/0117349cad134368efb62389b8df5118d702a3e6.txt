commit 0117349cad134368efb62389b8df5118d702a3e6
Author: Thomas Kluyver <takowl@gmail.com>
Date:   Sun Oct 16 18:57:11 2011 +0100

    Add tests for .min and .max on dataframes with integer columns.

diff --git a/pandas/tests/test_frame.py b/pandas/tests/test_frame.py
index 84576c173..85d2aa432 100644
--- a/pandas/tests/test_frame.py
+++ b/pandas/tests/test_frame.py
@@ -2609,9 +2609,11 @@ class TestDataFrame(unittest.TestCase, CheckIndexing,
 
     def test_min(self):
         self._check_stat_op('min', np.min)
+        self._check_stat_op('min', np.min, frame=self.intframe)
 
     def test_max(self):
         self._check_stat_op('max', np.max)
+        self._check_stat_op('max', np.max, frame=self.intframe)
 
     def test_mad(self):
         f = lambda x: np.abs(x - x.mean()).mean()
