commit 930a26779165628f54a788b996142808c218a204
Author: y-p <yoval@gmx.com>
Date:   Sat Nov 10 01:25:39 2012 +0200

    BUG: coerce ndarray dtype to object when comparing series
    
    fixes #1926 (partialy at least)

diff --git a/pandas/core/series.py b/pandas/core/series.py
index 4194cbd4e..b6e144851 100644
--- a/pandas/core/series.py
+++ b/pandas/core/series.py
@@ -110,7 +110,10 @@ def _comp_method(op, name):
                 y = lib.list_to_object_array(y)
 
             if isinstance(y, np.ndarray):
-                result = lib.vec_compare(x, y, op)
+                if y.dtype != np.object_:
+                    result = lib.vec_compare(x, y.astype(np.object_), op)
+                else:
+                    result = lib.vec_compare(x, y, op)
             else:
                 result = lib.scalar_compare(x, y, op)
         else:
