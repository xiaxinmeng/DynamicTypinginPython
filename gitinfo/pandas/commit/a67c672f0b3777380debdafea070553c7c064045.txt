commit a67c672f0b3777380debdafea070553c7c064045
Author: jreback <jeff@reback.net>
Date:   Wed Apr 30 07:40:48 2014 -0400

    BLD: travis changes forced system site-packages now unavailable
    BLD: fix nose version at 1.3.0 to avoid py3 issues

diff --git a/.travis.yml b/.travis.yml
index cb8376a9f..5eef300d0 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -1,3 +1,4 @@
+
 language: python
 
 env:
@@ -61,12 +62,6 @@ matrix:
         - NUMPY_BUILD=master
         - PANDAS_TESTING_MODE="numpy_deprecate"
 
-# allow importing from site-packages,
-# so apt-get python-x works for system pythons
-# That's 2.7/3.2 on Ubuntu 12.04
-virtualenv:
-  system_site_packages: true
-
 before_install:
   - echo "before_install"
   - echo $VIRTUAL_ENV
diff --git a/ci/install.sh b/ci/install.sh
index 765794fcf..e6829750b 100755
--- a/ci/install.sh
+++ b/ci/install.sh
@@ -35,6 +35,7 @@ python_major_version="${TRAVIS_PYTHON_VERSION:0:1}"
 pip install -I pip==1.5.1
 pip install -I setuptools==2.2
 pip install wheel==0.22
+pip install nose==1.3.0
 
 # comment this line to disable the fetching of wheel files
 base_url=http://pandas.pydata.org/pandas-build/dev/wheels
diff --git a/pandas/util/print_versions.py b/pandas/util/print_versions.py
index 1c1c2e322..5e2230ca5 100644
--- a/pandas/util/print_versions.py
+++ b/pandas/util/print_versions.py
@@ -60,6 +60,7 @@ def show_versions(as_json=False):
     deps = [
         # (MODULE_NAME, f(mod) -> mod version)
         ("pandas", lambda mod: mod.__version__),
+        ("nose", lambda mod: mod.__version__),
         ("Cython", lambda mod: mod.__version__),
         ("numpy", lambda mod: mod.version.version),
         ("scipy", lambda mod: mod.version.version),
