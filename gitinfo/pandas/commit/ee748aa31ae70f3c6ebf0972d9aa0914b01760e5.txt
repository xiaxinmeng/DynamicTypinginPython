commit ee748aa31ae70f3c6ebf0972d9aa0914b01760e5
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Wed Jun 22 22:04:23 2011 -0400

    DataFrame fancy indexing gives view

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index 0ff0b450d..3231f4d4a 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -2512,15 +2512,23 @@ class DataFrame(PandasGeneric):
             return self
 
         axis_name = self._get_axis_name(axis)
-
         labels = getattr(self, axis_name)
         if _is_label_slice(labels, slice_obj):
             i, j = labels.slice_locs(slice_obj.start, slice_obj.stop)
-            new_labels = labels[i:j]
+            slicer = slice(i, j)
+        else:
+            slicer = slice_obj
+
+        if axis == 0:
+            new_index = self.index[slicer]
+            new_columns = self.columns
+            new_values = self.values[slicer]
         else:
-            new_labels = labels[slice_obj]
+            new_index = self.index
+            new_columns = self.columns[slicer]
+            new_values = self.values[:, slicer]
 
-        return self.reindex(**{axis_name : new_labels})
+        return DataFrame(new_values, index=new_index, columns=new_columns)
 
 class _DataFrameIndexer(object):
     """
diff --git a/pandas/core/tests/test_frame.py b/pandas/core/tests/test_frame.py
index 443602ce6..412a4926f 100644
--- a/pandas/core/tests/test_frame.py
+++ b/pandas/core/tests/test_frame.py
@@ -195,6 +195,12 @@ class CheckIndexing(object):
         # slice columns
         assert_frame_equal(ix[:, :2], f.reindex(columns=['A', 'B']))
 
+        # get view
+        exp = f.copy()
+        ix[5:10].values[:] = 5
+        exp.values[5:10] = 5
+        assert_frame_equal(f, exp)
+
     def test_getitem_fancy_1d(self):
         f = self.frame
         ix = f.ix
