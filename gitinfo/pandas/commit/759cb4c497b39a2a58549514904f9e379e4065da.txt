commit 759cb4c497b39a2a58549514904f9e379e4065da
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Thu Jul 14 13:27:22 2011 -0400

    pickle with highest protocol, doesn't work in python 2.6 without

diff --git a/pandas/tests/test_sparse.py b/pandas/tests/test_sparse.py
index fe3353c33..23fc7b874 100644
--- a/pandas/tests/test_sparse.py
+++ b/pandas/tests/test_sparse.py
@@ -246,7 +246,7 @@ class TestSparseSeries(TestCase):
 
     def test_pickle(self):
         def _test_roundtrip(series):
-            pickled = pickle.dumps(series)
+            pickled = pickle.dumps(series, protocol=pickle.HIGHEST_PROTOCOL)
             unpickled = pickle.loads(pickled)
             assert_sp_series_equal(series, unpickled)
             assert_series_equal(series.to_dense(), unpickled.to_dense())
