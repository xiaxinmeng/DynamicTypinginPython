commit 8ec6236b87a7106fe81e8fd23598375477b7574e
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Mon Mar 12 23:47:29 2012 -0400

    TST: unit test for #893

diff --git a/pandas/tests/test_frame.py b/pandas/tests/test_frame.py
index 073c4d323..8f8e5e370 100644
--- a/pandas/tests/test_frame.py
+++ b/pandas/tests/test_frame.py
@@ -1784,6 +1784,14 @@ class TestDataFrame(unittest.TestCase, CheckIndexing,
         self.assertEqual(len(records.dtype.names), 2)
         self.assert_('index' not in records.dtype.names)
 
+    def test_from_records_nones(self):
+        tuples = [(1, 2, None, 3),
+                  (1, 2, None, 3),
+                  (None, 2, 5, 3)]
+
+        df = DataFrame.from_records(tuples, columns=['a', 'b', 'c', 'd'])
+        self.assert_(np.isnan(df['c'][0]))
+
     def test_to_records_floats(self):
         df = DataFrame(np.random.rand(10,10))
         df.to_records()
