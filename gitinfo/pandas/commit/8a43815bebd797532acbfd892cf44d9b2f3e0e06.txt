commit 8a43815bebd797532acbfd892cf44d9b2f3e0e06
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Sat Nov 24 23:58:09 2012 -0500

    BUG: Series footer formatting when name field is not 'True'

diff --git a/pandas/core/format.py b/pandas/core/format.py
index 13e504a8e..4230f3c19 100644
--- a/pandas/core/format.py
+++ b/pandas/core/format.py
@@ -79,7 +79,7 @@ class SeriesFormatter(object):
             if getattr(self.series.index, 'freq', None):
                 footer += 'Freq: %s' % self.series.index.freqstr
 
-            if footer and self.series.name:
+            if footer and self.series.name is not None:
                 footer += ', '
 
             series_name = com.pprint_thing(self.series.name)
diff --git a/pandas/tests/test_format.py b/pandas/tests/test_format.py
index 10bb75bfb..542e5ee96 100644
--- a/pandas/tests/test_format.py
+++ b/pandas/tests/test_format.py
@@ -838,6 +838,13 @@ class TestSeriesFormatting(unittest.TestCase):
         last_line = result.split('\n')[-1].strip()
         self.assertEqual(last_line, "Freq: B, Name: foo, Length: %d" % len(cp))
 
+    def test_freq_name_separation(self):
+        s = Series(np.random.randn(10),
+                   index=pd.date_range('1/1/2000', periods=10), name=0)
+
+        result = repr(s)
+        self.assertTrue('Freq: D, Name: 0' in result)
+
     def test_to_string_mixed(self):
         s = Series(['foo', np.nan, -1.23, 4.56])
         result = s.to_string()
