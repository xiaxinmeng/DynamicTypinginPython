commit 4fb14f9567f00c215c7a456ed989088e85cb4747
Author: Adam Klein <adamklein@gmail.com>
Date:   Sat Feb 4 18:08:29 2012 -0500

    BUG: fixed test for #742

diff --git a/pandas/tests/test_frame.py b/pandas/tests/test_frame.py
index f4a76da75..bacfddf1c 100644
--- a/pandas/tests/test_frame.py
+++ b/pandas/tests/test_frame.py
@@ -4175,15 +4175,16 @@ class TestDataFrame(unittest.TestCase, CheckIndexing,
         expected = DataFrame([[2.0, 3.0, 1.0], [1, 3, 2]])
         assert_frame_equal(result, expected)
 
-        df = DataFrame([['b',None,'a'],['a','c','b']])
-        result = df.rank(1)
-        expected = DataFrame([[2.0, 1.5, 1.0], [1, 1.5, 2]])
+        df = DataFrame([['b', None,'a'],['a','c','b']])
+        result = df.rank(0)
+        expected = DataFrame([[3.0, 1.0, 2.0], [1.0, 3.0, 2.0]])
         assert_frame_equal(result, expected)
 
-        result = df.rank(0)
-        expected = DataFrame([[2.0, 3.0, 1.0], [1, 3, 2]])
+        result = df.rank(1)
+        expected = DataFrame([[2.0, 1.0, 1.0], [1.0, 2.0, 2.0]])
         assert_frame_equal(result, expected)
 
+
     def test_describe(self):
         desc = self.tsframe.describe()
         desc = self.mixed_frame.describe()
