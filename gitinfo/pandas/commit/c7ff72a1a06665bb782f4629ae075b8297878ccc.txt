commit c7ff72a1a06665bb782f4629ae075b8297878ccc
Author: Viktor Kerkez <alefnula@gmail.com>
Date:   Sat Sep 28 23:37:27 2013 +0200

    API: Enable changing the DataFrame in the DataFrameWidget.

diff --git a/pandas/sandbox/qtpandas.py b/pandas/sandbox/qtpandas.py
index 86a5478da..3f284990e 100644
--- a/pandas/sandbox/qtpandas.py
+++ b/pandas/sandbox/qtpandas.py
@@ -88,17 +88,18 @@ class DataFrameWidget(QWidget):
         super(DataFrameWidget, self).__init__(parent)
 
         self.dataModel = DataFrameModel()
-        self.dataModel.setDataFrame(dataFrame)
-
         self.dataTable = QTableView()
         self.dataTable.setModel(self.dataModel)
-        self.dataModel.signalUpdate()
 
         layout = QVBoxLayout()
         layout.addWidget(self.dataTable)
         self.setLayout(layout)
+        # Set DataFrame
+        self.setDataFrame(dataFrame)
 
-    def resizeColumnsToContents(self):
+    def setDataFrame(self, dataFrame):
+        self.dataModel.setDataFrame(dataFrame)
+        self.dataModel.signalUpdate()
         self.dataTable.resizeColumnsToContents()
 
 #-----------------stand alone test code
