commit 497024896108d3f9fafdf1d0ea4f4bd95bcaef56
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Tue Dec 11 13:04:58 2012 -0500

    BUG: fix long column wide repr formatting bug (final columns omitted)

diff --git a/pandas/core/format.py b/pandas/core/format.py
index 152529716..2a00a96a7 100644
--- a/pandas/core/format.py
+++ b/pandas/core/format.py
@@ -1439,8 +1439,8 @@ def _binify(cols, width):
         if curr_width + 2 > width:
             bins.append(i)
             curr_width = w
-        elif i + 1== len(cols):
-            bins.append(i + 1)
+
+    bins.append(i + 1)
     return bins
 
 if __name__ == '__main__':
diff --git a/pandas/tests/test_format.py b/pandas/tests/test_format.py
index b0b311179..5082aa6c1 100644
--- a/pandas/tests/test_format.py
+++ b/pandas/tests/test_format.py
@@ -518,6 +518,16 @@ class TestDataFrameFormatting(unittest.TestCase):
         set_option('test.interactive', False)
         set_option('print.line_width', 80)
 
+    def test_wide_repr_wide_long_columns(self):
+        set_option('test.interactive', True)
+
+        df = DataFrame({'a': ['a'*30, 'b'*30], 'b': ['c'*70, 'd'*80]})
+
+        result = repr(df)
+        self.assertTrue('ccccc' in result)
+        self.assertTrue('ddddd' in result)
+        set_option('test.interactive', False)
+
     def test_to_string(self):
         from pandas import read_table
         import re
