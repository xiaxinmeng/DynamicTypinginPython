commit 77a466427b62719d1c555c37c1288831109f17e5
Author: Adam Klein <adamklein@gmail.com>
Date:   Tue Feb 7 16:23:59 2012 -0500

    ENH: reset jenkins-specific change, shouldn't be needed.

diff --git a/setup.py b/setup.py
index 2e8e5ed8b..65cf6528d 100755
--- a/setup.py
+++ b/setup.py
@@ -173,15 +173,7 @@ QUALIFIER = 'rc1'
 
 FULLVERSION = VERSION
 if not ISRELEASED:
-    try:
-        sha = os.environ['GIT_COMMIT']
-        if sha is not None and sha != '':
-            FULLVERSION += sha
-        else:
-            FULLVERSION += '.dev'
-    except KeyError:
-        FULLVERSION += '.dev'
-
+    FULLVERSION += '.dev'
     try:
         import subprocess
         pipe = subprocess.Popen(["git", "rev-parse", "--short", "HEAD"],
