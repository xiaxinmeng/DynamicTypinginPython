commit 793a5c94bddd5fd1dc13e7363d200786123ef90d
Author: y-p <yoval@gmx.com>
Date:   Sat Mar 30 22:03:15 2013 +0300

    TST: PeriodIndex pickle roundtrip does not recreate freq GH2891

diff --git a/pandas/tests/test_series.py b/pandas/tests/test_series.py
index 3ba0a034c..bd9e61ba5 100644
--- a/pandas/tests/test_series.py
+++ b/pandas/tests/test_series.py
@@ -1324,6 +1324,16 @@ class TestSeries(unittest.TestCase, CheckNameIntegration):
         ts2 = ts.ix[np.random.randint(0, len(ts) - 1, 400)]
         repr(ts).splitlines()[-1]
 
+    def test_timeseries_periodindex(self):
+        # GH2891
+        import pickle
+        from pandas import period_range
+        prng = period_range('1/1/2011', '1/1/2012', freq='M')
+        ts = Series(np.random.randn(len(prng)), prng)
+        new_ts = pickle.loads(pickle.dumps(ts))
+        self.assertEqual(new_ts.index.freq,'M')
+
+
     def test_iter(self):
         for i, val in enumerate(self.series):
             self.assertEqual(val, self.series[i])
diff --git a/pandas/tseries/tests/test_period.py b/pandas/tseries/tests/test_period.py
index 3bb9008f7..8bce80493 100644
--- a/pandas/tseries/tests/test_period.py
+++ b/pandas/tseries/tests/test_period.py
@@ -1946,6 +1946,12 @@ class TestPeriodIndex(TestCase):
         self.assert_(isinstance(result.index, PeriodIndex))
         self.assertEquals(result.index[0], s1.index[0])
 
+    def test_pickle_freq(self):
+        # GH2891
+        import pickle
+        prng = period_range('1/1/2011', '1/1/2012', freq='M')
+        new_prng = pickle.loads(pickle.dumps(prng))
+        self.assertEqual(new_prng.freq,'M')
 
 def _permute(obj):
     return obj.take(np.random.permutation(len(obj)))
