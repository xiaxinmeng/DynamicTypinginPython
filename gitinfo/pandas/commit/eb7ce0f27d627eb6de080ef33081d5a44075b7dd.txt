commit eb7ce0f27d627eb6de080ef33081d5a44075b7dd
Author: onesandzeroes <onesandzeroes@github.com>
Date:   Fri Nov 14 12:08:59 2014 +1100

    TST: Skip hist testing for matplotlib <= 1.2
    
    Add skip to the hist test
    
    Add to release notes

diff --git a/doc/source/whatsnew/v0.15.2.txt b/doc/source/whatsnew/v0.15.2.txt
index fe1454d4d..da5aad42b 100644
--- a/doc/source/whatsnew/v0.15.2.txt
+++ b/doc/source/whatsnew/v0.15.2.txt
@@ -63,3 +63,4 @@ Bug Fixes
 - Bug in slicing a multi-index with an empty list and at least one boolean indexer (:issue:`8781`)
 - ``io.data.Options`` now raises ``RemoteDataError`` when no expiry dates are available from Yahoo (:issue:`8761`).
 - ``Timedelta`` kwargs may now be numpy ints and floats (:issue:`8757`).
+- Skip testing of histogram plots for matplotlib <= 1.2 (:issue:`8648`).
diff --git a/pandas/tests/test_graphics.py b/pandas/tests/test_graphics.py
index a5d203d68..7469abdbd 100644
--- a/pandas/tests/test_graphics.py
+++ b/pandas/tests/test_graphics.py
@@ -2041,6 +2041,9 @@ class TestDataFramePlots(TestPlotBase):
 
     @slow
     def test_hist_df(self):
+        if self.mpl_le_1_2_1:
+            raise nose.SkipTest("not supported in matplotlib <= 1.2.x")
+
         df = DataFrame(randn(100, 4))
         series = df[0]
 
