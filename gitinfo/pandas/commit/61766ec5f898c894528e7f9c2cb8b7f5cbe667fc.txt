commit 61766ec5f898c894528e7f9c2cb8b7f5cbe667fc
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Tue Nov 20 16:26:23 2012 -0500

    BUG: fix python3 issues

diff --git a/RELEASE.rst b/RELEASE.rst
index 042ede155..12b8cb4c0 100644
--- a/RELEASE.rst
+++ b/RELEASE.rst
@@ -41,6 +41,7 @@ pandas 0.10.0
 **Improvements to existing features**
 
   - Grouped histogram via `by` keyword in Series/DataFrame.hist (#2186)
+  - Add ``nrows`` option to DataFrame.from_records for iterators (#1794)
 
 **Bug fixes**
 
diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index cd6541a32..f7f296e82 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -911,7 +911,10 @@ class DataFrame(NDFrame):
                 return DataFrame()
 
             try:
-                first_row = data.next()
+                if py3compat.PY3:
+                    first_row = next(data)
+                else:
+                    first_row = data.next()
             except StopIteration:
                 return DataFrame(index=index, columns=columns)
 
diff --git a/pandas/tests/test_strings.py b/pandas/tests/test_strings.py
index 43312a0b1..9972d0b3b 100644
--- a/pandas/tests/test_strings.py
+++ b/pandas/tests/test_strings.py
@@ -710,10 +710,10 @@ class TestStringMethods(unittest.TestCase):
         exp = encodeBase.map(f)
         tm.assert_series_equal(result, exp)
 
-        decodeBase = Series(['a', 'b', 'a\x9d'])
+        decodeBase = Series([b'a', b'b', b'a\x9d'])
 
         self.assertRaises(UnicodeDecodeError,
-                          decodeBase.str.encode, 'cp1252')
+                          decodeBase.str.decode, 'cp1252')
 
         f = lambda x: x.decode('cp1252', 'ignore')
         result = decodeBase.str.decode('cp1252', 'ignore')
