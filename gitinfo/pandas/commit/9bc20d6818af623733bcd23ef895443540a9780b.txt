commit 9bc20d6818af623733bcd23ef895443540a9780b
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Mon Jan 16 00:13:32 2012 -0500

    BUG: fix buglet with xs with level, GH #371

diff --git a/pandas/core/indexing.py b/pandas/core/indexing.py
index ef606919a..e3cb28073 100644
--- a/pandas/core/indexing.py
+++ b/pandas/core/indexing.py
@@ -97,7 +97,7 @@ class _NDFrameIndexer(object):
         if isinstance(self.obj._get_axis(0), MultiIndex):
             try:
                 return self._get_label(tup, axis=0)
-            except (KeyError, TypeError):
+            except Exception:
                 pass
 
         try:
diff --git a/pandas/tests/test_multilevel.py b/pandas/tests/test_multilevel.py
index 530d1b239..3d0a742b0 100644
--- a/pandas/tests/test_multilevel.py
+++ b/pandas/tests/test_multilevel.py
@@ -222,6 +222,14 @@ class TestMultiLevel(unittest.TestCase):
 
         assert_frame_equal(result, expected)
 
+        index = MultiIndex.from_tuples([('x', 'y', 'z'), ('a', 'b', 'c'),
+                                        ('p', 'q', 'r')])
+        df = DataFrame(np.random.randn(3, 5), index=index)
+        result = df.xs('c', level=2)
+        expected = df[1:2]
+        expected.index = expected.index.droplevel(2)
+        assert_frame_equal(result, expected)
+
     def test_xs_level_series(self):
         s = self.frame['A']
         result = s[:, 'two']
