commit 9229a2da148784bbbebc659d6cfafdb5cc7d9045
Author: Chang She <chang@lambdafoundry.com>
Date:   Wed May 2 11:03:23 2012 -0400

    CMP: add __hash__ to make Period hashable in Py3

diff --git a/pandas/tseries/period.py b/pandas/tseries/period.py
index b7c66e22a..8d39555c8 100644
--- a/pandas/tseries/period.py
+++ b/pandas/tseries/period.py
@@ -155,6 +155,9 @@ class Period(object):
                     and _gfc(self.freq) == _gfc(other.freq))
         return False
 
+    def __hash__(self):
+        return hash((self.ordinal, self.freq))
+
     def __add__(self, other):
         if isinstance(other, (int, long)):
             return Period(self.ordinal + other, self.freq)
