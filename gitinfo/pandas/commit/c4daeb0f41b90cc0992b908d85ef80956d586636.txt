commit c4daeb0f41b90cc0992b908d85ef80956d586636
Author: y-p <yoval@gmx.com>
Date:   Thu Dec 13 06:27:30 2012 +0200

    BLD: add numexpr, libhdf-dev to FULL_DEPS build for pytables

diff --git a/ci/install.sh b/ci/install.sh
index f2894ceea..7181609ec 100755
--- a/ci/install.sh
+++ b/ci/install.sh
@@ -38,6 +38,8 @@ if [ x"$FULL_DEPS" == x"true" ]; then
     fi
 
     if [ ${TRAVIS_PYTHON_VERSION:0:1} == "2" ]; then
+        sudo apt-get $APT_ARGS install libhdf5-serial-dev;
+        pip install numexpr
         pip install tables
     fi
 
