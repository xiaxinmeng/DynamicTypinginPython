commit bd2d105fd45e3587e02005f4161c0ab0b0be2a6e
Author: Skipper Seabold <jsseabold@gmail.com>
Date:   Mon May 5 15:47:21 2014 -0400

    DOC: Add parameter to docstring

diff --git a/pandas/tools/tile.py b/pandas/tools/tile.py
index 99fa1eaba..c2512ba2b 100644
--- a/pandas/tools/tile.py
+++ b/pandas/tools/tile.py
@@ -38,6 +38,10 @@ def cut(x, bins, right=True, labels=None, retbins=False, precision=3,
     retbins : bool, optional
         Whether to return the bins or not. Can be useful if bins is given
         as a scalar.
+    precision : int
+        The precision at which to store and display the bins labels
+    include_lowest : bool
+        Whether the first interval should be left-inclusive or not.
 
     Returns
     -------
@@ -121,6 +125,8 @@ def qcut(x, q, labels=None, retbins=False, precision=3):
     retbins : bool, optional
         Whether to return the bins or not. Can be useful if bins is given
         as a scalar.
+    precision : int
+        The precision at which to store and display the bins labels
 
     Returns
     -------
