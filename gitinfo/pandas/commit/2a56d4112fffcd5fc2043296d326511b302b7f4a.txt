commit 2a56d4112fffcd5fc2043296d326511b302b7f4a
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Mon Dec 17 10:15:45 2012 -0500

    BUG: don't fail in PeriodIndex.format when passing formatter. close #2549

diff --git a/pandas/tseries/period.py b/pandas/tseries/period.py
index 602dabe54..5be006ea6 100644
--- a/pandas/tseries/period.py
+++ b/pandas/tseries/period.py
@@ -994,7 +994,7 @@ class PeriodIndex(Int64Index):
 
             return PeriodIndex(result, name=self.name, freq=self.freq)
 
-    def format(self, name=False):
+    def format(self, name=False, formatter=None):
         """
         Render a string representation of the Index
         """
diff --git a/pandas/tseries/tests/test_period.py b/pandas/tseries/tests/test_period.py
index dfa4b341d..e3b51033a 100644
--- a/pandas/tseries/tests/test_period.py
+++ b/pandas/tseries/tests/test_period.py
@@ -1596,6 +1596,13 @@ class TestPeriodIndex(TestCase):
         ts = Series(np.random.randn(len(index)), index=index)
         repr(ts)
 
+    def test_frame_index_to_string(self):
+        index = PeriodIndex(['2011-1', '2011-2', '2011-3'], freq='M')
+        frame = DataFrame(np.random.randn(3,4), index=index)
+
+        # it works!
+        frame.to_string()
+
     def test_asfreq_ts(self):
         index = PeriodIndex(freq='A', start='1/1/2001', end='12/31/2010')
         ts = Series(np.random.randn(len(index)), index=index)
