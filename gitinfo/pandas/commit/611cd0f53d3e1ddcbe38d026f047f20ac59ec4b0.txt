commit 611cd0f53d3e1ddcbe38d026f047f20ac59ec4b0
Author: y-p <yoval@gmx.com>
Date:   Sun Nov 11 01:44:16 2012 +0200

    TST: df.iteritems() should yield Series even with non-unique column labels

diff --git a/pandas/tests/test_frame.py b/pandas/tests/test_frame.py
index c30dde8cc..01b5d6ae4 100644
--- a/pandas/tests/test_frame.py
+++ b/pandas/tests/test_frame.py
@@ -1145,6 +1145,11 @@ class CheckIndexing(object):
                 expected = self.frame[col][idx]
                 assert_almost_equal(result, expected)
 
+    def test_iteritems(self):
+        df=DataFrame([[1,2,3],[4,5,6]],columns=['a','a','b'])
+        for k,v in df.iteritems():
+            self.assertEqual(type(v),Series)
+
     def test_lookup(self):
         def alt(df, rows, cols):
             result = []
@@ -7458,6 +7463,7 @@ starting,ending,measure
                 self.assert_(r0.all())
                 self.assert_(r1.all())
 
+
 if __name__ == '__main__':
     # unittest.main()
     import nose
