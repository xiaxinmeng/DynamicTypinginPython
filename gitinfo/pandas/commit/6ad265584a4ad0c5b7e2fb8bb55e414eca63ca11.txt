commit 6ad265584a4ad0c5b7e2fb8bb55e414eca63ca11
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Sat May 19 14:50:08 2012 -0400

    BUG: fix logical error in values_at_time, close #1249

diff --git a/pandas/src/datetime.pyx b/pandas/src/datetime.pyx
index 93e1ced2d..3ba6bbbd7 100644
--- a/pandas/src/datetime.pyx
+++ b/pandas/src/datetime.pyx
@@ -1190,7 +1190,7 @@ def values_at_time(ndarray[int64_t] stamps, int64_t time):
 
     # Nanosecond resolution
     count = 0
-    for i in range(1, n):
+    for i in range(n):
         if times[i] == time:
             count += 1
 
diff --git a/pandas/tseries/tests/test_timeseries.py b/pandas/tseries/tests/test_timeseries.py
index 1868b5617..4e62e8ab1 100644
--- a/pandas/tseries/tests/test_timeseries.py
+++ b/pandas/tseries/tests/test_timeseries.py
@@ -577,6 +577,13 @@ class TestTimeSeries(unittest.TestCase):
         expected = result_df[-1:]
         tm.assert_frame_equal(result, expected)
 
+        # midnight, everything
+        rng = date_range('1/1/2000', '1/31/2000')
+        ts = Series(np.random.randn(len(rng)), index=rng)
+
+        result = ts.at_time(time(0, 0))
+        assert_series_equal(result, ts)
+
     def test_dti_constructor_preserve_dti_freq(self):
         rng = date_range('1/1/2000', '1/2/2000', freq='5min')
 
