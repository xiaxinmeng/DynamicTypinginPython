commit 1a9abc44bbfd65675fd99701fe33aad8805ab147
Author: Neil Parley <mail@neilparley.co.uk>
Date:   Mon Jun 27 09:03:34 2016 -0400

    Force rebuilding of python files in PR
    
    Force the rebuilding of cython files in PR as can't rely on the git
    history
    
    Author: Neil Parley <mail@neilparley.co.uk>
    
    Closes #13515 from nparley/no-pr-cache and squashes the following commits:
    
    75e71e7 [Neil Parley] Undo test commit
    7fcccc0 [Neil Parley] Testing commit
    3cc7afe [Neil Parley] Force rebuilding of python files in PR

diff --git a/ci/prep_cython_cache.sh b/ci/prep_cython_cache.sh
index 162f7a103..3295bd15d 100755
--- a/ci/prep_cython_cache.sh
+++ b/ci/prep_cython_cache.sh
@@ -21,6 +21,8 @@ if [ -f "$CACHE_File" ] && [ "$USE_CACHE" ]; then
         echo "PR: checking for any cython file changes from last 5 commits"
         git diff PR_HEAD~5 --numstat | grep -E "pyx|pxd"
         retval=$(git diff PR_HEAD~5 --numstat | grep -E "pyx|pxd"| wc -l)
+        echo "Forcing cython rebuild due to possibility of history rewritting in PR"
+        retval=-1
     fi
     echo "number of cython files changed: $retval"
 fi
