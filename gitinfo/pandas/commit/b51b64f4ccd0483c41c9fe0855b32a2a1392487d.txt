commit b51b64f4ccd0483c41c9fe0855b32a2a1392487d
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Mon Apr 23 15:54:02 2012 -0400

    BUG: weekofyear/dayofyear performance problems #1112

diff --git a/pandas/src/datetime.pyx b/pandas/src/datetime.pyx
index eb872e49f..0a9abb98d 100644
--- a/pandas/src/datetime.pyx
+++ b/pandas/src/datetime.pyx
@@ -940,7 +940,7 @@ def fast_field_accessor(ndarray[int64_t] dtindex, object field):
         for i in range(count):
             PyArray_DatetimeToDatetimeStruct(dtindex[i], NPY_FR_us, &dts)
             isleap = is_leapyear(dts.year)
-            out[i] = _month_offset[isleap][dts.month-1] + dts.day
+            out[i] = _month_offset[isleap, dts.month-1] + dts.day
         return out
 
     elif field == 'dow':
@@ -953,7 +953,7 @@ def fast_field_accessor(ndarray[int64_t] dtindex, object field):
         for i in range(count):
             PyArray_DatetimeToDatetimeStruct(dtindex[i], NPY_FR_us, &dts)
             isleap = is_leapyear(dts.year)
-            out[i] = _month_offset[isleap][dts.month-1] + dts.day
+            out[i] = _month_offset[isleap, dts.month - 1] + dts.day
             out[i] = ((out[i] - 1) / 7) + 1
         return out
 
diff --git a/pandas/tseries/index.py b/pandas/tseries/index.py
index 4e9d4c0c7..634785211 100644
--- a/pandas/tseries/index.py
+++ b/pandas/tseries/index.py
@@ -843,6 +843,7 @@ class DatetimeIndex(Int64Index):
     @property
     def weekofyear(self):
         return lib.fast_field_accessor(self.asi8, 'woy')
+    week = weekofyear
 
     @property
     def dayofweek(self):
