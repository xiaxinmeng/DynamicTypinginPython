commit 36b96e20d4aaae5646745c096c34e0c7642dd367
Author: sanguineturtle <mamckay@gmail.com>
Date:   Wed Jun 18 11:30:00 2014 +1000

    [doc fix] minor update to fix latex usepackage syntax

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index 9c1d59318..1653eedd2 100755
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -1358,7 +1358,7 @@ class DataFrame(NDFrame):
                  bold_rows=True, longtable=False, escape=True):
         """
         Render a DataFrame to a tabular environment table. You can splice
-        this into a LaTeX document. Requires \\usepackage(booktabs}.
+        this into a LaTeX document. Requires \\usepackage{booktabs}.
 
         `to_latex`-specific options:
 
