commit 78c3c16402b00b8a27fa628dcf9bc84e024f5caa
Author: Ryan Nazareth <ryankarlos@gmail.com>
Date:   Sun Nov 10 19:11:27 2019 +0000

    TST:Add test to check single category col returns series with single row slice (#29521)
    
    Closes https://github.com/pandas-dev/pandas/issues/14011

diff --git a/pandas/tests/frame/test_dtypes.py b/pandas/tests/frame/test_dtypes.py
index f7b197a51..89fd7ccd9 100644
--- a/pandas/tests/frame/test_dtypes.py
+++ b/pandas/tests/frame/test_dtypes.py
@@ -762,6 +762,15 @@ class TestDataFrameDataTypes:
         with pytest.raises(TypeError, match=xpr):
             df["A"].astype(cls)
 
+    def test_singlerow_slice_categoricaldtype_gives_series(self):
+        # GH29521
+        df = pd.DataFrame({"x": pd.Categorical("a b c d e".split())})
+        result = df.iloc[0]
+        raw_cat = pd.Categorical(["a"], categories=["a", "b", "c", "d", "e"])
+        expected = pd.Series(raw_cat, index=["x"], name=0, dtype="category")
+
+        tm.assert_series_equal(result, expected)
+
     @pytest.mark.parametrize("dtype", ["Int64", "Int32", "Int16"])
     def test_astype_extension_dtypes(self, dtype):
         # GH 22578
