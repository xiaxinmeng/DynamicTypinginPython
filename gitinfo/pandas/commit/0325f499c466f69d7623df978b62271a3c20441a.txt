commit 0325f499c466f69d7623df978b62271a3c20441a
Author: Chang She <changshe@gmail.com>
Date:   Sat Jan 19 22:57:45 2013 -0500

    BUG: BDay for n>5 bug if starting not on offset #2680

diff --git a/pandas/tseries/offsets.py b/pandas/tseries/offsets.py
index 24594d1be..592cc551a 100644
--- a/pandas/tseries/offsets.py
+++ b/pandas/tseries/offsets.py
@@ -329,6 +329,8 @@ class BusinessDay(CacheableOffset, DateOffset):
             if abs(n) > 5:
                 k = n // 5
                 result = result + timedelta(7 * k)
+                if n < 0 and result.weekday() > 4:
+                    n += 1
                 n -= 5 * k
 
             while n != 0:
diff --git a/pandas/tseries/tests/test_offsets.py b/pandas/tseries/tests/test_offsets.py
index b95a0bb2e..f42e9b387 100644
--- a/pandas/tseries/tests/test_offsets.py
+++ b/pandas/tseries/tests/test_offsets.py
@@ -274,6 +274,16 @@ class TestBusinessDay(unittest.TestCase):
         result = dt + BDay(100) - BDay(100)
         self.assertEqual(result, dt)
 
+        off = BDay() * 6
+        rs = datetime(2012, 1, 1) - off
+        xp = datetime(2011, 12, 23)
+        self.assertEqual(rs, xp)
+
+        st = datetime(2011, 12, 18)
+        rs = st + off
+        xp = datetime(2011, 12, 26)
+        self.assertEqual(rs, xp)
+
     def test_apply_corner(self):
         self.assertRaises(Exception, BDay().apply, BMonthEnd())
 
