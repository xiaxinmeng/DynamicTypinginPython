commit da5f7105e9e61247627b13af6b41c6551f5b1ee5
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Fri Apr 27 13:48:35 2012 -0400

    TST: better error message, close #1118

diff --git a/pandas/tseries/period.py b/pandas/tseries/period.py
index e3222bbf5..125811821 100644
--- a/pandas/tseries/period.py
+++ b/pandas/tseries/period.py
@@ -495,8 +495,9 @@ class PeriodIndex(Int64Index):
                                  'collection of some kind, %s was passed'
                                  % repr(data))
 
-            if isinstance(data, Period):
-                data = [data]
+            elif isinstance(data, Period):
+                raise ValueError('Data must be array of dates, strings, '
+                                 'or Period objects')
 
             # other iterable of some kind
             if not isinstance(data, (list, tuple)):
diff --git a/pandas/tseries/tests/test_period.py b/pandas/tseries/tests/test_period.py
index 821870e2d..a3c27baff 100644
--- a/pandas/tseries/tests/test_period.py
+++ b/pandas/tseries/tests/test_period.py
@@ -870,6 +870,13 @@ class TestPeriodIndex(TestCase):
         series = Series(1, index=index)
         self.assert_(isinstance(series, TimeSeries))
 
+    def test_constructor_use_start_freq(self):
+        # GH #1118
+        p = Period('4/2/2012', freq='B')
+        index = PeriodIndex(start=p, periods=10)
+        expected = PeriodIndex(start='4/2/2012', periods=10, freq='B')
+        self.assert_(index.equals(expected))
+
     def test_to_timestamp(self):
         index = PeriodIndex(freq='A', start='1/1/2001', end='12/1/2009')
         series = Series(1, index=index, name='foo')
