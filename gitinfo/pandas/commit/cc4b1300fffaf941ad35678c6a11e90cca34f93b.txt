commit cc4b1300fffaf941ad35678c6a11e90cca34f93b
Author: Phillip Cloud <cpcloud@gmail.com>
Date:   Thu Oct 3 20:56:26 2013 -0400

    CLN: remove copying of PeriodIndex values on astype(int)

diff --git a/pandas/tseries/period.py b/pandas/tseries/period.py
index 860fde6e4..579b0b301 100644
--- a/pandas/tseries/period.py
+++ b/pandas/tseries/period.py
@@ -714,7 +714,7 @@ class PeriodIndex(Int64Index):
         if dtype == np.object_:
             return Index(np.array(list(self), dtype), dtype)
         elif dtype == _INT64_DTYPE:
-            return Index(self.values.copy(), dtype)
+            return Index(self.values, dtype)
         raise ValueError('Cannot cast PeriodIndex to dtype %s' % dtype)
 
     def __iter__(self):
