commit e7fc7e5e013fe471ab7e08dc10f5f557145982d2
Author: Phillip Cloud <cpcloud@gmail.com>
Date:   Tue Sep 24 14:26:16 2013 -0400

    CLN: do not use mutable default arguments

diff --git a/pandas/tools/plotting.py b/pandas/tools/plotting.py
index 18109e8c6..6631a3cf8 100644
--- a/pandas/tools/plotting.py
+++ b/pandas/tools/plotting.py
@@ -201,8 +201,8 @@ plot_params = _Options()
 
 
 def scatter_matrix(frame, alpha=0.5, figsize=None, ax=None, grid=False,
-                   diagonal='hist', marker='.', density_kwds={}, hist_kwds={},
-                   **kwds):
+                   diagonal='hist', marker='.', density_kwds=None,
+                   hist_kwds=None, **kwds):
     """
     Draw a matrix of scatter plots.
 
@@ -243,6 +243,9 @@ def scatter_matrix(frame, alpha=0.5, figsize=None, ax=None, grid=False,
 
     marker = _get_marker_compat(marker)
 
+    hist_kwds = hist_kwds or {}
+    density_kwds = density_kwds or {}
+
     for i, a in zip(lrange(n), df.columns):
         for j, b in zip(lrange(n), df.columns):
             ax = axes[i, j]
