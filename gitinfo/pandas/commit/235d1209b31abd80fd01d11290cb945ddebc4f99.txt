commit 235d1209b31abd80fd01d11290cb945ddebc4f99
Author: pqzx <jc3664@gmail.com>
Date:   Fri Nov 8 10:08:13 2019 +1100

    Fix mypy error in pandas\tests\indexes\interval\test_interval_tree.py (#29021)

diff --git a/pandas/tests/indexes/interval/test_interval_tree.py b/pandas/tests/indexes/interval/test_interval_tree.py
index 87f9eaa20..85dac5ea3 100644
--- a/pandas/tests/indexes/interval/test_interval_tree.py
+++ b/pandas/tests/indexes/interval/test_interval_tree.py
@@ -154,14 +154,14 @@ class TestIntervalTree:
             (np.array([0, 2, np.nan]), np.array([1, 3, np.nan]), False),
         ],
     )
-    @pytest.mark.parametrize("order", map(list, permutations(range(3))))
+    @pytest.mark.parametrize("order", (list(x) for x in permutations(range(3))))
     def test_is_overlapping(self, closed, order, left, right, expected):
         # GH 23309
         tree = IntervalTree(left[order], right[order], closed=closed)
         result = tree.is_overlapping
         assert result is expected
 
-    @pytest.mark.parametrize("order", map(list, permutations(range(3))))
+    @pytest.mark.parametrize("order", (list(x) for x in permutations(range(3))))
     def test_is_overlapping_endpoints(self, closed, order):
         """shared endpoints are marked as overlapping"""
         # GH 23309
diff --git a/setup.cfg b/setup.cfg
index a3da6c38e..10670a4ea 100644
--- a/setup.cfg
+++ b/setup.cfg
@@ -145,9 +145,6 @@ ignore_errors=True
 [mypy-pandas.tests.extension.json.test_json]
 ignore_errors=True
 
-[mypy-pandas.tests.indexes.interval.test_interval_tree]
-ignore_errors=True
-
 [mypy-pandas.tests.indexes.test_base]
 ignore_errors=True
 
