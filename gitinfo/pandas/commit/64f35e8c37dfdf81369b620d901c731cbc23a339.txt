commit 64f35e8c37dfdf81369b620d901c731cbc23a339
Author: y-p <yoval@gmx.com>
Date:   Wed Mar 20 20:27:23 2013 +0200

    BLD: fix cython artifact caching, for fast testing with tox

diff --git a/setup.py b/setup.py
index ad39ad7ac..26d61468f 100755
--- a/setup.py
+++ b/setup.py
@@ -521,6 +521,8 @@ class CachingBuildExt(build_ext, CompilationCacheExtMixin):
             build_ext.cython_sources(self, [s], extension)
             self._put_to_cache(hash, target)
 
+        sources = [x for x in sources if x.startswith("pandas")]
+
         return sources
 
 
diff --git a/tox.ini b/tox.ini
index 76c5cf18a..32e8d1942 100644
--- a/tox.ini
+++ b/tox.ini
@@ -12,6 +12,7 @@ deps =
     numpy >= 1.6.1
     nose
     pytz
+    six
 
 # cd to anything but the default {toxinidir} which
 # contains the pandas subdirectory and confuses
diff --git a/tox_prll.ini b/tox_prll.ini
index 0b9a483e8..50227f0f9 100644
--- a/tox_prll.ini
+++ b/tox_prll.ini
@@ -13,6 +13,7 @@ deps =
     numpy >= 1.6.1
     nose
     pytz
+    six
 
 # cd to anything but the default {toxinidir} which
 # contains the pandas subdirectory and confuses
