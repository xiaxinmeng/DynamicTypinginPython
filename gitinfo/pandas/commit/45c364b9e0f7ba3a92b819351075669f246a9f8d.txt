commit 45c364b9e0f7ba3a92b819351075669f246a9f8d
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Sun Oct 9 17:41:54 2011 -0400

    TST: skip scikits.statsmodels-depending tests if not installed

diff --git a/pandas/stats/tests/test_ols.py b/pandas/stats/tests/test_ols.py
index be6b436e8..dd53183e3 100644
--- a/pandas/stats/tests/test_ols.py
+++ b/pandas/stats/tests/test_ols.py
@@ -46,6 +46,14 @@ class TestOLS(BaseTest):
     # with trusted implementations of panel OLS (e.g. R).
     # TODO: Add tests for non pooled OLS.
 
+    @classmethod
+    def setupClass(cls):
+        try:
+            import scikits.statsmodels.api as _
+        except ImportError:
+            import nose
+            raise nose.SkipTest
+
     def testOLSWithDatasets(self):
         import scikits.statsmodels.datasets as datasets
 
@@ -171,6 +179,13 @@ class TestOLSMisc(unittest.TestCase):
     '''
     For test coverage with faux data
     '''
+    @classmethod
+    def setupClass(cls):
+        try:
+            import scikits.statsmodels.api as _
+        except ImportError:
+            import nose
+            raise nose.SkipTest
 
     def test_f_test(self):
         x = tm.makeTimeDataFrame()
