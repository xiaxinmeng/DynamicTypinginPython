commit 043efa6c94e6abdd033293ba55cd8da7e3763d16
Author: Jeff Reback <jeff@reback.net>
Date:   Fri Mar 17 15:24:35 2017 -0400

    CI: remove miniconda from actual cache scripts

diff --git a/ci/check_cache.sh b/ci/check_cache.sh
index cd7a6e8f6..1c9de7b01 100755
--- a/ci/check_cache.sh
+++ b/ci/check_cache.sh
@@ -12,14 +12,12 @@ else
     ci_changes=$(git diff PR_HEAD~2 --numstat | grep -E "ci/"| wc -l)
 fi
 
-MINICONDA_DIR="$HOME/miniconda/"
 CACHE_DIR="$HOME/.cache/"
 CCACHE_DIR="$HOME/.ccache/"
 
 if [ $ci_changes -ne 0 ]
 then
     echo "Files have changed in ci/ deleting all caches"
-    rm -rf "$MINICONDA_DIR"
     rm -rf "$CACHE_DIR"
     rm -rf "$CCACHE_DIR"
-fi
\ No newline at end of file
+fi
