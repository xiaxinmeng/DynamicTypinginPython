commit 9aa9db9b85ee0285e11fc950f570a886233bc5b1
Author: jbrockmendel <jbrockmendel@gmail.com>
Date:   Sat Sep 7 12:43:42 2019 -0700

    catch more specific (#28198)

diff --git a/pandas/core/groupby/ops.py b/pandas/core/groupby/ops.py
index 1a3f0da3c..40517eefe 100644
--- a/pandas/core/groupby/ops.py
+++ b/pandas/core/groupby/ops.py
@@ -212,9 +212,12 @@ class BaseGrouper:
                 # This Exception is also raised if `f` triggers an exception
                 # but it is preferable to raise the exception in Python.
                 pass
-            except TypeError:
-                # occurs if we have any EAs
-                pass
+            except TypeError as err:
+                if "Cannot convert" in str(err):
+                    # via apply_frame_axis0 if we pass a non-ndarray
+                    pass
+                else:
+                    raise
 
         for key, (i, group) in zip(group_keys, splitter):
             object.__setattr__(group, "name", key)
