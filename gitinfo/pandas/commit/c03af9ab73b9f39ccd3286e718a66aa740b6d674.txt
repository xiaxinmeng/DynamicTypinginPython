commit c03af9ab73b9f39ccd3286e718a66aa740b6d674
Author: Chang She <changshe@gmail.com>
Date:   Fri Dec 7 18:46:43 2012 -0500

    BUG: get rid of offending user-access-control specification in setup.py

diff --git a/setup.py b/setup.py
index 35b35ac4f..5fd800ce0 100755
--- a/setup.py
+++ b/setup.py
@@ -661,9 +661,6 @@ if suffix == '.pyx' and 'setuptools' in sys.modules:
 if _have_setuptools:
     setuptools_kwargs["test_suite"] = "nose.collector"
 
-user_options = {'bdist_wininst': {'user-access-control': 'auto'}}
-options = {}
-
 write_version_py()
 setup(name=DISTNAME,
       version=FULLVERSION,
@@ -708,6 +705,4 @@ setup(name=DISTNAME,
       long_description=LONG_DESCRIPTION,
       classifiers=CLASSIFIERS,
       platforms='any',
-      options=options,
-      user_options=user_options,
       **setuptools_kwargs)
