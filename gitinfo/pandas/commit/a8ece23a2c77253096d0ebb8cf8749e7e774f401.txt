commit a8ece23a2c77253096d0ebb8cf8749e7e774f401
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Fri May 25 12:31:53 2012 -0400

    BUG: don't lose Index name when grouping by level=0, close #1313

diff --git a/RELEASE.rst b/RELEASE.rst
index 6519adbd8..ae03d3b25 100644
--- a/RELEASE.rst
+++ b/RELEASE.rst
@@ -175,6 +175,7 @@ pandas 0.7.3
   - Improper int dtype DataFrame construction from data with NaN (GH #846)
   - Removes default 'result' name in grouby results (GH #995)
   - DataFrame.from_records no longer mutate input columns (PR #975)
+  - Use Index name when grouping by it (#1313)
 
 pandas 0.7.2
 ============
diff --git a/pandas/core/groupby.py b/pandas/core/groupby.py
index 9a31363a0..e9b2107a5 100644
--- a/pandas/core/groupby.py
+++ b/pandas/core/groupby.py
@@ -944,7 +944,7 @@ class Grouping(object):
         self.sort = sort
 
         # right place for this?
-        if isinstance(grouper, Series) and name is None:
+        if isinstance(grouper, (Series, Index)) and name is None:
             self.name = grouper.name
 
         # pre-computed
diff --git a/pandas/tests/test_groupby.py b/pandas/tests/test_groupby.py
index 1891e80ea..86b7a0b99 100644
--- a/pandas/tests/test_groupby.py
+++ b/pandas/tests/test_groupby.py
@@ -1134,6 +1134,13 @@ class TestGroupBy(unittest.TestCase):
         assert_frame_equal(result0, expected0)
         assert_frame_equal(result1, expected1)
 
+    def test_groupby_level_0_nonmulti(self):
+        # #1313
+        a = Series([1,2,3,10,4,5,20,6], Index([1,2,3,1,4,5,2,6], name='foo'))
+
+        result = a.groupby(level=0).sum()
+        self.assertEquals(result.index.name, a.index.name)
+
     def test_level_preserve_order(self):
         grouped = self.mframe.groupby(level=0)
         exp_labels = np.array([0, 0, 0, 1, 1, 2, 2, 3, 3, 3])
