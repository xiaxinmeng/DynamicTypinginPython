commit 3b5984522c20b549a073d2f7cf791184edbaa07b
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Sat Dec 3 12:47:13 2011 -0500

    BUG/ENH: override Index.astype to prevent Index with non-object dtype, GH #412

diff --git a/RELEASE.rst b/RELEASE.rst
index 86984159f..7e1fee893 100644
--- a/RELEASE.rst
+++ b/RELEASE.rst
@@ -41,6 +41,7 @@ pandas 0.6.1
     as in DataFrame (PR #420)
   - DataFrame.delevel will try to infer better dtype for new columns (GH #440)
   - Exclude non-numeric types in DataFrame.{corr, cov}
+  - Override Index.astype to enable dtype casting (GH #412)
 
 **Bug fixes**
 
@@ -57,8 +58,9 @@ pandas 0.6.1
 Thanks
 ------
 - Ralph Bean
-- Joon Ro
 - Wouter Overmeire
+- Joon Ro
+- Chang She
 - Chris Uga
 
 pandas 0.6.0
diff --git a/pandas/core/index.py b/pandas/core/index.py
index 545c9707b..540a3b3bf 100644
--- a/pandas/core/index.py
+++ b/pandas/core/index.py
@@ -65,6 +65,9 @@ class Index(np.ndarray):
     def __array_finalize__(self, obj):
         self.name = getattr(obj, 'name', None)
 
+    def astype(self, dtype):
+        return Index(self.values.astype(dtype))
+
     @property
     def dtype(self):
         return self.values.dtype
diff --git a/pandas/tests/test_index.py b/pandas/tests/test_index.py
index c27357fe1..0f88fe345 100644
--- a/pandas/tests/test_index.py
+++ b/pandas/tests/test_index.py
@@ -62,6 +62,12 @@ class TestIndex(unittest.TestCase):
         # corner case
         self.assertRaises(Exception, Index, 0)
 
+    def test_astype(self):
+        casted = self.intIndex.astype('i8')
+
+        # it works!
+        casted.get_loc(5)
+
     def test_compat(self):
         self.strIndex.tolist()
 
diff --git a/pandas/util/testing.py b/pandas/util/testing.py
index a4b914e43..a9b5d44c8 100644
--- a/pandas/util/testing.py
+++ b/pandas/util/testing.py
@@ -156,7 +156,7 @@ def makeStringIndex(k):
     return Index([rands(10) for _ in xrange(k)])
 
 def makeIntIndex(k):
-    return Index(np.arange(k))
+    return Index(range(k))
 
 def makeDateIndex(k):
     dates = list(DateRange(datetime(2000, 1, 1), periods=k))
