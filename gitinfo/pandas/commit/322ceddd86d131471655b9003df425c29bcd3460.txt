commit 322ceddd86d131471655b9003df425c29bcd3460
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Thu Nov 3 17:44:49 2011 -0400

    BUG: don't use __builtin__.sum in Series.order, address GH #327

diff --git a/pandas/core/series.py b/pandas/core/series.py
index 6a8f02037..b606ae229 100644
--- a/pandas/core/series.py
+++ b/pandas/core/series.py
@@ -1320,11 +1320,11 @@ copy : boolean, default False
             argsorted = argsorted[::-1]
 
         if na_last:
-            n = sum(good)
+            n = good.sum()
             sortedIdx[:n] = idx[good][argsorted]
             sortedIdx[n:] = idx[bad]
         else:
-            n = sum(bad)
+            n = bad.sum()
             sortedIdx[n:] = idx[good][argsorted]
             sortedIdx[:n] = idx[bad]
 
