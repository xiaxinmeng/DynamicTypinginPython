commit ec1aad8fe4013ae24e1ece58497ac85aeec5cca6
Author: Pietro Battiston <me@pietrobattiston.it>
Date:   Thu Feb 27 19:46:55 2014 +0100

    DOC: Clarify that methods taking a MultiIndex level index also accept the name

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index e8d106dcf..228fa1fd0 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -3850,7 +3850,7 @@ class DataFrame(NDFrame):
         ----------
         axis : {0, 1}
             0 for row-wise, 1 for column-wise
-        level : int, default None
+        level : int or level name, default None
             If the axis is a MultiIndex (hierarchical), count along a
             particular level, collapsing into a DataFrame
         numeric_only : boolean, default False
@@ -3926,7 +3926,7 @@ class DataFrame(NDFrame):
         skipna : boolean, default True
             Exclude NA/null values. If an entire row/column is NA, the result
             will be NA
-        level : int, default None
+        level : int or level name, default None
             If the axis is a MultiIndex (hierarchical), count along a
             particular level, collapsing into a DataFrame
         bool_only : boolean, default None
@@ -3957,7 +3957,7 @@ class DataFrame(NDFrame):
         skipna : boolean, default True
             Exclude NA/null values. If an entire row/column is NA, the result
             will be NA
-        level : int, default None
+        level : int or level name, default None
             If the axis is a MultiIndex (hierarchical), count along a
             particular level, collapsing into a DataFrame
         bool_only : boolean, default None
diff --git a/pandas/core/generic.py b/pandas/core/generic.py
index b7bcd5578..e5d764e11 100644
--- a/pandas/core/generic.py
+++ b/pandas/core/generic.py
@@ -3454,7 +3454,7 @@ axis : """ + axis_descr + """
 skipna : boolean, default True
     Exclude NA/null values. If an entire row/column is NA, the result
     will be NA
-level : int, default None
+level : int or level name, default None
         If the axis is a MultiIndex (hierarchical), count along a
         particular level, collapsing into a """ + name + """
 numeric_only : boolean, default None
diff --git a/pandas/core/series.py b/pandas/core/series.py
index 3d5c97e8a..7b5cf72c0 100644
--- a/pandas/core/series.py
+++ b/pandas/core/series.py
@@ -1064,7 +1064,7 @@ class Series(base.IndexOpsMixin, generic.NDFrame):
 
         Parameters
         ----------
-        level : int, default None
+        level : int or level name, default None
             If the axis is a MultiIndex (hierarchical), count along a
             particular level, collapsing into a smaller Series
 
