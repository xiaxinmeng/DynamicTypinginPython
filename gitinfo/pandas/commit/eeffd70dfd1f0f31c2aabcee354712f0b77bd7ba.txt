commit eeffd70dfd1f0f31c2aabcee354712f0b77bd7ba
Author: Brian J. McGuirk <brian.mcguirk@ode.ri.gov>
Date:   Tue Oct 13 11:25:56 2015 -0400

    Correct typo in warning message.
    
    Change `# duplicate columns & possible reduce dimensionaility` to `# duplicate columns & possible reduce dimensionality`.

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index 2cdb6d9b0..920d9ad96 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -1925,7 +1925,7 @@ class DataFrame(NDFrame):
         if self.columns.is_unique:
             return self._get_item_cache(key)
 
-        # duplicate columns & possible reduce dimensionaility
+        # duplicate columns & possible reduce dimensionality
         result = self._constructor(self._data.get(key))
         if result.columns.is_unique:
             result = result[key]
