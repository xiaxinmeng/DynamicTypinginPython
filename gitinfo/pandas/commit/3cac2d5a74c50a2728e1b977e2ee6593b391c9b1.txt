commit 3cac2d5a74c50a2728e1b977e2ee6593b391c9b1
Author: Matthew Brett <matthew.brett@gmail.com>
Date:   Thu Mar 16 12:12:08 2017 -0700

    MAINT: test with manylinux numpy/scipy pre-release (#15702)
    
    Numpy switching to daily manylinux wheels of trunk, instead of building
    wheels specific to Ubuntu 12.04 for every commit.  Use these new wheels
    for numpy pre-release testing.

diff --git a/.travis.yml b/.travis.yml
index b0331941e..ee093e5bf 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -123,11 +123,6 @@ matrix:
         - PANDAS_TESTING_MODE="deprecate"
         - CACHE_NAME="35_numpy_dev"
         - USE_CACHE=true
-      addons:
-        apt:
-          packages:
-          - libatlas-base-dev
-          - gfortran
 #    In allow_failures
     - python: 3.5
       env:
@@ -167,11 +162,6 @@ matrix:
           - PANDAS_TESTING_MODE="deprecate"
           - CACHE_NAME="35_numpy_dev"
           - USE_CACHE=true
-        addons:
-          apt:
-            packages:
-            - libatlas-base-dev
-            - gfortran
       - python: 3.5
         env:
         - PYTHON_VERSION=3.5
diff --git a/ci/requirements-3.5_NUMPY_DEV.build.sh b/ci/requirements-3.5_NUMPY_DEV.build.sh
index b6c8a477e..4af1307f2 100644
--- a/ci/requirements-3.5_NUMPY_DEV.build.sh
+++ b/ci/requirements-3.5_NUMPY_DEV.build.sh
@@ -8,6 +8,7 @@ echo "install numpy master wheel"
 pip uninstall numpy -y
 
 # install numpy wheel from master
-pip install --pre --upgrade --no-index --timeout=60 --trusted-host travis-dev-wheels.scipy.org -f http://travis-dev-wheels.scipy.org/ numpy
+PRE_WHEELS="https://7933911d6844c6c53a7d-47bd50c35cd79bd838daf386af554a83.ssl.cf2.rackcdn.com"
+pip install --pre --upgrade --timeout=60 -f $PRE_WHEELS numpy scipy
 
 true
