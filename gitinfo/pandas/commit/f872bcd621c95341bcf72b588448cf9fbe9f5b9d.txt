commit f872bcd621c95341bcf72b588448cf9fbe9f5b9d
Author: Samesh Lakhotia <43701530+sameshl@users.noreply.github.com>
Date:   Tue Aug 13 15:11:58 2019 +0530

    DOC: Add append example in to_excel documentation (#27852)

diff --git a/pandas/core/generic.py b/pandas/core/generic.py
index 5a1b2a5dc..71d5068e2 100644
--- a/pandas/core/generic.py
+++ b/pandas/core/generic.py
@@ -2179,6 +2179,12 @@ class NDFrame(PandasObject, SelectionMixin):
     ...     df1.to_excel(writer, sheet_name='Sheet_name_1')
     ...     df2.to_excel(writer, sheet_name='Sheet_name_2')
 
+    ExcelWriter can also be used to append to an existing Excel file:
+
+    >>> with pd.ExcelWriter('output.xlsx',
+    ...                     mode='a') as writer:  # doctest: +SKIP
+    ...     df.to_excel(writer, sheet_name='Sheet_name_3')
+
     To set the library that is used to write the Excel file,
     you can pass the `engine` keyword (the default engine is
     automatically chosen depending on the file extension):
