commit b53b899b15e40e3c6c2dc2b1383f175b27fa05a4
Author: Harutaka Kawamura <hkawamura0130@gmail.com>
Date:   Sun Oct 6 07:44:53 2019 +0900

    Fix incorrect doc for to_datetime (#28670)

diff --git a/pandas/core/tools/datetimes.py b/pandas/core/tools/datetimes.py
index 32dc3c1f3..7b136fa29 100644
--- a/pandas/core/tools/datetimes.py
+++ b/pandas/core/tools/datetimes.py
@@ -637,7 +637,7 @@ def to_datetime(
         datetime strings, and if it can be inferred, switch to a faster
         method of parsing them. In some cases this can increase the parsing
         speed by ~5-10x.
-    origin : scalar, default is 'unix'
+    origin : scalar, default 'unix'
         Define the reference date. The numeric values would be parsed as number
         of units (defined by `unit`) since this reference date.
 
