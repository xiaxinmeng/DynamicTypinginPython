commit edfc6db356834dfd3f1326a0ae4556694deb41d8
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Fri Aug 10 15:14:11 2012 -0400

    BUG: don't lose tzinfo in DataFrame constructor, close #1682

diff --git a/RELEASE.rst b/RELEASE.rst
index 982d83b24..521c757e6 100644
--- a/RELEASE.rst
+++ b/RELEASE.rst
@@ -53,6 +53,7 @@ pandas 0.8.2
   - Fix statsmodels import in pandas.stats.var (#1734)
   - Fix DataFrame repr/info summary with non-unique columns (#1700)
   - Fix Series.iget_value for non-unique indexes (#1694)
+  - Don't lose tzinfo when passing DatetimeIndex as DataFrame column (#1682)
 
 pandas 0.8.1
 ============
diff --git a/pandas/core/internals.py b/pandas/core/internals.py
index 1966b51f5..39e51628d 100644
--- a/pandas/core/internals.py
+++ b/pandas/core/internals.py
@@ -1263,6 +1263,12 @@ def form_blocks(data, axes):
         int_block = _simple_blockify(int_dict, items, np.int64)
         blocks.append(int_block)
 
+    for k, v in list(datetime_dict.items()):
+        # hackeroo
+        if hasattr(v, 'tz') and v.tz is not None:
+            del datetime_dict[k]
+            object_dict[k] = v.asobject
+
     if len(datetime_dict):
         datetime_block = _simple_blockify(datetime_dict, items,
                                           np.dtype('M8[ns]'))
diff --git a/pandas/tseries/tests/test_timezones.py b/pandas/tseries/tests/test_timezones.py
index fab71279b..7c4279b68 100644
--- a/pandas/tseries/tests/test_timezones.py
+++ b/pandas/tseries/tests/test_timezones.py
@@ -376,6 +376,12 @@ class TestTimeZoneSupport(unittest.TestCase):
         result = to_datetime(arr, utc=True)
         self.assert_(result.tz is pytz.utc)
 
+    def test_frame_no_datetime64_dtype(self):
+        dr = date_range('2011/1/1', '2012/1/1', freq='W-FRI')
+        dr_tz = dr.tz_localize('US/Eastern')
+        e = DataFrame({'A': 'foo', 'B': dr_tz}, index=dr)
+        self.assert_(e['B'].dtype == object)
+
 class TestTimeZones(unittest.TestCase):
 
     def setUp(self):
