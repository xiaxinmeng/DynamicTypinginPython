commit a46d9ce8e7fbf60d50317d450e904c64befc5143
Author: y-p <yoval@gmx.com>
Date:   Sun Sep 30 12:35:43 2012 +0200

    use basestring rather then (unicode,str) in isinstance

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index 66524e571..a8a7cca2f 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -1366,7 +1366,7 @@ class DataFrame(NDFrame):
             counts = self.count()
             assert(len(cols) == len(counts))
             for col, count in counts.iteritems():
-                if not isinstance(col, (unicode, str)):
+                if not isinstance(col, basestring):
                     col = str(col)
                 lines.append(_put_str(col, space) +
                              '%d  non-null values' % count)
