commit 9d71aaedd08f5fad15795b04a8c997bfee67f896
Author: Joris Van den Bossche <jorisvandenbossche@gmail.com>
Date:   Thu Jul 10 14:23:04 2014 +0200

    DOC: fix problem with part of code example cut off
    
    Probably a bug in the ipython directive, but for now just fixing by putting it in seperate code blocks (only the first def() rendered)

diff --git a/doc/source/cookbook.rst b/doc/source/cookbook.rst
index 283cf0438..844112312 100644
--- a/doc/source/cookbook.rst
+++ b/doc/source/cookbook.rst
@@ -653,11 +653,14 @@ To globally provide aliases for axis names, one can define these 2 functions:
             raise Exception("invalid axis [%s] for alias [%s]" % (axis, alias))
         cls._AXIS_ALIASES[alias] = axis
 
+.. ipython:: python
+
    def clear_axis_alias(cls, axis, alias):
         if axis not in cls._AXIS_NUMBERS:
             raise Exception("invalid axis [%s] for alias [%s]" % (axis, alias))
         cls._AXIS_ALIASES.pop(alias,None)
 
+.. ipython:: python
 
    set_axis_alias(DataFrame,'columns', 'myaxis2')
    df2 = DataFrame(randn(3,2),columns=['c1','c2'],index=['i1','i2','i3'])
