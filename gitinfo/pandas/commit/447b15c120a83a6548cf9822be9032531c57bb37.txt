commit 447b15c120a83a6548cf9822be9032531c57bb37
Author: y-p <yoval@gmx.com>
Date:   Thu Nov 29 04:55:58 2012 +0200

    TST: "x in ix" fails for dupe keys in DatetimeIndex

diff --git a/pandas/tseries/tests/test_timeseries.py b/pandas/tseries/tests/test_timeseries.py
index ef35c44b5..7db86a3e2 100644
--- a/pandas/tseries/tests/test_timeseries.py
+++ b/pandas/tseries/tests/test_timeseries.py
@@ -59,6 +59,11 @@ class TestTimeSeriesDuplicates(unittest.TestCase):
         uniques = self.dups.index.unique()
         self.assert_(uniques.dtype == 'M8[ns]') # sanity
 
+    def test_index_dupes_contains(self):
+        d = datetime(2011, 12, 5, 20, 30)
+        ix=DatetimeIndex([d,d])
+        self.assertTrue(d in ix)
+
     def test_duplicate_dates_indexing(self):
         ts = self.dups
 
