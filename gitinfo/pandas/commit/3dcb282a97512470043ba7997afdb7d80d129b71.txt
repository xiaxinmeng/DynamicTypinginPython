commit 3dcb282a97512470043ba7997afdb7d80d129b71
Author: Chang She <chang@lambdafoundry.com>
Date:   Fri Apr 27 18:15:55 2012 -0400

    BUG: isnull(DatetimeIndex) raises exception

diff --git a/pandas/core/common.py b/pandas/core/common.py
index cbf99a5aa..f210feb7f 100644
--- a/pandas/core/common.py
+++ b/pandas/core/common.py
@@ -73,7 +73,7 @@ def isnull(obj):
                 result = Series(result, index=obj.index, copy=False)
         elif obj.dtype == np.datetime64:
             # this is the NaT pattern
-            result = obj.view('i8') == lib.NaT
+            result = np.array(obj).view('i8') == lib.NaT
         else:
             result = -np.isfinite(obj)
         return result
diff --git a/pandas/tests/test_common.py b/pandas/tests/test_common.py
index a69175257..fa94d7c68 100644
--- a/pandas/tests/test_common.py
+++ b/pandas/tests/test_common.py
@@ -2,7 +2,7 @@ from datetime import datetime
 
 import unittest
 
-from pandas import Series, DataFrame
+from pandas import Series, DataFrame, date_range, DatetimeIndex
 from pandas.core.common import notnull, isnull
 import pandas.core.common as com
 import pandas.util.testing as tm
@@ -44,6 +44,17 @@ def test_isnull_datetime():
     assert (not isnull(datetime.now()))
     assert notnull(datetime.now())
 
+    idx = date_range('1/1/1990', periods=20)
+    assert(notnull(idx).all())
+
+    import pandas._tseries as lib
+    idx = np.asarray(idx)
+    idx[0] = lib.NaT
+    idx = DatetimeIndex(idx)
+    mask = isnull(idx)
+    assert(mask[0])
+    assert(not mask[1:].any())
+
 def test_any_none():
     assert(com._any_none(1, 2, 3, None))
     assert(not com._any_none(1, 2, 3, 4))
