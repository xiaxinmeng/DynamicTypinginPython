commit 4bebdfe4b91ab84c6f0b24d6921dd00bb0ffe31e
Author: Chang She <chang@lambdafoundry.com>
Date:   Thu Jul 12 22:49:19 2012 -0400

    DOC: example for x-y plot

diff --git a/doc/source/visualization.rst b/doc/source/visualization.rst
index d4b39ec99..ca6120f38 100644
--- a/doc/source/visualization.rst
+++ b/doc/source/visualization.rst
@@ -91,6 +91,20 @@ You may pass ``logy`` to get a log-scale Y axis.
    @savefig series_plot_logy.png width=4.5in
    ts.plot(logy=True)
 
+You can plot one column versus another using the `x` and `y` keywords in
+`DataFrame.plot`:
+
+.. ipython:: python
+
+   plt.figure()
+
+   df = DataFrame(np.random.randn(1000, 2), columns=['B', 'C']).cumsum()
+   df['A'] = Series(range(len(df)))
+
+   @savefig df_plot_xy.png width=4.5in
+   df.plot(x='A', y='B')
+
+
 Plotting on a Secondary Y-axis
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 
diff --git a/pandas/tools/plotting.py b/pandas/tools/plotting.py
index 127e674ee..8900a1eb3 100644
--- a/pandas/tools/plotting.py
+++ b/pandas/tools/plotting.py
@@ -1110,11 +1110,12 @@ def plot_frame(frame=None, x=None, y=None, subplots=False, sharex=True,
 
     if isinstance(x, int):
         x = frame.columns[x]
+    if isinstance(y, int):
+        y = frame.columns[y]
+
     if x is not None:
         frame = frame.set_index(x).sort_index()
 
-    if isinstance(y, int):
-        y = frame.columns[y]
     if y is not None:
         return plot_series(frame[y], label=y, kind=kind, use_index=True,
                            rot=rot, xticks=xticks, yticks=yticks,
