commit 88f4e53e8288fd3238aba53ceaf273fb0a219305
Author: Wouter Overmeire <lodagro@gmail.com>
Date:   Thu Nov 1 21:53:05 2012 +0100

    DOC: correct default sep in to_csv, close #2156

diff --git a/doc/source/io.rst b/doc/source/io.rst
index 508f6076f..f74120ad7 100644
--- a/doc/source/io.rst
+++ b/doc/source/io.rst
@@ -654,7 +654,7 @@ function takes a number of arguments. Only the first is required.
     (default), and `header` and `index` are True, then the index names are
     used. (A sequence should be given if the DataFrame uses MultiIndex).
   - ``mode`` : Python write mode, default 'w'
-  - ``sep`` : Field delimiter for the output file (default "'")
+  - ``sep`` : Field delimiter for the output file (default ",")
   - ``encoding``: a string representing the encoding to use if the contents are
     non-ascii, for python versions prior to 3
 
