commit 9c21bb96f8a76da059ba573ba38f50df497ba6f9
Author: gfyoung <gfyoung17+GitHub@gmail.com>
Date:   Tue Oct 22 13:17:30 2019 -0700

    Remove unnecessary check for Python >= 3.5 (#29167)

diff --git a/doc/source/conf.py b/doc/source/conf.py
index 86f78d9c0..cdabf2d47 100644
--- a/doc/source/conf.py
+++ b/doc/source/conf.py
@@ -608,11 +608,7 @@ def linkcode_resolve(domain, info):
             return None
 
     try:
-        # inspect.unwrap() was added in Python version 3.4
-        if sys.version_info >= (3, 5):
-            fn = inspect.getsourcefile(inspect.unwrap(obj))
-        else:
-            fn = inspect.getsourcefile(obj)
+        fn = inspect.getsourcefile(inspect.unwrap(obj))
     except TypeError:
         fn = None
     if not fn:
