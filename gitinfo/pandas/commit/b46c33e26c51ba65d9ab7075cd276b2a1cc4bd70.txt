commit b46c33e26c51ba65d9ab7075cd276b2a1cc4bd70
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Sun Dec 2 11:51:07 2012 -0500

    BLD: add user-access-control option to setup.py. close #2345

diff --git a/setup.py b/setup.py
index 96b0feb12..3b3b51b20 100755
--- a/setup.py
+++ b/setup.py
@@ -657,6 +657,8 @@ if suffix == '.pyx' and 'setuptools' in sys.modules:
 if _have_setuptools:
     setuptools_kwargs["test_suite"] = "nose.collector"
 
+options = {'bdist_wininst': {'user-access-control': 'auto'}}
+
 write_version_py()
 setup(name=DISTNAME,
       version=FULLVERSION,
@@ -700,4 +702,5 @@ setup(name=DISTNAME,
       long_description=LONG_DESCRIPTION,
       classifiers=CLASSIFIERS,
       platforms='any',
+      options=options,
       **setuptools_kwargs)
