commit c25c9280e5ca424ebf73c7ac426880c47ca20f7f
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Tue Nov 6 19:32:41 2012 -0500

    BUG: catch any/all case that fails in NumPy > 1.6 with stricter casting rules

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index ffc220fd7..56114a824 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -4585,9 +4585,7 @@ class DataFrame(NDFrame):
                     result = result.astype(np.float64)
                 elif filter_type == 'bool' and notnull(result).all():
                     result = result.astype(np.bool_)
-                else:
-                    raise NotImplementedError
-
+                # otherwise, accept it
             except (ValueError, TypeError):
                 pass
 
