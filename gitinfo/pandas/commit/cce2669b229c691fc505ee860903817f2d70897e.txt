commit cce2669b229c691fc505ee860903817f2d70897e
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Thu Sep 3 23:11:24 2009 +0000

    added regression examples
    
    git-svn-id: http://pandas.googlecode.com/svn/trunk@17 d5231056-7de3-11de-ac95-d976489f1ece

diff --git a/examples/regressions.py b/examples/regressions.py
new file mode 100644
index 000000000..46e6cf19f
--- /dev/null
+++ b/examples/regressions.py
@@ -0,0 +1,31 @@
+from datetime import datetime
+import string
+
+from pandas.core.api import DataMatrix, DateRange
+
+N = 100
+
+start = datetime(2009, 9, 2)
+dateRange = DateRange(start, periods=N)
+
+def makeDataMatrix():
+    data = DataMatrix(np.random.randn(N, 7),
+                      columns=list(string.ascii_uppercase[:7]),
+                      index=dateRange)
+
+    return data
+
+data = makeDataMatrix()
+model = LinearModel(data, window=50, minPeriods=20)
+model.parseFormula('A ~ B + C + D + E + F + G + I')
+model.fit()
+
+data = {
+    'A' : makeDataMatrix(),
+    'B' : makeDataMatrix(),
+    'C' : makeDataMatrix()
+}
+
+panelModel = XSLinearModel(data, window=50, minPeriods=20)
+panelModel.parseFormula('A ~ B + C + I')
+panelModel.fit()
