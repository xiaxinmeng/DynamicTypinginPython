commit e77ee4be2460347b138152f6397db2155e6451b4
Author: Benjamin Thyreau <benjamin.thyreau@gmail.com>
Date:   Tue Sep 16 12:38:28 2014 +0900

    ENH groupby: more user-friendly error message (GH7511)

diff --git a/pandas/core/groupby.py b/pandas/core/groupby.py
index b2218c10a..350f81069 100644
--- a/pandas/core/groupby.py
+++ b/pandas/core/groupby.py
@@ -1941,7 +1941,8 @@ class Grouping(object):
             # no level passed
             if not isinstance(self.grouper, (Series, Index, np.ndarray)):
                 if getattr(self.grouper,'ndim', 1) != 1:
-                    raise ValueError("Grouper result with an ndim != 1")
+                    t = self.name or str(type(self.grouper))
+                    raise ValueError("Grouper for '%s' not 1-dimensional" % t)
                 self.grouper = self.index.map(self.grouper)
                 if not (hasattr(self.grouper, "__len__") and
                         len(self.grouper) == len(self.index)):
