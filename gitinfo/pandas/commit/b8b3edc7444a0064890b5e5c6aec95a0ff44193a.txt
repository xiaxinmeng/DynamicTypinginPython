commit b8b3edc7444a0064890b5e5c6aec95a0ff44193a
Author: Chang She <chang@lambdafoundry.com>
Date:   Tue May 29 15:40:28 2012 -0400

    BUG: PeriodIndex.map tries to get super(DatetimIndex, self)

diff --git a/pandas/tseries/period.py b/pandas/tseries/period.py
index 83c05f13c..7a9fe005c 100644
--- a/pandas/tseries/period.py
+++ b/pandas/tseries/period.py
@@ -753,7 +753,7 @@ class PeriodIndex(Int64Index):
         try:
             return func_to_map(self)
         except:
-            return super(DatetimeIndex, self).map(func_to_map)
+            return super(PeriodIndex, self).map(func_to_map)
 
     def _mpl_repr(self):
         # how to represent ourselves to matplotlib
diff --git a/pandas/tseries/tests/test_period.py b/pandas/tseries/tests/test_period.py
index d57b299f5..55288d9d7 100644
--- a/pandas/tseries/tests/test_period.py
+++ b/pandas/tseries/tests/test_period.py
@@ -11,6 +11,7 @@ from datetime import datetime, timedelta
 import unittest
 
 from numpy.ma.testutils import assert_equal
+from numpy.testing import assert_array_equal
 
 from pandas.tseries.frequencies import MONTHS, DAYS
 from pandas.tseries.period import Period, PeriodIndex, period_range
@@ -1515,6 +1516,12 @@ class TestPeriodIndex(TestCase):
 
         self.assert_(index[:0].is_full)
 
+    def test_map(self):
+        index = PeriodIndex([2005, 2007, 2009], freq='A')
+        result = index.map(lambda x: x + 1)
+        expected = index + 1
+        assert_array_equal(result, expected)
+
 def _permute(obj):
     return obj.take(np.random.permutation(len(obj)))
 
