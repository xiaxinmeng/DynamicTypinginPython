commit 40ba6eb3772f841625534a602447e3e2c9865511
Author: Xiang Zhang <xzhang23@ncsu.edu>
Date:   Sat May 7 10:48:39 2016 -0400

    DOC: add warning section in indexing docs
    
    Replace PR #13060
    close #12947
    
    Author: Xiang Zhang <xzhang23@ncsu.edu>
    
    Closes #13070 from zhangxiangnick/doc_indexing_v2 and squashes the following commits:
    
    f9ade87 [Xiang Zhang] add warning section in indexing doc.

diff --git a/doc/source/indexing.rst b/doc/source/indexing.rst
index 6227b0858..7380c5438 100644
--- a/doc/source/indexing.rst
+++ b/doc/source/indexing.rst
@@ -191,6 +191,26 @@ raised. Multiple columns can also be set in this manner:
 You may find this useful for applying a transform (in-place) to a subset of the
 columns.
 
+.. warning::
+
+   pandas aligns all AXES when setting ``Series`` and ``DataFrame`` from ``.loc``, ``.iloc`` and ``.ix``.
+
+   This will **not** modify ``df`` because the column alignment is before value assignment.
+
+   .. ipython:: python
+
+      df[['A', 'B']]
+      df.loc[:,['B', 'A']] = df[['A', 'B']]
+      df[['A', 'B']]
+
+   The correct way is to use raw values
+
+   .. ipython:: python
+
+      df.loc[:,['B', 'A']] = df[['A', 'B']].values
+      df[['A', 'B']]
+
+
 Attribute Access
 ----------------
 
