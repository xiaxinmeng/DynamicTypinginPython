commit 0fbc88a3e5376ff507755f23b50c099fc7f580d7
Author: Frank Pinter <fpinter@users.noreply.github.com>
Date:   Tue Aug 4 14:32:21 2015 -0700

    Fix docstring spelling
    
    "analagous" -> "analogous"

diff --git a/pandas/core/strings.py b/pandas/core/strings.py
index e73fa2071..7837fb60d 100644
--- a/pandas/core/strings.py
+++ b/pandas/core/strings.py
@@ -193,7 +193,7 @@ def str_contains(arr, pat, case=True, flags=0, na=np.nan, regex=True):
 
     See Also
     --------
-    match : analagous, but stricter, relying on re.match instead of re.search
+    match : analogous, but stricter, relying on re.match instead of re.search
 
     """
     if regex:
