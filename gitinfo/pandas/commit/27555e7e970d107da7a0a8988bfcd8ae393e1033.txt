commit 27555e7e970d107da7a0a8988bfcd8ae393e1033
Author: jreback <jeff@reback.net>
Date:   Mon Jun 9 09:00:51 2014 -0400

    BUG: Bug in multi-index slicing with incomplete indexers (GH7399)

diff --git a/doc/source/v0.14.1.txt b/doc/source/v0.14.1.txt
index 586e47ff4..0e668f3e8 100644
--- a/doc/source/v0.14.1.txt
+++ b/doc/source/v0.14.1.txt
@@ -160,7 +160,7 @@ Bug Fixes
 
 
 
-
+- Bug in multi-index slicing with incomplete indexers (:issue:`7399`)
 
 
 
diff --git a/pandas/core/index.py b/pandas/core/index.py
index fbadd92c1..1069f7df4 100644
--- a/pandas/core/index.py
+++ b/pandas/core/index.py
@@ -3526,8 +3526,8 @@ class MultiIndex(Index):
             # handle a slice, returnig a slice if we can
             # otherwise a boolean indexer
 
-            start = level_index.get_loc(key.start)
-            stop  = level_index.get_loc(key.stop)
+            start = level_index.get_loc(key.start or  0)
+            stop  = level_index.get_loc(key.stop or len(level_index)-1)
             step = key.step
 
             if level > 0 or self.lexsort_depth == 0:
diff --git a/pandas/tests/test_indexing.py b/pandas/tests/test_indexing.py
index 7610ccc6c..e11ccec22 100644
--- a/pandas/tests/test_indexing.py
+++ b/pandas/tests/test_indexing.py
@@ -1325,6 +1325,18 @@ class TestIndexing(tm.TestCase):
         result = df.loc[[1,2]]
         assert_frame_equal(result, expected)
 
+        # GH 7399
+        # incomplete indexers
+        s = pd.Series(np.arange(15,dtype='int64'),MultiIndex.from_product([range(5), ['a', 'b', 'c']]))
+        expected = s.loc[:, 'a':'c']
+        result = s.loc[0:4, 'a':'c']
+        assert_series_equal(result, expected)
+
+        result = s.loc[:4, 'a':'c']
+        assert_series_equal(result, expected)
+
+        result = s.loc[0:, 'a':'c']
+        assert_series_equal(result, expected)
 
     def test_series_getitem_multiindex(self):
 
