commit e1809f791d9e9c06bff43e30cd2f87b4df777101
Author: Paul van Mulbregt <pvanmulbregt@users.noreply.github.com>
Date:   Mon Jul 30 06:06:32 2018 -0400

    DOC: Fix spaces and brackets in ValueError message for option_context. (#22121)

diff --git a/pandas/core/config.py b/pandas/core/config.py
index 369e05683..abcdbfa12 100644
--- a/pandas/core/config.py
+++ b/pandas/core/config.py
@@ -391,7 +391,7 @@ class option_context(object):
     def __init__(self, *args):
         if not (len(args) % 2 == 0 and len(args) >= 2):
             raise ValueError('Need to invoke as'
-                             'option_context(pat, val, [(pat, val), ...)).')
+                             ' option_context(pat, val, [(pat, val), ...]).')
 
         self.ops = list(zip(args[::2], args[1::2]))
 
