commit d7e8f316dc7155252fa47ea23a3074d7d0197e54
Author: Jeff Reback <jeff@reback.net>
Date:   Sat Dec 24 10:46:06 2016 -0500

    CI: use conda defaults for 3.6 build

diff --git a/.travis.yml b/.travis.yml
index 3e24f3798..becb347c3 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -80,11 +80,10 @@ matrix:
         apt:
           packages:
           - xsel
-    - python: 3.6-dev
+    - python: 3.6
       env:
         - PYTHON_VERSION=3.6
-        - JOB_NAME: "36_dev"
-        - JOB_TAG=_DEV
+        - JOB_NAME: "36"
         - NOSE_ARGS="not slow and not network and not disabled"
         - PANDAS_TESTING_MODE="deprecate"
       addons:
diff --git a/ci/install-3.6_DEV.sh b/ci/install-3.6_DEV.sh
deleted file mode 100644
index 0b95f1cd4..000000000
--- a/ci/install-3.6_DEV.sh
+++ /dev/null
@@ -1,16 +0,0 @@
-#!/bin/bash
-
-echo "install 3.6 dev"
-
-conda config --set add_pip_as_python_dependency false
-conda create -n pandas python=3.6 -c conda-forge/label/prerelease
-
-source activate pandas
-
-# ensure we have pip
-python -m ensurepip
-
-# install deps
-pip3.6 install nose cython numpy pytz python-dateutil
-
-true
diff --git a/ci/install_travis.sh b/ci/install_travis.sh
index b9b111509..f35e21655 100755
--- a/ci/install_travis.sh
+++ b/ci/install_travis.sh
@@ -90,7 +90,15 @@ if [ -e ${INSTALL} ]; then
 else
 
     # create new env
-    time conda create -n pandas python=$PYTHON_VERSION nose coverage flake8 || exit 1
+    time conda create -n pandas python=$PYTHON_VERSION nose || exit 1
+
+    if [ "$COVERAGE" ]; then
+        pip install coverage
+    fi
+    if [ "$LINT" ]; then
+        conda install flake8
+        pip install cpplint
+    fi
 fi
 
 # build deps
diff --git a/ci/lint.sh b/ci/lint.sh
index 32ac606a4..5c4613f88 100755
--- a/ci/lint.sh
+++ b/ci/lint.sh
@@ -7,7 +7,6 @@ source activate pandas
 RET=0
 
 if [ "$LINT" ]; then
-    pip install cpplint
 
     # pandas/rpy is deprecated and will be removed.
     # pandas/src is C code, so no need to search there.
diff --git a/ci/requirements-3.6.build b/ci/requirements-3.6.build
new file mode 100644
index 000000000..9558cf00d
--- /dev/null
+++ b/ci/requirements-3.6.build
@@ -0,0 +1,4 @@
+python-dateutil
+pytz
+numpy
+cython
diff --git a/ci/requirements-3.6.run b/ci/requirements-3.6.run
new file mode 100644
index 000000000..b9d543f55
--- /dev/null
+++ b/ci/requirements-3.6.run
@@ -0,0 +1,3 @@
+python-dateutil
+pytz
+numpy
