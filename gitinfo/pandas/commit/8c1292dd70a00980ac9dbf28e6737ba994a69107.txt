commit 8c1292dd70a00980ac9dbf28e6737ba994a69107
Author: Adam Klein <adamklein@gmail.com>
Date:   Thu Jan 26 12:51:44 2012 -0500

    BUG: #680, print consistently when dataframe is empty

diff --git a/pandas/core/format.py b/pandas/core/format.py
index bb97546d7..411201776 100644
--- a/pandas/core/format.py
+++ b/pandas/core/format.py
@@ -179,10 +179,9 @@ class DataFrameFormatter(object):
         to_write = []
 
         if len(frame.columns) == 0 or len(frame.index) == 0:
-            info_line = ('Empty %s\nColumns: %s\nIndex: %s' 
-                         % (type(self.frame).__name__, 
-                            repr(frame.columns), 
-                            repr(frame.index)))
+            info_line = (u'Empty %s\nColumns: %s\nIndex: %s'
+                         % (type(self.frame).__name__,
+                            frame.columns, frame.index))
             to_write.append(info_line)
         else:
             # may include levels names also
diff --git a/pandas/core/index.py b/pandas/core/index.py
index 2d0f4fcf2..8e97a6d5a 100644
--- a/pandas/core/index.py
+++ b/pandas/core/index.py
@@ -121,13 +121,13 @@ class Index(np.ndarray):
         name = type(self).__name__
         return '%s: %s entries%s' % (name, len(self), index_summary)
 
-    def __repr__(self):
+    def __str__(self):
         try:
             return np.array_repr(self.values)
         except UnicodeError:
-            return np.array_repr(np.array(
-                        [x if not isinstance(x, basestring) else unicode(x)
-                         for x in self.values]))
+            converted = u','.join(unicode(x) for x in self.values)
+            return u'%s([%s], dtype=''%s'')' % (type(self).__name__, converted,
+                                              str(self.values.dtype))
 
     @property
     def values(self):
