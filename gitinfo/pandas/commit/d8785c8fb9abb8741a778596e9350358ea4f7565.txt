commit d8785c8fb9abb8741a778596e9350358ea4f7565
Author: Phillip Cloud <cpcloud@gmail.com>
Date:   Mon Jul 15 20:05:49 2013 -0400

    BUG: remove six import

diff --git a/pandas/core/reshape.py b/pandas/core/reshape.py
index 1b3aa0f96..cb34d0bad 100644
--- a/pandas/core/reshape.py
+++ b/pandas/core/reshape.py
@@ -5,8 +5,6 @@ import itertools
 
 import numpy as np
 
-import six
-
 from pandas.core.series import Series
 from pandas.core.frame import DataFrame
 
@@ -691,7 +689,7 @@ def melt(frame, id_vars=None, value_vars=None,
         else:
             var_name = [frame.columns.name if frame.columns.name is not None
                         else 'variable']
-    if isinstance(var_name, six.string_types):
+    if isinstance(var_name, basestring):
         var_name = [var_name]
 
     N, K = frame.shape
