commit a1b8fe2d1e2992055fc3600d6f3734ea920a0344
Author: y-p <yoval@gmx.com>
Date:   Sat Dec 8 00:59:41 2012 +0200

    BLD: update setup.py numpy version deps for python3

diff --git a/setup.py b/setup.py
index 15452e3e0..35b35ac4f 100755
--- a/setup.py
+++ b/setup.py
@@ -51,11 +51,15 @@ except ImportError:
 setuptools_kwargs = {}
 if sys.version_info[0] >= 3:
 
+    min_numpy_ver = 1.6
+    if sys.version_info[1] >= 3: # 3.3 needs numpy 1.7+
+        min_numpy_ver = 1.7
+
     setuptools_kwargs = {'use_2to3': True,
                          'zip_safe': False,
                          'install_requires': ['python-dateutil >= 2',
                                               'pytz',
-                                              'numpy >= 1.4'],
+                                              'numpy >= %s' % min_numpy_ver],
                          'use_2to3_exclude_fixers': ['lib2to3.fixes.fix_next',
                                                     ],
                         }
