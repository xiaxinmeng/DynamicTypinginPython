commit 02295fe5681b51986259153b0bf23cea6ddd55d0
Author: Skipper Seabold <jsseabold@gmail.com>
Date:   Wed Sep 14 19:53:03 2011 -0400

    BUG: Allow int to pass with sort on index level

diff --git a/pandas/core/index.py b/pandas/core/index.py
index 209172d23..a30883dd7 100644
--- a/pandas/core/index.py
+++ b/pandas/core/index.py
@@ -800,10 +800,11 @@ class MultiIndex(Index):
         sorted_index : MultiIndex
         """
         labels = list(self.labels)
-        try:
-            level = self.names.index(level)
-        except:
-            raise ValueError("level %s not in index names" % level)
+        if not isinstance(level, int):
+            try:
+                level = self.names.index(level)
+            except:
+                raise ValueError("level %s not in index names" % level)
 
         primary = labels.pop(level)
 
