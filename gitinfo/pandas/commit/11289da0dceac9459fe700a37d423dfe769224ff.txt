commit 11289da0dceac9459fe700a37d423dfe769224ff
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Mon Apr 30 16:36:37 2012 -0400

    BUG; cast to uint8 for row_bool_subset

diff --git a/pandas/core/groupby.py b/pandas/core/groupby.py
index 1fd3e764d..c2c5e8951 100644
--- a/pandas/core/groupby.py
+++ b/pandas/core/groupby.py
@@ -671,7 +671,9 @@ class Grouper(object):
         result = trans_func(result)
 
         if self._filter_empty_groups:
-            result = lib.row_bool_subset(result, counts > 0)
+            # python 2.5
+            result = lib.row_bool_subset(result,
+                                         (counts > 0).view(np.uint8))
 
         if vdim == 1:
             result = result[:, 0]
@@ -887,7 +889,8 @@ class BinGrouper(Grouper):
         result = trans_func(result)
 
         if self._filter_empty_groups:
-            result = lib.row_bool_subset(result, counts > 0)
+            result = lib.row_bool_subset(result,
+                                         (counts > 0).view(np.uint8))
 
         if vdim == 1 and arity == 1:
             result = result[:, 0]
