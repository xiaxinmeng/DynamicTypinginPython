commit 4b8ff26412cecc288c5a8bbc448390233a156215
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Mon Sep 12 15:01:30 2011 -0400

    BUG: Python 2.5 compat in fillna

diff --git a/README.rst b/README.rst
index f0aadb1a1..0e5302314 100644
--- a/README.rst
+++ b/README.rst
@@ -59,6 +59,24 @@ And via ``easy_install`` or ``pip``::
     easy_install pandas
     pip install pandas
 
+Dependencies
+============
+
+  * `NumPy <http://www.numpy.org>`__: 1.4.0 or higher. Recommend 1.5.1 or
+    higher
+  * `python-dateutil <http://labix.org/python-dateutil>`__ 1.5
+
+Optional dependencies
+~~~~~~~~~~~~~~~~~~~~~
+
+  * `SciPy <http://www.scipy.org>`__: miscellaneous statistical functions
+  * `PyTables <http://www.pytables.org>`__: necessary for HDF5-based storage
+  * `matplotlib <http://matplotlib.sourceforge.net/>`__: for plotting
+  * `scikits.statsmodels <http://statsmodels.sourceforge.net/>`__
+     * Needed for parts of :mod:`pandas.stats`
+  * `pytz <http://pytz.sourceforge.net/>`__
+     * Needed for time zone support with ``DateRange``
+
 Installation from sources
 =========================
 
diff --git a/doc/source/install.rst b/doc/source/install.rst
index 0b9d7679c..cd1c814c4 100644
--- a/doc/source/install.rst
+++ b/doc/source/install.rst
@@ -43,6 +43,8 @@ Optional dependencies
   * `matplotlib <http://matplotlib.sourceforge.net/>`__: for plotting
   * `scikits.statsmodels <http://statsmodels.sourceforge.net/>`__
      * Needed for parts of :mod:`pandas.stats`
+  * `pytz <http://pytz.sourceforge.net/>`__
+     * Needed for time zone support with ``DateRange``
 
 .. note::
 
diff --git a/pandas/core/series.py b/pandas/core/series.py
index 41692ebec..d074ede1e 100644
--- a/pandas/core/series.py
+++ b/pandas/core/series.py
@@ -25,9 +25,6 @@ import pandas._tseries as _tseries
 
 __all__ = ['Series', 'TimeSeries']
 
-def _numpy_lt_151():
-    return np.__version__ < '1.5.1'
-
 #-------------------------------------------------------------------------------
 # Wrapper function for Series arithmetic methods
 
@@ -1395,8 +1392,8 @@ copy : boolean, default False
 
             mask = isnull(self.values)
 
-            if _numpy_lt_151():  # pragma: no cover
-                mask = mask.astype(np.uint8)
+            # sadness. for Python 2.5 compatibility
+            mask = mask.astype(np.uint8)
 
             if method == 'pad':
                 indexer = _tseries.get_pad_indexer(mask)
