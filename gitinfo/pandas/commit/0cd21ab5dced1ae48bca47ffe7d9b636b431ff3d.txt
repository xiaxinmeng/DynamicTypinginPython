commit 0cd21ab5dced1ae48bca47ffe7d9b636b431ff3d
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Mon Jul 9 16:19:49 2012 -0400

    BUG: fix resampling bug with lowercase daily freq close #1588

diff --git a/RELEASE.rst b/RELEASE.rst
index a4c0e989c..aa48423e5 100644
--- a/RELEASE.rst
+++ b/RELEASE.rst
@@ -31,6 +31,7 @@ pandas 0.8.1
 
   - Handle TypeError issues inside PyObject_RichCompareBool calls in khash
     (#1318)
+  - Fix resampling bug to lower case daily frequency (#1588)
 
 pandas 0.8.0
 ============
diff --git a/pandas/tseries/frequencies.py b/pandas/tseries/frequencies.py
index 98f9fbe3c..76806c6cd 100644
--- a/pandas/tseries/frequencies.py
+++ b/pandas/tseries/frequencies.py
@@ -117,11 +117,9 @@ _offset_map = {
     'A-OCT' : YearEnd(month=10),
     'A-NOV' : YearEnd(month=11),
     'A-DEC' : YearEnd(month=12),
-    'A'     : YearEnd(month=12),
 
     # Annual - Calendar (start)
     'AS-JAN' : YearBegin(month=1),
-    'AS'     : YearBegin(month=1),
     'AS-FEB' : YearBegin(month=2),
     'AS-MAR' : YearBegin(month=3),
     'AS-APR' : YearBegin(month=4),
@@ -147,11 +145,9 @@ _offset_map = {
     'BA-OCT' : BYearEnd(month=10),
     'BA-NOV' : BYearEnd(month=11),
     'BA-DEC' : BYearEnd(month=12),
-    'BA'     : BYearEnd(month=12),
 
     # Annual - Business (Start)
     'BAS-JAN' : BYearBegin(month=1),
-    'BAS'     : BYearBegin(month=1),
     'BAS-FEB' : BYearBegin(month=2),
     'BAS-MAR' : BYearBegin(month=3),
     'BAS-APR' : BYearBegin(month=4),
@@ -299,6 +295,11 @@ _rule_aliases = {
     'Q@MAR': 'BQ-MAR',
     'Q' : 'Q-DEC',
 
+    'A'     : 'A-DEC', # YearEnd(month=12),
+    'AS'     : 'AS-JAN', # YearBegin(month=1),
+    'BA'     : 'BA-DEC', # BYearEnd(month=12),
+    'BAS'     : 'BAS-JAN', # BYearBegin(month=1),
+
     'A@JAN' : 'BA-JAN',
     'A@FEB' : 'BA-FEB',
     'A@MAR' : 'BA-MAR',
diff --git a/pandas/tseries/offsets.py b/pandas/tseries/offsets.py
index f35a85970..0cd36a112 100644
--- a/pandas/tseries/offsets.py
+++ b/pandas/tseries/offsets.py
@@ -1015,7 +1015,7 @@ class Tick(DateOffset):
         else:
             return DateOffset.__ne__(self, other)
 
-    @cache_readonly
+    @property
     def delta(self):
         return self.n * self._inc
 
diff --git a/pandas/tseries/resample.py b/pandas/tseries/resample.py
index a2b1c2033..762c19d23 100644
--- a/pandas/tseries/resample.py
+++ b/pandas/tseries/resample.py
@@ -35,7 +35,7 @@ class TimeGrouper(CustomGrouper):
                  nperiods=None, axis=0,
                  fill_method=None, limit=None, loffset=None, kind=None,
                  convention=None, base=0):
-        self.freq = freq
+        self.freq = to_offset(freq)
         self.closed = closed
         self.label = label
         self.nperiods = nperiods
diff --git a/pandas/tseries/tests/test_resample.py b/pandas/tseries/tests/test_resample.py
index 15c82b8c9..57e875f63 100644
--- a/pandas/tseries/tests/test_resample.py
+++ b/pandas/tseries/tests/test_resample.py
@@ -495,6 +495,11 @@ class TestResample(unittest.TestCase):
         self.assert_(len(result) == 1)
         self.assert_(result.index[0] == Period('2000-04', freq='M'))
 
+    def test_anchored_lowercase_buglet(self):
+        dates = date_range('4/16/2012 20:00', periods=50000, freq='s')
+        ts = Series(np.random.randn(len(dates)), index=dates)
+        # it works!
+        ts.resample('d')
 
 def _simple_ts(start, end, freq='D'):
     rng = date_range(start, end, freq=freq)
