commit 918cc482022188fb3d0a6215804aa332cadbae37
Author: Jake Vanderplas <jakevdp@google.com>
Date:   Sun Feb 2 09:12:31 2020 -0800

    Fix docstring for to_markdown buf parameter (#31565)

diff --git a/pandas/core/generic.py b/pandas/core/generic.py
index 3b1d7e4c5..3bb584d4d 100644
--- a/pandas/core/generic.py
+++ b/pandas/core/generic.py
@@ -1922,10 +1922,8 @@ class NDFrame(PandasObject, SelectionMixin, indexing.IndexingMixin):
 
     Parameters
     ----------
-    buf : writable buffer, defaults to sys.stdout
-        Where to send the output. By default, the output is printed to
-        sys.stdout. Pass a writable buffer if you need to further process
-        the output.
+    buf : str, Path or StringIO-like, optional, default None
+        Buffer to write to. If None, the output is returned as a string.
     mode : str, optional
         Mode in which file is opened.
     **kwargs
