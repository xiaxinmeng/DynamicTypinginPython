commit c20106f25e603c16c1be16a91173874bad71e1c7
Author: MomIsBestFriend <50263213+MomIsBestFriend@users.noreply.github.com>
Date:   Wed Dec 4 15:48:43 2019 +0200

    DOC: Added "Raises `KeyError`" (#30014)

diff --git a/pandas/compat/chainmap.py b/pandas/compat/chainmap.py
index 84824207d..479eddf0c 100644
--- a/pandas/compat/chainmap.py
+++ b/pandas/compat/chainmap.py
@@ -10,6 +10,12 @@ class DeepChainMap(ChainMap):
         self.maps[0][key] = value
 
     def __delitem__(self, key):
+        """
+        Raises
+        ------
+        KeyError
+            If `key` doesn't exist.
+        """
         for mapping in self.maps:
             if key in mapping:
                 del mapping[key]
