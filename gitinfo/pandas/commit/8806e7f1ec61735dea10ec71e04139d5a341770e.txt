commit 8806e7f1ec61735dea10ec71e04139d5a341770e
Author: Chang She <chang@lambdafoundry.com>
Date:   Thu Jul 12 11:16:31 2012 -0400

    BUG: CSV parser fails with single line, no separator #1553

diff --git a/pandas/io/parsers.py b/pandas/io/parsers.py
index 4ed3c7f30..a6ed259fe 100644
--- a/pandas/io/parsers.py
+++ b/pandas/io/parsers.py
@@ -576,7 +576,10 @@ class TextParser(object):
                 counts[col] = cur_count + 1
             self._clear_buffer()
         else:
-            line = self._next_line()
+            if len(self.buf) > 0:
+                line = self.buf[0]
+            else:
+                line = self._next_line()
 
             ncols = len(line)
             if not names:
diff --git a/pandas/io/tests/test_parsers.py b/pandas/io/tests/test_parsers.py
index 4e369df2e..8d47162ac 100644
--- a/pandas/io/tests/test_parsers.py
+++ b/pandas/io/tests/test_parsers.py
@@ -252,6 +252,10 @@ KORD,19990127 22:00:00, 21:56:00, -0.5900, 1.7100, 5.1000, 0.0000, 290.0000
         d = datetime(1999, 1, 27, 19, 0)
         self.assert_(df.index[0] == d)
 
+    def test_single_line(self):
+        df = read_csv(StringIO('1,2'), names=['a', 'b'], sep=None)
+        assert_frame_equal(DataFrame({'a': [1], 'b': [2]}), df)
+
     def test_multiple_date_cols_with_header(self):
         data = """\
 ID,date,NominalTime,ActualTime,TDew,TAir,Windspeed,Precip,WindDir
