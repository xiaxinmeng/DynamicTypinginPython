commit 76eb112f7ff6add16d229cb0df59a86a5b90f92c
Author: y-p <yoval@gmx.com>
Date:   Tue Nov 26 14:55:08 2013 +0200

    VB: add to_html vbench

diff --git a/vb_suite/frame_methods.py b/vb_suite/frame_methods.py
index b7754e286..a7c863345 100644
--- a/vb_suite/frame_methods.py
+++ b/vb_suite/frame_methods.py
@@ -145,6 +145,20 @@ df = DataFrame(randn(100, 10))
 frame_to_string_floats = Benchmark('df.to_string()', setup,
                                    start_date=datetime(2010, 6, 1))
 
+#----------------------------------------------------------------------
+# to_html
+
+setup = common_setup + """
+nrows=500
+df = DataFrame(randn(nrows, 10))
+df[0]=period_range("2000","2010",nrows)
+df[1]=range(nrows)
+
+"""
+
+frame_to_html_mixed = Benchmark('df.to_html()', setup,
+                                   start_date=datetime(2010, 6, 1))
+
 # insert many columns
 
 setup = common_setup + """
