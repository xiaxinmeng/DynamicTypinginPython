commit 031bd144442362f6ae77c804938d6986c7c4b7d5
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Mon Jan 10 18:46:23 2011 -0500

    fix bug in Index.__add__

diff --git a/pandas/__init__.py b/pandas/__init__.py
index d20be9170..b68ef49e6 100644
--- a/pandas/__init__.py
+++ b/pandas/__init__.py
@@ -12,6 +12,7 @@ from pandas.info import __doc__
 from pandas.core.api import *
 from pandas.io.parsers import parseCSV, parseText, parseExcel
 from pandas.stats.api import *
+from pandas.util.testing import debug
 
 from numpy.testing import Tester
 class NoseWrapper(Tester):
diff --git a/pandas/core/index.py b/pandas/core/index.py
index 490f4388b..702307430 100644
--- a/pandas/core/index.py
+++ b/pandas/core/index.py
@@ -149,7 +149,7 @@ class Index(np.ndarray):
         if isinstance(other, Index):
             return self.union(other)
         else:
-            return np.ndarray.__add__(self, other)
+            return Index(self.view(np.ndarray) + other)
 
     __eq__ = _indexOp('__eq__')
     __lt__ = _indexOp('__lt__')
diff --git a/pandas/core/tests/test_index.py b/pandas/core/tests/test_index.py
index 0c75096a2..60304eff6 100644
--- a/pandas/core/tests/test_index.py
+++ b/pandas/core/tests/test_index.py
@@ -118,6 +118,14 @@ class TestIndex(unittest.TestCase):
         # this is valid too
         shifted = self.dateIndex + timedelta(1)
 
+    def test_add_string(self):
+        # from bug report
+        index = Index(['a', 'b', 'c'])
+        index2 = index + 'foo'
+
+        self.assert_('a' not in index2.indexMap)
+        self.assert_('afoo' in index2.indexMap)
+
     def test_shift(self):
         shifted = self.dateIndex.shift(0, timedelta(1))
         self.assert_(shifted is self.dateIndex)
diff --git a/pandas/io/parsers.py b/pandas/io/parsers.py
index fb62cdea7..393a41aaa 100644
--- a/pandas/io/parsers.py
+++ b/pandas/io/parsers.py
@@ -6,7 +6,6 @@ from datetime import datetime, timedelta
 from itertools import izip
 import string
 
-from dateutil import parser
 import numpy as np
 
 from pandas.core.index import Index
@@ -18,6 +17,16 @@ def simpleParser(nestedList, colNames=None, header=0, indexCol=0):
 
     Should be replaced by np.genfromtxt
     """
+    try:
+        from dateutil import parser
+        parse_date = parser.parse
+    except ImportError:
+        def parse_date(s):
+            try:
+                return datetime.strptime(s, '%m/%d/%Y')
+            except Exception:
+                return s
+
     naValues = set(['-1.#IND', '1.#QNAN', '1.#IND',
                     '-1.#QNAN','1.#INF','-1.#INF', '1.#INF000000',
                     'NA', 'NULL', 'NaN', 'nan', ''])
@@ -68,7 +77,7 @@ def simpleParser(nestedList, colNames=None, header=0, indexCol=0):
             dates = []
             for s in data[columns[0]]:
                 try:
-                    dates.append(parser.parse(s))
+                    dates.append(parse_date(s))
                 except Exception:
                     dates.append(s)
             data[columns[0]] = dates
diff --git a/pandas/util/testing.py b/pandas/util/testing.py
index ff4e6943b..e1125982c 100644
--- a/pandas/util/testing.py
+++ b/pandas/util/testing.py
@@ -38,8 +38,7 @@ def rands(n):
 # Console debugging tools
 
 def debug(f, *args, **kwargs):
-    from IPython.Debugger import Pdb
-    Pdb(color_scheme='Linux').set_trace()
+    set_trace()
     return f(*args, **kwargs)
 
 def set_trace():
