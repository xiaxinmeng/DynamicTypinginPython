commit 93a67dbbb6f8adad0061eaa21a046ba44e297957
Author: Simon Hawkins <simonjayhawkins@gmail.com>
Date:   Wed May 29 11:17:24 2019 +0100

    CI: unary operator expected error in log files (#26547)

diff --git a/ci/setup_env.sh b/ci/setup_env.sh
index e2667558a..8f73bb228 100755
--- a/ci/setup_env.sh
+++ b/ci/setup_env.sh
@@ -118,12 +118,12 @@ echo "conda list"
 conda list
 
 # Install DB for Linux
-if [ ${TRAVIS_OS_NAME} == "linux" ]; then
+if [ "${TRAVIS_OS_NAME}" == "linux" ]; then
   echo "installing dbs"
   mysql -e 'create database pandas_nosetest;'
   psql -c 'create database pandas_nosetest;' -U postgres
 else
-   echo "not using dbs on non-linux"
+   echo "not using dbs on non-linux Travis builds or Azure Pipelines"
 fi
 
 echo "done"
