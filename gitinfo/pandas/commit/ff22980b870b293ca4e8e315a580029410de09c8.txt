commit ff22980b870b293ca4e8e315a580029410de09c8
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Sat Dec 15 23:44:49 2012 -0500

    BUG: remove pdb

diff --git a/pandas/core/internals.py b/pandas/core/internals.py
index 1cd713130..639141e4e 100644
--- a/pandas/core/internals.py
+++ b/pandas/core/internals.py
@@ -27,11 +27,7 @@ class Block(object):
         if values.ndim != ndim:
             raise AssertionError('Wrong number of dimensions')
 
-        nitems = len(items)
-        nvalues = len(values)
-        if nitems != nvalues:
-            import pdb
-            pdb.set_trace()
+        if len(items) != len(values):
             raise AssertionError('Wrong number of items passed (%d vs %d)'
                                  % (len(items), len(values)))
 
