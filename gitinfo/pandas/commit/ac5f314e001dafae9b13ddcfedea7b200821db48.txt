commit ac5f314e001dafae9b13ddcfedea7b200821db48
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Tue Nov 8 00:20:17 2011 -0500

    BUG: for multiindexed data, assumed index_col ascending but not always true, GH #349

diff --git a/pandas/io/parsers.py b/pandas/io/parsers.py
index 1b6df2fd5..dc80b8119 100644
--- a/pandas/io/parsers.py
+++ b/pandas/io/parsers.py
@@ -368,9 +368,9 @@ class TextParser(object):
                 index = []
                 for idx in self.index_col:
                     index.append(zipped_content[idx])
-                #remove index items from content and columns, don't pop in loop
-                for i in range(len(self.index_col)):
-                    zipped_content.remove(index[i])
+                # remove index items from content and columns, don't pop in loop
+                for i in reversed(sorted(self.index_col)):
+                    zipped_content.pop(i)
 
             if np.isscalar(self.index_col):
                 if self.parse_dates:
diff --git a/pandas/io/tests/test_parsers.py b/pandas/io/tests/test_parsers.py
index 90630a1a2..f8e801f26 100644
--- a/pandas/io/tests/test_parsers.py
+++ b/pandas/io/tests/test_parsers.py
@@ -382,6 +382,10 @@ bar,two,12,13,14,15
         df = read_csv(StringIO(data), index_col=[0, 1], parse_dates=True)
         self.assert_(isinstance(df.index.levels[0][0], datetime))
 
+        # specify columns out of order!
+        df2 = read_csv(StringIO(data), index_col=[1, 0], parse_dates=True)
+        self.assert_(isinstance(df2.index.levels[1][0], datetime))
+
     def test_skip_footer(self):
         data = """A,B,C
 1,2,3
