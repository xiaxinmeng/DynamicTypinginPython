commit b0757dc77ef11df4eaa1297384fcadbe1ae2da05
Author: jreback <jeff@reback.net>
Date:   Mon Jun 8 20:31:26 2015 -0400

    TST: only test for np.float128 if np supports

diff --git a/pandas/tests/test_nanops.py b/pandas/tests/test_nanops.py
index 951a693d2..a903b76b3 100644
--- a/pandas/tests/test_nanops.py
+++ b/pandas/tests/test_nanops.py
@@ -340,7 +340,12 @@ class TestnanopsDataFrame(tm.TestCase):
                 self.assertTrue(result.dtype == np.float64)
 
     def test_returned_dtype(self):
-        for dtype in [np.int16, np.int32, np.int64, np.float32, np.float64, np.float128]:
+
+        dtypes = [np.int16, np.int32, np.int64, np.float32, np.float64]
+        if hasattr(np,'float128'):
+            dtypes.append(np.float128)
+
+        for dtype in dtypes:
             s = Series(range(10), dtype=dtype)
             group_a = ['mean', 'std', 'var', 'skew', 'kurt']
             group_b = ['min', 'max']
