commit f55dfedd1305b6eb201fc7c27f55eca8f7b8a204
Author: Chang She <changshe@gmail.com>
Date:   Sun Dec 9 12:43:30 2012 -0500

    BUG: mixed frequency alignment bug due to PeriodIndex.asfreq defaults #2448

diff --git a/pandas/tseries/plotting.py b/pandas/tseries/plotting.py
index 4d25e6bad..5fe01161c 100644
--- a/pandas/tseries/plotting.py
+++ b/pandas/tseries/plotting.py
@@ -89,7 +89,7 @@ def _maybe_resample(series, ax, freq, plotf, kwargs):
     if ax_freq is not None and freq != ax_freq:
         if frequencies.is_superperiod(freq, ax_freq):  # upsample input
             series = series.copy()
-            series.index = series.index.asfreq(ax_freq)
+            series.index = series.index.asfreq(ax_freq, how='s')
             freq = ax_freq
         elif _is_sup(freq, ax_freq):  # one is weekly
             how = kwargs.pop('how', 'last')
@@ -157,7 +157,7 @@ def _replot_ax(ax, freq, plotf, kwargs):
     if data is not None:
         for series, kwds in data:
             series = series.copy()
-            idx = series.index.asfreq(freq)
+            idx = series.index.asfreq(freq, how='S')
             series.index = idx
             ax._plot_data.append(series)
             args = _maybe_mask(series)
diff --git a/pandas/tseries/tests/test_plotting.py b/pandas/tseries/tests/test_plotting.py
index 3bc6a2243..bdc48e08d 100644
--- a/pandas/tseries/tests/test_plotting.py
+++ b/pandas/tseries/tests/test_plotting.py
@@ -616,6 +616,21 @@ class TestTSPlot(unittest.TestCase):
         for l in ax.get_lines():
             self.assert_(PeriodIndex(data=l.get_xdata()).freq == 'D')
 
+    @slow
+    def test_mixed_freq_alignment(self):
+        import matplotlib.pyplot as plt
+        ts_ind = date_range('2012-01-01 13:00', '2012-01-02', freq='H')
+        ts_data = np.random.randn(12)
+
+        ts = Series(ts_data, index=ts_ind)
+        ts2 = ts.asfreq('T').interpolate()
+
+        plt.close('all')
+        ax = ts.plot()
+        ts2.plot(style='r')
+
+        self.assert_(ax.lines[0].get_xdata()[0] == ax.lines[1].get_xdata()[0])
+
     @slow
     def test_mixed_freq_lf_first(self):
         import matplotlib.pyplot as plt
