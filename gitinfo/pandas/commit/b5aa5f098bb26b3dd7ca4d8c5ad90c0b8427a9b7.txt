commit b5aa5f098bb26b3dd7ca4d8c5ad90c0b8427a9b7
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Tue Jan 17 19:18:09 2012 -0500

    ENH: add keys method to HDFStore

diff --git a/pandas/io/pytables.py b/pandas/io/pytables.py
index 11fa5e888..531c2be14 100644
--- a/pandas/io/pytables.py
+++ b/pandas/io/pytables.py
@@ -140,6 +140,13 @@ class HDFStore(object):
 
         return output
 
+    def keys(self):
+        """
+        Return a (potentially unordered) list of the keys corresponding to the
+        objects stored in the HDFStore
+        """
+        return self.handle.root._v_children.keys()
+
     def open(self, mode='a', warn=True):
         """
         Open the file in the specified mode
diff --git a/pandas/io/tests/test_pytables.py b/pandas/io/tests/test_pytables.py
index 88aadbefe..f6070fb2b 100644
--- a/pandas/io/tests/test_pytables.py
+++ b/pandas/io/tests/test_pytables.py
@@ -30,12 +30,13 @@ class TesttHDFStore(unittest.TestCase):
         self.store.close()
         os.remove(self.path)
 
-    def test_len(self):
+    def test_len_keys(self):
         self.store['a'] = tm.makeTimeSeries()
         self.store['b'] = tm.makeStringSeries()
         self.store['c'] = tm.makeDataFrame()
         self.store['d'] = tm.makePanel()
         self.assertEquals(len(self.store), 4)
+        self.assert_(set(self.store.keys()) == set(['a', 'b', 'c', 'd']))
 
     def test_repr(self):
         repr(self.store)
