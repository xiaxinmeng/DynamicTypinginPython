commit ea487fc9b197285f25b066450c46fc456db09e2a
Author: gfyoung <gfyoung17@gmail.com>
Date:   Sun Jul 16 23:19:27 2017 -0700

    DOC: Clarify 'it' in aggregate doc (#16989)
    
    Closes gh-16988.

diff --git a/pandas/core/generic.py b/pandas/core/generic.py
index e4e2e0093..f12592fea 100644
--- a/pandas/core/generic.py
+++ b/pandas/core/generic.py
@@ -3152,7 +3152,7 @@ it is assumed to be aliases for the column names.')
     (e.g., np.mean(arr_2d, axis=0)) as opposed to
     mimicking the default Numpy behavior (e.g., np.mean(arr_2d)).
 
-    agg is an alias for aggregate. Use it.
+    `agg` is an alias for `aggregate`. Use the alias.
 
     Returns
     -------
