commit 35253aaa36462a17ba46a7fa7a7a0f655654821e
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Fri Oct 14 19:52:43 2011 -0400

    ENH: use level name when calling unstack

diff --git a/pandas/core/reshape.py b/pandas/core/reshape.py
index 3fe653819..ebaa82d06 100644
--- a/pandas/core/reshape.py
+++ b/pandas/core/reshape.py
@@ -61,8 +61,8 @@ class _Unstacker(object):
         self.new_index_levels = list(index.levels)
         self.new_index_names = list(index.names)
 
-        self.removed_name = self.new_index_names.pop(level)
-        self.removed_level = self.new_index_levels.pop(level)
+        self.removed_name = self.new_index_names.pop(self.level)
+        self.removed_level = self.new_index_levels.pop(self.level)
 
         v = self.level
         lshape = self.index.levshape
diff --git a/pandas/tests/test_multilevel.py b/pandas/tests/test_multilevel.py
index b6cfac1a7..f4a93a5c0 100644
--- a/pandas/tests/test_multilevel.py
+++ b/pandas/tests/test_multilevel.py
@@ -377,6 +377,11 @@ class TestMultiLevel(unittest.TestCase):
         restacked = unstacked.stack()
         self.assertEquals(restacked.index.names, self.frame.index.names)
 
+    def test_unstack_level_name(self):
+        result = self.frame.unstack('second')
+        expected = self.frame.unstack(level=1)
+        assert_frame_equal(result, expected)
+
     def test_groupby_transform(self):
         s = self.frame['A']
         grouper = s.index.get_level_values(0)
diff --git a/pandas/tools/pivot.py b/pandas/tools/pivot.py
new file mode 100644
index 000000000..8b1378917
--- /dev/null
+++ b/pandas/tools/pivot.py
@@ -0,0 +1 @@
+
