commit 3824af12fb41ef6b488cde670daa90a751931f2e
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Wed Jul 11 18:29:21 2012 -0400

    BUG: override ndarray.tolist in Index for MultiIndex compat, close #1576

diff --git a/RELEASE.rst b/RELEASE.rst
index 2c7e3d026..10bfc89cb 100644
--- a/RELEASE.rst
+++ b/RELEASE.rst
@@ -57,6 +57,7 @@ pandas 0.8.1
   - Fix resampling of tz-aware time series with "anchored" freq (#1591)
   - Fix DataFrame.rank error on integer data (#1589)
   - Selection of multiple SparseDataFrame columns by list in __getitem__ (#1585)
+  - Override Index.tolist for compatibility with MultiIndex (#1576)
 
 pandas 0.8.0
 ============
diff --git a/pandas/core/index.py b/pandas/core/index.py
index 44c26167a..a3733ef8d 100644
--- a/pandas/core/index.py
+++ b/pandas/core/index.py
@@ -150,6 +150,12 @@ class Index(np.ndarray):
     def _assert_can_do_setop(self, other):
         return True
 
+    def tolist(self):
+        """
+        Overridden version of ndarray.tolist
+        """
+        return list(self.values)
+
     @property
     def dtype(self):
         return self.values.dtype
diff --git a/pandas/tests/test_index.py b/pandas/tests/test_index.py
index b8e3b5071..7f0745dab 100644
--- a/pandas/tests/test_index.py
+++ b/pandas/tests/test_index.py
@@ -1571,6 +1571,10 @@ class TestMultiIndex(unittest.TestCase):
                                    [0, 1, 2, 0, 0, 1, 2]])
         self.assert_(index.has_duplicates)
 
+    def test_tolist(self):
+        result = self.index.tolist()
+        exp = list(self.index.values)
+        self.assertEqual(result, exp)
 
 
 def test_get_combined_index():
