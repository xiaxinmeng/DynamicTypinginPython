commit a152c30a41ca944189b3ad2edf0c98ea8c4fecc2
Author: jbrockmendel <jbrockmendel@gmail.com>
Date:   Tue Feb 25 18:07:33 2020 -0800

    PERF: lazify consolidate_check and dont consolidate in get_slice (#32224)

diff --git a/pandas/core/internals/managers.py b/pandas/core/internals/managers.py
index d4f9c15a9..329bfdf54 100644
--- a/pandas/core/internals/managers.py
+++ b/pandas/core/internals/managers.py
@@ -141,7 +141,7 @@ class BlockManager(PandasObject):
         if do_integrity_check:
             self._verify_integrity()
 
-        self._consolidate_check()
+        self._known_consolidated = False
 
         self._rebuild_blknos_and_blklocs()
 
@@ -726,7 +726,6 @@ class BlockManager(PandasObject):
         new_axes[axis] = new_axes[axis][slobj]
 
         bm = type(self)(new_blocks, new_axes, do_integrity_check=False)
-        bm._consolidate_inplace()
         return bm
 
     def __contains__(self, item) -> bool:
