commit e7b6a25bdee1adf4fd26b878809a6c840a51c932
Author: Thomas Li <47963215+lithomas1@users.noreply.github.com>
Date:   Fri Jan 24 19:17:38 2020 -0800

    CI: Fix Assign CI not working with quotes (#31237)

diff --git a/.github/workflows/assign.yml b/.github/workflows/assign.yml
index 019ecfc48..a6d3f1f38 100644
--- a/.github/workflows/assign.yml
+++ b/.github/workflows/assign.yml
@@ -7,9 +7,8 @@ jobs:
   one:
     runs-on: ubuntu-latest
     steps:
-      - name:
-        run: |
-            if [[ "${{ github.event.comment.body }}" == "take" ]]; then
-                echo "Assigning issue ${{ github.event.issue.number }} to ${{ github.event.comment.user.login }}"
-                curl -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" -d '{"assignees": ["${{ github.event.comment.user.login }}"]}' https://api.github.com/repos/${{ github.repository }}/issues/${{ github.event.issue.number }}/assignees
-            fi
+    - if: github.event.comment.body == 'take'
+      name:
+      run: |
+        echo "Assigning issue ${{ github.event.issue.number }} to ${{ github.event.comment.user.login }}"
+        curl -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" -d '{"assignees": ["${{ github.event.comment.user.login }}"]}' https://api.github.com/repos/${{ github.repository }}/issues/${{ github.event.issue.number }}/assignees
