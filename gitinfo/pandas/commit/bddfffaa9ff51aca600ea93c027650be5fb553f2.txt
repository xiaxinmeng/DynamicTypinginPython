commit bddfffaa9ff51aca600ea93c027650be5fb553f2
Author: jreback <jeff@reback.net>
Date:   Wed Feb 13 21:41:45 2013 -0500

    ENH: return dtype on invalid function in where is now float64

diff --git a/pandas/core/internals.py b/pandas/core/internals.py
index 7267ee047..bfed5dbcd 100644
--- a/pandas/core/internals.py
+++ b/pandas/core/internals.py
@@ -496,7 +496,7 @@ class Block(object):
                                     % repr(o))
                 else:
                     # return the values
-                    result = np.empty(v.shape,dtype='O')
+                    result = np.empty(v.shape,dtype='float64')
                     result.fill(np.nan)
                     return result
 
