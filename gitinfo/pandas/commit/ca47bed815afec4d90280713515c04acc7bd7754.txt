commit ca47bed815afec4d90280713515c04acc7bd7754
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Tue Jan 17 16:23:57 2012 -0500

    BUG: BlockManager must have __nonzero__ -> True for pickling of empty DataFrame
    to work correctly, GH #640

diff --git a/pandas/core/internals.py b/pandas/core/internals.py
index 1071ebcc1..be00e8abb 100644
--- a/pandas/core/internals.py
+++ b/pandas/core/internals.py
@@ -284,6 +284,9 @@ class BlockManager(object):
         if do_integrity_check:
             self._verify_integrity()
 
+    def __nonzero__(self):
+        return True
+
     @property
     def ndim(self):
         return len(self.axes)
diff --git a/pandas/sparse/tests/test_sparse.py b/pandas/sparse/tests/test_sparse.py
index 5b7136844..4cf07bc60 100644
--- a/pandas/sparse/tests/test_sparse.py
+++ b/pandas/sparse/tests/test_sparse.py
@@ -766,6 +766,7 @@ class TestSparseDataFrame(TestCase, test_frame.SafeForSparse):
             unpickled = pickle.loads(pickled)
             assert_sp_frame_equal(frame, unpickled)
 
+        _test_roundtrip(SparseDataFrame())
         self._check_all(_test_roundtrip)
 
     def test_dense_to_sparse(self):
diff --git a/pandas/tests/test_frame.py b/pandas/tests/test_frame.py
index e4cb7db47..b335fe8dd 100644
--- a/pandas/tests/test_frame.py
+++ b/pandas/tests/test_frame.py
@@ -1497,6 +1497,10 @@ class TestDataFrame(unittest.TestCase, CheckIndexing,
         # buglet
         self.mixed_frame._data.ndim
 
+        # empty
+        unpickled = pickle.loads(pickle.dumps(self.empty))
+        repr(unpickled)
+
     def test_to_dict(self):
         test_data = {
                 'A' : {'1' : 1, '2' : 2},
