commit 0dc93cd712ae1a1f0e862331ec31791a451a422a
Author: ShilpaSugan <51447420+ShilpaSugan@users.noreply.github.com>
Date:   Tue Mar 3 12:31:30 2020 +0000

    TST: add message check to pytest.raises (tests/arrays/test_boolean.py) (#32397)

diff --git a/pandas/tests/arrays/test_boolean.py b/pandas/tests/arrays/test_boolean.py
index d14d6f3ff..f4b466f48 100644
--- a/pandas/tests/arrays/test_boolean.py
+++ b/pandas/tests/arrays/test_boolean.py
@@ -248,7 +248,11 @@ def test_coerce_to_numpy_array():
     tm.assert_numpy_array_equal(result, expected)
     # with missing values will raise error
     arr = pd.array([True, False, None], dtype="boolean")
-    with pytest.raises(ValueError):
+    msg = (
+        "cannot convert to 'bool'-dtype NumPy array with missing values. "
+        "Specify an appropriate 'na_value' for this dtype."
+    )
+    with pytest.raises(ValueError, match=msg):
         np.array(arr, dtype="bool")
 
 
