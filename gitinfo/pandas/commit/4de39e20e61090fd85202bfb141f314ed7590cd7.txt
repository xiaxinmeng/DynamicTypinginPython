commit 4de39e20e61090fd85202bfb141f314ed7590cd7
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Mon Jan 3 09:29:11 2011 -0500

    updated NumPy version in PyPI info

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index 9adbc4d6e..596ec459b 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -36,6 +36,20 @@ def arith_method(func, name):
 
     return f
 
+def comp_method(func, name):
+    def f(self, other):
+        if isinstance(other, DataFrame):    # Another DataFrame
+            return self._combine_frame(other, func)
+        elif isinstance(other, Series):
+            return self._combine_series(other, func)
+        else:
+            return self._combine_const(other, func)
+
+    f.__name__ = name
+    f.__doc__ = 'Wrapper for arithmetic method %s' % name
+
+    return f
+
 #-------------------------------------------------------------------------------
 # DataFrame class
 
diff --git a/pandas/version.py b/pandas/version.py
index 661ebe4d1..c442786e5 100644
--- a/pandas/version.py
+++ b/pandas/version.py
@@ -1,3 +1,3 @@
 from datetime import datetime
 
-version = '0.3.0.beta'
+version = '0.3.0.beta2'
diff --git a/setup.py b/setup.py
index 229eefb4e..4e8387074 100644
--- a/setup.py
+++ b/setup.py
@@ -26,7 +26,7 @@ accomplish the following:
 
 Note
 ----
-Windows binaries built against NumPy 1.4.0+
+Windows binaries built against NumPy 1.5.1
 """
 
 DISTNAME = 'pandas'
