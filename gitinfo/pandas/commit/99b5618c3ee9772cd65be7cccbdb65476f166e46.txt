commit 99b5618c3ee9772cd65be7cccbdb65476f166e46
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Mon Apr 9 11:12:09 2012 -0400

    BUG: override Series.reshape to return ndarray when new shape has multiple dimensions, close #989

diff --git a/pandas/core/series.py b/pandas/core/series.py
index df62991ab..60bf8694a 100644
--- a/pandas/core/series.py
+++ b/pandas/core/series.py
@@ -572,6 +572,15 @@ copy : boolean, default False
         casted = com._astype_nansafe(self.values, dtype)
         return self._constructor(casted, index=self.index, name=self.name)
 
+    def reshape(self, newshape, order='C'):
+        """
+        See numpy.ndarray.reshape
+        """
+        if isinstance(newshape, tuple) and len(newshape) > 1:
+            return self.values.reshape(newshape, order=order)
+        else:
+            return ndarray.reshape(self, newshape, order)
+
     def get(self, label, default=None):
         """
         Returns value occupying requested label, default to specified
diff --git a/pandas/tests/test_series.py b/pandas/tests/test_series.py
index e32bd5bfa..bc14d69b7 100644
--- a/pandas/tests/test_series.py
+++ b/pandas/tests/test_series.py
@@ -617,6 +617,18 @@ class TestSeries(unittest.TestCase, CheckNameIntegration):
         self.assertRaises(Exception, self.ts.__setitem__,
                           [5, slice(None, None)], 2)
 
+    def test_reshape_2d_return_array(self):
+        x = Series(np.random.random(201), name='x')
+        result = x.reshape((-1,1))
+        self.assert_(not isinstance(result, Series))
+
+        result2 = np.reshape(x, (-1,1))
+        self.assert_(not isinstance(result, Series))
+
+        result = x[:, None]
+        expected = x.reshape((-1,1))
+        assert_almost_equal(result, expected)
+
     def test_basic_getitem_with_labels(self):
         indices = self.ts.index[[5, 10, 15]]
 
