commit 1f01e49736b2a499ac37e80e6abaab2763373254
Author: unknown <Aman@shikha.(none)>
Date:   Tue Oct 18 23:57:29 2011 -0400

    fixed #251 used empty = self[col].count() == 0

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index 889b71408..b0566e5f2 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -2871,7 +2871,7 @@ class DataFrame(NDFrame):
             x = range(len(self))
 
         for i, col in enumerate(_try_sort(self.columns)):
-            empty = np.all(np.isnan(self[col].values))
+            empty = self[col].count() == 0
             y = self[col].values if not empty else np.zeros(x.shape)
             if subplots:
                 ax = axes[i]
