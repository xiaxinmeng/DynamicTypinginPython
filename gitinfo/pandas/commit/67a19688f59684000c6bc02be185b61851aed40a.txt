commit 67a19688f59684000c6bc02be185b61851aed40a
Author: Skipper Seabold <jsseabold@gmail.com>
Date:   Tue May 14 15:39:13 2013 -0400

    TST: Test Categorical constructor with levels=None

diff --git a/pandas/tests/test_factor.py b/pandas/tests/test_factor.py
index de2fcaa94..2b1f41dea 100644
--- a/pandas/tests/test_factor.py
+++ b/pandas/tests/test_factor.py
@@ -111,6 +111,12 @@ class TestCategorical(unittest.TestCase):
 
         self.assert_(np.array_equal(com.isnull(cat), labels == -1))
 
+    def test_levels_none(self):
+        factor = Categorical(['a', 'b', 'b', 'a',
+                              'a', 'c', 'c', 'c'])
+        self.assert_(factor.equals(self.factor))
+
+
 if __name__ == '__main__':
     import nose
     nose.runmodule(argv=[__file__, '-vvs', '-x', '--pdb', '--pdb-failure'],
