commit 75d5b48221e8d5d871d3127c1110fc1b53e0ca26
Author: Tom Augspurger <TomAugspurger@users.noreply.github.com>
Date:   Mon Feb 11 07:08:15 2019 -0600

    Revert "BLD: prevent asv from calling sys.stdin.close() by using different launch method (#25237)" (#25253)
    
    This reverts commit f67b7fd2c75db951e63513003b1c6d3d1161a4b2.

diff --git a/azure-pipelines.yml b/azure-pipelines.yml
index c86d5c507..f0567d766 100644
--- a/azure-pipelines.yml
+++ b/azure-pipelines.yml
@@ -104,7 +104,7 @@ jobs:
       if git diff upstream/master --name-only | grep -q "^asv_bench/"; then
           cd asv_bench
           asv machine --yes
-          ASV_OUTPUT="$(asv run --quick --show-stderr --python=same --launch-method=spawn)"
+          ASV_OUTPUT="$(asv dev)"
           if [[ $(echo "$ASV_OUTPUT" | grep "failed") ]]; then
               echo "##vso[task.logissue type=error]Benchmarks run with errors"
               echo "$ASV_OUTPUT"
