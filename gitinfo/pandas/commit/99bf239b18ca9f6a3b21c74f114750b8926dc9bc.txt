commit 99bf239b18ca9f6a3b21c74f114750b8926dc9bc
Author: jreback <jeff@reback.net>
Date:   Sun Sep 7 12:21:44 2014 -0400

    COMPAT:  dtype on windows related to GH8172

diff --git a/pandas/src/join.pyx b/pandas/src/join.pyx
index f8eef55a8..8a9cf0137 100644
--- a/pandas/src/join.pyx
+++ b/pandas/src/join.pyx
@@ -116,6 +116,8 @@ def left_outer_join(ndarray[int64_t] left, ndarray[int64_t] right,
         else:
             rev, _ = groupsort_indexer(left_indexer, len(left))
 
+        if rev.dtype != np.int_:
+              rev = rev.astype(np.int_)
         right_indexer = right_indexer.take(rev)
         left_indexer = left_indexer.take(rev)
 
