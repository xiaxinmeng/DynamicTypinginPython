commit 4956e6bcd4fff552c0674839ffadc04fd493cbb8
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Thu Jun 23 15:03:16 2011 -0400

    BUG: DataFrame.ix[0] should return view

diff --git a/RELEASE.rst b/RELEASE.rst
index f38589252..2e7645bc5 100644
--- a/RELEASE.rst
+++ b/RELEASE.rst
@@ -126,6 +126,9 @@ Release notes
 * Fixed __truediv__ issue in `DataFrame`
 * Fixed `DataFrame.toCSV` bug preventing IO round trips in some cases
 * Fixed bug in `Series.plot` causing matplotlib to barf in exceptional cases
+* Disabled `Index` objects from being hashable, like ndarrays
+* Added `__ne__` implementation to `Index` so that operations like ts[ts != idx]
+  will work
 
 Thanks
 ------
diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index 8923a91cb..1c48a3307 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -2506,7 +2506,10 @@ class DataFrame(PandasGeneric):
             if isinstance(key, int):
                 idx = self.index[key]
 
-            return self.xs(idx)
+            if self._is_mixed_type:
+                return self.xs(idx)
+            else:
+                return self.xs(idx, copy=False)
         else:
             col = key
             if isinstance(key, int):
diff --git a/pandas/core/tests/test_frame.py b/pandas/core/tests/test_frame.py
index 4c7e08411..b77840578 100644
--- a/pandas/core/tests/test_frame.py
+++ b/pandas/core/tests/test_frame.py
@@ -233,6 +233,21 @@ class CheckIndexing(object):
         # single column
         assert_series_equal(ix[:, 'A'], f['A'])
 
+        # return view
+        exp = f.copy()
+        exp.values[5] = 4
+        ix[5][:] = 4
+        assert_frame_equal(exp, f)
+
+        exp.values[:, 1] = 6
+        ix[:, 1][:] = 6
+        assert_frame_equal(exp, f)
+
+        # slice of mixed-frame
+        xs = self.mixed_frame.ix[5]
+        exp = self.mixed_frame.xs(self.mixed_frame.index[5])
+        assert_series_equal(xs, exp)
+
     def test_getitem_fancy_scalar(self):
         f = self.frame
         ix = f.ix
