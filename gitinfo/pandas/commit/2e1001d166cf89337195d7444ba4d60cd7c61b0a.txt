commit 2e1001d166cf89337195d7444ba4d60cd7c61b0a
Author: y-p <yoval@gmx.com>
Date:   Tue Oct 9 01:02:45 2012 +0200

    ENH: add is_number() helper to pd.core.common

diff --git a/pandas/core/common.py b/pandas/core/common.py
index c4e90a0c5..086ceab0c 100644
--- a/pandas/core/common.py
+++ b/pandas/core/common.py
@@ -753,6 +753,9 @@ def is_iterator(obj):
     # python 3 generators have __next__ instead of next
     return hasattr(obj, 'next') or hasattr(obj, '__next__')
 
+def is_number(obj):
+    return isinstance(obj, (np.number, int, long, float))
+
 def is_integer_dtype(arr_or_dtype):
     if isinstance(arr_or_dtype, np.dtype):
         tipo = arr_or_dtype.type
