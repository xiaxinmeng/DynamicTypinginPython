commit 6e75c0d539f4731df8d96d2eb11c8f8ec016dbb6
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Fri Oct 7 14:31:35 2011 -0400

    BUG: isnull/notnull fail on other objects like datetime

diff --git a/pandas/core/common.py b/pandas/core/common.py
index 92f2afa98..ad9ddec52 100644
--- a/pandas/core/common.py
+++ b/pandas/core/common.py
@@ -53,8 +53,8 @@ def isnull(obj):
     elif isinstance(obj, PandasObject):
         # TODO: optimize for DataFrame, etc.
         return obj.apply(isnull)
-    else:  # pragma: no cover
-        raise TypeError('cannot handle %s type' % type(obj))
+    else:
+        return obj is None
 
 def notnull(obj):
     '''
@@ -69,9 +69,10 @@ def notnull(obj):
     -------
     boolean ndarray or boolean
     '''
-    if np.isscalar(obj) or obj is None:
-        return not lib.checknull(obj)
-    return -isnull(obj)
+    res = isnull(obj)
+    if np.isscalar(res):
+        return not res
+    return -res
 
 def _pickle_array(arr):
     arr = arr.view(np.ndarray)
diff --git a/pandas/tests/test_common.py b/pandas/tests/test_common.py
index 9765edf51..4c200bacb 100644
--- a/pandas/tests/test_common.py
+++ b/pandas/tests/test_common.py
@@ -1,3 +1,5 @@
+from datetime import datetime
+
 import unittest
 
 from pandas import Series, DataFrame
@@ -38,6 +40,10 @@ def test_isnull():
     expected = result.apply(isnull)
     tm.assert_frame_equal(result, expected)
 
+def test_isnull_datetime():
+    assert (not isnull(datetime.now()))
+    assert notnull(datetime.now())
+
 def test_any_none():
     assert(common._any_none(1, 2, 3, None))
     assert(not common._any_none(1, 2, 3, 4))
