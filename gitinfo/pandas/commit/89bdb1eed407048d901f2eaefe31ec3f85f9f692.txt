commit 89bdb1eed407048d901f2eaefe31ec3f85f9f692
Author: Dieter Vandenbussche <dvandenbussche@axioma.com>
Date:   Sat Mar 3 14:25:58 2012 -0500

    Fix plotting with datetime.date index values

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index ff5cfff9e..aded67a77 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -3828,7 +3828,7 @@ class DataFrame(NDFrame):
 
         if kind == 'line':
             if use_index:
-                if self.index.is_numeric() or self.index.is_datetime():
+                if self.index.is_numeric() or self.index.is_datetype():
                     """
                     Matplotlib supports numeric values or datetime objects as
                     xaxis values. Taking LBYL approach here, by the time
diff --git a/pandas/core/index.py b/pandas/core/index.py
index ea6e15612..e0776c05f 100644
--- a/pandas/core/index.py
+++ b/pandas/core/index.py
@@ -1,6 +1,6 @@
 # pylint: disable=E1101,E1103,W0232
 
-from datetime import time, datetime
+from datetime import time, datetime, date
 from itertools import izip
 
 import numpy as np
@@ -145,9 +145,9 @@ class Index(np.ndarray):
     def is_numeric(self):
         return issubclass(self.dtype.type, np.number)
 
-    def is_datetime(self):
+    def is_datetype(self):
         for key in self.values:
-            if not isinstance(key, datetime):
+            if not isinstance(key, (datetime, date)):
                 return False
         return True
 
diff --git a/pandas/core/series.py b/pandas/core/series.py
index fe91a6ba9..fa51be1a4 100644
--- a/pandas/core/series.py
+++ b/pandas/core/series.py
@@ -2076,7 +2076,7 @@ copy : boolean, default False
 
         if kind == 'line':
             if use_index:
-                if self.index.is_numeric() or self.index.is_datetime():
+                if self.index.is_numeric() or self.index.is_datetype():
                     """
                     Matplotlib supports numeric values or datetime objects as
                     xaxis values. Taking LBYL approach here, by the time
