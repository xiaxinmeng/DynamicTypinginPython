commit c7af4ae94092110b1872750d89ce55bff131b39f
Author: Joris Van den Bossche <jorisvandenbossche@gmail.com>
Date:   Mon Mar 26 14:48:49 2018 +0200

    BUG: raise error when setting cached properties (#20487)

diff --git a/pandas/_libs/properties.pyx b/pandas/_libs/properties.pyx
index e3f16f224..0f2900619 100644
--- a/pandas/_libs/properties.pyx
+++ b/pandas/_libs/properties.pyx
@@ -37,6 +37,9 @@ cdef class CachedProperty(object):
             PyDict_SetItem(cache, self.name, val)
         return val
 
+    def __set__(self, obj, value):
+        raise AttributeError("Can't set attribute")
+
 
 cache_readonly = CachedProperty
 
diff --git a/pandas/tests/indexes/test_base.py b/pandas/tests/indexes/test_base.py
index 22ef2fe7a..ff9c86fbf 100644
--- a/pandas/tests/indexes/test_base.py
+++ b/pandas/tests/indexes/test_base.py
@@ -2056,6 +2056,11 @@ Index([u'a', u'bb', u'ccc', u'a', u'bb', u'ccc', u'a', u'bb', u'ccc', u'a',
         ser.index -= 1
         assert ser.index.name == "foo"
 
+    def test_cached_properties_not_settable(self):
+        idx = pd.Index([1, 2, 3])
+        with tm.assert_raises_regex(AttributeError, "Can't set attribute"):
+            idx.is_unique = False
+
 
 class TestMixedIntIndex(Base):
     # Mostly the tests from common.py for which the results differ
