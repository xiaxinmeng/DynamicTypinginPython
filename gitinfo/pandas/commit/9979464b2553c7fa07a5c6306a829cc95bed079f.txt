commit 9979464b2553c7fa07a5c6306a829cc95bed079f
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Fri Dec 7 18:35:44 2012 -0500

    BUG: user-access-control should be user_options and not options

diff --git a/setup.py b/setup.py
index a29f24ee4..15452e3e0 100755
--- a/setup.py
+++ b/setup.py
@@ -657,8 +657,8 @@ if suffix == '.pyx' and 'setuptools' in sys.modules:
 if _have_setuptools:
     setuptools_kwargs["test_suite"] = "nose.collector"
 
-options = {'bdist_wininst': {'user-access-control': 'auto'}}
-# options = {}
+user_options = {'bdist_wininst': {'user-access-control': 'auto'}}
+options = {}
 
 write_version_py()
 setup(name=DISTNAME,
@@ -705,4 +705,5 @@ setup(name=DISTNAME,
       classifiers=CLASSIFIERS,
       platforms='any',
       options=options,
+      user_options=user_options,
       **setuptools_kwargs)
