commit 0a5e328de4399ab23a106a2716102ccd34d22f2d
Author: y-p <yoval@gmx.com>
Date:   Mon Jan 13 08:39:09 2014 +0200

    BLD: match version tags when using git-describe to gen version string

diff --git a/setup.py b/setup.py
index 4e9dd94a7..1c82da240 100755
--- a/setup.py
+++ b/setup.py
@@ -203,7 +203,7 @@ if not ISRELEASED:
     pipe = None
     for cmd in ['git','git.cmd']:
         try:
-            pipe = subprocess.Popen([cmd, "describe", "--always"],
+            pipe = subprocess.Popen([cmd, "describe", "--always", "--match", "v[0-9]*"],
                                 stdout=subprocess.PIPE)
             (so,serr) = pipe.communicate()
             if pipe.returncode == 0:
