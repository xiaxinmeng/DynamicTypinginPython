commit eb184941ff07519dd9028f1f413699a968b11cc3
Author: y-p <yoval@gmx.com>
Date:   Sun Dec 16 13:02:57 2012 +0200

    BLD:  add pytables as dep in VBENCH travis build

diff --git a/ci/install.sh b/ci/install.sh
index 79268aeca..ce1b5f667 100755
--- a/ci/install.sh
+++ b/ci/install.sh
@@ -49,6 +49,11 @@ if [ x"$FULL_DEPS" == x"true" ]; then
 fi
 
 if [ x"$VBENCH" == x"true" ]; then
+    if [ ${TRAVIS_PYTHON_VERSION:0:1} == "2" ]; then
+        sudo apt-get $APT_ARGS install libhdf5-serial-dev;
+        pip install numexpr
+        pip install tables
+    fi
     pip $PIP_ARGS install sqlalchemy git+git://github.com/pydata/vbench.git;
 fi
 
