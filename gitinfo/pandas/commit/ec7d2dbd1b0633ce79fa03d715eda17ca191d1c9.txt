commit ec7d2dbd1b0633ce79fa03d715eda17ca191d1c9
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Sun Sep 9 19:29:55 2012 -0400

    BUG: fix broken interaction between Series.any/all and np.nansum

diff --git a/pandas/core/series.py b/pandas/core/series.py
index 4130e6120..56688eae9 100644
--- a/pandas/core/series.py
+++ b/pandas/core/series.py
@@ -244,7 +244,8 @@ def _unbox(func):
     def f(self, *args, **kwargs):
         result = func(self, *args, **kwargs)
         if isinstance(result, np.ndarray) and result.ndim == 0:
-            return result.item()
+            # return NumPy type
+            return result.dtype.type(result.item())
         else:  # pragma: no cover
             return result
     f.__name__ = func.__name__
diff --git a/pandas/tests/test_series.py b/pandas/tests/test_series.py
index e45cdb764..aa8e140eb 100644
--- a/pandas/tests/test_series.py
+++ b/pandas/tests/test_series.py
@@ -203,6 +203,10 @@ class TestNanops(unittest.TestCase):
         df = DataFrame(np.empty((10, 0)))
         self.assert_((df.sum(1) == 0).all())
 
+    def test_nansum_buglet(self):
+        s = Series([1.0, np.nan], index=[0,1])
+        result = np.nansum(s)
+        assert_almost_equal(result, 1)
 
 class SafeForSparse(object):
     pass
