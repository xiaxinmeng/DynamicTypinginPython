commit 04caa569e5316e1a6526f0c78142a677ca4b2ee9
Author: alphaCTzo7G <29691914+alphaCTzo7G@users.noreply.github.com>
Date:   Thu Jul 5 19:35:38 2018 -0700

    Fixes example code in docstring of pandas.DataFrame.query (#21731)

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index 7b5f31031..c4e0849ae 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -2841,9 +2841,9 @@ class DataFrame(NDFrame):
 
         Examples
         --------
-        >>> from numpy.random import randn
-        >>> from pandas import DataFrame
-        >>> df = pd.DataFrame(randn(10, 2), columns=list('ab'))
+        >>> import numpy as np
+        >>> import pandas as pd
+        >>> df = pd.DataFrame(np.random.randn(10, 2), columns=list('ab'))
         >>> df.query('a > b')
         >>> df[df.a > df.b]  # same result as the previous expression
         """
