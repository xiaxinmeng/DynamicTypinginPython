commit 8cd25bac752dbf5128215116a1e18dbf38e42d2e
Author: Adam Klein <adamklein@gmail.com>
Date:   Wed Mar 21 17:39:47 2012 -0400

    BUG: fixed boolean comparison of indices resulting in unwanted wrapping

diff --git a/pandas/core/index.py b/pandas/core/index.py
index 212e00ca8..e9209de16 100644
--- a/pandas/core/index.py
+++ b/pandas/core/index.py
@@ -15,12 +15,16 @@ __all__ = ['Index']
 
 def _indexOp(opname):
     """
-    Wrapper function for Series arithmetic operations, to avoid
+    Wrapper function for index comparison operations, to avoid
     code duplication.
     """
     def wrapper(self, other):
         func = getattr(self.view(np.ndarray), opname)
-        return func(other)
+        result = func(other)
+        try:
+            return result.view(np.ndarray)
+        except:
+            return result
     return wrapper
 
 
diff --git a/pandas/tests/test_index.py b/pandas/tests/test_index.py
index 5a0cfbe43..fac6a1688 100644
--- a/pandas/tests/test_index.py
+++ b/pandas/tests/test_index.py
@@ -417,6 +417,16 @@ class TestIndex(unittest.TestCase):
         self.assert_(len(result) == 0)
         self.assert_(result.dtype == np.bool_)
 
+    def test_boolean_cmp(self):
+        values = [1,2,3,4]
+
+        idx = Index(values)
+        res = (idx == values)
+
+        self.assert_( res.all() )
+        self.assert_( res.dtype == 'bool' )
+        self.assert_( not isinstance(res, Index) )
+
 class TestInt64Index(unittest.TestCase):
 
     def setUp(self):
