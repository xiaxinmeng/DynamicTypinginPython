commit d823bce480987578d079ce9de28dc6ab60d2e26c
Author: Chang She <changshe@gmail.com>
Date:   Fri Nov 2 19:28:41 2012 -0400

    BUG: error plotting inferred freq with N>1 #2116

diff --git a/pandas/tools/plotting.py b/pandas/tools/plotting.py
index 0c26533ab..25bcff3c5 100644
--- a/pandas/tools/plotting.py
+++ b/pandas/tools/plotting.py
@@ -1025,6 +1025,7 @@ class LinePlot(MPLPlot):
                 freq = getattr(data.index, 'inferred_freq', None)
             if isinstance(freq, DateOffset):
                 freq = freq.rule_code
+            freq = get_base_alias(freq)
             freq = get_period_alias(freq)
 
             if freq is None:
diff --git a/pandas/tseries/tests/test_plotting.py b/pandas/tseries/tests/test_plotting.py
index 8d2cd17bd..1e11bd1ce 100644
--- a/pandas/tseries/tests/test_plotting.py
+++ b/pandas/tseries/tests/test_plotting.py
@@ -65,6 +65,12 @@ class TestTSPlot(unittest.TestCase):
         df2.plot()
         plt.close('all')
 
+        # N > 1
+        idx = date_range('2008-1-1 00:15:00', freq='15T', periods=10)
+        idx = DatetimeIndex(idx.values, freq=None)
+        df = DataFrame(np.random.randn(len(idx), 3), index=idx)
+        df.plot()
+
     @slow
     def test_tsplot(self):
         from pandas.tseries.plotting import tsplot
