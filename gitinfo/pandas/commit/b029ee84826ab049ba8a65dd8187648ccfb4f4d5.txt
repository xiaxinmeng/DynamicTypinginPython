commit b029ee84826ab049ba8a65dd8187648ccfb4f4d5
Author: Jeff Reback <jeff@reback.net>
Date:   Wed Oct 28 08:35:41 2015 -0400

    BLD: add build_dist.sh script

diff --git a/build_dist.sh b/build_dist.sh
new file mode 100755
index 000000000..687e2eb58
--- /dev/null
+++ b/build_dist.sh
@@ -0,0 +1,20 @@
+#!/bin/bash
+
+# build the distribution
+LAST=`git tag --sort v:refname | grep -v rc | tail -1`
+
+echo "Building distribution for: $LAST"
+git checkout $LAST
+
+read -p "Ok to continue (y/n)? " answer
+case ${answer:0:1} in
+    y|Y )
+        echo "Building distribution"
+        python setup.py clean
+        python setup.py build_ext --inplace
+        python setup.py sdist --formats=zip,gztar
+    ;;
+    * )
+        echo "Not building distribution"
+    ;;
+esac
