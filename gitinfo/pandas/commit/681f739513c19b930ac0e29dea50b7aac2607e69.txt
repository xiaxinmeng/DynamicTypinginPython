commit 681f739513c19b930ac0e29dea50b7aac2607e69
Author: Jacques Kvam <jwkvam@gmail.com>
Date:   Sun Jun 17 18:16:08 2012 -0700

    make parsing position independent therefore more robust

diff --git a/pandas/io/data.py b/pandas/io/data.py
index 0cabd6885..a81b0c7cb 100644
--- a/pandas/io/data.py
+++ b/pandas/io/data.py
@@ -96,8 +96,7 @@ def get_quote_yahoo(symbols):
     for line in lines:
         fields = line.strip().split(',')
         for i,field in enumerate(fields):
-            # assumes change_pct is in the 5th index
-            if i == 5:
+            if field[-2:] == '%"':
                 data[header[i]].append(float(field.strip('"%')))
             elif field[0] == '"':
                 data[header[i]].append( field.strip('"'))
