commit 3571b3c2733f01e56005b8dd424228afc99ba0f3
Author: Jeff Reback <jeff@reback.net>
Date:   Sat Sep 5 12:46:14 2015 -0400

    COMPAT: test dtype fix for windows

diff --git a/pandas/tests/test_panel.py b/pandas/tests/test_panel.py
index 64edf2991..d45e6e50c 100644
--- a/pandas/tests/test_panel.py
+++ b/pandas/tests/test_panel.py
@@ -1231,6 +1231,9 @@ class TestPanel(tm.TestCase, PanelTests, CheckIndexing,
         dfb = DataFrame(np.array(np.arange(10, 22, dtype='int64')).reshape(4,3), columns=list("ABC"), index=index)
         p = Panel({'f':dfa, 'g':dfb})
         result = p.apply(lambda x: x.sum(), axis=0)
+
+        # on windows this will be in32
+        result = result.astype('int64')
         expected = p.sum(0)
         assert_frame_equal(result,expected)
 
