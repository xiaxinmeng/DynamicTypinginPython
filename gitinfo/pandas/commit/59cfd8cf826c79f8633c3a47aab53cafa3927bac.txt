commit 59cfd8cf826c79f8633c3a47aab53cafa3927bac
Author: Paul Reidy <paul_reidy@outlook.com>
Date:   Fri Sep 14 12:13:50 2018 +0100

    BUG: Incorrect addition of Week(weekday=6) to DatetimeIndex (#22695)
    
    * BUG: Incorrect addition of Week(weekday=6) to DatetimeIndex

diff --git a/pandas/tests/arithmetic/test_datetime64.py b/pandas/tests/arithmetic/test_datetime64.py
index a3fa4e6b8..b19cc61a2 100644
--- a/pandas/tests/arithmetic/test_datetime64.py
+++ b/pandas/tests/arithmetic/test_datetime64.py
@@ -1764,6 +1764,7 @@ def test_dt64_with_DateOffsets_relativedelta(klass):
     'MonthBegin', 'MonthEnd',
     'SemiMonthEnd', 'SemiMonthBegin',
     'Week', ('Week', {'weekday': 3}),
+    'Week', ('Week', {'weekday': 6}),
     'BusinessDay', 'BDay', 'QuarterEnd', 'QuarterBegin',
     'CustomBusinessDay', 'CDay', 'CBMonthEnd',
     'CBMonthBegin', 'BMonthBegin', 'BMonthEnd',
diff --git a/pandas/tseries/offsets.py b/pandas/tseries/offsets.py
index d4a8211c1..0a9931c46 100644
--- a/pandas/tseries/offsets.py
+++ b/pandas/tseries/offsets.py
@@ -1313,7 +1313,7 @@ class Week(DateOffset):
         base_period = dtindex.to_period(base)
         if self.n > 0:
             # when adding, dates on end roll to next
-            normed = dtindex - off
+            normed = dtindex - off + Timedelta(1, 'D') - Timedelta(1, 'ns')
             roll = np.where(base_period.to_timestamp(how='end') == normed,
                             self.n, self.n - 1)
         else:
