commit ed7a3d2a29572fb8b9f28e0e3bc4e05dcb8d8ccb
Author: Chang She <chang@lambdafoundry.com>
Date:   Thu Jun 7 23:47:55 2012 -0400

    BUG: isin method buggy on SparseSeries #1412

diff --git a/pandas/core/series.py b/pandas/core/series.py
index 1bb38aed3..9be6ae9df 100644
--- a/pandas/core/series.py
+++ b/pandas/core/series.py
@@ -2310,7 +2310,7 @@ copy : boolean, default False
         isin : Series (boolean dtype)
         """
         value_set = set(values)
-        result = lib.ismember(self, value_set)
+        result = lib.ismember(self.values, value_set)
         return Series(result, self.index, name=self.name)
 
     def between(self, left, right, inclusive=True):
diff --git a/pandas/sparse/tests/test_sparse.py b/pandas/sparse/tests/test_sparse.py
index be19b9574..7e19b7324 100644
--- a/pandas/sparse/tests/test_sparse.py
+++ b/pandas/sparse/tests/test_sparse.py
@@ -1271,6 +1271,11 @@ class TestSparseDataFrame(TestCase, test_frame.SafeForSparse):
         expected = df.add(df2, fill_value=0).to_sparse()
         assert_sp_frame_equal(result, expected)
 
+    def test_isin(self):
+        sparse_df = DataFrame({'flag': [1., 0., 1.]}).to_sparse(fill_value=0.)
+        xp = sparse_df[sparse_df.flag == 1.]
+        rs = sparse_df[sparse_df.flag.isin([1.])]
+        assert_frame_equal(xp, rs)
 
 def _dense_series_compare(s, f):
     result = f(s)
