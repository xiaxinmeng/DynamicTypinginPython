commit 45952c75b3a2789f55e44c04a37d952a6b8a6be3
Author: Phan Duc Nhat Minh <phanducnhatminh@gmail.com>
Date:   Wed Jun 24 04:13:51 2020 +0800

    Fix DataFrame/Series stack/unstack docs (#34927)

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index 4e8a75fe1..e5de9b428 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -6783,8 +6783,6 @@ NaN 12.3   33.0
             level(s) is (are) taken from the prescribed level(s) and
             the output is a DataFrame.
 
-        The new index levels are sorted.
-
         Parameters
         ----------
         level : int, str, list, default -1
@@ -7020,8 +7018,6 @@ NaN 12.3   33.0
         If the index is not a MultiIndex, the output will be a Series
         (the analogue of stack when the columns are not a MultiIndex).
 
-        The level involved will automatically get sorted.
-
         Parameters
         ----------
         level : int, str, or list of these, default -1 (last level)
diff --git a/pandas/core/series.py b/pandas/core/series.py
index 3674537c0..d8cf83081 100644
--- a/pandas/core/series.py
+++ b/pandas/core/series.py
@@ -3835,8 +3835,6 @@ Keep all original rows and also all original values
         """
         Unstack, also known as pivot, Series with MultiIndex to produce DataFrame.
 
-        The level involved will automatically get sorted.
-
         Parameters
         ----------
         level : int, str, or list of these, default last level
