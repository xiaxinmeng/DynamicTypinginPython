commit b8b6fe23b0a6adeb41c3997a1ca5ce77ed7f5657
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Fri Sep 23 21:08:03 2011 -0400

    ENH: don't boundscheck or wraparound

diff --git a/pandas/src/reindex.pyx b/pandas/src/reindex.pyx
index ab0fdba93..d65c8079c 100644
--- a/pandas/src/reindex.pyx
+++ b/pandas/src/reindex.pyx
@@ -559,8 +559,8 @@ def outer_join_indexer(ndarray[int64_t] left, ndarray[int64_t] right):
             lindexer[:count].copy(),
             rindexer[:count].copy())
 
-# @cython.wraparound(False)
-# @cython.boundscheck(False)
+@cython.wraparound(False)
+@cython.boundscheck(False)
 def take_axis0(ndarray[float64_t, ndim=2] values,
                ndarray[int32_t] indexer,
                out=None):
diff --git a/scripts/bench_join.py b/scripts/bench_join.py
index 40d637be8..a0b0bab76 100644
--- a/scripts/bench_join.py
+++ b/scripts/bench_join.py
@@ -135,7 +135,7 @@ def bench_python(n=100000, pct_overlap=0.20):
     import gc
     ns = [2, 3, 4, 5, 6]
     iterations = 50
-    K = 5
+    K = 1
     pct_overlap = 0.2
     kinds = ['outer', 'left', 'inner']
 
