commit d2fa4b42812093d147cd864ca366d6cccac2db30
Author: gfyoung <gfyoung17+GitHub@gmail.com>
Date:   Sat Jan 26 00:01:00 2019 -0800

    DOC: State that we support scalars in to_numeric (#24944)
    
    We support it and test it already.
    
    xref gh-24910.

diff --git a/pandas/core/tools/numeric.py b/pandas/core/tools/numeric.py
index 803723dab..79d8ee386 100644
--- a/pandas/core/tools/numeric.py
+++ b/pandas/core/tools/numeric.py
@@ -21,7 +21,7 @@ def to_numeric(arg, errors='raise', downcast=None):
 
     Parameters
     ----------
-    arg : list, tuple, 1-d array, or Series
+    arg : scalar, list, tuple, 1-d array, or Series
     errors : {'ignore', 'raise', 'coerce'}, default 'raise'
         - If 'raise', then invalid parsing will raise an exception
         - If 'coerce', then invalid parsing will be set as NaN
