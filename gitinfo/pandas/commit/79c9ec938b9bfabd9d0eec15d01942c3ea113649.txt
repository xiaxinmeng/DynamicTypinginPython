commit 79c9ec938b9bfabd9d0eec15d01942c3ea113649
Author: Simon Hawkins <simonjayhawkins@gmail.com>
Date:   Thu Mar 5 10:49:41 2020 +0000

    CLN: avoid _internal_get_values in groupby.generic (#32427)

diff --git a/pandas/core/groupby/generic.py b/pandas/core/groupby/generic.py
index fb935c906..ac522fc78 100644
--- a/pandas/core/groupby/generic.py
+++ b/pandas/core/groupby/generic.py
@@ -589,7 +589,7 @@ class SeriesGroupBy(GroupBy):
         """
         ids, _, _ = self.grouper.group_info
 
-        val = self.obj._internal_get_values()
+        val = self.obj._values
 
         codes, _ = algorithms.factorize(val, sort=False)
         sorter = np.lexsort((codes, ids))
@@ -657,7 +657,7 @@ class SeriesGroupBy(GroupBy):
             )
 
         ids, _, _ = self.grouper.group_info
-        val = self.obj._internal_get_values()
+        val = self.obj._values
 
         # groupby removes null keys from groupings
         mask = ids != -1
@@ -774,7 +774,7 @@ class SeriesGroupBy(GroupBy):
             Count of values within each group.
         """
         ids, _, ngroups = self.grouper.group_info
-        val = self.obj._internal_get_values()
+        val = self.obj._values
 
         mask = (ids != -1) & ~isna(val)
         ids = ensure_platform_int(ids)
