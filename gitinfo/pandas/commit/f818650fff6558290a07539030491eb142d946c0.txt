commit f818650fff6558290a07539030491eb142d946c0
Author: Luca Beltrame <luca.beltrame@marionegri.it>
Date:   Mon Oct 17 14:37:33 2011 +0200

    Add documentation for the parameter

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index dbe171134..51caf08c9 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -489,6 +489,10 @@ class DataFrame(NDFrame):
             `header` and `index` are True, then the index names are used. A
             sequence should be given if the DataFrame uses MultiIndex.
         mode : Python write mode, default 'w'
+        delimiter : character, default ","
+            Field delimiter for the output file.
+
+
         """
         f = open(path, mode)
         csvout = csv.writer(f, lineterminator='\n', delimiter=delimiter)
