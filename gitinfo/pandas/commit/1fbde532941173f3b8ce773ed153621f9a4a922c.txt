commit 1fbde532941173f3b8ce773ed153621f9a4a922c
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Mon Apr 30 17:00:11 2012 -0400

    BUG: another bool uint8 cast, add period shortaliases after making reverse_period_code_map

diff --git a/pandas/src/engines.pyx b/pandas/src/engines.pyx
index 381b17832..3de282b26 100644
--- a/pandas/src/engines.pyx
+++ b/pandas/src/engines.pyx
@@ -270,7 +270,8 @@ cdef class Int64Engine(IndexEngine):
         values = self._get_index_values()
         n = len(values)
 
-        indexer = np.empty(n, dtype=bool)
+        result = np.empty(n, dtype=bool)
+        indexer = result.view(np.uint8)
 
         for i in range(n):
             if values[i] == val:
@@ -282,7 +283,7 @@ cdef class Int64Engine(IndexEngine):
         if count == 0:
             raise KeyError(val)
 
-        return indexer
+        return result
 
 cdef class Float64Engine(IndexEngine):
 
diff --git a/pandas/tseries/frequencies.py b/pandas/tseries/frequencies.py
index c1b234d0c..c2608b60c 100644
--- a/pandas/tseries/frequencies.py
+++ b/pandas/tseries/frequencies.py
@@ -437,7 +437,6 @@ def get_standard_freq(freq):
 _period_code_map = {
     # Annual freqs with various fiscal year ends.
     # eg, 2005 for A-FEB runs Mar 1, 2004 to Feb 28, 2005
-    "A"     : 1000,  # Annual
     "A-DEC" : 1000,  # Annual - December year end
     "A-JAN" : 1001,  # Annual - January year end
     "A-FEB" : 1002,  # Annual - February year end
@@ -453,7 +452,6 @@ _period_code_map = {
 
     # Quarterly frequencies with various fiscal year ends.
     # eg, Q42005 for Q-OCT runs Aug 1, 2005 to Oct 31, 2005
-    "Q"     : 2000,    # Quarterly - December year end (default quarterly)
     "Q-DEC" : 2000 ,    # Quarterly - December year end
     "Q-JAN" : 2001,    # Quarterly - January year end
     "Q-FEB" : 2002,    # Quarterly - February year end
@@ -469,7 +467,6 @@ _period_code_map = {
 
     "M"     : 3000,   # Monthly
 
-    "W"     : 4000,    # Weekly
     "W-SUN" : 4000,    # Weekly - Sunday end of week
     "W-MON" : 4001,    # Weekly - Monday end of week
     "W-TUE" : 4002,    # Weekly - Tuesday end of week
@@ -484,8 +481,20 @@ _period_code_map = {
     "T"      : 8000,   # Minutely
     "S"      : 9000,   # Secondly
     None     : -10000  # Undefined
+
 }
 
+_reverse_period_code_map = {}
+for k, v in _period_code_map.iteritems():
+    _reverse_period_code_map[v] = k
+
+# Additional aliases
+_period_code_map.update({
+    "Q"     : 2000,    # Quarterly - December year end (default quarterly)
+    "A"     : 1000,  # Annual
+    "W"     : 4000,    # Weekly
+})
+
 def _period_alias_dictionary():
     """
     Build freq alias dictionary to support freqs from original c_dates.c file
@@ -581,10 +590,6 @@ def _period_alias_dictionary():
 
     return alias_dict
 
-_reverse_period_code_map = {}
-for k, v in _period_code_map.iteritems():
-    _reverse_period_code_map[v] = k
-
 _reso_period_map = {
     "year"    : "A",
     "quarter" : "Q",
