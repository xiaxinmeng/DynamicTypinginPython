commit 62fda2ef506e05d3cdaa41b0dfc68e4d3e6b9664
Author: Gabriel Moreira <gabrielfm2@al.insper.edu.br>
Date:   Tue May 19 11:57:51 2020 -0300

    add note to explain columns parameter inside rename_axis() (#34194)

diff --git a/pandas/core/generic.py b/pandas/core/generic.py
index 85b6a8431..c1c659ffd 100644
--- a/pandas/core/generic.py
+++ b/pandas/core/generic.py
@@ -1012,6 +1012,9 @@ class NDFrame(PandasObject, SelectionMixin, indexing.IndexingMixin):
         index, columns : scalar, list-like, dict-like or function, optional
             A scalar, list-like, dict-like or functions transformations to
             apply to that axis' values.
+            Note that the ``columns`` parameter is not allowed if the
+            object is a Series. This parameter only apply for DataFrame
+            type objects.
 
             Use either ``mapper`` and ``axis`` to
             specify the axis to target with ``mapper``, or ``index``
