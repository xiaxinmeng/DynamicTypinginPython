commit 5a356bba7f2ee2ecc3d6e5a9a8807c34a099dbd3
Author: Adam Klein <adamklein@gmail.com>
Date:   Sun Feb 5 18:08:33 2012 -0500

    more tests passing, mostly incompatible timestamp/datetime stuff

diff --git a/pandas/core/series.py b/pandas/core/series.py
index 7db65dfe1..fbd902cf0 100644
--- a/pandas/core/series.py
+++ b/pandas/core/series.py
@@ -2003,11 +2003,16 @@ copy : boolean, default False
         v = self.get(date)
 
         if isnull(v):
+            # this will convert datetime -> datetime64 index
             candidates = self.index[notnull(self)]
-            index = candidates.searchsorted(date)
+
+            index = candidates.searchsorted(lib.Timestamp(date))
 
             if index > 0:
                 asOfDate = candidates[index - 1]
+                if (isinstance(asOfDate, lib.Timestamp) and 
+                    self.index.dtype != 'M8[us]'):
+                    asOfDate = asOfDate.to_datetime()
             else:
                 return nan
 
diff --git a/pandas/src/datetime.pyx b/pandas/src/datetime.pyx
index 3e178e3bb..08aa6b28f 100644
--- a/pandas/src/datetime.pyx
+++ b/pandas/src/datetime.pyx
@@ -221,6 +221,9 @@ cdef class Timestamp(object):
     def strftime(Timestamp self, object fmtstr):
         return self.dtval.strftime(fmtstr)
 
+    def to_datetime(Timestamp self):
+        return self.dtval
+
     def weekday(Timestamp self):
         return dayofweek(self.dtval.year, self.dtval.month, self.dtval.day)
 
