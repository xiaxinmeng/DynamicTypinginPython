commit bf538a960379358615f64c67ddf02544b86148e9
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Tue Nov 13 19:10:32 2012 -0500

    BUG: icol duplicate columns with integer sequence failure. close #2228

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index 9a537fc5b..4b826b98f 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -1726,7 +1726,11 @@ class DataFrame(NDFrame):
         else:
             label = self.columns[i]
             if isinstance(label, Index):
-                return self.reindex(columns=label)
+                if self.columns.inferred_type == 'integer':
+                    # XXX re: #2228
+                    return self.reindex(columns=label)
+                else:
+                    return self.ix[:, i]
 
             values = self._data.iget(i)
             return Series.from_array(values, index=self.index, name=label)
diff --git a/pandas/tests/test_frame.py b/pandas/tests/test_frame.py
index 49173d2ae..7787a5c6e 100644
--- a/pandas/tests/test_frame.py
+++ b/pandas/tests/test_frame.py
@@ -1323,6 +1323,10 @@ class CheckIndexing(object):
         xp = df.T.ix[0]
         assert_series_equal(rs, xp)
 
+        rs = df.icol([0])
+        xp = df.ix[:, [0]]
+        assert_frame_equal(rs, xp)
+
     def test_iget_value(self):
         for i, row in enumerate(self.frame.index):
             for j, col in enumerate(self.frame.columns):
