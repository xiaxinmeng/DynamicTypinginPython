commit 78147e9c84fb55a67af382a2d3a090e6b0828532
Author: Shubham Chaudhary <shubhamchaudhary92@gmail.com>
Date:   Mon Jan 8 19:32:09 2018 +0000

    DOC: Add documentation regarding no compression in to_parquet (#19131)

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index 62993a3d1..a8c405385 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -1670,8 +1670,8 @@ class DataFrame(NDFrame):
             Parquet reader library to use. If 'auto', then the option
             'io.parquet.engine' is used. If 'auto', then the first
             library to be installed is used.
-        compression : str, optional, default 'snappy'
-            compression method, includes {'gzip', 'snappy', 'brotli'}
+        compression : {'snappy', 'gzip', 'brotli', 'None'}
+            Name of the compression to use. Use ``None`` for no compression
         kwargs
             Additional keyword arguments passed to the engine
         """
diff --git a/pandas/io/parquet.py b/pandas/io/parquet.py
index e431c9447..0c88706a3 100644
--- a/pandas/io/parquet.py
+++ b/pandas/io/parquet.py
@@ -218,8 +218,8 @@ def to_parquet(df, path, engine='auto', compression='snappy', **kwargs):
         Parquet reader library to use. If 'auto', then the option
         'io.parquet.engine' is used. If 'auto', then the first
         library to be installed is used.
-    compression : str, optional, default 'snappy'
-        compression method, includes {'gzip', 'snappy', 'brotli'}
+    compression : {'snappy', 'gzip', 'brotli', 'None'}
+        Name of the compression to use. Use ``None`` for no compression
     kwargs
         Additional keyword arguments passed to the engine
     """
