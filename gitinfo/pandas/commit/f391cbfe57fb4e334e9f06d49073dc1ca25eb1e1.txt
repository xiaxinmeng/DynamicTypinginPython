commit f391cbfe57fb4e334e9f06d49073dc1ca25eb1e1
Author: Pepe Flores <ppflrs@users.noreply.github.com>
Date:   Mon Feb 5 20:43:02 2018 +0200

    DOC: Fix typo in example (#19537)
    
    Fix typo in the example for pandas.io.formats.style.Styler.format

diff --git a/pandas/io/formats/style.py b/pandas/io/formats/style.py
index 58796aa30..20e72dd6b 100644
--- a/pandas/io/formats/style.py
+++ b/pandas/io/formats/style.py
@@ -364,7 +364,7 @@ class Styler(object):
         >>> df = pd.DataFrame(np.random.randn(4, 2), columns=['a', 'b'])
         >>> df.style.format("{:.2%}")
         >>> df['c'] = ['a', 'b', 'c', 'd']
-        >>> df.style.format({'C': str.upper})
+        >>> df.style.format({'c': str.upper})
         """
         if subset is None:
             row_locs = range(len(self.data))
