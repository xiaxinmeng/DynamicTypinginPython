commit 4f433773141d2eb384325714a2776bcc5b2e20f7
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Thu Jul 28 10:58:32 2011 -0400

    BUG: apparently True == 1 and False == 0

diff --git a/pandas/core/common.py b/pandas/core/common.py
index aca5786e6..46a8e012a 100644
--- a/pandas/core/common.py
+++ b/pandas/core/common.py
@@ -104,7 +104,7 @@ def _is_bool_indexer(key):
         return True
     elif isinstance(key, list):
         try:
-            return set([True, False]).issubset(set(key))
+            return np.asarray(key).dtype == np.bool_
         except TypeError:
             return False
 
diff --git a/pandas/tests/test_series.py b/pandas/tests/test_series.py
index 6ddba9402..136649596 100644
--- a/pandas/tests/test_series.py
+++ b/pandas/tests/test_series.py
@@ -142,6 +142,12 @@ class TestSeries(unittest.TestCase):
         d = self.ts.index[0] - datetools.bday
         self.assertRaises(Exception, self.ts.__getitem__, d),
 
+    def test_getitem_regression(self):
+        from pandas import Series
+        s = Series(range(5), index=range(5))
+        result = s[range(5)]
+        assert_series_equal(result, s)
+
     def test_getitem_int64(self):
         idx = np.int64(5)
         self.assertEqual(self.ts[idx], self.ts[5])
