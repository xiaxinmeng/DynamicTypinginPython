commit ebcbd33d1e8dd65429e51663000ab86f6b12fbb5
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Tue Oct 23 15:54:33 2012 -0400

    ENH: BusinessDay.apply much faster when n large

diff --git a/RELEASE.rst b/RELEASE.rst
index af34ce85b..2fb396c71 100644
--- a/RELEASE.rst
+++ b/RELEASE.rst
@@ -29,6 +29,8 @@ pandas 0.9.1
 
 **Improvements to existing features**
 
+  - Improve performance of datetime + business day offset with large number of
+    offset periods
   - Improve HTML display of DataFrame objects with hierarchical columns
 
 **Bug fixes**
diff --git a/pandas/tseries/offsets.py b/pandas/tseries/offsets.py
index 99421f679..71e6866c6 100644
--- a/pandas/tseries/offsets.py
+++ b/pandas/tseries/offsets.py
@@ -323,6 +323,12 @@ class BusinessDay(CacheableOffset, DateOffset):
 
             result = other
 
+            # avoid slowness below
+            if abs(n) > 5:
+                k = n // 5
+                result = result + timedelta(7 * k)
+                n -= 5 * k
+
             while n != 0:
                 k = n // abs(n)
                 result = result + timedelta(k)
diff --git a/pandas/tseries/tests/test_offsets.py b/pandas/tseries/tests/test_offsets.py
index 1c09aefac..3242b4b3e 100644
--- a/pandas/tseries/tests/test_offsets.py
+++ b/pandas/tseries/tests/test_offsets.py
@@ -250,6 +250,15 @@ class TestBusinessDay(unittest.TestCase):
             for base, expected in cases.iteritems():
                 assertEq(offset, base, expected)
 
+    def test_apply_large_n(self):
+        dt = datetime(2012, 10, 23)
+
+        result = dt + BDay(10)
+        self.assertEqual(result, datetime(2012, 11, 6))
+
+        result = dt + BDay(100) - BDay(100)
+        self.assertEqual(result, dt)
+
     def test_apply_corner(self):
         self.assertRaises(Exception, BDay().apply, BMonthEnd())
 
