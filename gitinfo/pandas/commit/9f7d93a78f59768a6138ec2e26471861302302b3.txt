commit 9f7d93a78f59768a6138ec2e26471861302302b3
Author: Chang She <chang@lambdafoundry.com>
Date:   Fri Dec 28 10:54:25 2012 -0500

    BUG: xy-plot default label shows y on x-axis

diff --git a/pandas/tests/test_graphics.py b/pandas/tests/test_graphics.py
index b88956d99..79f264840 100644
--- a/pandas/tests/test_graphics.py
+++ b/pandas/tests/test_graphics.py
@@ -226,8 +226,11 @@ class TestDataFramePlots(unittest.TestCase):
         import matplotlib.pyplot as plt
         plt.close('all')
         df = DataFrame(np.random.randn(10, 3), columns=['a', 'b', 'c'])
-        ax = df.plot(x='a', y='b', label='LABEL', legend=True)
-        self.assert_(ax.get_legend().get_texts()[0].get_text() == 'LABEL')
+        ax = df.plot(x='a', y='b')
+        self.assert_(ax.xaxis.get_label().get_text() == 'a')
+
+        plt.close('all')
+        ax = df.plot(x='a', y='b', label='LABEL')
         self.assert_(ax.xaxis.get_label().get_text() == 'LABEL')
 
     @slow
diff --git a/pandas/tools/plotting.py b/pandas/tools/plotting.py
index 0a1c071b9..9fd61f743 100644
--- a/pandas/tools/plotting.py
+++ b/pandas/tools/plotting.py
@@ -1416,7 +1416,8 @@ def plot_frame(frame=None, x=None, y=None, subplots=False, sharex=True,
     if y is not None:
         if com.is_integer(y) and not frame.columns.holds_integer():
             y = frame.columns[y]
-        label = kwds.pop('label', y)
+        label = x if x is not None else frame.index.name
+        label = kwds.pop('label', label)
         ser = frame[y]
         ser.index.name = label
         return plot_series(ser, label=label, kind=kind,
