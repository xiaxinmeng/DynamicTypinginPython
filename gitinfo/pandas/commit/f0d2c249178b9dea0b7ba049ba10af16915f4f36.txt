commit f0d2c249178b9dea0b7ba049ba10af16915f4f36
Author: Andy Hayden <andyhayden1@gmail.com>
Date:   Mon Jan 27 18:23:16 2014 -0800

    DOC add str.get_dummies to api

diff --git a/doc/source/api.rst b/doc/source/api.rst
index b21cd1456..90a12d449 100644
--- a/doc/source/api.rst
+++ b/doc/source/api.rst
@@ -467,6 +467,7 @@ details the methods show up here as methods of the
    StringMethods.strip
    StringMethods.title
    StringMethods.upper
+   StringMethods.get_dummies
 
 Plotting
 ~~~~~~~~
diff --git a/doc/source/basics.rst b/doc/source/basics.rst
index f1e0013db..4eeb09914 100644
--- a/doc/source/basics.rst
+++ b/doc/source/basics.rst
@@ -1232,8 +1232,7 @@ For example if they are seperated by a ``'|'``:
       s = pd.Series(['a', 'a|b', np.nan, 'a|c'])
       s.str.get_dummies(sep='|')
 
-See also ``pd.get_dummies``.
-
+See also :func:`~pandas.get_dummies`.
 
 .. _basics.sorting:
 
diff --git a/doc/source/reshaping.rst b/doc/source/reshaping.rst
index 288cd48b1..9c289c4c3 100644
--- a/doc/source/reshaping.rst
+++ b/doc/source/reshaping.rst
@@ -418,6 +418,8 @@ This function is often used along with discretization functions like ``cut``:
 
    get_dummies(cut(values, bins))
 
+See also :func:`~pandas.Series.str.get_dummies`.
+
 Factorizing values
 ------------------
 
diff --git a/pandas/core/strings.py b/pandas/core/strings.py
index a41c06a6a..bd8e1b196 100644
--- a/pandas/core/strings.py
+++ b/pandas/core/strings.py
@@ -492,7 +492,7 @@ def str_get_dummies(arr, sep='|'):
         tags.update(ts)
     tags = sorted(tags - set([""]))
 
-    dummies = np.empty((len(arr), len(tags)), dtype=int)
+    dummies = np.empty((len(arr), len(tags)), dtype=np.int64)
 
     for i, t in enumerate(tags):
         pat = sep + t + sep
