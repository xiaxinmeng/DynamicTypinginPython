commit 942d873f6cd1cdc13dc073d6aa0710cdd5f2b075
Author: Phillip Cloud <cpcloud@gmail.com>
Date:   Fri May 3 19:53:51 2013 -0400

    BUG: fix qt console repr

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index 8bfdee3b7..3fd95e161 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -746,8 +746,8 @@ class DataFrame(NDFrame):
                 self.info(buf=buf, verbose=verbose)
                 info = buf.getvalue()
                 info = info.replace('&', r'&amp;')
-                info = info.replace('<', r'&lt')
-                info = info.replace('>', r'&gt')
+                info = info.replace('<', r'&lt;')
+                info = info.replace('>', r'&gt;')
                 return ('<pre>\n' + info + '\n</pre>')
         else:
             return None
