commit a6ba45619a1672e4b393fc2786c0e013a5058796
Author: jreback <jeff@reback.net>
Date:   Tue Jun 24 10:33:38 2014 -0400

    API: add/clarify numpy ufunc prepare/wrap

diff --git a/pandas/core/generic.py b/pandas/core/generic.py
index 214994a6f..756de479a 100644
--- a/pandas/core/generic.py
+++ b/pandas/core/generic.py
@@ -717,8 +717,8 @@ class NDFrame(PandasObject):
     def __array__(self, dtype=None):
         return _values_from_object(self)
 
-    def __array_wrap__(self, result, copy=False):
-        d = self._construct_axes_dict(self._AXIS_ORDERS, copy=copy)
+    def __array_wrap__(self, result, context=None):
+        d = self._construct_axes_dict(self._AXIS_ORDERS, copy=False)
         return self._constructor(result, **d).__finalize__(self)
 
     # ideally we would define this to avoid the getattr checks, but
diff --git a/pandas/core/series.py b/pandas/core/series.py
index bdad1f9e5..78b049f4b 100644
--- a/pandas/core/series.py
+++ b/pandas/core/series.py
@@ -366,18 +366,23 @@ class Series(base.IndexOpsMixin, generic.NDFrame):
                                  index=self.index).__finalize__(self)
 
     def __array__(self, result=None):
-        """ the array interface, return my values """
+        """
+        the array interface, return my values
+        """
         return self.values
 
-    def __array_wrap__(self, result, copy=False):
+    def __array_wrap__(self, result, context=None):
         """
-        Gets called prior to a ufunc (and after)
+        Gets called after a ufunc
         """
         return self._constructor(result, index=self.index,
-                                 copy=copy).__finalize__(self)
+                                 copy=False).__finalize__(self)
 
-    def __contains__(self, key):
-        return key in self.index
+    def __array_prepare__(self, result, context=None):
+        """
+        Gets called prior to a ufunc
+        """
+        return result
 
     # complex
     @property
