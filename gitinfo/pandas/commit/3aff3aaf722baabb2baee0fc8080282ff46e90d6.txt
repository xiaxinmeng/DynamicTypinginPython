commit 3aff3aaf722baabb2baee0fc8080282ff46e90d6
Author: y-p <yoval@gmx.com>
Date:   Thu Mar 21 13:18:49 2013 +0200

    BUG: series.plot(kind='bar') should respect pylab color scheme GH3115

diff --git a/pandas/tools/plotting.py b/pandas/tools/plotting.py
index 303376d37..aca5e8a72 100644
--- a/pandas/tools/plotting.py
+++ b/pandas/tools/plotting.py
@@ -1272,8 +1272,17 @@ class BarPlot(MPLPlot):
 
         return f
 
+    def _get_colors(self):
+        import matplotlib.pyplot as plt
+        cycle = plt.rcParams.get('axes.color_cycle', list('bgrcmyk'))
+        if isinstance(cycle, basestring):
+            cycle = list(cycle)
+        has_colors = 'color' in self.kwds
+        colors = self.kwds.get('color', cycle)
+        return colors
+
     def _make_plot(self):
-        colors = self.kwds.pop('color', 'brgyk')
+        colors = self._get_colors()
         rects = []
         labels = []
 
