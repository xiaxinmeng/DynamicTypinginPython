commit 6f7f367482aa38b216dfe54060b1fa3993386dfd
Author: Tom Augspurger <TomAugspurger@users.noreply.github.com>
Date:   Tue Apr 3 01:32:07 2018 -0500

    BUG: Series[EA].astype(str) works (#20581)
    
    Closes https://github.com/pandas-dev/pandas/issues/20578

diff --git a/pandas/core/internals.py b/pandas/core/internals.py
index b0a6086c4..e8fab3748 100644
--- a/pandas/core/internals.py
+++ b/pandas/core/internals.py
@@ -655,7 +655,7 @@ class Block(PandasObject):
 
                     # astype formatting
                     else:
-                        values = self.values
+                        values = self.get_values()
 
                 else:
                     values = self.get_values(dtype=dtype)
diff --git a/pandas/tests/extension/base/casting.py b/pandas/tests/extension/base/casting.py
index 74fe8f196..7146443bf 100644
--- a/pandas/tests/extension/base/casting.py
+++ b/pandas/tests/extension/base/casting.py
@@ -16,3 +16,8 @@ class BaseCastingTests(BaseExtensionTests):
         result = pd.Series(data).tolist()
         expected = list(data)
         assert result == expected
+
+    def test_astype_str(self, data):
+        result = pd.Series(data[:5]).astype(str)
+        expected = pd.Series(data[:5].astype(str))
+        self.assert_series_equal(result, expected)
diff --git a/pandas/tests/extension/json/test_json.py b/pandas/tests/extension/json/test_json.py
index 5e9639c48..87668cc11 100644
--- a/pandas/tests/extension/json/test_json.py
+++ b/pandas/tests/extension/json/test_json.py
@@ -127,7 +127,12 @@ class TestMethods(base.BaseMethodsTests):
 
 
 class TestCasting(base.BaseCastingTests):
-    pass
+    @pytest.mark.xfail
+    def test_astype_str(self):
+        """This currently fails in NumPy on np.array(self, dtype=str) with
+
+        *** ValueError: setting an array element with a sequence
+        """
 
 
 class TestGroupby(base.BaseGroupbyTests):
