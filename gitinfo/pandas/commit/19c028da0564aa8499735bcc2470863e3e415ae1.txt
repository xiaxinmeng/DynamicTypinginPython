commit 19c028da0564aa8499735bcc2470863e3e415ae1
Author: David Jung <t9@pobox.com>
Date:   Wed May 7 14:37:24 2014 -0400

    Added text to the HDFStore.put() method indicating dropna is available and settable via option, as for append()

diff --git a/pandas/io/pytables.py b/pandas/io/pytables.py
index 95daa2bbc..9fabf0ae9 100644
--- a/pandas/io/pytables.py
+++ b/pandas/io/pytables.py
@@ -809,6 +809,8 @@ class HDFStore(StringMixin):
             This will force Table format, append the input data to the
             existing.
         encoding : default None, provide an encoding for strings
+        dropna   : boolean, default True, do not write an ALL nan row to
+            the store settable by the option 'io.hdf.dropna_table'
         """
         if format is None:
             format = get_option("io.hdf.default_format") or 'fixed'
