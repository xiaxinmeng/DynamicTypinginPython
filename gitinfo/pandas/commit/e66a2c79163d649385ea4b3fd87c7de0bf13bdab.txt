commit e66a2c79163d649385ea4b3fd87c7de0bf13bdab
Author: hasnain2808 <hasnain2808@gmail.com>
Date:   Fri Dec 20 03:07:19 2019 +0530

    CLN: format replaced with f-strings #29547 (#30355)

diff --git a/pandas/core/indexers.py b/pandas/core/indexers.py
index 433bca940..209f889e8 100644
--- a/pandas/core/indexers.py
+++ b/pandas/core/indexers.py
@@ -144,9 +144,7 @@ def validate_indices(indices: np.ndarray, n: int) -> None:
     if len(indices):
         min_idx = indices.min()
         if min_idx < -1:
-            msg = "'indices' contains values less than allowed ({} < {})".format(
-                min_idx, -1
-            )
+            msg = f"'indices' contains values less than allowed ({min_idx} < -1)"
             raise ValueError(msg)
 
         max_idx = indices.max()
