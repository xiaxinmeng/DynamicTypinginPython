commit 884f7b24a8d4909d65cd03593522689c218361e9
Author: Adam Klein <adamklein@gmail.com>
Date:   Sat Dec 31 16:05:03 2011 -0500

    rebased master

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index a3dff13ec..aadc0aca7 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -1597,7 +1597,6 @@ class DataFrame(NDFrame):
             # TODO: speed up on homogeneous DataFrame objects
             new_data = new_data.reindex_indexer(columns, col_indexer, axis=0)
         elif columns is not None and columns is not new_data.axes[0]:
-            assert(False)
             new_data = new_data.reindex_items(columns)
 
         if copy and new_data is self._data:
