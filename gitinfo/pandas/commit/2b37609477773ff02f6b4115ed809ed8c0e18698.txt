commit 2b37609477773ff02f6b4115ed809ed8c0e18698
Author: epizzigoni <62080595+epizzigoni@users.noreply.github.com>
Date:   Fri Mar 13 23:03:29 2020 +0100

    DOC: filter method example is more clear (#32690)

diff --git a/pandas/core/generic.py b/pandas/core/generic.py
index 6f743d738..f6853a1bb 100644
--- a/pandas/core/generic.py
+++ b/pandas/core/generic.py
@@ -4558,6 +4558,10 @@ class NDFrame(PandasObject, SelectionMixin, indexing.IndexingMixin):
         >>> df = pd.DataFrame(np.array(([1, 2, 3], [4, 5, 6])),
         ...                   index=['mouse', 'rabbit'],
         ...                   columns=['one', 'two', 'three'])
+        >>> df
+                one  two  three
+        mouse     1    2      3
+        rabbit    4    5      6
 
         >>> # select columns by name
         >>> df.filter(items=['one', 'three'])
