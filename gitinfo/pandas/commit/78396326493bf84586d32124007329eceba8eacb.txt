commit 78396326493bf84586d32124007329eceba8eacb
Author: jreback <jeff@reback.net>
Date:   Sun Apr 27 11:45:52 2014 -0400

    COMPAT: windows dtype fix in for Panel.count

diff --git a/pandas/core/nanops.py b/pandas/core/nanops.py
index 9c2df9b5d..aaf053048 100644
--- a/pandas/core/nanops.py
+++ b/pandas/core/nanops.py
@@ -99,7 +99,7 @@ def _bn_ok_dtype(dt, name):
         # bottleneck does not properly upcast during the sum
         # so can overflow
         if name == 'nansum':
-            if dt != np.bool_ and dt.itemsize < 8:
+            if dt.itemsize < 8:
                 return False
 
         return True
