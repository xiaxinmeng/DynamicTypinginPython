commit ab56daca7e12e0bc043c9554a351efb1c42e26b5
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Thu Dec 1 19:45:53 2011 -0500

    ENH: accept date strings in data functions

diff --git a/.gitignore b/.gitignore
index ae175ad9a..029e140cb 100644
--- a/.gitignore
+++ b/.gitignore
@@ -4,6 +4,7 @@ build
 dist
 MANIFEST
 *.c
+*.cpp
 *.so
 *.pyd
 pandas/version.py
diff --git a/pandas/io/data.py b/pandas/io/data.py
index ce4dfd6cc..b991649c9 100644
--- a/pandas/io/data.py
+++ b/pandas/io/data.py
@@ -56,6 +56,9 @@ def DataReader(name, data_source=None, start=None, end=None):
         return get_data_famafrench(name=name)
 
 def _sanitize_dates(start, end):
+    from pandas.core.datetools import to_datetime
+    start = to_datetime(start)
+    end = to_datetime(end)
     if start is None:
         start = dt.datetime.today() - dt.timedelta(365)
     if end is None:
