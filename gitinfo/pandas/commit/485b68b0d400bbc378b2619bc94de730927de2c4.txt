commit 485b68b0d400bbc378b2619bc94de730927de2c4
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Sun Nov 13 19:28:10 2011 -0500

    TST: DataFrame test coverage

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index 8f419eed6..10e8362b6 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -2444,7 +2444,7 @@ class DataFrame(NDFrame):
             if len(on) == 1:
                 join_key = self[on[0]].values
             else:
-                join_key = zip(*[self[k] for k in on])
+                join_key = lib.fast_zip([self[k] for k in on])
                 join_key = common._asarray_tuplesafe(join_key,
                                                      dtype=np.object_)
         elif isinstance(on, np.ndarray) and len(on) == len(self):
diff --git a/pandas/tests/test_frame.py b/pandas/tests/test_frame.py
index fe8e57339..2df81d142 100644
--- a/pandas/tests/test_frame.py
+++ b/pandas/tests/test_frame.py
@@ -2605,6 +2605,11 @@ class TestDataFrame(unittest.TestCase, CheckIndexing,
         assert_series_equal(result0, expected0)
         assert_series_equal(result1, expected1)
 
+        # no reduction
+        result = self.frame.apply(lambda x: x * 2, raw=True)
+        expected = self.frame * 2
+        assert_frame_equal(result, expected)
+
     def test_apply_axis1(self):
         d = self.frame.index[0]
         tapplied = self.frame.apply(np.mean, axis=1)
diff --git a/scripts/bench_join_multi.py b/scripts/bench_join_multi.py
index c591ca7d8..6ea851366 100644
--- a/scripts/bench_join_multi.py
+++ b/scripts/bench_join_multi.py
@@ -28,27 +28,3 @@ data = DataFrame({'A' : np.random.randn(500000),
                   'key2' : np.repeat(key2, 5)})
 
 # data.join(to_join, on=['key1', 'key2'])
-
-"""
-Cython function for list_to_object_array
-
-def list_to_object_array(list obj):
-    '''
-    Convert list to object ndarray. Seriously can't believe I had to write this
-    function
-    '''
-    cdef:
-        Py_ssize_t i, n
-        ndarray[object] arr
-
-    n = len(obj)
-    arr = np.empty(n, dtype=object)
-
-    for i from 0 <= i < n:
-        arr[i] = obj[i]
-
-    return arr
-"""
-
-
-
