commit a987c54f1598038d15e65aa6f5bb1199782952de
Author: sardonick <nich.a.leary@gmail.com>
Date:   Tue Dec 24 11:04:39 2019 -0400

    CLN: changed .format to fstring in pandas/io/formats/csvs.py (#30425)

diff --git a/pandas/io/formats/csvs.py b/pandas/io/formats/csvs.py
index 3a91d65ab..72ba1a892 100644
--- a/pandas/io/formats/csvs.py
+++ b/pandas/io/formats/csvs.py
@@ -238,10 +238,7 @@ class CSVFormatter:
         if has_aliases:
             if len(header) != len(cols):
                 raise ValueError(
-                    (
-                        "Writing {ncols} cols but got {nalias} "
-                        "aliases".format(ncols=len(cols), nalias=len(header))
-                    )
+                    f"Writing {len(cols)} cols but got {len(header)} aliases"
                 )
             else:
                 write_cols = header
