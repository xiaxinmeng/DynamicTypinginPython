commit a5583141404ddf91516e70dade7942c315cb3646
Author: Jeffrey Tratner <jeffrey.tratner@gmail.com>
Date:   Mon Jul 1 08:06:47 2013 -0400

    CLN: Move _constructor checks to PandasObject base

diff --git a/pandas/core/base.py b/pandas/core/base.py
index f59c8c0ae..6122e78fa 100644
--- a/pandas/core/base.py
+++ b/pandas/core/base.py
@@ -42,6 +42,11 @@ class StringMixin(object):
 class PandasObject(StringMixin):
     """baseclass for various pandas objects"""
 
+    @property
+    def _constructor(self):
+        """class constructor (for this class it's just `__class__`"""
+        return self.__class__
+
     def __unicode__(self):
         """
         Return a string representation for a particular object.
diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index da52e5c5e..5fe2d6099 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -584,10 +584,6 @@ class DataFrame(NDFrame):
     def axes(self):
         return [self.index, self.columns]
 
-    @property
-    def _constructor(self):
-        return self.__class__
-
     @property
     def shape(self):
         return (len(self.index), len(self.columns))
diff --git a/pandas/core/generic.py b/pandas/core/generic.py
index 0c392defc..6be5f456b 100644
--- a/pandas/core/generic.py
+++ b/pandas/core/generic.py
@@ -633,10 +633,6 @@ class NDFrame(PandasContainer):
         mgr = self._data.astype(dtype, copy = copy, raise_on_error = raise_on_error)
         return self._constructor(mgr)
 
-    @property
-    def _constructor(self):
-        return NDFrame
-
     @property
     def axes(self):
         return self._data.axes
diff --git a/pandas/core/index.py b/pandas/core/index.py
index 0e15f9ee3..a3aa0804b 100644
--- a/pandas/core/index.py
+++ b/pandas/core/index.py
@@ -208,10 +208,6 @@ class Index(PandasObject, np.ndarray):
 
     names = property(fset=_set_names, fget=_get_names)
 
-    @property
-    def _constructor(self):
-        return Index
-
     @property
     def _has_complex_internals(self):
         # to disable groupby tricks in MultiIndex
@@ -1379,10 +1375,6 @@ class Int64Index(Index):
     def inferred_type(self):
         return 'integer'
 
-    @property
-    def _constructor(self):
-        return Int64Index
-
     @property
     def asi8(self):
         # do not cache or you'll create a memory leak
diff --git a/pandas/core/panel.py b/pandas/core/panel.py
index 89623bbf2..d33f7144c 100644
--- a/pandas/core/panel.py
+++ b/pandas/core/panel.py
@@ -186,10 +186,6 @@ class Panel(NDFrame):
     major_axis = lib.AxisProperty(1)
     minor_axis = lib.AxisProperty(2)
 
-    @property
-    def _constructor(self):
-        return type(self)
-
     # return the type of the slice constructor
     _constructor_sliced = DataFrame
 
diff --git a/pandas/core/series.py b/pandas/core/series.py
index 0870446c7..5ea029b41 100644
--- a/pandas/core/series.py
+++ b/pandas/core/series.py
@@ -519,10 +519,6 @@ class Series(generic.PandasContainer, pa.Array):
                  copy=False):
         pass
 
-    @property
-    def _constructor(self):
-        return Series
-
     @property
     def _can_hold_na(self):
         return not is_integer_dtype(self.dtype)
