commit 930c140da806bfcf7776bced839de9f4ad5eff2c
Author: Chang She <changshe@gmail.com>
Date:   Sat Nov 24 01:17:06 2012 -0500

    BUG: Series.where always inplace

diff --git a/pandas/core/series.py b/pandas/core/series.py
index 739a287fe..fda78acaf 100644
--- a/pandas/core/series.py
+++ b/pandas/core/series.py
@@ -585,8 +585,8 @@ copy : boolean, default False
 
         ser = self if inplace else self.copy()
         if not isinstance(other, (list, tuple, np.ndarray)):
-            self._set_with(~cond, other)
-            return self
+            ser._set_with(~cond, other)
+            return ser
 
         if isinstance(other, Series):
             other = other.reindex(ser.index)
diff --git a/pandas/tests/test_series.py b/pandas/tests/test_series.py
index 0416dab96..d7343c3ee 100644
--- a/pandas/tests/test_series.py
+++ b/pandas/tests/test_series.py
@@ -952,6 +952,7 @@ class TestSeries(unittest.TestCase, CheckNameIntegration):
 
         rs  = s.where(cond)
         assert(s.shape == rs.shape)
+        assert(rs is not s)
 
         rs = s.where(cond[:3], -s)
         assert_series_equal(rs, s.abs()[:3].append(s[3:]))
