commit fdda543e9c3158e166291e31b50231e6ada69a36
Author: William Ayd <william.ayd@icloud.com>
Date:   Tue Apr 23 05:10:35 2019 -0700

    Fixed code issue from compat with new numpydoc (#26188)

diff --git a/environment.yml b/environment.yml
index 48b873d4c..cf17dc128 100644
--- a/environment.yml
+++ b/environment.yml
@@ -24,7 +24,7 @@ dependencies:
   - pytest>=4.0.2
   - pytest-mock
   - sphinx
-  - numpydoc
+  - numpydoc>=0.9.0
   - pip
 
   # optional
diff --git a/requirements-dev.txt b/requirements-dev.txt
index 5600e43ea..115a93495 100644
--- a/requirements-dev.txt
+++ b/requirements-dev.txt
@@ -15,7 +15,7 @@ pycodestyle
 pytest>=4.0.2
 pytest-mock
 sphinx
-numpydoc
+numpydoc>=0.9.0
 pip
 beautifulsoup4>=4.2.1
 blosc
diff --git a/scripts/validate_docstrings.py b/scripts/validate_docstrings.py
index 19df1b924..ebb09e8f3 100755
--- a/scripts/validate_docstrings.py
+++ b/scripts/validate_docstrings.py
@@ -472,9 +472,12 @@ class Docstring:
 
     @property
     def see_also(self):
-        return collections.OrderedDict((name, ''.join(desc))
-                                       for name, desc, _
-                                       in self.doc['See Also'])
+        result = collections.OrderedDict()
+        for funcs, desc in self.doc['See Also']:
+            for func, _ in funcs:
+                result[func] = ''.join(desc)
+
+        return result
 
     @property
     def examples(self):
@@ -731,7 +734,7 @@ def get_validation_data(doc):
             if doc.method_returns_something:
                 errs.append(error('RT01'))
         else:
-            if len(doc.returns) == 1 and doc.returns[0][1]:
+            if len(doc.returns) == 1 and doc.returns[0].name:
                 errs.append(error('RT02'))
             for name_or_type, type_, desc in doc.returns:
                 if not desc:
