commit 1139a8d490a4e192dd6781511b11753e823a4663
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Mon Jan 9 20:43:44 2012 -0500

    TST: add vbench for nested dict DataFrame ctor, GH #540

diff --git a/vb_suite/frame_ctor.py b/vb_suite/frame_ctor.py
new file mode 100644
index 000000000..a524c64de
--- /dev/null
+++ b/vb_suite/frame_ctor.py
@@ -0,0 +1,26 @@
+from vbench.benchmark import Benchmark
+from datetime import datetime
+
+common_setup = """from pandas_vb_common import *
+"""
+
+#----------------------------------------------------------------------
+# Creation from nested dict
+
+setup = common_setup + """
+N, K = 5000, 50
+index = [rands(10) for _ in xrange(N)]
+columns = [rands(10) for _ in xrange(K)]
+frame = DataFrame(np.random.randn(N, K), index=index, columns=columns)
+data = frame.to_dict()
+dict_list = [dict(zip(columns, row)) for row in frame.values]
+"""
+
+frame_ctor_nested_dict = \
+    Benchmark("DataFrame(data)", setup, name='frame_ctor_nested_dict')
+
+# From JSON-like stuff
+
+frame_ctor_list_of_dict = \
+    Benchmark("DataFrame(dict_list)", setup, name='frame_ctor_list_of_dict',
+              start_date=datetime(2011, 12, 20))
diff --git a/vb_suite/reindex.py b/vb_suite/reindex.py
index 8eec6912e..d0c0ab7c9 100644
--- a/vb_suite/reindex.py
+++ b/vb_suite/reindex.py
@@ -47,7 +47,7 @@ s2 = s1[::2]
 statement = "s1.reindex(s2.index)"
 reindex_multi = Benchmark(statement, setup,
                           name='reindex_multiindex',
-                          start_date=datetime(2011, 8, 1))
+                          start_date=datetime(2011, 9, 1))
 
 #----------------------------------------------------------------------
 # Pad / backfill
diff --git a/vb_suite/suite.py b/vb_suite/suite.py
index f632ff956..f6e9016e7 100644
--- a/vb_suite/suite.py
+++ b/vb_suite/suite.py
@@ -5,7 +5,7 @@ import os
 
 modules = ['groupby', 'indexing', 'reindex',
            'sparse', 'index_object', 'miscellaneous',
-           'stat_ops', 'join_merge', 'panel_ctor']
+           'stat_ops', 'join_merge', 'panel_ctor', 'frame_ctor']
 
 by_module = {}
 benchmarks = []
