commit b29d58d1a3e0ebb1ed3bf3bed1e1cf4848092c77
Author: jbrockmendel <jbrockmendel@gmail.com>
Date:   Sat Jan 4 11:44:48 2020 -0800

    REF: change TDI.delete behavior to match DTI.delete (#30655)

diff --git a/pandas/core/indexes/timedeltas.py b/pandas/core/indexes/timedeltas.py
index d1273c40b..6fa7a7d3d 100644
--- a/pandas/core/indexes/timedeltas.py
+++ b/pandas/core/indexes/timedeltas.py
@@ -493,7 +493,7 @@ class TimedeltaIndex(
         """
         new_tds = np.delete(self.asi8, loc)
 
-        freq = "infer"
+        freq = None
         if is_integer(loc):
             if loc in (0, -len(self), -1, len(self) - 1):
                 freq = self.freq
@@ -504,7 +504,7 @@ class TimedeltaIndex(
                 if loc.start in (0, None) or loc.stop in (len(self), None):
                     freq = self.freq
 
-        return TimedeltaIndex(new_tds, name=self.name, freq=freq)
+        return self._shallow_copy(new_tds, freq=freq)
 
 
 TimedeltaIndex._add_comparison_ops()
diff --git a/pandas/tests/indexes/timedeltas/test_timedelta.py b/pandas/tests/indexes/timedeltas/test_timedelta.py
index 7703fbbbc..3b52b93fa 100644
--- a/pandas/tests/indexes/timedeltas/test_timedelta.py
+++ b/pandas/tests/indexes/timedeltas/test_timedelta.py
@@ -201,6 +201,13 @@ class TestTimedeltaIndex(DatetimeLike):
         result = a.append(c)
         assert (result["B"] == td).all()
 
+    def test_delete_doesnt_infer_freq(self):
+        # GH#30655 behavior matches DatetimeIndex
+
+        tdi = pd.TimedeltaIndex(["1 Day", "2 Days", None, "3 Days", "4 Days"])
+        result = tdi.delete(2)
+        assert result.freq is None
+
     def test_fields(self):
         rng = timedelta_range("1 days, 10:11:12.100123456", periods=2, freq="s")
         tm.assert_index_equal(rng.days, Index([1, 1], dtype="int64"))
