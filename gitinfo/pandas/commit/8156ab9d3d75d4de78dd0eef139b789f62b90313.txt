commit 8156ab9d3d75d4de78dd0eef139b789f62b90313
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Wed Aug 8 22:24:49 2012 -0400

    BUG: handle Ellipsis in Series indexing close #1721

diff --git a/RELEASE.rst b/RELEASE.rst
index dd9ac0770..ea7094941 100644
--- a/RELEASE.rst
+++ b/RELEASE.rst
@@ -42,6 +42,7 @@ pandas 0.8.2
   - Fix MM-YYYY time series indexing case (#1672)
   - Fix case where Categorical group key was not being passed into index in
     GroupBy result (#1701)
+  - Handle Ellipsis in Series.__getitem__/__setitem__ (#1721)
 
 pandas 0.8.1
 ============
diff --git a/pandas/core/series.py b/pandas/core/series.py
index 1dbc240e7..140df4c6e 100644
--- a/pandas/core/series.py
+++ b/pandas/core/series.py
@@ -435,6 +435,8 @@ copy : boolean, default False
             if isinstance(key, tuple) and isinstance(self.index, MultiIndex):
                 # kludge
                 pass
+            elif key is Ellipsis:
+                return self
             else:
                 raise
         except Exception:
@@ -536,6 +538,9 @@ copy : boolean, default False
 
                 values[key] = value
                 return
+            elif key is Ellipsis:
+                self[:] = value
+                return
 
             raise KeyError('%s not in this series!' % str(key))
         except TypeError, e:
diff --git a/pandas/tests/test_series.py b/pandas/tests/test_series.py
index 0e5d4411c..12b9caee8 100644
--- a/pandas/tests/test_series.py
+++ b/pandas/tests/test_series.py
@@ -89,6 +89,17 @@ class CheckNameIntegration(object):
         result = self.ts[5:10]
         self.assertEquals(result.name, self.ts.name)
 
+    def test_getitem_setitem_ellipsis(self):
+        s = Series(np.random.randn(10))
+
+        np.fix(s)
+
+        result = s[...]
+        assert_series_equal(result, s)
+
+        s[...] = 5
+        self.assert_((result == 5).all())
+
     def test_multilevel_name_print(self):
         index = MultiIndex(levels=[['foo', 'bar', 'baz', 'qux'],
                                    ['one', 'two', 'three']],
