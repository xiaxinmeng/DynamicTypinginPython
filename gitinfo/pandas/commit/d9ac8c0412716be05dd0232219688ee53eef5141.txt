commit d9ac8c0412716be05dd0232219688ee53eef5141
Author: Laura Collard, PhD <35954013+LauraCollard@users.noreply.github.com>
Date:   Wed Oct 2 09:59:25 2019 +0100

    DOC: Fixed PR06 docstrings errors in pandas.timedelta_range (#28719)

diff --git a/pandas/core/indexes/timedeltas.py b/pandas/core/indexes/timedeltas.py
index b03d60c7b..49dcea4da 100644
--- a/pandas/core/indexes/timedeltas.py
+++ b/pandas/core/indexes/timedeltas.py
@@ -717,17 +717,17 @@ def timedelta_range(
 
     Parameters
     ----------
-    start : string or timedelta-like, default None
+    start : str or timedelta-like, default None
         Left bound for generating timedeltas
-    end : string or timedelta-like, default None
+    end : str or timedelta-like, default None
         Right bound for generating timedeltas
-    periods : integer, default None
+    periods : int, default None
         Number of periods to generate
-    freq : string or DateOffset, default 'D'
+    freq : str or DateOffset, default 'D'
         Frequency strings can have multiples, e.g. '5H'
-    name : string, default None
+    name : str, default None
         Name of the resulting TimedeltaIndex
-    closed : string, default None
+    closed : str, default None
         Make the interval closed with respect to the given frequency to
         the 'left', 'right', or both sides (None)
 
