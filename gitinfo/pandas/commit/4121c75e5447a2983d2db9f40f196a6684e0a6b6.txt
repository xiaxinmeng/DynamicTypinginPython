commit 4121c75e5447a2983d2db9f40f196a6684e0a6b6
Author: Jeff Reback <jeff@reback.net>
Date:   Wed Mar 1 19:39:14 2017 -0500

    COMPAT: if docstring_wrapper is activated on a class, don't fail

diff --git a/pandas/util/decorators.py b/pandas/util/decorators.py
index ee7e2f430..62ff6ef14 100644
--- a/pandas/util/decorators.py
+++ b/pandas/util/decorators.py
@@ -261,6 +261,11 @@ class docstring_wrapper(object):
                          if hasattr(func, attr)])
 
     def __get__(self, instance, cls=None):
+
+        # we are called with a class
+        if instance is None:
+            return self
+
         # we want to return the actual passed instance
         return types.MethodType(self, instance)
 
