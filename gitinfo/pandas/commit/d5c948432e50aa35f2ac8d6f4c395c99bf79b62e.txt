commit d5c948432e50aa35f2ac8d6f4c395c99bf79b62e
Author: ischwabacher <ischwabacher@wisc.edu>
Date:   Mon Feb 17 18:50:50 2014 -0600

    TST: Add tests for `to_offset` with leading zeroes
    
    Test for the issues fixed by cbc8c73fdf617a4644baa130dff05476c7a61645.

diff --git a/pandas/tseries/tests/test_frequencies.py b/pandas/tseries/tests/test_frequencies.py
index 876204d22..3e8600af3 100644
--- a/pandas/tseries/tests/test_frequencies.py
+++ b/pandas/tseries/tests/test_frequencies.py
@@ -72,6 +72,16 @@ def test_to_offset_negative():
     freqstr = '-5min10s'
     result = to_offset(freqstr)
     assert(result.n == -310)
+    
+
+def test_to_offset_leading_zero():
+    freqstr = '00H 00T 01S'
+    result = to_offset(freqstr)
+    assert(result.n == 1)
+
+    freqstr = '-00H 03T 14S'
+    result = to_offset(freqstr)
+    assert(result.n == -194)
 
 
 def test_anchored_shortcuts():
