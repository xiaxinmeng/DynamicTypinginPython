commit 46ca395106044653d2437d8f0e60a4f286ddfb13
Author: Simon Hawkins <simonjayhawkins@gmail.com>
Date:   Sun Dec 30 18:35:51 2018 +0000

    use capsys.readouterr() as named tuple (#24489)

diff --git a/pandas/tests/series/test_duplicates.py b/pandas/tests/series/test_duplicates.py
index 26222637e..fe4797571 100644
--- a/pandas/tests/series/test_duplicates.py
+++ b/pandas/tests/series/test_duplicates.py
@@ -76,12 +76,12 @@ def test_is_unique_class_ne(capsys):
         def __ne__(self, other):
             raise Exception("NEQ not supported")
 
-    li = [Foo(i) for i in range(5)]
-    s = Series(li, index=[i for i in range(5)])
-    _, err = capsys.readouterr()
+    with capsys.disabled():
+        li = [Foo(i) for i in range(5)]
+        s = Series(li, index=[i for i in range(5)])
     s.is_unique
-    _, err = capsys.readouterr()
-    assert len(err) == 0
+    captured = capsys.readouterr()
+    assert len(captured.err) == 0
 
 
 @pytest.mark.parametrize(
