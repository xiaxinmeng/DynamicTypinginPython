commit 12902bc1d055cbabd77085996542896e4b3dd629
Author: Chang She <changshe@gmail.com>
Date:   Thu Nov 1 11:49:12 2012 -0400

    ENH: ~ for DataFrame #2110

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index f14d13e73..0f270c6f6 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -176,8 +176,6 @@ class DataConflictError(Exception):
 
 #----------------------------------------------------------------------
 # Factory helper methods
-
-
 def _arith_method(op, name, default_axis='columns'):
     def na_op(x, y):
         try:
@@ -702,6 +700,10 @@ class DataFrame(NDFrame):
         arr = operator.neg(self.values)
         return self._wrap_array(arr, self.axes, copy=False)
 
+    def __invert__(self):
+        arr = operator.inv(self.values)
+        return self._wrap_array(arr, self.axes, copy=False)
+
     # Comparison methods
     __eq__ = _comp_method(operator.eq, '__eq__')
     __ne__ = _comp_method(operator.ne, '__ne__')
diff --git a/pandas/tests/test_frame.py b/pandas/tests/test_frame.py
index 2d80fe03d..c989e8c98 100644
--- a/pandas/tests/test_frame.py
+++ b/pandas/tests/test_frame.py
@@ -2966,6 +2966,9 @@ class TestDataFrame(unittest.TestCase, CheckIndexing,
         # what to do?
         assert_frame_equal(-self.frame, -1 * self.frame)
 
+    def test_invert(self):
+        assert_frame_equal(-(self.frame < 0), ~(self.frame <0))
+
     def test_first_last_valid(self):
         N = len(self.frame.index)
         mat = randn(N)
