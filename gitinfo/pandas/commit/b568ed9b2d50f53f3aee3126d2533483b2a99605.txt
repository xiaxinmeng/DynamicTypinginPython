commit b568ed9b2d50f53f3aee3126d2533483b2a99605
Author: Yaroslav Halchenko <debian@onerussian.com>
Date:   Thu Jan 19 11:56:55 2012 -0500

    ENH: pass **kwds into plots.boxplot from DataFrame.plot
    
    not sure if that was intentional to swallow all those, so if it was --
    please at least pass through figsize

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index f144e07e8..51c6fcaac 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -3558,7 +3558,7 @@ class DataFrame(NDFrame):
         import pandas.tools.plotting as plots
         import matplotlib.pyplot as plt
         ax = plots.boxplot(self, column=column, by=by, ax=ax, fontsize=fontsize,
-                           grid=grid, rot=rot)
+                           grid=grid, rot=rot, **kwds)
         plt.draw_if_interactive()
         return ax
 
