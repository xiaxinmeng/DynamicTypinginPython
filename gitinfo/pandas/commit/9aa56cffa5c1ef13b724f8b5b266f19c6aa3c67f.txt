commit 9aa56cffa5c1ef13b724f8b5b266f19c6aa3c67f
Author: Gregg Lind <gregg.lind@gmail.com>
Date:   Wed Dec 21 15:05:27 2011 -0600

    One approach to requirements handling
    
    Others could be:
    
    * have requirements{2,3}.txt, and read from them with a function

diff --git a/pandas/core/datetools.py b/pandas/core/datetools.py
index 315f1b2d3..14eb839fb 100644
--- a/pandas/core/datetools.py
+++ b/pandas/core/datetools.py
@@ -15,6 +15,7 @@ try:
                         'install version 1.5!')
 except ImportError: # pragma: no cover
     print 'Please install python-dateutil via easy_install or some method!'
+    raise # otherwise a 2nd import won't show the message
 
 import calendar
 
diff --git a/requirments.txt b/requirments.txt
new file mode 100644
index 000000000..152f3e54f
--- /dev/null
+++ b/requirments.txt
@@ -0,0 +1,4 @@
+# so python-dateutil2 is for py3!
+python-dateutil < 2  
+numpy
+
diff --git a/setup.py b/setup.py
index 421d14d22..ee1697ef8 100755
--- a/setup.py
+++ b/setup.py
@@ -24,10 +24,13 @@ if sys.version_info[0] >= 3:
     except ImportError:
         raise ImportError('require setuptools/distribute for Py3k')
     setuptools_args = {'use_2to3': True,
-                       'zip_safe': False,}
+                       'zip_safe': False,
+                       'install_requires': ['python-dateutil > 2','numpy'],
+                        }
 else:
-    setuptools_args = {}
-
+    setuptools_args = {
+        'install_requires': ['python-dateutil < 2','numpy'],
+    }
 
 import numpy as np
 
