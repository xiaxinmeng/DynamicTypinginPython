commit 73d755468964a10cb882e34ccbfb3772880813fc
Author: jreback <jeff@reback.net>
Date:   Fri Dec 21 09:45:34 2012 -0500

    BUG: more robust to whitespace in Terms

diff --git a/doc/source/io.rst b/doc/source/io.rst
index bc710f933..802256174 100644
--- a/doc/source/io.rst
+++ b/doc/source/io.rst
@@ -1160,7 +1160,7 @@ Valid terms can be created from ``dict, list, tuple, or string``. Objects can be
 
        - ``dict(field = 'index', op = '>', value = '20121114')``
        - ``('index', '>', '20121114')``
-       - ``'index>20121114'``
+       - ``'index > 20121114'``
        - ``('index', '>', datetime(2012,11,14))``
        - ``('index', ['20121114','20121115'])``
        - ``('major_axis', '=', Timestamp('2012/11/14'))``
@@ -1220,7 +1220,7 @@ You can designate (and index) certain columns that you want to be able to perfor
    store.select('df_dc',[ Term('B>0') ])
 
    # getting creative
-   store.select('df_dc',[ Term('B>0'), Term('C>0'), Term('string=foo') ])
+   store.select('df_dc',[ 'B > 0', 'C > 0', 'string == foo' ])
 
    # this is in-memory version of this type of selection
    df_dc[(df_dc.B > 0) & (df_dc.C > 0) & (df_dc.string == 'foo')]
diff --git a/pandas/io/pytables.py b/pandas/io/pytables.py
index 38eb22310..5766db4f7 100644
--- a/pandas/io/pytables.py
+++ b/pandas/io/pytables.py
@@ -2317,7 +2317,7 @@ class Term(object):
     """
 
     _ops     = ['<=','<','>=','>','!=','=']
-    _search  = re.compile("^(?P<field>\w+)(?P<op>%s)(?P<value>.+)$" % '|'.join(_ops))
+    _search  = re.compile("^(?P<field>\w+)\s*(?P<op>%s)\s*(?P<value>.+)$" % '|'.join(_ops))
 
     def __init__(self, field, op = None, value = None, queryables = None):
         self.field = None
diff --git a/pandas/io/tests/test_pytables.py b/pandas/io/tests/test_pytables.py
index d76800e5a..4a5167669 100644
--- a/pandas/io/tests/test_pytables.py
+++ b/pandas/io/tests/test_pytables.py
@@ -902,9 +902,9 @@ class TestHDFStore(unittest.TestCase):
             ('major_axis', '>', '20121114'),
             (('major_axis', ['20121114','20121114']),),
             ('major_axis', datetime.datetime(2012,11,14)),
-            'major_axis>20121114',
-            'major_axis>20121114',
-            'major_axis>20121114',
+            'major_axis> 20121114',
+            'major_axis >20121114',
+            'major_axis > 20121114',
             (('minor_axis', ['A','B']),),
             (('minor_axis', ['A','B']),),
             ((('minor_axis', ['A','B']),),),
