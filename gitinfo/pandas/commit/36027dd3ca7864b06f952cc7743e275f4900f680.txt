commit 36027dd3ca7864b06f952cc7743e275f4900f680
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Sat Sep 8 21:15:12 2012 -0400

    STY: pep8 cleanup, release notes

diff --git a/RELEASE.rst b/RELEASE.rst
index 68dc1d184..fbe147be0 100644
--- a/RELEASE.rst
+++ b/RELEASE.rst
@@ -49,6 +49,8 @@ pandas 0.9.0
   - Deprecated ``day_of_year`` API removed from PeriodIndex, use ``dayofyear``
     (#1723)
   - Don't modify NumPy suppress printoption at import time
+  - The internal HDF5 data arrangement for DataFrames has been
+    transposed. Legacy files will still be readable by HDFStore (#1834, #1824)
 
 **Bug fixes**
 
@@ -115,6 +117,7 @@ pandas 0.9.0
   - Fix OLS.summary when column is a tuple (#1837)
   - Fix bug in __doc__ patching when -OO passed to interpreter (#1792, #1741)
   - Fix unicode console encoding issue in IPython notebook (#1782, #1768)
+  - Fix unicode formatting issue with Series.name (#1782)
 
 pandas 0.8.1
 ============
diff --git a/pandas/core/format.py b/pandas/core/format.py
index 92079e8d4..933496af6 100644
--- a/pandas/core/format.py
+++ b/pandas/core/format.py
@@ -80,15 +80,16 @@ class SeriesFormatter(object):
                 footer += ', '
 
             if self.series.name:
-                if isinstance(self.series.name,basestring):
-                    series_name=self.series.name
-                elif hasattr(self.series.name, "__getitem__") or \
-                    hasattr(self.series.name, "__iter__"):
-                    series_name="('"
-                    series_name+= "', '".join(map(unicode,self.series.name))
-                    series_name+= "')"
+                if isinstance(self.series.name, basestring):
+                    series_name = self.series.name
+                elif (hasattr(self.series.name, "__getitem__") or
+                      hasattr(self.series.name, "__iter__")):
+                    series_name ="('"
+                    series_name += "', '".join([unicode(x)
+                                                for x in self.series.name])
+                    series_name += "')"
                 else:
-                    series_name=str(self.series.name)
+                    series_name = str(self.series.name)
             else:
                 series_name=self.series.name
 
