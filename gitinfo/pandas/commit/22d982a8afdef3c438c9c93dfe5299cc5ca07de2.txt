commit 22d982a8afdef3c438c9c93dfe5299cc5ca07de2
Author: Dimitris Spathis <dispathis@gmail.com>
Date:   Wed Nov 23 22:23:32 2016 +0100

    Update frame.py (#14724)
    
    typo "explicitly"

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index f704a6104..bf1ff28cd 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -3233,7 +3233,7 @@ class DataFrame(NDFrame):
                 # try to be helpful
                 if isinstance(self.columns, MultiIndex):
                     raise ValueError('Cannot sort by column %s in a '
-                                     'multi-index you need to explicity '
+                                     'multi-index you need to explicitly '
                                      'provide all the levels' % str(by))
 
                 raise ValueError('Cannot sort by duplicate column %s' %
