commit 255fa04d400529d5ac935661ec0871e261b6946a
Author: Jeff Reback <jeff@reback.net>
Date:   Tue Oct 6 18:58:58 2015 -0400

    skip some plotting tests if scipy is not installed

diff --git a/pandas/tests/test_graphics.py b/pandas/tests/test_graphics.py
index 27069ddfd..b2d8ff8ba 100644
--- a/pandas/tests/test_graphics.py
+++ b/pandas/tests/test_graphics.py
@@ -462,6 +462,9 @@ class TestPlotBase(tm.TestCase):
 
         spndx=1
         for kind in kinds:
+            if not _ok_for_gaussian_kde(kind):
+                continue
+
             self.plt.subplot(1,4*len(kinds),spndx); spndx+=1
             mpl.rc('axes',grid=False)
             obj.plot(kind=kind, **kws)
@@ -3430,6 +3433,8 @@ class TestDataFramePlots(TestPlotBase):
 
         results = {}
         for kind in plotting._plot_klass.keys():
+            if not _ok_for_gaussian_kde(kind):
+                continue
             args = {}
             if kind in ['hexbin', 'scatter', 'pie']:
                 df = self.hexbin_df
