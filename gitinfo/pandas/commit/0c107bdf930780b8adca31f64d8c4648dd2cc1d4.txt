commit 0c107bdf930780b8adca31f64d8c4648dd2cc1d4
Author: Daniel Saxton <2658661+dsaxton@users.noreply.github.com>
Date:   Tue Feb 18 19:30:08 2020 -0600

    DOC: Update sort_index docs (#31898)

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index e4f36e128..9fe1ec7b7 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -4732,8 +4732,9 @@ class DataFrame(NDFrame):
             and 1 identifies the columns.
         level : int or level name or list of ints or list of level names
             If not None, sort on values in specified index level(s).
-        ascending : bool, default True
-            Sort ascending vs. descending.
+        ascending : bool or list of bools, default True
+            Sort ascending vs. descending. When the index is a MultiIndex the
+            sort direction can be controlled for each level individually.
         inplace : bool, default False
             If True, perform operation in-place.
         kind : {'quicksort', 'mergesort', 'heapsort'}, default 'quicksort'
diff --git a/pandas/core/series.py b/pandas/core/series.py
index 15fe0bb98..9c0ff9780 100644
--- a/pandas/core/series.py
+++ b/pandas/core/series.py
@@ -2950,11 +2950,11 @@ Name: Max Speed, dtype: float64
         self,
         axis=0,
         level=None,
-        ascending=True,
-        inplace=False,
-        kind="quicksort",
-        na_position="last",
-        sort_remaining=True,
+        ascending: bool = True,
+        inplace: bool = False,
+        kind: str = "quicksort",
+        na_position: str = "last",
+        sort_remaining: bool = True,
         ignore_index: bool = False,
     ):
         """
@@ -2969,8 +2969,9 @@ Name: Max Speed, dtype: float64
             Axis to direct sorting. This can only be 0 for Series.
         level : int, optional
             If not None, sort on values in specified index level(s).
-        ascending : bool, default true
-            Sort ascending vs. descending.
+        ascending : bool or list of bools, default True
+            Sort ascending vs. descending. When the index is a MultiIndex the
+            sort direction can be controlled for each level individually.
         inplace : bool, default False
             If True, perform operation in-place.
         kind : {'quicksort', 'mergesort', 'heapsort'}, default 'quicksort'
