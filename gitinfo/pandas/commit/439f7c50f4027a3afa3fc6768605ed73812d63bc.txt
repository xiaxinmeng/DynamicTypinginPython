commit 439f7c50f4027a3afa3fc6768605ed73812d63bc
Author: Dieter Vandenbussche <dvandenbussche@axioma.com>
Date:   Tue Jun 28 20:40:31 2011 -0400

    Minor change to CleanCommand so build works with stdeb

diff --git a/setup.py b/setup.py
index cb565e35f..1d7f460fd 100644
--- a/setup.py
+++ b/setup.py
@@ -92,9 +92,10 @@ version = '%s'
 class CleanCommand(Command):
     """Custom distutils command to clean the .so and .pyc files."""
 
-    user_options = [ ]
+    user_options = [("all", "a", "") ]
 
     def initialize_options(self):
+        self.all = True
         self._clean_me = []
         self._clean_trees = []
         for root, dirs, files in list(os.walk('pandas')):
