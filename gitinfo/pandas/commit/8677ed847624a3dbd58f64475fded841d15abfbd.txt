commit 8677ed847624a3dbd58f64475fded841d15abfbd
Author: y-p <yoval@gmx.com>
Date:   Thu Nov 22 18:33:51 2012 +0200

    TST: df.to_string() should respect self.col_space

diff --git a/pandas/tests/test_format.py b/pandas/tests/test_format.py
index 10bb75bfb..41e6d7a50 100644
--- a/pandas/tests/test_format.py
+++ b/pandas/tests/test_format.py
@@ -182,6 +182,13 @@ class TestDataFrameFormatting(unittest.TestCase):
         # this should work
         buf.getvalue()
 
+    def test_to_string_with_col_space(self):
+        df = DataFrame(np.random.random(size=(1,3)))
+        c10=len(df.to_string(col_space=10).split("\n")[1])
+        c20=len(df.to_string(col_space=20).split("\n")[1])
+        c30=len(df.to_string(col_space=30).split("\n")[1])
+        self.assertTrue( c10 < c20 < c30 )
+
     def test_to_html_unicode(self):
         # it works!
         df = DataFrame({u'\u03c3' : np.arange(10.)})
