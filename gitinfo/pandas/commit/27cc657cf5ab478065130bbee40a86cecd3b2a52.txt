commit 27cc657cf5ab478065130bbee40a86cecd3b2a52
Author: duozhang <duozhang@vmware.com>
Date:   Fri Jun 28 14:01:24 2013 +0800

    adjusted the indentation of doc of io.sql.read_frame

diff --git a/pandas/io/sql.py b/pandas/io/sql.py
index bc8afa349..d98fcfa5b 100644
--- a/pandas/io/sql.py
+++ b/pandas/io/sql.py
@@ -140,8 +140,8 @@ def read_frame(sql, con, index_col=None, coerce_float=True):
     index_col: string, optional
         column name to use for the returned DataFrame object.
     coerce_float : boolean, default True
-            Attempt to convert values to non-string, non-numeric objects (like
-            decimal.Decimal) to floating point, useful for SQL result sets
+        Attempt to convert values to non-string, non-numeric objects (like
+        decimal.Decimal) to floating point, useful for SQL result sets
     """
     cur = execute(sql, con)
     rows = _safe_fetch(cur)
