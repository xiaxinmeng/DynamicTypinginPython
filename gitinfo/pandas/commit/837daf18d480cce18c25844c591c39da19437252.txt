commit 837daf18d480cce18c25844c591c39da19437252
Author: Simon Hawkins <simonjayhawkins@gmail.com>
Date:   Thu Apr 23 04:09:10 2020 +0100

    CLN: misc cleanups from LGTM (#33724)

diff --git a/pandas/_testing.py b/pandas/_testing.py
index 4f957b7a5..21e74dafc 100644
--- a/pandas/_testing.py
+++ b/pandas/_testing.py
@@ -279,16 +279,10 @@ def write_to_compressed(compression, path, data, dest="test"):
     ValueError : An invalid compression value was passed in.
     """
     if compression == "zip":
-        import zipfile
-
         compress_method = zipfile.ZipFile
     elif compression == "gzip":
-        import gzip
-
         compress_method = gzip.GzipFile
     elif compression == "bz2":
-        import bz2
-
         compress_method = bz2.BZ2File
     elif compression == "xz":
         compress_method = _get_lzma_file(lzma)
diff --git a/pandas/core/series.py b/pandas/core/series.py
index 9ef865a96..a9db60f23 100644
--- a/pandas/core/series.py
+++ b/pandas/core/series.py
@@ -275,7 +275,6 @@ class Series(base.IndexOpsMixin, generic.NDFrame):
                         "Cannot construct a Series from an ndarray with "
                         "compound dtype.  Use DataFrame instead."
                     )
-                pass
             elif isinstance(data, ABCSeries):
                 if index is None:
                     index = data.index
