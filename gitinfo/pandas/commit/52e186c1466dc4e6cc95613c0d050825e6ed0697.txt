commit 52e186c1466dc4e6cc95613c0d050825e6ed0697
Author: Licht Takeuchi <licht-t@outlook.jp>
Date:   Sat Nov 4 23:43:06 2017 +0900

    TST: Add regression test for empty DataFrame groupby (#18097)

diff --git a/pandas/tests/groupby/test_groupby.py b/pandas/tests/groupby/test_groupby.py
index 6f022aeff..2f750a762 100644
--- a/pandas/tests/groupby/test_groupby.py
+++ b/pandas/tests/groupby/test_groupby.py
@@ -2732,6 +2732,16 @@ class TestGroupBy(MixIn):
 
         assert_series_equal(result, expected)
 
+    def test_empty_dataframe_groupby(self):
+        # GH8093
+        df = DataFrame(columns=['A', 'B', 'C'])
+
+        result = df.groupby('A').sum()
+        expected = DataFrame(columns=['B', 'C'], dtype=np.float64)
+        expected.index.name = 'A'
+
+        assert_frame_equal(result, expected)
+
 
 def _check_groupby(df, result, keys, field, f=lambda x: x.sum()):
     tups = lmap(tuple, df[keys].values)
