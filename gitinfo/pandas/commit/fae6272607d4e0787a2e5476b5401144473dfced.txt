commit fae6272607d4e0787a2e5476b5401144473dfced
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Wed Dec 28 10:05:53 2011 -0500

    REF: to_long -> to_frame

diff --git a/pandas/core/panel.py b/pandas/core/panel.py
index 5c673c934..794faef18 100644
--- a/pandas/core/panel.py
+++ b/pandas/core/panel.py
@@ -840,9 +840,9 @@ class Panel(NDFrame):
 
         return Panel(new_values, *new_axes)
 
-    def to_long(self, filter_observations=True):
+    def to_frame(self, filter_observations=True):
         """
-        Transform wide format into long (stacked) format
+        Transform wide format into long (stacked) format as DataFrame
 
         Parameters
         ----------
@@ -881,7 +881,8 @@ class Panel(NDFrame):
 
         return DataFrame(data, index=index, columns=self.items)
 
-    toLong = deprecate('toLong', to_long)
+    to_long = deprecate('to_long', to_frame)
+    toLong = deprecate('toLong', to_frame)
 
     def filter(self, items):
         """
diff --git a/pandas/io/tests/test_pytables.py b/pandas/io/tests/test_pytables.py
index fa2d6ec21..88aadbefe 100644
--- a/pandas/io/tests/test_pytables.py
+++ b/pandas/io/tests/test_pytables.py
@@ -344,14 +344,13 @@ class TesttHDFStore(unittest.TestCase):
 
     def test_long(self):
         def _check(left, right):
-            tm.assert_panel_equal(left.to_wide(),
-                                  right.to_wide())
+            tm.assert_panel_equal(left.to_panel(), right.to_panel())
 
         wp = tm.makePanel()
-        self._check_roundtrip(wp.to_long(), _check)
+        self._check_roundtrip(wp.to_frame(), _check)
 
         # empty
-        self.assertRaises(ValueError, self._check_roundtrip, wp.to_long()[:0],
+        self.assertRaises(ValueError, self._check_roundtrip, wp.to_frame()[:0],
                           _check)
 
     def test_longpanel(self):
diff --git a/pandas/sparse/panel.py b/pandas/sparse/panel.py
index f00b072e0..8c3a0d769 100644
--- a/pandas/sparse/panel.py
+++ b/pandas/sparse/panel.py
@@ -14,6 +14,7 @@ from pandas.core.panel import Panel
 
 from pandas.sparse.frame import SparseDataFrame
 
+from pandas.util.decorators import deprecate
 
 class SparsePanelAxis(object):
 
@@ -219,7 +220,7 @@ class SparsePanel(Panel):
                            default_fill_value=self.default_fill_value,
                            default_kind=self.default_kind)
 
-    def to_long(self, filter_observations=True):
+    def to_frame(self, filter_observations=True):
         """
         Convert SparsePanel to (dense) DataFrame
 
@@ -269,6 +270,9 @@ class SparsePanel(Panel):
         df = DataFrame(values, index=index, columns=self.items)
         return df.sortlevel(level=0)
 
+    to_long = deprecate('to_long', to_frame)
+    toLong = deprecate('toLong', to_frame)
+
     def reindex(self, major=None, items=None, minor=None, major_axis=None,
                 minor_axis=None, copy=False):
         """
