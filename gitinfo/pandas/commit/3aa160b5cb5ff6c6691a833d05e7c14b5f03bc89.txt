commit 3aa160b5cb5ff6c6691a833d05e7c14b5f03bc89
Author: Vytautas Jancauskas <unaudio@gmail.com>
Date:   Mon Jul 30 18:07:03 2012 +0300

    Got rid of the is_trellis method, use isinstance instead

diff --git a/pandas/tools/rplot.py b/pandas/tools/rplot.py
index 86b6002bf..d7e776b7c 100644
--- a/pandas/tools/rplot.py
+++ b/pandas/tools/rplot.py
@@ -226,11 +226,6 @@ class Layer:
 		else:
 			self.aes = aes_
 
-	def is_trellis(self):
-		"""Return false to indicate this is not a TrellisGrid.
-		"""
-		return False
-
 	def work(self, fig=None, ax=None):
 		pass
 
@@ -343,11 +338,6 @@ class TrellisGrid(Layer):
 		"""
 		self.by = by
 
-	def is_trellis(self):
-		"""Return true to indicate this is a TrellisGrid.
-		"""
-		return True
-
 	def render(self, ax=None, fig=None):
 		"""Render the trellis plot on a figure.
 
@@ -479,7 +469,7 @@ class RPlot:
 		# Look for the last TrellisGrid instance in the layer list
 		last_trellis = None
 		for layer in self.layers:
-			if layer.is_trellis():
+			if isinstance(layer, TrellisGrid):
 				last_trellis = layer
 		if last_trellis is None:
 			# We have a simple, non-trellised plot
