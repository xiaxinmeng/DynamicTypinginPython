commit 93d446104c4e81fe91ea5e57946b648b5ade86d3
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Wed Jul 6 20:54:52 2011 -0400

    BUG: fixed GH #56

diff --git a/pandas/core/series.py b/pandas/core/series.py
index 123e93b4e..1b3b1fc75 100644
--- a/pandas/core/series.py
+++ b/pandas/core/series.py
@@ -35,6 +35,10 @@ def _arith_method(op, name):
         '__sub__' : '__rsub__',
         '__div__' : '__rdiv__',
         '__mul__' : '__rmul__',
+        '__radd__' : '__add__',
+        '__rsub__' : '__sub__',
+        '__rdiv__' : '__div__',
+        '__rmul__' : '__mul__',
     }
     def wrapper(self, other):
         from pandas.core.frame import DataFrame
@@ -445,6 +449,14 @@ class Series(np.ndarray, PandasGeneric):
     __div__ = _arith_method(operator.div, '__div__')
     __truediv__ = _arith_method(operator.truediv, '__truediv__')
     __pow__ = _arith_method(operator.pow, '__pow__')
+    __truediv__ = _arith_method(operator.truediv, '__truediv__')
+
+    __radd__ = _arith_method(operator.add, '__add__')
+    __rmul__ = _arith_method(operator.mul, '__mul__')
+    __rsub__ = _arith_method(lambda x, y: y - x, '__sub__')
+    __rdiv__ = _arith_method(lambda x, y: y / x, '__div__')
+    __rtruediv__ = _arith_method(lambda x, y: y / x, '__truediv__')
+    __rpow__ = _arith_method(lambda x, y: y ** x, '__pow__')
 
     # Inplace operators
     __iadd__ = __add__
diff --git a/pandas/tests/test_series.py b/pandas/tests/test_series.py
index 102c9648c..6cc7e0bca 100644
--- a/pandas/tests/test_series.py
+++ b/pandas/tests/test_series.py
@@ -461,7 +461,7 @@ class TestSeries(unittest.TestCase):
         def _check_op(arr, op):
             result = op(1., arr)
             expected = op(1., arr.astype(float))
-            assert_series_equal(result, expected)
+            assert_series_equal(result.astype(float), expected)
 
         _check_op(arr, operator.add)
         _check_op(arr, operator.sub)
