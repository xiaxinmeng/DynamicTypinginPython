commit 4b1624edd6af26b11d59696479aae60a1c8ae7c3
Author: Tan Tran <42478131+tranctan96@users.noreply.github.com>
Date:   Tue Apr 16 18:59:04 2019 +0700

    BUG: positional getitem indexing with list on Series with duplicate integer index fails  (#26083)

diff --git a/pandas/tests/indexing/test_indexing.py b/pandas/tests/indexing/test_indexing.py
index 5cb2b138f..fa5955a32 100644
--- a/pandas/tests/indexing/test_indexing.py
+++ b/pandas/tests/indexing/test_indexing.py
@@ -241,6 +241,14 @@ class TestFancy(Base):
         result = df.loc[[1, 2], ['a', 'b']]
         tm.assert_frame_equal(result, expected)
 
+    @pytest.mark.parametrize('case', [lambda s: s, lambda s: s.loc])
+    def test_duplicate_int_indexing(self, case):
+        # GH 17347
+        s = pd.Series(range(3), index=[1, 1, 3])
+        expected = s[1]
+        result = case(s)[[1]]
+        tm.assert_series_equal(result, expected)
+
     def test_indexing_mixed_frame_bug(self):
 
         # GH3492
