commit c91572bb945e0f175f5ebdfb76b8d8ba95681a04
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Fri Nov 18 12:35:22 2011 -0500

    TST: plot methods return axes and test per #368, PR #385

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index 8bb11104e..91ad173b4 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -3127,6 +3127,10 @@ class DataFrame(NDFrame):
             pass
 
         plt.draw_if_interactive()
+        if subplots:
+            return axes
+        else:
+            return ax
 
     def _bar_plot(self, axes, subplots=False, use_index=True, grid=True,
                   rot=30, legend=True, **kwds):
@@ -3192,6 +3196,7 @@ class DataFrame(NDFrame):
             ax.set_title(col)
             ax.grid(grid)
 
+        return axes
     #----------------------------------------------------------------------
     # Deprecated stuff
 
diff --git a/pandas/core/series.py b/pandas/core/series.py
index 1eeca2f82..922ae5a35 100644
--- a/pandas/core/series.py
+++ b/pandas/core/series.py
@@ -1684,6 +1684,8 @@ copy : boolean, default False
 
         plt.draw_if_interactive()
 
+        return ax
+
     def hist(self, ax=None, grid=True, **kwds):
         """
         Draw histogram of the input series using matplotlib
@@ -1710,6 +1712,8 @@ copy : boolean, default False
         ax.hist(values)
         ax.grid(grid)
 
+        return ax
+
     def to_csv(self, path):
         """
         Write the Series to a CSV file
diff --git a/pandas/tests/test_graphics.py b/pandas/tests/test_graphics.py
index e42789975..038c72e8e 100644
--- a/pandas/tests/test_graphics.py
+++ b/pandas/tests/test_graphics.py
@@ -107,12 +107,14 @@ def _check_plot_works(f, *args, **kwargs):
     fig = plt.gcf()
     plt.clf()
     ax = fig.add_subplot(211)
-    f(*args, **kwargs)
+    ret = f(*args, **kwargs)
+    assert(ret is not None)  # do something more intelligent
 
     ax = fig.add_subplot(212)
     try:
         kwargs['ax'] = ax
-        f(*args, **kwargs)
+        ret = f(*args, **kwargs)
+        assert(ret is not None)  # do something more intelligent
     except Exception:
         pass
     plt.savefig(PNG_PATH)
