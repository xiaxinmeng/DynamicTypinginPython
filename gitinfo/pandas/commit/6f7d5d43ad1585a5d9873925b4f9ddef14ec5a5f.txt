commit 6f7d5d43ad1585a5d9873925b4f9ddef14ec5a5f
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Mon Jan 3 19:57:31 2011 -0500

    cols() returns list type

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index 4206e84ed..adada71b3 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -688,7 +688,7 @@ class DataFrame(Picklable, Groupable):
 
     def cols(self):
         """Return sorted list of frame's columns"""
-        return self.columns
+        return list(self.columns)
 
     def iteritems(self):
         """Iterator over (column, series) pairs"""
diff --git a/pandas/core/matrix.py b/pandas/core/matrix.py
index 31e7aecd9..4d3ce993e 100644
--- a/pandas/core/matrix.py
+++ b/pandas/core/matrix.py
@@ -913,9 +913,9 @@ class DataMatrix(DataFrame):
     def cols(self):
         """Return sorted list of frame's columns"""
         if self.objects is not None and len(self.objects.columns) > 0:
-            return self.columns.union(self.objects.columns)
+            return list(self.columns.union(self.objects.columns))
         else:
-            return self.columns
+            return list(self.columns)
 
     def copy(self):
         """
