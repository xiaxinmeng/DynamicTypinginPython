commit 6147541aa6c87507129973ecbbe4af3197450eda
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Mon Mar 26 21:00:43 2012 -0400

    BUG: work around matplotlib bug 775

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index a9f3eb958..c37f9b6ab 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -3995,7 +3995,7 @@ class DataFrame(NDFrame):
                 ax.set_title(col)
             else:
                 rects.append(ax.bar(xinds + i * 0.75 / K, y, 0.75 / K,
-                                    bottom=np.zeros(N), label=col,
+                                    bottom=np.zeros(N), label=str(col),
                                     color=colors[i % len(colors)], **kwds))
                 labels.append(col)
 
