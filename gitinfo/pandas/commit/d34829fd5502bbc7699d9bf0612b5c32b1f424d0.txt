commit d34829fd5502bbc7699d9bf0612b5c32b1f424d0
Author: Wouter Overmeire <lodagro@gmail.com>
Date:   Wed Mar 14 11:08:14 2012 +0100

    Make sure unicode_series.csv file (needed for unittest) gets installed.

diff --git a/pandas/tests/test_format.py b/pandas/tests/test_format.py
index 7015dece0..37a60c848 100644
--- a/pandas/tests/test_format.py
+++ b/pandas/tests/test_format.py
@@ -134,7 +134,7 @@ class TestDataFrameFormatting(unittest.TestCase):
 
     def test_string_repr_encoding(self):
         pth = curpath()
-        filepath = os.path.join(pth, 'unicode_series.csv')
+        filepath = os.path.join(pth, 'data', 'unicode_series.csv')
         df = pandas.read_csv(filepath, header=None)
         repr(df)
         repr(df['X.2'])
diff --git a/setup.py b/setup.py
index 2868b765d..b1e9f2f83 100755
--- a/setup.py
+++ b/setup.py
@@ -390,8 +390,9 @@ setup(name=DISTNAME,
       package_data={'pandas.io' : ['tests/*.h5',
                                    'tests/*.csv',
                                    'tests/*.xls'],
-                    'pandas.tests' : ['data/*.pickle']
-                    },
+                    'pandas.tests' : ['data/*.pickle',
+                                      'data/*.csv']
+                   },
       ext_modules=extensions,
       maintainer_email=EMAIL,
       description=DESCRIPTION,
