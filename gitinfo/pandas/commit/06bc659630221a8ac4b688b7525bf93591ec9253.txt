commit 06bc659630221a8ac4b688b7525bf93591ec9253
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Sun May 15 21:25:32 2011 -0400

    addressed panel constructor case

diff --git a/pandas/core/sparse.py b/pandas/core/sparse.py
index be4bdd3b7..4398e423a 100644
--- a/pandas/core/sparse.py
+++ b/pandas/core/sparse.py
@@ -849,10 +849,10 @@ class SparseWidePanel(WidePanel):
 
         self._frames = clean_frames
 
-        # TODO: !
+        # do we want to fill missing ones?
         for item in items:
             if item not in clean_frames:
-                raise Exception('foo')
+                raise Exception('column %s not found in data' % item)
 
         self.items = items
         self.major_axis = major_axis
diff --git a/pandas/core/tests/test_sparse.py b/pandas/core/tests/test_sparse.py
index c6b359361..13519f789 100644
--- a/pandas/core/tests/test_sparse.py
+++ b/pandas/core/tests/test_sparse.py
@@ -867,6 +867,10 @@ class TestSparseWidePanel(TestCase):
         }
         self.panel = SparseWidePanel(self.data_dict)
 
+    def test_constructor(self):
+        self.assertRaises(Exception, SparseWidePanel, self.data_dict,
+                          items=['item0', 'item1', 'item2'])
+
     def test_from_dict(self):
         fd = SparseWidePanel.from_dict(self.data_dict)
         assert_sp_panel_equal(fd, self.panel)
