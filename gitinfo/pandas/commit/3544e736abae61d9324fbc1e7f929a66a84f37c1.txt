commit 3544e736abae61d9324fbc1e7f929a66a84f37c1
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Mon Dec 12 20:42:18 2011 -0500

    ENH: get_level_values accept level name

diff --git a/RELEASE.rst b/RELEASE.rst
index 2218193a1..a3cc5a140 100644
--- a/RELEASE.rst
+++ b/RELEASE.rst
@@ -81,6 +81,7 @@ pandas 0.6.1
     affecting DataFrame.dropna speed
   - Column deletion in DataFrame copies no data (computes views on blocks) (GH
     #158)
+  - MultiIndex.get_level_values can take the level name
 
 **Bug fixes**
 
diff --git a/pandas/core/index.py b/pandas/core/index.py
index 304b72cb2..ce92fabb6 100644
--- a/pandas/core/index.py
+++ b/pandas/core/index.py
@@ -925,8 +925,9 @@ class MultiIndex(Index):
         -------
         values : ndarray
         """
-        unique_vals = self.levels[level].values
-        labels = self.labels[level]
+        num = self._get_level_number(level)
+        unique_vals = self.levels[num].values
+        labels = self.labels[num]
         return unique_vals.take(labels)
 
     def format(self, space=2, sparsify=True, adjoin=True, names=False):
diff --git a/pandas/tests/test_index.py b/pandas/tests/test_index.py
index 68c568b9a..9aad135f5 100644
--- a/pandas/tests/test_index.py
+++ b/pandas/tests/test_index.py
@@ -7,8 +7,7 @@ import unittest
 
 import numpy as np
 
-from pandas.core.index import (Index, Int64Index, Factor,
-                               MultiIndex)
+from pandas.core.index import Index, Int64Index, Factor, MultiIndex
 from pandas.util.testing import assert_almost_equal
 import pandas.util.testing as tm
 import pandas._tseries as tseries
@@ -663,6 +662,10 @@ class TestMultiIndex(unittest.TestCase):
         expected = ['foo', 'foo', 'bar', 'baz', 'qux', 'qux']
         self.assert_(np.array_equal(result, expected))
 
+        result = self.index.get_level_values('first')
+        expected = self.index.get_level_values(0)
+        self.assert_(np.array_equal(result, expected))
+
     def test_nlevels(self):
         self.assertEquals(self.index.nlevels, 2)
 
