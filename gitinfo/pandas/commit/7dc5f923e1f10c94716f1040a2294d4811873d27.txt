commit 7dc5f923e1f10c94716f1040a2294d4811873d27
Author: y-p <yoval@gmx.com>
Date:   Sun Mar 17 06:08:54 2013 +0200

    ENH: replace variable lookup by constant .
    
    this is getting silly.

diff --git a/pandas/lib.pyx b/pandas/lib.pyx
index 850cd7fb9..e12b524dd 100644
--- a/pandas/lib.pyx
+++ b/pandas/lib.pyx
@@ -806,7 +806,7 @@ def write_csv_rows(list data, list data_index, int nlevels, list cols, object wr
             row = rows[j % N]
             row[0] = data_index[j]
             for i in range(ncols):
-                row[nlevels+i] = data[i][j]
+                row[1+i] = data[i][j]
 
             if j >= N-1 and j % N == N-1:
                 writer.writerows(rows)
