commit 634a41f0dc703b9aa7314806484555dd687fff4d
Author: jbrockmendel <jbrockmendel@gmail.com>
Date:   Tue Feb 11 20:17:26 2020 -0800

    CLN: remove odious kludge (#31907)

diff --git a/pandas/core/indexing.py b/pandas/core/indexing.py
index 44b3c3183..f498e1696 100755
--- a/pandas/core/indexing.py
+++ b/pandas/core/indexing.py
@@ -755,18 +755,11 @@ class _LocationIndexer(_NDFrameIndexerBase):
                     new_key = tup[:i] + (_NS,) + tup[i + 1 :]
 
                 else:
+                    # Note: the section.ndim == self.ndim check above
+                    #  rules out having DataFrame here, so we dont need to worry
+                    #  about transposing.
                     new_key = tup[:i] + tup[i + 1 :]
 
-                    # unfortunately need an odious kludge here because of
-                    # DataFrame transposing convention
-                    if (
-                        isinstance(section, ABCDataFrame)
-                        and i > 0
-                        and len(new_key) == 2
-                    ):
-                        a, b = new_key
-                        new_key = b, a
-
                     if len(new_key) == 1:
                         new_key = new_key[0]
 
