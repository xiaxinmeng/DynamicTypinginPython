commit b570153235891d6ba5e5e776c4afedf5c2514d92
Author: Wouter Overmeire <lodagro@gmail.com>
Date:   Mon Feb 13 15:26:45 2012 +0100

    ENH: wrap _repr_html in a scrollable div, switch over to info for large frame

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index 461cfa427..1b7c44ae9 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -463,7 +463,14 @@ class DataFrame(NDFrame):
         return com.console_encode(buf.getvalue())
 
     def _repr_html_(self):
-        return self.to_html()
+        if len(self.index) <= 1000 and len(self.columns)<= 20:
+            return ('<div style="max-height:1000px;'
+                    'max-width:1500px;overflow:auto;">' +
+                    self.to_html() + '</div>')
+        else:
+            buf = StringIO()
+            self.info(buf=buf, verbose=self._verbose_info)
+            return '<pre>' + com.console_encode(buf.getvalue()) + '</pre>'
 
     def __iter__(self):
         """
