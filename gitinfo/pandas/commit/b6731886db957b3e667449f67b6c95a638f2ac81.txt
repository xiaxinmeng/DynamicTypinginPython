commit b6731886db957b3e667449f67b6c95a638f2ac81
Author: Wouter De Coster <decosterwouter@gmail.com>
Date:   Fri Feb 22 16:13:30 2019 +0100

    DOC: Edited docstring of Interval (#25410)
    
    The docstring contained a repeated segment, which I removed.

diff --git a/pandas/_libs/interval.pyx b/pandas/_libs/interval.pyx
index eb511b1ad..e86b692e9 100644
--- a/pandas/_libs/interval.pyx
+++ b/pandas/_libs/interval.pyx
@@ -150,9 +150,6 @@ cdef class Interval(IntervalMixin):
         Left bound for the interval.
     right : orderable scalar
         Right bound for the interval.
-    closed : {'left', 'right', 'both', 'neither'}, default 'right'
-        Whether the interval is closed on the left-side, right-side, both or
-        neither.
     closed : {'right', 'left', 'both', 'neither'}, default 'right'
         Whether the interval is closed on the left-side, right-side, both or
         neither. See the Notes for more detailed explanation.
