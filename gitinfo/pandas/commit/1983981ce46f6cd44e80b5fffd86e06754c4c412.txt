commit 1983981ce46f6cd44e80b5fffd86e06754c4c412
Author: akittredge <andrewlkittredge@gmail.com>
Date:   Sat Jan 11 17:45:17 2014 -0500

    docstring grammar.

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index 0f49c976d..47ec392a1 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -718,7 +718,7 @@ class DataFrame(NDFrame):
         exclude : sequence, default None
             Columns or fields to exclude
         columns : sequence, default None
-            Column names to use. If the passed data do not have named
+            Column names to use. If the passed data do not have names
             associated with them, this argument provides names for the
             columns. Otherwise this argument indicates the order of the columns
             in the result (any names not found in the data will become all-NA
