commit 332e2e6fbdb1d7a1036297d0597aef392228c555
Author: Laura Collard, PhD <35954013+LauraCollard@users.noreply.github.com>
Date:   Tue Oct 1 16:43:52 2019 +0100

    DOC: Fixed PR06 docstring errors in pandas.DataFrame (#28718)

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index 16f34fee5..9467978f1 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -7948,7 +7948,7 @@ class DataFrame(NDFrame):
         ----------
         axis : {0 or 'index', 1 or 'columns'}, default 0
             The axis to use. 0 or 'index' for row-wise, 1 or 'columns' for column-wise
-        skipna : boolean, default True
+        skipna : bool, default True
             Exclude NA/null values. If an entire row/column is NA, the result
             will be NA.
 
@@ -7985,7 +7985,7 @@ class DataFrame(NDFrame):
         ----------
         axis : {0 or 'index', 1 or 'columns'}, default 0
             The axis to use. 0 or 'index' for row-wise, 1 or 'columns' for column-wise
-        skipna : boolean, default True
+        skipna : bool, default True
             Exclude NA/null values. If an entire row/column is NA, the result
             will be NA.
 
