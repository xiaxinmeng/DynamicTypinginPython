commit 2e33a704291ff5f559ee50b9cb5996ac73e33e55
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Thu Aug 11 13:30:53 2011 -0400

    MSC: don't assert Index.ndim == 1 because matplotlib sometimes adds axes

diff --git a/RELEASE.rst b/RELEASE.rst
index d9271db84..969b3ee5f 100644
--- a/RELEASE.rst
+++ b/RELEASE.rst
@@ -121,6 +121,7 @@ Release notes
   (disabled by default)
 * `DataFrame.pivot` generalized to enable pivoting multiple columns into a
   `WidePanel`
+* `DataFrame` constructor can accept structured / record arrays
 
 **API Changes**
 
diff --git a/pandas/core/index.py b/pandas/core/index.py
index 1927faa89..49801c4ef 100644
--- a/pandas/core/index.py
+++ b/pandas/core/index.py
@@ -47,7 +47,8 @@ class Index(np.ndarray):
             subarr = np.empty(len(data), dtype=dtype)
             subarr[:] = data
 
-        assert(subarr.ndim == 1)
+        # assert(subarr.ndim == 1)
+
         return subarr.view(cls)
 
     def summary(self):
