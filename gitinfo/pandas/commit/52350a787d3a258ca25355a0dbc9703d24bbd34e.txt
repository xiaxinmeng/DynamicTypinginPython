commit 52350a787d3a258ca25355a0dbc9703d24bbd34e
Author: y-p <yoval@gmx.com>
Date:   Sat Feb 1 13:27:08 2014 +0200

    BLD: travis, skip cache upload when no changes

diff --git a/ci/submit_ccache.sh b/ci/submit_ccache.sh
index bf118ff75..ba21b3780 100755
--- a/ci/submit_ccache.sh
+++ b/ci/submit_ccache.sh
@@ -2,6 +2,14 @@
 
 ccache -s
 
+MISSES=$(ccache -s | grep "cache miss" | grep -Po "\d+")
+echo "MISSES: $MISSES"
+
+if [ x"$MISSES" == x"0" ]; then
+    echo "No cache misses detected, skipping upload"
+    exit 0
+fi
+
 if [ "$IRON_TOKEN" ]; then
     rm -rf ~/ccache.7z
 
