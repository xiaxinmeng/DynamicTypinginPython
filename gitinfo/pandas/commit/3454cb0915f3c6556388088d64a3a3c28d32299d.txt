commit 3454cb0915f3c6556388088d64a3a3c28d32299d
Author: Chang She <chang@lambdafoundry.com>
Date:   Fri Apr 13 18:42:24 2012 -0400

    ENH: key in HDFStore

diff --git a/pandas/io/pytables.py b/pandas/io/pytables.py
index 32fe2c62b..9d0a6c2d7 100644
--- a/pandas/io/pytables.py
+++ b/pandas/io/pytables.py
@@ -167,6 +167,9 @@ class HDFStore(object):
     def __setitem__(self, key, value):
         self.put(key, value)
 
+    def __contains__(self, key):
+        return hasattr(self.handle.root, key)
+
     def __len__(self):
         return len(self.handle.root._v_children)
 
diff --git a/pandas/io/tests/test_pytables.py b/pandas/io/tests/test_pytables.py
index 4989b3a94..827fdc6f1 100644
--- a/pandas/io/tests/test_pytables.py
+++ b/pandas/io/tests/test_pytables.py
@@ -65,6 +65,13 @@ class TesttHDFStore(unittest.TestCase):
         self.store['d'] = tm.makePanel()
         repr(self.store)
 
+    def test_contains(self):
+        self.store['a'] = tm.makeTimeSeries()
+        self.store['b'] = tm.makeDataFrame()
+        self.assert_('a' in self.store)
+        self.assert_('b' in self.store)
+        self.assert_('c' not in self.store)
+
     def test_reopen_handle(self):
         self.store['a'] = tm.makeTimeSeries()
         self.store.open('w', warn=False)
