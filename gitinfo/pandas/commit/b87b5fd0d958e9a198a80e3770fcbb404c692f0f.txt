commit b87b5fd0d958e9a198a80e3770fcbb404c692f0f
Author: Mortada Mehyar <mortada@users.noreply.github.com>
Date:   Thu Jul 5 09:23:14 2018 -0700

    DOC: Add nrows to DataFrame.from_records docstring (#21735)

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index a42026656..7b5f31031 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -1215,6 +1215,8 @@ class DataFrame(NDFrame):
         coerce_float : boolean, default False
             Attempt to convert values of non-string, non-numeric objects (like
             decimal.Decimal) to floating point, useful for SQL result sets
+        nrows : int, default None
+            Number of rows to read if data is an iterator
 
         Returns
         -------
