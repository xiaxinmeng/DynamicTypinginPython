commit 4e7e7f0dd6f60556a2b3abc6078af043302ed852
Author: gfyoung <gfyoung17+GitHub@gmail.com>
Date:   Sun Apr 14 19:50:03 2019 -0700

    STY: Turn on flake8 for pandas/compat/__init__.py (#26093)

diff --git a/pandas/compat/__init__.py b/pandas/compat/__init__.py
index 54a7afd90..e7ca0d4c8 100644
--- a/pandas/compat/__init__.py
+++ b/pandas/compat/__init__.py
@@ -14,14 +14,11 @@ Key items to import for compatible code:
 Other items:
 * platform checker
 """
-# pylint disable=W0611
-# flake8: noqa
 
-import re
-from distutils.version import LooseVersion
-import sys
 import platform
+import re
 import struct
+import sys
 
 PY36 = sys.version_info >= (3, 6)
 PY37 = sys.version_info >= (3, 7)
@@ -110,6 +107,7 @@ def raise_with_traceback(exc, traceback=Ellipsis):
         _, _, traceback = sys.exc_info()
     raise exc.with_traceback(traceback)
 
+
 # In Python 3.7, the private re._pattern_type is removed.
 # Python 3.5+ have typing.re.Pattern
 if PY36:
