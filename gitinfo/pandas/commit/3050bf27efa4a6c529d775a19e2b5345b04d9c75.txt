commit 3050bf27efa4a6c529d775a19e2b5345b04d9c75
Author: Adam Klein <adamklein@gmail.com>
Date:   Tue Feb 28 20:51:57 2012 -0500

    BUG: fixed null-check per #839

diff --git a/pandas/core/series.py b/pandas/core/series.py
index d8f2d14a9..1d45d6243 100644
--- a/pandas/core/series.py
+++ b/pandas/core/series.py
@@ -520,9 +520,9 @@ copy : boolean, default False
         self._set_values(indexer, value)
 
     def _set_values(self, key, value):
-        if (isnull(value) and 
-            issubclass(self.dtype.type, (np.integer, np.bool_))):
-            raise ValueError('Cannot assign nan to integer series')
+        if issubclass(self.dtype.type, (np.integer, np.bool_)):
+            if np.isscalar(value) and isnull(value):
+                raise ValueError('Cannot assign nan to integer series')
 
         self.values[key] = value
 
