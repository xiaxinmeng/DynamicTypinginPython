commit 4094bedbf13230cd845be6771d3f5e8f829365a5
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Fri Jul 15 12:43:36 2011 -0400

    int32/int64 issues

diff --git a/pandas/core/internals.py b/pandas/core/internals.py
index 1b91cadcd..377ed8d61 100644
--- a/pandas/core/internals.py
+++ b/pandas/core/internals.py
@@ -698,7 +698,7 @@ def form_blocks(data, index, items):
         blocks.append(float_block)
 
     if len(int_dict):
-        int_block = _simple_blockify(int_dict, items, np.int64)
+        int_block = _simple_blockify(int_dict, items, np.int_)
         blocks.append(int_block)
 
     if len(bool_dict):
diff --git a/setup.py b/setup.py
index aaac816ea..d6531c8e6 100755
--- a/setup.py
+++ b/setup.py
@@ -80,13 +80,13 @@ VERSION = '%d.%d.%d' % (MAJOR, MINOR, MICRO)
 
 FULLVERSION = VERSION
 if not ISRELEASED:
-    FULLVERSION += '.dev' + datetime.today().strftime('%Y%m%d')
+    FULLVERSION += '.dev'
     try:
         import subprocess
         pipe = subprocess.Popen(["git", "rev-parse", "--short", "HEAD"],
                                 stdout=subprocess.PIPE).stdout
         rev = pipe.read().strip()
-        FULLVERSION += "_%s" % rev
+        FULLVERSION += "-%s" % rev
     except:
         print "WARNING: Couldn't get git revision"
 
