commit db4095ec3f26c30892fe07989fa78b25ae4ca56b
Author: jbrockmendel <jbrockmendel@gmail.com>
Date:   Thu Nov 14 04:55:25 2019 -0800

    Check instead of try/except (#29587)

diff --git a/pandas/core/base.py b/pandas/core/base.py
index e070005c5..1c74c977e 100644
--- a/pandas/core/base.py
+++ b/pandas/core/base.py
@@ -602,9 +602,9 @@ class SelectionMixin:
         if not len(results):
             raise ValueError("no results")
 
-        try:
+        if all(np.ndim(x) > 0 for x in results):
             return concat(results, keys=keys, axis=1, sort=False)
-        except TypeError:
+        else:
 
             # we are concatting non-NDFrame objects,
             # e.g. a list of scalars
