commit 7ef4efa306bbd8685f849c9b65070523b10ecb15
Author: jreback <jeff@reback.net>
Date:   Fri Jun 13 15:50:51 2014 -0400

    ERR: fixup exception reports for duplicate axis reindexing, related (GH7432)

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index a00923da0..9c1d59318 100755
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -2022,9 +2022,13 @@ class DataFrame(NDFrame):
                 # GH 4107
                 try:
                     value = value.reindex(self.index).values
-                except ValueError as e:
-                    raise e
-                except:
+                except Exception as e:
+
+                    # duplicate axis
+                    if not value.index.is_unique:
+                        raise e
+
+                    # other
                     raise TypeError('incompatible index of inserted column '
                                     'with frame index')
 
diff --git a/pandas/tests/test_frame.py b/pandas/tests/test_frame.py
index ea77a4299..4ebf3dd99 100644
--- a/pandas/tests/test_frame.py
+++ b/pandas/tests/test_frame.py
@@ -2467,6 +2467,13 @@ class TestDataFrame(tm.TestCase, CheckIndexing,
 
     def test_insert_error_msmgs(self):
 
+        # GH 7432
+        df = DataFrame({'foo':['a', 'b', 'c'], 'bar':[1,2,3], 'baz':['d','e','f']}).set_index('foo')
+        s = DataFrame({'foo':['a', 'b', 'c', 'a'], 'fiz':['g','h','i','j']}).set_index('foo')
+        msg = 'cannot reindex from a duplicate axis'
+        with assertRaisesRegexp(ValueError, msg):
+            df['newcol'] = s
+
         # GH 4107, more descriptive error message
         df = DataFrame(np.random.randint(0,2,(4,4)),
                        columns=['a', 'b', 'c', 'd'])
