commit 3e9fc9cba73ed949f227c7551a6149077de2b543
Author: Valentin Haenel <valentin.haenel@gmx.de>
Date:   Tue Aug 6 21:28:00 2013 +0200

    BUG: off-by-one when doing network test repeats
    
    Imagine num_runs is two, then runs will take on the values [0, 1] and runs <
    num_uns will always be False. Hence any genuine Exception the code might raise
    will be silently ignored and just printed.

diff --git a/pandas/util/testing.py b/pandas/util/testing.py
index 0481a522d..e7e930320 100644
--- a/pandas/util/testing.py
+++ b/pandas/util/testing.py
@@ -860,7 +860,7 @@ def network(t, raise_on_error=_RAISE_NETWORK_ERROR_DEFAULT,
                 except SkipTest:
                     raise
                 except Exception as e:
-                    if runs < num_runs:
+                    if runs < num_runs - 1:
                         print("Failed: %r" % e)
                     else:
                         raise
