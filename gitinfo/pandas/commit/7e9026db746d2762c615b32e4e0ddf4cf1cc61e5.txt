commit 7e9026db746d2762c615b32e4e0ddf4cf1cc61e5
Author: juricast <jurica.stanojkovic@rt-rk.com>
Date:   Thu Jun 25 10:47:04 2015 +0200

    BUG #GH10425 test_categorical big-endian fix
    
    whatsnew modified

diff --git a/doc/source/whatsnew/v0.17.0.txt b/doc/source/whatsnew/v0.17.0.txt
index 5b5814cb5..812743816 100644
--- a/doc/source/whatsnew/v0.17.0.txt
+++ b/doc/source/whatsnew/v0.17.0.txt
@@ -79,3 +79,4 @@ Bug Fixes
 - Bug in ``ExcelReader`` when worksheet is empty (:issue:`6403`)
 - Bug in ``Table.select_column`` where name is not preserved (:issue:`10392`)
 - Bug in ``DataFrame.interpolate`` with ``axis=1`` and ``inplace=True`` (:issue:`10395`)
+- Bug in ``test_categorical`` on big-endian builds (:issue:`10425`)
diff --git a/pandas/tests/test_categorical.py b/pandas/tests/test_categorical.py
index 3a2f388bd..beff41fd9 100755
--- a/pandas/tests/test_categorical.py
+++ b/pandas/tests/test_categorical.py
@@ -2883,7 +2883,7 @@ class TestCategoricalAsBlock(tm.TestCase):
         # this coerces
         result = df.to_records()
         expected = np.rec.array([(0, 'a'), (1, 'b'), (2, 'c')],
-                                dtype=[('index', '<i8'), ('0', 'O')])
+                                dtype=[('index', '=i8'), ('0', 'O')])
         tm.assert_almost_equal(result, expected)
 
     def test_numeric_like_ops(self):
