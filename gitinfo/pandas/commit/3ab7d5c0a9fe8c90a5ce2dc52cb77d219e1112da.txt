commit 3ab7d5c0a9fe8c90a5ce2dc52cb77d219e1112da
Author: Jean Helie <jean@semmle.com>
Date:   Mon Aug 7 11:46:06 2017 +0100

    use == to test String equality (#17171)

diff --git a/pandas/io/parquet.py b/pandas/io/parquet.py
index 0a4426b55..09603fd6f 100644
--- a/pandas/io/parquet.py
+++ b/pandas/io/parquet.py
@@ -10,10 +10,10 @@ from pandas.io.common import get_filepath_or_buffer
 def get_engine(engine):
     """ return our implementation """
 
-    if engine is 'auto':
+    if engine == 'auto':
         engine = get_option('io.parquet.engine')
 
-    if engine is 'auto':
+    if engine == 'auto':
         # try engines in this order
         try:
             return PyArrowImpl()
