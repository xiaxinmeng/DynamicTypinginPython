commit 04cc1eb08c6e267b73378794c2a2ca8a9f2961cc
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Sun Sep 25 17:04:46 2011 -0400

    ENH: don't raise exception when calling remove on non-existent node

diff --git a/pandas/io/pytables.py b/pandas/io/pytables.py
index 38e6c30a3..9a033ccaa 100644
--- a/pandas/io/pytables.py
+++ b/pandas/io/pytables.py
@@ -281,8 +281,9 @@ class HDFStore(object):
         if where is None:
             self.handle.removeNode(self.handle.root, key, recursive=True)
         else:
-            group = getattr(self.handle.root, key)
-            self._delete_from_table(group, where)
+            group = getattr(self.handle.root, key,None)
+            if group is not None:
+                self._delete_from_table(group, where)
 
     def append(self, key, value):
         """
diff --git a/pandas/io/tests/test_pytables.py b/pandas/io/tests/test_pytables.py
index be3faff37..e6d8cada8 100644
--- a/pandas/io/tests/test_pytables.py
+++ b/pandas/io/tests/test_pytables.py
@@ -129,6 +129,14 @@ class TesttHDFStore(unittest.TestCase):
         self.store.remove('b')
         self.assertEquals(len(self.store), 0)
 
+    def test_remove_where_not_exist(self):
+        crit1 = {
+            'field' : 'index',
+            'op' : '>',
+            'value' : 'foo'
+        }
+        self.store.remove('a', where=[crit1])
+
     def test_remove_crit(self):
         wp = tm.makePanel()
         self.store.put('wp', wp, table=True)
