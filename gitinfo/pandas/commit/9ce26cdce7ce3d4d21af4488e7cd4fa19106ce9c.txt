commit 9ce26cdce7ce3d4d21af4488e7cd4fa19106ce9c
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Tue Sep 27 00:12:35 2011 -0400

    ENH: major speed optimization in Index.intersection

diff --git a/pandas/core/index.py b/pandas/core/index.py
index 7713bc642..66082dd8e 100644
--- a/pandas/core/index.py
+++ b/pandas/core/index.py
@@ -255,8 +255,9 @@ class Index(np.ndarray):
         if self.equals(other):
             return self
 
-        theIntersection = sorted(set(self) & set(other))
-        return Index(theIntersection)
+        indexer = self.get_indexer(other)
+        indexer = indexer.take((indexer != -1).nonzero()[0])
+        return self.take(indexer)
 
     def diff(self, other):
         """
diff --git a/pandas/tests/test_index.py b/pandas/tests/test_index.py
index f3bb75d99..bddc46026 100644
--- a/pandas/tests/test_index.py
+++ b/pandas/tests/test_index.py
@@ -305,7 +305,7 @@ class TestIndex(unittest.TestCase):
         # intersection broken?
         int_idx = idx1.intersection(idx2)
         # needs to be 1d like idx1 and idx2
-        expected = pandas.Index(sorted(set(idx1) & set(idx2)))
+        expected = idx1[:4] # pandas.Index(sorted(set(idx1) & set(idx2)))
         self.assert_(int_idx.ndim == 1)
         self.assert_(int_idx.equals(expected))
 
