commit 117e9f474ad6cce01f0f527da3a97f0d14bb7119
Author: jreback <jeff@reback.net>
Date:   Tue Jan 28 19:56:53 2014 -0500

    TST: rec arrays don't support datetimes in creation on certain platforms, related (GH6140)

diff --git a/pandas/tests/test_frame.py b/pandas/tests/test_frame.py
index f9218d34f..50c5cca93 100644
--- a/pandas/tests/test_frame.py
+++ b/pandas/tests/test_frame.py
@@ -3967,7 +3967,14 @@ class TestDataFrame(tm.TestCase, CheckIndexing,
 
         arrdata = [np.array([datetime(2005, 3, 1, 0, 0), None])]
         dtypes = [('EXPIRY', '<M8[ns]')]
-        recarray = np.core.records.fromarrays(arrdata, dtype=dtypes)
+
+        # this may fail on certain platforms because of a numpy issue
+        # related GH6140
+        try:
+            recarray = np.core.records.fromarrays(arrdata, dtype=dtypes)
+        except (ValueError):
+            raise nose.SkipTest('rec arrays with datetimes not supported')
+
         result = DataFrame.from_records(recarray)
         assert_frame_equal(result,expected)
 
