commit 5e0495706f7ca9e95227442adac5b87cb9d2441e
Author: jbrockmendel <jbrockmendel@gmail.com>
Date:   Sun Mar 29 07:51:28 2020 -0700

    CLN: avoid internals in tz_convert, tz_localize (#33085)

diff --git a/pandas/core/generic.py b/pandas/core/generic.py
index 534804080..99ae519a7 100644
--- a/pandas/core/generic.py
+++ b/pandas/core/generic.py
@@ -9244,7 +9244,7 @@ class NDFrame(PandasObject, SelectionMixin, indexing.IndexingMixin):
                 raise ValueError(f"The level {level} is not valid")
             ax = _tz_convert(ax, tz)
 
-        result = self._constructor(self._data, copy=copy)
+        result = self.copy(deep=copy)
         result = result.set_axis(ax, axis=axis, inplace=False)
         return result.__finalize__(self)
 
@@ -9413,7 +9413,7 @@ class NDFrame(PandasObject, SelectionMixin, indexing.IndexingMixin):
                 raise ValueError(f"The level {level} is not valid")
             ax = _tz_localize(ax, tz, ambiguous, nonexistent)
 
-        result = self._constructor(self._data, copy=copy)
+        result = self.copy(deep=copy)
         result = result.set_axis(ax, axis=axis, inplace=False)
         return result.__finalize__(self)
 
