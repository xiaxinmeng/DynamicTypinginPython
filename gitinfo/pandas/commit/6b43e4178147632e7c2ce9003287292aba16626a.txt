commit 6b43e4178147632e7c2ce9003287292aba16626a
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Wed Dec 28 22:06:07 2011 -0500

    TST: verify fix for #525

diff --git a/pandas/core/panel.py b/pandas/core/panel.py
index 8e218e2fc..a37f7462a 100644
--- a/pandas/core/panel.py
+++ b/pandas/core/panel.py
@@ -855,7 +855,7 @@ class Panel(NDFrame):
         -------
         y : DataFrame
         """
-        I, N, K = self.shape
+        _, N, K = self.shape
 
         if filter_observations:
             mask = com.notnull(self.values).all(axis=0)
diff --git a/pandas/tests/test_panel.py b/pandas/tests/test_panel.py
index 3f1e54994..add5391e0 100644
--- a/pandas/tests/test_panel.py
+++ b/pandas/tests/test_panel.py
@@ -841,12 +841,16 @@ class TestPanel(unittest.TestCase, PanelTests, CheckIndexing,
     def test_to_frame(self):
         # filtered
         filtered = self.panel.to_frame()
+        expected = self.panel.to_frame().dropna(how='any')
+        assert_frame_equal(filtered, expected)
 
         # unfiltered
         unfiltered = self.panel.to_frame(filter_observations=False)
-
         assert_panel_equal(unfiltered.to_panel(), self.panel)
 
+        # names
+        self.assertEqual(unfiltered.index.names, ['major', 'minor'])
+
     def test_to_frame_mixed(self):
         panel = self.panel.fillna(0)
         panel['str'] = 'foo'
