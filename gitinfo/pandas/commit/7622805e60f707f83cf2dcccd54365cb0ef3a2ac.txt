commit 7622805e60f707f83cf2dcccd54365cb0ef3a2ac
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Fri Jul 29 20:34:28 2011 -0400

    BUG: calling select on empty DataFrame generates Exception

diff --git a/pandas/core/generic.py b/pandas/core/generic.py
index d11113365..f2f5af91d 100644
--- a/pandas/core/generic.py
+++ b/pandas/core/generic.py
@@ -134,7 +134,12 @@ class PandasObject(Picklable):
         """
         axis_name = self._get_axis_name(axis)
         axis = self._get_axis(axis)
-        new_axis = axis[np.asarray([crit(label) for label in axis])]
+
+        if len(axis) > 0:
+            new_axis = axis[np.asarray([crit(label) for label in axis])]
+        else:
+            new_axis = axis
+
         return self.reindex(**{axis_name : new_axis})
 
     def drop(self, labels, axis=0):
diff --git a/pandas/tests/test_frame.py b/pandas/tests/test_frame.py
index 8ce270642..9e722bfaf 100644
--- a/pandas/tests/test_frame.py
+++ b/pandas/tests/test_frame.py
@@ -1837,6 +1837,15 @@ class TestDataFrame(unittest.TestCase, CheckIndexing):
         filtered = self.mixed_frame.filter(like='foo')
         self.assert_('foo' in filtered)
 
+    def test_filter_corner(self):
+        empty = DataFrame()
+
+        result = empty.filter([])
+        assert_frame_equal(result, empty)
+
+        result = empty.filter(like='foo')
+        assert_frame_equal(result, empty)
+
     def test_select(self):
         f = lambda x: x.weekday() == 2
         result = self.tsframe.select(f, axis=0)
