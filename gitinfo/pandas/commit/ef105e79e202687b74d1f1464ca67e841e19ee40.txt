commit ef105e79e202687b74d1f1464ca67e841e19ee40
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Sun Aug 12 18:01:22 2012 -0400

    DOC: default of left_index/right_index is False close #1758

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index b72f9ce4c..4c1264bc0 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -127,11 +127,11 @@ left_on : label or list, or array-like
 right_on : label or list, or array-like
     Field names to join on in right DataFrame or vector/list of vectors per
     left_on docs
-left_index : boolean, default True
+left_index : boolean, default False
     Use the index from the left DataFrame as the join key(s). If it is a
     MultiIndex, the number of keys in the other DataFrame (either the index
     or a number of columns) must match the number of levels
-right_index : boolean, default True
+right_index : boolean, default False
     Use the index from the right DataFrame as the join key. Same caveats as
     left_index
 sort : boolean, default True
