commit fe12c8aba5250ae6298132cee15b761feef0cf5b
Author: Abraham Flaxman <abie@alum.mit.edu>
Date:   Mon Oct 8 14:08:12 2012 -0700

    ENH: special option for writing infs in csvs no longer needed

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index 8d21847e6..40e959e9d 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -1105,9 +1105,7 @@ class DataFrame(NDFrame):
             for i, col in enumerate(cols):
                 val = series[col][j]
                 if lib.checknull(val):
-                    # HACK: special-case option for including nan, inf, -inf in csv
-                    if na_rep != 'nans_and_infs':
-                        val = na_rep
+                    val = na_rep
 
                 if float_format is not None and com.is_float(val):
                     val = float_format % val
diff --git a/pandas/tests/test_frame.py b/pandas/tests/test_frame.py
index 60ac6b3ad..485557db9 100644
--- a/pandas/tests/test_frame.py
+++ b/pandas/tests/test_frame.py
@@ -3389,7 +3389,7 @@ class TestDataFrame(unittest.TestCase, CheckIndexing,
         self.frame['G'] = np.nan
         self.frame['H'] = self.frame.index.map(lambda x: [np.inf, np.nan][np.random.rand() < .5])
 
-        self.frame.to_csv(path, na_rep='nans_and_infs')
+        self.frame.to_csv(path)
         recons = DataFrame.from_csv(path)
 
         assert_frame_equal(self.frame, recons)
@@ -3404,7 +3404,7 @@ class TestDataFrame(unittest.TestCase, CheckIndexing,
         self.frame['E'] = np.inf
         self.frame['F'] = -np.inf
 
-        self.frame.to_csv(path, na_rep='nans_and_infs')
+        self.frame.to_csv(path)
         recons = DataFrame.from_csv(path)
 
         assert_frame_equal(self.frame, recons)
