commit c9126a586ca7dbc2f20e29c51985498fde8f52e7
Author: jreback <jeff@reback.net>
Date:   Wed Feb 19 10:23:17 2014 -0500

    TST: GH6410 / numpy 4328

diff --git a/pandas/core/groupby.py b/pandas/core/groupby.py
index 0919309af..817cf7c5b 100644
--- a/pandas/core/groupby.py
+++ b/pandas/core/groupby.py
@@ -1849,7 +1849,6 @@ class SeriesGroupBy(GroupBy):
 
         for name, group in self:
 
-            group = com.ensure_float(group)
             object.__setattr__(group, 'name', name)
             res = wrapper(group)
             if hasattr(res, 'values'):
diff --git a/pandas/tests/test_groupby.py b/pandas/tests/test_groupby.py
index 4f0b12ca8..3fb0d4452 100644
--- a/pandas/tests/test_groupby.py
+++ b/pandas/tests/test_groupby.py
@@ -2731,6 +2731,15 @@ class TestGroupBy(tm.TestCase):
         result = df.groupby('A')['A'].max()
         assert_series_equal(result,expected)
 
+    def test_groupby_datetime64_32_bit(self):
+        # GH 6410 / numpy 4328
+        # 32-bit under 1.9-dev indexing issue
+
+        df = DataFrame({"A": range(2), "B": [pd.Timestamp('2000-01-1')]*2})
+        result = df.groupby("A")["B"].transform(min)
+        expected = Series([pd.Timestamp('2000-01-1')]*2)
+        assert_series_equal(result,expected)
+
     def test_groupby_categorical_unequal_len(self):
         import pandas as pd
         #GH3011
