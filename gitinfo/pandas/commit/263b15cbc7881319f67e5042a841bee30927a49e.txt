commit 263b15cbc7881319f67e5042a841bee30927a49e
Author: Adam Klein <adamklein@gmail.com>
Date:   Tue Jan 31 13:57:46 2012 -0500

    BUG: closes #719, sortedness check in to_panel

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index 6e4799507..f90d4c2db 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -815,8 +815,9 @@ class DataFrame(NDFrame):
 
         self._consolidate_inplace()
 
-        if not self.index.is_monotonic:
-            selfsorted = self.sort()
+        # outermost level of labels must be sorted
+        if self.index.lexsort_depth < 1:
+            selfsorted = self.sortlevel(0)
         else:
             selfsorted = self
 
