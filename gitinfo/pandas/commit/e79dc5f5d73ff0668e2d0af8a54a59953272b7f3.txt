commit e79dc5f5d73ff0668e2d0af8a54a59953272b7f3
Author: MomIsBestFriend <50263213+MomIsBestFriend@users.noreply.github.com>
Date:   Thu Apr 2 01:09:49 2020 +0300

    CI: Checking all the examples in `pandas/core/series.py` (#33225)

diff --git a/ci/code_checks.sh b/ci/code_checks.sh
index d30785d67..c8d08277e 100755
--- a/ci/code_checks.sh
+++ b/ci/code_checks.sh
@@ -271,8 +271,7 @@ if [[ -z "$CHECK" || "$CHECK" == "doctests" ]]; then
     RET=$(($RET + $?)) ; echo $MSG "DONE"
 
     MSG='Doctests series.py' ; echo $MSG
-    pytest -q --doctest-modules pandas/core/series.py \
-        -k"-nonzero -reindex -searchsorted -to_dict"
+    pytest -q --doctest-modules pandas/core/series.py
     RET=$(($RET + $?)) ; echo $MSG "DONE"
 
     MSG='Doctests groupby.py' ; echo $MSG
