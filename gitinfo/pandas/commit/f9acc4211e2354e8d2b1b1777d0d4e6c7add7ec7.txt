commit f9acc4211e2354e8d2b1b1777d0d4e6c7add7ec7
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Fri Aug 19 22:13:47 2011 -0400

    ENH: revert to old GroupBy.get_group for DataFrame due to perf

diff --git a/pandas/core/groupby.py b/pandas/core/groupby.py
index a5c9aea3d..a54fc92ec 100644
--- a/pandas/core/groupby.py
+++ b/pandas/core/groupby.py
@@ -117,7 +117,7 @@ class GroupBy(object):
         if obj is None:
             obj = self.obj
 
-        labels = self.primary.get_group_labels(name)
+        labels = self.groups[name]
         axis_name = obj._get_axis_name(self.axis)
         return obj.reindex(**{axis_name : labels})
 
@@ -481,13 +481,13 @@ class SeriesGroupBy(GroupBy):
     def _agg_stride_shape(self):
         return ()
 
-    def get_group(self, name, obj=None):
-        # faster get_group for Series
-        if obj is None:
-            obj = self.obj
+    # def get_group(self, name, obj=None):
+    #     # faster get_group for Series
+    #     if obj is None:
+    #         obj = self.obj
 
-        inds = self.primary.indices[name]
-        return obj.take(inds)
+    #     inds = self.primary.indices[name]
+    #     return obj.take(inds)
 
     def aggregate(self, arg):
         """
@@ -657,14 +657,6 @@ def _ravel_names(axes, shape):
 
 class DataFrameGroupBy(GroupBy):
 
-    def get_group(self, name, obj=None):
-        # faster get_group for Series
-        if obj is None:
-            obj = self.obj
-
-        inds = self.primary.indices[name]
-        return obj.take(inds, axis=self.axis)
-
     @property
     def _agg_stride_shape(self):
         if self.axis == 0:
diff --git a/pandas/tests/test_index.py b/pandas/tests/test_index.py
index b09e0c751..9c2bc46df 100644
--- a/pandas/tests/test_index.py
+++ b/pandas/tests/test_index.py
@@ -233,7 +233,7 @@ class TestIndex(unittest.TestCase):
 
         index = Index([datetime.now()])
         formatted = index.format()
-        expected = str(index[0])
+        expected = [str(index[0])]
         self.assertEquals(formatted, expected)
 
     def test_take(self):
