commit 0bb24b7ac5c58bf9115485bba8c8b97442ab5059
Author: gfyoung <gfyoung17+GitHub@gmail.com>
Date:   Mon Nov 5 02:19:28 2018 -0800

    DOC: Fix syntax error in groupby docs (#23498)
    
    Follow-up to gh-23394.

diff --git a/doc/source/groupby.rst b/doc/source/groupby.rst
index 0a896bac0..17a723e2a 100644
--- a/doc/source/groupby.rst
+++ b/doc/source/groupby.rst
@@ -133,7 +133,8 @@ but the specified columns
 .. ipython:: python
 
    df2 = df.set_index(['A', 'B'])
-   grouped = df2.groupby(level=df2.index.names.difference(['B'])
+   grouped = df2.groupby(level=df2.index.names.difference(['B']))
+   grouped.sum()
 
 These will split the DataFrame on its index (rows). We could also split by the
 columns:
