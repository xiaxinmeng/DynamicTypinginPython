commit 3de54aef0fa5cbe786bb3645a03fb64d90a285f4
Author: Martin Blais <blais@furius.ca>
Date:   Sat Oct 13 15:04:48 2012 -0400

    BUG: Fixed bugs with Python 3.3 from a simple 'import pandas'

diff --git a/pandas/core/internals.py b/pandas/core/internals.py
index 156ccbd95..a2a74d50d 100644
--- a/pandas/core/internals.py
+++ b/pandas/core/internals.py
@@ -25,6 +25,7 @@ class Block(object):
         assert(values.ndim == ndim)
         assert(len(items) == len(values))
 
+        self._ref_locs = None
         self.values = values
         self.ndim = ndim
         self.items = _ensure_index(items)
@@ -39,7 +40,6 @@ class Block(object):
         # monotonicity
         return (self.ref_locs[1:] > self.ref_locs[:-1]).all()
 
-    _ref_locs = None
     @property
     def ref_locs(self):
         if self._ref_locs is None:
@@ -479,7 +479,7 @@ class BlockManager(object):
     -----
     This is *not* a public API class
     """
-    __slots__ = ['axes', 'blocks', 'ndim']
+    __slots__ = ['axes', 'blocks']
 
     def __init__(self, blocks, axes, do_integrity_check=True):
         self.axes = [_ensure_index(ax) for ax in axes]
