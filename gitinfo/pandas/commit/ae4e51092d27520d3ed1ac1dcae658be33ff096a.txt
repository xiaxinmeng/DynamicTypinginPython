commit ae4e51092d27520d3ed1ac1dcae658be33ff096a
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Sun Oct 9 16:14:01 2011 -0400

    BUG: explicit CSV lineterminator='\n' to work around 3.1/3.2 csv module bug on Win32

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index 404a9498f..633849255 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -498,7 +498,7 @@ class DataFrame(NDFrame):
         mode : Python write mode, default 'w'
         """
         f = open(path, mode)
-        csvout = csv.writer(f)
+        csvout = csv.writer(f, lineterminator='\n')
 
         if cols is None:
             cols = self.columns
diff --git a/setup.py b/setup.py
index a3f5ab51b..092de4057 100755
--- a/setup.py
+++ b/setup.py
@@ -23,11 +23,12 @@ if sys.version_info.major >= 3:
         _have_setuptools = True
     except ImportError:
         raise ImportError('require setuptools/distribute for Py3k')
-    setuptools_args = {'use_2to3': True}
+    setuptools_args = {'use_2to3': True,
+                       'zip_safe': False,}
 else:
     setuptools_args = {}
 
-    
+
 import numpy as np
 
 # from numpy.distutils.core import setup
