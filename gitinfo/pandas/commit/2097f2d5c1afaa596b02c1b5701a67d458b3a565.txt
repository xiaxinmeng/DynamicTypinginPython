commit 2097f2d5c1afaa596b02c1b5701a67d458b3a565
Author: Phillip Cloud <cpcloud@gmail.com>
Date:   Mon Sep 16 21:04:48 2013 -0400

    DOC: show a more informative query performance plot
    
    Corrects the plot to show a larger range of numbers of rows to see where the
    Python and NumExpr lines intersect.

diff --git a/bench/bench_with_subset.py b/bench/bench_with_subset.py
index 99b98c983..017401df3 100644
--- a/bench/bench_with_subset.py
+++ b/bench/bench_with_subset.py
@@ -112,5 +112,5 @@ if __name__ == '__main__':
 
     plot_perf(ev[ev.size <= 50000], engines, 'DataFrame.eval()',
               filename=join('eval-perf-small.png'))
-    plot_perf(qu[qu.size <= 100000], engines, 'DataFrame.query()',
+    plot_perf(qu[qu.size <= 500000], engines, 'DataFrame.query()',
               filename=join('query-perf-small.png'))
diff --git a/doc/source/_static/query-perf-small.png b/doc/source/_static/query-perf-small.png
index 56fcc787a..e14fa69db 100644
Binary files a/doc/source/_static/query-perf-small.png and b/doc/source/_static/query-perf-small.png differ
diff --git a/doc/source/indexing.rst b/doc/source/indexing.rst
index 2f2a47d4b..e7af0e325 100644
--- a/doc/source/indexing.rst
+++ b/doc/source/indexing.rst
@@ -969,7 +969,7 @@ large frames
 .. note::
 
    You will only see the performance benefits of using the ``numexpr`` engine
-   with ``DataFrame.query()`` if your frame has more than approximately 50,000
+   with ``DataFrame.query()`` if your frame has more than approximately 200,000
    rows
 
       .. image:: _static/query-perf-small.png
