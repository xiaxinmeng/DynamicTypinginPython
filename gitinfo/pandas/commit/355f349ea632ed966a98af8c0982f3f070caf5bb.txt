commit 355f349ea632ed966a98af8c0982f3f070caf5bb
Author: Chang She <chang@lambdafoundry.com>
Date:   Thu Apr 26 16:23:51 2012 -0400

    ENH: add legend for subplots. GH #1108

diff --git a/pandas/tests/test_graphics.py b/pandas/tests/test_graphics.py
index c5095966b..a799b31ac 100644
--- a/pandas/tests/test_graphics.py
+++ b/pandas/tests/test_graphics.py
@@ -96,6 +96,10 @@ class TestDataFramePlots(unittest.TestCase):
                        index=MultiIndex.from_tuples(tuples))
         _check_plot_works(df.plot, use_index=True)
 
+        axes = df.plot(subplots=True)
+        for ax in axes:
+            self.assert_(ax.get_legend() is not None)
+
     @slow
     def test_plot_bar(self):
         df = DataFrame(np.random.randn(6, 4),
diff --git a/pandas/tools/plotting.py b/pandas/tools/plotting.py
index 485eee6e4..b5b985cee 100644
--- a/pandas/tools/plotting.py
+++ b/pandas/tools/plotting.py
@@ -345,7 +345,8 @@ class LinePlot(MPLPlot):
         df = self.data
 
         if self.subplots and self.legend:
-            self.axes[0].legend(loc='best')
+            for ax in self.axes:
+                ax.legend(loc='best')
 
         condition = (df.index.is_all_dates
                      and not self.subplots
