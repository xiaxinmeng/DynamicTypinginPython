commit f46a952b5fce479074575f1b61392ac8ab51b854
Author: Jeremy Wagner <jeremy.wagner@laposte.net>
Date:   Tue Feb 26 15:25:49 2013 +0100

    BUG: fixes handling of the 'kind' argument in Series.order (GH #2811)

diff --git a/pandas/core/series.py b/pandas/core/series.py
index 317806b9d..0495c666c 100644
--- a/pandas/core/series.py
+++ b/pandas/core/series.py
@@ -725,7 +725,7 @@ class Series(pa.Array, generic.PandasObject):
                 raise IndexError(key)
             # Could not hash item
         except ValueError:
-            
+
             # reassign a null value to iNaT
             if com.is_timedelta64_dtype(self.dtype):
                 if isnull(value):
@@ -2141,7 +2141,7 @@ class Series(pa.Array, generic.PandasObject):
         def _try_mergesort(arr):
             # easier to ask forgiveness than permission
             try:
-                return arr.argsort(kind='mergesort')
+                return arr.argsort(kind=kind)
             except TypeError:
                 # stable sort not available for object dtype
                 return arr.argsort()
