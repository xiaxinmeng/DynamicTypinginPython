commit 8be6c7de4124a104d44b65543d69e187b46d4e20
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Wed Nov 23 15:29:55 2011 -0500

    BUG: careful in to_html with MultiIndex

diff --git a/pandas/core/format.py b/pandas/core/format.py
index 04ad07dbe..b26512e7b 100644
--- a/pandas/core/format.py
+++ b/pandas/core/format.py
@@ -144,9 +144,9 @@ class DataFrameFormatter(object):
             # write values
             for i in range(len(frame)):
                 row = []
-                try:
+                if isinstance(frame.index, MultiIndex):
                     row.extend(frame.index[i])
-                except TypeError:
+                else:
                     row.append(frame.index[i])
                 for column in frame.columns:
                     row.append(format_col(column, i))
