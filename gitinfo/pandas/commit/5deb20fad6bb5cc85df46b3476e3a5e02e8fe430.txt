commit 5deb20fad6bb5cc85df46b3476e3a5e02e8fe430
Author: Garrett Drapala <drapala@gmail.com>
Date:   Thu Apr 11 03:21:58 2013 -0400

    BUG: fix to_html double escaping

diff --git a/pandas/core/format.py b/pandas/core/format.py
index d08892905..8b0ee8ce6 100644
--- a/pandas/core/format.py
+++ b/pandas/core/format.py
@@ -11,6 +11,7 @@ except:
 from pandas.core.common import adjoin, isnull, notnull
 from pandas.core.index import Index, MultiIndex, _ensure_index
 from pandas.util import py3compat
+from pandas.util.compat import OrderedDict
 from pandas.core.config import get_option, set_option, reset_option
 import pandas.core.common as com
 import pandas.lib as lib
@@ -520,7 +521,10 @@ class HTMLFormatter(TableFormatter):
             start_tag = '<%s>' % kind
 
         if self.escape:
-            esc = {'<' : r'&lt;', '>' : r'&gt;', '&' : r'&amp;'}
+            # escape & first to prevent double escaping of &
+            esc = OrderedDict(
+                [('&', r'&amp;'), ('<', r'&lt;'), ('>', r'&gt;')]
+            )
         else:
             esc = {}
         rs = com.pprint_thing(s, escape_chars=esc)
