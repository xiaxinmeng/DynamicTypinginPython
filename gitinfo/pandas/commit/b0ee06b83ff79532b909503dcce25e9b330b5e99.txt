commit b0ee06b83ff79532b909503dcce25e9b330b5e99
Author: Adam Klein <adamklein@gmail.com>
Date:   Fri Jan 27 09:56:43 2012 -0500

    BUG: closes #697, groupby level bug

diff --git a/pandas/core/groupby.py b/pandas/core/groupby.py
index 51238a262..1657be888 100644
--- a/pandas/core/groupby.py
+++ b/pandas/core/groupby.py
@@ -701,7 +701,8 @@ def _get_groupings(obj, grouper=None, axis=0, level=None, sort=True):
         all_in_columns = False
 
     if (not any_callable and not all_in_columns
-        and not any_arraylike and match_axis_length):
+        and not any_arraylike and match_axis_length
+        and not level):
         groupers = [com._asarray_tuplesafe(groupers)]
 
     if isinstance(level, (tuple, list)):
diff --git a/pandas/tests/test_multilevel.py b/pandas/tests/test_multilevel.py
index 50a28f84c..0f263aaa4 100644
--- a/pandas/tests/test_multilevel.py
+++ b/pandas/tests/test_multilevel.py
@@ -687,6 +687,14 @@ x   q   30      3    -0.6662 -0.5243 -0.3580  0.89145  2.5838"""
         expected = grouped.transform(lambda x: x * 2)
         assert_series_equal(applied.reindex(expected.index), expected)
 
+    def test_groupby_corner(self):
+        midx = MultiIndex(levels=[['foo'],['bar'],['baz']],
+                          labels=[[0],[0],[0]], names=['one','two','three'])
+        df = DataFrame([np.random.rand(4)], columns=['a','b','c','d'],
+                       index=midx)
+        # should work
+        df.groupby(level='three')
+
     def test_join(self):
         a = self.frame.ix[:5, ['A']]
         b = self.frame.ix[2:, ['B', 'C']]
