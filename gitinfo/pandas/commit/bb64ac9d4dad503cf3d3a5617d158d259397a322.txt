commit bb64ac9d4dad503cf3d3a5617d158d259397a322
Author: Jacques Kvam <jwkvam@gmail.com>
Date:   Sun Jun 17 17:51:20 2012 -0700

    parse change_pct as a float instead of a string

diff --git a/pandas/io/data.py b/pandas/io/data.py
index 1a0d1c6bc..0cabd6885 100644
--- a/pandas/io/data.py
+++ b/pandas/io/data.py
@@ -95,9 +95,11 @@ def get_quote_yahoo(symbols):
 
     for line in lines:
         fields = line.strip().split(',')
-        #print fields
         for i,field in enumerate(fields):
-            if field[0] == '"':
+            # assumes change_pct is in the 5th index
+            if i == 5:
+                data[header[i]].append(float(field.strip('"%')))
+            elif field[0] == '"':
                 data[header[i]].append( field.strip('"'))
             else:
                 try:
