commit 7a5552e5f547e16ad05c73a49dd72f7e362d5aa7
Author: Dieter Vandenbussche <dvandenbussche@axioma.com>
Date:   Wed Mar 14 11:45:26 2012 -0400

    TST: run panel excel test with xls too, remove temp file

diff --git a/pandas/tests/test_panel.py b/pandas/tests/test_panel.py
index 95211daaa..2cfc472d2 100644
--- a/pandas/tests/test_panel.py
+++ b/pandas/tests/test_panel.py
@@ -1019,12 +1019,14 @@ class TestPanel(unittest.TestCase, PanelTests, CheckIndexing,
         except ImportError:
             raise nose.SkipTest
 
-        path = '__tmp__.xlsx'
-        self.panel.to_excel(path)
-        reader = ExcelFile(path)
-        for item, df in self.panel.iteritems():
-            recdf = reader.parse(str(item),index_col=0)
-            assert_frame_equal(df, recdf)
+        for ext in ['xls', 'xlsx']:
+            path = '__tmp__.' + ext
+            self.panel.to_excel(path)
+            reader = ExcelFile(path)
+            for item, df in self.panel.iteritems():
+                recdf = reader.parse(str(item),index_col=0)
+                assert_frame_equal(df, recdf)
+            os.remove(path)
 
 class TestLongPanel(unittest.TestCase):
     """
