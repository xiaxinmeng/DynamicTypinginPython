commit 03e59a5343cefa0f1475691757dfbe7a39852a43
Author: Skipper Seabold <jsseabold@gmail.com>
Date:   Mon Mar 26 14:27:52 2012 -0400

    ENH: Only give strings to _is_url

diff --git a/pandas/io/parsers.py b/pandas/io/parsers.py
index ecc4f9bf4..33aee1c66 100644
--- a/pandas/io/parsers.py
+++ b/pandas/io/parsers.py
@@ -112,11 +112,10 @@ def _is_url(url):
     else:
         return False
 
-
 def _read(cls, filepath_or_buffer, kwds):
     "Generic reader of line files."
 
-    if _is_url(filepath_or_buffer):
+    if isinstance(filepath_or_buffer, str) and _is_url(filepath_or_buffer):
         from urllib2 import urlopen
         filepath_or_buffer = urlopen(filepath_or_buffer)
 
