commit d115900f4f80d2f9016b41041bde1564980415b3
Author: Jeff Reback <jeff@reback.net>
Date:   Thu Sep 27 14:15:58 2018 -0400

    STYLE: lint

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index 0d0e186b1..d5b273f37 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -782,8 +782,8 @@ class DataFrame(NDFrame):
         r"""
         Iterator over (column name, Series) pairs.
 
-        Iterates over the DataFrame columns, returning a tuple with the column name
-        and the content as a Series.
+        Iterates over the DataFrame columns, returning a tuple with
+        the column name and the content as a Series.
 
         Yields
         ------
@@ -794,8 +794,10 @@ class DataFrame(NDFrame):
 
         See Also
         --------
-        DataFrame.iterrows : Iterate over DataFrame rows as (index, Series) pairs.
-        DataFrame.itertuples : Iterate over DataFrame rows as namedtuples of the values.
+        DataFrame.iterrows : Iterate over DataFrame rows as
+            (index, Series) pairs.
+        DataFrame.itertuples : Iterate over DataFrame rows as namedtuples
+            of the values.
 
         Examples
         --------
