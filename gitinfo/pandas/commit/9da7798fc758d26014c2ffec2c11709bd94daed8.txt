commit 9da7798fc758d26014c2ffec2c11709bd94daed8
Author: Joris Van den Bossche <jorisvandenbossche@gmail.com>
Date:   Sat May 13 14:10:52 2017 +0200

    DOC: add dataframe construction in merge_asof example (#16348)

diff --git a/pandas/core/reshape/merge.py b/pandas/core/reshape/merge.py
index c55f4b5bf..7bf25e373 100644
--- a/pandas/core/reshape/merge.py
+++ b/pandas/core/reshape/merge.py
@@ -347,12 +347,15 @@ def merge_asof(left, right, on=None,
 
     Examples
     --------
+    >>> left = pd.DataFrame({'a': [1, 5, 10], 'left_val': ['a', 'b', 'c']})
     >>> left
         a left_val
     0   1        a
     1   5        b
     2  10        c
 
+    >>> right = pd.DataFrame({'a': [1, 2, 3, 6, 7],
+    ...                       'right_val': [1, 2, 3, 6, 7]})
     >>> right
        a  right_val
     0  1          1
@@ -387,12 +390,15 @@ def merge_asof(left, right, on=None,
 
     We can use indexed DataFrames as well.
 
+    >>> left = pd.DataFrame({'left_val': ['a', 'b', 'c']}, index=[1, 5, 10])
     >>> left
        left_val
     1         a
     5         b
     10        c
 
+    >>> right = pd.DataFrame({'right_val': [1, 2, 3, 6, 7]},
+    ...                      index=[1, 2, 3, 6, 7])
     >>> right
        right_val
     1          1
