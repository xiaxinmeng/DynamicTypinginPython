commit 461e0e9fba67b1f5cbe6ac5bd1db0272042777ee
Author: Jeff Reback <jeff@reback.net>
Date:   Mon Sep 12 06:57:35 2016 -0400

    BLD: require cython if tempita is needed
    
    closes #14204

diff --git a/setup.py b/setup.py
index d1b97e1e3..846e2b7fa 100755
--- a/setup.py
+++ b/setup.py
@@ -125,6 +125,9 @@ for module, files in _pxi_dep_template.items():
 class build_ext(_build_ext):
     def build_extensions(self):
 
+        if not cython:
+            raise ImportError('Building pandas requires cython')
+
         for pxifile in _pxifiles:
             # build pxifiles first, template extention must be .pxi.in
             assert pxifile.endswith('.pxi.in')
