commit 41170d46db8345a5688cd7f5255389b8161b0f56
Author: Lukasz <uhho@users.noreply.github.com>
Date:   Wed Jan 11 00:37:14 2017 +0900

    fix misspelled word (#15097)
    
    `colunwise` should be `columnwise`

diff --git a/pandas/formats/style.py b/pandas/formats/style.py
index 4d5e72a38..b3e0f0f6c 100644
--- a/pandas/formats/style.py
+++ b/pandas/formats/style.py
@@ -781,7 +781,7 @@ class Styler(object):
         low, high: float
             compress the range by these values.
         axis: int or str
-            1 or 'columns' for colunwise, 0 or 'index' for rowwise
+            1 or 'columns' for columnwise, 0 or 'index' for rowwise
         subset: IndexSlice
             a valid slice for ``data`` to limit the style application to
 
