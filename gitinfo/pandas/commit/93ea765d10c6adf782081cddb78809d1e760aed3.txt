commit 93ea765d10c6adf782081cddb78809d1e760aed3
Author: Richard T. Guy <richardtguy84@gmail.com>
Date:   Fri Aug 23 10:32:41 2013 -0400

    ENH: avoid branch in thousands check. Thanks to @cancan101

diff --git a/pandas/src/parser/tokenizer.c b/pandas/src/parser/tokenizer.c
index 4da4bbabb..45b8b9263 100644
--- a/pandas/src/parser/tokenizer.c
+++ b/pandas/src/parser/tokenizer.c
@@ -1897,10 +1897,7 @@ static double xstrtod(const char *str, char **endptr, char decimal,
     p++;
     num_digits++;
 
-    if (tsep != '\0' && *p == tsep)
-    {
-      ++p;
-    }
+    p += (tsep != '\0' & *p == tsep);
   }
 
   // Process decimal part
