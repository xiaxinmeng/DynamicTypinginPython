commit 69d5bd8e9d5e94a23e80092523f510b5a0746874
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Thu Jan 26 22:38:30 2012 -0500

    BUG: integer slices should never access label-indexing, GH #700

diff --git a/pandas/core/series.py b/pandas/core/series.py
index 30702f689..3a0c50d94 100644
--- a/pandas/core/series.py
+++ b/pandas/core/series.py
@@ -309,7 +309,9 @@ copy : boolean, default False
     def _get_with(self, key):
         # other: fancy integer or otherwise
         if isinstance(key, slice):
-            if self.index.inferred_type == 'integer':
+            from pandas.core.indexing import _is_index_slice
+
+            if self.index.inferred_type == 'integer' or _is_index_slice(key):
                 indexer = key
             else:
                 indexer = self.ix._convert_to_indexer(key, axis=0)
@@ -390,7 +392,8 @@ copy : boolean, default False
     def _set_with(self, key, value):
         # other: fancy integer or otherwise
         if isinstance(key, slice):
-            if self.index.inferred_type == 'integer':
+            from pandas.core.indexing import _is_index_slice
+            if self.index.inferred_type == 'integer' or _is_index_slice(key):
                 indexer = key
             else:
                 indexer = self.ix._convert_to_indexer(key, axis=0)
diff --git a/pandas/tests/test_multilevel.py b/pandas/tests/test_multilevel.py
index 251b82ce7..0e5c66cdb 100644
--- a/pandas/tests/test_multilevel.py
+++ b/pandas/tests/test_multilevel.py
@@ -1015,6 +1015,21 @@ x   q   30      3    -0.6662 -0.5243 -0.3580  0.89145  2.5838"""
         assert_frame_equal(result, expected)
         assert_frame_equal(result2, expected)
 
+    def test_int_series_slicing(self):
+        s = self.ymd['A']
+        result = s[5:]
+        expected = s.reindex(s.index[5:])
+        assert_series_equal(result, expected)
+
+        exp = self.ymd['A'].copy()
+        s[5:] = 0
+        exp.values[5:] = 0
+        self.assert_(np.array_equal(s.values, exp.values))
+
+        result = self.ymd[5:]
+        expected = self.ymd.reindex(s.index[5:])
+        assert_frame_equal(result, expected)
+
 if __name__ == '__main__':
 
     # unittest.main()
