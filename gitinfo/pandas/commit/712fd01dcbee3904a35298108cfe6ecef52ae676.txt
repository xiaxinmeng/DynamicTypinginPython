commit 712fd01dcbee3904a35298108cfe6ecef52ae676
Author: jbrockmendel <jbrockmendel@gmail.com>
Date:   Mon Mar 2 17:34:30 2020 -0800

    CLN: remove _igetitem_cache (#32319)

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index 61715397e..bfb83b594 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -2741,7 +2741,7 @@ class DataFrame(NDFrame):
         """
         try:
             if takeable is True:
-                series = self._iget_item_cache(col)
+                series = self._ixs(col, axis=1)
                 series._set_value(index, value, takeable=True)
                 return
 
diff --git a/pandas/core/generic.py b/pandas/core/generic.py
index 5461c134f..d8fa829ee 100644
--- a/pandas/core/generic.py
+++ b/pandas/core/generic.py
@@ -3463,15 +3463,6 @@ class NDFrame(PandasObject, SelectionMixin, indexing.IndexingMixin):
             res._is_copy = self._is_copy
         return res
 
-    def _iget_item_cache(self, item: int):
-        """Return the cached item, item represents a positional indexer."""
-        ax = self._info_axis
-        if ax.is_unique:
-            lower = self._get_item_cache(ax[item])
-        else:
-            return self._ixs(item, axis=1)
-        return lower
-
     def _box_item_values(self, key, values):
         raise AbstractMethodError(self)
 
