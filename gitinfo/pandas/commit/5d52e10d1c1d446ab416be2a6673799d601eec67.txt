commit 5d52e10d1c1d446ab416be2a6673799d601eec67
Author: Chang She <chang@lambdafoundry.com>
Date:   Wed May 23 17:22:18 2012 -0400

    TST: test case for using frame column names in legend title f826c0e443

diff --git a/pandas/tests/test_graphics.py b/pandas/tests/test_graphics.py
index 6fe1f9344..e4cc57804 100644
--- a/pandas/tests/test_graphics.py
+++ b/pandas/tests/test_graphics.py
@@ -229,6 +229,17 @@ class TestDataFramePlots(unittest.TestCase):
         df = DataFrame(np.random.randn(100, 4)).cumsum()
         _check_plot_works(df.plot, legend=True)
 
+    @slow
+    def test_legend_name(self):
+        multi = DataFrame(np.random.randn(4, 4),
+                          columns=[np.array(['a', 'a', 'b', 'b']),
+                                   np.array(['x', 'y', 'x', 'y'])])
+        multi.columns.names = ['group', 'individual']
+
+        ax = multi.plot()
+        leg_title = ax.legend_.get_title()
+        self.assert_(leg_title.get_text(), 'group,individual')
+
     def _check_plot_fails(self, f, *args, **kwargs):
         self.assertRaises(Exception, f, *args, **kwargs)
 
