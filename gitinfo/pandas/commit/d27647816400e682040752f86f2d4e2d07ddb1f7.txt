commit d27647816400e682040752f86f2d4e2d07ddb1f7
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Tue Sep 27 23:09:20 2011 -0400

    ENH: change to buffered datetime check, is_monotonic function

diff --git a/pandas/core/index.py b/pandas/core/index.py
index e1bf367fc..8fd419338 100644
--- a/pandas/core/index.py
+++ b/pandas/core/index.py
@@ -255,7 +255,8 @@ class Index(np.ndarray):
 
     def intersection(self, other):
         """
-        Form the intersection of two Index objects and sorts if possible
+        Form the intersection of two Index objects. Sortedness of the result is
+        not guaranteed
 
         Parameters
         ----------
diff --git a/pandas/src/common.pyx b/pandas/src/common.pyx
index fd43bab8e..3704319a8 100644
--- a/pandas/src/common.pyx
+++ b/pandas/src/common.pyx
@@ -199,41 +199,35 @@ cdef class MultiMap:
         raise KeyError(key)
 
 
-def isAllDates(ndarray index):
-    cdef int i, length
-    cdef flatiter iter
+def isAllDates(ndarray[object, ndim=1] arr):
+    cdef int i, size = len(arr)
     cdef object date
 
-    iter = <flatiter> PyArray_IterNew(index)
-    length = PyArray_SIZE(index)
-
-    if length == 0:
+    if size == 0:
         return False
 
-    for i from 0 <= i < length:
-        date = PyArray_GETITEM(index, PyArray_ITER_DATA(iter))
+    for i from 0 <= i < size:
+        date = arr[i]
 
         if not PyDateTime_Check(date):
             return False
-        PyArray_ITER_NEXT(iter)
 
     return True
 
-def isAllDates2(ndarray[object, ndim=1] arr):
-    '''
-    cannot use
-    '''
+def is_monotonic(ndarray[object] arr):
+    cdef:
+        Py_ssize_t i, n
+        object prev, cur
 
-    cdef int i, size = len(arr)
-    cdef object date
+    n = len(arr)
 
-    if size == 0:
-        return False
+    if n < 2:
+        return True
 
-    for i from 0 <= i < size:
-        date = arr[i]
-
-        if not PyDateTime_Check(date):
+    prev = arr[0]
+    for i from 1 <= i < n:
+        cur = arr[i]
+        if cur < prev:
             return False
-
+        prev = cur
     return True
