commit 4d2aaf3008962aafd1feb9221f279440a8b09e1d
Author: Andy Hayden <andyhayden1@gmail.com>
Date:   Tue Mar 26 16:44:28 2013 +0000

    ENH add time to DatetimeIndex

diff --git a/pandas/tseries/index.py b/pandas/tseries/index.py
index 70e4a7010..dbc71760d 100644
--- a/pandas/tseries/index.py
+++ b/pandas/tseries/index.py
@@ -1289,6 +1289,13 @@ class DatetimeIndex(Int64Index):
     dayofyear = _field_accessor('dayofyear', 'doy')
     quarter = _field_accessor('quarter', 'q')
 
+    @property
+    def time(self):
+        """
+        Returns array of datetime.time. The time of the day
+        """
+        return self.map(lambda t: t.time())
+
     def normalize(self):
         """
         Return DatetimeIndex with times to midnight. Length is unaltered
diff --git a/pandas/tseries/tests/test_timeseries.py b/pandas/tseries/tests/test_timeseries.py
index 6073811b9..f0ade216f 100644
--- a/pandas/tseries/tests/test_timeseries.py
+++ b/pandas/tseries/tests/test_timeseries.py
@@ -1808,6 +1808,12 @@ class TestDatetimeIndex(unittest.TestCase):
         i1.union(i2)  # Works
         i2.union(i1)  # Fails with "AttributeError: can't set attribute"
 
+    def test_time(self):
+        rng = pd.date_range('1/1/2000', freq='12min', periods=10)
+        result = pd.Index(rng).time
+        expected = [t.time() for t in rng]
+        self.assert_((result == expected).all())
+
 
 class TestLegacySupport(unittest.TestCase):
     _multiprocess_can_split_ = True
