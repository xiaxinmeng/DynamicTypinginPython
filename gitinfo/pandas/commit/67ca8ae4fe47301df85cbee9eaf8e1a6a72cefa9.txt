commit 67ca8ae4fe47301df85cbee9eaf8e1a6a72cefa9
Author: jreback <jeff@reback.net>
Date:   Sat Mar 16 23:49:08 2013 -0400

    TST: test for to_csv on failing vbench
         duplicate column names across dtypes is a problem, and
         not-easy to fix, so letting test fail

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index 23d1ad08c..e2c053f53 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -1364,7 +1364,6 @@ class DataFrame(NDFrame):
                 break
 
             # create the data for a chunk
-
             blocks = self._data.blocks
             data =[None] * sum(len(b.items) for b in blocks)
             for i in range(len(blocks)):
