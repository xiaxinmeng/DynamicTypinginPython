commit a8104e97b48a1b1178d2a60e8390c425034e4c1f
Author: Thoralf Gutierrez <thoralf.gutierrez@realimpactanalytics.com>
Date:   Sun Jan 22 12:35:12 2017 +0100

    DOC: fix listing na_values in read_csv docstring - add subsequent_indent to wrapped text (#15173)

diff --git a/pandas/io/parsers.py b/pandas/io/parsers.py
index fdf26fdef..f8905dfa3 100755
--- a/pandas/io/parsers.py
+++ b/pandas/io/parsers.py
@@ -148,7 +148,8 @@ nrows : int, default None
 na_values : scalar, str, list-like, or dict, default None
     Additional strings to recognize as NA/NaN. If dict passed, specific
     per-column NA values.  By default the following values are interpreted as
-    NaN: '""" + fill("', '".join(sorted(_NA_VALUES)), 70) + """'`.
+    NaN: '""" + fill("', '".join(sorted(_NA_VALUES)),
+                     70, subsequent_indent="    ") + """'`.
 keep_default_na : bool, default True
     If na_values are specified and keep_default_na is False the default NaN
     values are overridden, otherwise they're appended to.
