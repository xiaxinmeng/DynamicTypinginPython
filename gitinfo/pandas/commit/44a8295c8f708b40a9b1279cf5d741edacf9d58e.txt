commit 44a8295c8f708b40a9b1279cf5d741edacf9d58e
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Fri Jun 22 15:16:00 2012 -0400

    BUG: fix NumPy 1.7 cast-to-bool failure and buglet

diff --git a/doc/source/timeseries.rst b/doc/source/timeseries.rst
index c616a0767..ac62bae50 100644
--- a/doc/source/timeseries.rst
+++ b/doc/source/timeseries.rst
@@ -297,7 +297,7 @@ We could have done the same thing with ``DateOffset``:
 
 .. ipython:: python
 
-   from pandas.core.datetools import *
+   from pandas.tseries.offsets import *
    d + DateOffset(months=4, days=5)
 
 The key features of a ``DateOffset`` object are:
diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index e84d51bbe..4d41c02ad 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -4226,9 +4226,9 @@ class DataFrame(NDFrame):
         if result.dtype == np.object_:
             try:
                 if filter_type is None or filter_type == 'numeric':
-                    result = result.astype('f8')
-                elif filter_type == 'bool':
-                    result = result.astype('b')
+                    result = result.astype(np.float64)
+                elif filter_type == 'bool' and notnull(result).all():
+                    result = result.astype(np.bool_)
                 else:
                     raise ValueError('Invalid dtype %s ' % str(filter_type))
 
diff --git a/pandas/core/index.py b/pandas/core/index.py
index db0bd1bd7..14edc86cc 100644
--- a/pandas/core/index.py
+++ b/pandas/core/index.py
@@ -405,7 +405,7 @@ class Index(np.ndarray):
     def asof_locs(self, where, mask):
         """
         where : array of timestamps
-        mask : array of booleans where data is NA
+        mask : array of booleans where data is not NA
 
         """
         locs = self.values[mask].searchsorted(where.values, side='right')
