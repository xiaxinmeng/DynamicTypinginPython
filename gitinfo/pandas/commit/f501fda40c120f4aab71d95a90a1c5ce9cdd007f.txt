commit f501fda40c120f4aab71d95a90a1c5ce9cdd007f
Author: jbrockmendel <jbrockmendel@gmail.com>
Date:   Thu Jan 2 16:48:23 2020 -0800

    REF: remove IntervalIndex.copy (#30627)

diff --git a/pandas/core/indexes/interval.py b/pandas/core/indexes/interval.py
index abc82dd3c..d16eb230b 100644
--- a/pandas/core/indexes/interval.py
+++ b/pandas/core/indexes/interval.py
@@ -460,17 +460,6 @@ class IntervalIndex(IntervalMixin, Index, accessor.PandasDelegate):
         d.update(self._get_attributes_dict())
         return _new_IntervalIndex, (type(self), d), None
 
-    @Appender(_index_shared_docs["copy"])
-    def copy(self, deep=False, name=None):
-        array = self._data
-        if deep:
-            array = array.copy()
-        attributes = self._get_attributes_dict()
-        if name is not None:
-            attributes.update(name=name)
-
-        return self._simple_new(array, **attributes)
-
     @Appender(_index_shared_docs["astype"])
     def astype(self, dtype, copy=True):
         with rewrite_exception("IntervalArray", type(self).__name__):
