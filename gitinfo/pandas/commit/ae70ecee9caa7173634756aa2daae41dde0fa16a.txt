commit ae70ecee9caa7173634756aa2daae41dde0fa16a
Author: Tom Augspurger <TomAugspurger@users.noreply.github.com>
Date:   Tue May 2 20:22:49 2017 -0500

    BUG: Fixed renaming of falsey names in build_table_schema (#16205)
    
    Closes https://github.com/pandas-dev/pandas/issues/16203

diff --git a/pandas/io/json/table_schema.py b/pandas/io/json/table_schema.py
index d8ef3afc9..c3865afa9 100644
--- a/pandas/io/json/table_schema.py
+++ b/pandas/io/json/table_schema.py
@@ -76,7 +76,11 @@ def set_default_names(data):
 
 def make_field(arr, dtype=None):
     dtype = dtype or arr.dtype
-    field = {'name': arr.name or 'values',
+    if arr.name is None:
+        name = 'values'
+    else:
+        name = arr.name
+    field = {'name': name,
              'type': as_json_table_type(dtype)}
 
     if is_categorical_dtype(arr):
diff --git a/pandas/tests/io/json/test_json_table_schema.py b/pandas/tests/io/json/test_json_table_schema.py
index 0f77a886d..c3a976973 100644
--- a/pandas/tests/io/json/test_json_table_schema.py
+++ b/pandas/tests/io/json/test_json_table_schema.py
@@ -461,3 +461,11 @@ class TestTableOrient(tm.TestCase):
                 data.to_json(orient='table')
 
             assert 'Overlapping' in str(excinfo.value)
+
+    def test_mi_falsey_name(self):
+        # GH 16203
+        df = pd.DataFrame(np.random.randn(4, 4),
+                          index=pd.MultiIndex.from_product([('A', 'B'),
+                                                            ('a', 'b')]))
+        result = [x['name'] for x in build_table_schema(df)['fields']]
+        assert result == ['level_0', 'level_1', 0, 1, 2, 3]
