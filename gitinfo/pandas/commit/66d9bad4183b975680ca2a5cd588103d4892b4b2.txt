commit 66d9bad4183b975680ca2a5cd588103d4892b4b2
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Thu Jan 5 13:31:21 2012 -0500

    TST: revert plm tinker so tests pass

diff --git a/pandas/stats/plm.py b/pandas/stats/plm.py
index 5734ba2e2..f5a077c1a 100644
--- a/pandas/stats/plm.py
+++ b/pandas/stats/plm.py
@@ -367,11 +367,11 @@ class PanelOLS(OLS):
     @cache_readonly
     def _rmse_raw(self):
         """Returns the raw rmse values."""
-        X = self._x.values
-        Y = self._y.values.squeeze()
+        # X = self._x.values
+        # Y = self._y.values.squeeze()
 
-        # X = self._x_trans.values
-        # Y = self._y_trans.values.squeeze()
+        X = self._x_trans.values
+        Y = self._y_trans.values.squeeze()
 
         resid = Y - np.dot(X, self._beta_raw)
         ss = (resid ** 2).sum()
diff --git a/scripts/roll_median_leak.py b/scripts/roll_median_leak.py
index bf4f16211..7cdec4865 100644
--- a/scripts/roll_median_leak.py
+++ b/scripts/roll_median_leak.py
@@ -18,5 +18,5 @@ proc = psutil.Process(pid)
 s = Series(np.random.randn(10000))
 
 for _ in xrange(1000):
-    print proc.get_memory_info()
+    # print proc.get_memory_info()
     result = rolling_median(s, 1000)
