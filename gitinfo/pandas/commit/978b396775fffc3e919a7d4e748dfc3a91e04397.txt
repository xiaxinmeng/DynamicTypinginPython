commit 978b396775fffc3e919a7d4e748dfc3a91e04397
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Mon Apr 23 14:08:55 2012 -0400

    BUG: fix formatting of pre-1900 dates in Index.format, close #1100

diff --git a/pandas/core/index.py b/pandas/core/index.py
index 25567fa01..cbce35dbf 100644
--- a/pandas/core/index.py
+++ b/pandas/core/index.py
@@ -338,7 +338,7 @@ class Index(np.ndarray):
             for dt in self:
                 if dt.time() != zero_time or dt.tzinfo is not None:
                     return header + ['%s' % x for x in self]
-                result.append(dt.strftime("%Y-%m-%d"))
+                result.append('%d-%d-%d' % (dt.year, dt.month, dt.day))
             return header + result
 
         values = self.values
diff --git a/pandas/tseries/tests/test_timeseries.py b/pandas/tseries/tests/test_timeseries.py
index 72e7e3bec..f5944ddfb 100644
--- a/pandas/tseries/tests/test_timeseries.py
+++ b/pandas/tseries/tests/test_timeseries.py
@@ -467,6 +467,13 @@ class TestTimeSeries(unittest.TestCase):
         expected = date_range('1/1/2000 02:00', '2/1/2000 02:00')
         self.assert_(result.equals(expected))
 
+    def test_format_pre_1900_dates(self):
+        rng = date_range('1/1/1850', '1/1/1950', freq='A-DEC')
+        rng.format()
+        ts = Series(1, index=rng)
+        repr(ts)
+
+
 def _simple_ts(start, end, freq='D'):
     rng = date_range(start, end, freq=freq)
     return Series(np.random.randn(len(rng)), index=rng)
