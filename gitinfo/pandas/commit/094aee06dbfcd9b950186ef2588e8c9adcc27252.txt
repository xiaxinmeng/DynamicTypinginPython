commit 094aee06dbfcd9b950186ef2588e8c9adcc27252
Author: Adam Klein <adamklein@gmail.com>
Date:   Thu Feb 23 16:33:41 2012 -0500

    BUG: close #816, fix exception thrown on np.diff

diff --git a/RELEASE.rst b/RELEASE.rst
index a637ec2af..4784e7522 100644
--- a/RELEASE.rst
+++ b/RELEASE.rst
@@ -43,6 +43,7 @@ pandas 0.7.1
   - Fixed groupby corner case when passing dictionary grouper and as_index is
     False (#819)
   - Fixed bug whereby bool array sometimes had object dtype (#820)
+  - Fix exception thrown on np.diff (#816)
 
 pandas 0.7.0
 ============
diff --git a/pandas/core/series.py b/pandas/core/series.py
index 4e3774687..2d48b7833 100644
--- a/pandas/core/series.py
+++ b/pandas/core/series.py
@@ -369,6 +369,8 @@ copy : boolean, default False
 
     def __getitem__(self, key):
         try:
+            if type(key) == tuple and len(key)==1:
+                key = key[0]
             return self.index.get_value(self, key)
         except InvalidIndexError:
             pass
diff --git a/pandas/tests/test_series.py b/pandas/tests/test_series.py
index 6c25aa2d0..c71960bd1 100644
--- a/pandas/tests/test_series.py
+++ b/pandas/tests/test_series.py
@@ -911,6 +911,11 @@ class TestSeries(unittest.TestCase, CheckNameIntegration):
 
         self.assert_(np.array_equal(result, expected))
 
+    def test_npdiff(self):
+        s = Series(np.arange(5))
+        r = np.diff(s)
+        assert_series_equal(Series([nan, 0, 0, 0, nan]), r)
+
     def _check_stat_op(self, name, alternate, check_objects=False):
         from pandas import DateRange
         import pandas.core.nanops as nanops
