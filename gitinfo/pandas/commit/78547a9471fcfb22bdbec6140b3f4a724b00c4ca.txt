commit 78547a9471fcfb22bdbec6140b3f4a724b00c4ca
Author: Chang She <changshe@gmail.com>
Date:   Sun Nov 25 17:10:48 2012 -0500

    BUG: series comp non-object dtype does not raise error on diff length

diff --git a/pandas/core/series.py b/pandas/core/series.py
index 8101dace1..f0b093311 100644
--- a/pandas/core/series.py
+++ b/pandas/core/series.py
@@ -117,6 +117,10 @@ def _comp_method(op, name):
             else:
                 result = lib.scalar_compare(x, y, op)
         else:
+            if (isinstance(x, np.ndarray) and
+                isinstance(y, np.ndarray) and
+                len(x) != len(y)):
+                raise ValueError('Array lengths must equal to compare')
             result = op(x, y)
 
         return result
diff --git a/pandas/tests/test_series.py b/pandas/tests/test_series.py
index f09c01386..f3f24e83b 100644
--- a/pandas/tests/test_series.py
+++ b/pandas/tests/test_series.py
@@ -1598,6 +1598,10 @@ class TestSeries(unittest.TestCase, CheckNameIntegration):
         b = Series(['b', 'a'])
         self.assertRaises(ValueError, a.__lt__, b)
 
+        a = Series([1, 2])
+        b = Series([2, 3, 4])
+        self.assertRaises(ValueError, a.__eq__, b)
+
     def test_between(self):
         s = Series(bdate_range('1/1/2000', periods=20).asobject)
         s[::2] = np.nan
