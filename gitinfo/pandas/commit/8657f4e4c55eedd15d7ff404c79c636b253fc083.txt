commit 8657f4e4c55eedd15d7ff404c79c636b253fc083
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Mon Jan 3 18:21:06 2011 -0500

    unpickling compatibility with old pickle files

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index 5376c7fa9..4206e84ed 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -192,10 +192,15 @@ class DataFrame(Picklable, Groupable):
         return series, columns, index
 
     def __setstate__(self, state):
-        series, cols, idx = state
+        # for compatibility with old pickle files
+        if len(state) == 2: # pragma: no cover
+            series, idx = state
+            columns = sorted(series)
+        else:
+            series, cols, idx = state
+            columns = _unpickle_array(cols)
 
         index = _unpickle_array(idx)
-        columns = _unpickle_array(cols)
         self._series = dict((k, Series(v, index=index))
                             for k, v in series.iteritems())
         self.index = index
