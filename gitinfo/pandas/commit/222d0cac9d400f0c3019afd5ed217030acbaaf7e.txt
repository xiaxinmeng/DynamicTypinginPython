commit 222d0cac9d400f0c3019afd5ed217030acbaaf7e
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Mon Sep 5 16:50:58 2011 -0400

    REF: removed make_long_index method and switched things to directly create MultiIndex

diff --git a/pandas/core/panel.py b/pandas/core/panel.py
index 9c1b6b9bf..8f5c82217 100644
--- a/pandas/core/panel.py
+++ b/pandas/core/panel.py
@@ -1496,8 +1496,7 @@ def pivot(index, columns, values):
     if len(index) == 0:
         return DataFrame(index=[])
 
-    hindex = _make_long_index(index, columns)
-
+    hindex = MultiIndex.from_arrays([index, columns])
     series = Series(values.ravel(), index=hindex)
     series = series.sortlevel(0)
     return series.unstack()
@@ -1513,9 +1512,6 @@ def make_mask(index):
     mask.put(selector, True)
     return mask
 
-def _make_long_index(major_values, minor_values):
-    return MultiIndex.from_arrays([major_values, minor_values])
-
 def _slow_pivot(index, columns, values):
     """
     Produce 'pivot' table based on 3 columns of this DataFrame.
diff --git a/pandas/io/pytables.py b/pandas/io/pytables.py
index 2aeea3371..7d0b021c0 100644
--- a/pandas/io/pytables.py
+++ b/pandas/io/pytables.py
@@ -9,7 +9,8 @@ from datetime import datetime
 import time
 
 import numpy as np
-from pandas import Series, TimeSeries, DataFrame, WidePanel, LongPanel
+from pandas import (Series, TimeSeries, DataFrame, WidePanel, LongPanel,
+                    MultiIndex)
 from pandas.core.common import adjoin
 import pandas._tseries as _tseries
 
@@ -549,7 +550,6 @@ class HDFStore(object):
         return self._read_panel_table(group, where)['value']
 
     def _read_panel_table(self, group, where=None):
-        from pandas.core.panel import _make_long_index
         table = getattr(group, 'table')
 
         # create the selection
@@ -562,8 +562,7 @@ class HDFStore(object):
         index = _maybe_convert(sel.values['index'],
                                table._v_attrs.index_kind)
         # reconstruct
-        long_index = _make_long_index(np.asarray(index),
-                                      np.asarray(columns))
+        long_index = MultiIndex.from_arrays([index, columns])
         lp = LongPanel(sel.values['values'], index=long_index,
                        columns=fields)
         lp = lp.sortlevel(level=0)
