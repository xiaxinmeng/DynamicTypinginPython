commit 433e0246d363e7562852866e6a38be823a7eff3f
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Sun Dec 18 02:32:46 2011 -0500

    ENH: more speed tweaking Series.__getitem__

diff --git a/pandas/core/series.py b/pandas/core/series.py
index aaf526dae..b372179ab 100644
--- a/pandas/core/series.py
+++ b/pandas/core/series.py
@@ -251,20 +251,18 @@ copy : boolean, default False
     def __getitem__(self, key):
         # Label-based
         try:
+            return self.index._engine.get_value(self, key)
+        except KeyError, e1:
             if isinstance(self.index, MultiIndex):
                 return self._multilevel_index(key)
-            else:
-                hash(key)
-                try:
-                    return self.get_value(key)
-                except KeyError, e1:
-                    try:
-                        return _gin.get_value_at(self, key)
-                    except IndexError:
-                        raise
-                    except Exception, _:
-                        pass
-                    raise e1
+
+            try:
+                return _gin.get_value_at(self, key)
+            except IndexError:
+                raise
+            except Exception, _:
+                pass
+            raise e1
         except TypeError:
             pass
 
