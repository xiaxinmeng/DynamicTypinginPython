commit 6e680fffdb357fb65a818fe663e32932fc8a845c
Author: Kaiqi Dong <kaiqi@kth.se>
Date:   Wed May 6 23:30:50 2020 +0200

    DOC/PLT: Add `stacked` in doc and doc example for barh and bar plot (#33337)

diff --git a/pandas/plotting/_core.py b/pandas/plotting/_core.py
index efa69c9b7..748b83aec 100644
--- a/pandas/plotting/_core.py
+++ b/pandas/plotting/_core.py
@@ -1007,6 +1007,13 @@ class PlotAccessor(PandasObject):
             ...                    'lifespan': lifespan}, index=index)
             >>> ax = df.plot.bar(rot=0)
 
+        Plot stacked bar charts for the DataFrame
+
+        .. plot::
+            :context: close-figs
+
+            >>> ax = df.plot.bar(stacked=True)
+
         Instead of nesting, the figure can be split by column with
         ``subplots=True``. In this case, a :class:`numpy.ndarray` of
         :class:`matplotlib.axes.Axes` are returned.
@@ -1088,6 +1095,13 @@ class PlotAccessor(PandasObject):
             ...                    'lifespan': lifespan}, index=index)
             >>> ax = df.plot.barh()
 
+        Plot stacked barh charts for the DataFrame
+
+        .. plot::
+            :context: close-figs
+
+            >>> ax = df.plot.barh(stacked=True)
+
         We can specify colors for each column
 
         .. plot::
