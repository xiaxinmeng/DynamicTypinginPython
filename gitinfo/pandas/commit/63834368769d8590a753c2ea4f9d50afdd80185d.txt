commit 63834368769d8590a753c2ea4f9d50afdd80185d
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Thu Oct 20 10:34:31 2011 -0400

    ENH: perf enh, only cast to float if numeric already

diff --git a/pandas/core/groupby.py b/pandas/core/groupby.py
index eb32db4c3..f662d8ea0 100644
--- a/pandas/core/groupby.py
+++ b/pandas/core/groupby.py
@@ -333,9 +333,10 @@ class GroupBy(object):
         output = {}
         cannot_agg = []
         for name, obj in self._iterate_slices():
-            try:
-                obj = np.asarray(obj, dtype=float)
-            except ValueError:
+            if issubclass(obj.dtype.type, np.number):
+                if obj.dtype != np.float64:
+                    obj = obj.astype('f8')
+            else:
                 cannot_agg.append(name)
                 continue
 
