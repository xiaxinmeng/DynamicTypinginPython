commit 16c247bcfb214d296ddf192520db51cb310268e8
Author: David Read <david.read@hackneyworkshop.com>
Date:   Thu May 18 21:43:25 2017 +0100

    Fixes truncated error message "C extension: umpy.core.multiarray failed to import" (#16366)

diff --git a/pandas/__init__.py b/pandas/__init__.py
index 48ac9d173..8d9b75ccd 100644
--- a/pandas/__init__.py
+++ b/pandas/__init__.py
@@ -28,7 +28,7 @@ try:
                              tslib as _tslib)
 except ImportError as e:  # pragma: no cover
     # hack but overkill to use re
-    module = str(e).lstrip('cannot import name ')
+    module = str(e).replace('cannot import name ', '')
     raise ImportError("C extension: {0} not built. If you want to import "
                       "pandas from the source directory, you may need to run "
                       "'python setup.py build_ext --inplace --force' to build "
