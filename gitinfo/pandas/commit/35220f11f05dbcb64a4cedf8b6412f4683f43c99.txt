commit 35220f11f05dbcb64a4cedf8b6412f4683f43c99
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Fri Nov 2 16:54:48 2012 -0400

    TST: raise assertionerror instead

diff --git a/pandas/core/frame.py b/pandas/core/frame.py
index 2dbef5ef6..6710bc683 100644
--- a/pandas/core/frame.py
+++ b/pandas/core/frame.py
@@ -1904,8 +1904,9 @@ class DataFrame(NDFrame):
                 else:
                     value = value.reindex(self.index).values
             else:
-                msg = 'Length of values does not match length of index'
-                assert len(value) == len(self.index), msg
+                if len(value) != len(self.index):
+                    raise AssertionError('Length of values does not match '
+                                         'length of index')
 
                 if not isinstance(value, np.ndarray):
                     value = com._asarray_tuplesafe(value)
