commit 32d132354d8b2ef02e3ea8e4624abf59be32109f
Author: Skipper Seabold <jsseabold@gmail.com>
Date:   Sun May 19 00:11:33 2013 -0400

    ENH: Allow passing of flags in replace

diff --git a/pandas/core/strings.py b/pandas/core/strings.py
index 3521c9ff9..87a9ff7e9 100644
--- a/pandas/core/strings.py
+++ b/pandas/core/strings.py
@@ -692,8 +692,9 @@ class StringMethods(object):
         return self._wrap_result(result)
 
     @copy(str_replace)
-    def replace(self, pat, repl, n=-1, case=True):
-        result = str_replace(self.series, pat, repl, n=n, case=case)
+    def replace(self, pat, repl, n=-1, case=True, flags=0):
+        result = str_replace(self.series, pat, repl, n=n, case=case,
+                             flags=flags)
         return self._wrap_result(result)
 
     @copy(str_repeat)
