commit 2149c50ef2f91c224f8685e6498f0bce5655b665
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Sat Nov 24 23:26:01 2012 -0500

    TST: reduce memory usage of vbenchmarks

diff --git a/vb_suite/indexing.py b/vb_suite/indexing.py
index c7cf58b8d..c0f9c5a59 100644
--- a/vb_suite/indexing.py
+++ b/vb_suite/indexing.py
@@ -112,8 +112,8 @@ sort_level_one = Benchmark("midx.sortlevel(1)", setup,
 # Panel subset selection
 
 setup = common_setup + """
-p = Panel(np.random.randn(500, 500, 500))
-inds = range(0, 500, 10)
+p = Panel(np.random.randn(100, 100, 100))
+inds = range(0, 100, 10)
 """
 
 indexing_panel_subset = Benchmark('p.ix[inds, inds, inds]', setup,
diff --git a/vb_suite/sparse.py b/vb_suite/sparse.py
index ef08a6e24..46a7f48f7 100644
--- a/vb_suite/sparse.py
+++ b/vb_suite/sparse.py
@@ -33,7 +33,7 @@ setup = common_setup + """
 from pandas.core.sparse import SparseDataFrame
 """
 
-stmt = "SparseDataFrame(columns=np.arange(100), index=np.arange(1e6))"
+stmt = "SparseDataFrame(columns=np.arange(100), index=np.arange(1e4))"
 
 sparse_constructor = Benchmark(stmt, setup, name="sparse_frame_constructor",
                                start_date=datetime(2012, 6, 1))
