commit dc8e07cffde7920495bb563aabdc49662432b716
Author: y-p <yoval@gmx.com>
Date:   Tue Dec 4 05:58:33 2012 +0200

    TST: use integer division operator for 3.3

diff --git a/pandas/tests/test_groupby.py b/pandas/tests/test_groupby.py
index ef0a59075..630ea79e6 100644
--- a/pandas/tests/test_groupby.py
+++ b/pandas/tests/test_groupby.py
@@ -1437,7 +1437,7 @@ class TestGroupBy(unittest.TestCase):
         labels = np.random.randint(0, 100, size=N)
         df = DataFrame({'key': labels,
                         'value1': np.random.randn(N),
-                        'value2': ['foo', 'bar', 'baz', 'qux'] * (N / 4)})
+                        'value2': ['foo', 'bar', 'baz', 'qux'] * (N // 4)})
 
         grouped = df.groupby('key')
 
