commit 173c7283617630f29204a2c39671cd72da192411
Author: Chang She <chang@lambdafoundry.com>
Date:   Fri Apr 27 15:29:19 2012 -0400

    BUG: default value for freq should be None

diff --git a/pandas/tseries/period.py b/pandas/tseries/period.py
index 1ef070859..d642e5aac 100644
--- a/pandas/tseries/period.py
+++ b/pandas/tseries/period.py
@@ -509,7 +509,7 @@ class PeriodIndex(Int64Index):
                 data = np.array(data, dtype='O')
 
             if freq is None and len(data) > 0:
-                freq = getattr(data[0], 'freq')
+                freq = getattr(data[0], 'freq', None)
 
             if freq is None:
                 raise ValueError(('freq not specified and cannot be inferred '
