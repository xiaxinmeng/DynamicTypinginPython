commit 8ffcbf8bed46ecbe4794a28e0029d2a10b02625e
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Mon Jan 9 22:19:15 2012 -0500

    BUG: formatting of index level names in MultiIndex, per #601

diff --git a/RELEASE.rst b/RELEASE.rst
index 1af03216f..6f9e7339b 100644
--- a/RELEASE.rst
+++ b/RELEASE.rst
@@ -112,6 +112,8 @@ pandas 0.7.0
     when sorting by multiple columns
   - Substantially improve performance of DataFrame and Series constructors when
     passed a nested dict or dict, respectively (GH #540)
+  - Modified setup.py so that pip / setuptools will install dependencies (GH
+    #507, various pull requests)
 
 **Bug fixes**
 
diff --git a/pandas/core/index.py b/pandas/core/index.py
index eab1908fe..2a701f0d1 100644
--- a/pandas/core/index.py
+++ b/pandas/core/index.py
@@ -1112,7 +1112,7 @@ class MultiIndex(Index):
             level = []
 
             if names:
-                level.append(name if name is not None else '')
+                level.append(str(name) if name is not None else '')
 
             level.extend(np.array(lev, dtype=object).take(lab))
             result_levels.append(level)
diff --git a/pandas/tests/test_index.py b/pandas/tests/test_index.py
index 7afd2ffe4..472e80c38 100644
--- a/pandas/tests/test_index.py
+++ b/pandas/tests/test_index.py
@@ -955,6 +955,12 @@ class TestMultiIndex(unittest.TestCase):
         self.index.format()
         self.index[:0].format()
 
+    def test_format_integer_names(self):
+        index = MultiIndex(levels=[[0, 1], [0, 1]],
+                           labels=[[0, 0, 1, 1], [0, 1, 0, 1]],
+                           names=[0, 1])
+        index.format(names=True)
+
     def test_bounds(self):
         self.index._bounds
 
diff --git a/setup.py b/setup.py
index 449c1496c..21e8e4b9b 100755
--- a/setup.py
+++ b/setup.py
@@ -49,7 +49,7 @@ if sys.version_info[0] >= 3:
 
 else:
     setuptools_kwargs = {
-        'install_requires': ['python-dateutil < 2','numpy'],
+        'install_requires': ['python-dateutil < 2', 'numpy'],
     }
     if not _have_setuptools:
         try:
