commit c14a47034714ed7c789d8c448f3f82c388811c90
Author: Wes McKinney <wesmckinn@gmail.com>
Date:   Thu Jul 12 12:13:48 2012 -0400

    TST: fix test errors on Python 3, close #1550

diff --git a/pandas/sparse/frame.py b/pandas/sparse/frame.py
index 836154351..d001f28ed 100644
--- a/pandas/sparse/frame.py
+++ b/pandas/sparse/frame.py
@@ -276,7 +276,7 @@ class SparseDataFrame(DataFrame):
             self._insert_column(key)
 
     def _insert_column(self, key):
-        self.columns = Index(np.concatenate((self.columns, [key])))
+        self.columns = self.columns.insert(len(self.columns), key)
 
     def __delitem__(self, key):
         """
diff --git a/pandas/stats/tests/test_fama_macbeth.py b/pandas/stats/tests/test_fama_macbeth.py
index f48dde20f..f2ebef5fd 100644
--- a/pandas/stats/tests/test_fama_macbeth.py
+++ b/pandas/stats/tests/test_fama_macbeth.py
@@ -36,7 +36,7 @@ class TestFamaMacBeth(BaseTest):
             end = index[i + window - 1]
 
             x2 = {}
-            for k, v in x.iteritems():
+            for k, v in x.iterkv():
                 x2[k] = v.truncate(start, end)
             y2 = y.truncate(start, end)
 
diff --git a/pandas/stats/tests/test_ols.py b/pandas/stats/tests/test_ols.py
index 8877c4e69..363664b06 100644
--- a/pandas/stats/tests/test_ols.py
+++ b/pandas/stats/tests/test_ols.py
@@ -490,7 +490,7 @@ class TestPanelOLS(BaseTest):
 
         stack_y = y.stack()
         stack_x = DataFrame(dict((k, v.stack())
-                                  for k, v in x.iteritems()))
+                                  for k, v in x.iterkv()))
 
         weights = x.std('items')
         stack_weights = weights.stack()
