commit 4c6515e29995e0d2b5cc121129806443a2c8ee82
Author: Brian Coca <bcoca@ansible.com>
Date:   Fri Jan 15 15:00:40 2016 -0500

    reverse order of cron commands
    
    does not affect linux but fixes issue with busybox caring about order
    fixes #2807

diff --git a/lib/ansible/modules/system/cron.py b/lib/ansible/modules/system/cron.py
index d1ead7b108..dfe5b30e5b 100644
--- a/lib/ansible/modules/system/cron.py
+++ b/lib/ansible/modules/system/cron.py
@@ -380,7 +380,7 @@ class CronTab(object):
                 return "chown %s %s ; su '%s' -c '%s %s'" % (pipes.quote(self.user), pipes.quote(path), pipes.quote(self.user), CRONCMD, pipes.quote(path))
             else:
                 user = '-u %s' % pipes.quote(self.user)
-        return "%s %s %s" % (CRONCMD , user, pipes.quote(path))
+        return "%s %s %s" % (CRONCMD , pipes.quote(path), user)
 
 
 
