commit 311b93995cf20322efcc7d3bf10ea08fa52b8c7a
Author: Michael DeHaan <michael.dehaan@gmail.com>
Date:   Thu Mar 15 22:37:37 2012 -0400

    Add some comments

diff --git a/library/file b/library/file
index 6d046a31eb..9c2608e64c 100755
--- a/library/file
+++ b/library/file
@@ -83,11 +83,14 @@ for x in items:
 
 state     = params.get('state','file')
 path      = params.get('path', params.get('dest', params.get('name', None)))
-link      = params.get('link', 'false')
 mode      = params.get('mode', None)
 owner     = params.get('owner', None)
 group     = params.get('group', None)
+
+# presently unused, we always use -R (FIXME?)
 recurse   = params.get('recurse', 'false')
+
+# presently unused, implement (FIXME)
 secontext = params.get('secontext', None)
 
 if state not in [ 'file', 'directory', 'absent' ]:
