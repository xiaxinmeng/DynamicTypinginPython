commit aaac05c96c64aa9719b115c4366106ed7063e25c
Author: Guilherme Carlos <guilherme.thcarlos@gmail.com>
Date:   Wed Jul 22 17:55:35 2015 -0300

    Fix login_user on mysql_user
    
    `login_username` was provided but `login_user` is the correct option

diff --git a/lib/ansible/modules/database/mysql/mysql_user.py b/lib/ansible/modules/database/mysql/mysql_user.py
index c7a8c72a8b..854641ec52 100644
--- a/lib/ansible/modules/database/mysql/mysql_user.py
+++ b/lib/ansible/modules/database/mysql/mysql_user.py
@@ -110,7 +110,7 @@ options:
 notes:
    - Requires the MySQLdb Python package on the remote host. For Ubuntu, this
      is as easy as apt-get install python-mysqldb.
-   - Both C(login_password) and C(login_username) are required when you are
+   - Both C(login_password) and C(login_user) are required when you are
      passing credentials. If none are present, the module will attempt to read
      the credentials from C(~/.my.cnf), and finally fall back to using the MySQL
      default login of 'root' with no password.
