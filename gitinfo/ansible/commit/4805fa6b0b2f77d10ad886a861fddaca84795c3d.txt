commit 4805fa6b0b2f77d10ad886a861fddaca84795c3d
Author: Brian Coca <bcoca@ansible.com>
Date:   Wed Mar 4 09:39:06 2015 -0500

    fix ansible-doc listing with new docstring
    fixes #10295

diff --git a/bin/ansible-doc b/bin/ansible-doc
index 3447f9d090..dff7cecce7 100755
--- a/bin/ansible-doc
+++ b/bin/ansible-doc
@@ -197,7 +197,7 @@ def get_module_list_text(module_list):
             continue
 
         try:
-            doc, plainexamples = module_docs.get_docstring(filename)
+            doc, plainexamples, returndocs = module_docs.get_docstring(filename)
             desc = tty_ify(doc.get('short_description', '?')).strip()
             if len(desc) > linelimit:
                 desc = desc[:linelimit] + '...'
