commit 1ea5b04e05254d388877be0d0c867b2a1dc0af5d
Author: Richard C Isaacson <richard.c.isaacson@gmail.com>
Date:   Tue Mar 11 22:39:42 2014 -0500

    Bulk update of choices=BOOLEANS to type='bool'

diff --git a/library/cloud/gce b/library/cloud/gce
index b14ce8996d..2d95c8143b 100755
--- a/library/cloud/gce
+++ b/library/cloud/gce
@@ -351,7 +351,7 @@ def main():
             metadata = dict(),
             name = dict(),
             network = dict(default='default'),
-            persistent_boot_disk = dict(type='bool', choices=BOOLEANS, default=False),
+            persistent_boot_disk = dict(type='bool', default=False),
             state = dict(choices=['active', 'present', 'absent', 'deleted'],
                     default='present'),
             tags = dict(type='list'),
