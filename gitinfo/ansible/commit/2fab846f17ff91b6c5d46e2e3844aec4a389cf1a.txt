commit 2fab846f17ff91b6c5d46e2e3844aec4a389cf1a
Author: Stephen Fromm <sfromm@gmail.com>
Date:   Tue Nov 6 09:05:25 2012 -0800

    Fix to user module to use self when calling group_exists

diff --git a/library/user b/library/user
index afa2364aed..cb35608e65 100755
--- a/library/user
+++ b/library/user
@@ -246,7 +246,7 @@ class User(object):
             cmd.append(self.uid)
 
         if self.group is not None:
-            if not user.group_exists(self.group):
+            if not self.group_exists(self.group):
                 self.module.fail_json(msg="Group %s does not exist" % self.group)
             cmd.append('-g')
             cmd.append(self.group)
