commit 9a2c1cf94d9a8a640fe02f3ecefb9e9c9a941d99
Author: James Cammarata <jimi@sngx.net>
Date:   Tue Mar 22 11:45:26 2016 -0400

    Force removal of docker container after test ends

diff --git a/test/utils/run_tests.sh b/test/utils/run_tests.sh
index f8d76c725c..b174c514c7 100755
--- a/test/utils/run_tests.sh
+++ b/test/utils/run_tests.sh
@@ -17,6 +17,6 @@ else
     docker kill $(cat /tmp/cid_${TARGET})
 
     if [ "X${TESTS_KEEP_CONTAINER}" = "X" ]; then
-        docker rm "${C_NAME}"
+        docker rm -f "${C_NAME}"
     fi
 fi
