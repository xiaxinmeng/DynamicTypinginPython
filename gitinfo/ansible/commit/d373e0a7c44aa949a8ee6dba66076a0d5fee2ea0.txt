commit d373e0a7c44aa949a8ee6dba66076a0d5fee2ea0
Author: Jirayut 'Dear' Nimsaeng <jirayut.n@kaidee.com>
Date:   Wed Jun 1 17:26:51 2016 +0700

    Fix volume to support not binding volumes to host

diff --git a/lib/ansible/modules/cloud/docker/docker_container.py b/lib/ansible/modules/cloud/docker/docker_container.py
index 474ef881f5..1e21c8d794 100644
--- a/lib/ansible/modules/cloud/docker/docker_container.py
+++ b/lib/ansible/modules/cloud/docker/docker_container.py
@@ -336,7 +336,7 @@ options:
       - always
       - no
       - on-failure
-      - unless-stopped 
+      - unless-stopped
     default: on-failure
     required: false
   restart_retries:
@@ -831,7 +831,7 @@ class TaskParameters(DockerBaseClass):
                     host = os.path.abspath(host)
                     new_vols.append("%s:%s:rw" % (host, container))
             else:
-                new_vols.append("%s:%s:rw" % (os.path.abspath(vol), vol))
+                new_vols.append(vol)
         return new_vols
 
     def _get_mounts(self):
@@ -940,11 +940,6 @@ class TaskParameters(DockerBaseClass):
                         bind=container,
                         mode=mode
                     )
-                else:
-                    self.volume_binds[vol] = dict(
-                        bind=vol,
-                        mode='rw'
-                    )
 
 
     def _parse_exposed_ports(self):
