commit 068ef0e9794f05d596c6b5d4478da0d57fdb3fff
Author: Jan-Piet Mens <jpmens@gmail.com>
Date:   Thu Nov 22 07:23:10 2012 +0100

    module_formatter: re-instate code

diff --git a/hacking/module_formatter.py b/hacking/module_formatter.py
index e4f8dab45d..6a28be16f8 100755
--- a/hacking/module_formatter.py
+++ b/hacking/module_formatter.py
@@ -66,8 +66,7 @@ def html_ify(text):
     t = _BOLD.sub("<b>" + r"\1" + "</b>", t)
     t = _MODULE.sub("<span class='module'>" + r"\1" + "</span>", t)
     t = _URL.sub("<a href='" + r"\1" + "'>" + r"\1" + "</a>", t)
-    #t = _CONST.sub("<code>" + r"\1" + "</code>", t)
-    t = _CONST.sub(r"\1", t)
+    t = _CONST.sub("<code>" + r"\1" + "</code>", t)
     return t
 
 def json_ify(text):
diff --git a/library/file b/library/file
index a4c0542341..ff51608c23 100644
--- a/library/file
+++ b/library/file
@@ -35,7 +35,7 @@ short_description: Sets attributes of files
 description: 
      - Sets attributes of files, symlinks, and directories, or removes
        files/symlinks/directories. Many other modules support the same options as
-       the file module - including M(copy), M(template), and M(assemble).
+       the M(file) module - including M(copy), M(template), and M(assemble).
 options:
   path:
     description:
