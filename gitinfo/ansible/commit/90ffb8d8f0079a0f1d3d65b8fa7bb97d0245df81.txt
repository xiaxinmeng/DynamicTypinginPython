commit 90ffb8d8f0079a0f1d3d65b8fa7bb97d0245df81
Author: James Cammarata <jimi@sngx.net>
Date:   Tue Mar 22 13:53:41 2016 -0400

    Also remove volumes when removing containers

diff --git a/test/utils/run_tests.sh b/test/utils/run_tests.sh
index b174c514c7..b7bd5c3e28 100755
--- a/test/utils/run_tests.sh
+++ b/test/utils/run_tests.sh
@@ -17,6 +17,6 @@ else
     docker kill $(cat /tmp/cid_${TARGET})
 
     if [ "X${TESTS_KEEP_CONTAINER}" = "X" ]; then
-        docker rm -f "${C_NAME}"
+        docker rm -vf "${C_NAME}"
     fi
 fi
