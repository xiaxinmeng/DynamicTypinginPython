commit 89fbc64b60181c6ae88499b9205eb80c774df699
Author: Matt Martz <matt@sivel.net>
Date:   Mon Jan 20 16:42:41 2014 -0600

    Fix regex match, the printf match should work with 0 or more numeric padding characters, not 1 or more

diff --git a/library/cloud/rax b/library/cloud/rax
index 03e99ea2a9..1cf946ad17 100644
--- a/library/cloud/rax
+++ b/library/cloud/rax
@@ -464,7 +464,7 @@ def cloudservers(module, state, name, flavor, image, meta, key_name, files,
                         else:
                             module.fail_json(msg=e.message)
 
-                    pattern = re.sub(r'%\d+[sd]', r'(\d+)', name)
+                    pattern = re.sub(r'%\d*[sd]', r'(\d+)', name)
                     for server in cs.servers.list():
                         if server.metadata.get('group') == group:
                             servers.append(server)
@@ -525,7 +525,7 @@ def cloudservers(module, state, name, flavor, image, meta, key_name, files,
                         else:
                             module.fail_json(msg=e.message)
 
-                    pattern = re.sub(r'%\d+[sd]', r'(\d+)', name)
+                    pattern = re.sub(r'%\d*[sd]', r'(\d+)', name)
                     for server in cs.servers.list():
                         if server.metadata.get('group') == group:
                             servers.append(server)
