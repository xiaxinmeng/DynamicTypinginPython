commit e3782689bc3cb1088e0f1eacabf9a0af90860668
Author: Aaron Kurtz <a.kurtz@hardsun.net>
Date:   Mon Dec 7 01:51:16 2015 -0800

    Remove 'cache_table' from required params
    
    Starting in Django 1.7, the createcachetable command looks for cache
    table names in the CACHES settings dictionary, so cache_table is no
    longer required, but is still allowed.

diff --git a/lib/ansible/modules/web_infrastructure/django_manage.py b/lib/ansible/modules/web_infrastructure/django_manage.py
index 660f55fd9f..d62378d15c 100644
--- a/lib/ansible/modules/web_infrastructure/django_manage.py
+++ b/lib/ansible/modules/web_infrastructure/django_manage.py
@@ -183,7 +183,6 @@ def main():
 
     command_required_param_map = dict(
         loaddata=('fixtures', ),
-        createcachetable=('cache_table', ),
         )
 
     # forces --noinput on every command that needs it
