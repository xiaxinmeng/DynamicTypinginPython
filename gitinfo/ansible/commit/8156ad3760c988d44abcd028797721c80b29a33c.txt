commit 8156ad3760c988d44abcd028797721c80b29a33c
Author: JustInVTime <github@hebikdit.nl>
Date:   Thu May 7 18:37:28 2015 +0200

    Delimiter param must be a string

diff --git a/v1/ansible/runner/lookup_plugins/csvfile.py b/v1/ansible/runner/lookup_plugins/csvfile.py
index ce5a2b77d2..a9ea8ed90c 100644
--- a/v1/ansible/runner/lookup_plugins/csvfile.py
+++ b/v1/ansible/runner/lookup_plugins/csvfile.py
@@ -63,7 +63,10 @@ class LookupModule(object):
                 for param in params[1:]:
                     name, value = param.split('=')
                     assert(name in paramvals)
-                    paramvals[name] = value
+                    if name == 'delimiter':
+                        paramvals[name] = str(value)
+                    else:
+                        paramvals[name] = value
             except (ValueError, AssertionError), e:
                 raise errors.AnsibleError(e)
 
