commit 301ebf8d04c0e588536c2c216f4d074854e70e81
Author: Matt Martz <matt@sivel.net>
Date:   Thu Dec 19 12:58:07 2013 -0600

    Don't place (or higher) in the min_ansible_version

diff --git a/bin/ansible-galaxy b/bin/ansible-galaxy
index aa0b9af2d2..d1d278a45a 100755
--- a/bin/ansible-galaxy
+++ b/bin/ansible-galaxy
@@ -509,7 +509,7 @@ def execute_init(args, options):
                 author = 'your name',
                 company = 'your company (optional)',
                 license = 'license (GPLv2, CC-BY, etc)',
-                min_ansible_version = '1.2 (or higher)',
+                min_ansible_version = '1.2',
                 platforms = platform_groups,
                 categories = categories,
             )
