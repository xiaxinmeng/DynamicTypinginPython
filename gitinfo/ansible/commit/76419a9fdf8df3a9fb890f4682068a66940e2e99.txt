commit 76419a9fdf8df3a9fb890f4682068a66940e2e99
Author: Grzegorz Nosek <root@localdomain.pl>
Date:   Mon Aug 20 21:18:37 2012 +0200

    Point to the proper config file
    
    The global config file is loaded from /etc/ansible, not /etc

diff --git a/examples/ansible.cfg b/examples/ansible.cfg
index c309fc41c9..bc892fc5ab 100644
--- a/examples/ansible.cfg
+++ b/examples/ansible.cfg
@@ -1,6 +1,6 @@
 # config file for ansible -- http://ansible.github.com
 # nearly all parameters can be overridden in ansible-playbook or with command line flags
-# ansible will read ~/.ansible.cfg or /etc/ansible.cfg, whichever it finds first
+# ansible will read ~/.ansible.cfg or /etc/ansible/ansible.cfg, whichever it finds first
 
 [defaults]
 
