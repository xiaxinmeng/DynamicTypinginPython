commit 8f2580fc988dbb710525786a90fea9e71f6a39ae
Author: Jens Rantil <jens.rantil@telavox.se>
Date:   Wed Dec 4 13:56:57 2013 +0100

    module(apt_key): don't catch `SystemExit`
    
    If we do this, we risk outputting two JSON payloads, which will be
    unparsable.

diff --git a/library/packaging/apt_key b/library/packaging/apt_key
index 17b52888d4..dee73762c6 100644
--- a/library/packaging/apt_key
+++ b/library/packaging/apt_key
@@ -138,7 +138,7 @@ def download_key(module, url):
             module.fail_json("error connecting to download key from url")
         data = connection.read()
         return data
-    except:
+    except Exception:
         module.fail_json(msg="error getting key id from url", traceback=format_exc())
 
 
