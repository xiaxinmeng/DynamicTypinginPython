commit 69db160e2fff407ca89340ed5728d8fda89b65b2
Author: Chris Jacobs <zonk1024@gmail.com>
Date:   Tue Jun 4 18:16:09 2013 -0700

    Skip dotfile in hosts dirs

diff --git a/lib/ansible/inventory/dir.py b/lib/ansible/inventory/dir.py
index ac1e2aa5bf..50ac33d2b1 100644
--- a/lib/ansible/inventory/dir.py
+++ b/lib/ansible/inventory/dir.py
@@ -48,6 +48,9 @@ class InventoryDirectory(object):
                 # this file is generated on a failed playbook and should only be
                 # used when run specifically
                 continue
+            # Skip hidden files
+            if i.startswith('.'):
+                continue
             # These are things inside of an inventory basedir
             if i in ("host_vars", "group_vars", "vars_plugins"):
                 continue
