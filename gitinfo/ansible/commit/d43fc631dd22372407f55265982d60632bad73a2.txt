commit d43fc631dd22372407f55265982d60632bad73a2
Author: Brian Coca <bcoca@ansible.com>
Date:   Fri Mar 18 07:25:29 2016 -0700

    mount facts now include network mounts (nfs)

diff --git a/lib/ansible/module_utils/facts.py b/lib/ansible/module_utils/facts.py
index 335248f536..203a72996d 100644
--- a/lib/ansible/module_utils/facts.py
+++ b/lib/ansible/module_utils/facts.py
@@ -1084,8 +1084,8 @@ class LinuxHardware(Hardware):
         self.facts['mounts'] = []
         mtab = get_file_content('/etc/mtab', '')
         for line in mtab.split('\n'):
-            if line.startswith('/'):
-                fields = line.rstrip('\n').split()
+            fields = line.rstrip('\n').split()
+            if fields[0].startswith('/') or ':/' in fields[0]:
                 if(fields[2] != 'none'):
                     size_total, size_available = self._get_mount_size_facts(fields[1])
                     if fields[0] in uuids:
