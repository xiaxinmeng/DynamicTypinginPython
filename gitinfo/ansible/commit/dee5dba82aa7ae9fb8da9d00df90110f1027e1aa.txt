commit dee5dba82aa7ae9fb8da9d00df90110f1027e1aa
Author: Dag Wieers <dag@wieers.com>
Date:   Wed Feb 3 01:52:17 2016 +0100

    Handle when the lookup-plugin is used in jinja-style
    
    This fixes #14190.

diff --git a/lib/ansible/plugins/lookup/first_found.py b/lib/ansible/plugins/lookup/first_found.py
index 915363180d..8145c72b02 100644
--- a/lib/ansible/plugins/lookup/first_found.py
+++ b/lib/ansible/plugins/lookup/first_found.py
@@ -171,7 +171,7 @@ class LookupModule(LookupBase):
                 else:
                     total_search.append(term)
         else:
-            total_search = terms
+            total_search = self._flatten(terms)
 
         roledir = variables.get('roledir')
         for fn in total_search:
