commit 575d7eb8be487b7420856224c5bec4326fe7f42e
Author: James Tanner <tanner.jc@gmail.com>
Date:   Thu Nov 14 10:59:24 2013 -0500

    Set required = True for src in unarchive module.

diff --git a/library/files/unarchive b/library/files/unarchive
index bd62bc9372..5fb7de1342 100644
--- a/library/files/unarchive
+++ b/library/files/unarchive
@@ -149,7 +149,7 @@ def main():
     module = AnsibleModule(
         # not checking because of daisy chain to file module
         argument_spec = dict(
-            src               = dict(required=False),
+            src               = dict(required=True),
             original_basename = dict(required=False), # used to handle 'dest is a directory' via template, a slight hack
             dest              = dict(required=True),
         ),
