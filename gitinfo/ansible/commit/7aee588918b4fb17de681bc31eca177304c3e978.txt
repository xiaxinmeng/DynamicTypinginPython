commit 7aee588918b4fb17de681bc31eca177304c3e978
Author: Nicolas Grilly <nicolas@gardentechno.com>
Date:   Thu May 23 18:37:30 2013 +0200

    Idiomatic Python: use in operator instead of method find

diff --git a/lib/ansible/inventory/__init__.py b/lib/ansible/inventory/__init__.py
index 821145871a..3d407a92c0 100644
--- a/lib/ansible/inventory/__init__.py
+++ b/lib/ansible/inventory/__init__.py
@@ -63,7 +63,7 @@ class Inventory(object):
         self._subset = None
 
         if isinstance(host_list, basestring):
-            if host_list.find(",") != -1:
+            if "," in host_list:
                 host_list = host_list.split(",")
                 host_list = [ h for h in host_list if h and h.strip() ]
 
@@ -72,8 +72,8 @@ class Inventory(object):
             all = Group('all')
             self.groups = [ all ]
             for x in host_list:
-                if x.find(":") != -1:
-                    tokens = x.split(":",1)
+                if ":" in x:
+                    tokens = x.split(":", 1)
                     all.add_host(Host(tokens[0], tokens[1]))
                 else:
                     all.add_host(Host(x))
