commit 02335843433bfdcd2f2bbe6c13235e0e4bf31775
Author: Brian Coca <bcoca@ansible.com>
Date:   Thu Feb 6 11:59:37 2014 -0500

    fixed main() issue created in prev fix, now values are empty string if they were None
    
    Signed-off-by: Brian Coca <briancoca+ansible@gmail.com>

diff --git a/library/system/debconf b/library/system/debconf
index 35545343b2..52c646e23c 100644
--- a/library/system/debconf
+++ b/library/system/debconf
@@ -100,8 +100,10 @@ def set_selection(module, pkg, question, vtype, value, unseen):
     answer = [ question ]
     if 'vtype':
         answer.append(vtype)
-    answer.append(value)
 
+    if value is None:
+        value = ''
+    answer.append(value)
     data = ' '.join(answer)
 
     setsel = module.get_bin_path('debconf-set-selections', True)
@@ -159,3 +161,5 @@ def main():
 
 # import module snippets
 from ansible.module_utils.basic import *
+
+main()
