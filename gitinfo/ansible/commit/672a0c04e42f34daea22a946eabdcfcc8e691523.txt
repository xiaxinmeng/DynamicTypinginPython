commit 672a0c04e42f34daea22a946eabdcfcc8e691523
Author: Nace Oroz <nace@celtra.com>
Date:   Wed Oct 2 13:53:10 2013 +0200

    Fixes service update-rc.d functionality on debian

diff --git a/library/system/service b/library/system/service
index 478a59a58f..4529d6a3cc 100644
--- a/library/system/service
+++ b/library/system/service
@@ -633,6 +633,12 @@ class LinuxService(Service):
                     self.changed = True
                     break
 
+            # Debian compatibility
+            for line in err.splitlines():
+                if self.enable and line.find('no runlevel symlinks to modify') != -1:
+                    self.changed = True
+                    break
+
             if self.module.check_mode:
                 self.module.exit_json(changed=self.changed)
 
