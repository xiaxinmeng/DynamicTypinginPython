commit 75f1573ed506c59775d6d80223f7f09eeef5cd83
Author: Peter Sprygada <psprygada@ansible.com>
Date:   Sun Sep 11 23:35:54 2016 -0400

    roll up of updates to vyos shared module
    
    * cleans up method signature
    * removes save_config method in favor of super class

diff --git a/lib/ansible/module_utils/vyos.py b/lib/ansible/module_utils/vyos.py
index 156eb993e4..5b629650b3 100644
--- a/lib/ansible/module_utils/vyos.py
+++ b/lib/ansible/module_utils/vyos.py
@@ -96,15 +96,13 @@ class Cli(CliBase):
 
         return diff
 
-    def get_config(self, output='config'):
-        if output not in ['config', 'set']:
+    def get_config(self, output='text'):
+        if output not in ['text', 'set']:
             raise ValueError('invalid output format specified')
         if output == 'set':
             return self.execute(['show configuration commands'])[0]
         else:
             return self.execute(['show configuration'])[0]
 
-    def save_config(self):
-        raise NotImplementedError
 
 Cli = register_transport('cli', default=True)(Cli)
