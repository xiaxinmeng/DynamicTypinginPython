commit bcb9644f39ae7277d492f225d20dcee0684cf038
Author: Martin Krizek <martin.krizek@gmail.com>
Date:   Wed Aug 16 17:24:51 2017 +0200

    yum: cover "Nothing to do" not only for groups (#28283)

diff --git a/lib/ansible/modules/packaging/os/yum.py b/lib/ansible/modules/packaging/os/yum.py
index e924a3bdc0..2e4b89d53d 100644
--- a/lib/ansible/modules/packaging/os/yum.py
+++ b/lib/ansible/modules/packaging/os/yum.py
@@ -688,11 +688,8 @@ def exec_install(module, items, action, pkgs, res, yum_basecmd):
     res['msg'] += err
     res['changed'] = True
 
-    # special case for groups
-    for spec in items:
-        if spec.startswith('@'):
-            if ('Nothing to do' in out and rc == 0) or ('does not have any packages to install' in err):
-                res['changed'] = False
+    if ('Nothing to do' in out and rc == 0) or ('does not have any packages to install' in err):
+        res['changed'] = False
 
     if rc != 0:
         res['changed'] = False
