commit 1a88a3362f42dfd68c8d9ab955d546e9a8e74872
Author: Michael DeHaan <michael.dehaan@gmail.com>
Date:   Mon Jul 16 20:18:58 2012 -0400

    add noninteractive to debian module per #602

diff --git a/library/apt b/library/apt
index 9229325e1b..5f26114a35 100755
--- a/library/apt
+++ b/library/apt
@@ -33,7 +33,7 @@ import warnings;
 warnings.filterwarnings('ignore', "apt API not stable yet", FutureWarning)
 
 APT_PATH = "/usr/bin/apt-get"
-APT = "DEBIAN_PRIORITY=critical %s" % APT_PATH
+APT = "DEBIAN_FRONTEND=noninteractive DEBIAN_PRIORITY=critical %s" % APT_PATH
 
 def exit_json(rc=0, **kwargs):
     print json.dumps(kwargs)
