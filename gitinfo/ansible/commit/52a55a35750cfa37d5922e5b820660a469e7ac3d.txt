commit 52a55a35750cfa37d5922e5b820660a469e7ac3d
Author: Jan-Piet Mens <jpmens@gmail.com>
Date:   Fri Dec 14 11:12:26 2012 +0100

    cleanup Example output in ansible-doc

diff --git a/bin/ansible-doc b/bin/ansible-doc
index c892242357..7648e44178 100755
--- a/bin/ansible-doc
+++ b/bin/ansible-doc
@@ -89,8 +89,10 @@ def print_man(doc):
                             subsequent_indent=opt_indent)
 
 
-    for ex in doc['examples']:
-        print "%s%s" % (opt_indent, ex['code'])
+    if 'examples' in doc and len(doc['examples']) > 0:
+        print "Example%s:\n" % ('' if len(doc['examples']) < 2 else 's')
+        for ex in doc['examples']:
+            print "%s\n" % (ex['code'])
 
 def print_snippet(doc):
 
