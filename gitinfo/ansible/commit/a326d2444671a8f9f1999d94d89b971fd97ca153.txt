commit a326d2444671a8f9f1999d94d89b971fd97ca153
Author: Aaron Rice <aaron@sneakybox.com>
Date:   Sun Feb 9 16:33:34 2014 +0000

    Save api id to index as an int instead of a list

diff --git a/plugins/inventory/linode.py b/plugins/inventory/linode.py
index 472b56c8bc..b4bcb1fad6 100755
--- a/plugins/inventory/linode.py
+++ b/plugins/inventory/linode.py
@@ -132,7 +132,7 @@ class LinodeInventory(object):
                 if os.path.isfile(self.cache_path_index):
                     return True
         return False
- 
+
     def read_settings(self):
         """Reads the settings from the .ini file."""
         config = ConfigParser.SafeConfigParser()
@@ -204,7 +204,7 @@ class LinodeInventory(object):
         dest = node.label
 
         # Add to index
-        self.index[dest] = [node.api_id]
+        self.index[dest] = node.api_id
 
         # Inventory: Group by node ID (always a group of 1)
         self.inventory[node.api_id] = [dest]
