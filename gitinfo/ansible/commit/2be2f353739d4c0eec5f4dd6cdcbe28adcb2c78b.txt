commit 2be2f353739d4c0eec5f4dd6cdcbe28adcb2c78b
Author: Brian J. Dowling <bjd-dev@simplicity.net>
Date:   Thu Oct 13 14:35:45 2016 -0400

    Quick ansible-doc fix -- don't run pager if there was an error (no text)

diff --git a/lib/ansible/cli/doc.py b/lib/ansible/cli/doc.py
index 9d5a6afcbd..a49dba04ab 100644
--- a/lib/ansible/cli/doc.py
+++ b/lib/ansible/cli/doc.py
@@ -137,7 +137,8 @@ class DocCLI(CLI):
                 display.vvv(traceback.print_exc())
                 raise AnsibleError("module %s missing documentation (or could not parse documentation): %s\n" % (module, str(e)))
 
-        self.pager(text)
+        if text:
+            self.pager(text)
         return 0
 
     def find_modules(self, path):
