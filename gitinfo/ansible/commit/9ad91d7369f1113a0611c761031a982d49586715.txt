commit 9ad91d7369f1113a0611c761031a982d49586715
Author: Sundar Raman <cybertoast@gmail.com>
Date:   Thu Aug 9 17:54:29 2012 -0400

    Fix syntax to work on Python2.4
    
    The `val if something else something-else` syntax does not work prior to 2.5

diff --git a/library/pip b/library/pip
index 62cfa7b592..2e75cbb69e 100755
--- a/library/pip
+++ b/library/pip
@@ -25,7 +25,11 @@ ENV = None
 
 
 def _get_full_name(name, version=None):
-    return name if version is None else name + '==' + version
+    if version is None:
+        resp = name
+    else:
+        resp = name + '==' + version
+    return resp
 
 
 def _find_pip():
