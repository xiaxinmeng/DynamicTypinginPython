commit 46503c6d1fe57d4f3ef5981c28956aa565f82688
Author: JimFicarra <JimFicarra@users.noreply.github.com>
Date:   Sat Feb 4 03:32:06 2017 -0500

    win_msi:Fixing Issue #20315 - shows changed even when msi not found (#20358)
    
    * win_msi #20315 - shows changed even when msi not found
    
    * make brace placement consistent

diff --git a/lib/ansible/modules/windows/win_msi.ps1 b/lib/ansible/modules/windows/win_msi.ps1
index 07126583d1..abb778036c 100644
--- a/lib/ansible/modules/windows/win_msi.ps1
+++ b/lib/ansible/modules/windows/win_msi.ps1
@@ -37,6 +37,10 @@ if (($creates -ne $null) -and ($state -ne "absent") -and (Test-Path $creates)) {
     Exit-Json $result
 }
 
+if (-not (Test-Path $path)) {
+	Fail-Json $result "Cannot find $path."
+}
+
 $logfile = [IO.Path]::GetTempFileName()
 if ($state -eq "absent") {
 
