commit 89f28a98dd154955b37f8a93f6f1d42971c2d71b
Author: Jan-Piet Mens <jpmens@gmail.com>
Date:   Thu Oct 25 14:03:13 2012 +0200

    User is alias for name in 'user' module

diff --git a/library/user b/library/user
index 8e8278d012..d05a609dab 100755
--- a/library/user
+++ b/library/user
@@ -30,6 +30,7 @@ description:
 options:
     name:
         required: true
+        aliases: [ "user" ]
         description:
             - Name of the user to create, remove or modify.
     comment:
@@ -430,7 +431,7 @@ def main():
     module = AnsibleModule(
         argument_spec = dict(
             state=dict(default='present', choices=['present', 'absent']),
-            name=dict(required=True),
+            name=dict(required=True, aliases=['user']),
             uid=dict(default=None),
             group=dict(default=None),
             groups=dict(default=None),
