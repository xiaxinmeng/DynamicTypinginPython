commit 72cf11f8e1b7f802cf6f3f0e2216782cf0d3a163
Author: Michael Scherer <misc@zarb.org>
Date:   Wed Apr 15 00:59:39 2015 -0400

    Fix serialize function by using the right members name

diff --git a/v2/ansible/playbook/role/metadata.py b/v2/ansible/playbook/role/metadata.py
index 05ed2f3585..461a9a4a62 100644
--- a/v2/ansible/playbook/role/metadata.py
+++ b/v2/ansible/playbook/role/metadata.py
@@ -82,8 +82,8 @@ class RoleMetadata(Base):
 
     def serialize(self):
         return dict(
-            allow_duplicates = self.allow_duplicates,
-            dependencies     = self.dependencies,
+            allow_duplicates = self._allow_duplicates,
+            dependencies     = self._dependencies,
         )
 
     def deserialize(self, data):
