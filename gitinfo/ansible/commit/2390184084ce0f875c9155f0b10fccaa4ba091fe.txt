commit 2390184084ce0f875c9155f0b10fccaa4ba091fe
Author: Andrew Shults <shults@hioscar.com>
Date:   Tue Dec 9 17:14:16 2014 -0500

    Strip white space to support multiline permissions in YAML

diff --git a/lib/ansible/modules/database/mysql/mysql_user.py b/lib/ansible/modules/database/mysql/mysql_user.py
index 1fc57dc953..3ceed6e0b7 100644
--- a/lib/ansible/modules/database/mysql/mysql_user.py
+++ b/lib/ansible/modules/database/mysql/mysql_user.py
@@ -277,8 +277,8 @@ def privileges_unpack(priv):
     not specified in the string, as MySQL will always provide this by default.
     """
     output = {}
-    for item in priv.split('/'):
-        pieces = item.split(':')
+    for item in priv.strip().split('/'):
+        pieces = item.strip().split(':')
         if '.' in pieces[0]:
             pieces[0] = pieces[0].split('.')
             for idx, piece in enumerate(pieces):
