commit aee68511e65f7b955ff08d4bce8db0b43cb32139
Author: Thomas Ronner <thomas@ronner.org>
Date:   Thu Oct 17 15:02:23 2013 +0000

    Fix illegal set operation in FreeBSD section of user module.

diff --git a/library/system/user b/library/system/user
index 2e47055b7e..078d516003 100644
--- a/library/system/user
+++ b/library/system/user
@@ -745,7 +745,7 @@ class FreeBsdUser(User):
                 cmd.append('-G')
                 new_groups = groups
                 if self.append:
-                    new_groups.extend(current_groups)
+                    new_groups = groups | set(current_groups)
                 cmd.append(','.join(new_groups))
 
         # modify the user if cmd will do anything
