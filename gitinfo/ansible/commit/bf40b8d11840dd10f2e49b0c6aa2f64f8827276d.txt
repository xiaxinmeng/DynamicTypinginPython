commit bf40b8d11840dd10f2e49b0c6aa2f64f8827276d
Author: Jadi <jadijadi@gmail.com>
Date:   Fri Feb 8 06:17:50 2019 +0330

    Fixed wrong limit on 'Fetch all deployments' sample (#51897)
    
    Fixes: #51675
    
    the sample for 'Fetch all deployments' is "{{ lookup('k8s', kind='Deployment', namespace='testing') }}" but it should be "{{ lookup('k8s', kind='Deployment') }}"

diff --git a/lib/ansible/plugins/lookup/k8s.py b/lib/ansible/plugins/lookup/k8s.py
index 13965a2eb7..943513fd0c 100644
--- a/lib/ansible/plugins/lookup/k8s.py
+++ b/lib/ansible/plugins/lookup/k8s.py
@@ -135,7 +135,7 @@ EXAMPLES = """
 
 - name: Fetch all deployments
   set_fact:
-    deployments: "{{ lookup('k8s', kind='Deployment', namespace='testing') }}"
+    deployments: "{{ lookup('k8s', kind='Deployment') }}"
 
 - name: Fetch all deployments in a namespace
   set_fact:
