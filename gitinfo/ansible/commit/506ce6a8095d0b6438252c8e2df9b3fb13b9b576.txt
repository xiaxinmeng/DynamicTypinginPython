commit 506ce6a8095d0b6438252c8e2df9b3fb13b9b576
Author: Michael Scherer <misc@zarb.org>
Date:   Thu Oct 31 18:22:37 2013 +0100

    remove unused function

diff --git a/library/system/cron b/library/system/cron
index 81e411168b..c6f89682c7 100644
--- a/library/system/cron
+++ b/library/system/cron
@@ -341,12 +341,6 @@ class CronTab(object):
             result += '\n'
         return result
 
-    def _read_file_execute(self):
-        """
-        Returns the command line for reading a crontab
-        """
-        return "%s -l %s" % (CRONCMD, self._user_execute())
-
     def _read_user_execute(self):
         """
         Returns the command line for reading a crontab
