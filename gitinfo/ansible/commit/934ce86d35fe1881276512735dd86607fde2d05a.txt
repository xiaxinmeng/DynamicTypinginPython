commit 934ce86d35fe1881276512735dd86607fde2d05a
Author: Scott Cunningham <scott.cunningham@ensighten.com>
Date:   Tue Aug 4 18:29:37 2015 -0700

    update credstash lookup plugin to use Python 2.4-compatible exception catching

diff --git a/lib/ansible/plugins/lookup/credstash.py b/lib/ansible/plugins/lookup/credstash.py
index 5ff585f0e3..febccfa5c6 100644
--- a/lib/ansible/plugins/lookup/credstash.py
+++ b/lib/ansible/plugins/lookup/credstash.py
@@ -45,7 +45,7 @@ class LookupModule(LookupBase):
                 val = credstash.getSecret(term, **kwargs)
             except credstash.ItemNotFound:
                 raise AnsibleError('Key {} not found'.format(term))
-            except Exception as e:
+            except Exception, e:
                 raise AnsibleError('Encountered exception while fetching {}: {}'.format(term, e.message))
             ret.append(val)
 
