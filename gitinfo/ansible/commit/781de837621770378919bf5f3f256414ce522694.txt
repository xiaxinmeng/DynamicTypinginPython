commit 781de837621770378919bf5f3f256414ce522694
Author: James Tanner <tanner.jc@gmail.com>
Date:   Mon May 9 14:24:39 2016 -0400

    When walking through module directories, always follow symlinks. (#15784)
    
    Fixes #15783

diff --git a/lib/ansible/plugins/__init__.py b/lib/ansible/plugins/__init__.py
index e8558fef0a..19b217319e 100644
--- a/lib/ansible/plugins/__init__.py
+++ b/lib/ansible/plugins/__init__.py
@@ -136,7 +136,7 @@ class PluginLoader:
     def _all_directories(self, dir):
         results = []
         results.append(dir)
-        for root, subdirs, files in os.walk(dir):
+        for root, subdirs, files in os.walk(dir, followlinks=True):
            if '__init__.py' in files:
                for x in subdirs:
                    results.append(os.path.join(root,x))
