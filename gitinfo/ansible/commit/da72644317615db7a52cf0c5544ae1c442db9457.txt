commit da72644317615db7a52cf0c5544ae1c442db9457
Author: YuandongXu <786018072@qq.com>
Date:   Sat Jun 29 04:03:48 2019 +0800

    update acl interface. (#57905)
    
    * update acl interface.
    
    * update ce_interface
    
    * update
    
    * add a changelog fragment.
    
    * update

diff --git a/changelogs/fragments/57905-update-ce_acl_interface-to-fix-bugs.yml b/changelogs/fragments/57905-update-ce_acl_interface-to-fix-bugs.yml
new file mode 100644
index 0000000000..27b52a4ca2
--- /dev/null
+++ b/changelogs/fragments/57905-update-ce_acl_interface-to-fix-bugs.yml
@@ -0,0 +1,2 @@
+bugfixes:
+  - ce_acl_interface - Strict regularity can't find anything.
diff --git a/lib/ansible/modules/network/cloudengine/ce_acl_interface.py b/lib/ansible/modules/network/cloudengine/ce_acl_interface.py
index 837f85d4e3..321cedb309 100644
--- a/lib/ansible/modules/network/cloudengine/ce_acl_interface.py
+++ b/lib/ansible/modules/network/cloudengine/ce_acl_interface.py
@@ -169,7 +169,7 @@ class AclInterface(object):
                         msg='Error: The len of acl_name is out of [1 - 32].')
 
         if self.interface:
-            cmd = "display current-configuration | ignore-case section include ^interface %s$" % self.interface
+            cmd = "display current-configuration | ignore-case section include interface %s" % self.interface
             rc, out, err = exec_command(self.module, cmd)
             if rc != 0:
                 self.module.fail_json(msg=err)
@@ -199,7 +199,7 @@ class AclInterface(object):
     def get_existing(self):
         """ Get existing config """
 
-        cmd = "display current-configuration | ignore-case section include ^interface %s$ | include traffic-filter" % self.interface
+        cmd = "display current-configuration | ignore-case section include interface %s | include traffic-filter" % self.interface
         rc, out, err = exec_command(self.module, cmd)
         if rc != 0:
             self.module.fail_json(msg=err)
@@ -217,7 +217,7 @@ class AclInterface(object):
     def get_end_state(self):
         """ Get config end state """
 
-        cmd = "display current-configuration | ignore-case section include ^interface %s$ | include traffic-filter" % self.interface
+        cmd = "display current-configuration | ignore-case section include interface %s | include traffic-filter" % self.interface
         rc, out, err = exec_command(self.module, cmd)
         if rc != 0:
             self.module.fail_json(msg=err)
