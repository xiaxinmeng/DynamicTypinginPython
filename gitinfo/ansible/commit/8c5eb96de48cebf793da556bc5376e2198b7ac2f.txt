commit 8c5eb96de48cebf793da556bc5376e2198b7ac2f
Author: Hiroaki Nakamura <hnakamur@gmail.com>
Date:   Tue Oct 15 23:41:49 2013 +0900

    Do not use shortcut conditional operator which is not supported in Python 2.4.

diff --git a/library/system/hostname b/library/system/hostname
index 9836b98c71..826e0f630d 100644
--- a/library/system/hostname
+++ b/library/system/hostname
@@ -58,8 +58,10 @@ class UnimplementedStrategy(object):
     def unimplemented_error(self):
         platform = get_platform()
         distribution = get_distribution()
-        msg_platform = '%s (%s)' % (platform, distribution) \
-            if distribution is not None else platform
+        if distribution is not None:
+            msg_platform = '%s (%s)' % (platform, distribution)
+        else:
+            msg_platform = platform
         self.module.fail_json(
             msg='hostname module cannot be used on platform %s' % msg_platform)
 
