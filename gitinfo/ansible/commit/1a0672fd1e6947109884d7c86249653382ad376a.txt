commit 1a0672fd1e6947109884d7c86249653382ad376a
Author: Michael DeHaan <michael.dehaan@gmail.com>
Date:   Wed Mar 14 12:51:26 2012 -0400

    Fix exception type

diff --git a/lib/ansible/utils.py b/lib/ansible/utils.py
index a0aec630af..86774463b4 100755
--- a/lib/ansible/utils.py
+++ b/lib/ansible/utils.py
@@ -196,7 +196,7 @@ def parse_json(data):
         tokens = shlex.split(data)
         for t in tokens:
             if t.find("=") == -1:
-                raise AnsibleException("failed to parse: %s" % data)
+                raise AnsibleError("failed to parse: %s" % data)
             (key,value) = t.split("=", 1)
             if key == 'changed' or 'failed':
                 if value.lower() in [ 'true', '1' ] :
