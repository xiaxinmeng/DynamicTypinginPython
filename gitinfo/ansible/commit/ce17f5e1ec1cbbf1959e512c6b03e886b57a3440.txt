commit ce17f5e1ec1cbbf1959e512c6b03e886b57a3440
Author: Matt Stephenson <mattstep@mattstep.net>
Date:   Mon Feb 23 12:51:00 2015 -0800

    Fix indentation

diff --git a/lib/ansible/runner/filter_plugins/ipaddr.py b/lib/ansible/runner/filter_plugins/ipaddr.py
index bcb19b16fd..0a86e5200e 100644
--- a/lib/ansible/runner/filter_plugins/ipaddr.py
+++ b/lib/ansible/runner/filter_plugins/ipaddr.py
@@ -152,9 +152,9 @@ def _multicast_query(v, value):
         return value
 
 def _net_query(v):
-        if v.size > 1:
-            if v.ip == v.network:
-                return str(v.network) + '/' + str(v.prefixlen)
+    if v.size > 1:
+        if v.ip == v.network:
+            return str(v.network) + '/' + str(v.prefixlen)
 
 def _netmask_query(v):
     if v.size > 1:
