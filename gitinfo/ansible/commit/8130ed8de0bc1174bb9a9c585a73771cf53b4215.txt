commit 8130ed8de0bc1174bb9a9c585a73771cf53b4215
Author: Devin Christensen <quixoten@gmail.com>
Date:   Tue Dec 2 14:23:38 2014 -0700

    Allow .gitkeep in the jsonfile fact cache dir

diff --git a/lib/ansible/cache/jsonfile.py b/lib/ansible/cache/jsonfile.py
index 81918a2836..8b4c892a40 100644
--- a/lib/ansible/cache/jsonfile.py
+++ b/lib/ansible/cache/jsonfile.py
@@ -98,7 +98,7 @@ class CacheModule(BaseCacheModule):
     def keys(self):
         keys = []
         for k in os.listdir(self._cache_dir):
-            if not self.has_expired(k):
+            if not (k.startswith('.') or self.has_expired(k)):
                 keys.append(k)
         return keys
 
