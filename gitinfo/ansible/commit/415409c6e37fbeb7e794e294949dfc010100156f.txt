commit 415409c6e37fbeb7e794e294949dfc010100156f
Author: Matt Clay <matt@mystile.com>
Date:   Fri Feb 7 14:09:01 2020 -0800

    Rename azure_rm_virtualmachineimage_facts test.

diff --git a/test/integration/targets/azure_rm_virtualmachineimage_facts/aliases b/test/integration/targets/azure_rm_virtualmachineimage_info/aliases
similarity index 100%
rename from test/integration/targets/azure_rm_virtualmachineimage_facts/aliases
rename to test/integration/targets/azure_rm_virtualmachineimage_info/aliases
diff --git a/test/integration/targets/azure_rm_virtualmachineimage_facts/meta/main.yml b/test/integration/targets/azure_rm_virtualmachineimage_info/meta/main.yml
similarity index 100%
rename from test/integration/targets/azure_rm_virtualmachineimage_facts/meta/main.yml
rename to test/integration/targets/azure_rm_virtualmachineimage_info/meta/main.yml
diff --git a/test/integration/targets/azure_rm_virtualmachineimage_facts/tasks/main.yml b/test/integration/targets/azure_rm_virtualmachineimage_info/tasks/main.yml
similarity index 65%
rename from test/integration/targets/azure_rm_virtualmachineimage_facts/tasks/main.yml
rename to test/integration/targets/azure_rm_virtualmachineimage_info/tasks/main.yml
index e7e82697d9..5d16141dfa 100644
--- a/test/integration/targets/azure_rm_virtualmachineimage_facts/tasks/main.yml
+++ b/test/integration/targets/azure_rm_virtualmachineimage_info/tasks/main.yml
@@ -3,7 +3,7 @@
     location: eastus
 
 - name: Get facts for a specific image
-  azure_rm_virtualmachineimage_facts:
+  azure_rm_virtualmachineimage_info:
     location: "{{ location }}"
     publisher: OpenLogic
     offer: CentOS
@@ -12,10 +12,10 @@
   register: output
 
 - assert:
-      that: azure_vmimages | length == 1
+      that: output['vmimages'] | length == 1
 
 - name: List available versions
-  azure_rm_virtualmachineimage_facts:
+  azure_rm_virtualmachineimage_info:
     location: "{{ location }}"
     publisher: OpenLogic
     offer: CentOS
@@ -23,21 +23,21 @@
   register: output
 
 - assert:
-      that: azure_vmimages | length > 0
+      that: output['vmimages'] | length > 0
 
 - name: List available offers
-  azure_rm_virtualmachineimage_facts:
+  azure_rm_virtualmachineimage_info:
     location: "{{ location }}"
     publisher: OpenLogic
   register: output
 
 - assert:
-      that: azure_vmimages | length > 0
+      that: output['vmimages'] | length > 0
 
 - name: List available publishers
-  azure_rm_virtualmachineimage_facts:
+  azure_rm_virtualmachineimage_info:
     location: "{{ location }}"
   register: output
 
 - assert:
-      that: azure_vmimages | length > 0
+      that: output['vmimages'] | length > 0
