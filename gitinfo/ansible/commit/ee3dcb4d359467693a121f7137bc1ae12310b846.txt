commit ee3dcb4d359467693a121f7137bc1ae12310b846
Author: Evgeni Golov <evgeni@golov.de>
Date:   Wed Jan 15 20:19:16 2020 +0100

    use `query` instead of `q` in "if files exist" example (#66409)
    
    ##### SUMMARY
    It's not obvious that `q` is the same as `query` (which is referred to in the name of the task) and using long-names is generally preferred in examples.
    
    ##### ISSUE TYPE
    - Docs Pull Request
    
    +label: docsite_pr

diff --git a/lib/ansible/plugins/lookup/first_found.py b/lib/ansible/plugins/lookup/first_found.py
index a1828e6b9c..2e7b8b5a39 100644
--- a/lib/ansible/plugins/lookup/first_found.py
+++ b/lib/ansible/plugins/lookup/first_found.py
@@ -49,7 +49,7 @@ EXAMPLES = """
       files:
         - path/tasks.yaml
         - path/other_tasks.yaml
-  loop: "{{ q('first_found', params, errors='ignore') }}"
+  loop: "{{ query('first_found', params, errors='ignore') }}"
 
 - name: |
         copy first existing file found to /some/file,
