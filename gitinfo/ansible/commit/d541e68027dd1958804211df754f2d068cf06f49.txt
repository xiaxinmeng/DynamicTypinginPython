commit d541e68027dd1958804211df754f2d068cf06f49
Author: Jesse Keating <jesse.keating@rackspace.com>
Date:   Thu Jun 27 17:37:40 2013 -0700

    Elapsed time should account for the delay

diff --git a/library/utilities/wait_for b/library/utilities/wait_for
index a7fee8935f..fd7b8cbc29 100644
--- a/library/utilities/wait_for
+++ b/library/utilities/wait_for
@@ -92,11 +92,11 @@ def main():
     port = int(params['port'])
     state = params['state']
 
+    start = datetime.datetime.now()
+
     if delay:
         time.sleep(delay)
 
-    start = datetime.datetime.now()
-
     if state == 'stopped':
         ### first wait for the host to go down
         end = start + datetime.timedelta(seconds=timeout)
