commit 909f7f2ce5ec8ec2632ef66303767ac5685a9d84
Author: Gonéri Le Bouder <goneri@lebouder.net>
Date:   Thu Jul 11 04:36:28 2019 +0200

    vmware_dvswitch_nioc: fix the integration test (#58920)
    
    Correct the name of the vcenter variables.

diff --git a/test/integration/targets/vmware_dvswitch_nioc/tasks/main.yml b/test/integration/targets/vmware_dvswitch_nioc/tasks/main.yml
index c48b84897d..1aa4da06cd 100644
--- a/test/integration/targets/vmware_dvswitch_nioc/tasks/main.yml
+++ b/test/integration/targets/vmware_dvswitch_nioc/tasks/main.yml
@@ -35,9 +35,9 @@
     - &enable_nioc
       name: Enable NIOC
       vmware_dvswitch_nioc:
-        hostname: "{{ vcenter_server }}"
-        username: "{{ vcenter_user }}"
-        password: "{{ vcenter_pass }}"
+        hostname: "{{ vcenter_hostname }}"
+        username: "{{ vcenter_username }}"
+        password: "{{ vcenter_password }}"
         switch: dvswitch_0001
         version: version3
         validate_certs: no
@@ -74,9 +74,9 @@
     - &disable_nioc
       name: Disable NIOC
       vmware_dvswitch_nioc:
-        hostname: "{{ vcenter_server }}"
-        username: "{{ vcenter_user }}"
-        password: "{{ vcenter_pass }}"
+        hostname: "{{ vcenter_hostname }}"
+        username: "{{ vcenter_username }}"
+        password: "{{ vcenter_password }}"
         switch: dvswitch_0001
         validate_certs: no
         state: absent
