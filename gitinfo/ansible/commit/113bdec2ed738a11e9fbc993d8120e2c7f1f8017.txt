commit 113bdec2ed738a11e9fbc993d8120e2c7f1f8017
Author: Matt Martz <matt@sivel.net>
Date:   Tue Aug 28 15:52:07 2018 -0500

    Fix subelements lookup docs example (#44782)
    
    * Fix subelements lookup docs example
    
    * Fix grammar

diff --git a/lib/ansible/plugins/lookup/subelements.py b/lib/ansible/plugins/lookup/subelements.py
index 0059a38bd2..9212d820dd 100644
--- a/lib/ansible/plugins/lookup/subelements.py
+++ b/lib/ansible/plugins/lookup/subelements.py
@@ -72,9 +72,9 @@ EXAMPLES = """
         - "{{ users }}"
         - mysql.hosts
 
-    - name: list groups for user that have them, dont error if they don't
+    - name: list groups for users that have them, don't error if groups key is missing
       debug: var=item
-      with_list: "{{lookup('subelements', users, 'groups', 'skip_missing=True')}}"
+      loop: "{{lookup('subelements', users, 'groups', {'skip_missing': True})}}"
 """
 
 RETURN = """
