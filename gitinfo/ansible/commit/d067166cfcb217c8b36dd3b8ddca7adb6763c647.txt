commit d067166cfcb217c8b36dd3b8ddca7adb6763c647
Author: Tim Rupp <caphrim007@gmail.com>
Date:   Tue Oct 24 16:13:14 2017 -0700

    Segment out the update method (#32103)
    
    This patch changes the base parameters class to segment out the
    update method. This is done often throughout the f5 modules, so
    this just bakes it into the mod utils so that I can delete it
    from each module

diff --git a/lib/ansible/module_utils/f5_utils.py b/lib/ansible/module_utils/f5_utils.py
index 4f3f0d1bac..dd4a14fa1d 100644
--- a/lib/ansible/module_utils/f5_utils.py
+++ b/lib/ansible/module_utils/f5_utils.py
@@ -327,6 +327,11 @@ class AnsibleF5Client(object):
 class AnsibleF5Parameters(object):
     def __init__(self, params=None):
         self._values = defaultdict(lambda: None)
+        self._values['__warnings'] = []
+        if params:
+            self.update(params=params)
+
+    def update(self, params=None):
         if params:
             for k, v in iteritems(params):
                 if self.api_map is not None and k in self.api_map:
