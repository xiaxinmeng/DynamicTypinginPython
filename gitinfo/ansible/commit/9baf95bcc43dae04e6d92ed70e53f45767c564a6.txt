commit 9baf95bcc43dae04e6d92ed70e53f45767c564a6
Author: Benjamin JOLIVOT <bjolivot@users.noreply.github.com>
Date:   Wed Feb 15 23:30:06 2017 +0100

    Add status_code & failed:false when status==200 (#21457)
    
    * Add status_code & failed:false when status==200
    
    * remove failed=false, it's broking tests

diff --git a/lib/ansible/modules/network/basics/get_url.py b/lib/ansible/modules/network/basics/get_url.py
index a5a8c91085..a000ecbffc 100644
--- a/lib/ansible/modules/network/basics/get_url.py
+++ b/lib/ansible/modules/network/basics/get_url.py
@@ -457,7 +457,7 @@ def main():
 
     res_args = dict(
         url = url, dest = dest, src = tmpsrc, md5sum = md5sum, checksum_src = checksum_src,
-        checksum_dest = checksum_dest, changed = changed, msg = info.get('msg', '')
+        checksum_dest = checksum_dest, changed = changed, msg = info.get('msg', ''), status_code=info.get('status','')
     )
     if backup_file:
         res_args['backup_file'] = backup_file
