commit 274f15b52c2c0ff2d453d93f64451192084b41b7
Author: Daniel Hokka Zakrisson <daniel@hozac.com>
Date:   Sat Feb 23 12:03:59 2013 +0100

    Fix version_added for insertbefore in lineinfile

diff --git a/library/lineinfile b/library/lineinfile
index 89cc460095..7c50829a61 100644
--- a/library/lineinfile
+++ b/library/lineinfile
@@ -66,7 +66,7 @@ options:
     choices: [ 'EOF', '*regex*' ]
   insertbefore:
     required: false
-    versionadded: 1.1
+    version_added: 1.1
     description:
       - Used with C(state=present). If specified, the line will be inserted
         before the specified regular expression. A value is available;
@@ -209,8 +209,8 @@ def main():
             create=dict(default=False, choices=BOOLEANS),
             backup=dict(default=False, choices=BOOLEANS),
         ),
-    mutually_exclusive = [['insertbefore', 'insertafter']],
-    supports_check_mode = True
+        mutually_exclusive = [['insertbefore', 'insertafter']],
+        supports_check_mode = True
     )
 
     params = module.params
