commit 07ccd3c86bda94462ee1bbc05ecd178d66f89842
Author: Isaac Simmons <ids23@drexel.edu>
Date:   Mon Mar 23 11:46:59 2015 -0400

    Handle ini file properties that contain interpolation errors in the initial values

diff --git a/lib/ansible/modules/files/ini_file.py b/lib/ansible/modules/files/ini_file.py
index 9242821ae9..fff153af6a 100644
--- a/lib/ansible/modules/files/ini_file.py
+++ b/lib/ansible/modules/files/ini_file.py
@@ -120,6 +120,9 @@ def do_ini(module, filename, section=None, option=None, value=None, state='prese
                     if cp.get(section, option):
                         cp.remove_option(section, option)
                         changed = True
+                except ConfigParser.InterpolationError:
+                    cp.remove_option(section, option)
+                    changed = True
                 except:
                     pass
 
@@ -143,6 +146,9 @@ def do_ini(module, filename, section=None, option=None, value=None, state='prese
             except ConfigParser.NoOptionError:
                 cp.set(section, option, value)
                 changed = True
+            except ConfigParser.InterpolationError:
+                cp.set(section, option, value)
+                changed = True
 
     if changed and not module.check_mode:
         if backup:
