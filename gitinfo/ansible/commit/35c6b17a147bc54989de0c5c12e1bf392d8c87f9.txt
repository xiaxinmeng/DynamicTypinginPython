commit 35c6b17a147bc54989de0c5c12e1bf392d8c87f9
Author: Martin Krizek <martin.krizek@gmail.com>
Date:   Tue Dec 19 12:26:05 2017 +0100

    playbook_loops.rst: fix formatting (#34026)

diff --git a/docs/docsite/rst/playbooks_loops.rst b/docs/docsite/rst/playbooks_loops.rst
index 4a25d0d3e1..9473c91935 100644
--- a/docs/docsite/rst/playbooks_loops.rst
+++ b/docs/docsite/rst/playbooks_loops.rst
@@ -266,9 +266,9 @@ When using complex data structures for looping the display might get a bit too "
             nic02: 10Gb
             ...
       loop_control:
-        label: "{{item.name}}"
+        label: "{{ item.name }}"
 
-This will now display just the 'label' field instead of the whole structure per 'item', it defaults to '"{{item}}"' to display things as usual.
+This will now display just the ``label`` field instead of the whole structure per ``item``, it defaults to ``{{ item }}`` to display things as usual.
 
 .. versionadded:: 2.2
 
