commit 571f343e7d7dabae5ec82119a9dfae28fa0b51b3
Author: Matt Martz <matt@sivel.net>
Date:   Mon Jul 28 12:44:33 2014 -0500

    Make sure to assign pyrax.cloudservers to cs. Fixes #8250

diff --git a/library/cloud/rax_scaling_group b/library/cloud/rax_scaling_group
index 818c6089c8..2c635592b7 100644
--- a/library/cloud/rax_scaling_group
+++ b/library/cloud/rax_scaling_group
@@ -155,7 +155,8 @@ def rax_asg(module, cooldown=300, disk_config=None, files={}, flavor=None,
 
     au = pyrax.autoscale
     cnw = pyrax.cloud_networks
-    if not au or not cnw:
+    cs = pyrax.cloudservers
+    if not au or not cnw or not cs:
         module.fail_json(msg='Failed to instantiate clients. This '
                              'typically indicates an invalid region or an '
                              'incorrectly capitalized region name.')
