commit 6a7a8cda40e751587e2ed9c7df3c1644f175c9f0
Author: Lester Wade <lwade@firefly.home.net>
Date:   Wed Apr 24 09:45:01 2013 -0700

    Corrected instance password data check.  Previously broken after adding region support.

diff --git a/library/ec2_vol b/library/ec2_vol
index bf6435fd07..2899a47d59 100644
--- a/library/ec2_vol
+++ b/library/ec2_vol
@@ -176,7 +176,7 @@ def main():
 
     if device_name is None and instance:
         try:
-            if inst.get_password_data == '':
+            if not ec2.get_password_data(inst.id):
                 device_name = '/dev/sdf'
                 attach = volume.attach(inst.id, device_name)
                 while volume.attachment_state() != 'attached':
