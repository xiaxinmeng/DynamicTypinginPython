commit 8708a00cbd61e15bc349fd4e5c2e8df1ef4706db
Author: James Cammarata <jimi@sngx.net>
Date:   Wed Sep 10 09:06:40 2014 -0500

    Fix typo in redis cache change from 10afaee1

diff --git a/lib/ansible/cache/redis.py b/lib/ansible/cache/redis.py
index a80cbd9d83..c55b74469d 100644
--- a/lib/ansible/cache/redis.py
+++ b/lib/ansible/cache/redis.py
@@ -98,5 +98,5 @@ class CacheModule(BaseCacheModule):
         # FIXME: there is probably a better way to do this in redis
         ret = dict()
         for key in self.keys():
-            ret[key] self.get(key)
+            ret[key] = self.get(key)
         return ret
