commit 80e2de0e5ddec8b79cec7c0dbf9d966c1c782008
Author: Alexander Gubin <alex@alxg.de>
Date:   Mon Jan 12 17:43:51 2015 +0100

    lvol: Cast size(LogicalVolumeSize) to lower to fix the difference between lvs and lvcreate

diff --git a/lib/ansible/modules/extras/system/lvol.py b/lib/ansible/modules/extras/system/lvol.py
index e9d477edf8..8276d753db 100644
--- a/lib/ansible/modules/extras/system/lvol.py
+++ b/lib/ansible/modules/extras/system/lvol.py
@@ -132,8 +132,8 @@ def main():
 
         # LVCREATE(8) -L --size option unit
         elif size[-1].isalpha():
-            if size[-1] in 'bBsSkKmMgGtTpPeE':
-                size_unit = size[-1]
+            if size[-1].lower() in 'bskmgtpe':
+                size_unit = size[-1].lower()
                 if size[0:-1].isdigit():
                     size = int(size[0:-1])
                 else:
