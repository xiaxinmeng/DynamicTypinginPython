commit 93fd06d48c02c4b0e069f2694c27d48f1b958b09
Author: James Cammarata <jimi@sngx.net>
Date:   Fri Sep 20 16:57:29 2013 -0500

    Minor doc fix and a CHANGELOG update for the new rax_clb_nodes module

diff --git a/CHANGELOG.md b/CHANGELOG.md
index 2967ee7375..9e49144ea2 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -12,7 +12,7 @@ Highlighted new features:
 
 New modules:
 
-* TBA
+* cloud:rax_clb_nodes -- manage Rackspace cloud load balancers
 
 Misc changes:
 
diff --git a/library/cloud/rax_clb_nodes b/library/cloud/rax_clb_nodes
index 894303fb15..9276db458d 100644
--- a/library/cloud/rax_clb_nodes
+++ b/library/cloud/rax_clb_nodes
@@ -100,8 +100,8 @@ options:
 requirements: [ "pyrax" ]
 author: Lukasz Kawczynski
 notes:
-  - The following environment variables can be used: C(RAX_USERNAME),
-    C(RAX_API_KEY), C(RAX_CREDENTIALS) and C(RAX_REGION).
+  - "The following environment variables can be used: C(RAX_USERNAME),
+    C(RAX_API_KEY), C(RAX_CREDENTIALS) and C(RAX_REGION)."
 '''
 
 EXAMPLES = '''
