commit 21e28a431bbc6f08a02bbc6e7d5a30d4d262984b
Author: Trishna Guha <trishnaguha17@gmail.com>
Date:   Fri May 19 10:57:16 2017 +0530

    Remove unused method from nxos_ospf (#24820)

diff --git a/lib/ansible/modules/network/nxos/nxos_ospf.py b/lib/ansible/modules/network/nxos/nxos_ospf.py
index 746e7187a5..6fb4f7091a 100644
--- a/lib/ansible/modules/network/nxos/nxos_ospf.py
+++ b/lib/ansible/modules/network/nxos/nxos_ospf.py
@@ -100,15 +100,6 @@ def get_existing(module):
     return existing
 
 
-def apply_key_map(key_map, table):
-    new_dict = {}
-    for key, value in table:
-        new_key = key_map.get(key)
-        if new_key:
-            new_dict[new_key] = table.get(key)
-    return new_dict
-
-
 def state_present(module, proposed, candidate):
     commands = ['router ospf {0}'.format(proposed['ospf'])]
     candidate.add(commands, parents=[])
