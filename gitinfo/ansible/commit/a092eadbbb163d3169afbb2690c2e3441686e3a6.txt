commit a092eadbbb163d3169afbb2690c2e3441686e3a6
Author: Matt Wright <matt@nobien.net>
Date:   Fri Aug 31 11:03:14 2012 -0400

    Fix supervisorctl module after merge issue

diff --git a/library/supervisorctl b/library/supervisorctl
index 2f33c37ecd..7cf55fb1fd 100755
--- a/library/supervisorctl
+++ b/library/supervisorctl
@@ -19,10 +19,20 @@
 # along with Ansible.  If not, see <http://www.gnu.org/licenses/>.
 #
 
+def _find_supervisorctl():
+    paths = ['/usr/local/bin', '/usr/bin']
+
+    for p in paths:
+        e = p + '/supervisorctl'
+        if os.path.exists(e):
+            return e
+
+
 def _is_present(name):
     rc, out, err = _run('%s status' % _find_supervisorctl())
     return name in out
 
+
 def _is_running(name, supervisorctl):
     rc, out, err = _run('%s status %s' % (supervisorctl, name))
     return 'RUNNING' in out
