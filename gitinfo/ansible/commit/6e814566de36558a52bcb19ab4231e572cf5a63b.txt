commit 6e814566de36558a52bcb19ab4231e572cf5a63b
Author: James Cammarata <jimi@sngx.net>
Date:   Mon Jul 28 16:34:59 2014 -0500

    Unquote params passed to an include statement
    
    Fixes #8315

diff --git a/lib/ansible/playbook/play.py b/lib/ansible/playbook/play.py
index 49397b7cef..8f8ed36a78 100644
--- a/lib/ansible/playbook/play.py
+++ b/lib/ansible/playbook/play.py
@@ -573,6 +573,7 @@ class Play(object):
                 mv = task_vars.copy()
                 for t in tokens[1:]:
                     (k,v) = t.split("=", 1)
+                    v = utils.splitter.unquote(v)
                     mv[k] = template(self.basedir, v, mv)
                 dirname = self.basedir
                 if original_file:
diff --git a/test/integration/roles/test_good_parsing/tasks/main.yml b/test/integration/roles/test_good_parsing/tasks/main.yml
index e61a52cf19..f19078bf1e 100644
--- a/test/integration/roles/test_good_parsing/tasks/main.yml
+++ b/test/integration/roles/test_good_parsing/tasks/main.yml
@@ -121,3 +121,7 @@
   include: test_include.yml param={{ test_input }}
   register: result
 
+- name: test includes with quoted params
+  include: test_include.yml param="this is a param"
+  register: result
+
