commit 0e22afef5205deee3e829bb643c332ae1a035813
Author: jborean93 <jborean93@gmail.com>
Date:   Thu Sep 21 11:09:55 2017 +1000

    fix for make webdocs

diff --git a/docs/bin/plugin_formatter.py b/docs/bin/plugin_formatter.py
index d2ed99fa3c..5286a3aa28 100755
--- a/docs/bin/plugin_formatter.py
+++ b/docs/bin/plugin_formatter.py
@@ -123,7 +123,7 @@ def write_data(text, output_dir, outputname, module=None):
             outputname = outputname % module
 
         if not os.path.exists(output_dir):
-            os.mkdir(output_dir)
+            os.makedirs(output_dir)
         fname = os.path.join(output_dir, outputname)
         fname = fname.replace(".py", "")
         with open(fname, 'wb') as f:
