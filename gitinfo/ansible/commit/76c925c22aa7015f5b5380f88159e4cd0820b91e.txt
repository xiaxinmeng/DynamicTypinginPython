commit 76c925c22aa7015f5b5380f88159e4cd0820b91e
Author: Dave Johnson <dave@ansibleworks.com>
Date:   Wed Jul 30 15:48:30 2014 -0400

    Parameters to disable_rollback are "true" or "false"

diff --git a/library/cloud/cloudformation b/library/cloud/cloudformation
index 02132f5632..6a7838a51b 100644
--- a/library/cloud/cloudformation
+++ b/library/cloud/cloudformation
@@ -32,8 +32,8 @@ options:
     description:
       - If a stacks fails to form, rollback will remove the stack
     required: false
-    default: "no"
-    choices: [ "yes", "no" ]
+    default: "false"
+    choices: [ "true", "false" ]
     aliases: []
   template_parameters:
     description:
@@ -99,7 +99,7 @@ tasks:
 - name: launch ansible cloudformation example
   action: cloudformation >
     stack_name="ansible-cloudformation" state=present
-    region=us-east-1 disable_rollback=yes
+    region=us-east-1 disable_rollback=true
     template=files/cloudformation-example.json
   args:
     template_parameters:
