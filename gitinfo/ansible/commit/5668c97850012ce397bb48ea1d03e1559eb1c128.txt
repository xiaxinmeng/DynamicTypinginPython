commit 5668c97850012ce397bb48ea1d03e1559eb1c128
Author: Benjamin Schwarze <benjamin.schwarze@mailboxd.de>
Date:   Fri Jun 10 16:14:48 2016 +0200

    enable su support for docker connection

diff --git a/lib/ansible/plugins/connection/docker.py b/lib/ansible/plugins/connection/docker.py
index 67dc5a3042..ef227c31c3 100644
--- a/lib/ansible/plugins/connection/docker.py
+++ b/lib/ansible/plugins/connection/docker.py
@@ -51,10 +51,7 @@ class Connection(ConnectionBase):
 
     transport = 'docker'
     has_pipelining = True
-    # su currently has an undiagnosed issue with calculating the file
-    # checksums (so copy, for instance, doesn't work right)
-    # Have to look into that before re-enabling this
-    become_methods = frozenset(C.BECOME_METHODS).difference(('su',))
+    become_methods = frozenset(C.BECOME_METHODS)
 
     def __init__(self, play_context, new_stdin, *args, **kwargs):
         super(Connection, self).__init__(play_context, new_stdin, *args, **kwargs)
