commit 9b91068e23b47eebfaccf8bc409c2c988706f596
Author: John Barker <john@johnrbarker.com>
Date:   Mon Feb 24 17:09:53 2020 +0000

    Remove unused DCI script

diff --git a/test/utils/dci/dci-coverage.sh b/test/utils/dci/dci-coverage.sh
deleted file mode 100755
index 42eba4554b..0000000000
--- a/test/utils/dci/dci-coverage.sh
+++ /dev/null
@@ -1,33 +0,0 @@
-#!/bin/bash -eux
-
-set -o pipefail
-
-# Script invoked by DCI-Agents hooks/post-running.yml
-
-
-if find test/results/coverage/ -mindepth 1 -name '.*' -prune -o -print -quit | grep -q .; then
-    stub=""
-
-    # shellcheck disable=SC2086
-    bin/ansible-test coverage xml -v --requirements --group-by command --group-by version ${stub:+"$stub"}
-
-    # upload coverage report to codecov.io
-    for file in test/results/reports/coverage=*.xml; do
-        flags="${file##*/coverage=}"
-        flags="${flags%.xml}"
-        flags="${flags//=/,}"
-        flags="${flags//[^a-zA-Z0-9_,]/_}"
-
-        bash <(curl -s https://codecov.io/bash) \
-            -f "${file}" \
-            -F "${flags}" \
-            -t 83cd8957-dc76-488c-9ada-210dcea51633 \
-            -X coveragepy \
-            -X gcov \
-            -X fix \
-            -X search \
-            -X xcode \
-            -K \
-        || echo "Failed to upload code coverage report to codecov.io: ${file}"
-    done
-fi
