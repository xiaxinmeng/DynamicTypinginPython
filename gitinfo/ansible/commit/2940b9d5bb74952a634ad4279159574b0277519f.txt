commit 2940b9d5bb74952a634ad4279159574b0277519f
Author: ABond <ajbond2005@gmail.com>
Date:   Fri Feb 9 21:22:10 2018 -0500

    Removed sort from json dump since response is a mix of strings and integers (#34217)

diff --git a/contrib/inventory/digital_ocean.py b/contrib/inventory/digital_ocean.py
index 35991ccc97..f6b4c14eb3 100755
--- a/contrib/inventory/digital_ocean.py
+++ b/contrib/inventory/digital_ocean.py
@@ -313,7 +313,7 @@ class DigitalOceanInventory(object):
             self.write_to_cache()
 
         if self.args.pretty:
-            print(json.dumps(json_data, sort_keys=True, indent=2))
+            print(json.dumps(json_data, indent=2))
         else:
             print(json.dumps(json_data))
 
@@ -517,7 +517,7 @@ class DigitalOceanInventory(object):
     def write_to_cache(self):
         """ Writes data in JSON format to a file """
         data = {'data': self.data, 'inventory': self.inventory}
-        json_data = json.dumps(data, sort_keys=True, indent=2)
+        json_data = json.dumps(data, indent=2)
 
         with open(self.cache_filename, 'w') as cache:
             cache.write(json_data)
