commit ca6db40cb4f7da7d5a57b951bf99b5782e7a675d
Author: James Cammarata <jimi@sngx.net>
Date:   Tue Aug 19 09:02:50 2014 -0500

    When serializing inventory, pop from the start of the hosts list
    
    Fixes #8646

diff --git a/lib/ansible/playbook/__init__.py b/lib/ansible/playbook/__init__.py
index c52acf3caa..da6357bf23 100644
--- a/lib/ansible/playbook/__init__.py
+++ b/lib/ansible/playbook/__init__.py
@@ -677,7 +677,7 @@ class PlayBook(object):
                 play_hosts = []
                 for x in range(serial):
                     if len(all_hosts) > 0:
-                        play_hosts.append(all_hosts.pop())
+                        play_hosts.append(all_hosts.pop(0))
                 serialized_batch.append(play_hosts)
 
         task_errors = False
