commit 917f929e86817f7b5c232b4a8ad192c329e0f1bb
Author: Michael DeHaan <michael.dehaan@gmail.com>
Date:   Wed Mar 14 19:08:10 2012 -0400

    Fix template module

diff --git a/library/template b/library/template
index 3850cb152c..110d397aa1 100755
--- a/library/template
+++ b/library/template
@@ -31,8 +31,13 @@ except ImportError:
 # to a dictionary
 # FIXME: make more idiomatic
 
-args = " ".join(sys.argv[1:])
-items = shlex.split(args)
+if len(sys.argv) == 1:
+    sys.exit(1)
+argfile = sys.argv[1]
+if not os.path.exists(argfile):
+    sys.exit(1)
+items = shlex.split(open(argfile, 'r').read())
+
 params = {}
 for x in items:
     (k, v) = x.split("=")
