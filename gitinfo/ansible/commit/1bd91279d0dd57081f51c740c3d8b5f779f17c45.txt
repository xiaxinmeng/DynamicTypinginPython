commit 1bd91279d0dd57081f51c740c3d8b5f779f17c45
Author: Abhijeet Kasurde <akasurde@redhat.com>
Date:   Fri Jul 5 11:00:10 2019 +0530

    Fixed fail_json call in command module (#58615)
    
    Signed-off-by: Abhijeet Kasurde <akasurde@redhat.com>

diff --git a/changelogs/fragments/command_fail_json.yml b/changelogs/fragments/command_fail_json.yml
new file mode 100644
index 0000000000..b945de5038
--- /dev/null
+++ b/changelogs/fragments/command_fail_json.yml
@@ -0,0 +1,2 @@
+minor_changes:
+- Corrected API call for module.fail_json in command module.
diff --git a/lib/ansible/modules/commands/command.py b/lib/ansible/modules/commands/command.py
index d561855c2a..0365fc1b05 100644
--- a/lib/ansible/modules/commands/command.py
+++ b/lib/ansible/modules/commands/command.py
@@ -262,12 +262,12 @@ def main():
         try:
             chdir = to_bytes(os.path.abspath(chdir), errors='surrogate_or_strict')
         except ValueError as e:
-            module.jail_json('Unable to use supplied chdir: %s' % to_text(e))
+            module.fail_json(msg='Unable to use supplied chdir: %s' % to_text(e))
 
         try:
             os.chdir(chdir)
         except (IOError, OSError) as e:
-            module.fail_json('Unable to change directory before execution: %s' % to_text(e))
+            module.fail_json(msg='Unable to change directory before execution: %s' % to_text(e))
 
     if creates:
         # do not run the command if the line contains creates=filename
