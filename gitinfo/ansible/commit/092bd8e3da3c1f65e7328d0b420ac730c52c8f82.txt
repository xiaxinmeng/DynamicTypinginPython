commit 092bd8e3da3c1f65e7328d0b420ac730c52c8f82
Author: Michael DeHaan <michael.dehaan@gmail.com>
Date:   Thu Aug 30 19:09:02 2012 -0400

    make parameter required

diff --git a/library/selinux b/library/selinux
index 0e38fdf134..c2302ad1f7 100644
--- a/library/selinux
+++ b/library/selinux
@@ -101,7 +101,7 @@ def main():
     module = AnsibleModule(
         argument_spec = dict(
             policy=dict(required=True),
-            state=dict(choices=['enforcing', 'permissive', 'disabled'], default='enforcing'),
+            state=dict(choices=['enforcing', 'permissive', 'disabled'], required=True),
             configfile=dict(aliases=['conf','file'], default='/etc/selinux/config')
         )
     )
