commit cf9b22103d882e99da99d198cca4f77bf2afe409
Author: Matt Martz <matt@sivel.net>
Date:   Mon Oct 5 11:16:44 2015 -0500

    Warnings should not increment the exit status

diff --git a/ansible_testing/modules.py b/ansible_testing/modules.py
index 640c00885e..32bebf573c 100644
--- a/ansible_testing/modules.py
+++ b/ansible_testing/modules.py
@@ -72,7 +72,7 @@ class Validator(object):
         if warnings:
             for warning in self.warnings:
                 print('WARNING: %s' % warning)
-                ret.append(1)
+                # ret.append(1)  # Don't incrememt exit status for warnings
 
         if self.errors or (warnings and self.warnings):
             print()
