commit 896eecfdad7c47d51b0f886322f47d7e801f14a3
Author: Brian Coca <bcoca@ansible.com>
Date:   Fri Jan 1 01:52:52 2016 -0500

    fixed domain_id to actually be supported
    
    also added domain as an alias
    alt fixes #1437

diff --git a/lib/ansible/modules/extras/cloud/openstack/os_project.py b/lib/ansible/modules/extras/cloud/openstack/os_project.py
index 37901f8412..b159264ebd 100644
--- a/lib/ansible/modules/extras/cloud/openstack/os_project.py
+++ b/lib/ansible/modules/extras/cloud/openstack/os_project.py
@@ -49,6 +49,7 @@ options:
         - Domain id to create the project in if the cloud supports domains
      required: false
      default: None
+     aliases: ['domain']
    enabled:
      description:
         - Is the project enabled
@@ -138,7 +139,7 @@ def main():
     argument_spec = openstack_full_argument_spec(
         name=dict(required=True),
         description=dict(required=False, default=None),
-        domain=dict(required=False, default=None),
+        domain_id=dict(required=False, default=None, aliases=['domain']),
         enabled=dict(default=True, type='bool'),
         state=dict(default='present', choices=['absent', 'present'])
     )
@@ -155,7 +156,7 @@ def main():
 
     name = module.params['name']
     description = module.params['description']
-    domain = module.params['domain']
+    domain = module.params['domain_id']
     enabled = module.params['enabled']
     state = module.params['state']
 
