commit 40ff3eb106709094942f02d15b21271e354d5656
Author: Michael DeHaan <michael.dehaan@gmail.com>
Date:   Wed Aug 8 21:19:20 2012 -0400

    Catch socket errors so if you control-c a playbook it does not traceback

diff --git a/lib/ansible/runner/__init__.py b/lib/ansible/runner/__init__.py
index e907bea4c5..ec71af5fe4 100644
--- a/lib/ansible/runner/__init__.py
+++ b/lib/ansible/runner/__init__.py
@@ -28,6 +28,7 @@ import base64
 import getpass
 import codecs
 import collections
+import socket
 import re
 
 import ansible.constants as C
@@ -741,8 +742,11 @@ class Runner(object):
                 worker.join()
 
         results = []
-        while not result_queue.empty():
-            results.append(result_queue.get(block=False))
+        try:
+            while not result_queue.empty():
+                results.append(result_queue.get(block=False))
+        except socket.error:
+            raise errors.AnsibleError("<interrupted>")
         return results
 
     # *****************************************************
