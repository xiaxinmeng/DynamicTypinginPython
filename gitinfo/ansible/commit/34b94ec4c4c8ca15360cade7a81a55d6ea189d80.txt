commit 34b94ec4c4c8ca15360cade7a81a55d6ea189d80
Author: Brian Coca <bcoca@ansible.com>
Date:   Mon Jun 13 15:48:43 2016 -0400

    Revert "Add mode option to the list" (#3946)

diff --git a/lib/ansible/modules/network/basics/get_url.py b/lib/ansible/modules/network/basics/get_url.py
index 3ea571d9cd..d79b24dc0a 100644
--- a/lib/ansible/modules/network/basics/get_url.py
+++ b/lib/ansible/modules/network/basics/get_url.py
@@ -159,13 +159,6 @@ options:
     description:
       - all arguments accepted by the M(file) module also work here
     required: false
-  mode:
-    description:
-      - mode the destination file should be. For those used to /usr/bin/chmod remember that modes are actually
-        octal numbers (like 0644). Leaving off the leading zero will likely have unexpected results.
-        As of version 1.8, the mode may be specified as a symbolic mode (for example, C(u+rwx) or C(u=rw,g=r,o=r)).
-    required: false
-    version_added: '2.2'
 # informational: requirements for nodes
 requirements: [ ]
 author: "Jan-Piet Mens (@jpmens)"
