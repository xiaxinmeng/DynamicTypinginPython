commit 6b0ef5f10ea5f744d8eb80f491c0fb85ed3fe4ff
Author: David Fox <fox.david@epa.gov>
Date:   Sat Mar 22 15:12:56 2014 -0400

    Modified library/system/user so that password change date is set on Solaris when updating password

diff --git a/library/system/user b/library/system/user
index a6d3a0ec32..12b1a62d27 100644
--- a/library/system/user
+++ b/library/system/user
@@ -1186,6 +1186,7 @@ class SunOS(User):
                             lines.append(line)
                             continue
                         fields[1] = self.password
+                        fields[2] = str(int(time.time() / 86400))
                         line = ':'.join(fields)
                         lines.append('%s\n' % line)
                     open(self.SHADOWFILE, 'w+').writelines(lines)
@@ -1272,6 +1273,7 @@ class SunOS(User):
                             lines.append(line)
                             continue
                         fields[1] = self.password
+                        fields[2] = str(int(time.time() / 86400))	
                         line = ':'.join(fields)
                         lines.append('%s\n' % line)
                     open(self.SHADOWFILE, 'w+').writelines(lines)
