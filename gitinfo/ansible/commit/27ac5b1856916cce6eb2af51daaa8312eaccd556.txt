commit 27ac5b1856916cce6eb2af51daaa8312eaccd556
Author: Matt Martz <matt@sivel.net>
Date:   Sun May 25 19:39:43 2014 -0500

    Don't fail in rax_cbs when the volume is not found

diff --git a/library/cloud/rax_cbs b/library/cloud/rax_cbs
index 443c833e7d..ade2b83eac 100644
--- a/library/cloud/rax_cbs
+++ b/library/cloud/rax_cbs
@@ -141,6 +141,8 @@ def cloud_block_storage(module, state, name, description, meta, size,
     except ValueError:
         try:
             volume = cbs.find(name=name)
+        except pyrax.exc.NotFound:
+            pass
         except Exception, e:
             module.fail_json(msg='%s' % e)
 
