commit 9a2545957ff05a0b350bea808ee1b6af3c0b8046
Author: Nigel Metheringham <nigel.metheringham@jrtheatre.co.uk>
Date:   Mon Oct 29 22:04:06 2012 +0000

    Turn off some of the additional debug syslogging

diff --git a/library/user b/library/user
index 21c0d0d0d9..88b20b89b4 100755
--- a/library/user
+++ b/library/user
@@ -220,7 +220,9 @@ class User(object):
             self.ssh_file = module.params['ssh_key_file']
         else:
             self.ssh_file = os.path.join('.ssh', 'id_%s' % self.ssh_type)
-        self.syslogging = True
+
+        # select whether we dump additional debug info through syslog
+        self.syslogging = False
 
     def execute_command(self,cmd):
         if self.syslogging:
