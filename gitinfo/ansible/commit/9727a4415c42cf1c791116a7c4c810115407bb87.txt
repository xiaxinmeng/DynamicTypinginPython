commit 9727a4415c42cf1c791116a7c4c810115407bb87
Author: James Tanner <tanner.jc@gmail.com>
Date:   Tue Nov 19 16:41:51 2013 -0500

    Add warning comment to rhn_register module about fixing pep8 errors

diff --git a/library/packaging/rhn_register b/library/packaging/rhn_register
index 9a4fbcb3b0..ba6ddead39 100644
--- a/library/packaging/rhn_register
+++ b/library/packaging/rhn_register
@@ -172,6 +172,8 @@ class Rhn(RegistrationBase):
         # configuration.  Yeah, I know this should be subclassed ... but, oh
         # well
         def get_option_default(self, key, default=''):
+            # ignore pep8 W601 errors for this line
+            # setting this to use 'in' does not work in the rhn library
             if self.has_key(key):
                 return self[key]
             else:
