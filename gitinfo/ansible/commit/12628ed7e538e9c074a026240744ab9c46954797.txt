commit 12628ed7e538e9c074a026240744ab9c46954797
Author: sirkubax <jakubxmuszynski@gmail.com>
Date:   Tue Jan 17 03:05:54 2017 +0100

    fix https://github.com/ansible/ansible/issues/20183 (#20217)
    
    * fix https://github.com/ansible/ansible/issues/20183
    
    * make it nicer

diff --git a/lib/ansible/modules/files/unarchive.py b/lib/ansible/modules/files/unarchive.py
index 0f287f6fae..ac11346ae6 100644
--- a/lib/ansible/modules/files/unarchive.py
+++ b/lib/ansible/modules/files/unarchive.py
@@ -555,9 +555,10 @@ class ZipArchive(object):
         return dict(unarchived=unarchived, rc=rc, out=out, err=err, cmd=cmd, diff=diff)
 
     def unarchive(self):
-        cmd = [ self.cmd_path, '-o', self.src ]
+        cmd = [ self.cmd_path, '-o' ]
         if self.opts:
             cmd.extend(self.opts)
+        cmd.append(self.src)
          # NOTE: Including (changed) files as arguments is problematic (limits on command line/arguments)
 #        if self.includes:
             # NOTE: Command unzip has this strange behaviour where it expects quoted filenames to also be escaped
