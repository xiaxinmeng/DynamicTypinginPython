commit e6ced9c73f30422bf2a25f46f8e551589e45bfcd
Author: John R Barker <john@johnrbarker.com>
Date:   Sat Dec 29 13:01:35 2018 +0000

    Debug: Size of coverage file (#50380)

diff --git a/test/utils/shippable/shippable.sh b/test/utils/shippable/shippable.sh
index 42f2e57663..9f52b79b4a 100755
--- a/test/utils/shippable/shippable.sh
+++ b/test/utils/shippable/shippable.sh
@@ -88,6 +88,8 @@ function cleanup
         # upload coverage report to codecov.io only when using complete on-demand coverage
         if [ "${COVERAGE}" ] && [ "${CHANGED}" == "" ]; then
             for file in test/results/reports/coverage=*.xml; do
+                ls -l "${file}"
+
                 flags="${file##*/coverage=}"
                 flags="${flags%.xml}"
                 flags="${flags//=/,}"
