commit 48e82b4ca811642951f4d7ad7d3182ae34002430
Author: Matt Clay <matt@mystile.com>
Date:   Tue Mar 7 16:31:53 2017 -0800

    Add `test/runner` to $PATH in `hacking/env-setup`. (#22387)

diff --git a/hacking/env-setup b/hacking/env-setup
index 869f3b6551..6ac339c293 100644
--- a/hacking/env-setup
+++ b/hacking/env-setup
@@ -31,7 +31,7 @@ FULL_PATH=$($PYTHON_BIN -c "import os; print(os.path.realpath('$HACKING_DIR'))")
 export ANSIBLE_HOME="$(dirname "$FULL_PATH")"
 
 PREFIX_PYTHONPATH="$ANSIBLE_HOME/lib"
-PREFIX_PATH="$ANSIBLE_HOME/bin"
+PREFIX_PATH="$ANSIBLE_HOME/bin:$ANSIBLE_HOME/test/runner"
 PREFIX_MANPATH="$ANSIBLE_HOME/docs/man"
 
 expr "$PYTHONPATH" : "${PREFIX_PYTHONPATH}.*" > /dev/null || export PYTHONPATH="$PREFIX_PYTHONPATH:$PYTHONPATH"
