commit 3c64ac22640cc816b83844b27173ac2563574bf1
Author: Stephen Ryan <ryaner@users.noreply.github.com>
Date:   Tue Jun 30 16:44:10 2020 +0100

    Clean up symlink cache during 'make clean' (#70350)
    
    Make building across versions, renamed modules can result in no such
    file errors by the install.

diff --git a/Makefile b/Makefile
index 60934b2891..79110d8f07 100644
--- a/Makefile
+++ b/Makefile
@@ -153,6 +153,8 @@ clean:
 	rm -f test/units/.coverage*
 	rm -rf test/results/*/*
 	find test/ -type f -name '*.retry' -delete
+	@echo "Cleaning up symlink cache"
+	rm -f SYMLINK_CACHE.json
 	@echo "Cleaning up Debian building stuff"
 	rm -rf debian
 	rm -rf deb-build
