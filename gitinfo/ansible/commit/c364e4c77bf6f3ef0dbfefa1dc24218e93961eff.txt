commit c364e4c77bf6f3ef0dbfefa1dc24218e93961eff
Author: Daniel Hokka Zakrisson <daniel@hozac.com>
Date:   Thu Feb 21 21:14:29 2013 +0100

    cron: Work even without initial crontab
    
    Fixes #2165.

diff --git a/library/cron b/library/cron
index 51a8abf293..7d0ef5db4c 100644
--- a/library/cron
+++ b/library/cron
@@ -333,12 +333,9 @@ def main():
             os.unlink(backupfile)
             module.exit_json(changed=changed,cron_file=cron_file,state=state)
 
-    if (rc != 0):
-        module.fail_json(msg=err)
-
     if changed:
         # If the file is empty - remove it
-        if rm:
+        if rm and cron_file:
             remove_job_file(cron_file)
         else:
             if backup:
