commit 622ea7ca629e2d8294b0ed267a5a081bf1773f94
Author: James Cammarata <jimi@sngx.net>
Date:   Tue Aug 1 18:49:34 2017 -0500

    Change default answer in release.yml to "no" for submodules
    
    Since releases on 2.2 and prior are few and far between, it makes more
    sense to set the default to "no" for 2.3+ now.

diff --git a/packaging/release/release.yml b/packaging/release/release.yml
index 8b7ce7d9fc..11759eb749 100644
--- a/packaging/release/release.yml
+++ b/packaging/release/release.yml
@@ -33,7 +33,7 @@
   #  private: no
   - name: has_submodules
     prompt: "Does this branch have git submodules?"
-    default: "yes"
+    default: "no"
     private: no
   - name: is_final
     prompt: "Is this a final release (not a beta/rc)?"
