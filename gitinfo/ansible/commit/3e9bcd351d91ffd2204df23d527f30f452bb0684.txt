commit 3e9bcd351d91ffd2204df23d527f30f452bb0684
Author: Sebastien Bocahu <sebastien@ispfr.net>
Date:   Mon Aug 13 19:23:53 2012 +0200

    Add support for Debian in apt_repository

diff --git a/library/apt_repository b/library/apt_repository
index 3fe7dceb8d..b495b7fed1 100755
--- a/library/apt_repository
+++ b/library/apt_repository
@@ -72,7 +72,7 @@ def main():
 
     cmd = '%s %s' % (add_apt_repository, repo)
 
-    if float(platform.dist()[1]) >= 11.10:
+    if platform.dist()[0] == 'debian' or float(platform.dist()[1]) >= 11.10:
         cmd = cmd + ' -y'
 
     rc, out, err = _run(cmd)
