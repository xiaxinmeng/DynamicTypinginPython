commit 6da2c371128cea88dde2498c2d30a410b449d31e
Author: James Tanner <tanner.jc@gmail.com>
Date:   Tue Mar 11 21:38:00 2014 -0400

    Fixes #6411 Return None for ec2 api endpoints that return 404

diff --git a/library/cloud/ec2_facts b/library/cloud/ec2_facts
index 31024b65f8..09c9d761ef 100644
--- a/library/cloud/ec2_facts
+++ b/library/cloud/ec2_facts
@@ -71,7 +71,11 @@ class Ec2Metadata(object):
 
     def _fetch(self, url):
         (response, info) = fetch_url(self.module, url, force=True)
-        return response.read()
+        if response:
+            data = response.read()
+        else:
+            data = None
+        return data
 
     def _mangle_fields(self, fields, uri, filter_patterns=['public-keys-0']):
         new_fields = {}
