commit 49bb4803b544eb1a436084ac96a80c55ccc96d62
Author: Brian Coca <bcoca@ansible.com>
Date:   Tue Mar 8 10:55:38 2016 -0500

    clarify --step prompt
    
    now shows full words and indicates default
    fixes #7433

diff --git a/lib/ansible/plugins/strategy/__init__.py b/lib/ansible/plugins/strategy/__init__.py
index 9f4e504cf0..3875211402 100644
--- a/lib/ansible/plugins/strategy/__init__.py
+++ b/lib/ansible/plugins/strategy/__init__.py
@@ -623,10 +623,10 @@ class StrategyBase:
     def _take_step(self, task, host=None):
 
         ret=False
+        msg=u'Perform task: %s ' % task
         if host:
-            msg = u'Perform task: %s on %s (y/n/c): ' % (task, host)
-        else:
-            msg = u'Perform task: %s (y/n/c): ' % task
+            msg += u'on %s ' % host
+        msg += u'(N)o/(y)es/(c)ontinue: '
         resp = display.prompt(msg)
 
         if resp.lower() in ['y','yes']:
