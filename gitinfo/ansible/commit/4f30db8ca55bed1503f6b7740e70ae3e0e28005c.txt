commit 4f30db8ca55bed1503f6b7740e70ae3e0e28005c
Author: James Cammarata <jimi@sngx.net>
Date:   Mon Sep 14 14:41:22 2015 -0400

    Check if path is /dev/null when checking if a file is in fact a file

diff --git a/lib/ansible/parsing/__init__.py b/lib/ansible/parsing/__init__.py
index 11aad338c1..0c8e21fbd2 100644
--- a/lib/ansible/parsing/__init__.py
+++ b/lib/ansible/parsing/__init__.py
@@ -125,7 +125,7 @@ class DataLoader():
 
     def is_file(self, path):
         path = self.path_dwim(path)
-        return os.path.isfile(path)
+        return os.path.isfile(path) or path == os.devnull
 
     def is_directory(self, path):
         path = self.path_dwim(path)
