commit f482649cbb0535534b90dd8d5a6c1c8d9807633f
Author: Chris Gardner <chris_g_g@hotmail.com>
Date:   Wed May 29 21:00:21 2013 +0100

    Add clock speed to processor description for SPARC CPU.

diff --git a/library/system/setup b/library/system/setup
index 27c1651bf9..91dc0fff3d 100644
--- a/library/system/setup
+++ b/library/system/setup
@@ -669,8 +669,13 @@ class SunOSHardware(Hardware):
                 brand = ''
             elif key == 'brand':
                 brand = data[1].strip()
+            elif key == 'clock_MHz':
+                clock_mhz = data[1].strip()
             elif key == 'implementation':
                 processor = brand or data[1].strip()
+                # Add clock speed to description for SPARC CPU
+                if self.facts['machine'] != 'i86pc':
+                    processor += " @ " + clock_mhz + "MHz"
                 if 'processor' not in self.facts:
                     self.facts['processor'] = []
                 self.facts['processor'].append(processor)
