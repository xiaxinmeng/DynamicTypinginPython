commit 1f8b2f3e6e18370be97db592c8f2b4b06e9791bb
Author: Michael Scherer <misc@zarb.org>
Date:   Tue Aug 23 17:38:59 2016 +0200

    Add support for setting fstab location on Linux too for mount (#3271)
    
    Fix #3153

diff --git a/lib/ansible/modules/system/mount.py b/lib/ansible/modules/system/mount.py
index cf2a2823e5..0be9ee2416 100644
--- a/lib/ansible/modules/system/mount.py
+++ b/lib/ansible/modules/system/mount.py
@@ -233,6 +233,9 @@ def mount(module, **kwargs):
     if get_platform().lower() == 'freebsd':
         cmd += [ '-F', args['fstab'], ]
 
+    if get_platform().lower() == 'linux':
+        cmd += [ '-T', args['fstab'], ]
+
     cmd += [ name, ]
 
     rc, out, err = module.run_command(cmd)
