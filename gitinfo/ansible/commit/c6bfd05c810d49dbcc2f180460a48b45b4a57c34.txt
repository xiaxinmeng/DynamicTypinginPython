commit c6bfd05c810d49dbcc2f180460a48b45b4a57c34
Author: Michael DeHaan <michael.dehaan@gmail.com>
Date:   Tue Sep 30 15:17:28 2014 -0400

    Document pattern ranges on the patterns page.

diff --git a/docsite/rst/intro_patterns.rst b/docsite/rst/intro_patterns.rst
index 1dd1935f06..41e9affc81 100644
--- a/docsite/rst/intro_patterns.rst
+++ b/docsite/rst/intro_patterns.rst
@@ -68,6 +68,14 @@ It's also ok to mix wildcard patterns and groups at the same time::
 
     one*.com:dbservers
 
+As an advanced usage, you can also select the numbered server in a group::
+   
+    webservers[0]
+
+Or a portion of servers in a group::
+
+    webservers[0:25]
+
 Most people don't specify patterns as regular expressions, but you can.  Just start the pattern with a '~'::
 
     ~(web|db).*\.example\.com
