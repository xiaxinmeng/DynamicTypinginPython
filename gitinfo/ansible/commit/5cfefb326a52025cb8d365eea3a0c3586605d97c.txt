commit 5cfefb326a52025cb8d365eea3a0c3586605d97c
Author: James Laska <jlaska@ansible.com>
Date:   Fri Mar 14 10:24:52 2014 -0400

    Correct check_mode conditional logic
    
    The desired behavior is to _not_ add the ppa signing key when check_mode is
    enabled.  This fix corrects the conditional logic to comply with the stated
    behavior.

diff --git a/library/packaging/apt_repository b/library/packaging/apt_repository
index 7a19dabc16..8e902d5a44 100644
--- a/library/packaging/apt_repository
+++ b/library/packaging/apt_repository
@@ -353,9 +353,9 @@ def get_add_ppa_signing_key_callback(module):
         module.run_command(command, check_rc=True)
 
     if module.check_mode:
-        return _run_command 
-    else:
         return None
+    else:
+        return _run_command
 
 
 def main():
