commit 069c7959da77fcf1f7d7c645a071353d6096a1d9
Author: Jesus Alvarez <jeezusjr@gmail.com>
Date:   Fri Nov 15 02:49:01 2019 -0800

    Add timeout to jenkins_script csrf requests (#62850)

diff --git a/lib/ansible/modules/web_infrastructure/jenkins_script.py b/lib/ansible/modules/web_infrastructure/jenkins_script.py
index da598530fe..3c7f3c51a7 100644
--- a/lib/ansible/modules/web_infrastructure/jenkins_script.py
+++ b/lib/ansible/modules/web_infrastructure/jenkins_script.py
@@ -114,6 +114,7 @@ from ansible.module_utils._text import to_native
 def is_csrf_protection_enabled(module):
     resp, info = fetch_url(module,
                            module.params['url'] + '/api/json',
+                           timeout=module.params['timeout'],
                            method='GET')
     if info["status"] != 200:
         module.fail_json(msg="HTTP error " + str(info["status"]) + " " + info["msg"], output='')
@@ -126,6 +127,7 @@ def get_crumb(module, cookies):
     resp, info = fetch_url(module,
                            module.params['url'] + '/crumbIssuer/api/json',
                            method='GET',
+                           timeout=module.params['timeout'],
                            cookies=cookies)
     if info["status"] != 200:
         module.fail_json(msg="HTTP error " + str(info["status"]) + " " + info["msg"], output='')
