commit e1b7278265f11b8c77ceaf03a3dc5dee512c4325
Author: Stefhen Hovland <stefhen.hovland+github@gmail.com>
Date:   Fri Feb 14 00:12:29 2014 -0500

    Only display equals sign in summary for required options.

diff --git a/bin/ansible-doc b/bin/ansible-doc
index 96703d9bc1..a77fff8130 100755
--- a/bin/ansible-doc
+++ b/bin/ansible-doc
@@ -149,7 +149,12 @@ def get_snippet_text(doc):
     for o in sorted(doc['options'].keys()):
         opt = doc['options'][o]
         desc = tty_ify("".join(opt['description']))
-        s = o + "="
+
+        if opt.get('required', False):
+            s = o + "="
+        else:
+            s = o
+
         text.append("      %-20s   # %s" % (s, desc))
     text.append('')
 
