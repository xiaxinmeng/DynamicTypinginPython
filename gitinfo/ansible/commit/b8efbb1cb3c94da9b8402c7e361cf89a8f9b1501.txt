commit b8efbb1cb3c94da9b8402c7e361cf89a8f9b1501
Author: John Barker <john@johnrbarker.com>
Date:   Wed Apr 2 20:43:12 2014 +0100

    debconf correctly quote strings

diff --git a/library/system/debconf b/library/system/debconf
index 1dade71f8a..fded0b0e0b 100644
--- a/library/system/debconf
+++ b/library/system/debconf
@@ -107,7 +107,7 @@ def set_selection(module, pkg, question, vtype, value, unseen):
     data = ' '.join([ question, vtype, value ])
 
     setsel = module.get_bin_path('debconf-set-selections', True)
-    cmd = ["echo '%s %s' |" % (pipes.quote(pkg), pipes.quote(data)), setsel]
+    cmd = ["echo %s %s |" % (pipes.quote(pkg), pipes.quote(data)), setsel]
     if unseen:
         cmd.append('-u')
 
