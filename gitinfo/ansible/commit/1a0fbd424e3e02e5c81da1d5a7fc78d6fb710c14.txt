commit 1a0fbd424e3e02e5c81da1d5a7fc78d6fb710c14
Author: Abhijeet Kasurde <akasurde@redhat.com>
Date:   Sun Nov 12 03:09:28 2017 +0000

    Remove hypen from vmware powerstate (#32295)
    
    This fix removes hypen while comparing to current
    virtual machine's powerstate.
    
    Signed-off-by: Abhijeet Kasurde <akasurde@redhat.com>

diff --git a/lib/ansible/module_utils/vmware.py b/lib/ansible/module_utils/vmware.py
index baa5b06506..deaf1559a8 100644
--- a/lib/ansible/module_utils/vmware.py
+++ b/lib/ansible/module_utils/vmware.py
@@ -669,7 +669,7 @@ def set_vm_power_state(content, vm, state, force):
     requested states. force is forceful
     """
     facts = gather_vm_facts(content, vm)
-    expected_state = state.replace('_', '').lower()
+    expected_state = state.replace('_', '').replace('-', '').lower()
     current_state = facts['hw_power_status'].lower()
     result = dict(
         changed=False,
