commit b47add0f32a0a7deb6d674cc7edb713f53314250
Author: Skylar Saveland <skylar.saveland@chase.com>
Date:   Thu Oct 3 18:58:11 2013 -0700

    fixes 4365 - Mac with cisco vpn client, GenericBsdIfconfigNetwork get_default_interfaces fails with index out of range

diff --git a/library/system/setup b/library/system/setup
index a0901ec366..89d1ef9adc 100755
--- a/library/system/setup
+++ b/library/system/setup
@@ -1576,7 +1576,7 @@ class GenericBsdIfconfigNetwork(Network):
             for line in lines:
                 words = line.split()
                 # Collect output from route command
-                if len(words) > 0:
+                if len(words) > 1:
                     if words[0] == 'interface:':
                         interface[v]['interface'] = words[1]
                     if words[0] == 'gateway:':
