commit be8fa961cf8d7985c81a55720e6571bf19f5de5e
Author: Aaryn <10469251+arin-c@users.noreply.github.com>
Date:   Thu Dec 5 09:50:06 2019 +1300

    Fix RuntimeError in ec2_group_info (#65434)
    
    Modifying dictionary while iterating over it

diff --git a/lib/ansible/modules/cloud/amazon/ec2_group_info.py b/lib/ansible/modules/cloud/amazon/ec2_group_info.py
index cb6c7a6b99..4397bb35d8 100644
--- a/lib/ansible/modules/cloud/amazon/ec2_group_info.py
+++ b/lib/ansible/modules/cloud/amazon/ec2_group_info.py
@@ -140,7 +140,7 @@ def main():
 
     # Replace filter key underscores with dashes, for compatibility, except if we're dealing with tags
     sanitized_filters = module.params.get("filters")
-    for key in sanitized_filters:
+    for key in list(sanitized_filters):
         if not key.startswith("tag:"):
             sanitized_filters[key.replace("_", "-")] = sanitized_filters.pop(key)
 
