commit 676f0a6e7634b8010f6c1b893723d0d45cabfc27
Author: Tim Ruffles <oi@truffles.me.uk>
Date:   Wed Jun 25 14:00:01 2014 +0100

    grammar fix

diff --git a/library/cloud/docker_image b/library/cloud/docker_image
index 1727486f8a..e71878ced7 100644
--- a/library/cloud/docker_image
+++ b/library/cloud/docker_image
@@ -228,7 +228,7 @@ def main():
         if do_build:
             image_id = manager.build()
             if image_id:
-                msg = "Image builded: %s" % image_id
+                msg = "Image built: %s" % image_id
             else:
                 failed = True
                 msg = "Error: %s\nLog:%s" % (manager.error_msg, manager.get_log())
