commit 91a4b866b0eeb0af8d4cde6aeca1aa2b29eed2f7
Author: James Cammarata <jimi@sngx.net>
Date:   Mon Jul 20 15:40:08 2015 -0400

    Fix ssh pipelining

diff --git a/lib/ansible/plugins/connections/ssh.py b/lib/ansible/plugins/connections/ssh.py
index 66a13743de..d073b215da 100644
--- a/lib/ansible/plugins/connections/ssh.py
+++ b/lib/ansible/plugins/connections/ssh.py
@@ -42,13 +42,13 @@ from ansible.utils.debug import debug
 class Connection(ConnectionBase):
     ''' ssh based connections '''
 
+    has_pipelining = True
     become_methods = frozenset(C.BECOME_METHODS).difference(['runas'])
 
     def __init__(self, *args, **kwargs):
         # SSH connection specific init stuff
         self._common_args = []
         self.HASHED_KEY_MAGIC = "|1|"
-        self._has_pipelining = True
 
         # FIXME: move the lockfile locations to ActionBase?
         #fcntl.lockf(self.runner.process_lockfile, fcntl.LOCK_EX)
