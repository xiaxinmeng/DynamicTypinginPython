commit 7f6ab89b5b9ad4f84b19d1b14c2d03cb91e720bf
Author: Michael DeHaan <michael.dehaan@gmail.com>
Date:   Fri Sep 26 17:59:46 2014 -0400

    hacking/env-setup no longer needs to set library since modules appear as git submodules.

diff --git a/hacking/env-setup b/hacking/env-setup
index 6e4de1af72..4fed169097 100755
--- a/hacking/env-setup
+++ b/hacking/env-setup
@@ -21,8 +21,6 @@ PREFIX_MANPATH="$ANSIBLE_HOME/docs/man"
 
 [[ $PYTHONPATH != ${PREFIX_PYTHONPATH}* ]] && export PYTHONPATH=$PREFIX_PYTHONPATH:$PYTHONPATH
 [[ $PATH != ${PREFIX_PATH}* ]] && export PATH=$PREFIX_PATH:$PATH
-unset ANSIBLE_LIBRARY
-export ANSIBLE_LIBRARY="$ANSIBLE_HOME/library:`python $HACKING_DIR/get_library.py`"
 [[ $MANPATH != ${PREFIX_MANPATH}* ]] && export MANPATH=$PREFIX_MANPATH:$MANPATH
 
 # Print out values unless -q is set
@@ -33,7 +31,6 @@ if [ $# -eq 0 -o "$1" != "-q" ] ; then
     echo ""
     echo "PATH=$PATH"
     echo "PYTHONPATH=$PYTHONPATH"
-    echo "ANSIBLE_LIBRARY=$ANSIBLE_LIBRARY"
     echo "MANPATH=$MANPATH"
     echo ""
     
