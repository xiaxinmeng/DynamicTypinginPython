commit ac1948c04c11c035cedcefeb07e1ef4235e1b3a2
Author: James Cammarata <jimi@sngx.net>
Date:   Tue Apr 29 15:46:48 2014 -0500

    Minor tweak to rsync_path construction in synchronize module
    
    Fixes #7182

diff --git a/library/files/synchronize b/library/files/synchronize
index e4d789790b..0fea400320 100644
--- a/library/files/synchronize
+++ b/library/files/synchronize
@@ -300,7 +300,7 @@ def main():
                 'StrictHostKeyChecking=no')  # need ssh param
 
     if rsync_path:
-        cmd = cmd + " --rsync-path '%s'" %(rsync_path)
+        cmd = cmd + " --rsync-path=%s" % (rsync_path)
     if rsync_opts:
         cmd = cmd + " " +  " ".join(rsync_opts)
     changed_marker = '<<CHANGED>>'
