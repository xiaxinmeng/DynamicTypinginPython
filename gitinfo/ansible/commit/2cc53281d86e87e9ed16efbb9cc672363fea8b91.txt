commit 2cc53281d86e87e9ed16efbb9cc672363fea8b91
Author: fdupoux <fdupoux@users.sourceforge.net>
Date:   Sun May 31 12:38:45 2015 +0100

    Devices in the current_devs list must also be converted to absolute device paths so comparison with dev_list works

diff --git a/lib/ansible/modules/extras/system/lvg.py b/lib/ansible/modules/extras/system/lvg.py
index 955b94668d..3c6c5ef293 100644
--- a/lib/ansible/modules/extras/system/lvg.py
+++ b/lib/ansible/modules/extras/system/lvg.py
@@ -211,7 +211,7 @@ def main():
                     module.fail_json(msg="Refuse to remove non-empty volume group %s without force=yes"%(vg))
 
         ### resize VG
-        current_devs = [ pv['name'] for pv in pvs if pv['vg_name'] == vg ]
+        current_devs = [ os.path.realpath(pv['name']) for pv in pvs if pv['vg_name'] == vg ]
         devs_to_remove = list(set(current_devs) - set(dev_list))
         devs_to_add = list(set(dev_list) - set(current_devs))
 
