commit 8a6ef8c69a29bb6d8e1265be5dac7a9d80bc94f7
Author: Shaun Zinck <shaun.zinck@gmail.com>
Date:   Sun Mar 24 13:47:56 2013 -0500

    service: fix typo 'err' in SunOS service class

diff --git a/library/service b/library/service
index 3ac3f5c8f7..e179e04437 100644
--- a/library/service
+++ b/library/service
@@ -752,7 +752,7 @@ class SunOSService(Service):
         rc, stdout, stderr = self.execute_command("%s -l %s" % (self.svcs_cmd, self.name))
 
         if rc != 0:
-            if err:
+            if stderr:
                 self.module.fail_json(msg=stderr)
             else:
                 self.module.fail_json(msg=stdout)
