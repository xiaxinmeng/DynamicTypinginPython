commit d268471739c05bc067b505cd8e179c5e44fda4b3
Author: Ricardo Carrillo Cruz <ricardo.carrillo.cruz@gmail.com>
Date:   Wed Apr 5 19:11:12 2017 +0200

    Fix basic-login eapi tests for eos_banner (#23304)
    
    We should be passing the provider dict, not the transport.

diff --git a/test/integration/targets/eos_banner/tests/eapi/basic-login.yaml b/test/integration/targets/eos_banner/tests/eapi/basic-login.yaml
index c9cd17931e..b7cdb84ecc 100644
--- a/test/integration/targets/eos_banner/tests/eapi/basic-login.yaml
+++ b/test/integration/targets/eos_banner/tests/eapi/basic-login.yaml
@@ -5,7 +5,7 @@
     banner: login
     state: absent
     authorize: yes
-    transport: "{{ eapi }}"
+    provider: "{{ eapi }}"
 
 - name: Set login
   eos_banner:
@@ -16,7 +16,7 @@
       string
     state: present
     authorize: yes
-    transport: "{{ eapi }}"
+    provider: "{{ eapi }}"
   register: result
 
 - debug:
@@ -39,7 +39,7 @@
       string
     state: present
     authorize: yes
-    transport: "{{ eapi }}"
+    provider: "{{ eapi }}"
   register: result
 
 - assert:
