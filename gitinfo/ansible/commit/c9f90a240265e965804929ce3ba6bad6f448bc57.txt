commit c9f90a240265e965804929ce3ba6bad6f448bc57
Author: Rob White <wimnat@gmail.com>
Date:   Thu Jan 22 13:37:37 2015 +1100

    Convert symlinks specified in pvs to actual path

diff --git a/lib/ansible/modules/extras/system/lvg.py b/lib/ansible/modules/extras/system/lvg.py
index e568e9df67..295ee24e3c 100644
--- a/lib/ansible/modules/extras/system/lvg.py
+++ b/lib/ansible/modules/extras/system/lvg.py
@@ -135,7 +135,9 @@ def main():
     elif state == 'present':
         module.fail_json(msg="No physical volumes given.")
 
-
+    # LVM always uses real paths not symlinks so replace symlinks with actual path
+    for idx, dev in enumerate(dev_list):
+        dev_list[idx] = os.path.realpath(dev)
 
     if state=='present':
         ### check given devices
