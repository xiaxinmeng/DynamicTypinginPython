commit 95604573ee14309017571d615f8acd0eed943e31
Author: Brian Coca <bcoca@ansible.com>
Date:   Thu Nov 5 14:26:23 2015 -0500

    improved the message when there is no module path override

diff --git a/lib/ansible/cli/__init__.py b/lib/ansible/cli/__init__.py
index f5a81f684c..6045b5171a 100644
--- a/lib/ansible/cli/__init__.py
+++ b/lib/ansible/cli/__init__.py
@@ -346,7 +346,11 @@ class CLI(object):
         if gitinfo:
             result = result + " {0}".format(gitinfo)
         result += "\n  config file = %s" % C.CONFIG_FILE
-        result = result + "\n  configured module search path = %s" % C.DEFAULT_MODULE_PATH
+        if C.DEFAULT_MODULE_PATH is None:
+            cpath = "Default w/o overrides"
+        else:
+            cpath = C.DEFAULT_MODULE_PATH
+        result = result + "\n  configured module search path = %s" % cpath
         return result
 
     @staticmethod
