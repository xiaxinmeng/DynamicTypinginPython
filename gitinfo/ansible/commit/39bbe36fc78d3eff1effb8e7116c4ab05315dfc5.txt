commit 39bbe36fc78d3eff1effb8e7116c4ab05315dfc5
Author: Ton Kersten <github@tonkersten.com>
Date:   Sun Oct 26 15:46:08 2014 +0100

    Ignore compiled Python for documentation.
    
    When running `ansible-doc --list` a lot of errors are
    generated when `ansible-doc` tries to find documentation
    strings in `.pyc` files.

diff --git a/bin/ansible-doc b/bin/ansible-doc
index d949c8e537..d5143f33a1 100755
--- a/bin/ansible-doc
+++ b/bin/ansible-doc
@@ -33,7 +33,7 @@ import traceback
 
 MODULEDIR = C.DEFAULT_MODULE_PATH
 
-BLACKLIST_EXTS = ('.swp', '.bak', '~', '.rpm')
+BLACKLIST_EXTS = ('.pyc', '.swp', '.bak', '~', '.rpm')
 
 _ITALIC = re.compile(r"I\(([^)]+)\)")
 _BOLD   = re.compile(r"B\(([^)]+)\)")
