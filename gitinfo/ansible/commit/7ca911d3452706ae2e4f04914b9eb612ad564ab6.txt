commit 7ca911d3452706ae2e4f04914b9eb612ad564ab6
Author: Brian Maddy <brian@brianmaddy.com>
Date:   Fri Sep 16 17:48:21 2016 -0500

    typo in stat.executable (was stat.excutable) (#4886)
    
    I didn't actually run this because it's so simple, but it seems correct.

diff --git a/lib/ansible/modules/files/stat.py b/lib/ansible/modules/files/stat.py
index 38ede21164..48979d7379 100644
--- a/lib/ansible/modules/files/stat.py
+++ b/lib/ansible/modules/files/stat.py
@@ -369,7 +369,7 @@ def format_output(module, path, st, follow, get_md5, get_checksum,
         isgid=bool(mode & stat.S_ISGID),
         readable=os.access(path, os.R_OK),
         writeable=os.access(path, os.W_OK),
-        excutable=os.access(path, os.X_OK),
+        executable=os.access(path, os.X_OK),
     )
 
     if stat.S_ISLNK(mode):
