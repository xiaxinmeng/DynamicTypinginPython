commit 7b9f16becb6940b5ebea5d07cf962e0903185a3c
Author: Brian Coca <bcoca@ansible.com>
Date:   Wed Mar 1 13:12:24 2017 -0500

    minor display changes in metadata

diff --git a/lib/ansible/cli/doc.py b/lib/ansible/cli/doc.py
index efc63e9fe9..b08b034547 100644
--- a/lib/ansible/cli/doc.py
+++ b/lib/ansible/cli/doc.py
@@ -341,12 +341,12 @@ class DocCLI(CLI):
         text.append('')
 
         if doc['metadata'] and isinstance(doc['metadata'], dict):
-            text.append("Metadata:")
+            text.append("METADATA:")
             for k in doc['metadata']:
                 if isinstance(k, list):
                     text.append("\t%s: %s" % (k.capitalize(), ", ".join(doc['metadata'][k])))
                 else:
                     text.append("\t%s: %s" % (k.capitalize(), doc['metadata'][k]))
-            text.append("")
+            text.append('')
 
         return "\n".join(text)
