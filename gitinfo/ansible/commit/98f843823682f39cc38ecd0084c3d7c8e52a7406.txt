commit 98f843823682f39cc38ecd0084c3d7c8e52a7406
Author: James Cammarata <jimi@sngx.net>
Date:   Tue Jun 24 12:28:43 2014 -0500

    Updating the other classes in the group module to match #7249

diff --git a/library/system/group b/library/system/group
index 6b401d077e..a614277884 100644
--- a/library/system/group
+++ b/library/system/group
@@ -206,7 +206,7 @@ class AIX(Group):
         if len(cmd) == 1:
             return (None, '', '')
         if self.module.check_mode:
-            return (True, '', '')
+            return (0, '', '')
         cmd.append(self.name)
         return self.execute_command(cmd)
 
@@ -245,7 +245,7 @@ class FreeBsdGroup(Group):
         # modify the group if cmd will do anything
         if cmd_len != len(cmd):
             if self.module.check_mode:
-                return (True, '', '')
+                return (0, '', '')
             return self.execute_command(cmd)
         return (None, '', '')
 
@@ -287,7 +287,7 @@ class OpenBsdGroup(Group):
         if len(cmd) == 1:
             return (None, '', '')
         if self.module.check_mode:
-            return (True, '', '')
+            return (0, '', '')
         cmd.append(self.name)
         return self.execute_command(cmd)
 
@@ -329,7 +329,7 @@ class NetBsdGroup(Group):
         if len(cmd) == 1:
             return (None, '', '')
         if self.module.check_mode:
-            return (True, '', '')
+            return (0, '', '')
         cmd.append(self.name)
         return self.execute_command(cmd)
 
