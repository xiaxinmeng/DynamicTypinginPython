commit 785e888ea0572d1d3e0d99079751d94214d0fdb5
Author: Rene Moser <mail@renemoser.net>
Date:   Fri Feb 2 13:52:28 2018 +0100

    doc: fix loop examples (#34991)

diff --git a/docs/docsite/rst/playbooks_loops.rst b/docs/docsite/rst/playbooks_loops.rst
index 9473c91935..f17a87b6e7 100644
--- a/docs/docsite/rst/playbooks_loops.rst
+++ b/docs/docsite/rst/playbooks_loops.rst
@@ -195,14 +195,12 @@ One can use a regular ``loop`` with the ``ansible_play_batch`` or ``groups`` var
     # show all the hosts in the inventory
     - debug:
         msg: "{{ item }}"
-      loop:
-        - "{{ groups['all'] }}"
+      loop: "{{ groups['all'] }}"
 
     # show all the hosts in the current play
     - debug:
         msg: "{{ item }}"
-      loop:
-        - "{{ ansible_play_batch }}"
+      loop: "{{ ansible_play_batch }}"
 
 There is also a specific lookup plugin ``inventory_hostnames`` that can be used like this::
 
