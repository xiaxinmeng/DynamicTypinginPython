commit 1426a99cc061b42c378072c351c5120335fb65ab
Author: Mike Mars <codemeup@outlook.com>
Date:   Sat Jun 4 16:01:18 2016 -0700

    data type fix on format string

diff --git a/lib/ansible/modules/utilities/helper/accelerate.py b/lib/ansible/modules/utilities/helper/accelerate.py
index 3de8c794c0..27f714ad23 100644
--- a/lib/ansible/modules/utilities/helper/accelerate.py
+++ b/lib/ansible/modules/utilities/helper/accelerate.py
@@ -170,7 +170,7 @@ def daemonize_self(module, password, port, minutes, pid_file):
             module.exit_json(msg="daemonized accelerate on port %s for %s minutes with pid %s" % (port, minutes, str(pid)))
     except OSError:
         e       = get_exception()
-        message = "fork #1 failed: %s (%s)" % (e.errno, e.strerror)
+        message = "fork #1 failed: %d (%s)" % (e.errno, e.strerror)
         module.fail_json(message)
 
     # decouple from parent environment
@@ -190,7 +190,7 @@ def daemonize_self(module, password, port, minutes, pid_file):
             sys.exit(0)
     except OSError:
         e       = get_exception()
-        message = 'fork #2 failed: %s (%s)' % (e.errno, e.strerror)
+        message = 'fork #2 failed: %d (%s)' % (e.errno, e.strerror)
         module.fail_json(message)
 
     dev_null = file('/dev/null','rw')
