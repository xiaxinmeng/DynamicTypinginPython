commit 118ccc68c9275e75beb4135059790de622a2a1bf
Author: Dag Wieers <dag@wieers.com>
Date:   Wed Nov 21 16:11:20 2012 +0100

    Make the default an empty string
    
    Seems a bit strange I have to set a default in two locations.

diff --git a/library/service b/library/service
index 84e7b15255..911494d939 100644
--- a/library/service
+++ b/library/service
@@ -265,7 +265,7 @@ def main():
             state = dict(choices=['running', 'started', 'stopped', 'restarted', 'reloaded']),
             pattern = dict(required=False, default=None),
             enabled = dict(choices=BOOLEANS),
-            arguments = dict(aliases=['args']),
+            arguments = dict(aliases=['args'], default=''),
         )
     )
 
