commit 4ba7158282f148c90c72f824d6ebcd1a9953b580
Author: James Cammarata <jimi@sngx.net>
Date:   Thu Dec 17 16:33:23 2015 -0500

    Fixing a mistake from tweaking list stuff too much
    
    Use the action only if the task name is not set

diff --git a/lib/ansible/cli/playbook.py b/lib/ansible/cli/playbook.py
index d307abdfcc..dfd06b1920 100644
--- a/lib/ansible/cli/playbook.py
+++ b/lib/ansible/cli/playbook.py
@@ -187,7 +187,10 @@ class PlaybookCLI(CLI):
                                 if self.options.listtasks:
                                     cur_tags = list(mytags.union(set(task.tags)))
                                     cur_tags.sort()
-                                    taskmsg += "      %s" % task.action
+                                    if task.name:
+                                        taskmsg += "      %s" % task.get_name()
+                                    else:
+                                        taskmsg += "      %s" % task.action
                                     taskmsg += "\tTAGS: [%s]\n" % ', '.join(cur_tags)
 
                         if self.options.listtags:
