commit db8719cf8bcb51fa76bb0111a75f2f2c9e494b87
Author: Michael Scherer <misc@redhat.com>
Date:   Thu Oct 20 00:32:57 2016 +0200

    Add support for NetBSD
    
    While I only checked on NetBSD 7.0, it seems to use the same
    rc system as FreeBSD so it should be working for now.

diff --git a/lib/ansible/modules/system/hostname.py b/lib/ansible/modules/system/hostname.py
index c6432428fc..7af045b5a6 100644
--- a/lib/ansible/modules/system/hostname.py
+++ b/lib/ansible/modules/system/hostname.py
@@ -727,6 +727,11 @@ class FreeBSDHostname(Hostname):
     distribution = None
     strategy_class = FreeBSDStrategy
 
+class NetBSDHostname(Hostname):
+    platform = 'NetBSD'
+    distribution = None
+    strategy_class = FreeBSDStrategy
+
 
 # ===========================================
 
