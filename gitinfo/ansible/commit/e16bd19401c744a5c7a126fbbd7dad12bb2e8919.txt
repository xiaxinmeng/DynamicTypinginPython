commit e16bd19401c744a5c7a126fbbd7dad12bb2e8919
Author: Andrea Scarpino <me@andreascarpino.it>
Date:   Thu Feb 11 16:43:12 2016 +0100

    win_unzip: overwrite any existing file

diff --git a/lib/ansible/modules/extras/windows/win_unzip.ps1 b/lib/ansible/modules/extras/windows/win_unzip.ps1
index 0e5485dddb..f547c0081f 100644
--- a/lib/ansible/modules/extras/windows/win_unzip.ps1
+++ b/lib/ansible/modules/extras/windows/win_unzip.ps1
@@ -61,7 +61,8 @@ If ($ext -eq ".zip" -And $recurse -eq $false) {
         $shell = New-Object -ComObject Shell.Application
         $zipPkg = $shell.NameSpace($src)
         $destPath = $shell.NameSpace($dest)
-        $destPath.CopyHere($zipPkg.Items())
+        # 20 means do not display any dialog (4) and overwrite any file (16)
+        $destPath.CopyHere($zipPkg.Items(), 20)
         $result.changed = $true
     }
     Catch {
