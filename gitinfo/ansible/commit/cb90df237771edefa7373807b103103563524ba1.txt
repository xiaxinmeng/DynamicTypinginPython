commit cb90df237771edefa7373807b103103563524ba1
Author: Michael DeHaan <michael.dehaan@gmail.com>
Date:   Sat Feb 25 17:22:48 2012 -0500

    Allow multiple patterns to specified, just like Func did.

diff --git a/lib/ansible/runner.py b/lib/ansible/runner.py
index b80f97a22d..414a8c6600 100755
--- a/lib/ansible/runner.py
+++ b/lib/ansible/runner.py
@@ -83,8 +83,10 @@ class Runner(object):
             return False
         if not pattern:
             pattern = self.pattern
-        if fnmatch.fnmatch(host_name, pattern):
-            return True
+        subpatterns = pattern.split(";")
+        for subpattern in subpatterns:
+            if fnmatch.fnmatch(host_name, subpattern):
+                return True
         return False
 
     def _connect(self, host):
