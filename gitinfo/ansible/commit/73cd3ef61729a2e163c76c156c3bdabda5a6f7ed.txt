commit 73cd3ef61729a2e163c76c156c3bdabda5a6f7ed
Author: Daniel Hokka Zakrisson <daniel@hozac.com>
Date:   Thu Oct 25 14:26:37 2012 +0200

    Only strip trailing newlines from output, preserving other space

diff --git a/library/command b/library/command
index 31b253cda5..d7b18258a6 100755
--- a/library/command
+++ b/library/command
@@ -111,8 +111,8 @@ def main():
 
     module.exit_json(
         cmd     = args,
-        stdout  = out.strip(),
-        stderr  = err.strip(),
+        stdout  = out.rstrip("\r\n"),
+        stderr  = err.rstrip("\r\n"),
         rc      = cmd.returncode,
         start   = str(startd),
         end     = str(endd),
