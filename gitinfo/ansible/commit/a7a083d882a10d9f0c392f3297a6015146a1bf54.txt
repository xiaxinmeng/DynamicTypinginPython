commit a7a083d882a10d9f0c392f3297a6015146a1bf54
Author: Seth Vidal <skvidal@fedoraproject.org>
Date:   Mon Mar 11 09:48:32 2013 -0400

    when --step isn't specified make sure to print the banner

diff --git a/lib/ansible/callbacks.py b/lib/ansible/callbacks.py
index 90af6ab555..38f7838aaf 100644
--- a/lib/ansible/callbacks.py
+++ b/lib/ansible/callbacks.py
@@ -476,7 +476,8 @@ class PlaybookCallbacks(object):
                 print banner(msg)
             else:
                 self.skip_task = True
-        
+        else:
+            print banner(msg)                
         
         call_callback_module('playbook_on_task_start', name, is_conditional)
 
