commit 82cc382c6a4257b18713cf96863a7760a0eaec32
Author: Maxim Burgerhout <maxim@wzzrd.com>
Date:   Mon Oct 29 16:23:00 2012 +0100

    More useful error message for if not HAVE_SELINUX

diff --git a/library/seboolean b/library/seboolean
index 799aace89c..d9a3222ca0 100755
--- a/library/seboolean
+++ b/library/seboolean
@@ -164,7 +164,7 @@ def main():
     )
 
     if not HAVE_SELINUX:
-        module.fail_json(msg="SELinux not supported on this host.")
+        module.fail_json(msg="This module requires libselinux-python support")
 
     if not HAVE_SEMANAGE:
         module.fail_json(msg="This module requires libsemanage-python support")
