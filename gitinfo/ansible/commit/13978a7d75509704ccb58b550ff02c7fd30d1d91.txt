commit 13978a7d75509704ccb58b550ff02c7fd30d1d91
Author: Brian Coca <bcoca@ansible.com>
Date:   Thu Apr 30 21:27:29 2015 -0400

    fixed typo

diff --git a/v2/ansible/cli/playbook.py b/v2/ansible/cli/playbook.py
index c2b881d2b6..eb60bacbd2 100644
--- a/v2/ansible/cli/playbook.py
+++ b/v2/ansible/cli/playbook.py
@@ -118,7 +118,7 @@ class PlaybookCLI(CLI):
         only_tags = self.options.tags.split(",")
         skip_tags = self.options.skip_tags
         if self.options.skip_tags is not None:
-            skip_tags = self.ptions.skip_tags.split(",")
+            skip_tags = self.options.skip_tags.split(",")
 
         # initial error check, to make sure all specified playbooks are accessible
         # before we start running anything through the playbook executor
