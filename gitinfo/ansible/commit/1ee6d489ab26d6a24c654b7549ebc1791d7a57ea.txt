commit 1ee6d489ab26d6a24c654b7549ebc1791d7a57ea
Author: James Cammarata <jimi@sngx.net>
Date:   Thu Mar 17 14:19:28 2016 -0400

    Make sure tuples returned by _get_item() are handled correctly in formatting
    
    Fixes #14800

diff --git a/lib/ansible/plugins/callback/default.py b/lib/ansible/plugins/callback/default.py
index bbce48025a..99b1762a25 100644
--- a/lib/ansible/plugins/callback/default.py
+++ b/lib/ansible/plugins/callback/default.py
@@ -175,7 +175,7 @@ class CallbackModule(CallbackBase):
         else:
             msg += ": [%s]" % result._host.get_name()
 
-        msg += " => (item=%s)" % (self._get_item(result._result))
+        msg += " => (item=%s)" % (self._get_item(result._result),)
 
         if (self._display.verbosity > 0 or '_ansible_verbose_always' in result._result) and not '_ansible_verbose_override' in result._result:
             msg += " => %s" % self._dump_results(result._result)
