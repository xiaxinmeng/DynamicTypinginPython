commit 3568f1cd786ae25595542c625ef576f55f93a980
Author: Jérémie Astori <jeremie@astori.fr>
Date:   Thu Aug 20 22:40:15 2015 +0000

    Make sure permission-less entries are accepted when state=absent
    
    Also, remove that try condition as, at that stage, no permissions with
    other than 2 or 3 fields are sent to the function.

diff --git a/lib/ansible/modules/files/acl.py b/lib/ansible/modules/files/acl.py
index 2bd27f621f..58550c1912 100644
--- a/lib/ansible/modules/files/acl.py
+++ b/lib/ansible/modules/files/acl.py
@@ -127,12 +127,10 @@ def split_entry(entry):
     ''' splits entry and ensures normalized return'''
 
     a = entry.split(':')
-    a.reverse()
-    try:
-        p, e, t = a
-    except ValueError, e:
-        print "wtf?? %s => %s" % (entry, a)
-        raise e
+    if len(a) == 2:
+        a.append(None)
+
+    t, e, p = a
 
     if t.startswith("u"):
         t = "user"
