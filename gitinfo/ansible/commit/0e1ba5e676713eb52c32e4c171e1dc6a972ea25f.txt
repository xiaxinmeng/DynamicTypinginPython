commit 0e1ba5e676713eb52c32e4c171e1dc6a972ea25f
Author: Michael DeHaan <michael.dehaan@gmail.com>
Date:   Tue Feb 26 20:23:12 2013 -0500

    Prevent SVN checkouts from failing due self signed certs.

diff --git a/library/subversion b/library/subversion
index 2f86f91035..66d3917709 100644
--- a/library/subversion
+++ b/library/subversion
@@ -79,7 +79,7 @@ class Subversion(object):
         self.password = password
 
     def _exec(self, args):
-        bits = ["svn --non-interactive --no-auth-cache", ]
+        bits = ["svn --non-interactive --trust-server-cert --no-auth-cache", ]
         if self.username:
             bits.append("--username '%s'" % self.username)
         if self.password:
