commit fcb6f136cde38dd562f89cf079abb2450b0d4622
Author: Paweł Rozlach <vespian@users.noreply.github.com>
Date:   Mon Feb 18 18:40:51 2019 +0100

    flatpak_remote: Handle empty output in remote_exists (#52010)
    
    `flatpak remote-list -d` can return an empty output on Fedora 29 (version 1.2.0).

diff --git a/lib/ansible/modules/packaging/os/flatpak_remote.py b/lib/ansible/modules/packaging/os/flatpak_remote.py
index e77c41eabb..db208f1bd9 100644
--- a/lib/ansible/modules/packaging/os/flatpak_remote.py
+++ b/lib/ansible/modules/packaging/os/flatpak_remote.py
@@ -169,6 +169,8 @@ def remote_exists(module, binary, name, method):
     output = _flatpak_command(module, False, command)
     for line in output.splitlines():
         listed_remote = line.split()
+        if len(listed_remote) == 0:
+            continue
         if listed_remote[0] == to_native(name):
             return True
     return False
