commit aafe7f5b37ac23c345e2ed6ee2c897b0badb29b3
Author: James Tanner <tanner.jc@gmail.com>
Date:   Mon Nov 11 23:23:56 2013 -0500

    Fixes #4877 set type to bool for cache parameter in apt module

diff --git a/library/packaging/pkgng b/library/packaging/pkgng
index af1fdf93ef..dbae01bbb1 100644
--- a/library/packaging/pkgng
+++ b/library/packaging/pkgng
@@ -139,7 +139,7 @@ def main():
             argument_spec       = dict(
                 state           = dict(default="present", choices=["present","absent"]),
                 name            = dict(aliases=["pkg"], required=True),
-                cached          = dict(default="no", required=False, choices=["yes","no"]),
+                cached          = dict(default=False, type='bool'),
                 pkgsite         = dict(default="", required=False)),
             supports_check_mode = True)
 
