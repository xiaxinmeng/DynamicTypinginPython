commit 993e0f057e84cc0c8357fcb3b6cf0e48f077f258
Author: James Tanner <tanner.jc@gmail.com>
Date:   Thu Dec 19 14:19:14 2013 -0500

    Allow for no role dependencies in role metadata

diff --git a/lib/ansible/playbook/play.py b/lib/ansible/playbook/play.py
index b42b3dc6f2..a2fdc643f2 100644
--- a/lib/ansible/playbook/play.py
+++ b/lib/ansible/playbook/play.py
@@ -203,6 +203,8 @@ class Play(object):
                 data = utils.parse_yaml_from_file(meta)
                 if data:
                     dependencies = data.get('dependencies',[])
+                    if dependencies is None:
+                        dependencies = []
                     for dep in dependencies:
                         allow_dupes = False
                         (dep_path,dep_vars) = self._get_role_path(dep)
