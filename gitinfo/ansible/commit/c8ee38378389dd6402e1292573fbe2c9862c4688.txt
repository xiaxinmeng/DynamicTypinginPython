commit c8ee38378389dd6402e1292573fbe2c9862c4688
Author: Felix Fontein <felix@fontein.de>
Date:   Thu Aug 23 08:16:48 2018 +0200

    Clarify usage of env_file (#42211). (#44535)

diff --git a/lib/ansible/modules/cloud/docker/docker_container.py b/lib/ansible/modules/cloud/docker/docker_container.py
index 3c51c88904..b9d199ee32 100644
--- a/lib/ansible/modules/cloud/docker/docker_container.py
+++ b/lib/ansible/modules/cloud/docker/docker_container.py
@@ -98,7 +98,7 @@ options:
   env_file:
     version_added: "2.2"
     description:
-      - Path to a file containing environment variables I(FOO=BAR).
+      - Path to a file, present on the target, containing environment variables I(FOO=BAR).
       - If variable also present in C(env), then C(env) value will override.
   entrypoint:
     description:
@@ -574,6 +574,12 @@ EXAMPLES = '''
     name: sleepy
     purge_networks: yes
 
+- name: Start a container and use an env file
+  docker_container:
+    name: agent
+    image: jenkinsci/ssh-slave
+    env_file: /var/tmp/jenkins/agent.env
+
 - name: Create a container with limited capabilities
   docker_container:
     name: sleepy
