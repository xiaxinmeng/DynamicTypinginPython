commit dd932c9acebe7a0bb72ee7abe830e2fd0b4717e4
Author: Nathaniel Case <this.is@nathanielca.se>
Date:   Fri Jun 22 14:03:48 2018 -0400

    Make validate_certs togglable for httpapi (#41538)
    
    * Make validate_certs togglable for httpapi
    
    * Add `version_added`

diff --git a/lib/ansible/plugins/connection/httpapi.py b/lib/ansible/plugins/connection/httpapi.py
index 853a4d0a4a..a029580d6d 100644
--- a/lib/ansible/plugins/connection/httpapi.py
+++ b/lib/ansible/plugins/connection/httpapi.py
@@ -66,6 +66,13 @@ options:
     default: False
     vars:
       - name: ansible_httpapi_use_ssl
+  validate_certs:
+    version_added: '2.7'
+    description:
+      - Whether to validate SSL certificates
+    default: True
+    vars:
+      - name: ansible_httpapi_validate_certs
   timeout:
     type: int
     description:
@@ -296,7 +303,7 @@ class Connection(ConnectionBase):
         '''
         url_kwargs = dict(
             url_username=self.get_option('remote_user'), url_password=self.get_option('password'),
-            timeout=self.get_option('timeout'),
+            timeout=self.get_option('timeout'), validate_certs=self.get_option('validate_certs'),
         )
         url_kwargs.update(kwargs)
 
