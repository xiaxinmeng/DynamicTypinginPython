commit 58d59c180dbfcb22b12612517183633bdfa462bb
Author: madema <martin.adema@tiscali.nl>
Date:   Thu May 16 14:30:42 2013 +0200

    -o and -r option don't exist on AIX

diff --git a/library/system/user b/library/system/user
index 9cf665965c..1fa114f8a8 100644
--- a/library/system/user
+++ b/library/system/user
@@ -894,9 +894,6 @@ class AIX(User):
             cmd.append('-u')
             cmd.append(self.uid)
 
-        if self.non_unique:
-            cmd.append('-o')
-
         if self.group is not None:
             if not self.group_exists(self.group):
                 self.module.fail_json(msg="Group %s does not exist" % self.group)
@@ -925,9 +922,6 @@ class AIX(User):
         if self.createhome:
             cmd.append('-m')
 
-        if self.system:
-            cmd.append('-r')
-
         cmd.append(self.name)
         (rc, out, err) = self.execute_command(cmd)
 
@@ -950,9 +944,6 @@ class AIX(User):
             cmd.append('-u')
             cmd.append(self.uid)
 
-        if self.non_unique:
-            cmd.append('-o')
-
         if self.group is not None:
             if not self.group_exists(self.group):
                 self.module.fail_json(msg="Group %s does not exist" % self.group)
