commit 9541b47b5203403f81b14a595faed54c026a148d
Author: Tim Bielawa <tbielawa@redhat.com>
Date:   Mon Oct 8 12:25:09 2012 -0400

    Keep the man3 directory. 'make docs' breaks without it.

diff --git a/Makefile b/Makefile
index 09272705ec..132152bc35 100644
--- a/Makefile
+++ b/Makefile
@@ -96,9 +96,10 @@ clean:
 	@echo "Cleaning up editor backup files"
 	find . -type f \( -name "*~" -or -name "#*" \) -delete
 	find . -type f \( -name "*.swp" \) -delete
-	@echo "Cleaning up asciidoc to man transformations and results"
+	@echo "Cleaning up manpage stuff"
 	find ./docs/man -type f -name "*.xml" -delete
 	find ./docs/man -type f -name "*.asciidoc" -delete
+	find ./docs/man/man3 -type f -name "*.3" -delete
 	@echo "Cleaning up output from test runs"
 	rm -rf test/test_data
 	@echo "Cleaning up RPM building stuff"
diff --git a/docs/man/man3/.gitdir b/docs/man/man3/.gitdir
new file mode 100644
index 0000000000..e69de29bb2
