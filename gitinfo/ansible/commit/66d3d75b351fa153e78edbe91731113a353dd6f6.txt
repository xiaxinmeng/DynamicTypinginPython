commit 66d3d75b351fa153e78edbe91731113a353dd6f6
Author: Matt Ferrante <mferrante@dataxu.com>
Date:   Mon Jul 14 12:52:33 2014 -0400

    Added setting of the health_check options on an existing ASG

diff --git a/library/cloud/ec2_asg b/library/cloud/ec2_asg
index 94cab19733..162d22fda1 100755
--- a/library/cloud/ec2_asg
+++ b/library/cloud/ec2_asg
@@ -120,7 +120,8 @@ except ImportError:
     sys.exit(1)
 
 ASG_ATTRIBUTES = ('launch_config_name', 'max_size', 'min_size', 'desired_capacity',
-                     'vpc_zone_identifier', 'availability_zones')
+                     'vpc_zone_identifier', 'availability_zones',
+                     'health_check_period', 'health_check_type')
 
 def enforce_required_arguments(module):
     ''' As many arguments are not required for autoscale group deletion
