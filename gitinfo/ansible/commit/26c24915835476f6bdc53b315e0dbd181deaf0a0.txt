commit 26c24915835476f6bdc53b315e0dbd181deaf0a0
Author: Atlas Health <github@atlashealth.com>
Date:   Wed May 21 07:18:30 2014 -0700

    Stopped instances can also be terminated

diff --git a/library/cloud/ec2 b/library/cloud/ec2
index 51773768d8..ac1f4012f4 100644
--- a/library/cloud/ec2
+++ b/library/cloud/ec2
@@ -990,7 +990,7 @@ def terminate_instances(module, ec2, instance_ids):
     terminated_instance_ids = []
     for res in ec2.get_all_instances(instance_ids):
         for inst in res.instances:
-            if inst.state == 'running':
+            if inst.state == 'running' or inst.state == 'stopped':
                 terminated_instance_ids.append(inst.id)
                 instance_dict_array.append(get_instance_info(inst))
                 try:
