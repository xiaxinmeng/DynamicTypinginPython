commit 2218e763f8025b2158e6bd72f032af82f5d5c421
Author: Matt Martz <matt@sivel.net>
Date:   Wed Nov 18 16:18:21 2015 -0800

    Get the index of the tab, not the line itself

diff --git a/ansible_testing/modules.py b/ansible_testing/modules.py
index 4bb86858ee..34d409d63a 100644
--- a/ansible_testing/modules.py
+++ b/ansible_testing/modules.py
@@ -177,7 +177,7 @@ class ModuleValidator(Validator):
             indent = INDENT_REGEX.search(line)
             for i in indent.groups():
                 if '\t' in i:
-                    index = line.index(i)
+                    index = line.index('\t')
                     self.errors.append('indentation contains tabs. line %d '
                                        'column %d' % (line_no + 1, index))
 
