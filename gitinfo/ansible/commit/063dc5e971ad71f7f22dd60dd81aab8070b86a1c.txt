commit 063dc5e971ad71f7f22dd60dd81aab8070b86a1c
Author: Harsha Yalamanchili <harsha@thought-matrix.com>
Date:   Wed Mar 5 14:04:36 2014 -0800

    Update debian install documentation to include dependency resolution

diff --git a/packaging/debian/README.md b/packaging/debian/README.md
index bbca98cdb8..b328a07b64 100644
--- a/packaging/debian/README.md
+++ b/packaging/debian/README.md
@@ -13,7 +13,7 @@ The debian package file will be placed in the `../` directory. This can then be
 
 Note that `dpkg -i` does not resolve dependencies.
 
-To install the Ansbiel DEB package and resolve redepdencies:
+To install the Ansible DEB package and resolve redepdencies:
 
     sudo dpkg -i <package-file>
     sudo apt-get -fy install
\ No newline at end of file
