commit a2650cbe0512cf9b17230fefed4347d82880216e
Author: Nathaniel Case <this.is@nathanielca.se>
Date:   Tue Dec 12 10:06:06 2017 -0500

    Fix last eos issue in net_* tests (#33726)
    
    This should finally fix it.

diff --git a/lib/ansible/modules/network/eos/eos_vrf.py b/lib/ansible/modules/network/eos/eos_vrf.py
index 6c4406b753..5e73ce306f 100644
--- a/lib/ansible/modules/network/eos/eos_vrf.py
+++ b/lib/ansible/modules/network/eos/eos_vrf.py
@@ -237,8 +237,10 @@ def check_declarative_intent_params(want, module):
             for i in w['interfaces']:
                 obj_in_have = search_obj_in_list(w['name'], have)
 
-                if obj_in_have and i not in obj_in_have.get('interfaces', []):
-                    module.fail_json(msg="Interface %s not configured on vrf %s" % (i, w['name']))
+                if obj_in_have:
+                    interfaces = obj_in_have.get('interfaces')
+                    if interfaces is not None and i not in interfaces:
+                        module.fail_json(msg="Interface %s not configured on vrf %s" % (i, w['name']))
 
 
 def main():
