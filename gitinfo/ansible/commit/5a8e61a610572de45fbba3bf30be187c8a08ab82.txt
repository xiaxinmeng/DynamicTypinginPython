commit 5a8e61a610572de45fbba3bf30be187c8a08ab82
Author: Michael DeHaan <michael.dehaan@gmail.com>
Date:   Thu May 31 22:14:33 2012 -0400

    Fix parsing error in file module when returning errors.

diff --git a/library/file b/library/file
index 2872d3879c..4e52cf39c8 100755
--- a/library/file
+++ b/library/file
@@ -33,7 +33,7 @@ except ImportError:
     HAVE_SELINUX=False
 
 def dump_kv(vars):
-    return " ".join("%s=%s" % (k,v) for (k,v) in vars.items())
+    return " ".join("%s='%s'" % (k,v) for (k,v) in vars.items())
 
 def exit_kv(rc=0, **kwargs):
     if 'path' in kwargs:
