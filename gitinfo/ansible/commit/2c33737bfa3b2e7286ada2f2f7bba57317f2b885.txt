commit 2c33737bfa3b2e7286ada2f2f7bba57317f2b885
Author: James Tanner <tanner.jc@gmail.com>
Date:   Fri Nov 15 10:13:19 2013 -0500

    Fix docstring and remove redundant parameter value in portinstall module

diff --git a/library/packaging/portinstall b/library/packaging/portinstall
index 6a20c394ed..4e27cdf12a 100644
--- a/library/packaging/portinstall
+++ b/library/packaging/portinstall
@@ -44,7 +44,6 @@ options:
         required: false
         default: yes
 author: berenddeboer
-notes:
 '''
 
 EXAMPLES = '''
@@ -191,7 +190,7 @@ def main():
             argument_spec    = dict(
                 state        = dict(default="present", choices=["present","absent"]),
                 name         = dict(aliases=["pkg"], required=True),
-                use_packages = dict(type='bool', choices=BOOLEANS, default='yes')))
+                use_packages = dict(type='bool', default='yes')))
 
     p = module.params
 
