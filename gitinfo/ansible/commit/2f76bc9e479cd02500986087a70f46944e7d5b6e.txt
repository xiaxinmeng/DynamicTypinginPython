commit 2f76bc9e479cd02500986087a70f46944e7d5b6e
Author: Jan-Piet Mens <jpmens@gmail.com>
Date:   Tue Apr 30 08:31:39 2013 +0200

    ansible-doc: skip directories (new library/ format)

diff --git a/bin/ansible-doc b/bin/ansible-doc
index d3cd614d10..5c7d8742be 100755
--- a/bin/ansible-doc
+++ b/bin/ansible-doc
@@ -155,6 +155,8 @@ def main():
                 continue
 
             filename = utils.plugins.module_finder.find_plugin(module)
+            if os.path.isdir(filename):
+                continue
             try:
                 doc, plainexamples = module_docs.get_docstring(filename)
                 desc = tty_ify(doc.get('short_description', '?'))
