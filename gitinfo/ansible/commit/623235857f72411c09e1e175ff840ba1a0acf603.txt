commit 623235857f72411c09e1e175ff840ba1a0acf603
Author: Ricardo Carrillo Cruz <ricardo.carrillo.cruz@gmail.com>
Date:   Fri Aug 4 12:57:36 2017 +0200

    Change tx delay to 2 on the rest of junos_lldp tests (#27753)

diff --git a/test/integration/targets/junos_lldp/tests/netconf/basic.yaml b/test/integration/targets/junos_lldp/tests/netconf/basic.yaml
index fea07073ba..f5e714c1bc 100644
--- a/test/integration/targets/junos_lldp/tests/netconf/basic.yaml
+++ b/test/integration/targets/junos_lldp/tests/netconf/basic.yaml
@@ -46,7 +46,7 @@
   junos_lldp:
     interval: 10
     hold_multiplier: 5
-    transmit_delay: 30
+    transmit_delay: 2
     state: present
     provider: "{{ netconf }}"
   register: result
@@ -59,7 +59,7 @@
   junos_lldp:
     interval: 10
     hold_multiplier: 5
-    transmit_delay: 30
+    transmit_delay: 2
     state: disabled
     provider: "{{ netconf }}"
   register: result
@@ -69,14 +69,14 @@
       - "result.changed == true"
       - result.diff.prepared | search("\+ *disable")
       - "'advertisement-interval 10;' not in result.diff.prepared"
-      - "'transmit-delay 30;' not in result.diff.prepared"
+      - "'transmit-delay 2;' not in result.diff.prepared"
       - "'hold-multiplier 5;' not in result.diff.prepared"
 
 - name: configure lldp parameters and enable lldp
   junos_lldp:
     interval: 10
     hold_multiplier: 5
-    transmit_delay: 30
+    transmit_delay: 2
     state: enabled
     provider: "{{ netconf }}"
   register: result
@@ -86,14 +86,14 @@
       - "result.changed == true"
       - result.diff.prepared | search("\- *disable")
       - "'advertisement-interval 10;' not in result.diff.prepared"
-      - "'transmit-delay 30;' not in result.diff.prepared"
+      - "'transmit-delay 2;' not in result.diff.prepared"
       - "'hold-multiplier 5;' not in result.diff.prepared"
 
 - name: Remove lldp configuration and diable lldp
   junos_lldp:
     interval: 10
     hold_multiplier: 5
-    transmit_delay: 30
+    transmit_delay: 2
     state: absent
     provider: "{{ netconf }}"
   register: result
@@ -103,7 +103,7 @@
       - "result.changed == true"
       - result.diff.prepared | search("\+ *disable")
       - result.diff.prepared | search("\- *advertisement-interval 10")
-      - result.diff.prepared | search("\- *transmit-delay 30")
+      - result.diff.prepared | search("\- *transmit-delay 2")
       - result.diff.prepared | search("\- *hold-multiplier 5")
 
 - name: Remove lldp (idempotent)
