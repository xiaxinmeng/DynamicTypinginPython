commit 726a1a33e0b10a93a6a922ff7214ae42b10670fc
Author: Michael DeHaan <michael.dehaan@gmail.com>
Date:   Mon Jul 28 12:07:31 2014 -0400

    Update unarchive example.

diff --git a/library/files/unarchive b/library/files/unarchive
index 3dbce9762c..460353a63b 100644
--- a/library/files/unarchive
+++ b/library/files/unarchive
@@ -70,8 +70,8 @@ EXAMPLES = '''
 # Example from Ansible Playbooks
 - unarchive: src=foo.tgz dest=/var/lib/foo
 
-# Unarchive a file on a remote machine
-- unarchive: src=/tmp/foo.zip dest=/usr/local/bin copy=false
+# Unarchive a file that is already on the remote machine
+- unarchive: src=/tmp/foo.zip dest=/usr/local/bin copy=no
 '''
 
 import os
