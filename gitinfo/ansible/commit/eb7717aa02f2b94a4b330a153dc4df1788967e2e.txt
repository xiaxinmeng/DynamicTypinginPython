commit eb7717aa02f2b94a4b330a153dc4df1788967e2e
Author: James Cammarata <jimi@sngx.net>
Date:   Tue Mar 18 12:30:54 2014 -0500

    Make sure all tags are in a list before merging for role deps
    
    Fixes #6557

diff --git a/lib/ansible/playbook/play.py b/lib/ansible/playbook/play.py
index 5d2f9e0a73..9195c5f2b6 100644
--- a/lib/ansible/playbook/play.py
+++ b/lib/ansible/playbook/play.py
@@ -234,6 +234,8 @@ class Play(object):
                         # (dep_vars) to look for tags going forward
                         def __merge_tags(var_obj):
                             old_tags = dep_vars.get('tags', [])
+                            if isinstance(old_tags, basestring):
+                                old_tags = [old_tags, ]
                             if isinstance(var_obj, dict):
                                 new_tags = var_obj.get('tags', [])
                                 if isinstance(new_tags, basestring):
