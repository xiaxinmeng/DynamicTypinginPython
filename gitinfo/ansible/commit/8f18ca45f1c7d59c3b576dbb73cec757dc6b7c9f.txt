commit 8f18ca45f1c7d59c3b576dbb73cec757dc6b7c9f
Author: Ricardo Carrillo Cruz <ricardo.carrillo.cruz@gmail.com>
Date:   Wed Aug 2 00:46:31 2017 +0200

    Comment cli provider creds on junos group_vars (#27611)
    
    We set the ansible_ssh_user and ansible_ssh_pass at the inventory level,
    commenting these out to avoid precedence issue.

diff --git a/test/integration/group_vars/junos.yaml b/test/integration/group_vars/junos.yaml
index 48d233dbc2..4672197b7d 100644
--- a/test/integration/group_vars/junos.yaml
+++ b/test/integration/group_vars/junos.yaml
@@ -7,6 +7,6 @@ netconf:
 
 cli:
   host: "{{ ansible_ssh_host }}"
-  username: "{{ junos_cli_user | default('ansible') }}"
-  password: "{{ junos_cli_pass | default('Ansible') }}"
+  #username: "{{ junos_cli_user | default('ansible') }}"
+  #password: "{{ junos_cli_pass | default('Ansible') }}"
   transport: cli
