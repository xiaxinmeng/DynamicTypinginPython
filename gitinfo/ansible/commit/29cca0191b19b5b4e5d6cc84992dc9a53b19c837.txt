commit 29cca0191b19b5b4e5d6cc84992dc9a53b19c837
Author: riedel <riedel@squishypuppy.com>
Date:   Sat Mar 7 10:17:41 2015 -0500

    Adding oVirt recognition for oVirt guests. oVirt uses KVM.

diff --git a/lib/ansible/module_utils/facts.py b/lib/ansible/module_utils/facts.py
index 41f79a4e8c..9781bb53ae 100644
--- a/lib/ansible/module_utils/facts.py
+++ b/lib/ansible/module_utils/facts.py
@@ -2406,6 +2406,11 @@ class LinuxVirtual(Virtual):
             self.facts['virtualization_role'] = 'guest'
             return
 
+	if sys_vendor == 'oVirt':
+            self.facts['virtualization_type'] = 'kvm'
+            self.facts['virtualization_role'] = 'guest'
+            return
+
         if os.path.exists('/proc/self/status'):
             for line in get_file_lines('/proc/self/status'):
                 if re.match('^VxID: \d+', line):
