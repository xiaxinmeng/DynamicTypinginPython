commit a8daff1590d95c1ced745bac931936f6babf8d09
Author: Peter Sprygada <psprygada@ansible.com>
Date:   Sun Sep 11 23:32:05 2016 -0400

    clean up iosxr shared module
    
    fixes up method signatures in iosxr shared module removing unneeded **kwargs

diff --git a/lib/ansible/module_utils/iosxr.py b/lib/ansible/module_utils/iosxr.py
index 076c923365..bd9866c22b 100644
--- a/lib/ansible/module_utils/iosxr.py
+++ b/lib/ansible/module_utils/iosxr.py
@@ -59,7 +59,7 @@ class Cli(CliBase):
 
     ### Config methods ###
 
-    def configure(self, commands, **kwargs):
+    def configure(self, commands):
         cmds = ['configure terminal']
         if commands[-1] == 'end':
             commands.pop()
@@ -68,7 +68,7 @@ class Cli(CliBase):
         responses = self.execute(cmds)
         return responses[1:]
 
-    def get_config(self, flags=None, **kwargs):
+    def get_config(self, flags=None):
         cmd = 'show running-config'
         if flags:
             if isinstance(flags, list):
@@ -106,6 +106,7 @@ class Cli(CliBase):
             self.execute(['abort'])
             diff = None
             raise
+
         return diff[0]
 
 Cli = register_transport('cli', default=True)(Cli)
