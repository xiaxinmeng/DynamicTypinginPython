commit f56b3fba3e05e3f67757747b2495f950c5d1b4ce
Author: Jeremy Price <jeremy@singleplatform.com>
Date:   Thu Sep 19 00:32:13 2013 -0400

    dest in s3 module does not work with ~username notation for home directories.  Wrapped dest definition in os.path.expanduser() which will return the expanded path in case of successful tilde expansion and otherwise the original value

diff --git a/library/cloud/s3 b/library/cloud/s3
index 062d6e06e4..5ebb71b319 100644
--- a/library/cloud/s3
+++ b/library/cloud/s3
@@ -261,7 +261,7 @@ def main():
     bucket = module.params.get('bucket')
     obj = module.params.get('object')
     src = module.params.get('src')
-    dest = module.params.get('dest')
+    dest = os.path.expanduser(module.params.get('dest'))
     mode = module.params.get('mode')
     expiry = int(module.params['expiry'])
     s3_url = module.params.get('s3_url')
