commit 132c14eb57c56a05e65db7c86a609fd3a94b49f9
Author: Brian Coca <bcoca@ansible.com>
Date:   Tue Jul 28 22:31:40 2015 -0400

    indented hosts

diff --git a/lib/ansible/cli/playbook.py b/lib/ansible/cli/playbook.py
index f56c5692db..44cc251502 100644
--- a/lib/ansible/cli/playbook.py
+++ b/lib/ansible/cli/playbook.py
@@ -168,7 +168,7 @@ class PlaybookCLI(CLI):
                         playhosts = set(inventory.get_hosts(play.hosts))
                         msg += "\n    pattern: %s\n    total hosts: %d\n    hosts:" % (play.hosts, len(playhosts))
                         for host in playhosts:
-                            msg += "\n    %s" % host
+                            msg += "\n      %s" % host
 
                     self.display.display(msg)
 
