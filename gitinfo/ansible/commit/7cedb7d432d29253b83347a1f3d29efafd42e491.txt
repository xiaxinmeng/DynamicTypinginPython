commit 7cedb7d432d29253b83347a1f3d29efafd42e491
Author: Yves Dorfsman <yves@zioup.com>
Date:   Thu Feb 28 13:24:24 2013 -0700

    Doc that fetch returns ok when src not present.

diff --git a/library/fetch b/library/fetch
index 5b8d311cb7..9b789ccdc4 100644
--- a/library/fetch
+++ b/library/fetch
@@ -7,7 +7,8 @@ short_description: Fetches a file from remote nodes
 description:
      - This module works like M(copy), but in reverse. It is used for fetching
        files from remote machines and storing them locally in a file tree,
-       organized by hostname.
+       organized by hostname. Note that if the source file is missing, it
+       returns status=ok.
 version_added: "0.2"
 options:
   src:
