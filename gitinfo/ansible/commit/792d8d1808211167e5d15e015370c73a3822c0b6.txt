commit 792d8d1808211167e5d15e015370c73a3822c0b6
Author: Landry Breuil <landry@rhaalovely.net>
Date:   Tue Oct 28 10:04:59 2014 +0100

    Ignore swap or altroot entries

diff --git a/lib/ansible/module_utils/facts.py b/lib/ansible/module_utils/facts.py
index 555a81df2c..9e857ff994 100644
--- a/lib/ansible/module_utils/facts.py
+++ b/lib/ansible/module_utils/facts.py
@@ -948,6 +948,8 @@ class OpenBSDHardware(Hardware):
                 if line.startswith('#') or line.strip() == '':
                     continue
                 fields = re.sub(r'\s+',' ',line.rstrip('\n')).split()
+                if fields[1] == 'none' or fields[3] == 'xx':
+                    continue
                 self.facts['mounts'].append({'mount': fields[1], 'device': fields[0], 'fstype' : fields[2], 'options': fields[3]})
 
     def get_memory_facts(self):
