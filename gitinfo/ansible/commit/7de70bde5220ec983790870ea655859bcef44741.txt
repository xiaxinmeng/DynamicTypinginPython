commit 7de70bde5220ec983790870ea655859bcef44741
Author: Gonéri Le Bouder <goneri@lebouder.net>
Date:   Thu Oct 10 13:25:07 2019 -0400

    prepare_vmware_test: avoid the dependency on jmespath (#63344)
    
    We don't really need to depend on jmespath for this.

diff --git a/test/integration/targets/prepare_vmware_tests/tasks/setup_tag.yml b/test/integration/targets/prepare_vmware_tests/tasks/setup_tag.yml
index 9719454e52..142248a7c0 100644
--- a/test/integration/targets/prepare_vmware_tests/tasks/setup_tag.yml
+++ b/test/integration/targets/prepare_vmware_tests/tasks/setup_tag.yml
@@ -8,11 +8,7 @@
 
 - name: Get Category id for {{ cluster_category }}
   set_fact:
-    cluster_category_id: "{{ item.category_id }}"
-  with_items:
-    - "{{ cat_info.tag_category_info | json_query(query) }}"
-  vars:
-    query: "[?category_name=='{{ cluster_category }}']"
+    cluster_category_id: "{{ cat_info.tag_category_info[0].category_id }}"
 
 - name: Create a tag for cluster
   vmware_tag:
