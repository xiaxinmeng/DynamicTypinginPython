commit bc7de7bf3fcf07160ccb0fda4f20f680117315d4
Author: Michael DeHaan <michael.dehaan@gmail.com>
Date:   Sun Jun 30 18:53:55 2013 -0400

    Styling: indentation

diff --git a/library/cloud/rds b/library/cloud/rds
index 4354c8435b..cd336c4dcc 100644
--- a/library/cloud/rds
+++ b/library/cloud/rds
@@ -351,11 +351,11 @@ def main():
         az = response.read()
         for r in AWS_REGIONS:
             if az.startswith(r):
-              ec2_region = r
-              break
+                ec2_region = r
+                break
 
         if not ec2_region:
-          module.fail_json(msg = str("ec2_region not specified and unable to determine region from AWS."))
+            module.fail_json(msg = str("ec2_region not specified and unable to determine region from AWS."))
 
     # connect to the rds endpoint
     try:
@@ -450,7 +450,7 @@ def main():
             db = conn.create_dbinstance(instance_name, size, instance_type, username, password, **params)
         elif command == 'replicate':
             if instance_type:
-              params["instance_class"] = instance_type
+                params["instance_class"] = instance_type
             db = conn.create_dbinstance_read_replica(instance_name, source_instance, **params)
         elif command == 'delete':
             if snapshot:
