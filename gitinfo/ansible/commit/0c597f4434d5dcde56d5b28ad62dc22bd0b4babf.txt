commit 0c597f4434d5dcde56d5b28ad62dc22bd0b4babf
Author: Gonéri Le Bouder <goneri@lebouder.net>
Date:   Fri Feb 14 00:47:15 2020 -0500

    vmware_guest_register_operation/test: no deprecated mod (#67411)
    
    Don't use `vmware_guest_facts` to collect the Guest information. The
    module is deprecated and replaced by `vmware_guest_info`.

diff --git a/test/integration/targets/vmware_guest_register_operation/tasks/main.yml b/test/integration/targets/vmware_guest_register_operation/tasks/main.yml
index 169babacbe..8e9cafe5ad 100644
--- a/test/integration/targets/vmware_guest_register_operation/tasks/main.yml
+++ b/test/integration/targets/vmware_guest_register_operation/tasks/main.yml
@@ -13,7 +13,7 @@
 
 - block:
   - name: gather facts of vm
-    vmware_guest_facts:
+    vmware_guest_info:
       hostname: "{{ vcenter_hostname }}"
       username: "{{ vcenter_username }}"
       password: "{{ vcenter_password }}"
@@ -218,7 +218,7 @@
         | length == 1
 
 - name: Gather facts of vm
-  vmware_guest_facts:
+  vmware_guest_info:
     hostname: "{{ vcenter_hostname }}"
     username: "{{ vcenter_username }}"
     password: "{{ vcenter_password }}"
