commit dab31116b3727bc4a34bda04ab62c0fa07393651
Author: Daniel Hokka Zakrisson <daniel@hozac.com>
Date:   Wed Feb 6 16:52:04 2013 +0100

    Actually add stdout data to stdout buffer for daemon mode

diff --git a/library/service b/library/service
index 77612aef71..6a1721b037 100644
--- a/library/service
+++ b/library/service
@@ -187,6 +187,7 @@ class Service(object):
                     dat = os.read(p.stdout.fileno(), 4096)
                     if not dat:
                         fds.remove(p.stdout)
+                    stdout += dat
                 if p.stderr in rfd:
                     dat = os.read(p.stderr.fileno(), 4096)
                     if not dat:
