commit cde551392384648bdc0416b87037c7c9fbc628d1
Author: Monty Taylor <mordred@inaugust.com>
Date:   Sun Aug 3 10:43:39 2014 -0700

    Pass region_name to nova_keypair
    
    Again, on things with regions, when we're configuring it, we should
    actually pass it in to the compute constructor.

diff --git a/library/cloud/nova_keypair b/library/cloud/nova_keypair
index 553683d3a8..64539b6a00 100644
--- a/library/cloud/nova_keypair
+++ b/library/cloud/nova_keypair
@@ -104,6 +104,7 @@ def main():
                               module.params['login_password'],
                               module.params['login_tenant_name'],
                               module.params['auth_url'],
+                              region_name=module.params['region_name'],
                               service_type='compute')
     try:
         nova.authenticate()
