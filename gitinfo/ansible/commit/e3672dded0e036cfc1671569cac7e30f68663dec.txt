commit e3672dded0e036cfc1671569cac7e30f68663dec
Author: James Tanner <tanner.jc@gmail.com>
Date:   Wed Feb 26 14:53:36 2014 -0500

    Remove breakpoint

diff --git a/bin/ansible-pull b/bin/ansible-pull
index 917c878d49..e6c5712f75 100755
--- a/bin/ansible-pull
+++ b/bin/ansible-pull
@@ -181,7 +181,6 @@ def main(args):
         return 1
 
     cmd = 'ansible-playbook %s %s' % (base_opts, playbook)
-    import epdb; epdb.st()
     if options.vault_password_file:
         cmd += " --vault-password-file=%s" % options.vault_password_file
     if options.inventory:
