commit 8effa23ad74930a06a0f5386c64408850135624e
Author: Toshio Kuratomi <a.badger@gmail.com>
Date:   Fri Oct 16 12:59:23 2015 -0700

    Add unicode integration tests to be run in travis

diff --git a/.travis.yml b/.travis.yml
index 9efa0fb60f..29ed7d3d0f 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -25,6 +25,6 @@ script:
 - if test x"$TOXENV" != x'py24' ; then tox ; fi
 - if test x"$TOXENV" = x'py24' ; then python2.4 -V && python2.4 -m compileall -fq -x 'module_utils/(a10|rax|openstack|ec2|gce).py' lib/ansible/module_utils ; fi
   #- make -C docsite all
-- if test x"$INTEGRATION" = x'yes' ; then source ./hacking/env-setup && cd test/integration/ && make test_var_precedence ; fi
+- if test x"$INTEGRATION" = x'yes' ; then source ./hacking/env-setup && cd test/integration/ && make test_var_precedence && make unicode ; fi
 after_success:
   - coveralls
