commit 26cc0b37807c345a64dc0b4314cc59cd918d1b81
Author: Marc Abramowitz <marc@marc-abramowitz.com>
Date:   Mon Jun 30 21:40:11 2014 -0700

    nova.py: indented and sorted JSON output

diff --git a/plugins/inventory/nova.py b/plugins/inventory/nova.py
index 60da2d83e9..d935ca0a58 100755
--- a/plugins/inventory/nova.py
+++ b/plugins/inventory/nova.py
@@ -199,7 +199,7 @@ if len(sys.argv) == 2 and (sys.argv[1] == '--list'):
 		continue
 
     # Return server list
-    print json.dumps(groups)
+    print(json.dumps(groups, sort_keys=True, indent=2))
     sys.exit(0)
 
 #####################################################
@@ -228,7 +228,7 @@ elif len(sys.argv) == 3 and (sys.argv[1] == '--host'):
                 if key != 'os_manager':
                     results[key] = value
 
-    print json.dumps(results)
+    print(json.dumps(results, sort_keys=True, indent=2))
     sys.exit(0)
 
 else:
