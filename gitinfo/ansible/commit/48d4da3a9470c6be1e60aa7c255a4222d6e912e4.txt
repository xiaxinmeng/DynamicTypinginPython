commit 48d4da3a9470c6be1e60aa7c255a4222d6e912e4
Author: Chris Meyers <chris.meyers.fsu@gmail.com>
Date:   Thu Jan 15 16:19:50 2015 -0500

    add tagless grouping for destinations without any tags

diff --git a/plugins/inventory/ec2.py b/plugins/inventory/ec2.py
index 769ca433b0..f1b3524e12 100755
--- a/plugins/inventory/ec2.py
+++ b/plugins/inventory/ec2.py
@@ -465,6 +465,10 @@ class Ec2Inventory(object):
                 if self.nested_groups:
                     self.push_group(self.inventory, 'route53', name)
 
+        # Global Tag: instances without tags
+        if len(instance.tags) == 0:
+            self.push(self.inventory, 'tag_none', dest)
+            
         # Global Tag: tag all EC2 instances
         self.push(self.inventory, 'ec2', dest)
 
