commit aa6a7d5789fb02ba13e94e016f4dce50cfc7db66
Author: John R Barker <john@johnrbarker.com>
Date:   Tue Aug 2 18:20:13 2016 +0100

    State runnng v running
    
    Fix typo

diff --git a/lib/ansible/modules/network/eos/eos_eapi.py b/lib/ansible/modules/network/eos/eos_eapi.py
index d81a26f974..568ea720f7 100644
--- a/lib/ansible/modules/network/eos/eos_eapi.py
+++ b/lib/ansible/modules/network/eos/eos_eapi.py
@@ -152,7 +152,7 @@ def http_commands(protocol, port, enable, config):
             # update the port
             commands.append('protocol {0} port {1}'.format(protocol, port))
             changed = True
-    elif not started_config.get('runnng') and enable:
+    elif not started_config.get('running') and enable:
         # turn on server
         commands.append('protocol {0} port {1}'.format(protocol, port))
         changed = True
