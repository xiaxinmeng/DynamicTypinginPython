commit 9f2d58f7b45249775b3a3f05684e2cbfd623a2d9
Author: Ricardo Carrillo Cruz <ricardo.carrillo.cruz@gmail.com>
Date:   Wed Jul 12 10:38:25 2017 +0200

    Fix eos_config config test (#26679)
    
    We needed to re-register show-running config before doing the
    idempotency test.

diff --git a/test/integration/targets/eos_config/tests/cli/config.yaml b/test/integration/targets/eos_config/tests/cli/config.yaml
index e72dab0e00..c84decc6af 100644
--- a/test/integration/targets/eos_config/tests/cli/config.yaml
+++ b/test/integration/targets/eos_config/tests/cli/config.yaml
@@ -25,6 +25,12 @@
       - "result.changed == true"
       - "'hostname foo' in result.updates"
 
+- name: get current running-config
+  eos_command:
+    commands: show running-config
+    provider: "{{ cli }}"
+  register: config
+
 - name: configure hostname again
   eos_config:
     lines: hostname foo
