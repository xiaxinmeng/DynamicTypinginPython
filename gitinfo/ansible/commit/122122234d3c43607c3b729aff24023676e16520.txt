commit 122122234d3c43607c3b729aff24023676e16520
Author: Dirk Uys <dirkcuys@gmail.com>
Date:   Fri Apr 26 17:07:29 2013 +0300

    Fix bug causing mysqldump to fail

diff --git a/library/mysql_db b/library/mysql_db
index fb73e05763..c0f24a3fde 100644
--- a/library/mysql_db
+++ b/library/mysql_db
@@ -109,7 +109,7 @@ def db_delete(cursor, db):
     return True
 
 def db_dump(host, user, password, db_name, target):
-    res = os.system("/usr/bin/mysqldump -q -h "+host+"-u "+user+ " --password="+password+" "
+    res = os.system("/usr/bin/mysqldump -q -h "+host+" -u "+user+ " --password="+password+" "
             +db_name+" > "
             +target)
     return (res == 0)
