commit c7f33627950d352cdca46f71c93b0783981b8c89
Author: Johannes 'fish' Ziemke <github@freigeist.org>
Date:   Mon Apr 6 14:43:39 2015 +0200

    Replace - in ec2 inventory as well
    
    Dash (-) is not a variable ansible group name, so it needs to be replaced as well.

diff --git a/plugins/inventory/ec2.py b/plugins/inventory/ec2.py
index e93df1053d..76871b0266 100755
--- a/plugins/inventory/ec2.py
+++ b/plugins/inventory/ec2.py
@@ -787,7 +787,7 @@ class Ec2Inventory(object):
         ''' Converts 'bad' characters in a string to underscores so they can be
         used as Ansible groups '''
 
-        return re.sub("[^A-Za-z0-9\-]", "_", word)
+        return re.sub("[^A-Za-z0-9\_]", "_", word)
 
 
     def json_format_dict(self, data, pretty=False):
