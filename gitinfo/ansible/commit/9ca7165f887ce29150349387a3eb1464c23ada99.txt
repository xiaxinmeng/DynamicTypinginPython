commit 9ca7165f887ce29150349387a3eb1464c23ada99
Author: Chris Gardner <chris_g_g@hotmail.com>
Date:   Tue May 28 22:40:36 2013 +0100

    Use kstat for Solaris CPU info

diff --git a/library/system/setup b/library/system/setup
index a33d554920..2127071d3f 100644
--- a/library/system/setup
+++ b/library/system/setup
@@ -655,7 +655,7 @@ class SunOSHardware(Hardware):
         return self.facts
 
     def get_cpu_facts(self):
-        rc, out, err = module.run_command("/usr/sbin/kstat cpu_info")
+        rc, out, err = module.run_command("/usr/bin/kstat cpu_info")
         self.facts['processor'] = []
         for line in out.split('\n'):
             if len(line) < 1:
