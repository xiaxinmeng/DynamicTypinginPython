commit 3fa7423e8e6a3afcfd9b859d0952f7612b66b33a
Author: Wojciech Wypior <w.wypior@f5.com>
Date:   Thu Aug 22 04:50:28 2019 +0100

    fixes an issue where config sync with the module fails if there are sync failures on a different device group than the one being synced (#61067)

diff --git a/lib/ansible/modules/network/f5/bigip_configsync_action.py b/lib/ansible/modules/network/f5/bigip_configsync_action.py
index ae510bf2dc..dcc8db7ae0 100644
--- a/lib/ansible/modules/network/f5/bigip_configsync_action.py
+++ b/lib/ansible/modules/network/f5/bigip_configsync_action.py
@@ -352,7 +352,12 @@ class ModuleManager(object):
     def _get_details_from_resource(self):
         resource = self.read_current_from_device()
         stats = resource['entries'].copy()
-        tree = Tree(stats)
+        if HAS_OBJPATH:
+            tree = Tree(stats)
+        else:
+            raise F5ModuleError(
+                "objectpath module required, install objectpath module to continue. "
+            )
         details = list(tree.execute('$..*["details"]["description"]'))
         result = details[::-1]
         return result
