commit c419cacb04b0b77a3e0fab54f500449e156fb4a4
Author: Brian Coca <bcoca@ansible.com>
Date:   Mon Aug 17 17:58:35 2015 -0400

    check if host task is none before continuing
    fixes #11982

diff --git a/lib/ansible/plugins/strategies/linear.py b/lib/ansible/plugins/strategies/linear.py
index dd24b8bf0f..527843c692 100644
--- a/lib/ansible/plugins/strategies/linear.py
+++ b/lib/ansible/plugins/strategies/linear.py
@@ -88,10 +88,12 @@ class StrategyModule(StrategyBase):
                 if host_state_task is None:
                     continue
                 (s, t) = host_state_task
+                if t is None:
+                    continue
                 if s.run_state == cur_state and s.cur_block == cur_block:
                     new_t = iterator.get_next_task_for_host(host)
                     #if new_t != t:
-                    #    raise AnsibleError("iterator error, wtf?")
+                    #    raise AnsibleError("iterator error, wtf?") FIXME
                     rvals.append((host, t))
                 else:
                     rvals.append((host, noop_task))
