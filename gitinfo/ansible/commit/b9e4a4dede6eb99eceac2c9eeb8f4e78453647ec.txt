commit b9e4a4dede6eb99eceac2c9eeb8f4e78453647ec
Author: Antonio Huete Jimenez <tuxillo@quantumachine.net>
Date:   Thu Dec 22 23:27:28 2016 +0100

    Check for DragonFly BSD as well for DATE

diff --git a/Makefile b/Makefile
index 78bdd34114..af38391130 100644
--- a/Makefile
+++ b/Makefile
@@ -46,7 +46,7 @@ else
 GITINFO = ""
 endif
 
-ifeq ($(shell echo $(OS) | egrep -c 'Darwin|FreeBSD|OpenBSD'),1)
+ifeq ($(shell echo $(OS) | egrep -c 'Darwin|FreeBSD|OpenBSD|DragonFly'),1)
 DATE := $(shell date -j -r $(shell git log -n 1 --format="%at") +%Y%m%d%H%M)
 else
 DATE := $(shell date --utc --date="$(GIT_DATE)" +%Y%m%d%H%M)
