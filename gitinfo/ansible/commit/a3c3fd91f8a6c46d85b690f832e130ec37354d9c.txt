commit a3c3fd91f8a6c46d85b690f832e130ec37354d9c
Author: Sandra McCann <samccann@redhat.com>
Date:   Thu May 16 21:31:02 2019 -0400

    move too old version to 2.3 (#56540)

diff --git a/docs/bin/plugin_formatter.py b/docs/bin/plugin_formatter.py
index b136af5985..1774aca702 100755
--- a/docs/bin/plugin_formatter.py
+++ b/docs/bin/plugin_formatter.py
@@ -52,7 +52,7 @@ from ansible.utils._build_helpers import update_file_if_different
 
 # if a module is added in a version of Ansible older than this, don't print the version added information
 # in the module documentation because everyone is assumed to be running something newer than this already.
-TOO_OLD_TO_BE_NOTABLE = 2.0
+TOO_OLD_TO_BE_NOTABLE = 2.3
 
 # Get parent directory of the directory this script lives in
 MODULEDIR = os.path.abspath(os.path.join(
