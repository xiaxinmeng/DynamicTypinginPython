commit e97f789913f4de1069012e019eb836dd521c77cd
Author: Rik Theys <Rik.Theys@gmail.com>
Date:   Sun May 26 23:06:23 2013 +0200

    Fix tabs

diff --git a/library/cloud/virt b/library/cloud/virt
index 62c9158558..2ac079058a 100644
--- a/library/cloud/virt
+++ b/library/cloud/virt
@@ -281,11 +281,11 @@ class Virt(object):
         results = []
         for x in vms:
             try:
-		if state:
-	    	    vmstate = self.conn.get_status2(x)
-		    if vmstate == state:
-		        results.append(x.name())
-		else:
+                if state:
+                    vmstate = self.conn.get_status2(x)
+                    if vmstate == state:
+                        results.append(x.name())
+                else:
                     results.append(x.name())
             except:
                 pass
