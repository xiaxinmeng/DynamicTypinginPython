commit 5ce29734d250ef06c5793e07fcd696d2e8e013fa
Author: Andy Kluger <AndydeCleyre@gmail.com>
Date:   Thu Oct 10 17:18:06 2013 -0400

    Set default of 'yes' for update_cache, as is in the documentation

diff --git a/library/packaging/apt_repository b/library/packaging/apt_repository
index 83c2619dea..bf9f22a65c 100644
--- a/library/packaging/apt_repository
+++ b/library/packaging/apt_repository
@@ -333,7 +333,7 @@ def main():
         argument_spec=dict(
             repo=dict(required=True),
             state=dict(choices=['present', 'absent'], default='present'),
-            update_cache = dict(aliases=['update-cache'], type='bool'),
+            update_cache = dict(aliases=['update-cache'], type='bool', default='yes'),
         ),
         supports_check_mode=True,
     )
