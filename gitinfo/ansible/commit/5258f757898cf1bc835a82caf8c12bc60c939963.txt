commit 5258f757898cf1bc835a82caf8c12bc60c939963
Author: jjshoe <jjshoe@users.noreply.github.com>
Date:   Wed Apr 16 10:06:59 2014 -0500

    apt_key lists all keys in uppercase
    
    allow for a little user error

diff --git a/library/packaging/apt_key b/library/packaging/apt_key
index 2308d34329..450edcf4a8 100644
--- a/library/packaging/apt_key
+++ b/library/packaging/apt_key
@@ -210,6 +210,7 @@ def main():
             _ = int(key_id, 16)
             if key_id.startswith('0x'):
                 key_id = key_id[2:]
+            key_id = key_id.upper()
         except ValueError:
             module.fail_json(msg="Invalid key_id", id=key_id)
 
