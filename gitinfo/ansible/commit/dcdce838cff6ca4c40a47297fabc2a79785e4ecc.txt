commit dcdce838cff6ca4c40a47297fabc2a79785e4ecc
Author: Daniel Koch <dkoch@mm.st>
Date:   Wed Oct 30 20:24:03 2013 -0400

    Clean up temp file left behind by assemble module

diff --git a/library/files/assemble b/library/files/assemble
index 8811f5ca55..92e4aeaa1c 100644
--- a/library/files/assemble
+++ b/library/files/assemble
@@ -137,6 +137,8 @@ def main():
         shutil.copy(path, dest)
         changed = True
 
+    os.remove(path)
+
     file_args = module.load_file_common_arguments(module.params)
     changed = module.set_file_attributes_if_different(file_args, changed)
     # Mission complete
