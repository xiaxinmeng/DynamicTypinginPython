commit 0bbbefeb3f64253bef0b567d7d26a366983fb41d
Author: Bruce Pennypacker <bpennypacker@care.com>
Date:   Thu Jul 25 09:59:23 2013 -0400

    doc update

diff --git a/library/files/stat b/library/files/stat
index 0478e03036..c1d58995c3 100644
--- a/library/files/stat
+++ b/library/files/stat
@@ -40,10 +40,17 @@ author: Bruce Pennypacker
 EXAMPLES = '''
 # Obtain the stats of /etc/foo.conf, and check that the file still belongs
 # to 'root'. Fail otherwise.
-- stats: path=/etc/foo.conf
+- stat: path=/etc/foo.conf
   register: st
 - fail: msg="Whoops! file ownership has changed"
   when: st.stat.pw_name != 'root'
+
+# Determine if a path exists and is a directory.  Note we need to test
+# both that p.stat.isdir actually exists, and also that it's set to true.
+- stat: path=/path/to/something
+  register: p
+- debug: msg="Path exists and is a directory"
+  when: p.stat.isdir is defined and p.stat.isdir == true
 '''
 
 import os
