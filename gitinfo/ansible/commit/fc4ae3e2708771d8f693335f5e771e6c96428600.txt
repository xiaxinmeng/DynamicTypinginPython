commit fc4ae3e2708771d8f693335f5e771e6c96428600
Author: Derek Carter <goozbach@friocorte.com>
Date:   Sun Jul 1 17:38:17 2012 -0400

    remove UID/GID from output

diff --git a/library/file b/library/file
index 337fa15c00..2575c57c39 100755
--- a/library/file
+++ b/library/file
@@ -187,11 +187,11 @@ def user_and_group(filename):
     try:
         user = pwd.getpwuid(uid)[0]
     except KeyError:
-        user = 'UID' + str(uid)
+        user = str(uid)
     try:
         group = grp.getgrgid(gid)[0]
     except KeyError:
-        group = 'GID' + str(gid)
+        group = str(gid)
     return (user, group)
 
 def set_context_if_different(path, context, changed):
