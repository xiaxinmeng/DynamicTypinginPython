commit e8f83dcba34ce7ea22d63b0e5f0b57b9dd49e853
Author: James Cammarata <jimi@sngx.net>
Date:   Mon Nov 9 20:41:59 2015 -0500

    Revert "Temporarily disable template caching"
    
    This reverts commit 4d1757ce64fd9627cbc022c1533cde3f25ecdbb1.

diff --git a/lib/ansible/template/__init__.py b/lib/ansible/template/__init__.py
index 3eb085b2bb..709ee03ef8 100644
--- a/lib/ansible/template/__init__.py
+++ b/lib/ansible/template/__init__.py
@@ -332,8 +332,8 @@ class Templar:
                         # we only cache in the case where we have a single variable
                         # name, to make sure we're not putting things which may otherwise
                         # be dynamic in the cache (filters, lookups, etc.)
-                        #if var_name not in (None, 'item'):
-                        #    self._cached_result[sha1_hash] = result
+                        if var_name not in (None, 'item'):
+                            self._cached_result[sha1_hash] = result
 
                 return result
 
