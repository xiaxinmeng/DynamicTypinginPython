commit 32aa948dd0d26f61d09315c4f68f3861b4449397
Author: James Cammarata <jimi@sngx.net>
Date:   Fri Jun 20 10:12:14 2014 -0500

    Exclude ps1 modules from the TestModules unittest
    
    Fixes #7864

diff --git a/test/units/TestModules.py b/test/units/TestModules.py
index 54e3ec3213..83c2b7c398 100644
--- a/test/units/TestModules.py
+++ b/test/units/TestModules.py
@@ -15,7 +15,9 @@ class TestModules(unittest.TestCase):
         for path in paths:
             for (dirpath, dirnames, filenames) in os.walk(path):
                 for filename in filenames:
-                    module_list.append(os.path.join(dirpath, filename))
+                    (path, ext) = os.path.splitext(filename)
+                    if ext != ".ps1":
+                        module_list.append(os.path.join(dirpath, filename))
         return module_list
 
     def test_ast_parse(self):
