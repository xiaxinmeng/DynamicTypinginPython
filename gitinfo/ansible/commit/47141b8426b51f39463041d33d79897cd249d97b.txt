commit 47141b8426b51f39463041d33d79897cd249d97b
Author: Peter Sprygada <privateip@users.noreply.github.com>
Date:   Tue Feb 14 20:12:49 2017 -0500

    fixes minor issue with strict diff of network config (#21436)
    
    strips leading and trailing spaces for line comparision to prevent
    fails positiives

diff --git a/lib/ansible/module_utils/netcfg.py b/lib/ansible/module_utils/netcfg.py
index ceea081c44..90180085e3 100644
--- a/lib/ansible/module_utils/netcfg.py
+++ b/lib/ansible/module_utils/netcfg.py
@@ -247,9 +247,9 @@ class NetworkConfig(object):
         updates = list()
         for index, line in enumerate(self.items):
             try:
-                if line != other[index]:
+                if str(line).strip() != str(other[index]).strip():
                     updates.append(line)
-            except IndexError:
+            except (AttributeError, IndexError):
                 updates.append(line)
         return updates
 
