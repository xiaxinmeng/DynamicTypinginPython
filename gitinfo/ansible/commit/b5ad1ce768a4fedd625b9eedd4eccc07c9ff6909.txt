commit b5ad1ce768a4fedd625b9eedd4eccc07c9ff6909
Author: Michael DeHaan <michael.dehaan@gmail.com>
Date:   Thu Feb 28 16:03:23 2013 -0500

    In django module if no virtualenv is specified, don't choke

diff --git a/library/django_manage b/library/django_manage
index 006099b2e2..94a72bcc70 100644
--- a/library/django_manage
+++ b/library/django_manage
@@ -109,7 +109,10 @@ def _fail(module, cmd, out, err, **kwargs):
 
 
 def _ensure_virtualenv(module):
+
     venv_param = module.params['virtualenv']
+    if venv_param is None:
+        return
 
     virtualenv = module.get_bin_path('virtualenv', True)
 
