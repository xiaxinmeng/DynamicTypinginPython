commit e35b1cf1549c38fc9f81481ac14ff3077ddb3d07
Author: Brian Coca <bcoca@ansible.com>
Date:   Sun Feb 21 16:22:11 2016 -0500

    show task path on vv and above

diff --git a/lib/ansible/plugins/callback/default.py b/lib/ansible/plugins/callback/default.py
index b6d1d3f67c..6ef3352a1c 100644
--- a/lib/ansible/plugins/callback/default.py
+++ b/lib/ansible/plugins/callback/default.py
@@ -126,7 +126,7 @@ class CallbackModule(CallbackBase):
             args = ', '.join(('%s=%s' % a for a in task.args.items()))
             args = ' %s' % args
         self._display.banner("TASK [%s%s]" % (task.get_name().strip(), args))
-        if self._display.verbosity > 2:
+        if self._display.verbosity >= 2:
             path = task.get_path()
             if path:
                 self._display.display("task path: %s" % path, color=C.COLOR_DEBUG)
