commit ad490573306864f8596f9e9af49e69f70d8f15d0
Author: Gonéri Le Bouder <goneri@lebouder.net>
Date:   Wed Jul 10 12:05:58 2019 +0200

    fix the vm name

diff --git a/test/integration/targets/vmware_guest_sendkey/tasks/main.yml b/test/integration/targets/vmware_guest_sendkey/tasks/main.yml
index 4a1e0fedd1..eca69b2fb4 100644
--- a/test/integration/targets/vmware_guest_sendkey/tasks/main.yml
+++ b/test/integration/targets/vmware_guest_sendkey/tasks/main.yml
@@ -17,7 +17,7 @@
       hostname: "{{ vcenter_hostname }}"
       username: "{{ vcenter_username }}"
       password: "{{ vcenter_password }}"
-      name: "{{ infra.vm_list[0] }}"
+      name: "{{ virtual_machines[0].name }}"
       keys_send:
         - DOWNARROW
         - DOWNARROW
@@ -35,7 +35,7 @@
       hostname: "{{ vcenter_hostname }}"
       username: "{{ vcenter_username }}"
       password: "{{ vcenter_password }}"
-      name: "{{ infra.vm_list[0] }}"
+      name: "{{ virtual_machines[0].name }}"
       string_send: "test-user"
     register: send_string
   - debug: var=send_string
