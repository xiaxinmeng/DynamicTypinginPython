commit aac20aa0767906dc43e77352777cab0c5b0c72e5
Author: Brian Coca <bcoca@ansible.com>
Date:   Wed Oct 21 13:25:24 2015 -0400

    removed unused exception vars

diff --git a/contrib/inventory/ec2.py b/contrib/inventory/ec2.py
index 6fc57f6c95..fd1cebe202 100755
--- a/contrib/inventory/ec2.py
+++ b/contrib/inventory/ec2.py
@@ -360,7 +360,7 @@ class Ec2Inventory(object):
                 self.pattern_include = re.compile(pattern_include)
             else:
                 self.pattern_include = None
-        except configparser.NoOptionError as e:
+        except configparser.NoOptionError:
             self.pattern_include = None
 
         # Do we need to exclude hosts that match a pattern?
@@ -370,7 +370,7 @@ class Ec2Inventory(object):
                 self.pattern_exclude = re.compile(pattern_exclude)
             else:
                 self.pattern_exclude = None
-        except configparser.NoOptionError as e:
+        except configparser.NoOptionError:
             self.pattern_exclude = None
 
         # Instance filters (see boto and EC2 API docs). Ignore invalid filters.
