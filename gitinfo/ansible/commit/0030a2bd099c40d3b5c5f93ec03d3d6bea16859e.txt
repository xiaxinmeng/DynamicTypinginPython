commit 0030a2bd099c40d3b5c5f93ec03d3d6bea16859e
Author: Michael DeHaan <michael.dehaan@gmail.com>
Date:   Sun Jan 27 10:40:29 2013 -0500

    Allow handler files to be empty lists.

diff --git a/lib/ansible/playbook/play.py b/lib/ansible/playbook/play.py
index 608691c712..f414ec5553 100644
--- a/lib/ansible/playbook/play.py
+++ b/lib/ansible/playbook/play.py
@@ -101,6 +101,10 @@ class Play(object):
         ''' handle task and handler include statements '''
 
         results = []
+        if tasks is None:
+            # support empty handler files, and the like.
+            tasks = []
+
         for x in tasks:
             task_vars = self.vars.copy()
             task_vars.update(vars)
