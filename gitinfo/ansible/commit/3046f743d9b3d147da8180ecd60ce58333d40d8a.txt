commit 3046f743d9b3d147da8180ecd60ce58333d40d8a
Author: Matthew Williams <matthew@flowroute.com>
Date:   Tue Mar 27 11:12:33 2012 -0700

    removed hack

diff --git a/library/template b/library/template
index c6b0c8e3c1..03bdb18d57 100755
--- a/library/template
+++ b/library/template
@@ -63,13 +63,6 @@ if not os.path.exists(metadata):
 try:
    f = open(metadata)
    data = json.loads(f.read())
-# Hack by mgw to get nested variables -- use at your own risk
-#   data_in = json.loads(f.read())
-#   for k, v in data_in.items():
-#       try:
-#           data[k] = eval(v)
-#       except:
-#           data[k] = v
    f.close()
 except:
    print json.dumps({
