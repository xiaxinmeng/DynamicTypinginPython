commit 38d6421425dd5ea6d0529c523f89e92bdeb21a37
Author: rchikatw <55136127+rchikatw@users.noreply.github.com>
Date:   Wed Nov 20 14:59:22 2019 +0530

    Handle gfid tag when producing ansible facts in gluster_heal_info (#65041)
    
    we ignore lines beginning with / or < which will produce the requires json format.
    
    Fixes: #65095

diff --git a/lib/ansible/modules/storage/glusterfs/gluster_heal_info.py b/lib/ansible/modules/storage/glusterfs/gluster_heal_info.py
index 4af8eb95fb..0da1373b14 100644
--- a/lib/ansible/modules/storage/glusterfs/gluster_heal_info.py
+++ b/lib/ansible/modules/storage/glusterfs/gluster_heal_info.py
@@ -114,7 +114,7 @@ def get_self_heal_status(name):
             br_dict['status'] = line.split(":")[1].strip()
         elif 'Number' in line:
             br_dict['no_of_entries'] = line.split(":")[1].strip()
-        elif line.startswith('/') or '\n' in line:
+        elif line.startswith('/') or line.startswith('<') or '\n' in line:
             continue
         else:
             br_dict and heal_info.append(br_dict)
