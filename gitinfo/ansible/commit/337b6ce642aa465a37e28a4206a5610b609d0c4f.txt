commit 337b6ce642aa465a37e28a4206a5610b609d0c4f
Author: Michael DeHaan <michael.dehaan@gmail.com>
Date:   Thu Oct 31 20:00:11 2013 -0400

    Fix documentation formatting.

diff --git a/library/source_control/git b/library/source_control/git
index d6bfb554e3..213cccb0c7 100644
--- a/library/source_control/git
+++ b/library/source_control/git
@@ -82,10 +82,10 @@ options:
             - Path to git executable to use. If not supplied,
               the normal mechanism for resolving binary paths will be used.
 notes:
-    - If the task seems to be hanging, first verify remote host is in C(known_hosts).
+    - "If the task seems to be hanging, first verify remote host is in C(known_hosts).
       SSH will prompt user to authorize the first contact with a remote host.  To avoid this prompt, 
       one solution is to add the remote host public key in C(/etc/ssh/ssh_known_hosts) before calling 
-      the git module, with the following command: ssh-keyscan remote_host.com >> /etc/ssh/ssh_known_hosts.
+      the git module, with the following command: ssh-keyscan remote_host.com >> /etc/ssh/ssh_known_hosts."
 '''
 
 EXAMPLES = '''
diff --git a/library/source_control/hg b/library/source_control/hg
index 6da2e5675d..5293ed3d27 100644
--- a/library/source_control/hg
+++ b/library/source_control/hg
@@ -73,10 +73,10 @@ options:
             - Path to hg executable to use. If not supplied,
               the normal mechanism for resolving binary paths will be used.
 notes:
-    - If the task seems to be hanging, first verify remote host is in C(known_hosts).
+    - "If the task seems to be hanging, first verify remote host is in C(known_hosts).
       SSH will prompt user to authorize the first contact with a remote host.  To avoid this prompt, 
       one solution is to add the remote host public key in C(/etc/ssh/ssh_known_hosts) before calling 
-      the hg module, with the following command: ssh-keyscan remote_host.com >> /etc/ssh/ssh_known_hosts.
+      the hg module, with the following command: ssh-keyscan remote_host.com >> /etc/ssh/ssh_known_hosts."
 requirements: [ ]
 '''
 
