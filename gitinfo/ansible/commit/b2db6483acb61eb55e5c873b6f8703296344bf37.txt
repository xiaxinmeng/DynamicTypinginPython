commit b2db6483acb61eb55e5c873b6f8703296344bf37
Author: lonerr <oleg@mamontov.net>
Date:   Wed May 8 13:03:18 2013 +0400

    Fixed errors in FreeBSD user manipulations.

diff --git a/library/system/user b/library/system/user
index 5687c0d5b8..9cf665965c 100644
--- a/library/system/user
+++ b/library/system/user
@@ -568,7 +568,7 @@ class FreeBsdUser(User):
             cmd.append(self.home)
 
         if self.group is not None:
-            if not user.group_exists(self.group):
+            if not self.group_exists(self.group):
                 self.module.fail_json(msg="Group %s does not exist" % self.group)
             cmd.append('-g')
             cmd.append(self.group)
@@ -631,7 +631,7 @@ class FreeBsdUser(User):
             cmd.append(self.home)
 
         if self.group is not None:
-            if not user.group_exists(self.group):
+            if not self.group_exists(self.group):
                 self.module.fail_json(msg="Group %s does not exist" % self.group)
             ginfo = self.group_info(self.group)
             if info[3] != ginfo[2]:
