commit e121ff111db87fa80e4e6c85efaa58d607a894ac
Author: James Tanner <tanner.jc@gmail.com>
Date:   Tue Feb 25 09:24:14 2014 -0500

    Fix synchronize unit test to handle checkmode

diff --git a/test/units/TestSynchronize.py b/test/units/TestSynchronize.py
index 086287409d..7965f2295e 100644
--- a/test/units/TestSynchronize.py
+++ b/test/units/TestSynchronize.py
@@ -17,6 +17,7 @@ class FakeRunner(object):
         self.sudo = None
         self.remote_user = None
         self.private_key_file = None
+        self.check = False
 
     def _execute_module(self, conn, tmp, module_name, args, inject=None):
         self.executed_conn = conn
@@ -25,6 +26,9 @@ class FakeRunner(object):
         self.executed_args = args
         self.executed_inject = inject
 
+    def noop_on_check(self, inject):
+        return self.check
+
 class FakeConn(object):
     def __init__(self):
         self.host = None
