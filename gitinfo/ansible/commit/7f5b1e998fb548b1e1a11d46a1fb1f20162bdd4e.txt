commit 7f5b1e998fb548b1e1a11d46a1fb1f20162bdd4e
Author: Jon Pugh <jon@thinkdrop.net>
Date:   Tue May 21 11:28:17 2019 -0400

    Out put the version of the dependency so we can better understand the problem. (#56379)

diff --git a/lib/ansible/cli/galaxy.py b/lib/ansible/cli/galaxy.py
index 6d8f9ee90a..25304abad7 100644
--- a/lib/ansible/cli/galaxy.py
+++ b/lib/ansible/cli/galaxy.py
@@ -437,8 +437,8 @@ class GalaxyCLI(CLI):
                                     dep_role.remove()
                                     roles_left.append(dep_role)
                                 else:
-                                    display.warning('- dependency %s from role %s differs from already installed version (%s), skipping' %
-                                                    (to_text(dep_role), role.name, dep_role.install_info['version']))
+                                    display.warning('- dependency %s (%s) from role %s differs from already installed version (%s), skipping' %
+                                                    (to_text(dep_role), dep_role.version, role.name, dep_role.install_info['version']))
                             else:
                                 if force_deps:
                                     roles_left.append(dep_role)
