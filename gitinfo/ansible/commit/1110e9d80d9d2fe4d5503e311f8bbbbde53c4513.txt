commit 1110e9d80d9d2fe4d5503e311f8bbbbde53c4513
Author: Matt Clay <matt@mystile.com>
Date:   Sun Feb 9 11:17:33 2020 -0800

    Test AIX with smaller volumes.

diff --git a/test/lib/ansible_test/_data/setup/remote.sh b/test/lib/ansible_test/_data/setup/remote.sh
index 124924d1f1..ea6f912d4c 100644
--- a/test/lib/ansible_test/_data/setup/remote.sh
+++ b/test/lib/ansible_test/_data/setup/remote.sh
@@ -80,10 +80,10 @@ elif [ "${platform}" = "osx" ]; then
     done
 elif [ "${platform}" = "aix" ]; then
     chfs -a size=1G /
-    chfs -a size=5G /usr
+    chfs -a size=4G /usr
     chfs -a size=1G /var
     chfs -a size=1G /tmp
-    chfs -a size=5G /opt
+    chfs -a size=2G /opt
     while true; do
         yum install -q -y \
             gcc \
