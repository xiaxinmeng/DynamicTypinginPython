commit 87757ca38546613f5bee0b8a71e80781eb0c3622
Author: Ricardo Carrillo Cruz <ricardo.carrillo.cruz@gmail.com>
Date:   Thu Aug 10 16:25:38 2017 +0200

    Remove eos group_vars creds and increase eapi timeout (#28008)
    
    We don't need provider creds as we set those at inventory group vars,
    increasing eapi timeout as some eapi tests are flip flopping with
    timeouts.

diff --git a/test/integration/group_vars/eos.yaml b/test/integration/group_vars/eos.yaml
index 614a7b22f6..a301707165 100644
--- a/test/integration/group_vars/eos.yaml
+++ b/test/integration/group_vars/eos.yaml
@@ -1,16 +1,13 @@
 ---
 cli:
   host: "{{ ansible_host }}"
-  username: "{{ eos_cli_user | default('admin') }}"
-  password: "{{ eos_cli_pass | default('admin') }}"
   transport: cli
   authorize: yes
 
 eapi:
   host: "{{ ansible_host }}"
-  username: "{{ eos_eapi_user | default('admin') }}"
-  password: "{{ eos_eapi_pass | default('admin') }}"
   transport: eapi
+  timeout: 60
   use_ssl: no
   port: 80
   authorize: yes
