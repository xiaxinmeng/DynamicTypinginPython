commit bb79b16d598715992866384d3fb048765766ea3f
Author: James Cammarata <jimi@sngx.net>
Date:   Mon Feb 10 13:27:43 2014 -0600

    Specify mode in the makedirs call for fireball/accelerate keys

diff --git a/lib/ansible/utils/__init__.py b/lib/ansible/utils/__init__.py
index b38afb2fd4..49e9316d9a 100644
--- a/lib/ansible/utils/__init__.py
+++ b/lib/ansible/utils/__init__.py
@@ -89,7 +89,7 @@ def key_for_hostname(hostname):
 
     key_path = os.path.expanduser(C.ACCELERATE_KEYS_DIR)
     if not os.path.exists(key_path):
-        os.makedirs(key_path)
+        os.makedirs(key_path, mode=0700)
         os.chmod(key_path, int(C.ACCELERATE_KEYS_DIR_PERMS, 8))
     elif not os.path.isdir(key_path):
         raise errors.AnsibleError('ACCELERATE_KEYS_DIR is not a directory.')
