commit 4cd810a67487cc0f3903006bebcb6c228485443b
Author: James Cammarata <jimi@sngx.net>
Date:   Sat Sep 26 21:27:38 2015 -0400

    Only append -q option for ssh if we're using ssh

diff --git a/lib/ansible/plugins/connection/ssh.py b/lib/ansible/plugins/connection/ssh.py
index 651dc4d351..7ac4d5ce8b 100644
--- a/lib/ansible/plugins/connection/ssh.py
+++ b/lib/ansible/plugins/connection/ssh.py
@@ -128,7 +128,7 @@ class Connection(ConnectionBase):
 
         if self._play_context.verbosity > 3:
             self._command += ['-vvv']
-        else:
+        elif binary == 'ssh':
             self._command += ['-q']
 
         # Next, we add ansible_ssh_args from the inventory if it's set, or
