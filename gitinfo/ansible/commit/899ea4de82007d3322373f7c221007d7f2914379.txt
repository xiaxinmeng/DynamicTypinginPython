commit 899ea4de82007d3322373f7c221007d7f2914379
Author: Michael DeHaan <michael.dehaan@gmail.com>
Date:   Sun Jun 30 18:51:11 2013 -0400

    Styling: indentation

diff --git a/library/files/file b/library/files/file
index 22716935c8..5fbb9ecc2d 100644
--- a/library/files/file
+++ b/library/files/file
@@ -134,9 +134,9 @@ EXAMPLES = '''
 def dolink(src, path, state, module):
     try:
         if state == 'hard':
-           os.link(src,path)
+            os.link(src,path)
         else:
-           os.symlink(src, path)
+            os.symlink(src, path)
     except OSError, e:
         module.fail_json(path=path, msg='Error while linking: %s' % str(e))
 
