commit d05df5fc619cb8f5240f6fda166c1e423d37742d
Author: Matt Clay <matt@mystile.com>
Date:   Fri Sep 23 22:09:46 2016 -0700

    Disable expect continue in ansible-core-ci. (#17737)
    
    This will allow use of larger SSH keys.

diff --git a/test/utils/shippable/ansible-core-ci b/test/utils/shippable/ansible-core-ci
index 44fd1df105..e91b426ad0 100755
--- a/test/utils/shippable/ansible-core-ci
+++ b/test/utils/shippable/ansible-core-ci
@@ -334,9 +334,13 @@ class HttpRequest:
     def request(self, method, url, data=None, headers=None):
         args = ['/usr/bin/curl', '-s', '-S', '-i', '-X', method]
 
-        if headers is not None:
-            for header in headers:
-                args += ['-H', '%s: %s' % (header, headers[header])]
+        if headers is None:
+            headers = {}
+
+        headers['Expect'] = ''  # don't send expect continue header
+
+        for header in headers:
+            args += ['-H', '%s: %s' % (header, headers[header])]
 
         if data is not None:
             args += ['-d', data]
