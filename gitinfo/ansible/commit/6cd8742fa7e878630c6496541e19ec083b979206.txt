commit 6cd8742fa7e878630c6496541e19ec083b979206
Author: Matt Clay <matt@mystile.com>
Date:   Fri Dec 7 00:24:51 2018 -0800

    Add virtualenv symlink for RHEL 8.
    
    Also enable template_jinja2_latest now that it passes.

diff --git a/test/integration/targets/template_jinja2_latest/aliases b/test/integration/targets/template_jinja2_latest/aliases
index 8e6e9cb4cc..9af2d79042 100644
--- a/test/integration/targets/template_jinja2_latest/aliases
+++ b/test/integration/targets/template_jinja2_latest/aliases
@@ -1,3 +1,2 @@
 needs/root
 shippable/posix/group2
-skip/rhel8.0
diff --git a/test/runner/setup/remote.sh b/test/runner/setup/remote.sh
index 6100c16914..3d0c8cdf13 100644
--- a/test/runner/setup/remote.sh
+++ b/test/runner/setup/remote.sh
@@ -53,6 +53,9 @@ elif [ "${platform}" = "rhel" ]; then
         if [ ! -f /usr/bin/pip ]; then
             ln -s /usr/bin/pip3 /usr/bin/pip
         fi
+        if [ ! -f /usr/bin/virtualenv ]; then
+            ln -s /usr/bin/virtualenv-3 /usr/bin/virtualenv
+        fi
     else
         while true; do
             yum install -y \
