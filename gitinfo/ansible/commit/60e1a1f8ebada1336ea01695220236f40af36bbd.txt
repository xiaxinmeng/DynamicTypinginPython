commit 60e1a1f8ebada1336ea01695220236f40af36bbd
Author: James Cammarata <jimi@sngx.net>
Date:   Fri Sep 25 17:16:35 2015 -0400

    Fix bug in nested lookup where pop is called indiscriminately

diff --git a/lib/ansible/plugins/lookup/nested.py b/lib/ansible/plugins/lookup/nested.py
index 2593aa5d9f..8cf89b435d 100644
--- a/lib/ansible/plugins/lookup/nested.py
+++ b/lib/ansible/plugins/lookup/nested.py
@@ -27,7 +27,7 @@ class LookupModule(LookupBase):
 
     def _lookup_variables(self, terms, variables):
         foo = variables.copy()
-        foo.pop('vars')
+        foo.pop('vars', None)
         results = []
         for x in terms:
             try:
