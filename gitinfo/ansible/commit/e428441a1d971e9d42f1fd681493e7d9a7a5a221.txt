commit e428441a1d971e9d42f1fd681493e7d9a7a5a221
Author: abyss <abhineet.ayan.verma@gmail.com>
Date:   Thu Mar 14 18:27:20 2019 +0530

    pids: case insensitive string comparison for process names (#52564)

diff --git a/lib/ansible/modules/system/pids.py b/lib/ansible/modules/system/pids.py
index fe861998b4..80b48a1a2d 100644
--- a/lib/ansible/modules/system/pids.py
+++ b/lib/ansible/modules/system/pids.py
@@ -53,7 +53,7 @@ except ImportError:
 
 
 def get_pid(name):
-    return [p.info['pid'] for p in psutil.process_iter(attrs=['pid', 'name']) if name == p.info['name']]
+    return [p.info['pid'] for p in psutil.process_iter(attrs=['pid', 'name']) if p.info and p.info.get('name', None) and p.info['name'].lower() == name.lower()]
 
 
 def main():
