commit aeea46678da13eccab408970368189d43c3b0140
Author: Michael DeHaan <michael.dehaan@gmail.com>
Date:   Wed Mar 14 19:09:44 2012 -0400

    Fix the copy module to use the argsfile method

diff --git a/library/copy b/library/copy
index 288929a89a..632df96648 100755
--- a/library/copy
+++ b/library/copy
@@ -32,8 +32,14 @@ except ImportError:
 # to a dictionary
 # FIXME: make more idiomatic
 
-args = " ".join(sys.argv[1:])
-items = shlex.split(args)
+if len(sys.argv) == 1:
+    sys.exit(1)
+argfile = sys.argv[1]
+if not os.path.exists(argfile):
+    sys.exit(1)
+items = shlex.split(open(argfile, 'r').read())
+
+
 params = {}
 for x in items:
     (k, v) = x.split("=")
