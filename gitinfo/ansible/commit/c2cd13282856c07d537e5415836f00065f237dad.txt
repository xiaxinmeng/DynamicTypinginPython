commit c2cd13282856c07d537e5415836f00065f237dad
Author: Andrea Scarpino <me@andreascarpino.it>
Date:   Wed Oct 31 21:04:29 2018 +0100

    Cleanup INVENTORY_IGNORE_EXTS (#47880)
    
    BLACKLIST_EXTS already contains tilde

diff --git a/lib/ansible/config/base.yml b/lib/ansible/config/base.yml
index 0f35fea10b..188fb7c465 100644
--- a/lib/ansible/config/base.yml
+++ b/lib/ansible/config/base.yml
@@ -1435,7 +1435,7 @@ INVENTORY_EXPORT:
   type: bool
 INVENTORY_IGNORE_EXTS:
   name: Inventory ignore extensions
-  default: "{{(BLACKLIST_EXTS + ( '~', '.orig', '.ini', '.cfg', '.retry'))}}"
+  default: "{{(BLACKLIST_EXTS + ( '.orig', '.ini', '.cfg', '.retry'))}}"
   description: List of extensions to ignore when using a directory as an inventory source
   env: [{name: ANSIBLE_INVENTORY_IGNORE}]
   ini:
