commit a95894dfcb2077d1281cc92a494f8d8517087de1
Author: mikedlr <hubby.hilorg@xoxy.net>
Date:   Fri Dec 8 20:48:09 2017 +0000

    ec2_vpc_route_table - unmask exceptions during route create/delete (#32256)

diff --git a/lib/ansible/modules/cloud/amazon/ec2_vpc_route_table.py b/lib/ansible/modules/cloud/amazon/ec2_vpc_route_table.py
index 8fa3d875a4..62c55de580 100644
--- a/lib/ansible/modules/cloud/amazon/ec2_vpc_route_table.py
+++ b/lib/ansible/modules/cloud/amazon/ec2_vpc_route_table.py
@@ -397,6 +397,8 @@ def ensure_routes(vpc_conn, route_table, route_specs, propagating_vgw_ids,
             except EC2ResponseError as e:
                 if e.error_code == 'DryRunOperation':
                     pass
+                else:
+                    raise
 
         for route_spec in route_specs_to_create:
             try:
@@ -406,6 +408,8 @@ def ensure_routes(vpc_conn, route_table, route_specs, propagating_vgw_ids,
             except EC2ResponseError as e:
                 if e.error_code == 'DryRunOperation':
                     pass
+                else:
+                    raise
 
         for route_spec in route_specs_to_recreate:
             if not check_mode:
