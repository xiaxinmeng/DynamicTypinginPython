commit 8e0560f41c6bc72a0ba9b0d74bc09a53f429a338
Author: Ludovic Claude <ludovic.claude@lbpoker.com>
Date:   Mon Jul 16 18:28:32 2012 +0200

    Document sudo_user

diff --git a/rst/playbooks.rst b/rst/playbooks.rst
index fffbdf4ec8..6c63e995c9 100644
--- a/rst/playbooks.rst
+++ b/rst/playbooks.rst
@@ -79,6 +79,7 @@ Support for running things from sudo is also available::
     - hosts: webservers
       user: yourname
       sudo: True
+      sudo_user: privileged_user
 
 If you need to specify a password to sudo, run `ansible-playbook` with ``--ask-sudo-pass`` (`-K`).
 If you run a sudo playbook and the playbook seems to hang, it's probably stuck at the sudo prompt.
