commit bccf6b85f2f009c91a5e6e2a9564c5a233518317
Author: Matt Clay <matt@mystile.com>
Date:   Thu Jun 18 10:54:47 2020 -0700

    Change comprehension to `list()` in plugin loader.

diff --git a/changelogs/fragments/plugin-loader-cleanup.yml b/changelogs/fragments/plugin-loader-cleanup.yml
new file mode 100644
index 0000000000..9fca422e23
--- /dev/null
+++ b/changelogs/fragments/plugin-loader-cleanup.yml
@@ -0,0 +1,2 @@
+minor_changes:
+  - Minor code cleanup in plugin loader.
diff --git a/lib/ansible/plugins/loader.py b/lib/ansible/plugins/loader.py
index 249aebd7dd..49b85e57ac 100644
--- a/lib/ansible/plugins/loader.py
+++ b/lib/ansible/plugins/loader.py
@@ -981,7 +981,7 @@ class Jinja2Loader(PluginLoader):
 
         # We have to instantiate a list of all plugins so that we can reverse it.  We reverse it so
         # that calling code will deduplicate this correctly.
-        plugins = [p for p in super(Jinja2Loader, self).all(*args, **kwargs)]
+        plugins = list(super(Jinja2Loader, self).all(*args, **kwargs))
         plugins.reverse()
 
         return plugins
