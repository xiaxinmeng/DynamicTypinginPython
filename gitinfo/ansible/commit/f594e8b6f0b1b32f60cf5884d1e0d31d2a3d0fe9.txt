commit f594e8b6f0b1b32f60cf5884d1e0d31d2a3d0fe9
Author: patrickheeney <patrickheeney@gmail.com>
Date:   Tue Mar 18 11:17:20 2014 -0700

    Fix domain name in slack module

diff --git a/library/notification/slack b/library/notification/slack
index df069375a8..176d6b338f 100644
--- a/library/notification/slack
+++ b/library/notification/slack
@@ -50,7 +50,7 @@ options:
     default: ansible
   icon_url:
     description:
-      - Url for the message sender's icon (default C(http://www.ansibleworks.com/favicon.ico))
+      - Url for the message sender's icon (default C(http://www.ansible.com/favicon.ico))
     required: false
   icon_emoji:
     description:
@@ -143,7 +143,7 @@ def main():
             msg         = dict(type='str', required=True),
             channel     = dict(type='str', default=None),
             username    = dict(type='str', default='Ansible'),
-            icon_url    = dict(type='str', default='http://www.ansibleworks.com/favicon.ico'),
+            icon_url    = dict(type='str', default='http://www.ansible.com/favicon.ico'),
             icon_emoji  = dict(type='str', default=None),
             link_names  = dict(type='int', default=1, choices=[0,1]),
             parse       = dict(type='str', default=None, choices=['none', 'full']),
