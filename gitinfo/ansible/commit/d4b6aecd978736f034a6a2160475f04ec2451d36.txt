commit d4b6aecd978736f034a6a2160475f04ec2451d36
Author: Dag Wieers <dag@wieers.com>
Date:   Thu Nov 8 00:09:18 2012 +0100

    Fix broken RPM names due to static _rpmfilename

diff --git a/Makefile b/Makefile
index 4afb6404e0..fd4b5f6adc 100644
--- a/Makefile
+++ b/Makefile
@@ -145,7 +145,7 @@ rpm: rpmcommon
 	--define "_srcrpmdir %{_topdir}" \
 	--define "_specdir $(RPMSPECDIR)" \
 	--define "_sourcedir %{_topdir}" \
-	--define "_rpmfilename $(RPMNVR).%%{ARCH}.rpm" \
+	--define "_rpmfilename %%{NAME}-%%{VERSION}-%%{RELEASE}.%%{ARCH}.rpm" \
 	-ba rpm-build/$(NAME).spec
 	@rm -f rpm-build/$(NAME).spec
 	@echo "#############################################"
