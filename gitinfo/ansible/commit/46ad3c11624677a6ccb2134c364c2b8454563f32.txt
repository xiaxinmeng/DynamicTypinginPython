commit 46ad3c11624677a6ccb2134c364c2b8454563f32
Author: Ethan <smithe2413@gmail.com>
Date:   Thu Jul 2 10:59:01 2020 -0400

    Update pull.py (#70393)

diff --git a/lib/ansible/cli/pull.py b/lib/ansible/cli/pull.py
index 55951c5989..55b5a2b0fd 100644
--- a/lib/ansible/cli/pull.py
+++ b/lib/ansible/cli/pull.py
@@ -29,7 +29,7 @@ display = Display()
 
 
 class PullCLI(CLI):
-    ''' is used to up a remote copy of ansible on each managed node,
+    ''' Used to pull a remote copy of ansible on each managed node,
         each set to run via cron and update playbook source via a source repository.
         This inverts the default *push* architecture of ansible into a *pull* architecture,
         which has near-limitless scaling potential.
