commit b2542bff72d8b4f5adeec3e0bf6c14c64aaa7e18
Author: Scott Sturdivant <scott.sturdivant@absio.com>
Date:   Thu May 23 10:56:52 2013 -0600

    Chdir to tempdir to prevent permissions problems.

diff --git a/library/packaging/pip b/library/packaging/pip
index cc16556848..ccdec75834 100644
--- a/library/packaging/pip
+++ b/library/packaging/pip
@@ -201,6 +201,7 @@ def main():
                 cmd = '%s --system-site-packages %s' % (virtualenv, env)
             else:
                 cmd = '%s %s' % (virtualenv, env)
+            os.chdir(tempfile.gettempdir())
             rc, out_venv, err_venv = module.run_command(cmd)
             out += out_venv
             err += err_venv
