commit 8a016bb374b63e2ae4a267a58501d3f735a8b490
Author: James Cammarata <jimi@sngx.net>
Date:   Tue Sep 3 11:30:29 2013 -0500

    Adding the facility to the accelerate daemon syslog calls

diff --git a/library/utilities/accelerate b/library/utilities/accelerate
index 14ed00d9c0..4151f6fc23 100644
--- a/library/utilities/accelerate
+++ b/library/utilities/accelerate
@@ -86,7 +86,7 @@ PIDFILE = os.path.expanduser("~/.accelerate.pid")
 CHUNK_SIZE=10240
 
 def log(msg):
-    syslog.syslog(syslog.LOG_NOTICE, msg)
+    syslog.syslog(syslog.LOG_NOTICE|syslog.LOG_DAEMON, msg)
 
 if os.path.exists(PIDFILE):
     try:
