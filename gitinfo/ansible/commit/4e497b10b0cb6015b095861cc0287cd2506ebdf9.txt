commit 4e497b10b0cb6015b095861cc0287cd2506ebdf9
Author: Peter Sprygada <psprygada@ansible.com>
Date:   Mon Apr 4 08:10:22 2016 -0400

    bugfix in netcfg to handle multilevel commands
    
    this fixes a bug where netcfg would not properly find a statement that was
    more than one level deep

diff --git a/lib/ansible/module_utils/netcfg.py b/lib/ansible/module_utils/netcfg.py
index a94b1c2512..2deaba8c0f 100644
--- a/lib/ansible/module_utils/netcfg.py
+++ b/lib/ansible/module_utils/netcfg.py
@@ -174,7 +174,8 @@ class NetworkConfig(object):
     def get_object(self, path):
         for item in self.items:
             if item.text == path[-1]:
-                if item.parents == path[:-1]:
+                parents = [p.text for p in item.parents]
+                if parents == path[:-1]:
                     return item
 
     def get_children(self, path):
