commit 8257ee3e25a163716886e6f1adbe22446691d8d0
Author: jonathanbouvier <jonathan.bouvier@gmail.com>
Date:   Wed Jan 25 06:47:37 2017 -0500

    Fix issue with cc and bcc getting added to mail if unset (#20610)

diff --git a/lib/ansible/plugins/callback/mail.py b/lib/ansible/plugins/callback/mail.py
index 750c88c1aa..68a43ab3f1 100644
--- a/lib/ansible/plugins/callback/mail.py
+++ b/lib/ansible/plugins/callback/mail.py
@@ -58,9 +58,9 @@ def mail(subject='Ansible error mail', sender=None, to=None, cc=None, bcc=None,
     b_content += b_body
 
     b_addresses = b_to.split(b',')
-    if b_cc:
+    if cc:
         b_addresses += b_cc.split(b',')
-    if b_bcc:
+    if bcc:
         b_addresses += b_bcc.split(b',')
 
     for b_address in b_addresses:
