commit 4ad885111b878a69d23a0b4d7ea495ed355e8cc6
Author: Michael DeHaan <michael.dehaan@gmail.com>
Date:   Thu Mar 15 22:04:32 2012 -0400

    Disable debug

diff --git a/library/file b/library/file
index 725189a56e..c9d0aaadc5 100755
--- a/library/file
+++ b/library/file
@@ -32,7 +32,8 @@ import pwd
 
 def debug(msg):
     # ansible ignores stderr, so it's safe to use for debug
-    print >>sys.stderr, msg
+    # print >>sys.stderr, msg
+    pass
 
 def exit_json(rc=0, **kwargs):
     # FIXME: if path exists, include the user, group, mode and context
