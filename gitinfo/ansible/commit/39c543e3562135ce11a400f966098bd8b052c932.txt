commit 39c543e3562135ce11a400f966098bd8b052c932
Author: Toshio Kuratomi <a.badger@gmail.com>
Date:   Wed Jun 22 05:42:37 2016 -0700

    Use find -exec + so that failures will be visible in find's exit code (#16389)
    
    Fixes #16385

diff --git a/lib/ansible/plugins/shell/__init__.py b/lib/ansible/plugins/shell/__init__.py
index 3fb0211335..9bd92562b5 100644
--- a/lib/ansible/plugins/shell/__init__.py
+++ b/lib/ansible/plugins/shell/__init__.py
@@ -93,7 +93,7 @@ class ShellBase(object):
 
         cmd = ['setfacl', '-m', 'u:%s:%s' % (user, mode)]
         if recursive:
-            cmd = ['find', path, '-exec'] + cmd + ["'{}'", "';'"]
+            cmd = ['find', path, '-exec'] + cmd + ["'{}'", "'+'"]
         else:
             cmd.append(path)
 
