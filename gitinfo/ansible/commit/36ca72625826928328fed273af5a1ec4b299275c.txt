commit 36ca72625826928328fed273af5a1ec4b299275c
Author: James Tanner <tanner.jc@gmail.com>
Date:   Fri Nov 8 09:57:22 2013 -0500

    Fixes #4522 Document that comments in ini files will be discarded with the ini module

diff --git a/library/files/ini_file b/library/files/ini_file
index 7634a3b696..2c29402f96 100644
--- a/library/files/ini_file
+++ b/library/files/ini_file
@@ -27,6 +27,8 @@ description:
      - Manage (add, remove, change) individual settings in an INI-style file without having
        to manage the file as a whole with, say, M(template) or M(assemble). Adds missing
        sections if they don't exist.
+     - Comments are discarded when the source file is read, and therefore will not 
+       show up in the destination file.
 version_added: "0.9"
 options:
   dest:
