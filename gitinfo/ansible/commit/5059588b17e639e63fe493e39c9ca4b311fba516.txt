commit 5059588b17e639e63fe493e39c9ca4b311fba516
Author: Brendan Beveridge <brendan.beveridge@zeptonics.com>
Date:   Tue May 8 09:49:37 2012 +1000

    remove now useless append check in user_mod

diff --git a/library/user b/library/user
index efb4b36fde..0e78778377 100755
--- a/library/user
+++ b/library/user
@@ -173,10 +173,6 @@ def user_mod(user, **kwargs):
             if kwargs[key] is not None and info[1] != kwargs[key]:
                 cmd.append('-p')
                 cmd.append(kwargs[key])
-        elif key == 'append':
-            if kwargs[key] is not None and kwargs[key] == 'yes':
-                if 'groups' in kwargs and kwargs['groups'] is not None:
-                    cmd.append('-a')
     # skip if no changes to be made
     if len(cmd) == 1:
         return False
