commit e9a356bbde2aed82e36886c6ca10ace57f5b3318
Author: Yvan Cottyn <vanten@me.com>
Date:   Fri Nov 9 14:28:21 2012 +0100

    Fix exception catching for Python 2.4

diff --git a/library/mysql_user b/library/mysql_user
index e30fde8ee6..7114d7bbd6 100755
--- a/library/mysql_user
+++ b/library/mysql_user
@@ -283,7 +283,7 @@ def main():
         else:
             db_connection = MySQLdb.connect(host=module.params["login_host"], user=login_user, passwd=login_password, db="mysql")
         cursor = db_connection.cursor()
-    except Exception as e:
+    except Exception, e:
         module.fail_json(msg="unable to connect to database, check login_user and login_password are correct or ~/.my.cnf has the credentials")
 
     if state == "present":
