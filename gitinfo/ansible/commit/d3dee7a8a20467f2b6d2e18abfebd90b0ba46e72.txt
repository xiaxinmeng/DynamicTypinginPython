commit d3dee7a8a20467f2b6d2e18abfebd90b0ba46e72
Author: Serge van Ginderachter <serge@vanginderachter.be>
Date:   Thu Apr 11 23:39:01 2013 +0200

    add default=yes in upgrade argument spec

diff --git a/library/apt b/library/apt
index fdaaea86bd..69d7158815 100644
--- a/library/apt
+++ b/library/apt
@@ -234,7 +234,7 @@ def main():
             default_release = dict(default=None, aliases=['default-release']),
             install_recommends = dict(default='yes', aliases=['install-recommends'], type='bool'),
             force = dict(default='no', type='bool'),
-            upgrade = dict(choices=['yes', 'safe', 'full', 'dist'])
+            upgrade = dict(default='yes', choices=['yes', 'safe', 'full', 'dist'])
         ),
         mutually_exclusive = [['package', 'upgrade']],
         required_one_of = [['package', 'upgrade', 'update_cache']],
