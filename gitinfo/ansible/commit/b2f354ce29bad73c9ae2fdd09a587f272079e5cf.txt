commit b2f354ce29bad73c9ae2fdd09a587f272079e5cf
Author: Matt Clay <matt@mystile.com>
Date:   Thu Feb 21 13:59:56 2019 -0800

    Ignore errors during container cleanup in CI.

diff --git a/test/utils/shippable/shippable.sh b/test/utils/shippable/shippable.sh
index 739ff9e9fe..e9beaba9e7 100755
--- a/test/utils/shippable/shippable.sh
+++ b/test/utils/shippable/shippable.sh
@@ -14,7 +14,7 @@ docker images quay.io/ansible/*
 docker ps
 
 for container in $(docker ps --format '{{.Image}} {{.ID}}' | grep -v '^drydock/' | sed 's/^.* //'); do
-    docker rm -f "${container}"
+    docker rm -f "${container}" || true  # ignore errors
 done
 
 docker ps
