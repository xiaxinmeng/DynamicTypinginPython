commit 458268f4cbc34e2a56f26261eb21a4926baf434b
Author: Mark Chappell <mchappel@redhat.com>
Date:   Mon Dec 30 09:38:20 2019 +0100

    AWS Guidelines: Make 'security_token' optional so that they can be run outside of CI (#66107)

diff --git a/docs/docsite/rst/dev_guide/platforms/aws_guidelines.rst b/docs/docsite/rst/dev_guide/platforms/aws_guidelines.rst
index 0bbb942cc0..698cbec46b 100644
--- a/docs/docsite/rst/dev_guide/platforms/aws_guidelines.rst
+++ b/docs/docsite/rst/dev_guide/platforms/aws_guidelines.rst
@@ -649,7 +649,7 @@ for every call, it's preferable to use :ref:`module_defaults <module_defaults>`.
        group/aws:
          aws_access_key: "{{ aws_access_key }}"
          aws_secret_key: "{{ aws_secret_key }}"
-         security_token: "{{ security_token }}"
+         security_token: "{{ security_token | default(omit) }}"
          region: "{{ aws_region }}"
 
      block:
