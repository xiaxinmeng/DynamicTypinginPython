commit e1f1722e9ef38e357999cca247cfe2c74dedf37e
Author: Michael DeHaan <michael.dehaan@gmail.com>
Date:   Sat Feb 23 12:36:01 2013 -0500

    Docs tweak to setup module.

diff --git a/library/setup b/library/setup
index fa98a89418..30c05763de 100644
--- a/library/setup
+++ b/library/setup
@@ -33,9 +33,9 @@ module: setup
 short_description: Gathers facts about remote hosts
 options:
     filter:
+        version_added: 1.1
         description:
-            - a filter that will be applied to the keys; only key matching filter will
-            be returned. The filter should be a shell-style wildcard.
+            - if supplied, only return facts that match this shell-style (fnmatch) wildcard.
         required: false
         default: *
 description:
