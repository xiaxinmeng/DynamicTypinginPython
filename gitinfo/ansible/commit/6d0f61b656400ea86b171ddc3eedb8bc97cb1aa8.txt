commit 6d0f61b656400ea86b171ddc3eedb8bc97cb1aa8
Author: ppanczyk <pmpanczyk@gmail.com>
Date:   Wed Dec 14 09:13:46 2016 +0100

    Fixed expiredate parameter in user module
    
    Changed "--expiredate" to "-e", as on older systems "--expiredate" is not present.

diff --git a/lib/ansible/modules/system/user.py b/lib/ansible/modules/system/user.py
index 6158ad3bbe..cbaf5b9b4f 100644
--- a/lib/ansible/modules/system/user.py
+++ b/lib/ansible/modules/system/user.py
@@ -402,7 +402,7 @@ class User(object):
             cmd.append(self.shell)
 
         if self.expires:
-            cmd.append('--expiredate')
+            cmd.append('-e')
             cmd.append(time.strftime(self.DATE_FORMAT, self.expires))
 
         if self.password is not None:
@@ -515,7 +515,7 @@ class User(object):
             cmd.append(self.shell)
 
         if self.expires:
-            cmd.append('--expiredate')
+            cmd.append('-e')
             cmd.append(time.strftime(self.DATE_FORMAT, self.expires))
 
         if self.update_password == 'always' and self.password is not None and info[1] != self.password:
