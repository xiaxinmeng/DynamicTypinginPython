commit ecc2b6e74257bce09a61cf5f4942fd59ea394dfe
Author: Michael DeHaan <michael.dehaan@gmail.com>
Date:   Sun Jun 30 18:55:45 2013 -0400

    Styling: deprecation

diff --git a/library/packaging/rhn_register b/library/packaging/rhn_register
index 1a64041a59..defed9f618 100644
--- a/library/packaging/rhn_register
+++ b/library/packaging/rhn_register
@@ -172,7 +172,7 @@ class Rhn(RegistrationBase):
         # configuration.  Yeah, I know this should be subclassed ... but, oh
         # well
         def get_option_default(self, key, default=''):
-            if self.has_key(key):
+            if 'key' in self:
                 return self[key]
             else:
                 return default
