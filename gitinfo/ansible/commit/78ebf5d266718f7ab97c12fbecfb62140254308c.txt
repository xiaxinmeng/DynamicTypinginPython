commit 78ebf5d266718f7ab97c12fbecfb62140254308c
Author: Michael DeHaan <michael.dehaan@gmail.com>
Date:   Wed Mar 12 17:44:53 2014 -0400

    Still more shell fixes.

diff --git a/library/packaging/apt_key b/library/packaging/apt_key
index 48442349ae..e071833c3b 100644
--- a/library/packaging/apt_key
+++ b/library/packaging/apt_key
@@ -131,7 +131,7 @@ def all_keys(module, keyring):
     return results
 
 def key_present(module, key_id):
-    (rc, out, err) = module.run_command("apt-key list | 2>&1 grep -i -q %s" % key_id)
+    (rc, out, err) = module.run_command("apt-key list | 2>&1 grep -i -q %s" % pipes.quote(key_id), use_unsafe_shell=True)
     return rc == 0
 
 def download_key(module, url):
