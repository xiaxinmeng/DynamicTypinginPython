commit d2abfb9ff820ac0eb0331aa1c10523a68efd2490
Author: Daniel Hokka Zakrisson <daniel@hozac.com>
Date:   Sun Nov 18 18:36:37 2012 +0100

    Ensure runner adds its basedir, so ansible can benefit from plugins

diff --git a/lib/ansible/runner/__init__.py b/lib/ansible/runner/__init__.py
index 3a151a868c..257ad3346c 100644
--- a/lib/ansible/runner/__init__.py
+++ b/lib/ansible/runner/__init__.py
@@ -151,6 +151,8 @@ class Runner(object):
         if self.transport == 'local':
             self.remote_user = pwd.getpwuid(os.geteuid())[0]
 
+        utils.plugins.push_basedir(self.basedir)
+
         # ensure we are using unique tmp paths
         random.seed()
 
