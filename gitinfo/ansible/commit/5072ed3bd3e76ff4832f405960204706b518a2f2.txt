commit 5072ed3bd3e76ff4832f405960204706b518a2f2
Author: Matthew Johnson <mjohnson@blackboard.com>
Date:   Wed Sep 19 05:56:55 2012 -0400

    date parsing for OSX is BSD-like

diff --git a/Makefile b/Makefile
index a8aa8250bb..80faa51342 100644
--- a/Makefile
+++ b/Makefile
@@ -37,8 +37,12 @@ endif
 ifeq ($(OS), FreeBSD)
 DATE := $(shell date -j -f "%Y-%m-%d %H:%M:%s"  "$(GIT_DATE)" +%Y%m%d%H%M)
 else
+ifeq ($(OS), Darwin)
+DATE := $(shell date -j -f "%Y-%m-%d %H:%M:%S"  "$(GIT_DATE)" +%Y%m%d%H%M)
+else
 DATE := $(shell date --date="$(GIT_DATE)" +%Y%m%d%H%M)
 endif
+endif
 
 # RPM build parameters
 RPMSPECDIR= packaging/rpm
