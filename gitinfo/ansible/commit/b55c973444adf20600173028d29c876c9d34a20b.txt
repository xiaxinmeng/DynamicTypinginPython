commit b55c973444adf20600173028d29c876c9d34a20b
Author: Nijin Ashok <nashok@redhat.com>
Date:   Sun Oct 30 17:56:45 2016 +0530

    Fix issue in activating the VM disk while attaching disk
    
    Currently <active> tag is passed within the disk element which is
    incorrect. As a result, disk will remain inactive even though the
    default option is true.

diff --git a/lib/ansible/modules/extras/cloud/ovirt/ovirt_vms.py b/lib/ansible/modules/extras/cloud/ovirt/ovirt_vms.py
index 0652b24c10..0cda1f6296 100644
--- a/lib/ansible/modules/extras/cloud/ovirt/ovirt_vms.py
+++ b/lib/ansible/modules/extras/cloud/ovirt/ovirt_vms.py
@@ -532,8 +532,8 @@ class VmsModule(BaseModule):
                         otypes.DiskAttachment(
                             disk=otypes.Disk(
                                 id=disk_id,
-                                active=disk.get('activate', True),
                             ),
+                            active=disk.get('activate', True),
                             interface=otypes.DiskInterface(
                                 disk.get('interface', 'virtio')
                             ),
