commit 9d9863fec1b9990fc7d9edfab5b65ebd9a79fab4
Author: yanzhangi <51999930+yanzhangi@users.noreply.github.com>
Date:   Wed Sep 18 09:56:44 2019 +0800

    Update ce_ospf to fix bugs (#61684)

diff --git a/lib/ansible/modules/network/cloudengine/ce_ospf.py b/lib/ansible/modules/network/cloudengine/ce_ospf.py
index 2569281f8e..cb1b7fdc59 100644
--- a/lib/ansible/modules/network/cloudengine/ce_ospf.py
+++ b/lib/ansible/modules/network/cloudengine/ce_ospf.py
@@ -514,9 +514,6 @@ class OSPF(object):
                 for network in area.get("networks"):
                     if network["ipAddress"] == self.addr and network["wildcardMask"] == self.get_wildcard_mask():
                         return True
-            else:
-                break
-
         return False
 
     def is_nexthop_exist(self):
@@ -890,7 +887,8 @@ class OSPF(object):
         self.end_state["max_load_balance"] = ospf_info.get("maxLoadBalancing")
 
         if self.end_state == self.existing:
-            self.changed = False
+            if not self.auth_text_simple and not self.auth_text_md5:
+                self.changed = False
 
     def work(self):
         """worker"""
