commit c37fa5ddce75b15838d16c0ef57835f5b6f7e411
Author: James Tanner <tanner.jc@gmail.com>
Date:   Fri Dec 20 12:13:22 2013 -0500

    Use os.pathsep to split roles_path instead of a colon

diff --git a/bin/ansible-galaxy b/bin/ansible-galaxy
index 3ed74be998..9b00e750a2 100755
--- a/bin/ansible-galaxy
+++ b/bin/ansible-galaxy
@@ -199,8 +199,8 @@ def get_opt(options, k, defval=""):
     except:
         return defval
     if k == "roles_path":
-        if ":" in data:
-            data = data.split(':')[0]
+        if os.pathsep in data:
+            data = data.split(os.pathsep)[0]
     return data
 
 def exit_without_ignore(options, rc=1):
