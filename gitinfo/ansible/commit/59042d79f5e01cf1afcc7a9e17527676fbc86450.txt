commit 59042d79f5e01cf1afcc7a9e17527676fbc86450
Author: Alexander Lex <35256191+osLEccse@users.noreply.github.com>
Date:   Tue Jan 16 16:57:11 2018 +0100

    Update developing_api.rst to use correct inventory format for single explicit host (#34919)
    
    Fix for #34918

diff --git a/docs/docsite/rst/dev_guide/developing_api.rst b/docs/docsite/rst/dev_guide/developing_api.rst
index ce2a21955a..0ad3f2fdf8 100644
--- a/docs/docsite/rst/dev_guide/developing_api.rst
+++ b/docs/docsite/rst/dev_guide/developing_api.rst
@@ -76,7 +76,8 @@ In 2.0 things get a bit more complicated to start, but you end up with much more
     results_callback = ResultCallback()
 
     # create inventory and pass to var manager
-    inventory = InventoryManager(loader=loader, sources=['localhost'])
+    # use path to host config file as source or hosts in a comma separated string
+    inventory = InventoryManager(loader=loader, sources='localhost,')
     variable_manager = VariableManager(loader=loader, inventory=inventory)
 
     # create play with tasks
