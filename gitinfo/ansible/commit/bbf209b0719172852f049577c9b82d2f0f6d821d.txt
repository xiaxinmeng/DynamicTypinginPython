commit bbf209b0719172852f049577c9b82d2f0f6d821d
Author: Scott Butler <sbutler@redhat.com>
Date:   Fri Sep 2 12:26:33 2016 -0700

    Added semicolon to invalid operators list redux.

diff --git a/lib/ansible/modules/commands/command.py b/lib/ansible/modules/commands/command.py
index ca4d7d82bf..c80372df0c 100644
--- a/lib/ansible/modules/commands/command.py
+++ b/lib/ansible/modules/commands/command.py
@@ -29,7 +29,7 @@ description:
      - The M(command) module takes the command name followed by a list of space-delimited arguments.
      - The given command will be executed on all selected nodes. It will not be
        processed through the shell, so variables like C($HOME) and operations
-       like C("<"), C(">"), C("|"), and C("&") will not work (use the M(shell)
+       like C("<"), C(">"), C("|"), C(";") and C("&") will not work (use the M(shell)
        module if you need these features).
 options:
   free_form:
