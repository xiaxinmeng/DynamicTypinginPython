commit 9272e3d5474f14314b21b673f2d7384a59b59fbd
Author: ad1rie1 <ad1rie3@hotmail.fr>
Date:   Tue Jan 14 17:08:38 2020 +0100

    Fix Bug (#64553)

diff --git a/lib/ansible/plugins/action/telnet.py b/lib/ansible/plugins/action/telnet.py
index 785681e0a2..3fd68eb704 100644
--- a/lib/ansible/plugins/action/telnet.py
+++ b/lib/ansible/plugins/action/telnet.py
@@ -75,7 +75,7 @@ class ActionModule(ActionBase):
                     for cmd in commands:
                         display.vvvvv('>>> %s' % cmd)
                         tn.write(to_bytes(cmd + "\n"))
-                        index, match, out = tn.expect(list(map(to_bytes, prompts)))
+                        index, match, out = tn.expect(list(map(to_bytes, prompts)), timeout=timeout)
                         display.vvvvv('<<< %s' % cmd)
                         output.append(out)
                         sleep(pause)
