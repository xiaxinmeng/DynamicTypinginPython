commit e60d7cc8404fe8e8077105aad9a07a69c62bd506
Author: Brian Coca <bcoca@ansible.com>
Date:   Wed Dec 23 12:00:36 2015 -0500

    added examples to route53_zone
    
    fixes #1061

diff --git a/lib/ansible/modules/extras/cloud/amazon/route53_zone.py b/lib/ansible/modules/extras/cloud/amazon/route53_zone.py
index d5ba0dcd61..d3cfd567e7 100644
--- a/lib/ansible/modules/extras/cloud/amazon/route53_zone.py
+++ b/lib/ansible/modules/extras/cloud/amazon/route53_zone.py
@@ -50,6 +50,29 @@ extends_documentation_fragment: aws
 author: "Christopher Troup (@minichate)"
 '''
 
+EXAMPLES = '''
+# create a public zone
+- route53_zone: zone=example.com state=present comment="this is an example"
+
+# delete a public zone
+- route53_zone: zone=example.com state=absent
+
+- name: private zone for devel
+  route53_zome: zone=devel.example.com state=present vpc_id={{myvpc_id}} comment='developer domain'
+
+# more complex example
+- name: register output after creating zone in parameterized region
+  route53_zone:
+    vpc_id: "{{ vpc.vpc_id }}"
+    vpc_region: "{{ ec2_region }}"
+    zone: "{{ vpc_dns_zone }}"
+    state: present
+    register: zone_out
+
+- debug: var=zone_out
+
+'''
+
 import time
 
 try:
