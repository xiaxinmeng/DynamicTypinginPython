commit 4ef47fe6c98cded38146424f88937d4cb004a7fb
Author: Ricardo Carrillo Cruz <ricardo.carrillo.cruz@gmail.com>
Date:   Wed Jul 12 11:41:45 2017 +0200

    Switch eos_config save tests to use save_when (#26688)
    
    Save seems broken, just use save_when since we are deprecating save.

diff --git a/test/integration/targets/eos_config/tests/cli/save.yaml b/test/integration/targets/eos_config/tests/cli/save.yaml
index 0e8c7b4218..72cc4d0f9b 100644
--- a/test/integration/targets/eos_config/tests/cli/save.yaml
+++ b/test/integration/targets/eos_config/tests/cli/save.yaml
@@ -12,9 +12,9 @@
     provider: "{{ cli }}"
 
 
-- name: save config
+- name: save config always
   eos_config:
-    save: true
+    save_when: always
     provider: "{{ cli }}"
   register: result
 
@@ -22,9 +22,9 @@
     that:
       - "result.changed == true"
 
-- name: save should always run
+- name: save always again (not idempotent)
   eos_config:
-    save: true
+    save_when: always
     provider: "{{ cli }}"
   register: result
 
