commit 3f4ac0b9f774ade3cae66e46ac6ce1e23727706e
Author: Nathaniel Case <ncase@redhat.com>
Date:   Tue Nov 22 11:03:19 2016 -0500

    Fix for whitespace padding on JSON responses
    
    Fixes #17825, closes #17829

diff --git a/lib/ansible/module_utils/shell.py b/lib/ansible/module_utils/shell.py
index 2f30f58a06..9b096b249f 100644
--- a/lib/ansible/module_utils/shell.py
+++ b/lib/ansible/module_utils/shell.py
@@ -176,7 +176,7 @@ class Shell(object):
     def sanitize(self, cmd, resp):
         cleaned = []
         for line in resp.splitlines():
-            if line.startswith(str(cmd)) or self.find_prompt(line):
+            if line.lstrip().startswith(str(cmd)) or self.find_prompt(line):
                 continue
             cleaned.append(line)
         return "\n".join(cleaned)
