commit dca887a6dc3b1b60e6b8b821c54cf5eb926dbcd2
Author: Abhijeet Kasurde <akasurde@redhat.com>
Date:   Thu Aug 31 00:05:42 2017 +0530

    Use correct variable from response in github_key (#25846)
    
    Before fix, logic tries to access info from 're' library
    which raises AttributeError.
    Fix adds correct variable usage for accessing next/previous
    search results from github api.
    
    Signed-off-by: Abhijeet Kasurde <akasurde@redhat.com>

diff --git a/lib/ansible/modules/source_control/github_key.py b/lib/ansible/modules/source_control/github_key.py
index c8b80eceeb..79758fd7db 100644
--- a/lib/ansible/modules/source_control/github_key.py
+++ b/lib/ansible/modules/source_control/github_key.py
@@ -103,7 +103,7 @@ class GitHubResponse(object):
     def links(self):
         links = {}
         if 'link' in self.info:
-            link_header = re.info['link']
+            link_header = self.info['link']
             matches = re.findall('<([^>]+)>; rel="([^"]+)"', link_header)
             for url, rel in matches:
                 links[rel] = url
