commit 0fb0548d0b04cf2a1d9b6755697b7dca45d2dbf8
Author: Brian Coca <bcoca@ansible.com>
Date:   Thu Oct 30 13:40:34 2014 -0400

    removed no unused var that was not cleaned up properlly

diff --git a/bin/ansible-doc b/bin/ansible-doc
index d399e4668e..e4c7d19522 100755
--- a/bin/ansible-doc
+++ b/bin/ansible-doc
@@ -234,7 +234,7 @@ def main():
         for i in options.module_path.split(os.pathsep):
             utils.plugins.module_finder.add_directory(i)
 
-    if options.list_dir or options.list_deprecated:
+    if options.list_dir:
         # list modules
         paths = utils.plugins.module_finder._get_paths()
         module_list = []
