commit 4bf051b8bb3877f95ce1c807000aec1b2929aee0
Author: Mike Garrett <mgarrett33@users.noreply.github.com>
Date:   Wed Jun 12 13:40:30 2019 -0500

    redfish: fix a problem with Chassis/Power URI (#56185)
    
    Fixes #56137 - problem with Chassis/Power URI and GetChassisPower command - assemble URI from data in Chassis resource

diff --git a/changelogs/fragments/56185-problem-with-redfish-chassis-power-uri.yml b/changelogs/fragments/56185-problem-with-redfish-chassis-power-uri.yml
new file mode 100644
index 0000000000..9ae127587e
--- /dev/null
+++ b/changelogs/fragments/56185-problem-with-redfish-chassis-power-uri.yml
@@ -0,0 +1,2 @@
+bugfixes:
+  - Redfish - Instead of building the Power URI from assumptions about URI structure, assemble from @odata.id information in the Chassis resource (https://github.com/ansible/ansible/issues/56137).
diff --git a/lib/ansible/module_utils/redfish_utils.py b/lib/ansible/module_utils/redfish_utils.py
index 5da7274765..9a1c85cbd0 100644
--- a/lib/ansible/module_utils/redfish_utils.py
+++ b/lib/ansible/module_utils/redfish_utils.py
@@ -1195,8 +1195,7 @@ class RedfishUtils(object):
             result['ret'] = True
             data = response['data']
             if key in data:
-                response = self.get_request(self.root_uri + chassis_uri +
-                                            "/" + key)
+                response = self.get_request(self.root_uri + data[key]['@odata.id'])
                 data = response['data']
                 if 'PowerControl' in data:
                     if len(data['PowerControl']) > 0:
