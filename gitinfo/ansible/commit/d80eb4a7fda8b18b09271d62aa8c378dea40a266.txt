commit d80eb4a7fda8b18b09271d62aa8c378dea40a266
Author: greenbrian <greenbrian@gmail.com>
Date:   Mon Nov 25 10:44:39 2013 -0600

    Update unarchive
    
    Modified argument order provided to unzip command as the -o was being interpreted as a file.

diff --git a/library/files/unarchive b/library/files/unarchive
index 86bb4d7376..d85ae3cc59 100644
--- a/library/files/unarchive
+++ b/library/files/unarchive
@@ -73,7 +73,7 @@ class _zipfile(object):
         return dict(bool = False)
 
     def unarchive(self):
-        cmd = 'unzip "%s" -d "%s" -o'  % (self.src,self.dest)
+        cmd = 'unzip -o "%s" -d "%s"'  % (self.src,self.dest)
         rc, out, err = self.module.run_command(cmd)
         return dict(cmd = cmd, rc=rc, out=out, err=err)
 
