commit 2b363434514aa94aad145d2a6eacf4c1013490d8
Author: James Cammarata <jimi@sngx.net>
Date:   Wed Dec 9 17:57:52 2015 -0500

    Missed one place we were appending the incorrectly escaped item to raw params

diff --git a/lib/ansible/parsing/splitter.py b/lib/ansible/parsing/splitter.py
index f24d8ecf9d..feb0cd2b34 100644
--- a/lib/ansible/parsing/splitter.py
+++ b/lib/ansible/parsing/splitter.py
@@ -86,7 +86,7 @@ def parse_kv(args, check_raw=False):
                 # FIXME: make the retrieval of this list of shell/command
                 #        options a function, so the list is centralized
                 if check_raw and k not in ('creates', 'removes', 'chdir', 'executable', 'warn'):
-                    raw_params.append(x)
+                    raw_params.append(orig_x)
                 else:
                     options[k.strip()] = unquote(v.strip())
             else:
