commit 700db154520b9124fe1f4f201430923bd9bff2c4
Author: nitzmahone <mdavis@ansible.com>
Date:   Fri Apr 1 20:37:43 2016 -0700

    fix unconfigured plugin path case
    
    We tried to expand paths for None- ensure we're not passing [None] in that case, but [] instead.

diff --git a/lib/ansible/plugins/__init__.py b/lib/ansible/plugins/__init__.py
index f16bb0e6e5..c10d19cd38 100644
--- a/lib/ansible/plugins/__init__.py
+++ b/lib/ansible/plugins/__init__.py
@@ -64,8 +64,10 @@ class PluginLoader:
         self.subdir             = subdir
         self.aliases            = aliases
 
-        if not isinstance(config, list):
+        if config and not isinstance(config, list):
             config = [config]
+        else:
+            config = []
 
         self.config = config
 
