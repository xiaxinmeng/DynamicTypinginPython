commit b9710b4a3459d3941b8f8ef37c0c294ae344d900
Author: Brian Coca <bcoca@ansible.com>
Date:   Fri Jul 24 23:54:13 2015 -0400

    wrapped new system ssh key lookup in case file does not exist

diff --git a/lib/ansible/plugins/connections/paramiko_ssh.py b/lib/ansible/plugins/connections/paramiko_ssh.py
index cd38f38957..b3932b613d 100644
--- a/lib/ansible/plugins/connections/paramiko_ssh.py
+++ b/lib/ansible/plugins/connections/paramiko_ssh.py
@@ -152,7 +152,11 @@ class Connection(ConnectionBase):
         self.keyfile = os.path.expanduser("~/.ssh/known_hosts")
 
         if C.HOST_KEY_CHECKING:
-            ssh.load_system_host_keys("/etc/ssh/ssh_known_hosts")
+            try:
+                #TODO: check if we need to look at several possible locations, possible for loop
+                ssh.load_system_host_keys("/etc/ssh/ssh_known_hosts")
+            except IOError:
+                pass # file was not found, but not required to function
             ssh.load_system_host_keys()
 
         ssh.set_missing_host_key_policy(MyAddPolicy(self._new_stdin))
