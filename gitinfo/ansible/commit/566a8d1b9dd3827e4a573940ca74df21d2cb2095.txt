commit 566a8d1b9dd3827e4a573940ca74df21d2cb2095
Author: Toshio Kuratomi <a.badger@gmail.com>
Date:   Sun Jan 18 10:29:07 2015 -0800

    Fix issue with mixing quoting and glo9bbing.

diff --git a/hacking/env-setup b/hacking/env-setup
index f8b973b1a4..8ff6362df9 100644
--- a/hacking/env-setup
+++ b/hacking/env-setup
@@ -36,10 +36,10 @@ expr "$MANPATH" : "${PREFIX_MANPATH}.*" > /dev/null || export MANPATH="$PREFIX_M
 gen_egg_info()
 {
     python setup.py egg_info
-    if [ -e "$PREFIX_PYTHONPATH/ansible*.egg-info" ] ; then
-        rm -r "$PREFIX_PYTHONPATH/ansible*.egg-info"
+    if [ -e "$PREFIX_PYTHONPATH/ansible.egg-info" ] ; then
+        rm -r "$PREFIX_PYTHONPATH/ansible.egg-info"
     fi
-    mv "ansible*.egg-info" "$PREFIX_PYTHONPATH"
+    mv "ansible.egg-info" "$PREFIX_PYTHONPATH"
 }
 
 if [ "$ANSIBLE_HOME" != "$PWD" ] ; then
