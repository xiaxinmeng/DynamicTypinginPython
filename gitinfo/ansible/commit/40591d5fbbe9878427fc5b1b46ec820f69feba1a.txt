commit 40591d5fbbe9878427fc5b1b46ec820f69feba1a
Author: Baptiste Mille-Mathias <baptiste.millemathias@gmail.com>
Date:   Tue Jul 14 17:38:15 2020 +0200

    Document tags are not supported with task meta. (#70590)
    
    fixes #70338

diff --git a/lib/ansible/modules/meta.py b/lib/ansible/modules/meta.py
index 706ab9c4b9..dd23e6ea81 100644
--- a/lib/ansible/modules/meta.py
+++ b/lib/ansible/modules/meta.py
@@ -40,6 +40,7 @@ notes:
     - C(clear_facts) will remove the persistent facts from M(set_fact) using C(cacheable=True),
       but not the current host variable it creates for the current run.
     - Looping on meta tasks is not supported.
+    - Skipping C(meta) tasks using tags is not supported.
     - This module is also supported for Windows targets.
 seealso:
 - module: assert
