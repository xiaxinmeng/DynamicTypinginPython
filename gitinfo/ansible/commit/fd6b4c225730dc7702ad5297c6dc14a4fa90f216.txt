commit fd6b4c225730dc7702ad5297c6dc14a4fa90f216
Author: Bernhard Weitzhofer <bernhard@weitzhofer.org>
Date:   Tue Mar 26 11:45:47 2013 +0100

    "gem"-module: replace python 2.5 language feature
    
    replace conditional expression with python 2.4-compatible if/else
    statements

diff --git a/library/gem b/library/gem
index 4270d3dfdc..ee76875058 100755
--- a/library/gem
+++ b/library/gem
@@ -130,7 +130,10 @@ def install(module):
         return
 
     ver = get_rubygems_version(module)
-    major = ver[0] if ver else None
+    if ver:
+        major = ver[0]
+    else:
+        major = None
 
     cmd = [ module.get_bin_path('gem', True) ]
     cmd.append('install')
