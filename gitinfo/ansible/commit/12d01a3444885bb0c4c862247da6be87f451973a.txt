commit 12d01a3444885bb0c4c862247da6be87f451973a
Author: Jeroen Hoekx <jeroen.hoekx@hamok.be>
Date:   Fri Mar 30 11:46:30 2012 +0200

    Fix except statement in template module.
    
    Python 2.4 (RHEL 5) does not know 'as'.

diff --git a/library/template b/library/template
index 03bdb18d57..3421f497cc 100755
--- a/library/template
+++ b/library/template
@@ -97,7 +97,7 @@ try:
     # call Jinja2 here and save the new template file
     template = environment.from_string(source)
     data_out = template.render(data)
-except jinja2.TemplateError as  e:
+except jinja2.TemplateError, e:
     print json.dumps({
         "failed": True,
         "msg"   : e.message
