commit 317728f64955f0d38da014fd7e48cba97883b646
Author: James Cammarata <jimi@sngx.net>
Date:   Mon Mar 23 09:20:27 2015 -0500

    Allow ansible-galaxy to install symlinks

diff --git a/bin/ansible-galaxy b/bin/ansible-galaxy
index f281bf97ae..a6d625671e 100755
--- a/bin/ansible-galaxy
+++ b/bin/ansible-galaxy
@@ -556,7 +556,7 @@ def install_role(role_name, role_version, role_filename, options):
                 # we only extract files, and remove any relative path
                 # bits that might be in the file for security purposes
                 # and drop the leading directory, as mentioned above
-                if member.isreg():
+                if member.isreg() or member.issym():
                     parts = member.name.split("/")[1:]
                     final_parts = []
                     for part in parts:
