commit 60a13cf5401ad085e88af6989d22504a140a9bd0
Author: Michael DeHaan <michael.dehaan@gmail.com>
Date:   Sat Mar 10 21:19:41 2012 -0500

    Ignore commented out lines in the ansible setup files.

diff --git a/lib/ansible/runner.py b/lib/ansible/runner.py
index 4bf3aae62e..0eed2be9a2 100755
--- a/lib/ansible/runner.py
+++ b/lib/ansible/runner.py
@@ -127,10 +127,16 @@ class Runner(object):
         group_name = 'ungrouped'
         results    = []
         for item in lines:
+            item = item.lstrip().rstrip()
+            if item.startswith("#"): 
+                # ignore commented out lines
+                continue
             if item.startswith("["):
+                # looks like a group
                 group_name = item.replace("[","").replace("]","").lstrip().rstrip()
                 groups[group_name] = []
             else:
+                # looks like a regular host
                 groups[group_name].append(item)
                 results.append(item)
 
