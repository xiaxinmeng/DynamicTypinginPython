commit 464c502f95963b779f2cf4de68059d677fd50d9f
Author: Henry Finucane <henry.finucane@locationlabs.com>
Date:   Mon Sep 29 18:55:34 2014 -0700

    dpkg does not take a --force-yes option

diff --git a/lib/ansible/modules/packaging/os/apt.py b/lib/ansible/modules/packaging/os/apt.py
index 06f96f3f1d..3b5f6691de 100644
--- a/lib/ansible/modules/packaging/os/apt.py
+++ b/lib/ansible/modules/packaging/os/apt.py
@@ -394,7 +394,7 @@ def install_deb(m, debs, cache, force, install_recommends, dpkg_options):
         if m.check_mode:
             options += " --simulate"
         if force:
-            options += " --force-yes"
+            options += " --force-all"
 
         cmd = "dpkg %s -i %s" % (options, " ".join(pkgs_to_install))
         rc, out, err = m.run_command(cmd)
