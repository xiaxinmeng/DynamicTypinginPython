commit fb99277066c5c98c41d02290156b5e728520e38f
Author: chouseknecht <chousekn@redhat.com>
Date:   Wed Aug 17 16:54:39 2016 -0400

    Exposing tcp and udp for same port fails. Fixes issue #4354.

diff --git a/lib/ansible/modules/cloud/docker/docker_container.py b/lib/ansible/modules/cloud/docker/docker_container.py
index 0c7a77a19a..d1cdd95204 100644
--- a/lib/ansible/modules/cloud/docker/docker_container.py
+++ b/lib/ansible/modules/cloud/docker/docker_container.py
@@ -1012,7 +1012,8 @@ class TaskParameters(DockerBaseClass):
                 protocol = 'tcp'
                 match = re.search(r'(/.+$)', port)
                 if match:
-                    protocol = match.group(1)
+                    protocol = match.group(1).replace('/', '')
+                    port = re.sub(r'/.+$', '', port)
                 exposed.append((port, protocol))
         if published_ports:
             # Any published port should also be exposed
