commit 41ab7f8aedb16a0bf56d202f12d825a998f4fc63
Author: Hans-Joachim Kliemeck <git@kliemeck.de>
Date:   Fri Nov 27 11:32:54 2015 +0100

    do not move item, instead copy item because acl will be kept

diff --git a/lib/ansible/modules/windows/win_lineinfile.ps1 b/lib/ansible/modules/windows/win_lineinfile.ps1
index 4ba9086a6e..968f66bae4 100644
--- a/lib/ansible/modules/windows/win_lineinfile.ps1
+++ b/lib/ansible/modules/windows/win_lineinfile.ps1
@@ -96,7 +96,8 @@ function WriteLines($outlines, $dest, $linesep, $encodingobj, $validate) {
 	
 	# Commit changes to the destination file
 	$cleandest = $dest.Replace("/", "\");
-	Move-Item $temppath $cleandest -force;	
+	Copy-Item $temppath $cleandest -force;	
+	Remove-Item $temppath -force;
 }
 
 
