commit fedc3a4f85d471e28f9deb72ca4b6ecfa58ed564
Author: Brian Coca <bcoca@ansible.com>
Date:   Thu May 29 22:44:33 2014 -0400

    corrected doc example, non existing fact to the 2 it had 'merged'

diff --git a/library/utilities/include_vars b/library/utilities/include_vars
index 9810d34bae..4c7c39d903 100644
--- a/library/utilities/include_vars
+++ b/library/utilities/include_vars
@@ -32,7 +32,8 @@ EXAMPLES = """
 # Load a variable file based on the OS type, or a default if not found.
 - include_vars: "{{ item }}"
   with_first_found:
-   - "{{ ansible_os_distribution }}.yml"
+   - "{{ ansible_distribution }}.yml"
+   - "{{ ansible_os_family }}.yml"
    - "default.yml"
- 
+
 """
