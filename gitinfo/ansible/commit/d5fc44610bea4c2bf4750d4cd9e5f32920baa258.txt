commit d5fc44610bea4c2bf4750d4cd9e5f32920baa258
Author: Matthias Frey <freym@users.noreply.github.com>
Date:   Wed Jun 1 15:19:58 2016 +0200

    Fixed error in assemble module when validation failed. (#3814)
    
    Error was: TypeError: fail_json() takes exactly 1 argument (2 given)

diff --git a/lib/ansible/modules/files/assemble.py b/lib/ansible/modules/files/assemble.py
index d8cb5f2557..c4ce6a7eea 100644
--- a/lib/ansible/modules/files/assemble.py
+++ b/lib/ansible/modules/files/assemble.py
@@ -260,8 +260,7 @@ def main():
             result['validation'] = dict(rc=rc, stdout=out, stderr=err)
             if rc != 0:
                 cleanup(path)
-                result['msg'] = "failed to validate: rc:%s error:%s" % (rc, err)
-                module.fail_json(result)
+                module.fail_json(msg="failed to validate: rc:%s error:%s" % (rc, err))
         if backup and dest_hash is not None:
             result['backup_file'] = module.backup_local(dest)
 
