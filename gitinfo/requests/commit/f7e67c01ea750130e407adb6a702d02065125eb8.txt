commit f7e67c01ea750130e407adb6a702d02065125eb8
Author: Kenneth Reitz <me@kennethreitz.com>
Date:   Wed Dec 28 02:57:36 2011 -0500

    only use session verify when not modified

diff --git a/requests/sessions.py b/requests/sessions.py
index 2adc390b..280bd5cc 100644
--- a/requests/sessions.py
+++ b/requests/sessions.py
@@ -145,7 +145,10 @@ class Session(object):
         headers = {} if headers is None else headers
         params = {} if params is None else params
         hooks = {} if hooks is None else hooks
-        verify = verify or self.verify
+
+        if verify is None:
+            verify = self.verify
+
         # use session's hooks as defaults
         for key, cb in self.hooks.iteritems():
             hooks.setdefault(key, cb)
