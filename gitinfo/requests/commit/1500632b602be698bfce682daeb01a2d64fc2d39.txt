commit 1500632b602be698bfce682daeb01a2d64fc2d39
Author: Ian Cordasco <graffatcolmingov@gmail.com>
Date:   Fri Dec 13 22:29:30 2013 -0600

    Move comment to the right place

diff --git a/requests/adapters.py b/requests/adapters.py
index 5bb7b0c1..e079c436 100644
--- a/requests/adapters.py
+++ b/requests/adapters.py
@@ -88,6 +88,8 @@ class HTTPAdapter(BaseAdapter):
                     self.__attrs__)
 
     def __setstate__(self, state):
+        # Can't handle by adding 'proxy_manager' to self.__attrs__ because
+        # because self.poolmanager uses a lambda function, which isn't pickleable.
         self.proxy_manager = {}
         self.config = {}
 
@@ -97,11 +99,6 @@ class HTTPAdapter(BaseAdapter):
         self.init_poolmanager(self._pool_connections, self._pool_maxsize,
                               block=self._pool_block)
 
-        # Can't handle by adding 'proxy_manager' to self.__attrs__ because
-        # because self.poolmanager uses a lambda function, which isn't pickleable.
-        if not hasattr(self, 'proxy_manager'):
-            self.proxy_manager = {}
-
     def init_poolmanager(self, connections, maxsize, block=DEFAULT_POOLBLOCK):
         """Initializes a urllib3 PoolManager. This method should not be called
         from user code, and is only exposed for use when subclassing the
