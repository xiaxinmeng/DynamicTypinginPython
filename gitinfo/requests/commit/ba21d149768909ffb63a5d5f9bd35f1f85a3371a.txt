commit ba21d149768909ffb63a5d5f9bd35f1f85a3371a
Author: mgasvoda <mgasvoda01@gmail.com>
Date:   Fri Aug 11 16:12:36 2017 -0400

    Improving message formatting

diff --git a/requests/utils.py b/requests/utils.py
index 2104da8f..23a87bc1 100644
--- a/requests/utils.py
+++ b/requests/utils.py
@@ -868,8 +868,8 @@ def check_header_validity(header):
         if not pat.match(value):
             raise InvalidHeader("Invalid return character or leading space in header: %s" % name)
     except TypeError:
-        raise InvalidHeader("Header %s value %s must be of type str or bytes, "
-                            "not %s" % (name, value, type(value)))
+        raise InvalidHeader("Value for header {%s:%s} must be of type str or " 
+                            "bytes, not %s" % (name, value, type(value)))
 
 
 def urldefragauth(url):
