commit f37b9684756b9c495fdcea1db3b6edd6a76b6d47
Author: Jason Emerick <jason@mobelux.com>
Date:   Mon Apr 1 18:14:52 2013 -0400

    use compat.str instead of compat.builtin_str

diff --git a/requests/models.py b/requests/models.py
index 6ed2b599..8249ce3a 100644
--- a/requests/models.py
+++ b/requests/models.py
@@ -101,9 +101,9 @@ class RequestEncodingMixin(object):
         for field, val in fields:
             if isinstance(val, list):
                 for v in val:
-                    new_fields.append((field, builtin_str(v)))
+                    new_fields.append((field, str(v)))
             else:
-                new_fields.append((field, builtin_str(val)))
+                new_fields.append((field, str(val)))
 
         for (k, v) in files:
             # support for explicit filename
