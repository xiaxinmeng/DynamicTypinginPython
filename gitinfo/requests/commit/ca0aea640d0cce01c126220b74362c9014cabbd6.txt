commit ca0aea640d0cce01c126220b74362c9014cabbd6
Author: Kamil Szot <kamil.szot@gmail.com>
Date:   Tue Apr 2 13:42:40 2013 +0200

    Fix for the issue https://github.com/kennethreitz/requests/issues/1280

diff --git a/requests/sessions.py b/requests/sessions.py
index de0d9d67..7bfa306e 100644
--- a/requests/sessions.py
+++ b/requests/sessions.py
@@ -251,7 +251,8 @@ class Session(SessionRedirectMixin):
         hooks=None,
         stream=None,
         verify=None,
-        cert=None):
+        cert=None,
+        return_response=True):
         """Constructs a :class:`Request <Request>`, prepares it and sends it.
         Returns :class:`Response <Response>` object.
 
@@ -339,6 +340,9 @@ class Session(SessionRedirectMixin):
         req.cookies = cookies
         req.hooks = hooks
 
+	if not return_response:
+	  return req
+	
         # Prepare the Request.
         prep = req.prepare()
 
