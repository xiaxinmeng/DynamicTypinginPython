commit 7c91e27d0142c177add6a1f726712f58914d4f52
Author: Cory Benfield <lukasaoz@gmail.com>
Date:   Thu Nov 5 10:37:53 2015 +0000

    Remove threaded test

diff --git a/test_requests.py b/test_requests.py
index 38eedb8e..24284c04 100755
--- a/test_requests.py
+++ b/test_requests.py
@@ -448,21 +448,6 @@ class TestRequests(object):
         r = requests.get(url, auth=auth)
         assert '"auth"' in r.request.headers['Authorization']
 
-    def test_DIGESTAUTH_THREADED(self, httpbin):
-
-        auth = HTTPDigestAuth('user', 'pass')
-        url = httpbin('digest-auth', 'auth', 'user', 'pass')
-        session = requests.Session()
-        session.auth=auth
-
-        def do_request(i):
-            r = session.get(url)
-            assert '"auth"' in r.request.headers['Authorization']
-            return 1
-        if ThreadPool is not None:
-            pool = ThreadPool(processes=50)
-            pool.map(do_request, range(100))
-
     def test_POSTBIN_GET_POST_FILES(self, httpbin):
 
         url = httpbin('post')
