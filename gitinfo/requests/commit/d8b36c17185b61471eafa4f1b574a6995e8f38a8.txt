commit d8b36c17185b61471eafa4f1b574a6995e8f38a8
Author: Casey Davidson <casey@planetargon.com>
Date:   Fri Mar 4 15:52:13 2016 -0800

    Override __contains__ method of RequestsCookieJar to catch CookieConflictError. Refs #3028

diff --git a/requests/cookies.py b/requests/cookies.py
index b85fd2b6..eee5168f 100644
--- a/requests/cookies.py
+++ b/requests/cookies.py
@@ -277,6 +277,12 @@ class RequestsCookieJar(cookielib.CookieJar, collections.MutableMapping):
                 dictionary[cookie.name] = cookie.value
         return dictionary
 
+    def __contains__(self, name):
+        try:
+            return super(RequestsCookieJar, self).__contains__(name)
+        except CookieConflictError:
+            return True
+
     def __getitem__(self, name):
         """Dict-like __getitem__() for compatibility with client code. Throws
         exception if there are more than one cookie with name. In that case,
