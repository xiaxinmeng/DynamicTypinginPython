commit b98cc8b4e9d8c4c12b8f52c9a9be151c306cb977
Author: Chris Adams <chris@improbable.org>
Date:   Sun Jan 29 18:43:13 2012 -0500

    Revert "Initial stop-the-bleeding fix"
    
    This reverts commit 730c0e2e2bef77968a86962f9d5f2bebba4d19ec.

diff --git a/requests/models.py b/requests/models.py
index 770e9b04..b4118b93 100644
--- a/requests/models.py
+++ b/requests/models.py
@@ -210,8 +210,6 @@ class Request(object):
             while (('location' in r.headers) and
                    ((r.status_code is codes.see_other) or (self.allow_redirects))):
 
-                r.content  # Consume socket so it can be released
-
                 if not len(history) < self.config.get('max_redirects'):
                     raise TooManyRedirects()
 
@@ -264,7 +262,6 @@ class Request(object):
                 self.cookies.update(r.cookies)
 
             r.history = history
-            r.content  # Consume socket so it can be released
 
         self.response = r
         self.response.request = self
