commit f038ec3b6e9e3dc9ba118eb34f60db295c6a5534
Author: Kenneth Reitz <me@kennethreitz.com>
Date:   Sat Aug 20 19:44:24 2011 -0400

    may have multiple encoding values

diff --git a/requests/models.py b/requests/models.py
index d3483149..8692bbb9 100644
--- a/requests/models.py
+++ b/requests/models.py
@@ -446,7 +446,7 @@ class Response(object):
             self._content = self.read()
 
             # Decode GZip'd content.
-            if self.headers.get('content-encoding', '') == 'gzip':
+            if 'gzip' in self.headers.get('content-encoding', ''):
                 try:
                     self._content = zlib.decompress(self._content, 16+zlib.MAX_WBITS)
                 except zlib.error:
