commit 74a4d06d58106ac0e258df1ede94c5ecfad33945
Author: Craig de Stigter <craig.destigter@koordinates.com>
Date:   Tue Jun 26 12:42:14 2012 +1200

    fix a bug where client certificates never get set unless verify=True (which verifies server certs, so should be unrelated)

diff --git a/requests/models.py b/requests/models.py
index 47316ea4..0f7a2162 100644
--- a/requests/models.py
+++ b/requests/models.py
@@ -569,7 +569,7 @@ class Request(object):
             conn.cert_reqs = 'CERT_NONE'
             conn.ca_certs = None
 
-        if self.cert and self.verify:
+        if self.cert:
             if len(self.cert) == 2:
                 conn.cert_file = self.cert[0]
                 conn.key_file = self.cert[1]
