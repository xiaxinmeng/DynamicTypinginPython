commit def20cdb0cf1d4fcc3aa4cd3164602d6c4029ac3
Author: Kenneth Reitz <me@kennethreitz.com>
Date:   Mon Dec 17 05:12:59 2012 -0500

    remove iter_json, don't want to commit to it

diff --git a/requests/models.py b/requests/models.py
index 53fb2474..8d564561 100644
--- a/requests/models.py
+++ b/requests/models.py
@@ -558,11 +558,6 @@ class Response(object):
         except ValueError:
             return None
 
-    def iter_json(self):
-        for line in self.iter_lines():
-            if line: # filter out keep-alive new lines
-                yield json.loads(line)
-
     @property
     def links(self):
         """Returns the parsed header links of the response, if any."""
diff --git a/requests/sessions.py b/requests/sessions.py
index 94daa17a..b7526c44 100644
--- a/requests/sessions.py
+++ b/requests/sessions.py
@@ -182,9 +182,6 @@ class Session(SessionRedirectMixin):
     def __exit__(self, *args):
         self.close()
 
-    def close(self):
-        pass
-
     def request(self, method, url,
         params=None,
         data=None,
