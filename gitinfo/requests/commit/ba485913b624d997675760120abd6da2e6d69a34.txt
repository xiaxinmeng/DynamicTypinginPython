commit ba485913b624d997675760120abd6da2e6d69a34
Author: Ian Cordasco <graffatcolmingov@gmail.com>
Date:   Wed Jan 30 10:58:32 2013 -0500

    Thanks to @maxcountryman for the code review

diff --git a/requests/sessions.py b/requests/sessions.py
index c2eebac1..633d1380 100644
--- a/requests/sessions.py
+++ b/requests/sessions.py
@@ -367,12 +367,8 @@ class Session(SessionRedirectMixin):
         """Send a given PreparedRequest."""
         # Set up variables needed for resolve_redirects and dispatching of
         # hooks
-        allow_redirects = True
-        if 'allow_redirects' in kwargs:
-            allow_redirects = kwargs.pop('allow_redirects')
-        req = None
-        if 'req' in kwargs:
-            req = kwargs.pop('req')
+        allow_redirects = kwargs.pop('allow_redirects', True)
+        req = kwargs.pop('req', None)
         stream = kwargs.get('stream', False)
         timeout = kwargs.get('timeout')
         verify = kwargs.get('verify')
