commit bf499a80de4a0a6f0ab6a47caa0fbc5e8d03cc03
Author: Rolando Espinoza La fuente <darkrho@gmail.com>
Date:   Wed Nov 16 00:03:19 2011 -0400

    added support for explicit filenames in `files` parameter.
    
    files = {
        "files[]": ("myfile", open("foo")),
    }

diff --git a/requests/models.py b/requests/models.py
index 97237e77..75b98568 100644
--- a/requests/models.py
+++ b/requests/models.py
@@ -371,7 +371,13 @@ class Request(object):
                     fields = dict(self.data)
 
                 for (k, v) in self.files.items():
-                    fields.update({k: (guess_filename(k) or k, v.read())})
+                    # support for explicit filename
+                    if isinstance(v, (tuple, list)):
+                        fn, fp = v
+                    else:
+                        fn = guess_filename(v) or k
+                        fp = v
+                    fields.update({k: (fn, fp.read())})
 
                 (body, content_type) = encode_multipart_formdata(fields)
             else:
