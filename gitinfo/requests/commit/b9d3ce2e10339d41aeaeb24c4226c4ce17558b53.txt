commit b9d3ce2e10339d41aeaeb24c4226c4ce17558b53
Author: Locker537 <locker537@gmail.com>
Date:   Thu Aug 16 16:50:01 2012 -0400

    Change variable '_r' to 'bad_r' to avoid confusion about Python's private variable convention.

diff --git a/docs/user/quickstart.rst b/docs/user/quickstart.rst
index c0ecf8d2..7d0fe5b2 100644
--- a/docs/user/quickstart.rst
+++ b/docs/user/quickstart.rst
@@ -260,11 +260,11 @@ reference::
 If we made a bad request (non-200 response), we can raise it with
 :class:`Response.raise_for_status()`::
 
-    >>> r = requests.get('http://httpbin.org/status/404')
-    >>> r.status_code
+    >>> bad_r = requests.get('http://httpbin.org/status/404')
+    >>> bad_r.status_code
     404
 
-    >>> r.raise_for_status()
+    >>> bad_r.raise_for_status()
     Traceback (most recent call last):
       File "requests/models.py", line 832, in raise_for_status
         raise http_error
