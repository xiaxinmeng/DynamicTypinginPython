commit 92f747817447ad56d3318e393e222db0f347dfb3
Author: Kenneth Reitz <me@kennethreitz.com>
Date:   Fri Mar 22 15:48:27 2013 -0400

    first

diff --git a/requests/models.py b/requests/models.py
index 838e78a3..6e8a7338 100644
--- a/requests/models.py
+++ b/requests/models.py
@@ -399,7 +399,7 @@ class PreparedRequest(RequestEncodingMixin, RequestHooksMixin):
     def prepare_auth(self, auth, url=''):
         """Prepares the given HTTP auth data."""
 
-        # If no Auth is explicitly provided, extract it from the URL.
+        # If no Auth is explicitly provided, extract it from the URL first.
         if auth is None:
             url_auth = get_auth_from_url(self.url)
             auth = url_auth if any(url_auth) else None
