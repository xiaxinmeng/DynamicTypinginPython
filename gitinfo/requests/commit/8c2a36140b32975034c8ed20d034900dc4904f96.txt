commit 8c2a36140b32975034c8ed20d034900dc4904f96
Author: Kenneth Reitz <me@kennethreitz.org>
Date:   Fri May 26 22:08:46 2017 -0400

    fix for python 3

diff --git a/requests/sessions.py b/requests/sessions.py
index 1028bb7f..b21d2f26 100755
--- a/requests/sessions.py
+++ b/requests/sessions.py
@@ -674,7 +674,7 @@ class Session(SessionRedirectMixin):
         # If redirects aren't being followed, store the response on the Request for Response.next().
         if not allow_redirects:
             try:
-                r._next = self.resolve_redirects(r, request, yield_requests=True, **kwargs).next()
+                r._next = next(self.resolve_redirects(r, request, yield_requests=True, **kwargs))
             except StopIteration:
                 pass
 
