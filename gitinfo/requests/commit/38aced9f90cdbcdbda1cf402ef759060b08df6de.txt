commit 38aced9f90cdbcdbda1cf402ef759060b08df6de
Author: Matt Sweeney <mattswe@gmail.com>
Date:   Wed Sep 26 12:38:36 2012 -0700

    Simplify error handling when decoding unicode

diff --git a/requests/models.py b/requests/models.py
index 305e615e..2193c6e5 100644
--- a/requests/models.py
+++ b/requests/models.py
@@ -828,13 +828,10 @@ class Response(object):
         # Decode unicode from given encoding.
         try:
             content = str(self.content, encoding, errors='replace')
-        except LookupError:
+        except (LookupError, TypeError):
             # A LookupError is raised if the encoding was not found which could
             # indicate a misspelling or similar mistake.
             #
-            # So we try blindly encoding.
-            content = str(self.content, errors='replace')
-        except TypeError:
             # A TypeError can be raised if encoding is None
             #
             # So we try blindly encoding.
