commit e771aa93861355a704fa6650d59e14256ea0ee89
Author: Ian Cordasco <graffatcolmingov@gmail.com>
Date:   Wed Jan 23 11:28:30 2013 -0500

    Comment & replace something that keeps disappearing

diff --git a/requests/models.py b/requests/models.py
index 4197eaf0..1aacdda5 100644
--- a/requests/models.py
+++ b/requests/models.py
@@ -225,6 +225,7 @@ class Request(RequestHooksMixin):
         # Note that prepare_auth must be last to enable authentication schemes
         # such as OAuth to work on a fully prepared request.
         p.prepare_auth(self.auth)
+        # This MUST go after prepare_auth. Authenticators could add a hook
         p.prepare_hooks(self.hooks)
 
         return p
diff --git a/requests/sessions.py b/requests/sessions.py
index 061983bf..d3fc851c 100644
--- a/requests/sessions.py
+++ b/requests/sessions.py
@@ -130,8 +130,9 @@ class SessionRedirectMixin(object):
                     timeout=timeout,
                     verify=verify,
                     cert=cert,
-                    proxies=proxies
-                )
+                    proxies=proxies,
+                    hooks=req.hooks,
+            )
 
             i += 1
             yield resp
