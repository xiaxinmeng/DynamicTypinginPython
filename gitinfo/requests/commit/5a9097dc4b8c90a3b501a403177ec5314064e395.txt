commit 5a9097dc4b8c90a3b501a403177ec5314064e395
Author: Smiley Barry <smiley@users.noreply.github.com>
Date:   Fri Aug 14 17:15:51 2015 +0300

    Update quickstart.rst
    
    Added note about the `json` parameter in `requests.post`.

diff --git a/docs/user/quickstart.rst b/docs/user/quickstart.rst
index 7b14610c..2277528c 100644
--- a/docs/user/quickstart.rst
+++ b/docs/user/quickstart.rst
@@ -229,6 +229,15 @@ For example, the GitHub API v3 accepts JSON-Encoded POST/PATCH data::
     >>> payload = {'some': 'data'}
 
     >>> r = requests.post(url, data=json.dumps(payload))
+    
+You can use the `json` parameter (added in version 2.4.2) to avoid encoding the
+data yourself:
+
+    >>> import json
+    >>> url = 'https://api.github.com/some/endpoint'
+    >>> payload = {'some': 'data'}
+
+    >>> r = requests.post(url, json=payload)
 
 
 POST a Multipart-Encoded File
