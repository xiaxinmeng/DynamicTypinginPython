commit 852de116ddf7fa2d82a6c3453916acecc2da926d
Author: Dieter Verfaillie <dieterv@optionexplicit.be>
Date:   Wed Jul 31 09:29:24 2013 +0200

    remove duplicate comment
    
    This is already mentioned at line 357...

diff --git a/requests/models.py b/requests/models.py
index 24391533..15a07771 100644
--- a/requests/models.py
+++ b/requests/models.py
@@ -384,9 +384,6 @@ class PreparedRequest(RequestEncodingMixin, RequestHooksMixin):
                 self.headers['Content-Length'] = str(length)
             else:
                 self.headers['Transfer-Encoding'] = 'chunked'
-        # Check if file, fo, generator, iterator.
-        # If not, run through normal process.
-
         else:
             # Multi-part file uploads.
             if files:
