commit d3b7f9881490c1429b4310c97f79a432ec7d76a5
Author: Kenneth Reitz <me@kennethreitz.com>
Date:   Sat Feb 25 22:44:46 2012 -0500

    hurray for passing tests

diff --git a/requests/models.py b/requests/models.py
index 8dadf083..8368c9fb 100644
--- a/requests/models.py
+++ b/requests/models.py
@@ -739,8 +739,11 @@ class Response(object):
                     raise RuntimeError(
                         'The content for this response was already consumed')
 
-                self._content = bytes().join(self.iter_content()) or bytes()
-                # print repr(self._content)
+                if self.status_code is 0:
+                    self._content = None
+                else:
+                    self._content = bytes().join(self.iter_content()) or bytes()
+
             except AttributeError:
                 self._content = None
 
