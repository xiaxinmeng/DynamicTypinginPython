commit 17b6c5742ca7118858962d73220968a2e281b82e
Author: Kumar Varadarajulu <kumar.varadarajulu@dealertrack.com>
Date:   Mon May 16 06:11:25 2016 +0000

    consider plain ip notation of an ip in no_proxy if not in cidr notation

diff --git a/requests/utils.py b/requests/utils.py
index c08448cc..eaa89df2 100644
--- a/requests/utils.py
+++ b/requests/utils.py
@@ -539,6 +539,10 @@ def should_bypass_proxies(url):
                 if is_valid_cidr(proxy_ip):
                     if address_in_network(ip, proxy_ip):
                         return True
+                elif ip == proxy_ip:
+                    # If no_proxy ip was defined in plain IP notation instead of cidr notation &
+                    # matches the IP of the index
+                    return True
         else:
             for host in no_proxy:
                 if netloc.endswith(host) or netloc.split(':')[0].endswith(host):
