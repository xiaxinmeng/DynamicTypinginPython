commit 958c424c89079239401c1fe236464178f0038760
Author: Kenneth Reitz <me@kennethreitz.com>
Date:   Sat Oct 22 19:39:23 2011 -0400

    I don't even want to know.

diff --git a/test_requests.py b/test_requests.py
index 37d2a7d9..6360e7ed 100755
--- a/test_requests.py
+++ b/test_requests.py
@@ -68,12 +68,12 @@ class RequestsTestSuite(unittest.TestCase):
         self.assertEqual(r.status_code, 200)
 
     def test_HTTP_302_ALLOW_REDIRECT_GET(self):
-	r = requests.get(httpbin('redirect', '1'))
-	self.assertEqual(r.status_code, 200)
+        r = requests.get(httpbin('redirect', '1'))
+        self.assertEqual(r.status_code, 200)
 
     def test_HTTP_302_GET(self):
-	r = requests.get(httpbin('redirect', '1'), allow_redirects=False)
-	self.assertEqual(r.status_code, 302)
+        r = requests.get(httpbin('redirect', '1'), allow_redirects=False)
+        self.assertEqual(r.status_code, 302)
 
 
     def test_HTTP_200_OK_GET_WITH_PARAMS(self):
