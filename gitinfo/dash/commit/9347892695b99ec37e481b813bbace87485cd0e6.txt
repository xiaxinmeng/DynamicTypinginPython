commit 9347892695b99ec37e481b813bbace87485cd0e6
Author: Per Nilsson <per.nilsson@formidablelabs.com>
Date:   Wed May 25 12:25:59 2016 -0700

    :wrench: Switch to Webpack
    
    Warning; Watching is not yet supported, just building a large bundle.

diff --git a/.gitignore b/.gitignore
index 2a7f0041..f8a29bed 100644
--- a/.gitignore
+++ b/.gitignore
@@ -1,2 +1,4 @@
-node_modules
+build/
+node_modules/
 .npm
+
diff --git a/renderer/config/.eslintrc.json b/renderer/config/.eslintrc.json
new file mode 100644
index 00000000..34153e2b
--- /dev/null
+++ b/renderer/config/.eslintrc.json
@@ -0,0 +1,6 @@
+{
+    "env": {
+        "node": true
+    },
+    "extends": "eslint:recommended"
+}
\ No newline at end of file
diff --git a/renderer/config/webpack.config.js b/renderer/config/webpack.config.js
new file mode 100644
index 00000000..86fa06a1
--- /dev/null
+++ b/renderer/config/webpack.config.js
@@ -0,0 +1,38 @@
+var path = require('path');
+
+var ROOT = process.cwd();
+var SRC = path.join(ROOT, 'src');
+var BUILD = path.join(ROOT, 'build');
+
+module.exports = {
+    cache: false,
+    // Resolution path for `entry`.
+    context: SRC,
+    entry: {
+        bundle: './index.js'
+    },
+    output: {
+        path: BUILD,
+        filename: '[name].js'
+    },
+    resolve: {
+        // Need `''` so referencing modules by `name.js` works.
+        extensions: ['', '.js', '.jsx']
+    },
+    module: {
+        loaders: [
+            {
+                test: /\.js/,
+                include: [SRC],
+                /*
+                 * Use require.resolve to get a deterministic path
+                 * and avoid webpack's magick loader resolution
+                 */
+                loader: require.resolve('babel-loader'),
+                query: {
+                    presets: ['es2015', 'react']
+                }
+            }
+        ]
+    }
+};
diff --git a/renderer/package.json b/renderer/package.json
index a9f1a648..de602830 100644
--- a/renderer/package.json
+++ b/renderer/package.json
@@ -4,11 +4,11 @@
   "description": "render dash components in react",
   "main": "index.js",
   "scripts": {
-    "lint": "eslint ./*.js ./**/*.js --fix",
+    "build": "webpack --config=config/webpack.config.js",
+    "lint": "eslint --quiet --fix --ignore-path ../.gitignore ./*.js ./**/*.js",
     "server": "serv .",
-    "start": "npm run watch",
-    "test": "npm run lint",
-    "watch": "node_modules/.bin/watchify index.js -t babelify -p livereactload -o ../build/bundle.js -v"
+    "start": "npm run build && npm run server",
+    "test": "npm run lint"
   },
   "author": "chriddyp",
   "license": "MIT",
@@ -18,6 +18,8 @@
     ]
   },
   "dependencies": {
+    "babel-core": "^6.9.0",
+    "babel-loader": "^6.2.4",
     "immutable": "^3.8.1",
     "radium": "^0.17.1",
     "ramda": "^0.21.0",
@@ -27,19 +29,19 @@
     "react-dom": "^15.0.1",
     "react-redux": "^4.4.5",
     "redux": "^3.4.0",
-    "redux-actions": "^0.9.1"
+    "redux-actions": "^0.9.1",
+    "webpack": "^1.13.1",
+    "webpack-partial": "^1.2.0"
   },
   "devDependencies": {
     "babel-plugin-react-transform": "^2.0.2",
     "babel-preset-es2015": "^6.6.0",
     "babel-preset-react": "^6.5.0",
-    "babelify": "^7.2.0",
     "browserify": "^13.0.0",
     "eslint": "^2.8.0",
     "eslint-plugin-react": "^5.1.1",
-    "livereactload": "^2.2.2",
-    "react-proxy": "^1.1.8",
+    "react-hot-loader": "^1.3.0",
     "serv": "^0.2.4",
-    "watchify": "^3.7.0"
+    "webpack-dev-server": "^1.14.1"
   }
 }
diff --git a/renderer/src/.babelrc b/renderer/src/.babelrc
index 85d9aba6..6dacbf11 100644
--- a/renderer/src/.babelrc
+++ b/renderer/src/.babelrc
@@ -2,14 +2,7 @@
   "presets": ["es2015", "react"],
   "env": {
     "development": {
-      "plugins": [
-        ["react-transform", {
-          "transforms": [{
-            "transform": "livereactload/babel-transform",
-            "imports": ["react"]
-          }]
-        }]
-      ]
+
     }
   }
 }
