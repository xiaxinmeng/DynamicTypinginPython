commit eeb13ad9a2031e16f52044ebfcb63991b0098785
Author: chriddyp <chris@plot.ly>
Date:   Tue Apr 19 17:04:30 2016 -0400

    doh! add `build` to the package.json

diff --git a/.gitignore b/.gitignore
index 3f499594..2a7f0041 100644
--- a/.gitignore
+++ b/.gitignore
@@ -1,3 +1,2 @@
 node_modules
 .npm
-**/build/**
diff --git a/renderer/build/.gitignore b/renderer/build/.gitignore
new file mode 100644
index 00000000..8d8811be
--- /dev/null
+++ b/renderer/build/.gitignore
@@ -0,0 +1,5 @@
+# Ignore everything
+*
+
+# execpt this file
+!.gitignore
diff --git a/renderer/src/package.json b/renderer/src/package.json
index cbc99ac5..2493bcbc 100644
--- a/renderer/src/package.json
+++ b/renderer/src/package.json
@@ -5,7 +5,7 @@
   "main": "index.js",
   "scripts": {
     "test": "echo \"Error: no test specified\" && exit 1",
-    "watch": "mkdir ../build && node_modules/.bin/watchify index.js -t babelify -p livereactload -o ../build/bundle.js -v"
+    "watch": "node_modules/.bin/watchify index.js -t babelify -p livereactload -o ../build/bundle.js -v"
   },
   "author": "chriddyp",
   "license": "MIT",
