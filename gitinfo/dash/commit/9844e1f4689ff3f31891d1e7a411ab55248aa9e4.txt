commit 9844e1f4689ff3f31891d1e7a411ab55248aa9e4
Author: Chris P <chris@plot.ly>
Date:   Tue Apr 16 23:45:31 2019 -0400

    pylint & flake8

diff --git a/dash/development/_py_components_generation.py b/dash/development/_py_components_generation.py
index 760c73b2..66db6a12 100644
--- a/dash/development/_py_components_generation.py
+++ b/dash/development/_py_components_generation.py
@@ -448,19 +448,20 @@ def map_js_to_py_types_prop_types(type_object):
 
     def shape_or_exact():
         return 'dict containing keys {}.\n{}'.format(
-        ', '.join(
-            "'{}'".format(t)
-            for t in list(type_object['value'].keys())),
-        'Those keys have the following types:\n{}'.format(
-            '\n'.join(
-                create_prop_docstring(
-                    prop_name=prop_name,
-                    type_object=prop,
-                    required=prop['required'],
-                    description=prop.get('description', ''),
-                    indent_num=1
-                ) for prop_name, prop in
-                list(type_object['value'].items()))))
+            ', '.join(
+                "'{}'".format(t) for t in list(type_object['value'].keys())
+            ),
+            'Those keys have the following types:\n{}'.format(
+                '\n'.join(
+                    create_prop_docstring(
+                        prop_name=prop_name,
+                        type_object=prop,
+                        required=prop['required'],
+                        description=prop.get('description', ''),
+                        indent_num=1
+                    ) for prop_name, prop in
+                    list(type_object['value'].items())))
+            )
 
     return dict(
         array=lambda: 'list',
