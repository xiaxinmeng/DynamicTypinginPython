commit 01a75ab8ae10e61e3d29afa2a32d4744aca70fb5
Author: chriddyp <chris@plot.ly>
Date:   Mon Jun 19 11:16:07 2017 -0400

    add a classname to loading div to allow users to style

diff --git a/src/Authentication.react.js b/src/Authentication.react.js
index d00759b2..47c2432c 100644
--- a/src/Authentication.react.js
+++ b/src/Authentication.react.js
@@ -138,7 +138,7 @@ class UnconnectedOauthRedirect extends Component {
         let content;
         if (isEmpty(loginRequest) || loginRequest.status === 'loading') {
 
-            content = <div>Loading...</div>;
+            content = <div className="_dash-loading">Loading...</div>;
 
         } else if (loginRequest.status === 200) {
 
@@ -225,7 +225,7 @@ class Authentication extends Component {
 
         if (type(config) === "Null") {
 
-            return <div>Loading...</div>;
+            return <div className="_dash-loading">Loading...</div>;
 
         }
 
