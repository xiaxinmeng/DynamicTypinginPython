commit 03288a163368928e821d5376eb9c321e45397aa4
Author: Ryan Marren <rymarr@tuta.io>
Date:   Mon Jun 11 23:05:21 2018 -0400

    update generate_classes and tests to automatically look in lib/

diff --git a/dash/development/component_loader.py b/dash/development/component_loader.py
index 5ef84604..00479b61 100644
--- a/dash/development/component_loader.py
+++ b/dash/development/component_loader.py
@@ -55,8 +55,7 @@ def load_components(metadata_path,
     return components
 
 
-def generate_classes(metadata_path,
-                     namespace='default_namespace'):
+def generate_classes(metadata_path='lib/metadata.json', output_dir='lib'):
     """Load React component metadata into a format Dash can parse,
     then create python class files.
 
@@ -66,6 +65,8 @@ def generate_classes(metadata_path,
     metadata_path -- a path to a JSON file created by
     [`react-docgen`](https://github.com/reactjs/react-docgen).
 
+    output_dir -- the directory to put component classes in
+
     Returns:
     """
 
@@ -85,5 +86,5 @@ def generate_classes(metadata_path,
             name,
             componentData['props'],
             componentData['description'],
-            namespace
+            output_dir
         )
diff --git a/tests/development/test_base_component.py b/tests/development/test_base_component.py
index 70aab9ea..90c64f76 100644
--- a/tests/development/test_base_component.py
+++ b/tests/development/test_base_component.py
@@ -547,8 +547,8 @@ class TestGenerateClassFile(unittest.TestCase):
 
     def test_class_file(self):
         self.assertEqual(
-            self.expected_class_string[:100],
-            self.written_class_string[:100]
+            self.expected_class_string,
+            self.written_class_string
         )
 
 
diff --git a/tests/development/test_component_loader.py b/tests/development/test_component_loader.py
index ba36b8a0..cff677b6 100644
--- a/tests/development/test_component_loader.py
+++ b/tests/development/test_component_loader.py
@@ -6,7 +6,6 @@ import unittest
 from dash.development.component_loader import load_components, generate_classes
 from dash.development.base_component import (
     generate_class,
-    generate_class_file,
     Component
 )
 
@@ -186,7 +185,7 @@ class TestGenerateClasses(unittest.TestCase):
             'default_namespace'
         )
 
-        generate_classes(METADATA_PATH)
+        generate_classes(METADATA_PATH, 'default_namespace')
         from default_namespace.MyComponent import MyComponent \
             as MyComponent_buildtime
         from default_namespace.A import A as A_buildtime
