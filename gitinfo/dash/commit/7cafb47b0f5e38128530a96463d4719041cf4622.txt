commit 7cafb47b0f5e38128530a96463d4719041cf4622
Author: Philippe Duval <philippe@plot.ly>
Date:   Wed Jul 4 16:16:06 2018 -0400

    Add support for meta tags.

diff --git a/dash/dash.py b/dash/dash.py
index 43716a98..96f6435c 100644
--- a/dash/dash.py
+++ b/dash/dash.py
@@ -75,12 +75,14 @@ class Dash(object):
             'suppress_callback_exceptions': False,
             'routes_pathname_prefix': url_base_pathname,
             'requests_pathname_prefix': url_base_pathname,
-            'include_static_files': True,
+            'include_static_files': include_static_files,
         })
 
         # list of dependencies
         self.callback_map = {}
 
+        self._meta_tags = collections.OrderedDict()
+
         if compress:
             # gzip
             Compress(self.server)
@@ -318,6 +320,7 @@ class Dash(object):
         scripts = self._generate_scripts_html()
         css = self._generate_css_dist_html()
         config = self._generate_config_html()
+        metas = self._meta_tags.values()
         title = getattr(self, 'title', 'Dash')
         return '''
         <!DOCTYPE html>
@@ -609,6 +612,9 @@ class Dash(object):
                         'static_path': path
                     })
 
+    def add_meta_tag(self, name, content):
+        self._meta_tags[name] = {'name': name, 'content': content}
+
     def run_server(self,
                    port=8050,
                    debug=False,
