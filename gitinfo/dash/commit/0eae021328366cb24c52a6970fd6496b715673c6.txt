commit 0eae021328366cb24c52a6970fd6496b715673c6
Author: byron <byron@plot.ly>
Date:   Mon Aug 12 15:43:18 2019 -0400

    test package build

diff --git a/.circleci/config.yml b/.circleci/config.yml
index efc5a15f..7b335668 100644
--- a/.circleci/config.yml
+++ b/.circleci/config.yml
@@ -183,7 +183,8 @@ jobs:
         environment:
           PERCY_PARALLEL_TOTAL: '-1'
           PYVERSION: python37
-    parallelism: 3
+          PERCY_ENABLE: '0'
+    parallelism: 1
     steps:
       - checkout
       - run: echo $PYVERSION > ver.txt
@@ -194,11 +195,11 @@ jobs:
       - run:
           name: 🧪 Run Integration Tests
           command: |
-            . venv/bin/activate
-            cd packages && ls -la
+            . venv/bin/activate && cd packages && ls -la
             find . -name "*.gz" | xargs pip install --force-reinstall && pip list | grep dash && cd ..
             TESTFILES=$(circleci tests glob "tests/integration/**/test_*.py" | circleci tests split --split-by=timings)
-            pytest --headless --nopercyfinalize --junitxml=test-reports/junit_intg.xml ${TESTFILES}
+            pytest --headless tests/integration/test_calendar_props.py
+            # pytest --headless --nopercyfinalize --junitxml=test-reports/junit_intg.xml ${TESTFILES}
       - store_artifacts:
           path: test-reports
       - store_test_results:
