commit b09f08259a87eee9d6f974354d534469a1419f5c
Author: Chris P <chris@plot.ly>
Date:   Mon Apr 15 23:53:02 2019 -0400

    prettier

diff --git a/src/actions/index.js b/src/actions/index.js
index edc255d2..35027528 100644
--- a/src/actions/index.js
+++ b/src/actions/index.js
@@ -912,9 +912,11 @@ function updateOutput(
                         type: 'backEnd',
                         error: {
                             message: `Callback error updating ${
-                                isMultiOutputProp(payload.output) ?
-                                parseMultipleOutputs(payload.output).join(', ') :
-                                payload.output
+                                isMultiOutputProp(payload.output)
+                                    ? parseMultipleOutputs(payload.output).join(
+                                          ', '
+                                      )
+                                    : payload.output
                             }`,
                             html: text,
                         },
