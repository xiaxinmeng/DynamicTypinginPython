commit 8b4b8bb62be2d02da012fb4b0fbf9242d8cc29ac
Author: chriddyp <chris@plot.ly>
Date:   Mon Jun 5 15:50:53 2017 -0400

    use canonical `R.merge` instead of `Object.assign`

diff --git a/src/reducers/paths.js b/src/reducers/paths.js
index 5da7f823..78fe7b6e 100644
--- a/src/reducers/paths.js
+++ b/src/reducers/paths.js
@@ -21,7 +21,7 @@ const paths = (state = initialPaths, action) => {
                 ), R.keys(state));
                 newState = R.omit(removeKeys, state);
             } else {
-                newState = Object.assign({}, state);
+                newState = R.merge({}, state);
             }
 
             crawlLayout(subTree, function assignPath(child, itempath) {
