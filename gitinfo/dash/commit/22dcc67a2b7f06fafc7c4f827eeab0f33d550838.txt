commit 22dcc67a2b7f06fafc7c4f827eeab0f33d550838
Author: chriddyp <chris@plot.ly>
Date:   Tue Sep 5 18:11:04 2017 -0400

    another check that flake8 is running on circleci

diff --git a/dash/dash.py b/dash/dash.py
index 8f7d7d17..e725d649 100644
--- a/dash/dash.py
+++ b/dash/dash.py
@@ -344,10 +344,10 @@ class Dash(object):
 
             if not isinstance(args, list):
                 raise exceptions.IncorrectTypeException(
-                    'The {} argument `{}` is '
+                    'The {} argument `{}` is                                         '
                     'not a list of `dash.dependencies.{}`s.'.format(
                         name.lower(), str(args), name
-                    )) asdf
+                    ))
 
             for arg in args:
                 if not isinstance(arg, object):
diff --git a/setup.py b/setup.py
index 5094cbe0..4aa24c17 100644
--- a/setup.py
+++ b/setup.py
@@ -10,8 +10,7 @@ setup(
     author_email='chris@plot.ly',
     packages=find_packages(exclude=['tests*']),
     license='MIT',
-    description=('A Python framework for building reactive web-apps.' asdf
-                 'Developed by Plotly.'),
+    description=('A Python framework for building reactive web-apps.        Developed by Plotly.'),
     long_description=io.open('README.md', encoding='utf-8').read(),
     install_requires=[
         'Flask>=0.12',
