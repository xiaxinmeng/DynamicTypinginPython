commit 8cc99ccff2fa9c635b26d3799a3f17001bcc4c7d
Author: Per Nilsson <per.nilsson@formidablelabs.com>
Date:   Tue Jul 12 09:14:06 2016 -0700

    Add `content` kwarg in generate_class
    
    For some reason, passing `content` in `component_arguments`
    into `generate_class` doesn't work. Need to define it inside
    `generate_class` in `keyword_arguments`. Some subtle :snake:
    nuance I don't understand.

diff --git a/dash/dash/development/base_component.py b/dash/dash/development/base_component.py
index 93ac6367..74792fda 100644
--- a/dash/dash/development/base_component.py
+++ b/dash/dash/development/base_component.py
@@ -101,8 +101,9 @@ def generate_class(typename, component_arguments, setup):
             else:
                 return '{typename}(' + repr(self.content) + ')'
     '''
-    # TODO: This can probably be simplified:
-    keyword_arguments = [s for s in component_arguments]
+    # every component will at least have `content` argument
+    keyword_arguments = ['content']
+    keyword_arguments.extend([s for s in component_arguments if s not in keyword_arguments])
     list_of_valid_keys = repr(keyword_arguments)
     bullet_list_of_valid_keys = ('- ' + ' (dflt: None)\n- '.join(keyword_arguments) +
                                  ' (dflt: None)')
diff --git a/dash/dash/development/component_loader.py b/dash/dash/development/component_loader.py
index 70de2794..83b53f7c 100644
--- a/dash/dash/development/component_loader.py
+++ b/dash/dash/development/component_loader.py
@@ -7,7 +7,7 @@ def empty(self):
 
 
 def load_components(metadata_path,
-                    default_props=['content', 'id', 'key', 'className', 'style',
+                    default_props=['id', 'key', 'className', 'style',
                                    'dependencies', 'dangerouslySetInnerHTML',
                                    'suppressContentEditableWarning'],
                     namespace={},
