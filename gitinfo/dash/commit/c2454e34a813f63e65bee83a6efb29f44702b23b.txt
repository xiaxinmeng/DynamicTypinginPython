commit c2454e34a813f63e65bee83a6efb29f44702b23b
Author: Ryan Marren <rymarr@tuta.io>
Date:   Thu Oct 18 12:53:28 2018 -0400

    Try to fix just by not using python -m venv

diff --git a/.circleci/config.yml b/.circleci/config.yml
index 1fec1512..ff25e415 100644
--- a/.circleci/config.yml
+++ b/.circleci/config.yml
@@ -26,7 +26,7 @@ jobs:
           name: Install dependencies
           command: |
               sudo pip install virtualenv
-              python -m venv venv || virtualenv venv
+              virtualenv venv
               . venv/bin/activate
               pip install -r $REQUIREMENTS_FILE
 
diff --git a/tests/development/test_component_loader.py b/tests/development/test_component_loader.py
index 1be925b0..e36de5f9 100644
--- a/tests/development/test_component_loader.py
+++ b/tests/development/test_component_loader.py
@@ -165,8 +165,6 @@ class TestGenerateClasses(unittest.TestCase):
         init_file_path = 'default_namespace/__init__.py'
         with open(init_file_path, 'a'):
             os.utime(init_file_path, None)
-        # This is not the case on Python 3 CI boxes for some reason
-        sys.path.append(os.getcwd() + '/')
 
     def tearDown(self):
         os.remove(METADATA_PATH)
