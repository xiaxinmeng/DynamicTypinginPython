commit 6121b39db307799742d5f53c27215f2f3ada3349
Author: byron <byron@plot.ly>
Date:   Sun Jun 16 21:00:07 2019 -0400

    :wrench: add job name as key

diff --git a/.circleci/config.yml b/.circleci/config.yml
index 3a96d6d1..0176d65c 100644
--- a/.circleci/config.yml
+++ b/.circleci/config.yml
@@ -22,9 +22,10 @@ jobs:
             echo "RUNNING JOB: ${CIRCLE_JOB}"
             echo "JOB PARALLELISM: ${CIRCLE_NODE_TOTAL}"
             echo "CIRCLE_REPOSITORY_URL: ${CIRCLE_REPOSITORY_URL}"
+            echo $CIRCLE_JOB > circlejob.txt
 
       - restore_cache:
-          key: py-{{ checksum "requires-ci.txt" }}-{{ checksum "requires-install.txt" }}
+          key: v-{{ checksum "circlejob.txt" }}-{{ checksum "requires-ci.txt" }}-{{ checksum "requires-install.txt" }}
       - run:
           name: 🚧 pip dev requirements
           command: |
@@ -33,7 +34,7 @@ jobs:
             . venv/bin/activate
             pip install -r requires-install.txt -r requires-ci.txt --quiet
       - save_cache:
-          key: py-{{ checksum "requires-ci.txt" }}-{{ checksum "requires-install.txt" }}
+          key: v-{{ checksum "circlejob.txt" }}-{{ checksum "requires-ci.txt" }}-{{ checksum "requires-install.txt" }}
           paths:
               - "venv"
 
