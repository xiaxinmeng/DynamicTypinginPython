commit ee8a4898c2f8b2d1ce668bd97494957eeb1a57e9
Author: Ryan Marren <rymarr@tuta.io>
Date:   Wed Jul 18 21:50:48 2018 -0400

    Only validate callback to throw exception after failure to serialize.

diff --git a/dash/dash.py b/dash/dash.py
index a940aea9..bfaa23be 100644
--- a/dash/dash.py
+++ b/dash/dash.py
@@ -617,7 +617,6 @@ class Dash(object):
             def add_context(*args, **kwargs):
 
                 output_value = func(*args, **kwargs)
-                self._validate_callback_output(output_value, output)
                 response = {
                     'response': {
                         'props': {
@@ -626,9 +625,16 @@ class Dash(object):
                     }
                 }
 
+                try:
+                    jsonResponse = json.dumps(
+                        response,
+                        cls=plotly.utils.PlotlyJSONEncoder
+                    )
+                except TypeError:
+                    self._validate_callback_output(output_value, output)
+
                 return flask.Response(
-                    json.dumps(response,
-                               cls=plotly.utils.PlotlyJSONEncoder),
+                    jsonResponse,
                     mimetype='application/json'
                 )
 
diff --git a/dev-requirements.txt b/dev-requirements.txt
index 282b362e..f667e354 100644
--- a/dev-requirements.txt
+++ b/dev-requirements.txt
@@ -12,4 +12,4 @@ six
 plotly>=2.0.8
 requests[security]
 flake8
-pylint
+pylint==1.9.2
