commit c99b4c2cbdea51a2bed10008c2ed462864253b5c
Author: chriddyp <chris@plot.ly>
Date:   Tue Sep 5 18:14:55 2017 -0400

    fix intentional flake8 issues

diff --git a/dash/dash.py b/dash/dash.py
index e725d649..3abee056 100644
--- a/dash/dash.py
+++ b/dash/dash.py
@@ -344,7 +344,7 @@ class Dash(object):
 
             if not isinstance(args, list):
                 raise exceptions.IncorrectTypeException(
-                    'The {} argument `{}` is                                         '
+                    'The {} argument `{}` is '
                     'not a list of `dash.dependencies.{}`s.'.format(
                         name.lower(), str(args), name
                     ))
diff --git a/setup.py b/setup.py
index c254cea3..d1a0e4c2 100644
--- a/setup.py
+++ b/setup.py
@@ -10,7 +10,8 @@ setup(
     author_email='chris@plot.ly',
     packages=find_packages(exclude=['tests*']),
     license='MIT',
-    description=('A Python framework for building reactive web-apps.        Developed by Plotly.'),
+    description=('A Python framework for building reactive web-apps. '
+        'Developed by Plotly.'),
     long_description=io.open('README.md', encoding='utf-8').read(),
     install_requires=[
         'Flask>=0.12',
