commit 76e256103a8086be7f34e47817b082be5b10dfb8
Author: byron <byron@plot.ly>
Date:   Thu Apr 11 16:40:16 2019 -0400

    :bug: add fix for circular dependencies error

diff --git a/src/actions/index.js b/src/actions/index.js
index 911c6c2b..368f723b 100644
--- a/src/actions/index.js
+++ b/src/actions/index.js
@@ -56,17 +56,21 @@ function triggerDefaultState(dispatch, getState) {
     const {InputGraph, MultiGraph} = graphs;
     const allNodes = InputGraph.overallOrder();
     // overallOrder will assert circular dependencies for multi output.
+
     try {
         MultiGraph.overallOrder();
-    } catch (err) {
+    } catch(err) {
         dispatch(
             onError({
                 type: 'backEnd',
-                errorPage: err,
+                error: {
+                    message: 'Circular Dependencies',
+                    html: err.toString(),
+                }
             })
         );
-        return;
     }
+
     const inputNodeIds = [];
     allNodes.reverse();
     allNodes.forEach(nodeId => {
