commit f0cd7f72259b763cbbaa3b9c2e2457e0d8dcf260
Author: Philippe Duval <philippe@plot.ly>
Date:   Thu Nov 1 11:49:58 2018 -0400

    Add late component register test.

diff --git a/tests/test_integration.py b/tests/test_integration.py
index c507b80a..ef433030 100644
--- a/tests/test_integration.py
+++ b/tests/test_integration.py
@@ -531,3 +531,27 @@ class Tests(IntegrationTests):
 
         self.startServer(app)
         time.sleep(0.5)
+
+    def test_late_component_register(self):
+        app = dash.Dash()
+
+        app.layout = html.Div([
+            html.Button('Click me to put a dcc ', id='btn-insert'),
+            html.Div(id='output')
+        ])
+
+        @app.callback(Output('output', 'children'),
+                      [Input('btn-insert', 'n_clicks')])
+        def update_output(value):
+            if value is None:
+                raise PreventUpdate
+
+            return dcc.Input(id='inserted-input')
+
+        self.startServer(app)
+
+        btn = self.wait_for_element_by_css_selector('#btn-insert')
+        btn.click()
+        time.sleep(1)
+
+        self.wait_for_element_by_css_selector('#inserted-input')
