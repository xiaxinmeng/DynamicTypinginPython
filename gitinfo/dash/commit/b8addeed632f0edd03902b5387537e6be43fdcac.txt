commit b8addeed632f0edd03902b5387537e6be43fdcac
Author: byron <byron@plot.ly>
Date:   Fri Sep 6 17:04:00 2019 -0400

    :camel: nicer

diff --git a/dash-renderer/webpack.config.js b/dash-renderer/webpack.config.js
index e60880ee..81f1bda7 100644
--- a/dash-renderer/webpack.config.js
+++ b/dash-renderer/webpack.config.js
@@ -56,16 +56,13 @@ const defaultOptions = {
 };
 
 module.exports = (_, argv) => {
-    const build = argv.build || 'release';
+    const devtool = argv.build === 'local' ? 'source-map' : 'none';
     return [
-        R.mergeDeepLeft(
-            {devtool: build === 'release' ? 'none' : 'source-map'},
-            defaultOptions
-        ),
+        R.mergeDeepLeft({devtool}, defaultOptions),
         R.mergeDeepLeft(
             {
+                devtool: devtool,
                 mode: 'production',
-                devtool: build === 'release' ? 'none' : 'source-map',
                 output: {
                     filename: `${dashLibraryName}.min.js`,
                 },
