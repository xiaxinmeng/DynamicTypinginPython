commit 2315029736d2515138e904c4e49a9c27db302560
Author: chriddyp <chris@plot.ly>
Date:   Fri Jun 9 15:00:11 2017 -0400

    fallback to None if `value` isn't supplied
    
    this matches the way `input` is handled

diff --git a/dash/dash.py b/dash/dash.py
index 8c3d4ef4..560e4f1b 100644
--- a/dash/dash.py
+++ b/dash/dash.py
@@ -574,7 +574,7 @@ class Dash(object):
         for component_registration in self.callback_map[target_id]['state']:
             component_id = component_registration['id']
             args.append([
-                c['value'] for c in state if
+                c.get('value', None) for c in state if
                 c['property'] == component_registration['property'] and
                 c['id'] == component_registration['id']
             ][0])
