commit 9334718df816402fb5b912ee6bc4d946fe6bbee4
Author: Per Nilsson <per.nilsson@formidablelabs.com>
Date:   Wed Jun 15 14:40:49 2016 -0700

    :dolls::cow: document propTypes and fix some issues
    
    - :cow: Better code stylez
    - :bug: Make InputControl a controlled input to get rid of React warning.

diff --git a/component-suites/src/basic/components/EditableDiv.react.js b/component-suites/src/basic/components/EditableDiv.react.js
index b0ac8f7c..ae63ddcb 100644
--- a/component-suites/src/basic/components/EditableDiv.react.js
+++ b/component-suites/src/basic/components/EditableDiv.react.js
@@ -1,12 +1,5 @@
-/*
- * Example of a pretty generic editable component.
- * These types of components would live in their own set of modules ("component suites").
- * This component illustrates how the `onPropUpdate` prop can be used
- */
-
-
 import R from 'ramda';
-import React, { PropTypes } from 'react';
+import React, {Component, PropTypes} from 'react';
 import ReactDOM from 'react-dom';
 
 // each suite might have its own set of Styles
@@ -20,9 +13,9 @@ const baseStyles = {
 }
 
 /**
- * A div for displaying text that can be editable.
+ * A div for displaying text. The text is editable.
  */
-export default class EditableDiv extends React.Component {
+export default class EditableDiv extends Component {
 
     constructor(props) {
         super(props);
diff --git a/component-suites/src/basic/components/Header.react.js b/component-suites/src/basic/components/Header.react.js
index 038c0626..f418ecbc 100644
--- a/component-suites/src/basic/components/Header.react.js
+++ b/component-suites/src/basic/components/Header.react.js
@@ -1,11 +1,22 @@
 'use strict';
 
-import React from 'react';
+import React, {Component, PropTypes} from 'react';
 
-class Header extends React.Component {
+/**
+ * A component that greets a person in a header
+ */
+class Header extends Component {
     render() {
-        return <div>hlo {this.props.name} </div>;
+        return <h2>Hello, {this.props.name}</h2>;
     }
 }
 
+Header.propTypes = {
+
+    /**
+     * The name of the person you want to greet
+     */
+    name: PropTypes.string.isRequired
+};
+
 export default Header;
diff --git a/component-suites/src/basic/components/InputControl.react.js b/component-suites/src/basic/components/InputControl.react.js
index c3c7de0c..fc1d4a5f 100644
--- a/component-suites/src/basic/components/InputControl.react.js
+++ b/component-suites/src/basic/components/InputControl.react.js
@@ -1,12 +1,45 @@
-import React from 'react';
+import React, {Component, PropTypes} from 'react';
 
 /*
- * Example of a control that handles the notifyObservers prop
+ * A controlled input that calls `notifyObservers` on changes.
  */
+export default class InputControl extends Component {
+    constructor() {
+        super();
+        this.state = {
+            value: ''
+        }
+    }
 
-export default props => (
-    <input
-        onChange={e => props.notifyObservers({value: e.target.value})}
-        {...props}
-    />
-);
+    handleChange(value) {
+        this.setState({value});
+        /**
+         * TODO (#22): Remove conditional. Always pass a callback function
+         * to components that can change value.
+         */
+        if (this.props.notifyObservers) {
+            this.props.notifyObservers({value})
+        }
+    }
+
+    render() {
+        return (
+            <input
+                value={this.state.value}
+                onChange={e => this.handleChange(e.target.value)}
+                {...this.props}
+            />
+        );
+    }
+}
+
+InputControl.propTypes = {
+
+    /**
+     * Function that updates the state tree.
+     * Passed in from renderer IF the component has observers.
+     * TODO (#22): Always pass a callback function to components that can change
+     * value.
+     */
+    notifyObservers: PropTypes.func
+};
diff --git a/renderer/src/components/core/EditableContent.react.js b/renderer/src/components/core/EditableContent.react.js
index bf2baff3..4897c082 100644
--- a/renderer/src/components/core/EditableContent.react.js
+++ b/renderer/src/components/core/EditableContent.react.js
@@ -1,4 +1,4 @@
-import React, { PropTypes } from 'react';
+import React, {PropTypes} from 'react';
 
 import { connect } from 'react-redux'
 import { updateProps } from '../../actions'
diff --git a/renderer/src/components/core/NotifyObservers.react.js b/renderer/src/components/core/NotifyObservers.react.js
index 1f78fb31..474f514a 100644
--- a/renderer/src/components/core/NotifyObservers.react.js
+++ b/renderer/src/components/core/NotifyObservers.react.js
@@ -1,4 +1,4 @@
-import React, { PropTypes } from 'react';
+import React, {PropTypes} from 'react';
 import { connect } from 'react-redux';
 
 import { notifyObservers, updateProps } from '../../actions';
