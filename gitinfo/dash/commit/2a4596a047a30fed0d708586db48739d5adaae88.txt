commit 2a4596a047a30fed0d708586db48739d5adaae88
Author: Philippe Duval <philippe@plot.ly>
Date:   Fri Sep 7 14:52:21 2018 -0400

    Remove unused percy vars.

diff --git a/.circleci/config.yml b/.circleci/config.yml
index e3d31d4b..1fec1512 100644
--- a/.circleci/config.yml
+++ b/.circleci/config.yml
@@ -7,15 +7,10 @@ jobs:
         environment:
           REQUIREMENTS_FILE: dev-requirements.txt
           PYLINTRC: .pylintrc
-          PERCY_PARALLEL_TOTAL: 1
 
     steps:
       - checkout
 
-      - run:
-          name: Set PERCY_NONCE
-          command: echo export PERCY_PARALLEL_NONCE=$CIRCLE_SHA1 >> $HOME/.circlerc
-
       - run:
           name: Write deps cache key
           command: cat "$REQUIREMENTS_FILE" > reqs.txt
@@ -65,7 +60,6 @@ jobs:
         environment:
           REQUIREMENTS_FILE: dev-requirements.txt
           PYLINTRC: .pylintrc
-          PERCY_PARALLEL_TOTAL: 1
           PERCY_ENABLE: 0
 
   "python-3.7":
@@ -75,7 +69,6 @@ jobs:
         environment:
           REQUIREMENTS_FILE: dev-requirements-py37.txt
           PYLINTRC: .pylintrc37
-          PERCY_PARALLEL_TOTAL: 1
           PERCY_ENABLE: 0
 
 
