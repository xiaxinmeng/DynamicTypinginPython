commit b20d16b1326d68b89692aace72b90cfa66beea52
Author: byron <byron@plot.ly>
Date:   Tue Mar 26 22:22:34 2019 -0400

    :rotating_light: lint issue

diff --git a/src/TreeContainer.js b/src/TreeContainer.js
index 92dccc31..0a63d7fd 100644
--- a/src/TreeContainer.js
+++ b/src/TreeContainer.js
@@ -180,11 +180,6 @@ function mergeProps(stateProps, dispatchProps, ownProps) {
     };
 }
 
-export const AugmentedTreeContainer = connect(
-    mapStateToProps,
-    mapDispatchToProps,
-    mergeProps
-)(TreeContainer);
 function getLoadingState(layout, requestQueue) {
     const ids = isLoadingComponent(layout) ?
         getNestedIds(layout) :
@@ -253,4 +248,4 @@ function isLoadingComponent(layout) {
 
 export const AugmentedTreeContainer = connect(mapStateToProps, mapDispatchToProps, mergeProps)(TreeContainer);
 
-export default AugmentedTreeContainer;
+export default AugmentedTreeContainer;
\ No newline at end of file
