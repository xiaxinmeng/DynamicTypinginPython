commit a49db4d0b67bc4b8c42d546dfde3a0632bd71ab0
Author: chriddyp <chris@plot.ly>
Date:   Mon Mar 6 16:56:33 2017 -0500

    clear the request queue

diff --git a/src/actions/index.js b/src/actions/index.js
index 026554c9..3f521ef0 100644
--- a/src/actions/index.js
+++ b/src/actions/index.js
@@ -59,7 +59,13 @@ export const initialize = function() {
                     const {graphs} = getState();
                     const {EventGraph} = graphs;
                     EventGraph.overallOrder().forEach(nodeId => {
-                        dispatch(notifyObservers({event: 'propChange', id: nodeId}));
+                        // Only fire updates for the visible controllers
+                        if (has(nodeId, getState().paths)) {
+                            dispatch(notifyObservers({
+                                event: 'propChange', id: nodeId
+                            }));
+                        }
+
                     });
                 }, 50)
             }));
diff --git a/tests/test_render.py b/tests/test_render.py
index 02f0d8b1..48c55985 100644
--- a/tests/test_render.py
+++ b/tests/test_render.py
@@ -340,6 +340,13 @@ class Tests(IntegrationTests):
             }
         )
 
+        self.assertEqual(
+            self.driver.execute_script(
+                'return window.store.getState().requestQueue'
+            ),
+            []
+        )
+
         # Take a screenshot with percy
         # self.percy_runner.snapshot(name='dash_core_components')
 
@@ -392,6 +399,13 @@ class Tests(IntegrationTests):
             len('hello world')
         )
 
+        self.assertEqual(
+            self.driver.execute_script(
+                'return window.store.getState().requestQueue'
+            ),
+            []
+        )
+
         assert_clean_console(self)
 
     def test_callbacks_generating_content(self):
@@ -484,6 +498,13 @@ class Tests(IntegrationTests):
 
         self.assertEqual(call_count.value, 2)
 
+        self.assertEqual(
+            self.driver.execute_script(
+                'return window.store.getState().requestQueue'
+            ),
+            []
+        )
+
         assert_clean_console(self)
 
     def test_radio_buttons_callbacks_generating_content(self):
@@ -642,6 +663,13 @@ class Tests(IntegrationTests):
                 )
             )
 
+            self.assertEqual(
+                self.driver.execute_script(
+                    'return window.store.getState().requestQueue'
+                ),
+                []
+            )
+
         def chapter1_assertions():
             paths = self.driver.execute_script(
                 'return window.store.getState().paths'
@@ -821,4 +849,11 @@ class Tests(IntegrationTests):
         self.assertEqual(output_1_call_count.value, 2)
         self.assertEqual(output_2_call_count.value, 0)
 
+        self.assertEqual(
+            self.driver.execute_script(
+                'return window.store.getState().requestQueue'
+            ),
+            []
+        )
+
         assert_clean_console(self)
