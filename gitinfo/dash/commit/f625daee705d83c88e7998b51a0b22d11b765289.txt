commit f625daee705d83c88e7998b51a0b22d11b765289
Author: Marc-André Rivet <marcandre.rivet@sociablelabs.com>
Date:   Wed Apr 1 15:34:02 2020 -0400

    - Loosen version requirements
    - Bump dash-renderer to 1.3.0
    - Bump dash to 1.10.0
    - Changelog
    - Format

diff --git a/CHANGELOG.md b/CHANGELOG.md
index 13c7a1f1..f28986d9 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -2,7 +2,7 @@
 All notable changes to `dash` will be documented in this file.
 This project adheres to [Semantic Versioning](http://semver.org/).
 
-## [Unreleased]
+## [1.10.0] - 2020-04-01
 ### Added
 - [#1134](https://github.com/plotly/dash/pull/1134) Allow `dash.run_server()` host and port parameters to be set with environment variables HOST & PORT, respectively
 
diff --git a/dash-renderer/package.json b/dash-renderer/package.json
index a8b2b590..306bbe04 100644
--- a/dash-renderer/package.json
+++ b/dash-renderer/package.json
@@ -1,6 +1,6 @@
 {
   "name": "dash-renderer",
-  "version": "1.2.4",
+  "version": "1.3.0",
   "description": "render dash components in react",
   "main": "dash_renderer/dash_renderer.min.js",
   "scripts": {
diff --git a/dash/dash.py b/dash/dash.py
index ab49ab0f..a90c7328 100644
--- a/dash/dash.py
+++ b/dash/dash.py
@@ -635,9 +635,9 @@ class Dash(object):
         )
 
     def _generate_config_html(self):
-        return (
-            '<script id="_dash-config" type="application/json">{}</script>'
-        ).format(json.dumps(self._config()))
+        return ('<script id="_dash-config" type="application/json">{}</script>').format(
+            json.dumps(self._config())
+        )
 
     def _generate_renderer(self):
         return (
diff --git a/dash/version.py b/dash/version.py
index 38cf6dbe..fcfdf383 100644
--- a/dash/version.py
+++ b/dash/version.py
@@ -1 +1 @@
-__version__ = "1.9.1"
+__version__ = "1.10.0"
diff --git a/requires-install.txt b/requires-install.txt
index 5ff0365d..5d40aabe 100644
--- a/requires-install.txt
+++ b/requires-install.txt
@@ -1,8 +1,8 @@
 Flask>=1.0.2
 flask-compress
 plotly
-dash_renderer==1.2.4
-dash-core-components==1.8.1
-dash-html-components==1.0.2
-dash-table==4.6.1
+dash_renderer>=1.2.4
+dash-core-components>=1.8.1
+dash-html-components>=1.0.2
+dash-table>=4.6.1
 future
\ No newline at end of file
diff --git a/tests/integration/renderer/test_persistence.py b/tests/integration/renderer/test_persistence.py
index 7d10f4d8..2fc5672b 100644
--- a/tests/integration/renderer/test_persistence.py
+++ b/tests/integration/renderer/test_persistence.py
@@ -386,7 +386,9 @@ def test_rdps010_toggle_persistence(dash_duo):
 
     dash_duo.find_element("#persistence-val").send_keys("2")
     dash_duo.wait_for_text_to_equal("#out", "a")
-    dash_duo.find_element("#persisted").send_keys(Keys.BACK_SPACE)  # persist falsy value
+    dash_duo.find_element("#persisted").send_keys(
+        Keys.BACK_SPACE
+    )  # persist falsy value
     dash_duo.wait_for_text_to_equal("#out", "")
 
     # alpaca not saved with falsy persistence
@@ -397,7 +399,7 @@ def test_rdps010_toggle_persistence(dash_duo):
     dash_duo.find_element("#persistence-val").send_keys("s")
     dash_duo.wait_for_text_to_equal("#out", "anchovies")
     dash_duo.find_element("#persistence-val").send_keys("2")
-    dash_duo.wait_for_text_to_equal('#out', "")
+    dash_duo.wait_for_text_to_equal("#out", "")
 
 
 def test_rdps011_toggle_persistence2(dash_duo):
