commit 7e2f82920a59a51baa050218b126a0fde8c9fa2b
Author: Per Nilsson <per.nilsson@formidablelabs.com>
Date:   Fri Jul 29 10:16:42 2016 -0700

    Serve up static file bundle via Flask
    
    From localhost:8050

diff --git a/config/partials/babel.js b/config/partials/babel.js
index f940db4e..8ad71ade 100644
--- a/config/partials/babel.js
+++ b/config/partials/babel.js
@@ -9,6 +9,7 @@ var SRC = path.join(ROOT, 'src');
 module.exports = function (config) {
     return partial(config, {
         module: {
+            noParse: /node_modules\/json-schema\/lib\/validate\.js/, // used to get `request` to work: https://github.com/request/request/issues/1920#issuecomment-171246043
             loaders: [
                 { test: /\.json$/, loader: 'json-loader' },
                 {
diff --git a/dash/dash/react.py b/dash/dash/react.py
index 4412492d..aabbb417 100644
--- a/dash/dash/react.py
+++ b/dash/dash/react.py
@@ -1,7 +1,7 @@
 import flask
 import plotly
 import json
-from flask import Flask
+from flask import Flask, url_for
 from flask.ext.cors import CORS
 
 
@@ -14,7 +14,11 @@ class Dash(object):
         if server is not None:
             self.server = server
         else:
-            self.server = flask.Flask(name)
+            self.server = Flask(name)
+
+        # The name and port number of the server.
+        # Required for subdomain support (e.g.: 'myapp.dev:5000')
+        self.server.config['SERVER_NAME'] = 'localhost:8050'
 
         CORS(self.server) # TODO: lock this down to dev node server port
 
@@ -32,6 +36,9 @@ class Dash(object):
             endpoint='{}_{}'.format(url_namespace, 'index'),
             view_func=self.index)
 
+        with self.server.app_context():
+            url_for('static', filename='bundle.js')
+
     def index(self):
         return flask.render_template('index.html')
 
diff --git a/dash/static b/dash/static
new file mode 120000
index 00000000..f0d627b3
--- /dev/null
+++ b/dash/static
@@ -0,0 +1 @@
+../renderer/build/
\ No newline at end of file
diff --git a/dash/templates/index.html b/dash/templates/index.html
new file mode 100644
index 00000000..9d2d05a3
--- /dev/null
+++ b/dash/templates/index.html
@@ -0,0 +1,15 @@
+<!DOCTYPE html>
+<html>
+    <head>
+        <meta charset="UTF-8" />
+    </head>
+    <body>
+        <div id="react-entry-point"></div>
+    </body>
+
+    <script type="text/javascript">
+
+    </script>
+
+    <script type="text/javascript" src="static/bundle.js"></script>
+</html>
