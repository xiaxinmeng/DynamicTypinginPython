commit 8ea4142ccb253af7f6fc1a7fc8d158e829cf81c4
Author: chriddyp <chris@plot.ly>
Date:   Mon Jun 19 16:35:01 2017 -0400

    optional csrf

diff --git a/dash/dash.py b/dash/dash.py
index 9b354e77..5ee7d7c4 100644
--- a/dash/dash.py
+++ b/dash/dash.py
@@ -31,7 +31,8 @@ class Dash(object):
         filename=None,
         sharing=None,
         app_url=None,
-        url_base_pathname='/'
+        url_base_pathname='/',
+        csrf_protect=True
     ):
         # allow users to supply their own flask server
         if server is not None:
@@ -75,7 +76,8 @@ class Dash(object):
         Compress(self.server)
 
         # csrf protect
-        self._csrf = SeaSurf(self.server)
+        if csrf_protect:
+            self._csrf = SeaSurf(self.server)
 
         # static files from the packages
         self.css = Css()
