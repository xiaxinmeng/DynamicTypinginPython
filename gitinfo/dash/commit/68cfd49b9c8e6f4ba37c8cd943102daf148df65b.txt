commit 68cfd49b9c8e6f4ba37c8cd943102daf148df65b
Author: byron <byron@plot.ly>
Date:   Sat Jun 15 14:07:32 2019 -0400

    :truck: rename intg => ci

diff --git a/.circleci/config.yml b/.circleci/config.yml
index 1cc12dae..3a96d6d1 100644
--- a/.circleci/config.yml
+++ b/.circleci/config.yml
@@ -24,16 +24,16 @@ jobs:
             echo "CIRCLE_REPOSITORY_URL: ${CIRCLE_REPOSITORY_URL}"
 
       - restore_cache:
-          key: py-{{ checksum "requires-intg.txt" }}-{{ checksum "requires-install.txt" }}
+          key: py-{{ checksum "requires-ci.txt" }}-{{ checksum "requires-install.txt" }}
       - run:
           name: 🚧 pip dev requirements
           command: |
             sudo pip install --upgrade virtualenv
             python -m venv venv || virtualenv venv
             . venv/bin/activate
-            pip install -r requires-install.txt -r requires-intg.txt --quiet
+            pip install -r requires-install.txt -r requires-ci.txt --quiet
       - save_cache:
-          key: py-{{ checksum "requires-intg.txt" }}-{{ checksum "requires-install.txt" }}
+          key: py-{{ checksum "requires-ci.txt" }}-{{ checksum "requires-install.txt" }}
           paths:
               - "venv"
 
@@ -41,7 +41,7 @@ jobs:
           name: 🌸 linting
           command: |
             . venv/bin/activate
-            pip install -e .[intg] --quiet
+            pip install -e .[ci] --quiet
             pip list | grep dash
             flake8 dash setup.py
             flake8 --ignore=E123,E126,E501,E722,E731,F401,F841,W503,W504 --exclude=metadata_test.py tests
diff --git a/requires-intg.txt b/requires-ci.txt
similarity index 100%
rename from requires-intg.txt
rename to requires-ci.txt
diff --git a/setup.py b/setup.py
index 10c92080..b607a274 100644
--- a/setup.py
+++ b/setup.py
@@ -26,7 +26,7 @@ setup(
     long_description=io.open("README.md", encoding="utf-8").read(),
     long_description_content_type="text/markdown",
     install_requires=read_req_file("install"),
-    extra_require={"intg": read_req_file("intg")},
+    extra_require={"ci": read_req_file("ci")},
     entry_points={
         "console_scripts": [
             "dash-generate-components ="
