commit b4773165f0a4146d15e8d54de497a622c065b529
Author: Chris P <chris@plot.ly>
Date:   Sat Apr 13 20:51:28 2019 -0400

    create artifacts folder before running tests

diff --git a/.circleci/config.yml b/.circleci/config.yml
index dfa206c8..b8dd634c 100644
--- a/.circleci/config.yml
+++ b/.circleci/config.yml
@@ -56,6 +56,12 @@ jobs:
               pip install -e . --quiet
               pip list | grep dash
 
+      - run:
+          name: Create artifacts folder
+          command: |
+            mkdir /tmp/artifacts;
+            echo "test artifact files in a dir" > /tmp/artifacts/artifact-2;
+
       - run:
           name: Run tests
           command: |
@@ -63,12 +69,6 @@ jobs:
               python --version
               npm run test:py
 
-      - run:
-          name: Create artifacts folder
-          command: |
-            mkdir /tmp/artifacts;
-            echo "test artifact files in a dir" > /tmp/artifacts/artifact-2;
-
       - store_artifacts:
           path: /tmp/artifacts
 
