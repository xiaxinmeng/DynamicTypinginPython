commit 6a6b791b509082ccc2f2daf65294c7844d95a9cd
Author: chriddyp <chris@plot.ly>
Date:   Tue Sep 5 18:08:33 2017 -0400

    introduce a syntax error to test if circleci is running flake8 ok

diff --git a/dash/dash.py b/dash/dash.py
index 3abee056..8f7d7d17 100644
--- a/dash/dash.py
+++ b/dash/dash.py
@@ -347,7 +347,7 @@ class Dash(object):
                     'The {} argument `{}` is '
                     'not a list of `dash.dependencies.{}`s.'.format(
                         name.lower(), str(args), name
-                    ))
+                    )) asdf
 
             for arg in args:
                 if not isinstance(arg, object):
diff --git a/setup.py b/setup.py
index c68f97b2..5094cbe0 100644
--- a/setup.py
+++ b/setup.py
@@ -10,7 +10,7 @@ setup(
     author_email='chris@plot.ly',
     packages=find_packages(exclude=['tests*']),
     license='MIT',
-    description=('A Python framework for building reactive web-apps.'
+    description=('A Python framework for building reactive web-apps.' asdf
                  'Developed by Plotly.'),
     long_description=io.open('README.md', encoding='utf-8').read(),
     install_requires=[
