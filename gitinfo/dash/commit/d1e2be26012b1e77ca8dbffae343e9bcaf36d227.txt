commit d1e2be26012b1e77ca8dbffae343e9bcaf36d227
Author: byron <byron@plot.ly>
Date:   Wed Jul 31 18:41:05 2019 -0400

    use the percy flag in CI

diff --git a/.circleci/config.yml b/.circleci/config.yml
index 7317925f..982c8d4f 100644
--- a/.circleci/config.yml
+++ b/.circleci/config.yml
@@ -160,7 +160,7 @@ jobs:
             . venv/bin/activate
             cd packages && ls -la && pip install * && cd .. && pip list | grep dash
             TESTFILES=$(circleci tests glob "tests/integration/**/test_*.py" | circleci tests split --split-by=timings)
-            pytest --headless --durations=10 --junitxml=test-reports/junit_intg.xml ${TESTFILES}
+            pytest --headless --percyfinalize --junitxml=test-reports/junit_intg.xml ${TESTFILES}
       - store_artifacts:
           path: test-reports
       - store_test_results:
