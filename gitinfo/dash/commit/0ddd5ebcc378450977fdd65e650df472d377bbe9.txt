commit 0ddd5ebcc378450977fdd65e650df472d377bbe9
Author: byron <byron@plot.ly>
Date:   Tue Aug 20 02:12:57 2019 -0400

    more improvements

diff --git a/dash/_utils.py b/dash/_utils.py
index 5de99267..c58b6102 100644
--- a/dash/_utils.py
+++ b/dash/_utils.py
@@ -1,6 +1,5 @@
 # -*- coding: utf-8 -*-
 from __future__ import unicode_literals
-from builtins import str as text
 import shlex
 import sys
 import uuid
@@ -8,6 +7,7 @@ import hashlib
 import collections
 import subprocess
 import logging
+from io import open
 from functools import wraps
 
 import six
@@ -145,8 +145,8 @@ def run_command_with_process(cmd):
 
 
 def compute_md5(path):
-    with open(path) as fp:
-        return hashlib.md5(text(fp.read()).encode("utf-8")).hexdigest()
+    with open(path, encoding='utf-8') as fp:
+        return hashlib.md5(fp.read()).hexdigest()
 
 
 def job(msg=""):
diff --git a/dash/development/build_renderer.py b/dash/development/build_renderer.py
index ba8ece55..fac5275d 100644
--- a/dash/development/build_renderer.py
+++ b/dash/development/build_renderer.py
@@ -1,4 +1,5 @@
 # -*- coding: utf-8 -*-
+from __future__ import unicode_literals
 import os
 import sys
 import json
@@ -69,13 +70,13 @@ def npm():
 def bundles():
     # make sure we start from fresh folder
     if os.path.exists(assets):
-        logger.warning(u"🚨 %s already exists, remove it!", assets)
+        logger.warning("🚨 %s already exists, remove it!", assets)
         shutil.rmtree(assets)
 
     try:
         os.makedirs(assets)
     except OSError:
-        logger.exception(u"🚨 having issues manipulating %s", assets)
+        logger.exception("🚨 having issues manipulating %s", assets)
         sys.exit(1)
 
     # parse the package-lock.json and copy bundles
