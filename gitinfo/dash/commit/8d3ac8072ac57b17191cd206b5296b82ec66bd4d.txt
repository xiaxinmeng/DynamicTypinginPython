commit 8d3ac8072ac57b17191cd206b5296b82ec66bd4d
Author: sherry0429 <sherry0429@outlook.com>
Date:   Wed Apr 1 11:17:20 2020 +0800

    add warn when apiThunk receive not application/json response

diff --git a/dash-renderer/src/actions/api.js b/dash-renderer/src/actions/api.js
index 4ab3891f..34f124a8 100644
--- a/dash-renderer/src/actions/api.js
+++ b/dash-renderer/src/actions/api.js
@@ -2,6 +2,9 @@ import {mergeDeepRight} from 'ramda';
 import {handleAsyncError, getCSRFHeader} from '../actions';
 import {urlBase} from '../utils';
 
+/* eslint-disable-next-line no-console */
+const logWarningOnce = once(console.warn);
+
 function GET(path, fetchConfig) {
     return fetch(
         path,
@@ -53,6 +56,7 @@ export default function apiThunk(endpoint, method, store, id, body) {
                         return json;
                     });
                 }
+                logWarningOnce("Response miss content-type application/json")
                 return dispatch({
                     type: store,
                     payload: {
