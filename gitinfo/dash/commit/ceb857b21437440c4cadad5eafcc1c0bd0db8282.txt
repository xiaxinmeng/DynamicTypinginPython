commit ceb857b21437440c4cadad5eafcc1c0bd0db8282
Author: Marc-André Rivet <marcandre.rivet@sociablelabs.com>
Date:   Mon Mar 4 14:06:06 2019 -0500

    ci latest dash-*

diff --git a/.circleci/config.yml b/.circleci/config.yml
index bc92a8bc..75f0d836 100644
--- a/.circleci/config.yml
+++ b/.circleci/config.yml
@@ -19,9 +19,6 @@ jobs:
           name: Write job name
           command: echo $CIRCLE_JOB > circlejob.txt
 
-      - restore_cache:
-          key: deps1-{{ .Branch }}-{{ checksum "reqs.txt" }}-{{ checksum ".circleci/config.yml" }}-{{ checksum "circlejob.txt" }}
-
       - run:
           name: Install dependencies
           command: |
@@ -30,10 +27,18 @@ jobs:
               . venv/bin/activate
               pip install -r $REQUIREMENTS_FILE --force-reinstall
 
-      - save_cache:
-          key: deps1-{{ .Branch }}-{{ checksum "reqs.txt" }}-{{ checksum ".circleci/config.yml" }}-{{ checksum "circlejob.txt" }}
-          paths:
-              - "venv"
+      - run:
+          name: Install dependencies (dash)
+          command: |
+              git clone git@github.com:plotly/dash-renderer.git
+              git clone git@github.com:plotly/dash-core-components.git
+              git clone git@github.com:plotly/dash-html-components.git
+              git clone git@github.com:plotly/dash-table.git
+              . venv/bin/activate
+              cd dash-renderer && npm install --ignore-scripts && npm run build && pip install -e . && cd ..
+              cd dash-core-components && npm install --ignore-scripts && npm run build && pip install -e . && cd ..
+              cd dash-html-components && npm install --ignore-scripts && npm run build && pip install -e . && cd ..
+              cd dash-table && npm install --ignore-scripts && npm run build && pip install -e . && cd ..
 
       - run:
           name: Run tests
diff --git a/.circleci/requirements/dev-requirements-py37.txt b/.circleci/requirements/dev-requirements-py37.txt
index e8efc9e7..004659e7 100644
--- a/.circleci/requirements/dev-requirements-py37.txt
+++ b/.circleci/requirements/dev-requirements-py37.txt
@@ -1,8 +1,5 @@
-dash_core_components==0.43.1
-dash_html_components==0.13.5
 dash-flow-example==0.0.5
 dash-dangerously-set-inner-html
--e git+git://github.com/plotly/dash-renderer@master#egg=dash_renderer
 percy
 selenium
 mock
diff --git a/.circleci/requirements/dev-requirements.txt b/.circleci/requirements/dev-requirements.txt
index c3132a06..cccb0601 100644
--- a/.circleci/requirements/dev-requirements.txt
+++ b/.circleci/requirements/dev-requirements.txt
@@ -1,8 +1,5 @@
-dash_core_components==0.43.1
-dash_html_components==0.13.5
 dash_flow_example==0.0.5
 dash-dangerously-set-inner-html
--e git+git://github.com/plotly/dash-renderer@master#egg=dash_renderer
 percy
 selenium
 mock
