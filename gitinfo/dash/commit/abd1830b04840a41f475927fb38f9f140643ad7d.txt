commit abd1830b04840a41f475927fb38f9f140643ad7d
Author: Ryan Marren <rymarr@tuta.io>
Date:   Mon Jul 16 20:00:57 2018 -0400

    Give a good error if a user calls `run_server` before setting `layout`.

diff --git a/dash/dash.py b/dash/dash.py
index 143d1e80..548602ce 100644
--- a/dash/dash.py
+++ b/dash/dash.py
@@ -565,4 +565,13 @@ class Dash(object):
                    port=8050,
                    debug=False,
                    **flask_run_options):
+        # Make sure `layout` is set before running the server
+        value = getattr(self, 'layout')
+        if value is None:
+            raise Exception(
+                ''
+                'The layout was `None` '
+                'at the time that `run_server` was called. '
+                'Make sure to set the `layout` attribute of your application '
+                'before running the server.')
         self.server.run(port=port, debug=debug, **flask_run_options)
