commit d56104ded18b4e8bcf9a9474fb8de99ba0a49dec
Author: Chris P <chris@plot.ly>
Date:   Mon Apr 15 12:37:58 2019 -0400

    Revert "bring back the tests"
    
    This reverts commit 02a3a4f53df36ea99d4f9e90d41348aa763b2468.

diff --git a/.circleci/config.yml b/.circleci/config.yml
index a8251e2e..f915f51b 100644
--- a/.circleci/config.yml
+++ b/.circleci/config.yml
@@ -5,7 +5,7 @@ jobs:
     docker:
       - image: circleci/python:2.7-stretch-node-browsers
     environment:
-      PERCY_PARALLEL_TOTAL: 2
+      PERCY_PARALLEL_TOTAL: 1
 
     working_directory: ~/repo
 
@@ -61,6 +61,7 @@ jobs:
           name: Create artifacts folder
           command: |
             mkdir /tmp/artifacts;
+            echo "test artifact files in a dir" > /tmp/artifacts/artifact-2;
 
       - run:
           name: Run tests
diff --git a/package.json b/package.json
index e73a043d..84915e95 100644
--- a/package.json
+++ b/package.json
@@ -16,7 +16,7 @@
     "format": "prettier --config .prettierrc --write src/**/*.js src/**/*.react.js",
     "format:test": "prettier --config .prettierrc src/**/*.js src/**/*.react.js --list-different",
     "test": "npm run lint",
-    "test:py": "python -W ignore -m unittest -v tests.test_render tests.test_race_conditions"
+    "test:py": "python -W ignore -m unittest -v tests.test_render"
   },
   "author": "chriddyp",
   "license": "MIT",
diff --git a/tests/test_render.py b/tests/test_render.py
index 4606fa06..8b58f92d 100644
--- a/tests/test_render.py
+++ b/tests/test_render.py
@@ -109,7 +109,7 @@ class Tests(IntegrationTests):
         if expected_length is not None:
             self.assertEqual(len(request_queue), expected_length)
 
-
+    """
     def test_initial_state(self):
         app = Dash(__name__)
         my_class_attrs = {
@@ -2258,7 +2258,7 @@ class Tests(IntegrationTests):
         self.wait_for_text_to_equal('#input', 'hello')
         self.wait_for_text_to_equal('#side-effect', 'side effect')
         self.wait_for_text_to_equal('#output', 'output')
-
+    """
 
     def test_devtools_python_errors(self):
         app = dash.Dash(__name__)
