commit 495762ee782fa64706212fe3e0a97bd1f61879ea
Author: Philippe Duval <philippe@plot.ly>
Date:   Wed Jul 25 12:37:56 2018 -0400

    pylint fixes.

diff --git a/dash/dash.py b/dash/dash.py
index 7399e835..44fad1ef 100644
--- a/dash/dash.py
+++ b/dash/dash.py
@@ -707,7 +707,7 @@ class Dash(object):
     def _setup_server(self):
         if self.config.include_assets_files:
             self._walk_assets_directory()
-      
+
         # Make sure `layout` is set before running the server
         value = getattr(self, 'layout')
         if value is None:
@@ -717,7 +717,7 @@ class Dash(object):
                 'at the time that `run_server` was called. '
                 'Make sure to set the `layout` attribute of your application '
                 'before running the server.')
-            
+
         self._generate_scripts_html()
         self._generate_css_dist_html()
 
