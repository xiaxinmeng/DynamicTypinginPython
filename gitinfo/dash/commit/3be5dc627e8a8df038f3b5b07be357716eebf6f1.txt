commit 3be5dc627e8a8df038f3b5b07be357716eebf6f1
Author: alexcjohnson <alex@plot.ly>
Date:   Tue Jun 4 13:17:03 2019 -0400

    remove Dash `**kwargs`, unused except to warn about csrf removal

diff --git a/dash/dash.py b/dash/dash.py
index 9b5058c4..54fb5013 100644
--- a/dash/dash.py
+++ b/dash/dash.py
@@ -9,7 +9,6 @@ import importlib
 import json
 import pkgutil
 import threading
-import warnings
 import re
 import logging
 import pprint
@@ -107,22 +106,12 @@ class Dash(object):
             suppress_callback_exceptions=None,
             components_cache_max_age=None,
             show_undo_redo=False,
-            plugins=None,
-            **kwargs):
+            plugins=None):
 
         # Store some flask-related parameters for use in init_app()
         self.compress = compress
         self.name = name
 
-        # pylint-disable: too-many-instance-attributes
-        if 'csrf_protect' in kwargs:
-            warnings.warn('''
-                `csrf_protect` is no longer used,
-                CSRF protection has been removed as it is no longer
-                necessary.
-                See https://github.com/plotly/dash/issues/141 for details.
-                ''', DeprecationWarning)
-
         self._assets_folder = os.path.join(
             flask.helpers.get_root_path(name),
             assets_folder,
