commit 6312be42a3582827172bf90a6a52a073e3976029
Author: t4rk1n <t4rk@outlook.com>
Date:   Wed Dec 26 15:31:43 2018 -0500

    :ok_hand: Add invalid characters in id check.

diff --git a/dash/dash.py b/dash/dash.py
index 2e5e93c0..bf48d365 100644
--- a/dash/dash.py
+++ b/dash/dash.py
@@ -679,11 +679,13 @@ class Dash(object):
                             name.lower(), str(arg), name
                         ))
 
-                if '.' in arg.component_id:
+                invalid_characters = ['.', ':', '[', ']']
+                if any(x in arg.component_id for x in invalid_characters):
                     raise exceptions.IDsCantContainPeriods('''The element
-                    `{}` contains a period in its ID.
+                    `{}` contains {} in its ID.
                     Periods are not allowed in IDs right now.'''.format(
-                        arg.component_id
+                        arg.component_id,
+                        invalid_characters
                     ))
 
                 if (not self.config.first('suppress_callback_exceptions',
