commit 348f9ec1dbf021df12940c7cff53fd800b991c9e
Author: t4rk1n <t4rk@outlook.com>
Date:   Thu Dec 27 13:19:24 2018 -0500

    :ok_hand: Clean up duplicate output logic.

diff --git a/dash/dash.py b/dash/dash.py
index 373a0ad0..e0f51f61 100644
--- a/dash/dash.py
+++ b/dash/dash.py
@@ -768,14 +768,14 @@ class Dash(object):
             for x in self.callback_map
         )))
         ns = {
-            'duplicates': callback_id
+            'duplicates': set()
         }
         if is_multi:
             def duplicate_check():
-                ns['duplicates'] = intersection = callbacks.intersection(
+                ns['duplicates'] = callbacks.intersection(
                     _create_callback_id(y) for y in output
                 )
-                return intersection
+                return ns['duplicates']
         else:
             def duplicate_check():
                 return callback_id in callbacks
