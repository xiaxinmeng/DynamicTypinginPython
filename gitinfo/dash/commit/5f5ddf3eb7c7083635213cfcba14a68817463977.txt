commit 5f5ddf3eb7c7083635213cfcba14a68817463977
Author: byron <byron@plot.ly>
Date:   Fri Sep 6 12:19:47 2019 -0400

    :alembic: this works well without sourcemap

diff --git a/dash-renderer/MANIFEST.in b/dash-renderer/MANIFEST.in
index b17522ce..0c9ca09a 100644
--- a/dash-renderer/MANIFEST.in
+++ b/dash-renderer/MANIFEST.in
@@ -1,4 +1,4 @@
 include package.json
 include digest.json
 include dash_renderer/*.js
-include dash_renderer/*.map
+exclude dash_renderer/*.map
diff --git a/dash-renderer/init.template b/dash-renderer/init.template
index 17d90ca5..abdf6bdb 100644
--- a/dash-renderer/init.template
+++ b/dash-renderer/init.template
@@ -42,12 +42,4 @@ _js_dist = [
         "/dash_renderer/dash_renderer.min.js",
         "namespace": "dash_renderer",
     },
-    {
-        "relative_package_path": "{}.min.js.map".format(__name__),
-        "dev_package_path": "{}.dev.js.map".format(__name__),
-        "external_url": "https://unpkg.com/dash-renderer@$version"
-        "/dash_renderer/dash_renderer.min.js.map",
-        "namespace": "dash_renderer",
-        "dynamic": True,
-    },
 ]
diff --git a/dash-renderer/webpack.config.js b/dash-renderer/webpack.config.js
index 31069b8e..92124790 100644
--- a/dash-renderer/webpack.config.js
+++ b/dash-renderer/webpack.config.js
@@ -32,12 +32,13 @@ module.exports = (env, argv) => {
             library: dashLibraryName,
             libraryTarget: 'window',
         },
-        devtool: 'source-map',
+        // devtool: mode === 'development' ? 'source-map' : 'none',
+        devtool: 'none',
         externals: {
             react: 'React',
             'react-dom': 'ReactDOM',
             'plotly.js': 'Plotly',
-            'prop-types': 'PropTypes'
+            'prop-types': 'PropTypes',
         },
         module: {
             rules: [
@@ -61,12 +62,11 @@ module.exports = (env, argv) => {
                 },
                 {
                     test: /\.svg$/,
-                    use: ['@svgr/webpack']
+                    use: ['@svgr/webpack'],
                 },
                 {
                     test: /\.txt$/i,
                     use: 'raw-loader',
-
                 },
             ],
         },
