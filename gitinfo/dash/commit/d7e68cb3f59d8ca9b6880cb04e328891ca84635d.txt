commit d7e68cb3f59d8ca9b6880cb04e328891ca84635d
Author: Ryan Marren <rymarr@tuta.io>
Date:   Wed Jul 18 21:16:53 2018 -0400

    Move test to _setup_server, freeze pylint version

diff --git a/dash/dash.py b/dash/dash.py
index 206e6d14..891dbeb9 100644
--- a/dash/dash.py
+++ b/dash/dash.py
@@ -558,13 +558,6 @@ class Dash(object):
         return self.callback_map[target_id]['callback'](*args)
 
     def _setup_server(self):
-        self._generate_scripts_html()
-        self._generate_css_dist_html()
-
-    def run_server(self,
-                   port=8050,
-                   debug=False,
-                   **flask_run_options):
         # Make sure `layout` is set before running the server
         value = getattr(self, 'layout')
         if value is None:
@@ -574,4 +567,11 @@ class Dash(object):
                 'at the time that `run_server` was called. '
                 'Make sure to set the `layout` attribute of your application '
                 'before running the server.')
+        self._generate_scripts_html()
+        self._generate_css_dist_html()
+
+    def run_server(self,
+                   port=8050,
+                   debug=False,
+                   **flask_run_options):
         self.server.run(port=port, debug=debug, **flask_run_options)
diff --git a/dev-requirements.txt b/dev-requirements.txt
index 282b362e..f667e354 100644
--- a/dev-requirements.txt
+++ b/dev-requirements.txt
@@ -12,4 +12,4 @@ six
 plotly>=2.0.8
 requests[security]
 flake8
-pylint
+pylint==1.9.2
