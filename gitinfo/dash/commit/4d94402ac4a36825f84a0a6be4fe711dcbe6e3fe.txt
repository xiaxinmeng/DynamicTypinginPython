commit 4d94402ac4a36825f84a0a6be4fe711dcbe6e3fe
Author: byron <byron@plot.ly>
Date:   Wed Sep 25 13:56:20 2019 -0400

    :loud_sound: add info traces for serving component suites

diff --git a/dash/dash.py b/dash/dash.py
index 3d5c181e..3b397532 100644
--- a/dash/dash.py
+++ b/dash/dash.py
@@ -677,6 +677,14 @@ class Dash(object):
             'map': 'application/json'
         })[path_in_package_dist.split('.')[-1]]
 
+        loader = pkgutil.get_loader(package_name)
+        self.logger.info(
+            "serving package %s resource %s => %s",
+            package_name,
+            path_in_package_dist,
+            loader.path if hasattr(loader, 'path') else loader,
+        )
+
         return flask.Response(
             pkgutil.get_data(package_name, path_in_package_dist),
             mimetype=mimetype
