commit 69f3da949a3e83d22433a58dc7fc44f9d8bbd393
Author: chriddyp <chris@plot.ly>
Date:   Thu Sep 7 09:48:16 2017 -0400

    Add comment describing url prefixes

diff --git a/src/utils.js b/src/utils.js
index b702bb6a..57cb455e 100644
--- a/src/utils.js
+++ b/src/utils.js
@@ -1,5 +1,10 @@
 import {has, type} from 'ramda';
 
+
+/*
+ * requests_pathname_prefix is the new config parameter introduced in
+ * dash==0.18.0. The previous versions just had url_base_pathname
+ */
 export function urlBase(config) {
     if (type(config) === "Null" ||
         (type(config) === "Object") &&
