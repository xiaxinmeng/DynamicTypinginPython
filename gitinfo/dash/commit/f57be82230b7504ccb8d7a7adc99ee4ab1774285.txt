commit f57be82230b7504ccb8d7a7adc99ee4ab1774285
Author: byron <byron@plot.ly>
Date:   Tue Nov 19 20:16:17 2019 -0500

    use kill instead of communicate

diff --git a/dash/testing/application_runners.py b/dash/testing/application_runners.py
index 67b14329..3c173f42 100644
--- a/dash/testing/application_runners.py
+++ b/dash/testing/application_runners.py
@@ -242,8 +242,8 @@ class ProcessRunner(BaseDashRunner):
                     self.proc.communicate(timeout=self.stop_timeout)
                 else:
                     _except = Exception
-                    logger.info('communicate')
-                    self.proc.communicate()
+                    logger.info('kill the proc')
+                    self.proc.kill()
             except _except:
                 logger.exception(
                     "subprocess terminate not success, trying to kill "
