commit 493f99d248693858591eaa18061fe66210c08a71
Author: Philippe Duval <philippe@plot.ly>
Date:   Wed Sep 12 10:41:18 2018 -0400

    Add comment on the filename in _on_assets_change.

diff --git a/dash/dash.py b/dash/dash.py
index 0e96a636..fe0d7a07 100644
--- a/dash/dash.py
+++ b/dash/dash.py
@@ -1073,6 +1073,8 @@ class Dash(object):
         return debug
 
     def _on_assets_change(self, _):
+        # The `_` argument is the name of the file that changed.
+        # If we ever setup a logging system, we could use the parameter.
         self._lock.acquire()
         self._hard_reload = True
         self._reload_hash = _generate_hash()
