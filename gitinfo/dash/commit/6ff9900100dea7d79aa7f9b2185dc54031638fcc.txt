commit 6ff9900100dea7d79aa7f9b2185dc54031638fcc
Author: Jae Chang <jchang10@gmail.com>
Date:   Wed Sep 4 08:40:11 2019 -0700

    Revert "mypy wants property getter and setter to be consecutive to each other."
    
    This reverts commit 8ff001326e12e008fe9a6634ebf401657e3a1768.

diff --git a/dash/dash.py b/dash/dash.py
index b88a1841..61b23fe6 100644
--- a/dash/dash.py
+++ b/dash/dash.py
@@ -422,6 +422,10 @@ class Dash(object):
         # e.g. for adding authentication with flask_login
         self.routes.append(name)
 
+    @property
+    def layout(self):
+        return self._layout
+
     def _layout_value(self):
         if isinstance(self._layout, _patch_collections_abc('Callable')):
             self._cached_layout = self._layout()
@@ -429,10 +433,6 @@ class Dash(object):
             self._cached_layout = self._layout
         return self._cached_layout
 
-    @property
-    def layout(self):
-        return self._layout
-
     @layout.setter
     def layout(self, value):
         if (not isinstance(value, Component) and
