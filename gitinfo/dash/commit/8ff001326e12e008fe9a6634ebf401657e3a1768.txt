commit 8ff001326e12e008fe9a6634ebf401657e3a1768
Author: Jae Chang <jchang10@gmail.com>
Date:   Tue Sep 3 09:03:27 2019 -0700

    mypy wants property getter and setter to be consecutive to each other.

diff --git a/dash/dash.py b/dash/dash.py
index 8671ffac..514249f5 100644
--- a/dash/dash.py
+++ b/dash/dash.py
@@ -422,10 +422,6 @@ class Dash(object):
         # e.g. for adding authentication with flask_login
         self.routes.append(name)
 
-    @property
-    def layout(self):
-        return self._layout
-
     def _layout_value(self):
         if isinstance(self._layout, _patch_collections_abc('Callable')):
             self._cached_layout = self._layout()
@@ -433,6 +429,10 @@ class Dash(object):
             self._cached_layout = self._layout
         return self._cached_layout
 
+    @property
+    def layout(self):
+        return self._layout
+
     @layout.setter
     def layout(self, value):
         if (not isinstance(value, Component) and
