commit 6357877443b574a60390593b265623bf17807a41
Author: byron <byron@plot.ly>
Date:   Thu Sep 26 16:08:02 2019 -0400

    make the logging level more flexible

diff --git a/dash/dash.py b/dash/dash.py
index 0d1a3ef5..330bf2d6 100644
--- a/dash/dash.py
+++ b/dash/dash.py
@@ -678,7 +678,9 @@ class Dash(object):
         })[path_in_package_dist.split('.')[-1]]
 
         loader = pkgutil.get_loader(package_name)
-        self.logger.info(
+        msg = getattr(
+            self.logger, 'info' if self.config.serve_locally else 'debug')
+        msg(
             "serving -- package: %s resource: %s => location: %s",
             package_name,
             path_in_package_dist,
