commit 806514aebcfcad82c8d21affc546bf01cac2fd20
Author: byron <byron@plot.ly>
Date:   Mon Nov 4 13:25:44 2019 -0500

    reduce the logging info

diff --git a/dash/testing/browser.py b/dash/testing/browser.py
index c939a423..84330e4f 100644
--- a/dash/testing/browser.py
+++ b/dash/testing/browser.py
@@ -117,7 +117,14 @@ class Browser(DashPageMixin):
                 until(self._wait_for_callbacks, timeout=10, poll=0.5)
         except TestingTimeoutError:
             self.driver.refresh()
-
+            logger.warning(
+                "status of invalid rqs %s",
+                list(
+                    _
+                    for _ in self.redux_state_rqs
+                    if not _.get("responseTime") or _.get("status") == "loading"
+                ),
+            )
             # for snapshot intensive case, it might happens that
             # the selenium get stuck, give it one more chance after refresh
             until(self._wait_for_callbacks, timeout=60, poll=1)
diff --git a/dash/testing/dash_page.py b/dash/testing/dash_page.py
index d22aea89..d9b27ffd 100644
--- a/dash/testing/dash_page.py
+++ b/dash/testing/dash_page.py
@@ -45,7 +45,6 @@ class DashPageMixin(object):
 
     def _wait_for_callbacks(self):
         if self.window_store:
-            logger.debug("status of state rq %s", self.redux_state_rqs)
             return self.redux_state_rqs and all(
                 (
                     _.get("responseTime")
