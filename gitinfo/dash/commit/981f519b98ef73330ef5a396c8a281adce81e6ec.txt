commit 981f519b98ef73330ef5a396c8a281adce81e6ec
Author: chriddyp <chris@plot.ly>
Date:   Tue Apr 18 12:31:33 2017 -0400

    beta - serve a generic object of non-sensitive data through `/_config`

diff --git a/dash/dash.py b/dash/dash.py
index 447b6d6c..9c2bad8f 100644
--- a/dash/dash.py
+++ b/dash/dash.py
@@ -114,6 +114,10 @@ class Dash(object):
             endpoint='{}_{}'.format(url_namespace, 'index'),
             view_func=self.index)
 
+        self.server.add_url_rule(
+            '/_config',
+            view_func=self.serve_config)
+
         # catch-all for front-end routes
         self.server.add_url_rule(
             '{}/<path:path>'.format(url_namespace),
@@ -202,6 +206,13 @@ class Dash(object):
             mimetype='application/json'
         )
 
+    def serve_config(self):
+        return flask.Response(
+            json.dumps({'fid': self.fid},
+                       cls=plotly.utils.PlotlyJSONEncoder),
+            mimetype='application/json'
+        )
+
     @_requires_auth
     def serve_routes(self):
         return flask.Response(
