commit 718a23f46460ea960f11fa351205b334256aaeee
Author: chriddyp <chris@plot.ly>
Date:   Fri Mar 24 20:21:02 2017 -0400

    :dolls: update error message to be relevant

diff --git a/src/registry.js b/src/registry.js
index 439c6e21..3a664ce0 100644
--- a/src/registry.js
+++ b/src/registry.js
@@ -13,8 +13,6 @@ export default {
                 ${namespace}`);
         }
 
-        throw new Error(`${namespace} was not found, make sure to
-            \`pip install ${namespace}\` and pass it as string in the
-            \`component_suites\` kwarg to \`dash.run_server\`.`);
+        throw new Error(`${namespace} was not found.`);
     }
 };
