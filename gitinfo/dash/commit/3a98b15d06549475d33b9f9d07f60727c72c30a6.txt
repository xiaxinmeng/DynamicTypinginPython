commit 3a98b15d06549475d33b9f9d07f60727c72c30a6
Author: Chris P <chris@plot.ly>
Date:   Sun Apr 14 17:16:38 2019 -0400

    :bulb: dev-bundles weren't being built
    
    and up until now, we were only testing with the prod bundles

diff --git a/.circleci/config.yml b/.circleci/config.yml
index b8dd634c..afa80377 100644
--- a/.circleci/config.yml
+++ b/.circleci/config.yml
@@ -53,6 +53,7 @@ jobs:
           command: |
               . venv/bin/activate
               npm run build
+              npm run build:dev
               pip install -e . --quiet
               pip list | grep dash
 
