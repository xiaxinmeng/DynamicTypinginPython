commit cbfa3f5b0907404c8739efe48c5b99784eb6cc5c
Author: t4rk1n <t4rk@outlook.com>
Date:   Thu Dec 27 19:10:02 2018 -0500

    :ambulance: Skip dynamic external_url

diff --git a/dash/dash.py b/dash/dash.py
index 5133b32a..b13ee0f0 100644
--- a/dash/dash.py
+++ b/dash/dash.py
@@ -391,11 +391,12 @@ class Dash(object):
                             namespace=resource['namespace']
                         ))
             elif 'external_url' in resource:
-                if isinstance(resource['external_url'], str):
-                    srcs.append(resource['external_url'])
-                else:
-                    for url in resource['external_url']:
-                        srcs.append(url)
+                if not is_dynamic_resource:
+                    if isinstance(resource['external_url'], str):
+                        srcs.append(resource['external_url'])
+                    else:
+                        for url in resource['external_url']:
+                            srcs.append(url)
             elif 'absolute_path' in resource:
                 raise Exception(
                     'Serving files from absolute_path isn\'t supported yet'
