commit ce878c0533f80f9382a38cb7387eb4230c3e8aef
Author: Philippe Duval <philippe@plot.ly>
Date:   Wed Dec 19 14:53:27 2018 -0500

    reloaderReducer keep config.

diff --git a/src/reducers/reducer.js b/src/reducers/reducer.js
index f45d64a4..eba69b38 100644
--- a/src/reducers/reducer.js
+++ b/src/reducers/reducer.js
@@ -94,9 +94,9 @@ function recordHistory(reducer) {
 function reloaderReducer(reducer) {
     return function(state, action) {
         if (action.type === 'RELOAD') {
-            const {history} = state;
+            const {history, config} = state;
             // eslint-disable-next-line no-param-reassign
-            state = {history};
+            state = {history, config};
         }
         return reducer(state, action);
     };
