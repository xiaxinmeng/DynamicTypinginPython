commit 3cd6a78ee21620c77ccdbdf2724c995872383666
Author: chriddyp <chris@plot.ly>
Date:   Mon Dec 11 16:06:57 2017 -0500

    fix #32

diff --git a/dash/dash.py b/dash/dash.py
index 3e663d45..07477f28 100644
--- a/dash/dash.py
+++ b/dash/dash.py
@@ -35,13 +35,9 @@ class Dash(object):
                 See https://github.com/plotly/dash/issues/141 for details.
                 ''', DeprecationWarning)
 
+        name = name or 'dash'
         # allow users to supply their own flask server
-        if server is not None:
-            self.server = server
-        else:
-            if name is None:
-                name = 'dash'
-            self.server = Flask(name, static_folder=static_folder)
+        self.server = server or Flask(name, static_folder=static_folder)
 
         self.url_base_pathname = url_base_pathname
         self.config = _AttributeDict({
