commit 84fb0351e845800ddbff7e355363cc8a2b197aaf
Author: alexcjohnson <alex@plot.ly>
Date:   Thu Feb 28 21:38:44 2019 -0500

    pull self.renderer out of _add_url so we only do it once!

diff --git a/dash/dash.py b/dash/dash.py
index 3cea5092..219a4ec4 100644
--- a/dash/dash.py
+++ b/dash/dash.py
@@ -170,6 +170,9 @@ class Dash(object):
         self._meta_tags = meta_tags or []
         self._favicon = None
 
+        # default renderer string
+        self.renderer = 'var renderer = new DashRenderer();'
+
         if compress:
             # gzip
             Compress(self.server)
@@ -264,9 +267,6 @@ class Dash(object):
         # e.g. for adding authentication with flask_login
         self.routes.append(name)
 
-        # default renderer string
-        self.renderer = 'var renderer = new DashRenderer();'
-
     @property
     def layout(self):
         return self._layout
