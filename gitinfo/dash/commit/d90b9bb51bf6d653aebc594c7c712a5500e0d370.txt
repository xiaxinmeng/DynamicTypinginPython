commit d90b9bb51bf6d653aebc594c7c712a5500e0d370
Author: chriddyp <chris@plot.ly>
Date:   Tue Apr 25 00:07:11 2017 -0400

    set env variable

diff --git a/appveyor.yml b/appveyor.yml
index a4988a7b..6b52882b 100644
--- a/appveyor.yml
+++ b/appveyor.yml
@@ -18,6 +18,8 @@ environment:
       PYTHON_VERSION: "2.7.8"
       PYTHON_ARCH: "64"
 
+  NODE_ENV: production
+
 install:
   # Download setup scripts and unzip
   - ps: "wget https://github.com/cloudify-cosmo/appveyor-utils/archive/master.zip -OutFile ./master.zip"
@@ -40,7 +42,7 @@ install:
   - ps: Install-Product node $env:nodejs_version
   # install modules
   - npm install
-  - npm run build-prod
+  - ./node_modules/.bin/webpack --config=config/webpack.config.prod.js
 
 build: off
 
