commit f38ad5b13c79e592dbdeb2a48e3a76694ca5ff15
Author: Per Nilsson <per.nilsson@formidablelabs.com>
Date:   Wed May 25 10:12:05 2016 -0700

    :smile_cat: Add npm scripts for DX

diff --git a/renderer/src/package.json b/renderer/src/package.json
index 420dde67..f68c243d 100644
--- a/renderer/src/package.json
+++ b/renderer/src/package.json
@@ -4,7 +4,10 @@
   "description": "render dash components in react",
   "main": "index.js",
   "scripts": {
-    "test": "echo \"Error: no test specified\" && exit 1",
+    "lint": "eslint ./*.js ./**/*.js --fix",
+    "server": "serv .",
+    "start": "npm run watch & npm run server",
+    "test": "npm run lint",
     "watch": "node_modules/.bin/watchify index.js -t babelify -p livereactload -o ../build/bundle.js -v"
   },
   "author": "chriddyp",
@@ -35,6 +38,7 @@
     "eslint-plugin-react": "^5.1.1",
     "livereactload": "^2.2.2",
     "react-proxy": "^1.1.8",
+    "serv": "^0.2.4",
     "watchify": "^3.7.0"
   }
 }
