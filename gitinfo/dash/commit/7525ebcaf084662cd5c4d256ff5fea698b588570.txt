commit 7525ebcaf084662cd5c4d256ff5fea698b588570
Author: byron <byron@plot.ly>
Date:   Mon Aug 5 16:14:51 2019 -0400

    :wrench: add artifacts step

diff --git a/.circleci/config.yml b/.circleci/config.yml
index 14db684b..efae0226 100644
--- a/.circleci/config.yml
+++ b/.circleci/config.yml
@@ -6,6 +6,22 @@ jobs:
     steps:
       - run: percy finalize --all
 
+  "artifacts":
+    docker:
+      - image: circleci/python:3.7-stretch-node-browsers
+        environment:
+          PYVERSION: python37
+    steps:
+      - checkout
+      - run: echo $PYVERSION > ver.txt
+      - restore_cache:
+          key: dep-{{ checksum "ver.txt" }}-{{ checksum "requires-ci.txt" }}-{{ checksum "requires-install.txt" }}-{{ checksum "requires-testing.txt" }}
+      - attach_workspace:
+          at: ~/dash
+      - store_artifacts:
+          path: /tmp/packages
+          destination: ~/dash/packages
+
   "pip-install-37": &pip
     working_directory: ~/dash
     docker:
@@ -231,6 +247,13 @@ workflows:
       - "percy-finalize":
           requires:
             - test-37
+      - "artifacts":
+          requires:
+            - percy-finalize
+          filters:
+            tags:
+              only: /v*/
+
   python3.6:
     jobs:
       - pip-install-36
