commit af0ad494348d0f2d773232b5913d5c86787e39f5
Author: Thomas Kriechbaumer <thomas@kriechbaumer.name>
Date:   Fri May 19 11:58:19 2017 +0800

    disable double requires-io-master CI builds

diff --git a/.appveyor.yml b/.appveyor.yml
index 53e33614..549a0607 100644
--- a/.appveyor.yml
+++ b/.appveyor.yml
@@ -1,6 +1,10 @@
 version: '{build}'
 build: off  # Not a C# project
 
+branches:
+  except:
+    - requires-io-master
+
 environment:
   CI_DEPS: codecov>=2.0.5
   CI_COMMANDS: codecov
diff --git a/.travis.yml b/.travis.yml
index 26894eee..0ff0fb59 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -1,10 +1,15 @@
 sudo: false
 language: python
 
+branches:
+  except:
+    - requires-io-master
+
 env:
   global:
     - CI_DEPS=codecov>=2.0.5
     - CI_COMMANDS=codecov
+
 git:
   depth: 10000
 
@@ -104,4 +109,3 @@ cache:
   directories:
     - $HOME/.pyenv
     - $HOME/.cache/pip
-    # - $HOME/build/mitmproxy/mitmproxy/.tox
