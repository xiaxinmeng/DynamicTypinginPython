commit b5bbeb0ff3d13abec23d3884cb1449bfdb1b2047
Author: Aldo Cortesi <aldo@nullcube.com>
Date:   Sun Jul 17 11:11:22 2016 +1200

    dumper: correctly detect request replay
    
    Fixes #1361

diff --git a/mitmproxy/builtins/dumper.py b/mitmproxy/builtins/dumper.py
index 8ee1e93d..239630fb 100644
--- a/mitmproxy/builtins/dumper.py
+++ b/mitmproxy/builtins/dumper.py
@@ -118,8 +118,10 @@ class Dumper():
                 ),
                 bold=True
             )
-        else:
+        elif flow.request.is_replay:
             client = click.style("[replay]", fg="yellow", bold=True)
+        else:
+            client = ""
 
         method = flow.request.method
         method_color = dict(
@@ -183,7 +185,7 @@ class Dumper():
             size = human.pretty_size(len(flow.response.raw_content))
         size = click.style(size, bold=True)
 
-        arrows = click.style("<<", bold=True)
+        arrows = click.style("    <<", bold=True)
 
         line = "{replay} {arrows} {code} {reason} {size}".format(
             replay=replay,
