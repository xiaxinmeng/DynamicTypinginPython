commit 42930407aeb209f8790411a51fe49681159baf18
Author: Thomas Kriechbaumer <thomas@kriechbaumer.name>
Date:   Sat Jun 4 11:25:47 2016 +0200

    improve travis

diff --git a/.travis.yml b/.travis.yml
index 5ec8b3bd..64fe09b0 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -41,12 +41,12 @@ install:
     fi
   - pip install -U virtualenv
   - ./dev.sh
-  - source ./venv/bin/activate
+  - source venv/bin/activate
 
 before_script:
   - "openssl version -a"
   - "python -c \"from OpenSSL import SSL; print(SSL.SSLeay_version(SSL.SSLEAY_VERSION))\""
-  - "flake8 --count mitmproxy netlib pathod examples test"
+  - "flake8 --jobs 4 --count mitmproxy netlib pathod examples test"
 
 script:
   - "py.test --timeout 60 --cov netlib --cov mitmproxy --cov pathod ./test/$SCOPE"
@@ -71,6 +71,7 @@ notifications:
 
 cache:
   directories:
+    - $HOME/build/mitmproxy/mitmproxy/venv
     - $HOME/.cache/pip
     - $HOME/.pyenv
     - $HOME/Library/Caches/pip
