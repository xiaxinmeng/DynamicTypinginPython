commit 43671e723ff9cc783b21d447b448d661ce28734a
Author: Maximilian Hils <git@maximilianhils.com>
Date:   Sun Mar 20 19:39:02 2016 +0100

    py.test: disable coverage collection by default

diff --git a/.appveyor.yml b/.appveyor.yml
index 805be7e7..75cf6e0a 100644
--- a/.appveyor.yml
+++ b/.appveyor.yml
@@ -17,7 +17,7 @@ install:
   - "dev.bat"
   - "python -c \"from OpenSSL import SSL; print(SSL.SSLeay_version(SSL.SSLEAY_VERSION))\""
 test_script:
-  - "py.test"
+  - "py.test --cov netlib --cov mitmproxy --cov pathod"
 cache:
   - C:\Users\appveyor\AppData\Local\pip\cache
 deploy_script:
diff --git a/.travis.yml b/.travis.yml
index d0e5e62d..113fda09 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -46,7 +46,7 @@ before_script:
   - "python -c \"from OpenSSL import SSL; print(SSL.SSLeay_version(SSL.SSLEAY_VERSION))\""
 
 script:
-  - "py.test ./test/$SCOPE"
+  - "py.test --cov netlib --cov mitmproxy --cov pathod ./test/$SCOPE"
 
 after_success:
   - coveralls
diff --git a/setup.cfg b/setup.cfg
index ccd62228..96b23026 100644
--- a/setup.cfg
+++ b/setup.cfg
@@ -9,7 +9,7 @@ ignore = E251
 
 [pytest]
 testpaths = test
-addopts = --timeout 30 -s --cov netlib --cov mitmproxy --cov pathod
+addopts = --timeout 30 -s
 
 [coverage:run]
 branch = True
