commit 7746deedc226ee82878786060f850d9a2e263fad
Author: Jason <jason.daurus@gmail.com>
Date:   Fri Jun 24 00:13:05 2016 +0800

    [web] code format

diff --git a/web/src/js/ducks/views/main.js b/web/src/js/ducks/views/main.js
index 60a84fa4..5e063f4b 100755
--- a/web/src/js/ducks/views/main.js
+++ b/web/src/js/ducks/views/main.js
@@ -27,7 +27,6 @@ const sortKeyFuns = {
     },
 }
 
-
 const defaultState = {
     highlight: null,
     selected: [],
@@ -108,7 +107,12 @@ export default function reduce(state = defaultState, action) {
         case viewsActions.REMOVE:
             return {
                 ...state,
-                view: reduceView(state.view, viewActions.remove(action.id)),
+                view: reduceView(
+                    state.view,
+                    viewActions.remove(
+                        action.id
+                    )
+                ),
             }
 
         case viewsActions.RECEIVE:
