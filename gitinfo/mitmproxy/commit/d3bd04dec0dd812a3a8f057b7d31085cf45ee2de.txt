commit d3bd04dec0dd812a3a8f057b7d31085cf45ee2de
Author: Thomas Kriechbaumer <thomas@kriechbaumer.name>
Date:   Sun Nov 20 23:21:45 2016 +0100

    disable TCP support for now

diff --git a/test/mitmproxy/tservers.py b/test/mitmproxy/tservers.py
index 1020f23c..060275d0 100644
--- a/test/mitmproxy/tservers.py
+++ b/test/mitmproxy/tservers.py
@@ -30,9 +30,10 @@ class TestState:
         if f not in self.flows:
             self.flows.append(f)
 
-    def tcp_start(self, f):
-        if f not in self.flows:
-            self.flows.append(f)
+    # TODO: add TCP support?
+    # def tcp_start(self, f):
+    #     if f not in self.flows:
+    #         self.flows.append(f)
 
     # FIXME: compat with old state - remove in favor of len(state.flows)
     def flow_count(self):
