commit ed64b0e79699681bd5db3ff2823c47a424fbc3e1
Author: Aldo Cortesi <aldo@nullcube.com>
Date:   Sun Jul 22 12:35:16 2012 +1200

    Fix http_protocol parsing crash discovered with pathoc fuzzing.

diff --git a/netlib/http.py b/netlib/http.py
index 88e66ce4..9d6db003 100644
--- a/netlib/http.py
+++ b/netlib/http.py
@@ -141,10 +141,10 @@ def parse_http_protocol(s):
     """
     if not s.startswith("HTTP/"):
         return None
-    _, version = s.split('/')
+    _, version = s.split('/', 1)
     if "." not in version:
         return None
-    major, minor = version.split('.')
+    major, minor = version.split('.', 1)
     try:
         major = int(major)
         minor = int(minor)
