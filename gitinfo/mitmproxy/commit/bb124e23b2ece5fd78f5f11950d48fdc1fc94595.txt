commit bb124e23b2ece5fd78f5f11950d48fdc1fc94595
Author: Aldo Cortesi <aldo@nullcube.com>
Date:   Fri Jul 27 10:08:10 2012 +1200

    Prevent mitmproxy from treating logged info as urwid markup.

diff --git a/libmproxy/console/__init__.py b/libmproxy/console/__init__.py
index c047f9f9..482c762d 100644
--- a/libmproxy/console/__init__.py
+++ b/libmproxy/console/__init__.py
@@ -960,9 +960,9 @@ class ConsoleMaster(flow.FlowMaster):
 
     def add_event(self, e, level="info"):
         if level == "info":
-            e = urwid.Text(e)
+            e = urwid.Text(str(e))
         elif level == "error":
-            e = urwid.Text(("error", e))
+            e = urwid.Text(("error", str(e)))
 
         self.eventlist.append(e)
         if len(self.eventlist) > EVENTLOG_SIZE:
