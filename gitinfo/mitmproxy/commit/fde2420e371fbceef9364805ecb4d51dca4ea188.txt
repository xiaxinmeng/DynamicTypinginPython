commit fde2420e371fbceef9364805ecb4d51dca4ea188
Author: Maximilian Hils <git@maximilianhils.com>
Date:   Sat Sep 19 11:18:57 2015 +0200

    fix #778

diff --git a/README.rst b/README.rst
index 17832e6c..ec61e086 100644
--- a/README.rst
+++ b/README.rst
@@ -54,7 +54,7 @@ Then do the following:
     git clone https://github.com/mitmproxy/netlib.git
     git clone https://github.com/mitmproxy/pathod.git
     cd mitmproxy
-    source ./dev
+    ./dev
 
 
 The *dev* script will create a virtualenv environment in a directory called
diff --git a/dev b/dev
index 248a3d89..fe1a619b 100755
--- a/dev
+++ b/dev
@@ -2,11 +2,11 @@
 set -e
 VENV=../venv.mitmproxy
 
-python -m virtualenv $VENV --always-copy || { echo 'virtualenv is not installed. Exiting.' ; exit 1; }
+python -m virtualenv $VENV --always-copy
 . $VENV/bin/activate
 pip install --src .. -r requirements.txt
 
 echo ""
 echo "* Created virtualenv environment in $VENV."
 echo "* Installed all dependencies into the virtualenv."
-echo "* Activated virtualenv environment."
+echo "* You can now activate the virtualenv: \`. $VENV/bin/activate\`"
