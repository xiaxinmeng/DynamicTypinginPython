commit c1062db9fd19a3f5ff8468f8db8a6f29d65cc392
Author: Aldo Cortesi <aldo@nullcube.com>
Date:   Wed Nov 5 09:58:07 2014 +1300

    Add a pip requirement to pathod to work around a pip/requests screwup

diff --git a/setup.py b/setup.py
index b756dbfe..302b2db6 100644
--- a/setup.py
+++ b/setup.py
@@ -45,6 +45,9 @@ setup(
 
     install_requires=[
         'netlib>=%s' % version.MINORVERSION,
+        # It's INSANE that we have to do this, but...
+        # FIXME: Requirement to be removed at next release
+        "pip>=1.5.6",
         "requests>=2.4.1",
         "Flask>=0.10.1"
     ],
