commit 2e492b8e94362dbd9380d1d3eb38b5abaa8f137d
Author: Aldo Cortesi <aldo@nullcube.com>
Date:   Fri Feb 25 21:33:31 2011 +1300

    Minor command-line argument fixes.

diff --git a/mitmdump b/mitmdump
index 3560b191..f92444b8 100755
--- a/mitmdump
+++ b/mitmdump
@@ -32,7 +32,7 @@ if __name__ == '__main__':
     parser.add_option(
         "-p", action="store",
         type = "int", dest="port", default=8080,
-        help = "Port."
+        help = "Proxy service port."
     )
     parser.add_option("-c", 
                       action="store_true", dest="stickycookie_all", default=None,
diff --git a/mitmproxy b/mitmproxy
index 85e8679e..fed5dbac 100755
--- a/mitmproxy
+++ b/mitmproxy
@@ -36,7 +36,7 @@ if __name__ == '__main__':
     parser.add_option(
         "-p", "--port", action="store",
         type = "int", dest="port", default=8080,
-        help = "Port."
+        help = "Proxy service port."
     )
 
     group = OptionGroup(
@@ -66,17 +66,6 @@ if __name__ == '__main__':
     )
     parser.add_option_group(group)
 
-    group = OptionGroup(
-                        parser,
-                        "Recorder",
-                        "Options controlling recorder behavior"
-                    )
-    group.add_option(
-        "-C", "--cookies", action="append",
-        type = "str", dest="cookies", default=[],
-        help = "Persistent client cookies already set or generated in client"
-    )
-    parser.add_option_group(group)
 
     options, args = parser.parse_args()
 
