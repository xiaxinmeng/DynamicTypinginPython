commit 4a6db7671f2a472e45ab973653b442bf8a83d117
Author: Maximilian Hils <git@maximilianhils.com>
Date:   Sat Feb 6 01:22:27 2016 +0100

    update dependencies

diff --git a/rtool.py b/rtool.py
index 78cf7bd9..3e91ce24 100644
--- a/rtool.py
+++ b/rtool.py
@@ -210,7 +210,7 @@ def sdist():
 
 @cli.command("bdist")
 @click.option("--use-existing-sdist/--no-use-existing-sdist", default=False)
-@click.argument("pyinstaller_version", envvar="PYINSTALLER_VERSION", default="PyInstaller~=3.0.0")
+@click.argument("pyinstaller_version", envvar="PYINSTALLER_VERSION", default="PyInstaller~=3.1.1")
 @click.pass_context
 def bdist(ctx, use_existing_sdist, pyinstaller_version):
     """
diff --git a/setup.py b/setup.py
index e99c159c..359b7646 100644
--- a/setup.py
+++ b/setup.py
@@ -5,10 +5,10 @@ setup(
     version='1.0',
     py_modules=['rtool'],
     install_requires=[
-        'click>=6.2',
-        'twine>=1.6.4',
-        'virtualenv>=13.1.2',
-        'wheel>=0.26.0',
+        "click>=6.2, <7.0",
+        'twine>=1.6.5, <1.7',
+        'virtualenv>=14.0.5, <14.1',
+        'wheel>=0.26.0, <0.27',
     ],
     entry_points={
         'console_scripts': [
