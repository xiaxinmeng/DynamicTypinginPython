commit c903efcf5b34fa775d3e64623e54bd8e75b740cb
Author: Thomas Kriechbaumer <thomas@kriechbaumer.name>
Date:   Tue Aug 18 21:17:11 2015 +0200

    temporarily disable pypy with new openssl on travis

diff --git a/.travis.yml b/.travis.yml
index 9c781467..3b49224e 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -18,16 +18,16 @@ matrix:
             - debian-sid
           packages:
             - libssl-dev
-    - python: pypy
-      env: OPENSSL=1.0.2
-      addons:
-        apt:
-          sources:
-            # Debian sid currently holds OpenSSL 1.0.2
-            # change this with future releases!
-            - debian-sid
-          packages:
-            - libssl-dev
+    # - python: pypy
+    #   env: OPENSSL=1.0.2
+    #   addons:
+    #     apt:
+    #       sources:
+    #         # Debian sid currently holds OpenSSL 1.0.2
+    #         # change this with future releases!
+    #         - debian-sid
+    #       packages:
+    #         - libssl-dev
 
 install:
   - "pip install --src . -r requirements.txt"
