commit 95f0b4b506230ee43ea7a6d736ab752400747bbb
Author: Marcelo Glezer <mg@tekii.com.ar>
Date:   Wed Jan 14 16:44:59 2015 -0300

    trying to fix travis

diff --git a/.travis.yml b/.travis.yml
index 2dc898c2..e66501f1 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -3,6 +3,8 @@ sudo: false
 python:
   - "2.7"
   - pypy
+before_install:
+  - "sudo apt-get install -y xclip"
 # command to install dependencies, e.g. pip install -r requirements.txt --use-mirrors
 install:
   - "pip install --upgrade --src . -r requirements.txt"
