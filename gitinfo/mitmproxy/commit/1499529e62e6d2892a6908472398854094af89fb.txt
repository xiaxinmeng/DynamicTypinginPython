commit 1499529e62e6d2892a6908472398854094af89fb
Author: Aldo Cortesi <aldo@nullcube.com>
Date:   Fri Jan 18 17:07:35 2013 +1300

    Fix client cert typo.

diff --git a/netlib/tcp.py b/netlib/tcp.py
index 9c5cfa64..afb7e059 100644
--- a/netlib/tcp.py
+++ b/netlib/tcp.py
@@ -177,7 +177,7 @@ class TCPClient:
         if not options is None:
             ctx.set_options(options)
         if clientcert:
-            context.use_certificate_file(self.clientcert)
+            context.use_certificate_file(clientcert)
         self.connection = SSL.Connection(context, self.connection)
         self.ssl_established = True
         if sni:
