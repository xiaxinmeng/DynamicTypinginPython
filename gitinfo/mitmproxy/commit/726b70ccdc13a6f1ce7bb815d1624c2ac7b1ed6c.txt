commit 726b70ccdc13a6f1ce7bb815d1624c2ac7b1ed6c
Author: Maximilian Hils <git@maximilianhils.com>
Date:   Thu Oct 22 02:38:00 2015 +0200

    fix #800

diff --git a/libmproxy/console/statusbar.py b/libmproxy/console/statusbar.py
index ea2dbfa8..fd084f8c 100644
--- a/libmproxy/console/statusbar.py
+++ b/libmproxy/console/statusbar.py
@@ -4,7 +4,6 @@ import urwid
 
 import netlib.utils
 from . import pathedit, signals, common
-from .. import utils
 
 
 class ActionBar(urwid.WidgetWrap):
@@ -25,6 +24,7 @@ class ActionBar(urwid.WidgetWrap):
     def sig_message(self, sender, message, expire=None):
         w = urwid.Text(message)
         self._w = w
+        self.prompting = False
         if expire:
             def cb(*args):
                 if w == self._w:
@@ -88,6 +88,7 @@ class ActionBar(urwid.WidgetWrap):
 
     def clear(self):
         self._w = urwid.Text("")
+        self.prompting = False
 
     def prompt_done(self):
         self.prompting = False
