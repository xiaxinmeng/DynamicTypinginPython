commit ce02874e2a7c5dacd12e58652e9665857bd46c62
Author: Sam Cleveland <sam@zombisoft.com>
Date:   Sat Nov 14 13:42:43 2015 -0600

    Fixing test_wsgi to remove py.test warnings
    
    Renamed TestApp class to ExampleApp to prevent py.test from trying to
    collect it as a test.

diff --git a/test/test_wsgi.py b/test/test_wsgi.py
index ec6e8c63..8c782b27 100644
--- a/test/test_wsgi.py
+++ b/test/test_wsgi.py
@@ -10,8 +10,10 @@ def tflow():
     return wsgi.Flow(("127.0.0.1", 8888), req)
 
 
-class TestApp:
-
+class ExampleApp:
+    
+    def __init__(self):
+        self.called = False
 
     def __call__(self, environ, start_response):
         self.called = True
@@ -33,7 +35,7 @@ class TestWSGI:
         assert r["QUERY_STRING"] == "bar=voing"
 
     def test_serve(self):
-        ta = TestApp()
+        ta = ExampleApp()
         w = wsgi.WSGIAdaptor(ta, "foo", 80, "version")
         f = tflow()
         f.request.host = "foo"
