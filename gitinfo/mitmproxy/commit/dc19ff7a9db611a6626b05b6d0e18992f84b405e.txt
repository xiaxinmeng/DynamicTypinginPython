commit dc19ff7a9db611a6626b05b6d0e18992f84b405e
Author: Aldo Cortesi <aldo@nullcube.com>
Date:   Sun Oct 16 11:28:42 2016 +1300

    mitmdump: show script errors on startup
    
    Add the terminal logger before any other addons, so we see script errors on
    startup.

diff --git a/mitmproxy/dump.py b/mitmproxy/dump.py
index 92dce37b..70035fb7 100644
--- a/mitmproxy/dump.py
+++ b/mitmproxy/dump.py
@@ -37,9 +37,9 @@ class DumpMaster(flow.FlowMaster):
     def __init__(self, server, options):
         flow.FlowMaster.__init__(self, options, server, flow.DummyState())
         self.has_errored = False
-        self.addons.add(*builtins.default_addons())
         self.addons.add(dumper.Dumper())
         self.addons.add(termlog.TermLog())
+        self.addons.add(*builtins.default_addons())
         # This line is just for type hinting
         self.options = self.options  # type: Options
         self.set_stream_large_bodies(options.stream_large_bodies)
