commit 4cfa91a903f581856082e0ac2c009726082f23c8
Author: Maximilian Hils <git@maximilianhils.com>
Date:   Thu Dec 25 02:08:58 2014 +0100

    web: show mitmproxy version

diff --git a/libmproxy/web/app.py b/libmproxy/web/app.py
index d3c44bf7..b5ec41c9 100644
--- a/libmproxy/web/app.py
+++ b/libmproxy/web/app.py
@@ -3,6 +3,7 @@ import tornado.web
 import tornado.websocket
 import logging
 import json
+from .. import version
 
 
 class APIError(tornado.web.HTTPError):
@@ -112,6 +113,7 @@ class Settings(RequestHandler):
     def get(self):
         self.write(dict(
             data=dict(
+                version=version.VERSION,
                 mode=str(self.master.server.config.mode),
                 intercept=self.state.intercept_txt
             )
