commit f9c000ea6eea93e5fee6ea994c55b9d6761fe753
Author: Maximilian Hils <git@maximilianhils.com>
Date:   Sun Nov 29 15:23:44 2015 +0100

    travis: fix osx binary build condition

diff --git a/.travis.yml b/.travis.yml
index aa5847ef..21be2734 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -56,7 +56,7 @@ script:
 after_success:
   - coveralls
   - |
-    if [[ $TRAVIS_OS_NAME == "osx" && $TRAVIS_BRANCH == "master" && -z $TRAVIS_PULL_REQUEST ]]
+    if [[ $TRAVIS_OS_NAME == "osx" && $TRAVIS_BRANCH == "master" && $TRAVIS_PULL_REQUEST == "false" ]]
     then
         brew install curl --with-libssh2
         pip install virtualenv
