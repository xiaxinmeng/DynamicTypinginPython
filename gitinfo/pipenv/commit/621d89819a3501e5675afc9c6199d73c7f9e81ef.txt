commit 621d89819a3501e5675afc9c6199d73c7f9e81ef
Author: Kenneth Reitz <me@kennethreitz.org>
Date:   Tue Sep 26 13:09:46 2017 -0400

    PIPENV_ENV_LOCATION
    
    Signed-off-by: Kenneth Reitz <me@kennethreitz.org>

diff --git a/pipenv/cli.py b/pipenv/cli.py
index ae78adb2..b7718f66 100644
--- a/pipenv/cli.py
+++ b/pipenv/cli.py
@@ -48,7 +48,8 @@ from .environments import (
     PIPENV_SKIP_VALIDATION, PIPENV_HIDE_EMOJIS, PIPENV_INSTALL_TIMEOUT,
     PYENV_INSTALLED, PIPENV_YES, PIPENV_DONT_LOAD_ENV,
     PIPENV_DEFAULT_PYTHON_VERSION, PIPENV_MAX_SUBPROCESS,
-    PIPENV_DONT_USE_PYENV, SESSION_IS_INTERACTIVE, PIPENV_USE_SYSTEM
+    PIPENV_DONT_USE_PYENV, SESSION_IS_INTERACTIVE, PIPENV_USE_SYSTEM,
+    PIPENV_ENV_LOCATION
 )
 
 # Backport required for earlier versions of Python.
@@ -124,7 +125,7 @@ project = Project()
 
 def load_dot_env():
     if not PIPENV_DONT_LOAD_ENV:
-        denv = dotenv.find_dotenv(os.sep.join([project.project_directory, '.env']))
+        denv = dotenv.find_dotenv(PIPENV_ENV_LOCATION or os.sep.join([project.project_directory, '.env']))
         if os.path.isfile(denv):
             click.echo(crayons.normal('Loading .env environment variables…', bold=True), err=True)
         dotenv.load_dotenv(denv, override=True)
diff --git a/pipenv/environments.py b/pipenv/environments.py
index 7f870422..c655b2ca 100644
--- a/pipenv/environments.py
+++ b/pipenv/environments.py
@@ -52,6 +52,9 @@ PIPENV_USE_HASHES = True
 # Tells Pipenv to skip case-checking (slow internet connections).
 PIPENV_SKIP_VALIDATION = True
 
+# Tells Pipenv where to load .env from.
+PIPENV_ENV_LOCATION = os.environ.get('PIPENV_ENV_LOCATION')
+
 # Use shell compatibility mode when using venv in project mode.
 if PIPENV_VENV_IN_PROJECT:
     PIPENV_SHELL_COMPAT = True
