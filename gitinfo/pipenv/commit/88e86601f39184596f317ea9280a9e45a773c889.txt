commit 88e86601f39184596f317ea9280a9e45a773c889
Author: Kenneth Reitz <me@kennethreitz.org>
Date:   Sun Mar 4 17:00:35 2018 -0500

    try this
    
    Signed-off-by: Kenneth Reitz <me@kennethreitz.org>

diff --git a/run-tests.sh b/run-tests.sh
index 0983a066..d9ad656e 100644
--- a/run-tests.sh
+++ b/run-tests.sh
@@ -4,15 +4,16 @@ PYPI_VENDOR_DIR="$(pwd)/tests/pypi/"
 export PYPI_VENDOR_DIR
 
 
-if [[ ! "$TEST_SUITE" ]]; then
+if [[ ! -z "$TEST_SUITE" ]]; then
 	TEST_SUITE = ""
 fi
 
-RAM_DISK="/media/ramdisk"
-export RAM_DISK
-
-if [[ "$CI" ]]; then
+if [[ ! -z "$CI" ]]; then
 	echo "Creating RAM disk…"
+
+	RAM_DISK="/media/ramdisk"
+	export RAM_DISK
+
 	sudo mkdir -p "$RAM_DISK"
 	sudo mount -t tmpfs -o size=2048M tmpfs "$RAM_DISK"
 fi
