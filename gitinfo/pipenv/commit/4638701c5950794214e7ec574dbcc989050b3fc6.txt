commit 4638701c5950794214e7ec574dbcc989050b3fc6
Author: Kenneth Reitz <me@kennethreitz.org>
Date:   Thu Sep 21 12:31:42 2017 -0400

    fixes issue with capitalization in URLs
    
    Fixes #629
    
    Signed-off-by: Kenneth Reitz <me@kennethreitz.org>

diff --git a/pipenv/project.py b/pipenv/project.py
index e5823115..8e204a7a 100644
--- a/pipenv/project.py
+++ b/pipenv/project.py
@@ -226,12 +226,13 @@ class Project(object):
         pfile = pipfile.load(self.pipfile_location)
         lockfile = json.loads(pfile.lock())
 
-        for section in ('default', 'develop'):
-            lock_section = lockfile.get(section, {})
+        # We may need this later...
+        # for section in ('default', 'develop'):
+        #     lock_section = lockfile.get(section, {})
 
-            for key in list(lock_section.keys()):
-                norm_key = pep423_name(key)
-                lockfile[section][norm_key] = lock_section.pop(key)
+        #     for key in list(lock_section.keys()):
+        #         norm_key = pep423_name(key)
+        #         lockfile[section][norm_key] = lock_section.pop(key)
 
         return lockfile
 
