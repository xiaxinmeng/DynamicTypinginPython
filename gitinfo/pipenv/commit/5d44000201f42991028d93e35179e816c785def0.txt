commit 5d44000201f42991028d93e35179e816c785def0
Author: Erin O'Connell <erinocon5@gmail.com>
Date:   Fri Sep 1 23:12:52 2017 -0600

    added flake8 back in and added jobs for style and unit tests

diff --git a/.travis.yml b/.travis.yml
index 9b1ca12b..688b0d18 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -12,8 +12,12 @@ python:
 install:
   - "pip install -e . --upgrade"
   - "pipenv install --dev --skip-lock"
+
+
 # command to run tests
-script:
-  # flake8 has dropped support for Python 2.6.
-  # - if [[ "$TRAVIS_PYTHON_VERSION" != "2.6" ]]; then pipenv run flake8 --ignore=F821; fi
-  - pipenv run pytest tests
+jobs:
+  include:
+    - stage: Style Tests ✨
+      script: pipenv run flake8
+    - stage: Main Test Suite ✨✨
+      script: pipenv run pytest tests
diff --git a/setup.cfg b/setup.cfg
index 2979c981..f502548f 100644
--- a/setup.cfg
+++ b/setup.cfg
@@ -1,5 +1,5 @@
 [flake8]
-exclude = .git,__pycache__,docs/,pipenv/vendor/,get-pipenv.py
+exclude = .git,__pycache__,docs/,pipenv/vendor/,get-pipenv.py,setup.py
 ignore =
     # The default ignore list:
     E121,E123,E126,E226,E24,E704,
