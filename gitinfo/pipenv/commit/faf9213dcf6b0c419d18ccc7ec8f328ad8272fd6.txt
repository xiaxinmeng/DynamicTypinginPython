commit faf9213dcf6b0c419d18ccc7ec8f328ad8272fd6
Author: Kenneth Reitz <me@kennethreitz.org>
Date:   Thu Sep 14 13:14:38 2017 -0400

    debug windows
    
    Signed-off-by: Kenneth Reitz <me@kennethreitz.org>

diff --git a/pipenv/cli.py b/pipenv/cli.py
index bcd7ae60..b1168bbe 100644
--- a/pipenv/cli.py
+++ b/pipenv/cli.py
@@ -289,12 +289,6 @@ def find_a_system_python(python):
             if os.name == 'nt':
                 possibility = '{0}.exe'.format(possibility)
 
-            # Debug for windows
-            print(possibility)
-            print(system_which(possibility))
-            print(python_version(system_which(possibility)))
-            print()
-
             version = python_version(system_which(possibility))
             if version:
                 if python in version:
diff --git a/pipenv/utils.py b/pipenv/utils.py
index f4dc96a9..f5b6588f 100644
--- a/pipenv/utils.py
+++ b/pipenv/utils.py
@@ -25,9 +25,12 @@ requests = requests.session()
 
 
 def python_version(path_to_python):
+    print(path_to_python)
     try:
         TEMPLATE = 'Python {}.{}.{}'
         c = delegator.run('{0} --version'.format(path_to_python), block=False)
+        print('{0} --version'.format(path_to_python))
+        print(c)
         c.return_code == 0
     except Exception:
         return None
