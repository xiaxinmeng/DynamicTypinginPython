commit c6a3284d307b973059071071b6aba637688fdb16
Author: Dan Ryan <dan@danryan.co>
Date:   Sun Jul 15 18:07:31 2018 -0400

    Updated buildkite to set home properly
    
    Signed-off-by: Dan Ryan <dan@danryan.co>

diff --git a/run-tests.sh b/run-tests.sh
index 692a69ea..1243e033 100755
--- a/run-tests.sh
+++ b/run-tests.sh
@@ -15,7 +15,15 @@ if [[ ! -z "$TEST_SUITE" ]]; then
 	echo "Using TEST_SUITE=$TEST_SUITE"
 fi
 
-export PATH="$HOME/.local/bin:$PATH"
+HOME=$(readlink -f ~/)
+if [[ -z "$HOME" ]]; then
+    if [[ "$USER" == "root" ]]; then
+        HOME="/root"
+    fi
+fi
+if [[ ! -z "$HOME" ]]; then
+    export PATH="${HOME}/.local/bin:${PATH}"
+fi
 # pip uninstall -y pipenv
 echo "Path: $PATH"
 echo "Installing Pipenv…"
