commit 4a15f3b1c1332f6f6b4150feb7502ab94e48a6d7
Author: Tzu-ping Chung <uranusjr@gmail.com>
Date:   Fri Apr 13 00:04:03 2018 +0800

    Make sure --version succeeds
    
    And make sure what it returns makes sense. In case it isn't, return None.

diff --git a/pipenv/utils.py b/pipenv/utils.py
index 832105fa..e3eba96e 100644
--- a/pipenv/utils.py
+++ b/pipenv/utils.py
@@ -220,7 +220,11 @@ def python_version(path_to_python):
         c = delegator.run([path_to_python, '--version'], block=False)
     except Exception:
         return None
+    if c.return_code != 0:
+        return None
     version = parse_python_version(c.out.strip() or c.err.strip())
+    if not version:
+        return None
     return u'{major}.{minor}.{micro}'.format(**version)
 
 
