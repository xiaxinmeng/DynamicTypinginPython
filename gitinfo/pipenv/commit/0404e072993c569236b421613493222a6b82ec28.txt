commit 0404e072993c569236b421613493222a6b82ec28
Author: Kenneth Reitz <me@kennethreitz.org>
Date:   Wed Jan 25 16:05:09 2017 -0500

    pretty toml output for case checking

diff --git a/pipenv/cli.py b/pipenv/cli.py
index 2bd1260c..a45232bf 100644
--- a/pipenv/cli.py
+++ b/pipenv/cli.py
@@ -105,8 +105,9 @@ def ensure_proper_casing():
             del p['packages'][dep]
             p['packages'][new_casing] = old_value
 
+    # casing for [dev-packages]
     if 'dev-packages' in p:
-        # casing for [dev-packages]
+        # Replace each package with proper casing.
         for dep in p['dev-packages']:
 
             # Get new casing for package name.
diff --git a/pipenv/project.py b/pipenv/project.py
index 2cedc4c6..73f8270c 100644
--- a/pipenv/project.py
+++ b/pipenv/project.py
@@ -82,8 +82,9 @@ class Project(object):
             f.write(toml.dumps(data))
 
     def write(self, data):
+        # format TOML data.
         with open(self.pipfile_location, 'w') as f:
-            f.write(toml.dumps(data))
+            f.write(format_toml(toml.dumps(data)))
 
     @property
     def source(self):
