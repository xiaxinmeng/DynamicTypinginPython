commit 0120b4c1d20472256493a835b4b4a80826eb3746
Author: Erin O'Connell <erinocon5@gmail.com>
Date:   Sat Jun 10 16:41:41 2017 -0600

    accidently rewrote history and got rid of this on-liner

diff --git a/pipenv/cli.py b/pipenv/cli.py
index 662fa75d..101e74df 100644
--- a/pipenv/cli.py
+++ b/pipenv/cli.py
@@ -392,7 +392,7 @@ def do_create_virtualenv(three=None, python=None):
 
     # Actually create the virtualenv.
     with spinner():
-        c = delegator.run(cmd, block=False, binary=False, timeout=PIPENV_TIMEOUT)
+        c = delegator.run(cmd, block=False, timeout=PIPENV_TIMEOUT)
     click.echo(crayons.blue(c.out), err=True)
 
     # Say where the virtualenv is.
diff --git a/pipenv/environments.py b/pipenv/environments.py
index 2462be7f..ec008082 100644
--- a/pipenv/environments.py
+++ b/pipenv/environments.py
@@ -36,7 +36,5 @@ if PIPENV_VENV_IN_PROJECT:
 if os.name == 'nt':
     PIPENV_NOSPIN = True
 
-_pipenv_timout_raw = os.environ.get('PIPENV_TIMEOUT')
-
 # Tells pipenv how long to wait for virtualenvs to be created in seconds
-PIPENV_TIMEOUT = int(_pipenv_timout_raw) if _pipenv_timout_raw is not None else 120
+PIPENV_TIMEOUT = int(os.environ.get('PIPENV_TIMEOUT', 120))
