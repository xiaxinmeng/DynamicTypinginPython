commit 3992510c66e1c5db00479c48fe1077542b3a8b68
Author: Kenneth Reitz <me@kennethreitz.org>
Date:   Fri Jan 27 13:28:26 2017 -0500

    note on shell completions
    
    #125

diff --git a/README.rst b/README.rst
index c22281da..2f73b8de 100644
--- a/README.rst
+++ b/README.rst
@@ -58,6 +58,15 @@ Other Commands
 - ``run`` will run a given command from the virtualenv, with any arguments forwarded (e.g. ``$ pipenv run python``).
 - ``check`` asserts that PEP 508 requirements are being met by the current environment.
 
+Shell Completion
+////////////////
+
+Set `_PIPENV_COMPLETE and then source the output of the program. For example, with fish, put this in your ```~/.config/fish/completions/pipenv.fish``::
+
+    eval (env _PIPENV_COMPLETE=source-fish pipenv)
+
+Magic shell completions are now enabled!
+
 Caveats
 ///////
 
