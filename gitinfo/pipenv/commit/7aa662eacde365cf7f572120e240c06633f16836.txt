commit 7aa662eacde365cf7f572120e240c06633f16836
Author: Kenneth Reitz <me@kennethreitz.org>
Date:   Fri Mar 2 14:17:46 2018 -0500

    fix markers
    
    Signed-off-by: Kenneth Reitz <me@kennethreitz.org>

diff --git a/pipenv/resolver.py b/pipenv/resolver.py
index d9784a08..91a785cc 100644
--- a/pipenv/resolver.py
+++ b/pipenv/resolver.py
@@ -32,12 +32,18 @@ from docopt import docopt
 def cleanup_sysargv(argv):
 
     new = []
+
+    try:
+        argv = argv[1].split('!!!')
+    except IndexError:
+        argv = []
+
     for arg in argv[:]:
         if arg.startswith('-e '):
             new.append(arg)
             del argv[argv.index(arg)]
 
-    return argv[1:], new
+    return argv[:], new
 
 def which(*args, **kwargs):
     return sys.executable
diff --git a/pipenv/utils.py b/pipenv/utils.py
index 7364c637..ee0a8dd2 100644
--- a/pipenv/utils.py
+++ b/pipenv/utils.py
@@ -342,9 +342,7 @@ def venv_resolve_deps(deps, which, project, pre=False, verbose=False, clear=Fals
     import json
 
     resolver = shellquote(resolver.__file__.rstrip('co'))
-    cmd = '{0} {1} {2} {3} {4}'.format(which('python'), resolver, " ".join([shellquote(dep) for dep in deps]), '--pre' if pre else '', '--verbose' if verbose else '')
-
-    # print(cmd)
+    cmd = '{0} {1} {2} {3} {4}'.format(which('python'), resolver, "!!!".join(deps), '--pre' if pre else '', '--verbose' if verbose else '')
 
     c = delegator.run(cmd, block=True)
 
