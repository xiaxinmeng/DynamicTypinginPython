commit f0b527798c903346d9ea0eafcbfbd603b749ae1d
Author: Jonathon Belotti <jonathon@canva.com>
Date:   Tue Apr 9 22:58:09 2019 +1000

    remove always-True if-statement, and make function docstring accurate

diff --git a/pipenv/utils.py b/pipenv/utils.py
index 22c8653b..38e8e2e9 100644
--- a/pipenv/utils.py
+++ b/pipenv/utils.py
@@ -1374,15 +1374,14 @@ def walk_up(bottom):
 
 
 def find_requirements(max_depth=3):
-    """Returns the path of a Pipfile in parent directories."""
+    """Returns the path of a requirements.txt file in parent directories."""
     i = 0
     for c, d, f in walk_up(os.getcwd()):
         i += 1
         if i < max_depth:
-            if "requirements.txt":
-                r = os.path.join(c, "requirements.txt")
-                if os.path.isfile(r):
-                    return r
+            r = os.path.join(c, "requirements.txt")
+            if os.path.isfile(r):
+                return r
 
     raise RuntimeError("No requirements.txt found!")
 
