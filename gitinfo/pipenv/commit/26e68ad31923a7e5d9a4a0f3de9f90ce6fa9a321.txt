commit 26e68ad31923a7e5d9a4a0f3de9f90ce6fa9a321
Author: Kenneth Reitz <me@kennethreitz.org>
Date:   Wed Jan 25 13:48:21 2017 -0500

    fixes #93

diff --git a/pipenv/cli.py b/pipenv/cli.py
index 2ee787ed..f8c457ff 100644
--- a/pipenv/cli.py
+++ b/pipenv/cli.py
@@ -225,8 +225,8 @@ def parse_download_fname(fname):
     # TODO: Some versions might actually have dashes, will need to figure that out.
     # Will likely have to check of '-' comes at beginning or end of version.
     if '-' in version:
-        name = '{0}-{1}'.format(name, version.split('-')[0])
-        version = version.split('-')[1]
+        name = '-'.join([name] + version.split('-')[:-1])
+        version = version.split('-')[-1]
 
     return name, version
 
diff --git a/pipenv/utils.py b/pipenv/utils.py
index 657d8247..390275f6 100644
--- a/pipenv/utils.py
+++ b/pipenv/utils.py
@@ -15,6 +15,7 @@ def format_toml(data):
     return '\n'.join(data)
 
 
+
 def multi_split(s, split):
     """Splits on multiple given separators."""
     for r in split:
@@ -74,7 +75,6 @@ def convert_deps_to_pip(deps, r=True):
             extra = ''
 
         if 'hash' in deps[dep]:
-            # TODO: figure out why this doesn't work.
             extra = ' --hash={0}'.format(deps[dep]['hash'])
             # extra = ''
 
