commit 857a7d5a8e3dbb98703560abc7b35afbaa501bac
Author: vilhalmer <vil@vil.lv>
Date:   Mon Jan 30 19:47:02 2017 -0500

    Remove unnecessary conditional

diff --git a/pipenv/utils.py b/pipenv/utils.py
index 4a9f9585..c715b3bd 100644
--- a/pipenv/utils.py
+++ b/pipenv/utils.py
@@ -90,19 +90,18 @@ def convert_deps_to_pip(deps, r=True):
         if vcs:
             extra = '{0}+{1}'.format(vcs, deps[dep][vcs])
 
-            if extra:
-                # Support for @refs.
-                if 'ref' in deps[dep]:
-                    extra += '@{0}'.format(deps[dep]['ref'])
-
-                extra += '#egg={0}'.format(dep)
-
-                # Support for editable.
-                if 'editable' in deps[dep]:
-                    # Support for --egg.
-                    dep = '-e '
-                else:
-                    dep = ''
+            # Support for @refs.
+            if 'ref' in deps[dep]:
+                extra += '@{0}'.format(deps[dep]['ref'])
+
+            extra += '#egg={0}'.format(dep)
+
+            # Support for editable.
+            if 'editable' in deps[dep]:
+                # Support for --egg.
+                dep = '-e '
+            else:
+                dep = ''
 
         dependencies.append('{0}{1}{2}'.format(dep, version, extra))
 
