commit d87064f7dd988dfb0ab875a9a273a42054d8cc63
Author: Kenneth Reitz <me@kennethreitz.org>
Date:   Mon Mar 5 12:39:28 2018 -0500

    windows
    
    Signed-off-by: Kenneth Reitz <me@kennethreitz.org>

diff --git a/run-tests.sh b/run-tests.sh
index fa4d52c6..7726d9cb 100644
--- a/run-tests.sh
+++ b/run-tests.sh
@@ -21,4 +21,10 @@ if [[ ! -z "$CI" ]]; then
 	pipenv install --dev
 fi
 
+if [[ ! -z "$WINDOWS" ]]; then
+	echo "Installing Pipenv…"
+	pip install -e . --upgrade --upgrade-strategy=only-if-needed
+	pipenv install --dev
+fi
+
 pipenv run time pytest -v -n auto tests -m "$TEST_SUITE" --tap-stream | tee results.tap
\ No newline at end of file
