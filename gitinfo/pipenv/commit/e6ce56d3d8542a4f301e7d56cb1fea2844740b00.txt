commit e6ce56d3d8542a4f301e7d56cb1fea2844740b00
Author: Hynek Schlawack <hs@ox.cx>
Date:   Wed Jan 17 09:49:37 2018 +0100

    Fix AttributeError on pipenv uninstall --verbose

diff --git a/pipenv/cli.py b/pipenv/cli.py
index c7ef7216..e7cc2aad 100644
--- a/pipenv/cli.py
+++ b/pipenv/cli.py
@@ -1998,14 +1998,14 @@ def uninstall(
             crayons.green(package_name))
         )
 
-        cmd = '"{0}" uninstall {1} -y'
-        if verbose:
-            click.echo('$ {0}').format(cmd)
-
-        c = delegator.run(cmd.format(
+        cmd = '"{0}" uninstall {1} -y'.format(
             which_pip(allow_global=system),
             package_name
-        ))
+        )
+        if verbose:
+            click.echo('$ {0}'.format(cmd))
+
+        c = delegator.run(cmd)
 
         click.echo(crayons.blue(c.out))
 
