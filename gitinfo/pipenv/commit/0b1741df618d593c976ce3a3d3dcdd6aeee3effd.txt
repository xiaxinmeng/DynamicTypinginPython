commit 0b1741df618d593c976ce3a3d3dcdd6aeee3effd
Author: Kenneth Reitz <me@kennethreitz.org>
Date:   Fri Sep 15 13:20:04 2017 -0400

    fix for #559
    
    Signed-off-by: Kenneth Reitz <me@kennethreitz.org>

diff --git a/pipenv/utils.py b/pipenv/utils.py
index 42ab8e47..903f4ec3 100644
--- a/pipenv/utils.py
+++ b/pipenv/utils.py
@@ -165,7 +165,6 @@ def convert_deps_from_pip(dep):
     dependency = {}
 
     req = [r for r in requirements.parse(dep)][0]
-
     # File installs.
     if req.uri and not req.vcs:
 
@@ -206,7 +205,7 @@ def convert_deps_from_pip(dep):
         specs = None
         # Comparison operators: e.g. Django>1.10
         if req.specs:
-            r = multi_split(dep, '=<>')
+            r = multi_split(dep, '!=<>')
             specs = dep[len(r[0]):]
             dependency[req.name] = specs
 
