commit 9b5debf4964b1af3621d9202ffc0d928a86cf831
Author: Kenneth Reitz <kreitz@Kenneths-MacBook-Air.local>
Date:   Wed Feb 1 02:01:16 2017 -0500

    check —two

diff --git a/pipenv/cli.py b/pipenv/cli.py
index acabde33..6c81a532 100644
--- a/pipenv/cli.py
+++ b/pipenv/cli.py
@@ -893,7 +893,11 @@ def run(command, args, three=None, python=False):
 
 
 @click.command(help="Checks PEP 508 markers provided in Pipfile.")
-def check():
+@click.option('--three/--two', is_flag=True, default=None, help="Use Python 3/2 when creating virtualenv.")
+@click.option('--python', default=False, nargs=1, help="Specify which version of Python virtualenv should use.")
+def check(three=None, python=False):
+    # Ensure that virtualenv is available.
+    ensure_project(three=three, python=python, validate=False)
 
     click.echo(crayons.yellow('Checking PEP 508 requirements...'))
 
