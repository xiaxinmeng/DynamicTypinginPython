commit 99b0a75b5907f2a48d7ca7ec7fe505f184f30264
Author: Dan Ryan <dan@danryan.co>
Date:   Sat Jun 30 21:53:08 2018 -0400

    Add missed import
    
    Signed-off-by: Dan Ryan <dan@danryan.co>

diff --git a/pipenv/core.py b/pipenv/core.py
index fbc2ae54..a7cc851d 100644
--- a/pipenv/core.py
+++ b/pipenv/core.py
@@ -1001,7 +1001,6 @@ def do_lock(
     pypi_mirror=None,
 ):
     """Executes the freeze functionality."""
-    from .vendor.requirementslib import Requirement
     from .utils import get_vcs_deps
     cached_lockfile = {}
     if not pre:
@@ -1164,6 +1163,7 @@ def do_lock(
 
 def do_purge(bare=False, downloads=False, allow_global=False, verbose=False):
     """Executes the purge functionality."""
+    from .vendor.requirementslib.models.requirements import Requirement
     if downloads:
         if not bare:
             click.echo(
