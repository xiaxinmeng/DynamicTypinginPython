commit ae389f06e71e9e48186525123f4816087f052b38
Author: Dan Ryan <dan@danryan.co>
Date:   Mon Nov 19 00:02:20 2018 -0500

    Update vistir
    
    Signed-off-by: Dan Ryan <dan@danryan.co>

diff --git a/pipenv/vendor/vistir/contextmanagers.py b/pipenv/vendor/vistir/contextmanagers.py
index 32fad7ca..77fbb9df 100644
--- a/pipenv/vendor/vistir/contextmanagers.py
+++ b/pipenv/vendor/vistir/contextmanagers.py
@@ -137,10 +137,8 @@ def spinner(spinner_name=None, start_text=None, handler_map=None, nospin=False,
     use_yaspin = (has_yaspin is False) or (nospin is True)
     if has_yaspin is None or has_yaspin is True and not nospin:
         use_yaspin = True
-    if not start_text and use_yaspin is True:
+    if start_text is None and use_yaspin is True:
         start_text = "Running..."
-    else:
-        start_text = ""
     with create_spinner(
         spinner_name=spinner_name,
         text=start_text,
diff --git a/pipenv/vendor/vistir/misc.py b/pipenv/vendor/vistir/misc.py
index 74108d6f..110766b4 100644
--- a/pipenv/vendor/vistir/misc.py
+++ b/pipenv/vendor/vistir/misc.py
@@ -138,6 +138,7 @@ def _spawn_subprocess(script, env=None, block=True, cwd=None, combine_stderr=Tru
     return subprocess.Popen(script.cmdify(), **options)
 
 
+
 def _create_subprocess(
     cmd,
     env=None,
@@ -152,7 +153,7 @@ def _create_subprocess(
     write_to_stdout=True
 ):
     if not env:
-        env = os.environ.copy()
+        env = {}
     try:
         c = _spawn_subprocess(cmd, env=env, block=block, cwd=cwd,
                               combine_stderr=combine_stderr)
@@ -308,10 +309,12 @@ def run(
             verbose=verbose,
             spinner=sp,
             combine_stderr=combine_stderr,
-            start_text=start_text
+            start_text=start_text,
+            write_to_stdout=True
         )
 
 
+
 def load_path(python):
     """Load the :mod:`sys.path` from the given python executable's environment as json
 
diff --git a/pipenv/vendor/vistir/spin.py b/pipenv/vendor/vistir/spin.py
index c6ae5884..5e975b6f 100644
--- a/pipenv/vendor/vistir/spin.py
+++ b/pipenv/vendor/vistir/spin.py
@@ -46,8 +46,6 @@ class DummySpinner(object):
         if self.text and self.text != "None":
             if self.write_to_stdout:
                 self.write(self.text)
-            else:
-                self.write_err(self.text)
         return self
 
     def __exit__(self, exc_type, exc_val, traceback):
