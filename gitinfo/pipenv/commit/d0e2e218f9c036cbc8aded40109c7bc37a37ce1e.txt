commit d0e2e218f9c036cbc8aded40109c7bc37a37ce1e
Author: Kenneth Reitz <me@kennethreitz.org>
Date:   Sun Mar 4 16:31:07 2018 -0500

    see if that works
    
    Signed-off-by: Kenneth Reitz <me@kennethreitz.org>

diff --git a/.travis.yml b/.travis.yml
index bb399840..3506facc 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -1,4 +1,5 @@
 sudo: required
+dist: trusty
 cache: pip
 language: python
 python:
diff --git a/run-tests.sh b/run-tests.sh
index 89757517..fb374372 100644
--- a/run-tests.sh
+++ b/run-tests.sh
@@ -3,8 +3,15 @@
 PYPI_VENDOR_DIR="$(pwd)/tests/pypi/"
 export PYPI_VENDOR_DIR
 
+
 if [[ ! "$TEST_SUITE" ]]; then
 	TEST_SUITE = ""
 fi
 
+RAM_DISK="/media/ramdisk"
+export RAM_DISK
+
+sudo mkdir -p "$RAM_DISK"
+sudo mount -t tmpfs -o size=2048M tmpfs "$RAM_DISK"
+
 pytest -n 8 tests -m "$TEST_SUITE"
\ No newline at end of file
