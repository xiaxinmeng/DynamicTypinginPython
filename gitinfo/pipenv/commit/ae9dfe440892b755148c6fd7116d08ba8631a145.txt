commit ae9dfe440892b755148c6fd7116d08ba8631a145
Author: Kenneth Reitz <me@kennethreitz.org>
Date:   Tue Sep 12 18:20:27 2017 -0400

    further cleanups of system
    
    Signed-off-by: Kenneth Reitz <me@kennethreitz.org>

diff --git a/pipenv/cli.py b/pipenv/cli.py
index fbcfe193..6c564e1a 100644
--- a/pipenv/cli.py
+++ b/pipenv/cli.py
@@ -1343,12 +1343,13 @@ def run(command, args, three=None, python=False, system=True):
     ensure_project(three=three, python=python, validate=False)
 
     _which = 'which' if not os.name == 'nt' else 'where'
-    # Make absolute paths --system.
-    if os.path.isabs(command):
-        system = True
 
     command_path = which(command) if not system else command
 
+    # Make absolute paths --system.
+    if os.path.isabs(command_path):
+        system = True
+
     if system:
         # Activate virtualenv under the current interpreter's environment
         try:
