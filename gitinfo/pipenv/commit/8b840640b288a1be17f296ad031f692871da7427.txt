commit 8b840640b288a1be17f296ad031f692871da7427
Author: Chris Beaven <smileychris@gmail.com>
Date:   Wed Apr 25 16:32:58 2018 +1200

    Speed up lock by removing redundant freeze

diff --git a/pipenv/core.py b/pipenv/core.py
index 89108d56..fed628a1 100644
--- a/pipenv/core.py
+++ b/pipenv/core.py
@@ -1128,8 +1128,7 @@ def do_lock(
     # Add refs for VCS installs.
     # TODO: be smarter about this.
     vcs_deps = convert_deps_to_pip(project.vcs_packages, project, r=False)
-    pip_freeze = delegator.run('{0} freeze'.format(escape_grouped_arguments(which_pip(allow_global=system)))).out
-    for dep in vcs_deps:
+    if vcs_deps:
         for line in pip_freeze.strip().split('\n'):
             # if the line doesn't match a vcs dependency in the Pipfile,
             # ignore it
