commit c12204811a5b37e38ba9b81e75f3ae4504d01568
Author: Kenneth Reitz <me@kennethreitz.org>
Date:   Sun Sep 24 10:46:45 2017 -0400

    catch exceptions in pipreqs
    
    Signed-off-by: Kenneth Reitz <me@kennethreitz.org>

diff --git a/pipenv/cli.py b/pipenv/cli.py
index 9ff6242e..6eb9d702 100644
--- a/pipenv/cli.py
+++ b/pipenv/cli.py
@@ -275,11 +275,13 @@ def ensure_environment():
 
 def import_from_code(path='.'):
     rs = []
-    for r in pipreqs.get_all_imports(path):
-        if r not in BAD_PACKAGES:
-            rs.append(r)
-
-    return [proper_case(r) for r in rs]
+    try:
+        for r in pipreqs.get_all_imports(path):
+            if r not in BAD_PACKAGES:
+                rs.append(r)
+        return [proper_case(r) for r in rs]
+    except Exception:
+        return []
 
 
 def ensure_pipfile(validate=True):
