commit 73810b7466164fef8b22798242b03e7bc50c6d31
Author: Erin O'Connell <erinocon5@gmail.com>
Date:   Thu Sep 14 02:46:20 2017 -0600

    why is this erroring on travis but not my machine?

diff --git a/pipenv/utils.py b/pipenv/utils.py
index cf7db81a..cddf9876 100644
--- a/pipenv/utils.py
+++ b/pipenv/utils.py
@@ -46,6 +46,8 @@ def resolve_deps(deps, sources=None, verbose=False, python=False):
     using pip-tools -- and their hashes, using the warehouse API / pip.
     """
 
+    import pip
+
     # If a version of python is required...
     original_sys_info = sys.version_info
     if python:
@@ -56,8 +58,7 @@ def resolve_deps(deps, sources=None, verbose=False, python=False):
         # Hack sys.version_info to contain our information instead...
         sys.version_info = sys.version_info(int(python[0]), int(python[1]), int(python[2]), 'final', 0)
 
-    import pip
-
+    
     class PipCommand(pip.basecommand.Command):
         """Needed for pip-tools."""
         name = 'PipCommand'
