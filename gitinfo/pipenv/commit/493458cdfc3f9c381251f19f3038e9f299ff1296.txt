commit 493458cdfc3f9c381251f19f3038e9f299ff1296
Author: Felipe Arruda Pontes <felipe.arruda.pontes@gmail.com>
Date:   Sun Feb 19 14:31:13 2017 -0300

    breaking code for delegator.run and the cmd string into 2 different lines.

diff --git a/pipenv/cli.py b/pipenv/cli.py
index 7b3f8fc9..e51f4deb 100644
--- a/pipenv/cli.py
+++ b/pipenv/cli.py
@@ -566,14 +566,13 @@ def pip_install(package_name=None, r=None, allow_global=False):
 
 def pip_download(package_name):
     for source in project.sources:
-        c = delegator.run(
-            '{0} download "{1}"  -i {2} -d {3}'.format(
-                which_pip(),
-                package_name,
-                source['url'],
-                project.download_location
-            )
+        cmd = '{0} download "{1}"  -i {2} -d {3}'.format(
+            which_pip(),
+            package_name,
+            source['url'],
+            project.download_location
         )
+        c = delegator.run(cmd)
         if c.return_code == 0:
             break
 
