commit b44082115314113668ee700f2598324309a92916
Author: Rod Xavier Bondoc <rod.xavier.bondoc@gmail.com>
Date:   Wed Jul 18 16:24:16 2018 +1000

    Pass PIPENV_DOTENV_LOCATION directly to load_dotenv if present

diff --git a/pipenv/core.py b/pipenv/core.py
index a6372ad2..dc4d2acc 100644
--- a/pipenv/core.py
+++ b/pipenv/core.py
@@ -150,8 +150,8 @@ def load_dot_env():
     if not PIPENV_DONT_LOAD_ENV:
         # If the project doesn't exist yet, check current directory for a .env file
         project_directory = project.project_directory or "."
-        denv = dotenv.find_dotenv(
-            PIPENV_DOTENV_LOCATION or os.sep.join([project_directory, ".env"])
+        denv = PIPENV_DOTENV_LOCATION or dotenv.find_dotenv(
+            os.sep.join([project_directory, ".env"])
         )
         if os.path.isfile(denv):
             click.echo(
