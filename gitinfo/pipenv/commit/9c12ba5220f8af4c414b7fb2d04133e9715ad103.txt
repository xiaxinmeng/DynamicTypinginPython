commit 9c12ba5220f8af4c414b7fb2d04133e9715ad103
Author: Kenneth Reitz <me@kennethreitz.org>
Date:   Fri Sep 22 18:20:30 2017 -0400

    or if [ is in packages
    
    Signed-off-by: Kenneth Reitz <me@kennethreitz.org>

diff --git a/pipenv/utils.py b/pipenv/utils.py
index 10dd8149..5244acbf 100644
--- a/pipenv/utils.py
+++ b/pipenv/utils.py
@@ -270,7 +270,7 @@ packages = [
 
 def suggest_package(package):
     """Suggests a package name, given a package name."""
-    if '-' in package:
+    if ('-' in package) or ('[' in package):
         THRESHOLD = 90
     else:
         THRESHOLD = 86
@@ -280,6 +280,9 @@ def suggest_package(package):
         return package
 
     result = fuzzywuzzy.process.extractOne(package, packages)
+
+    print(result)
+
     if result[1] > THRESHOLD:
         return result[0]
 
