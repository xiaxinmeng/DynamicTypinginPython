commit 94af245f6d03c01c6c5e7467e6d0b3924326ffc8
Author: Dan Ryan <dan@danryan.co>
Date:   Thu Mar 29 20:08:02 2018 -0400

    Always use posix=False to avoid dropping quotes
    
    Signed-off-by: Dan Ryan <dan@danryan.co>

diff --git a/pipenv/project.py b/pipenv/project.py
index f349298c..8c7c08d4 100644
--- a/pipenv/project.py
+++ b/pipenv/project.py
@@ -370,10 +370,9 @@ class Project(object):
     @property
     def scripts(self):
         scripts = self.parsed_pipfile.get('scripts', {})
-        posix = os.name == 'posix'
         _scripts = {}
         for (k, v) in scripts.items():
-            _scripts[k] = shlex.split(str(v), posix=posix)
+            _scripts[k] = shlex.split(str(v), posix=False)
         return _scripts
 
     def update_settings(self, d):
