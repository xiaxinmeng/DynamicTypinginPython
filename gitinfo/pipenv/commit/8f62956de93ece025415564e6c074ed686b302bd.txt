commit 8f62956de93ece025415564e6c074ed686b302bd
Author: Erin O'Connell <erinocon5@gmail.com>
Date:   Sat Sep 2 13:09:13 2017 -0600

    fingers crossed x2

diff --git a/.travis.yml b/.travis.yml
index f2d966d0..a390c044 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -13,11 +13,12 @@ install:
   - "pip install -e . --upgrade"
   - "pipenv install --dev --skip-lock"
 
+script:
+  - "script: "pipenv run pytest tests"
+
 # command to run tests
 jobs:
   include:
-    - stage: "Pytest Suite ✨"
-      script: "pipenv run pytest tests"
     - stage: "Style Tests ✨✨"
       python: "3.6"
       script: "pipenv run flake8"
