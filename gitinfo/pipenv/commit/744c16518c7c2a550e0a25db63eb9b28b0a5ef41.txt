commit 744c16518c7c2a550e0a25db63eb9b28b0a5ef41
Author: Nate Prewitt <Nate.Prewitt@gmail.com>
Date:   Thu Jan 26 18:33:47 2017 -0700

    remove extraneous spaces from parsed source response

diff --git a/HISTORY.txt b/HISTORY.txt
index b4f906e7..03d9454b 100644
--- a/HISTORY.txt
+++ b/HISTORY.txt
@@ -4,6 +4,7 @@
  - Improvements to Pipfile re-ordering when writing. 
  - Fix proper casing mechanism.
  - Prevent invalid shebangs with Homebrew Python.
+ - Fix parsing issues with https://pypi.org/simple
 3.2.3:
  - $ pip uninstall --dev
  - Minor refactoring.
@@ -76,4 +77,4 @@
 0.2.0:
  - Added support for $ pipenv --three / --two, for initializing virtualenvs with a specific Python version.
  - Added support for VCS-backed installs, including editables.
- - TODO: Still need to support non-git-backed VCS installations in Pipfiles. 
\ No newline at end of file
+ - TODO: Still need to support non-git-backed VCS installations in Pipfiles.
diff --git a/pipenv/cli.py b/pipenv/cli.py
index d215de18..f27f20ce 100644
--- a/pipenv/cli.py
+++ b/pipenv/cli.py
@@ -481,6 +481,8 @@ def proper_case(package_name):
 
     class SimpleHTMLParser(HTMLParser):
         def handle_data(self, data):
+            # Remove extra blank data from https://pypi.org/simple
+            data = data.strip()
             if len(data) > 2:
                 collected.append(data)
 
