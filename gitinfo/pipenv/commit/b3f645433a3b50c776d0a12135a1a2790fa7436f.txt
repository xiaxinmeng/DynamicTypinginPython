commit b3f645433a3b50c776d0a12135a1a2790fa7436f
Author: Kyle Altendorf <kyle.altendorf@epcpower.com>
Date:   Fri May 4 16:46:44 2018 -0400

    Fix grouping of extras requirements in notpip

diff --git a/pipenv/patched/notpip/index.py b/pipenv/patched/notpip/index.py
index bf1cba9c..07881b08 100644
--- a/pipenv/patched/notpip/index.py
+++ b/pipenv/patched/notpip/index.py
@@ -210,20 +210,16 @@ class PackageFinder(object):
         requires = []
         extras = {}
 
-        current_section = None
+        current_list = requires
 
         for link in links:
             if not link:
-                current_section = None
-
-            if not current_section:
-                if not (link.startswith('[')):
-                    requires.append(link)
-                else:
-                    current_section = link[1:-1]
-                    extras[current_section] = []
+                current_list = requires
+            if link.startswith('['):
+                current_list = []
+                extras[link[1:-1]] = current_list
             else:
-                extras[current_section].append(link)
+                current_list.append(link)
 
         return extras
 
