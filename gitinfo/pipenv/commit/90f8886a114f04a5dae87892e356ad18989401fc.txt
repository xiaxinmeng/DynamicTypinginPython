commit 90f8886a114f04a5dae87892e356ad18989401fc
Author: Nate Prewitt <Nate.Prewitt@gmail.com>
Date:   Wed Apr 26 07:18:06 2017 -0600

    get appveyor running correctly

diff --git a/appveyor.yml b/appveyor.yml
index a8e9cd4e..60dd4a8e 100644
--- a/appveyor.yml
+++ b/appveyor.yml
@@ -4,6 +4,10 @@
 build: off
 
 environment:
+
+  # Create expected SHELL variable for pipenv.
+  SHELL: "windows"
+
   matrix:
     - PYTHON: "C:\\Python266-x64"
       PYTHON_VERSION: "2.6.6"
@@ -52,7 +56,9 @@ install:
   # Upgrade to the latest version of pip to avoid it displaying warnings
   # about it being out of date.
   - "pip install --disable-pip-version-check --user --upgrade pip"
-  - "C:\\MinGW\\bin\\mingw32-make"
+  - "python setup.py develop"
+  - "pipenv lock"
+  - "pipenv install --dev"
 
 test_script:
-  - "C:\\MinGW\\bin\\mingw32-make run-tests"
+  - "pipenv run pytest tests"
