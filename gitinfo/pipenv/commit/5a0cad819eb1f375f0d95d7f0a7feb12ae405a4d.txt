commit 5a0cad819eb1f375f0d95d7f0a7feb12ae405a4d
Author: Kenneth Reitz <me@kennethreitz.org>
Date:   Fri Mar 2 10:11:45 2018 -0500

    basics
    
    Signed-off-by: Kenneth Reitz <me@kennethreitz.org>

diff --git a/docs/basics.rst b/docs/basics.rst
index e93ed0bd..d580d516 100644
--- a/docs/basics.rst
+++ b/docs/basics.rst
@@ -133,29 +133,26 @@ Example Pipfile.lock
 ☤ Example Pipenv Workflow
 -------------------------
 
-Install from Pipfile::
+Clone / create project repository::
 
-    $ pipenv install
-
-Add a package::
+    …
+    $ cd myproject
 
-    $ pipenv install <module>
+Install from Pipfile, if there is one::
 
-Create a Pipfile.lock from the installed versions::
+    $ pipenv install
 
-    $ pipenv lock
+Or, add a package to your new project::
 
-Install from that Pipfile.lock::
+    $ pipenv install <package>
 
-    $ pipenv install
+This will create a ``Pipfile`` if one doesn't exist. If one does exist, it will automatically be edited with the new package your provided.
 
-Activate the Pipenv shell::
+Next, activate the Pipenv shell::
 
     $ pipenv shell
-
-Exit the Pipenv shell::
-
-    $ exit
+    $ python --version
+    …
 
 .. _initialization:
 
