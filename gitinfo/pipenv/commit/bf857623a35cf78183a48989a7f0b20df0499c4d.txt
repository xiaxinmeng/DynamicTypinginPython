commit bf857623a35cf78183a48989a7f0b20df0499c4d
Author: Kenneth Reitz <me@kennethreitz.org>
Date:   Fri Jan 20 13:07:56 2017 -0500

    working

diff --git a/Pipfile b/Pipfile
index 96f90d4f..22b994ff 100644
--- a/Pipfile
+++ b/Pipfile
@@ -10,3 +10,9 @@ click = "*"
 [develop]
 requests = "*"
 "requests==2.12.5" = "*"
+
+[develop."Django>1.10"]
+Django = ""
+
+[develop.Django]
+Django = "*"
diff --git a/pipenv.py b/pipenv.py
index 89c79e3c..436df14f 100644
--- a/pipenv.py
+++ b/pipenv.py
@@ -9,6 +9,7 @@ import delegator
 import click
 import crayons
 import _pipfile as pipfile
+from parse import parse
 
 __version__ = '0.0.0'
 
@@ -52,16 +53,24 @@ def add_package_to_pipfile(package_name, dev=False):
         if key not in p:
             p[key] = {}
 
+        package = convert_deps_from_pip(package_name)
+
         # Add the package to the group.
         if package_name not in p[key]:
             # TODO: Support >1.0.1
-            p[key][package_name] = '*'
+            p[key][package_name] = package
 
     # Write Pipfile.
     data = format_toml(toml.dumps(p))
     with open(pipfile_path, 'w') as f:
         f.write(data)
 
+def multi_split(s, split):
+    for r in split:
+        s = s.replace(r, '|')
+
+    return [i for i in s.split('|') if len(i) > 0]
+
 
 def remove_package_from_pipfile(package_name, dev=False):
     pipfile_path = pipfile.Pipfile.find()
@@ -80,11 +89,18 @@ def remove_package_from_pipfile(package_name, dev=False):
         f.write(data)
 
 def convert_deps_from_pip(dep):
+    dependency = {}
 
-        # requests
-        # requests==2.12.5
+    # Comparison operators: e.g. Django>1.10
+    if '=' in dep or '<' in dep or '>' in dep:
+        r = multi_split(dep, '=<>')
+        dependency[r[0]] = dep[len(r[0]):]
 
-    return dependencies
+    # Bare dependencies: e.g. requests
+    else:
+        dependency[dep] = '*'
+
+    return dependency
 
 
 def convert_deps_to_pip(deps):
diff --git a/test_pipenv.py b/test_pipenv.py
index ddac7a2a..81c67540 100644
--- a/test_pipenv.py
+++ b/test_pipenv.py
@@ -35,8 +35,19 @@ def test_convert_deps_to_pip():
 def test_convert_from_pip():
 
     # requests
+    dep = 'requests'
+    dep = pipenv.convert_deps_from_pip(dep)
+    assert dep == {'requests': '*'}
+
+    # Django>1.10
+    dep = 'Django>1.10'
+    dep = pipenv.convert_deps_from_pip(dep)
+    assert dep == {'Django': '>1.10'}
 
     # requests[socks]
+    dep = 'requests[socks]'
+    dep = pipenv.convert_deps_from_pip(dep)
+    assert dep == {'requests': {'extras': ['socks']}}
 
     # Django>1.10
     # deps = pipenv.convert_deps_to_pip(deps)
