commit ba76b0e3e8639910cf960614034584d25a424c76
Author: Kenneth Reitz <me@kennethreitz.org>
Date:   Mon Jan 23 14:52:34 2017 -0500

    activate the virtualenv always

diff --git a/pipenv/cli.py b/pipenv/cli.py
index d964dd71..0c2a71b4 100644
--- a/pipenv/cli.py
+++ b/pipenv/cli.py
@@ -444,9 +444,9 @@ def shell():
                 terminal_dimensions.columns
             )
         )
-    # Skip this step for bash.
-    if 'bash' not in shell:
-        c.send(activate_virtualenv() + '\n')
+
+    # Activate the virtualenv.
+    c.send(activate_virtualenv() + '\n')
 
     # Handler for terminal resizing events
     # Must be defined here to have the shell process in its context, since we
