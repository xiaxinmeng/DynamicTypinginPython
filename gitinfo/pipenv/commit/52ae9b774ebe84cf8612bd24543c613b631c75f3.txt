commit 52ae9b774ebe84cf8612bd24543c613b631c75f3
Author: Kenneth Reitz <me@kennethreitz.org>
Date:   Mon Sep 11 11:03:05 2017 -0400

    remove PIPENV_PIP_INSTALL_FLAGS
    
    Signed-off-by: Kenneth Reitz <me@kennethreitz.org>

diff --git a/HISTORY.txt b/HISTORY.txt
index 0c5668e8..3d37ae9d 100644
--- a/HISTORY.txt
+++ b/HISTORY.txt
@@ -2,7 +2,6 @@
  - Bug fixes and improvements.
  - Add refs to lockfile for VCS dependencies.
  - Don't re-capitalize URLs.
- - PIPENV_PIP_INSTALL_FLAGS, for special use cases.
 6.2.2:
  - Bug fix.
  - Support for passwords in git URLs.
diff --git a/pipenv/cli.py b/pipenv/cli.py
index 92dbcd5a..abfa6900 100644
--- a/pipenv/cli.py
+++ b/pipenv/cli.py
@@ -33,7 +33,7 @@ from . import pep508checker, progress
 from .environments import (
     PIPENV_COLORBLIND, PIPENV_NOSPIN, PIPENV_SHELL_COMPAT,
     PIPENV_VENV_IN_PROJECT, PIPENV_USE_SYSTEM, PIPENV_TIMEOUT,
-    PIPENV_SKIP_VALIDATION, PIPENV_PIP_INSTALL_FLAGS
+    PIPENV_SKIP_VALIDATION
 )
 
 # Backport required for earlier versions of Python.
@@ -791,12 +791,11 @@ def pip_install(
 
         no_deps = '--no-deps' if no_deps else ''
 
-        pip_command = '"{0}" install {3} {1} -i {2} --exists-action w {4}'.format(
+        pip_command = '"{0}" install {3} {1} -i {2} --exists-action w'.format(
             which_pip(allow_global=allow_global),
             install_reqs,
             source['url'],
-            no_deps,
-            PIPENV_PIP_INSTALL_FLAGS or ''
+            no_deps
         )
 
         if verbose:
