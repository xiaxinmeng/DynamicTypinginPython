commit 6cd63a55881c2c98022a738af56e3740921b6bdc
Author: Kenneth Reitz <me@kennethreitz.org>
Date:   Sun Mar 4 08:02:31 2018 -0500

    better
    
    Signed-off-by: Kenneth Reitz <me@kennethreitz.org>
    
    Signed-off-by: Kenneth Reitz <me@kennethreitz.org>

diff --git a/.travis.yml b/.travis.yml
index d705013f..a19271bc 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -7,14 +7,14 @@ python:
   - "3.5"
   - "3.6"
 env:
-  - TEST_SUITE='cli'
-  - TEST_SUITE='install'
-  - TEST_SUITE='run'
-  - TEST_SUITE='dotvenv check unused'
-  - TEST_SUITE='requirements'
-  - TEST_SUITE='complex'
-  - TEST_SUITE='project'
-  - TEST_SUITE='utils'
+  - TEST_SUITE='-m cli'
+  - TEST_SUITE='-m install'
+  - TEST_SUITE='-m run'
+  - TEST_SUITE='-m dotvenv -m check -m unused'
+  - TEST_SUITE='-m requirements'
+  - TEST_SUITE='-m complex'
+  - TEST_SUITE='-m project'
+  - TEST_SUITE='-m utils'
   # - "pypy" # too many cache issues
   # - "3.7-dev"  # no need, slows down builds
   # - "pypy3" # TODO: pkg_config issues
@@ -27,7 +27,7 @@ install:
 
 # command to run the dependencies
 script:
-  - "pipenv run pytest tests -m \"$TEST_SUITE\""
+  - "pipenv run pytest tests $TEST_SUITE"
 
 # command to run tests
 # jobs:
