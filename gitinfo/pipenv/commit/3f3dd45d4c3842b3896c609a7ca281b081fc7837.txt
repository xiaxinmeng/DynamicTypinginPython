commit 3f3dd45d4c3842b3896c609a7ca281b081fc7837
Author: Erin O'Connell <erinocon5@gmail.com>
Date:   Sat Sep 2 00:33:49 2017 -0600

    ugh

diff --git a/.travis.yml b/.travis.yml
index 4c71d690..0c20cd74 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -1,21 +1,38 @@
 language: python
-
-matrix:
-  include:
-    - python: "2.7"
-    - python: "3.4"
-    - python: "3.5"
-    - python: "3.6"
+# python:
+#   - 2.7
+#   - 3.4
+#   - 3.5
+#   - 3.6
+  # - "3.7-dev"
+  # - "pypy"
+  # - "pypy3" # TODO: pkg_config issues
 
 # command to install dependencies
-install:
-  - "pip install -e . --upgrade"
-  - "pipenv install --dev --skip-lock"
+# install:
+#   - "pip install -e . --upgrade"
+#   - "pipenv install --dev --skip-lock"
 
 # command to run tests
 jobs:
   include:
     - stage: Style Tests ✨
+      python:
+        - 2.7
+        - 3.4
+        - 3.5
+        - 3.6
+      install:
+        - "pip install -e . --upgrade"
+        - "pipenv install --dev --skip-lock"
       script: pipenv run flake8
     - stage: Main Test Suite ✨✨
-      script: pipenv run pytest tests
+      python:
+        - 2.7
+        - 3.4
+        - 3.5
+        - 3.6
+      install:
+        - "pip install -e . --upgrade"
+        - "pipenv install --dev --skip-lock"
+      script: pipenv run pytest tests
\ No newline at end of file
