commit b9ee2201d2c1261215bc818834acd1a3d17b7ec1
Author: Kenneth Reitz <me@kennethreitz.org>
Date:   Wed Sep 27 10:47:18 2017 -0400

    fix for #725
    
    Signed-off-by: Kenneth Reitz <me@kennethreitz.org>

diff --git a/pipenv/project.py b/pipenv/project.py
index 00ea33a6..a2be9e5b 100644
--- a/pipenv/project.py
+++ b/pipenv/project.py
@@ -36,8 +36,12 @@ class Project(object):
         self._pipfile_location = None
         self._requirements_location = None
 
-        # Hack to skip this during pipenv run.
-        if 'run' not in sys.argv:
+        # Hack to skip this during pipenv run, or -r.
+        if (
+            ('run' not in sys.argv) or
+            ('-r' not in sys.argv) or
+            ('--requirements' not in sys.argv)
+        ):
             try:
                 os.chdir(self.project_directory)
             except (TypeError, AttributeError):
