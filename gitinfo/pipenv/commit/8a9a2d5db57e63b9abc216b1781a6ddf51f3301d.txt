commit 8a9a2d5db57e63b9abc216b1781a6ddf51f3301d
Author: Kenneth Reitz <me@kennethreitz.org>
Date:   Sat Sep 30 07:42:23 2017 -0400

    bar on windows
    
    #767
    
    Signed-off-by: Kenneth Reitz <me@kennethreitz.org>

diff --git a/pipenv/progress.py b/pipenv/progress.py
index 2a11a23b..e50abaf2 100644
--- a/pipenv/progress.py
+++ b/pipenv/progress.py
@@ -31,7 +31,10 @@ else:
 if (sys.version_info[0] >= 3) and (os.name != 'nt'):
     BAR_TEMPLATE = u'  %s%s%s %i/%i — {0}\r'.format(crayons.black('%s'))
 else:
-    BAR_TEMPLATE = '  %s%s%s %i/%i — %s\r'
+    if os.name == 'nt':
+        BAR_TEMPLATE = u'  %s%s%s %i/%i - %s\r'
+    else:
+        BAR_TEMPLATE = u'  %s%s%s %i/%i — %s\r'
 
 MILL_CHARS = ['|', '/', '-', '\\']
 
