commit f532b2eb864a4012179d024a1c5228cccb16af18
Author: Dan Ryan <dan@danryan.co>
Date:   Sat May 26 23:39:34 2018 -0400

    Use pipenv cache dir for caching wheels
    
    Signed-off-by: Dan Ryan <dan@danryan.co>

diff --git a/pipenv/core.py b/pipenv/core.py
index 75bc6276..0f5c7e4f 100644
--- a/pipenv/core.py
+++ b/pipenv/core.py
@@ -73,6 +73,7 @@ from .environments import (
     PIPENV_SHELL,
     PIPENV_PYTHON,
     PIPENV_VIRTUALENV,
+    PIPENV_CACHE_DIR,
 )
 
 # Backport required for earlier versions of Python.
@@ -1494,7 +1495,7 @@ def pip_install(
     )
     if verbose:
         click.echo('$ {0}'.format(pip_command), err=True)
-    c = delegator.run(pip_command, block=block)
+    c = delegator.run(pip_command, block=block, env={'PIP_CACHE_DIR': PIPENV_CACHE_DIR})
     return c
 
 
@@ -1506,7 +1507,7 @@ def pip_download(package_name):
             source['url'],
             project.download_location,
         )
-        c = delegator.run(cmd)
+        c = delegator.run(cmd, env={'PIP_CACHE_DIR': PIPENV_CACHE_DIR})
         if c.return_code == 0:
             break
 
diff --git a/pipenv/utils.py b/pipenv/utils.py
index 3237b24c..599d8a0c 100644
--- a/pipenv/utils.py
+++ b/pipenv/utils.py
@@ -264,6 +264,7 @@ def actually_resolve_reps(
         constraints_file = f.name
     if verbose:
         print('Using pip: {0}'.format(' '.join(pip_args)))
+    pip_args = pip_args.extend(['--cache-dir', PIPENV_CACHE_DIR])
     pip_options, _ = pip_command.parse_args(pip_args)
     session = pip_command._build_session(pip_options)
     pypi = PyPIRepository(
