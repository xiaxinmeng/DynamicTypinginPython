commit ba9615cb7ec71a87b05b5b89a5de8403b0b55dc1
Author: Max Krivich <maxkrivich@gmail.com>
Date:   Thu Jun 28 18:11:15 2018 +0300

    Add `python_requires` in setup.py
    This argument prevents accident install `pipenv` on not supported python
    versions.
    
    Syntax for this string you can find in PEP440.
    
    Note: this feature works with pip 9.0+.

diff --git a/setup.py b/setup.py
index 9a31430d..0e0d9b7e 100644
--- a/setup.py
+++ b/setup.py
@@ -122,6 +122,7 @@ setup(
         "pipenv.patched.notpip._vendor.distlib._backport": ["sysconfig.cfg"],
         "pipenv.patched.notpip._vendor.distlib": ["t32.exe", "t64.exe", "w32.exe", "w64.exe"],
     },
+    python_requires='>=2.7,!=3.0.*,!=3.1.*,!=3.2.*',
     install_requires=required,
     extras_require={},
     include_package_data=True,
