commit 2eb3d5b7ced8e367236f79fe135d1538ff7179fe
Author: Dan Ryan <dan.ryan@canonical.com>
Date:   Fri Apr 10 12:08:29 2020 -0400

    Fix missing normalization import
    
    Signed-off-by: Dan Ryan <dan.ryan@canonical.com>

diff --git a/pipenv/environment.py b/pipenv/environment.py
index 6b267810..65e7188d 100644
--- a/pipenv/environment.py
+++ b/pipenv/environment.py
@@ -476,7 +476,7 @@ class Environment(object):
         prefixes = [
             vistir.compat.Path(prefix)
             for prefix in self.base_paths["libdirs"].split(os.pathsep)
-            if _normalized(prefix).startswith(_normalized(self.prefix.as_posix()))
+            if vistir.path.is_in_path(prefix, self.prefix.as_posix())
         ]
         for loc in prefixes:
             if not loc.exists():
