commit 0d3aa61c0e9aa76d6f4751ee62ce8c589fd4d041
Author: Kenneth Reitz <me@kennethreitz.org>
Date:   Fri Jan 27 23:09:27 2017 -0500

    lock before installing dependencies
    
    Fixes #130

diff --git a/pipenv/cli.py b/pipenv/cli.py
index 7f048eeb..4e8b69c3 100644
--- a/pipenv/cli.py
+++ b/pipenv/cli.py
@@ -475,13 +475,13 @@ def do_init(dev=False, requirements=False, skip_virtualenv=False, allow_global=F
 
             do_lock()
 
-    do_install_dependencies(dev=dev, requirements=requirements, allow_global=allow_global)
-
     # Write out the lockfile if it doesn't exist.
     if not project.lockfile_exists:
         click.echo(crayons.yellow('Pipfile.lock not found, creating...'), err=True)
         do_lock()
 
+    do_install_dependencies(dev=dev, requirements=requirements, allow_global=allow_global)
+
     # Activate virtualenv instructions.
     do_activate_virtualenv()
 
