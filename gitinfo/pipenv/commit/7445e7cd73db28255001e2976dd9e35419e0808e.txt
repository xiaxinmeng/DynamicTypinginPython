commit 7445e7cd73db28255001e2976dd9e35419e0808e
Author: Remi Rampin <remirampin@gmail.com>
Date:   Thu Mar 29 11:20:25 2018 -0400

    Mention PIPENV_IGNORE_VIRTUALENVS in help message
    
    When running pipenv in a virtualenv, it will use that virtualenv instead
    of creating its own. A message will be shown when this happens, but it
    didn't mention the way to avoid this behavior, which is setting the
    PIPENV_IGNORE_VIRTUALENVS environment variable.

diff --git a/pipenv/core.py b/pipenv/core.py
index b083ed9d..c8ca0782 100644
--- a/pipenv/core.py
+++ b/pipenv/core.py
@@ -1625,7 +1625,9 @@ def warn_in_virtualenv():
             click.echo(
                 '{0}: Pipenv found itself running within a virtual environment, '
                 'so it will automatically use that environment, instead of '
-                'creating its own for any project.'.format(
+                'creating its own for any project. You can set '
+                'PIPENV_IGNORE_VIRTUALENVS=1 to force pipenv to ignore that '
+                'environment and create its own instead.'.format(
                     crayons.green('Courtesy Notice')
                 ),
                 err=True,
