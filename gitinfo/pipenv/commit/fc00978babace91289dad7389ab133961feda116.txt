commit fc00978babace91289dad7389ab133961feda116
Author: Kenneth Reitz <me@kennethreitz.org>
Date:   Mon Mar 5 16:30:37 2018 -0500

    macos
    
    Signed-off-by: Kenneth Reitz <me@kennethreitz.org>

diff --git a/run-tests.sh b/run-tests.sh
index b8e39610..c34164ce 100755
--- a/run-tests.sh
+++ b/run-tests.sh
@@ -20,7 +20,7 @@ if [[ ! -z "$CI" ]]; then
 	pip install -e . --upgrade --upgrade-strategy=only-if-needed
 	pipenv install --deploy --system --dev
 else
-	echo "Using RAM disk (assuming OS X)…"
+	echo "Using RAM disk (assuming MacOS)…"
 	diskutil erasevolume HFS+ 'RAMDisk' $(hdiutil attach -nomount ram://8388608)
 	RAM_DISK="/Volumes/RAMDisk"
 	export RAM_DISK
