commit 0a15c9e5b729f87080e0805ebeedb3c3651ece5c
Author: Kenneth Reitz <me@kennethreitz.org>
Date:   Fri Sep 22 18:23:43 2017 -0400

    fix a nasty bug
    
    Signed-off-by: Kenneth Reitz <me@kennethreitz.org>

diff --git a/pipenv/cli.py b/pipenv/cli.py
index c0f95a5d..7b373a69 100644
--- a/pipenv/cli.py
+++ b/pipenv/cli.py
@@ -715,7 +715,7 @@ def do_install_dependencies(
 
     for dep, ignore_hash in deps_list_bar:
 
-        if len(procs) < PIPENV_MAX_SUBPROCESS or (not len(procs) == len(deps_list - 1)):
+        if len(procs) < PIPENV_MAX_SUBPROCESS:
             # Use a specific index, if specified.
             index = None
             if ' -i ' in dep:
