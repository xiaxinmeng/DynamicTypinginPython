commit 33455b22f7d45b4699815ecfb40c492048b5cb98
Author: Kenneth Reitz <me@kennethreitz.org>
Date:   Sun Mar 4 07:35:52 2018 -0500

    new travis.yml
    
    Signed-off-by: Kenneth Reitz <me@kennethreitz.org>

diff --git a/.travis.yml b/.travis.yml
index 8671fe20..b6d1a963 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -6,6 +6,9 @@ python:
   - "3.4"
   - "3.5"
   - "3.6"
+env:
+  - TEST_SUITE='cli'
+  - TEST_SUITE='not cli'
   # - "pypy" # too many cache issues
   # - "3.7-dev"  # no need, slows down builds
   # - "pypy3" # TODO: pkg_config issues
@@ -17,7 +20,7 @@ install:
 
 # command to run the dependencies
 script:
-  - "pipenv run pytest -n 8 tests"
+  - "pipenv run pytest -n 16 tests -m $TEST_SUITE"
 
 # command to run tests
 # jobs:
