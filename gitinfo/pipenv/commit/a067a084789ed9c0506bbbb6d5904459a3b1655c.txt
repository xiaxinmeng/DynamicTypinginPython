commit a067a084789ed9c0506bbbb6d5904459a3b1655c
Author: jxltom <jxltom@gmail.com>
Date:   Fri Nov 9 11:35:11 2018 +0800

    Pass requirements para to only in get_requirements

diff --git a/pipenv/core.py b/pipenv/core.py
index dabf065c..c77e14a1 100644
--- a/pipenv/core.py
+++ b/pipenv/core.py
@@ -721,7 +721,7 @@ def do_install_dependencies(
     # Allow pip to resolve dependencies when in skip-lock mode.
     no_deps = not skip_lock
     failed_deps_list = []
-    deps_list = list(lockfile.get_requirements(dev=dev, only=True))
+    deps_list = list(lockfile.get_requirements(dev=dev, only=requirements))
     if requirements:
         index_args = prepare_pip_source_args(project.sources)
         index_args = " ".join(index_args).replace(" -", "\n-")
